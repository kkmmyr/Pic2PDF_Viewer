[project]
name = "manga-ai-experiments"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.32",
    "ipython>=8.0,<9",
    "jedi>=0.18,<0.19",
    "Pillow>=11.0",
    "jupyter>=1.0",
]

[tool.uv]
package = false
