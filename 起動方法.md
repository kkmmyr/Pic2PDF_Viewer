# 起動方法

## モード一覧

| モード | 用途 | Backend | Frontend | 起動スクリプト |
|---|---|---|---|---|
| **開発** | コード修正・ホットリロード | 8766 | 5176 | `start.bat` |
| **リリース** | 普段使い・自動起動 | 8090（dist 統合） | — | `start_release.bat` |

---

## リリース起動（普段使い）

### 1. ビルド（フロント側を変更したときだけ）

```bat
build_release.bat
```

`frontend/dist/` を再生成します。

### 2. 起動

`start_release.bat` をダブルクリック。

- ブラウザが自動で `http://localhost:8090` を開きます。
- バックエンド 1 プロセス（uvicorn）で動作し、フロント・API・静的ファイルを同一オリジンで配信します。

### 3. ログイン時の自動起動（タスクスケジューラ）

タスクスケジューラに `Pic2PDF_Viewer` という名前で登録済み。
- ログオン時に自動起動
- 最小化されたコンソールで常駐
- クラッシュ時は 1 分後に最大 3 回まで自動再起動
- `start_release.bat` 内にも uvicorn 再起動ループあり（二重防御）

#### 登録方法（再登録用、PowerShell）

```powershell
$action = New-ScheduledTaskAction -Execute "powershell.exe" -Argument "-NoProfile -WindowStyle Hidden -Command Start-Process cmd -ArgumentList '/c D:\61.tool\Pic2PDF_Viewer\start_release.bat' -WindowStyle Minimized -WorkingDirectory 'D:\61.tool\Pic2PDF_Viewer'"
$trigger = New-ScheduledTaskTrigger -AtLogOn -User $env:USERNAME
$settings = New-ScheduledTaskSettingsSet -RestartCount 3 -RestartInterval (New-TimeSpan -Minutes 1) -StartWhenAvailable -ExecutionTimeLimit (New-TimeSpan -Seconds 0) -AllowStartIfOnBatteries -DontStopIfGoingOnBatteries -MultipleInstances IgnoreNew
$principal = New-ScheduledTaskPrincipal -UserId $env:USERNAME -LogonType Interactive -RunLevel Limited
Register-ScheduledTask -TaskName "Pic2PDF_Viewer" -Action $action -Trigger $trigger -Settings $settings -Principal $principal -Force
```

#### 解除

```powershell
Unregister-ScheduledTask -TaskName "Pic2PDF_Viewer" -Confirm:$false
```

または `taskschd.msc` を開いて「タスクスケジューラライブラリ」→ `Pic2PDF_Viewer` を削除。

---

## 開発起動（コード修正時）

`start.bat` をダブルクリックするとバックエンド・フロントエンドが Windows Terminal の別タブで同時に起動します。

- Backend: <http://localhost:8766>
- Frontend: <http://localhost:5176>

初回起動時は `uv` が依存パッケージを自動取得するため少し時間がかかります（2 回目以降は数秒）。

### Backend の再起動

Backend が起動中の状態で `start.bat` を再実行すると、**Backend のみ**を再起動します（Frontend タブはそのまま）。

- uvicorn プロセスを終了 → `backend/start_server.bat` の再起動ループが同タブ内で `uv sync` → uvicorn を自動再起動
- 新しいタブは開かず、古いタブも残らない
- 依存パッケージを追加した後などに使用する（通常のコード変更は `--reload` により自動リロードされる）

**Backend を完全に止めたい場合:** Backend タブで `Ctrl+C` → `Y`

### 個別起動

```bat
:: バックエンド
cd D:\61.tool\Pic2PDF_Viewer\backend
uv run uvicorn main:app --reload --port 8766

:: フロントエンド
cd D:\61.tool\Pic2PDF_Viewer\frontend
npm run dev
```

---

## 前提

- [uv](https://docs.astral.sh/uv/) がインストール済みであること（`uv --version` で確認）
- Node.js / npm がインストール済みであること（リリースビルドに必要）
- セットアップ手順は [docs/04_環境構築/uv環境セットアップ.md](docs/04_環境構築/uv環境セットアップ.md) を参照
