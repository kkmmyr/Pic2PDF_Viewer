# 起動方法

## モード一覧

| モード | 用途 | Backend | Frontend / 公開入口 | 起動・反映方法 |
|---|---|---|---|---|
| **開発** | コード修正・ホットリロード | 8766 | Vite 5176 | `scripts/start.bat` |
| **Linux 本番** | 通常運用 | uvicorn 127.0.0.1:8091 | nginx 8090 | systemd / `scripts/deploy_to_linux.sh` |
| **Windows ローカルリリース** | 互換・単独端末運用 | NSSM uvicorn 8090 | Backend が dist を統合配信 | `scripts/setup_service.bat`（初回） |

通常の利用 URL は `http://medaroserver:8090`。

---

## Linux 本番への反映

リポジトリルートから Git Bash で実行する。

```bash
# docs または mkdocs.yml を変更した場合
uvx --with mkdocs-material --with mkdocs-mermaid2-plugin mkdocs build --clean

# frontend build、コード転送、DBバックアップ、systemd再起動
bash scripts/deploy_to_linux.sh
```

PowerShell から実行する場合:

```powershell
& 'C:\Program Files\Git\bin\bash.exe' scripts/deploy_to_linux.sh
```

サービス状態とログ:

```bash
ssh amashio@medaroserver
sudo systemctl status pic2pdf-viewer
sudo journalctl -u pic2pdf-viewer -f
```

デプロイスクリプトは MkDocs を生成しないため、設計書変更時は
`uvx --with mkdocs-material --with mkdocs-mermaid2-plugin mkdocs build --clean`を先に実行する。

---

## 開発起動

Windows では `scripts/start.bat` を実行すると、Backend と Frontend が
Windows Terminal の別タブで起動する。

- Backend: <http://localhost:8766>
- Frontend: <http://localhost:5176>

Backend が起動中に `scripts/start.bat` を再実行すると、Backend のみを再起動する。
通常のコード変更は `--reload` により自動反映される。

個別起動:

```bat
:: バックエンド
cd D:\61.tool\Pic2PDF_Viewer\backend
uv run uvicorn main:app --reload --port 8766

:: フロントエンド
cd D:\61.tool\Pic2PDF_Viewer\frontend
npm run dev
```

Mac / Linux の開発環境では同じコマンドを各ターミナルで実行するか、
`scripts/start_dev.sh` を利用する。

---

## Windows ローカルリリース

Windows 単独端末で `:8090` を使う互換運用は NSSM サービスで管理する。

```bat
scripts\build_release.bat
scripts\setup_service.bat
scripts\restart_service.bat
scripts\open_viewer.bat
```

- `setup_service.bat` は初回登録時だけ管理者として実行する。
- 日常の更新は build 後に `restart_service.bat` を実行する。
- 旧タスクスケジューラの `Pic2PDF_Viewer` は二重起動防止のため無効化する。

> 現行`scripts/setup_service.bat`はルート`.venv`を登録する。既存端末に残る旧NSSM登録だけが
> `backend/.venv`を参照する可能性があるため、再利用時は[既知の問題](docs/log/既知の問題.md)を確認する。

詳細は
[Windows サービス化セットアップ](docs/design/環境構築/Windowsサービス化セットアップ.md)
を参照。

---

## 前提

- [uv](https://docs.astral.sh/uv/) がインストール済み
- Node.js / npm がインストール済み
- 初回はリポジトリルートで `uv sync`
- セットアップ詳細:
  [uv 環境セットアップ](docs/design/環境構築/uv環境セットアップ.md)
