# 起動方法

## ワンクリック起動（推奨）

`start.bat` をダブルクリックするとバックエンド・フロントエンドが同時に起動します。

- Backend: http://localhost:8000
- Frontend: http://localhost:5173

初回起動時は `uv` が依存パッケージを自動取得するため少し時間がかかります（2回目以降は数秒）。

## 個別起動

```bat
# バックエンド（uv が依存解決と起動を一括で行う）
cd D:\61.tool\Pic2PDF_Viewer\backend
uv run uvicorn main:app --reload

# フロントエンド
cd D:\61.tool\Pic2PDF_Viewer\frontend
npm run dev
```

## 前提

- [uv](https://docs.astral.sh/uv/) がインストール済みであること（`uv --version` で確認）
- セットアップ手順は [docs/04_環境構築/uv環境セットアップ.md](docs/04_環境構築/uv環境セットアップ.md) を参照
