# 読書会ロングフォーム拡張（動画尺トーク番組化） — 要件整理（準備メモ）

> status: living | last-verified: 2026-07-28

作成日: 2026-07-04
ステータス: **要件確定・バックログ起票済み**（Phase 単位で B-28〜B-30・C-14 に分割）
バックログ ID: B-28（Phase 1）/ B-29（Phase 2）/ B-30（Phase 3）/ C-14（Phase 4、長期構想）
関連バックログ: B-20（読書会ディスカッション生成、実装済み）/ B-26（書籍類似度推薦、要件確定・未着手）

---

## 1. 背景・目的

このツールの目的の一つとして「小説（ラノベ中心）について会話する」がある。現状の検証は「読書会ディスカッション」（B-20、2 ペルソナ SSE 対話生成、実装済み）。

最終目標: **特定の本（将来的にはシリーズ）について、10 分程度の動画に相当する分量の会話コンテンツを生成できるようにする。** さらに、その本だけで閉じず、

- 似た別の本を引き合いに出した比較トーク
- その本にまつわる時事ネタ・過去の流行などの小話（脱線）

を自然に織り交ぜられるようにしたい。これを実現するための土台として、本の内容を RAG で即座に引ける状態と、本の内容をタグのように管理する仕組みが要る、というのがユーザーの仮説。

**今回のスコープ**: 実装ではなく、計画書（要件定義）を作るための前段の情報整理。規模が大きいため他の日常作業とは別建てで扱う。

---

## 2. 現状の実装棚卸し（このリポジトリに既にあるもの）

| 領域 | 実装状況 | 主なファイル |
|---|---|---|
| 読書会 2 ペルソナ対話生成（B-20） | **実装済み**。書籍全文を Qwen 131k コンテキストに一括投入し、`[A]:`/`[B]:` マーカーで 1 回の LLM 呼び出しから対話を生成、SSE で逐次配信 | `backend/services/novel_db/discussion_service.py`, `backend/routers/novel_discussion.py` |
| 書籍サマリ生成（B-5, B-8） | 実装済み。必要情報と自然な文章を優先する可変長要約を`books.summary`に保存し、検索インデックスにも投入 | `backend/services/novel_db/summarizer.py` |
| キャラクター辞典（B-15） | 実装済み。書籍×キャラ単位の人物像サマリ、主要シーン抽出 | `backend/services/novel_db/character_summarizer.py`, `character_db.py` |
| 小説 RAG 検索・QA | 実装済み。OCR→チャンク/embedding→文脈付与→検索/QA、マルチターン会話も対応 | `docs/design/詳細設計/機能別/小説RAG_パイプライン設計.md`, `小説RAG_検索QA設計.md` |
| タグ/ジャンルメタデータ | comic/doujin/novel 共通の粗い genre 軸のみ（`meta_db.py`）。novel 専用の詳細トピックタグは**未実装** | `backend/services/meta_db.py`, `meta_store.py` |
| 書籍横断の類似度・推薦（軽量版 B-19） | **実装済み**（2026-05-17、`027533c`）。サマリ embedding の KNN で top-N 類似書籍を返す `find_similar_books` + `GET /api/novel_db/books/{name}/similar` + 書籍詳細ページ表示まで完備。※バックログ上は未実装扱いのまま残っている（ドリフト） | `backend/services/novel_db/book_summary_search.py` の `find_similar_books`, `backend/routers/novel_db/lib.py` |
| 書籍横断の類似度・推薦（フル版 B-26） | **要件確定済みだが未着手**。bge-m3 全文 embedding + サマリ embedding + タグ/著者 Jaccard のハイブリッド 3 軸、`book_similarities` テーブルに事前計算。B-19 の上位互換 | `docs/design/要件定義/書籍類似度推薦_要件.md` |

**要点**: 「本 1 冊について語る」土台（B-20）と「似た本を見つける」土台（B-26、未着手）は既に設計済み・部分実装済み。今回の構想はこれらを土台に、①長尺・構成化されたトーク台本、②本を横断した自然な脱線、③時事ネタ小話 を載せる拡張レイヤーという位置づけになる。

---

## 3. 今回の対話で確定した方針

| 論点 | 結論 |
|---|---|
| 最終成果物の範囲 | **台本（テキスト）まで**。音声化（TTS）・動画化は将来構想であり今回は対象外。ただし将来の TTS 連携を妨げない設計にする（話者ラベル付きテキストの構造は既に B-20 で担保） |
| 話者形式 | 現行は **2 人**（進行役+専門家等、知識レベルは対等・性格や観点が異なる）。長期的には **3 人以上のパネル形式**も視野に入れるが、今回はまず 2 人構成の拡張から着手 |
| 対象本の粒度 | まず **特定の 1 冊を完成させる**ことを優先。シリーズ・複数巻対応は後続 |
| 類似本の選定範囲 | **ハイブリッド**。RAG 取り込み済みの蔵書内（B-26 の類似度推薦と連携）を優先しつつ、蔵書にない本については LLM の一般知識で補う |
| 時事ネタ・過去の流行の情報源 | **LLM の学習済み知識のみで十分**。リアルタイム Web 検索は今回想定しない（コスト・精度・実装複雑度の観点で見送り） |
| ホストキャラの永続性（2026-07-06 追加） | イメージは「やる夫スレ／ゆっくり解説」的な**固定の司会キャラを本を跨いで使い回す**路線（本自身の登場人物が語る形式ではない）。ただし現行のレイ＆ミオという名前・人物設定自体は Phase 0b 検証用の仮設定であり、**正式なキャラクターデザインは未確定** |
| 演出強化の方向性（2026-07-06 追加） | §4.0 の「対立する主張」原則をさらに一歩進め、**教養・時事ネタの密度アップ**と**ボケ・ツッコミの掛け合い強化**（漫才的なテンポ）を明示的な品質目標とする |
| 品質基準と実行環境（2026-07-06 追加） | 現在はローカル LLM（Qwen、llama-server 経由）で検証中だが、これはあくまで検証用。**品質が不十分な場合は有償 LLM API（Claude 等）への切り替えも選択肢**として残す。品質最優先で、ローカル LLM への固定を前提にしない |
| 設計 3 原則との関係（2026-07-06 明文化） | 要件定義書 §1.2 の原則②「手動 + AI 提案（自動実行しない）」は**メタデータの自動書き換え**を禁じる原則であり、本機能のような**成果物（台本）を生成してユーザーに提示する機能**は適用対象外。既存の B-20・書籍サマリ・キャラクター辞典と同じ扱い |

---

## 4. 現行実装とのギャップ（新規に必要になりそうな能力）

### 4.0 面白さの設計原則（2026-07-04 Phase 0a へのユーザーフィードバックから確立）

Phase 0a の出力に対するユーザー評価は「**だらだらと内容について話している感じがして読む気が起きない**」「時事ネタ・過去の流行の小話が一切ない」。これは本機能の品質基準を定義する最重要フィードバック。分析すると Phase 0a 出力がつまらない原因は 4 つ:

1. **対立の不在**: 全ターンが相手への同意（「ええ、本当に！」「そうですよね！」）から始まり、緊張感・意外性がゼロ。ペルソナに「口調の違い」しか与えておらず、**主張（スタンス）の違い**を与えていなかった。
2. **あらすじの再演**: シーンを順に紹介して解釈を添えるだけ。読んだ人には冗長、未読の人には固有名詞の洪水で、どちらの読者にも刺さらない。
3. **視聴者の不在**: 2 人が内輪で確認し合う体裁で、「画面の向こうの視聴者に向けて話す」という概念がプロンプトに存在しなかった。フック・プレゼン・引きが構造的に生まれない。
4. **脱線素材の不在**: 現行プロンプトは「小説の内容を引用しながら語れ」としか言っておらず、モデルを本の中に閉じ込めている。時事ネタ・類似作品への連想は**素材として明示的に渡さない限り出ない**。

ここから導かれる設計原則（Phase 1 以降の要件の核）:

| 原則 | プロンプト/システムへの落とし込み |
|---|---|
| 面白さは創発しない。素材として渡す | 対立軸・フック・脱線ネタカードを生成前に用意してプロンプトに注入する（「構成作家」の仕事をシステム側が担う） |
| ペルソナ=口調ではなく主張 | 各ペルソナに「今回の推し解釈」を持たせ、意見が割れるテーマを最低 1 つ構成に含める |
| 視聴者ファースト | 「未読の視聴者がいる番組の台本」と定義。全肯定相槌の禁止・あらすじ再演の禁止・固有名詞の初出時一言説明・締めまで総括禁止をルール化 |
| 脱線ネタカード | 類似本（B-19 + `books.summary` / LLM 一般知識）・ジャンルの流行史・設定の系譜などを 2〜3 枚、根拠つきでプロンプトに同梱し「脱線コーナー」セグメントで消費させる |
| リズム | ターン長を可変（20〜300 字）にし、短いツッコミ・疑問形を混ぜる指示を入れる |

この原則を反映した「番組台本型プロンプト」の実証は Phase 0b（§6 実測結果参照）。

### 4.1 台本の構成化・尺のコントロール

- 10 分尺の目安: 日本語の自然な話速は概ね 300〜400 字/分 → 10 分で **概算 3,000〜4,500 字程度の発話量**。
- **分量だけなら現行 API パラメータの範囲内で到達可能**: `POST /api/novel/discussion/generate` は `num_turns` を 2〜20 で受け付け（デフォルト 6）、1 発言は 100〜300 字指示。平均 200 字とすると num_turns=10〜13 で 4,000〜5,200 字に届く。つまり Phase 0 の分量検証は**コード変更ゼロ**で既存 API を叩くだけで開始できる。
- 未解決なのは分量ではなく**構成の制御**（イントロ→本編深掘り→脱線→まとめのセグメント分け）と、長い単発出力の後半で品質・多様性が落ちないかの 2 点。構成指示は `_SYSTEM_TEMPLATE`（`discussion_service.py`）への追記で試せる（ペルソナの `style_description` は max 200 字なのでそこに構成指示を詰め込むのは不適）。
- 生成コスト実測の目安（技術知見 §より）: full-book 投入時 prompt processing ~670 t/s・end-to-end 9.8 t/s（入力 78k tok 時）→ 1 回の生成は**数分オーダー**。Phase 0 の反復検証は現実的な速度で回せる。
- **検討要**: 単発呼び出しのまま構成指示を強化するか、セグメントごとに複数回呼び出して結合するパイプライン化か。後者のレイテンシ懸念は当初想定より小さい — llama-server の KV cache は同一 prefix 再送でヒットする（マルチターン QA で実証済み、検索QA設計 §7）ため、書籍本文を含む system prefix を固定すれば 2 セグメント目以降の prompt processing コストはほぼゼロになる。真の課題はセグメント間の内容重複・話題の一貫性の方。

### 4.2 本を横断した自然な脱線（類似本トーク）

- **コンテキスト上の構造的制約（B-26 の要否とは独立に決まる）**: `MAX_INPUT_TOKENS = 112,000` は主対象本の全文でほぼ使い切る前提の設計。**類似本の全文を同時投入することは不可能**。したがって比較セグメントは必然的に非対称構成になる — 主対象本 = 全文、類似本 = 既存の可変長サマリ（`books.summary`）+ 必要ならキャラクター辞典の抜粋。サマリは文字数で機械的に切らず、投入時に全体のtoken budgetを確認する。
- **類似本の取得手段は既に存在する**（B-26 は前提にならない）:
  - `find_similar_books`（B-19、実装済み）がサマリ embedding KNN で top-N を返す。対話生成側はこれに `books.summary` の取得を組み合わせるだけでよく、新規の類似度インフラは不要。
  - **実測（2026-07-04、現蔵書 19 冊）**: 「後宮の烏」の類似 top-5 はスコア 0.49〜0.52。B-26 の閾値（合成スコア ≥ 0.7）を適用すると **0 件**になる。対話用途では閾値カットではなく「top-N をそのまま使い、スコアが低い場合は LLM 一般知識へフォールバック」する設計が現実的（§3 のハイブリッド方針と整合）。閾値 0.7 という B-26 の値自体も、フル版実装時に実測ベースで見直しが必要な可能性が高い。
  - B-26（ハイブリッド 3 軸 + 事前計算）は精度向上の後続改善として位置づけ直す。本構想の Phase 2 はB-19 で開始できる。
- 蔵書外の本を LLM 一般知識で語らせる場合、内容の正確性は保証されない（ハルシネーションのリスク）。台本用途であれば許容度は高いと考えられるが、事実として扱う体裁にしないなどのプロンプト設計上の配慮が要る。

### 4.3 時事ネタ・過去の流行の小話

- LLM 学習済み知識のみを使う前提なので新規のインフラは不要。プロンプトエンジニアリングで対応可能な領域だが、「本の内容とどう自然に接続するか」（トリガーとなるキーワード・トピックの抽出）には本のトピック情報が要る。

### 4.4 タグ／トピック管理

- 現行の genre 軸は粗すぎる（source 共通のジャンルタグのみ）。脱線トークの「接続点」を見つけるには、本ごとの**トピック・モチーフ単位のタグ**（例: 「学園もの」「タイムリープ」「異世界転生」等）が必要になりそうだが、これは B-26 のメタ Jaccard 軸とも重複しうる領域。両者を統合的に設計すべきか、脱線トーク専用の軽量な仕組みにするかは要検討。

### 4.5 話者フォーマットの拡張（3 人以上パネル）

- 長期構想として明記されているが、`discussion_service.py` の `[A]:`/`[B]:` マーカー方式は 2 話者決め打ちの実装。3 人以上に拡張する場合はマーカー体系・システムプロンプトの一般化が必要（今回は着手しない）。

### 4.6 ホストキャラの永続データ化（2026-07-06 確定）

- **現状の問題**: Phase 0b のレイ＆ミオは、口調・役割・「今回の推し解釈」がすべて 1 つのテストプロンプトにハードコードされている。本ごとに書き捨てる設計のままでは「固定キャストの使い回し」が実現できない。
- **必要な分離**: ホストの人格には 2 層ある。
    - **人格の核**（本に依存せず固定、以下で確定）: 名前・属性・読書傾向・得意分野
    - **本ごとに可変な部分**: 今回の推し解釈・スタンス（対立の種）は本の内容を読んで都度 LLM に考えさせる（人格の核に固定すると本ごとの対立が不自然になるため）

#### 確定した人物設定（名前は仮称のまま採用）

**ミオ**
- 社会人 8 年目・30 歳前後、SE
- 独身（過去に交際経験あり、現在フリー。台本内で明示する必要性は薄い背景設定）
- 現行作品も普通に読むが**回顧主義的**な傾向があり、昔の作品を好む
- 漫画・小説とも雑食（大人向け漫画・少年漫画も読む）だが、**少女漫画**（30 代向け女性漫画含む）・**女性向け恋愛漫画**・**中高生向け少女小説**を今も継続して読んでいる
- 少女漫画的な恋愛要素が好きで**カップリング厨**な面がある
- 得意分野: **世界史・数学**（YouTube でも関連動画を見る習慣あり）

**レイ**
- 社会人 5 年目・27 歳前後、企画職
- 独身（ミオ同様の背景設定）
- 漫画・小説とも雑食（ミオ同様少女漫画も読む）だが、**少年向け漫画**寄りの嗜好
- 昔から漫画・小説を読んでおり旧作にも詳しいが、ミオほど回顧主義的ではない
- 比較的**ショタコン**、**インテリア**好き
- 得意分野: **日本史・英語**

- **役割分担（ボケ / ツッコミ）**: 固定しない。**テーマごとに入れ替わる**方式を採用する（例: 恋愛・カップリング寄りの話題ではミオが感情優位でボケ側に回りやすく、少年漫画的な話題ではレイが同様の立場になりうる、といった程度の目安。厳密な固定ルールはプロンプト設計時に詰める）。
- **教養・脱線ネタとの接続**: 得意分野（ミオ=世界史・数学、レイ=日本史・英語）は脱線ネタカード（§4.7）の選定・言及に紐付く資源になる。ミオの回顧主義は「懐かし系」の脱線に、両者の雑食性は幅広いジャンル比較の脱線に活用できる。
- **今回未確定のまま残す点**: 決め台詞・口癖などの細部演出は Phase 1 実装時に詰める。ビジュアルデザインは台本（テキスト）どまりの今回スコープでは不要（§7 スコープ外）。データ構造（Config か DB か）は Phase 1 着手時に決める。

### 4.7 脱線ネタカードの密度強化（2026-07-06 追加）

- 現行 Phase 0b は「脱線ネタ 2〜3 枚を LLM にその場で考えさせてプロンプトに埋め込む」方式。教養・時事ネタの密度を上げる場合、その場の 1 回生成では量・質にばらつきが出るリスクがある。
- **選択肢**: ①プロンプト内の指示を強化して枚数を増やすだけで足りるか試す（低コスト） / ②ネタカード自体を生成する専用ステップ（本を読んでネタ候補を先出しさせる下準備の LLM 呼び出し）を切り出し、量と正確性を安定させる（高コスト・Phase 0b の「ハルシネーション」問題への対策にもなりうる）。どちらが要るかは Phase 1 実測で判断。

### 4.8 ボケ・ツッコミの明示化（2026-07-06 追加）

- 現行の「対立する主張」原則は話の内容面の対立が中心で、漫才的な掛け合い（ボケ→ツッコミの反射的なテンポ）とは別軸。
- 密度を上げるには、ペルソナ定義に**役割ラベル（ボケ / ツッコミ）**を明示し、「ボケへの反応パターン」「テンポの作り方（短い切り返し）」を会話ルールに追加する必要がありそう。§4.0 のリズム原則（ターン長 20〜300 字・短いツッコミ混在）と統合できる領域。

### 4.9 番組の世界観・関係性・コンテンツ境界線（2026-07-06 確定）

- **二人の関係性**: 同じマンションに住む、別々の会社に勤める友人。エレベーターで一緒になった際にお互い読書趣味が近いと気づいて仲良くなった、という間柄（同僚ではない）。
- **呼び方・口調のベース**: Phase 0b のテストを踏襲し、**レイ=丁寧語、ミオ=くだけた口調**を正式なベースとして採用。具体的なあだ名・一人称・口癖などの細部は Phase 1 実装時に詰める（今回は未確定のまま残す）。
- **番組ブランド化（番組名・定型オープニング/エンディング句）**: 今回は**保留**。台本の中身（構成・面白さ）を優先し、ブランディングは後回しでよいと判断。
- **性癖ネタ（レイのショタコン・ミオのカップリング厨）の扱い**: **文脈次第**。脱線ネタのコーナーで話題が来た時にのみキャラの個性として触れる程度に留め、本編の中心ネタにはしない。
- **対象視聴者層**: **オタク向け**（ラノベ・漫画に日常的に触れている層）。一般向けほど丁寧な説明は不要で、ジャンルの内輪ネタ・作品引用を前提にしてよい。
- **NG ライン**: **実在の人物・企業への評価・批判は避ける**。それ以外（政治・宗教等）については明示的な NG リストを設けず、常識的な判断に委ねる。

### 4.10 台本の閲覧・エクスポート UI（2026-07-06 確定）

現行 UI（`NovelDiscussionPage`: 書籍選択 + 自由編集ペルソナ×2 + 往復数スライダー → チャット吹き出しの逐次表示 + 折りたたみ履歴）を土台に、以下を確定:

- **B-20 との関係 — 置き換える**: 読書会ページ（`/novel/discussion`）を番組台本生成に一本化する。自由編集ペルソナ（`PersonaPanel`）と往復数スライダーは廃止（ミニマル化原則と整合。ペルソナはレイ＆ミオ固定、尺はセグメント構成が決めるため）。設定パネルは実質「書籍選択 + 生成ボタン」に簡素化される。旧形式で保存済みの履歴は引き続き閲覧可能のままとする。
- **セグメント表示 — 見出し付き区切りを表示**: 各セグメントの境目に区切り線 + 見出し（例: 「─ 脱線コーナー ─」）を挿入する。DoD 層1 M2 の機械可読境界マーカー（§10）をそのまま流用するため実装コストは小さい。長い台本の読みやすさに直結。
- **エクスポート — クリップボードコピー + Markdown ダウンロード**: セグメント見出し + 「ミオ: 〜」形式の台本を、①ワンクリック全文コピー、② `.md` ファイルダウンロードの 2 経路で持ち出せるようにする。構造化 JSON エクスポートは今回見送り（保存 JSON 自体が話者・セグメント構造を持つため、将来の TTS / 動画化はそちらを入力にすればよい）。
- **合否ゲートの表示 — 合否バッジ + 不合格理由 + 再生成ボタン**: 生成完了時に DoD 層1 の機械チェック結果を結果ヘッダにバッジ表示し、不合格項目（字数不足・言語リーク等）をリストで示す。ワンクリックで再生成できるボタンを添え、再生成運用（§10 層2）を UI で支える。

> ページ置き換えに伴う API 側の変更（`personas` / `num_turns` パラメータの扱い）は §4.11（保存スキーマ・API 契約）で確定。

### 4.11 保存スキーマ・API 契約（2026-07-06 確定）

現行: `POST /api/novel/discussion/generate`（body: `book_name` + `personas`×2 + `num_turns`）が SSE で `turn` イベントを流し、`data/kindle_novel/discussions/<書籍名>/<ts>.json` に `{book, personas, turns[speaker,text], partial, created_at}` を保存。削除 API なし。

#### 保存スキーマ v2

- **`turns` はフラット構造を維持**しつつ、各 turn に `segment` フィールドを追加（`{speaker, text, segment: "op_hook"}` 等）。トップレベルに順序付きの `segments: [{id, title}]` を持ち、UI のセグメント見出し表示（§4.10）はこれを参照する。ネスト構造にしない理由: SSE で 1 ターンずつ届くストリーミングと自然に整合し、旧形式（`segment` なし）との共存も単純になるため。
- **キャラ設定はスナップショット埋め込み**: 生成時点の人格核（名前・プロフィール）+ 書籍可変部（今回の推し解釈・スタンス）+ 使用した脱線ネタカードを保存 JSON に全文埋め込む。履歴が自己完結し、後からキャラ設定を変更しても過去台本の表示・再現性が壊れない。
- **機械チェック結果（DoD 層1）を `checks` フィールドとして保存**: M1〜M5 の合否と詳細（字数・リーク検出位置等）。履歴一覧での合否バッジ表示（§4.10）に使う。
- **`format_version: 2`** を導入。旧 JSON（フィールドなし）は v1 として読み、セグメントなしで表示する（旧履歴閲覧要件と整合）。
- 周辺修正: `save_discussion` のタイムスタンプが JST 値に UTC を示す `Z` サフィックスを付けている表記不整合（§6 末尾の周辺メモ）をこの機に修正する。

#### API 契約 v2

- **`POST /api/novel/discussion/generate` は破壊的変更**: body は `{book_name}` のみに簡素化（`personas` / `num_turns` は廃止。キャラはサーバー側の永続データ、セグメント別ターン数はサーバー側デフォルト設定が決める）。単一クライアント・API バージョニング不要の方針（バックログ 対象外テーブル）に沿い、旧契約は残さない。
- **SSE イベントを拡張**: 既存の `turn` に加え、`{type:"segment", id, title}`（セグメント境界。DoD 層1 M2 の機械可読マーカーをそのままイベント化）と、終端の `{type:"done", saved_path, checks:{...}}`（層1 チェック結果同梱）を追加。
- **`GET /history` は互換維持**: v1/v2 両形式の JSON を読めるようにする。
- **`DELETE /api/novel/discussion/history/{filename}` を新設**（保存・削除ポリシーは下記）。ファイル名指定の削除のため、パストラバーサル対策（`validate_safe_path` 相当の検証）を必須とする。

#### 保存・削除ポリシー

- **全保存 + 削除ボタン**: 機械チェック不合格の台本も保存する（チェックが厳しすぎないかの検証材料になり、機械不合格でも面白い「外れ回」を拾える余地を残す）。履歴カードに削除ボタンを付け、不要分は UI から手動掃除する。

---

## 5. まだ決まっていない・検証が必要な論点

1. ~~尺のコントロール方法~~ → **方式は決着（2026-07-04 Phase 0b 実測）**: 単発生成 + セグメント別ターン数指定で行く。パイプライン化は不要と判断（後半品質劣化は Phase 0a で否定済み、構成遵守は Phase 0b で実証済み）。残るのは 10 分尺への到達のみで、これはセグメント別ターン数の増量で調整し、DoD 層1 M1（§10）で検証する。
2. ~~類似本情報の取得方式~~ → **解決済み（2026-07-04 実機確認）**: `find_similar_books`（B-19、実装済み）+ `books.summary` の組み合わせで足りる。残る論点は「スコアがどの程度低い類似本まで対話に載せるか」の線引きのみ（現蔵書の実測ではスコア帯 0.5 前後が上位、§4.2）。
3. **トピックタグの設計主体**: 自動抽出（LLM にタグ付けさせる）か、手動タグ付けか。自動の場合はどの粒度・語彙で統一するか。
4. **蔵書外の類似本を語らせる際の事実性担保**: プロンプトで「これは会話上の一般的な言及であり事実確認はしていない」旨をどう扱うか（台本上は自然な会話を優先し、視聴者向けの注記は動画編集側の責務とする、等の役割分担が必要かもしれない）。
5. ~~完成の定義（Definition of Done）~~ → **解決済み（2026-07-06）**: 3 層構造の DoD として確定。機械チェック 5 項目 + 再生成運用前提の合格ライン + ユーザー主観評価（LLM 採点補助）。詳細は **§10**。
6. ~~ホストキャラの正式デザイン~~ → **解決済み（2026-07-06）**: 名前はレイ＆ミオを仮称のまま採用。年齢・職業・独身設定・読書傾向・得意分野（ミオ=世界史/数学、レイ=日本史/英語）まで確定（§4.6）。役割（ボケ/ツッコミ）は固定せずテーマごとに入れ替える方針。決め台詞・口癖等の細部は Phase 1 実装時に詰める。
7. **ローカル LLM ↔ 有償 LLM の比較タイミング・評価軸**（2026-07-06 追加）: 現在は Qwen（ローカル）で検証中だが品質最優先のため有償 API 切り替えも選択肢。①ローカルでの作り込みを一定まで進めてから比較するか、②早期に同一プロンプトを有償 API にも投げて品質差を実測するか。評価軸は §10 の DoD（特に層3の LLM 採点ルーブリック）を流用できる見込み。**コスト試算もこの論点に含める**: 全文投入（〜112k tok 入力）を有償 API に都度投げる場合の 1 生成あたり概算コストの桁感を B-28 着手時に算出する。
8. ~~台本の閲覧・エクスポート UI~~ → **解決済み（2026-07-06）**: 既存ページを番組台本生成に置き換え（B-20 の自由ペルソナ UI は廃止）・セグメント見出し付き区切り表示・クリップボードコピー + Markdown ダウンロード・合否バッジ + 再生成ボタン。詳細は **§4.10**。
9. ~~保存スキーマ・API 契約の変更~~ → **解決済み（2026-07-06）**: ①turn 単位の `segment` フィールド + `format_version: 2`、②キャラ設定はスナップショット埋め込み、③generate API は `{book_name}` のみの破壊的変更 + SSE に `segment` / `checks` イベント追加、④全保存 + DELETE API 新設。詳細は **§4.11**。

---

## 6. 想定される段階的アプローチ（技術検証の叩き台・未確定）

計画書化する際のたたき台として、リスクの低い順に並べる（着手順の確約ではない）:

1. **Phase 0a（技術検証・コード変更ゼロ）**: 既存 API を `num_turns=10〜13` で叩き、10 分尺相当（3,000〜4,500 字）の分量が出るか・後半のターンで品質が落ちないかを実測する。
2. **Phase 0b（技術検証・プロンプトのみ変更）**: `_SYSTEM_TEMPLATE` に「イントロ→本編深掘り→まとめ」程度の構成指示を追記し、単発生成のままで構成が守られるかを検証する。ここでパイプライン化の要否が判断できる。
3. **Phase 1**: Phase 0 の結果を踏まえ、単一書籍内で構成化された長尺台本生成を実装（既存 discussion_service の拡張、または新規サービスとして切り出し）。
4. **Phase 2**: 類似本比較セグメントを追加。必要な部品（`find_similar_books` = B-19 + `books.summary`）は実装済みで、残タスクは「類似本のサマリを対話プロンプトへ差し込む結線」のみ（§4.2）。B-26 フル実装は精度向上の後続改善として切り離す。
5. **Phase 3**: 時事ネタ・過去の流行の小話セグメントをプロンプト側で追加（新規インフラ不要、プロンプト設計とトピックタグ整備が中心）。
6. **Phase 4（長期・別バックログ）**: 3 人以上のパネル形式への拡張。

### Phase 0a 実測結果（2026-07-04 実施済み）

対象: 「後宮の烏」（134 ページ）、`num_turns=12`（12 往復 = 24 ターン要求）、ペルソナは冷静分析派（丁寧語）+ 感情移入派（くだけた口調）の 2 人。

| 計測項目 | 結果 |
|---|---|
| 生成ターン数 | **14 ターン（7 往復）で早期終了** — 要求の 58%。最終ターンで自ら「読書会を終えることができました」と締めに入った |
| 総発話量 | 2,902 字 ≈ **8.3 分**（350 字/分換算）。10 分にわずかに届かず |
| 所要時間 | 202 秒（prompt processing 152 秒 + 生成 50 秒）。反復検証に十分実用的 |
| 後半品質 | **劣化なし**。ターン長はむしろ増加（174→238 字）、固有名詞・シーン引用・ペルソナ書き分けも最終ターンまで維持 |
| 品質上の瑕疵 | ターン #10 に中国語混入 1 箇所（「〜の行為**变成了**」）。Qwen の言語リーク。後処理での検出 or プロンプトでの抑制指示が必要 |

**結論・示唆**:
- ボトルネックは分量上限でも後半品質でもなく、**ターン数指示の遵守**。モデルは自然な談話アーク（導入→深掘り→総括）に沿って早期収束する。皮肉なことに「構成」は既に自然発生しており、問題はそのアークが指定尺より短く閉じること。
- §5 論点 1 の判断軸を更新: 「単発長出力の後半品質劣化」は実測で否定された。次の Phase 0b では「まとめに入るタイミングの制御」（例: セグメント構成の明示 + 「合図があるまで総括しない」指示）で単発生成のまま尺が伸ばせるかを検証し、ダメならセグメント分割パイプラインに進む。
- 副産物: 中国語リーク対策（プロンプト 1 行 or 後処理）を Phase 1 の要件に含めること。
- **ユーザー評価（Phase 0a 出力）**: 「だらだらと内容について話している感じで読む気が起きない」「時事ネタ・脱線が一切ない」→ §4.0 の面白さの設計原則として要件化。

### Phase 0b 実測結果（2026-07-04 実施済み — 番組台本型プロンプト）

§4.0 の設計原則を反映したプロンプト（ペルソナに対立する主張・5 セグメント番組構成・脱線ネタカード 3 枚・全肯定/あらすじ再演/早期総括の禁止・セグメント別ターン数指定）で同一書籍・同一 LLM 設定のまま再生成。製品コードは未変更（テストハーネスでカスタム messages を `stream_discussion_turns` に直接投入）。

| 計測項目 | 結果 |
|---|---|
| 内容品質 | **激変**。OP フック（「また中華風後宮ロマンスかと敬遠していませんか？」）・明確な対立（「それは浅い読み方だと思います」vs「またその理屈の話？」）・脱線コーナー（『薬屋のひとりごと』比較→『十二国記』の系譜→本作へ復帰）・締め（「どんな人に刺さるか」+ 星 5 つ）がすべて指示どおり出現 |
| ターン数 | 実質 約 24 ターン（セグメント別ターン数指定が有効に機能。Phase 0a の全体数指定より遵守率が大幅改善） |
| 総発話量 | 2,681 字 ≈ 7.7 分。目標 10 分にはまだ不足 → セグメント別ターン数の増量で調整可能な見込み |
| 所要時間 | 223 秒（Phase 0a と同等） |
| **新規の問題 1: マーカードリフト** | 出力途中（5 ターン目以降）で話者マーカーが `[A]:` → `[A>:` に崩れ、既存パーサ（`\[([AB])\]:`）が分割に失敗。残り全ターンが 1 つの塊として扱われた。**Phase 1 でパーサの寛容化（`[A>:` 等の揺れ許容）が必須要件** |
| 新規の問題 2: 脱線でのハルシネーション | 『薬屋のひとりごと』の主人公を「ミソギ」と誤称（正: 猫猫）。**脱線ネタカードには作品名だけでなく正確な固有情報（主人公名等）を含めるか、「カードに書かれた事実以上の詳細に踏み込まない」指示が必要** |
| 言語リーク | 中国語混入は 0（禁止指示が有効）。代わりに英語 1 箇所（「琥珀」→「amber の魚」） |

**結論**: 「面白さは素材として渡す」原則は単発生成でも有効に機能する。Phase 1 の実装要件は (1) 番組構成テンプレート + 脱線ネタカードのプロンプト組み込み、(2) マーカーパーサの寛容化、(3) ネタカードへの正確な固有情報の同梱（ハルシネーション抑制）、(4) セグメント別ターン数の増量による 10 分尺到達、(5) 言語リークの後処理検出。

> 実装時の周辺メモ: `save_discussion`（`discussion_service.py`）のファイル名タイムスタンプは JST の `datetime.now(JST)` に UTC を示す `Z` サフィックスを付けており表記が不整合（docstring も「UTCタイムスタンプ」と記載）。実害はないが、本機能でディスカッション保存形式を触る際についでに直すとよい。

---

## 7. 今回はスコープ外にするもの

| 項目 | 理由 |
|---|---|
| TTS 音声合成・動画ファイル生成 | 台本(テキスト)までが今回のゴール。将来構想として設計上は妨げない程度に留める |
| リアルタイム Web 検索による時事ネタ収集 | コスト・精度・実装複雑度の観点でユーザーが今回は不要と判断 |
| 3 人以上のパネル形式の実装 | 長期構想。まず 2 人構成 × 単一書籍の完成を優先 |
| シリーズ（複数巻）通しの会話 | 単一書籍完成後の後続タスク |

---

## 8. 付録: Phase 0b 実証済みプロンプト（Phase 1 実装のベース）

Phase 0b で品質改善を実証したシステムプロンプトの全文（`{book_name}` / `{book_text}` は既存テンプレートと同じプレースホルダ。ペルソナの主張・テーマの問い・脱線ネタカードは書籍ごとに差し替える可変部）:

```
あなたは本好き向けトーク番組の放送作家兼出演者AIです。課題本について2人のパーソナリティが語り合う動画番組の「台本」を書いてください。画面の向こうには視聴者がいます。視聴者はこの本をまだ読んでいないかもしれません。

## 課題本
{book_name}

## 小説本文（全ページ）
{book_text}

## パーソナリティ（スタンスの対立が番組の生命線）
- **レイ [A]**: 構造・テーマ分析派。今回の主張は「本作は恋愛ものの皮を被った『歴史の隠蔽と沈黙』のミステリーである。ロマンスとして読むのはもったいない」。理屈っぽいが、たまにミオの熱量に押されて本音が漏れる。丁寧語。
- **ミオ [B]**: キャラクター・感情派。今回の主張は「理屈はどうでもいい、寿雪と高峻の不器用な関係性こそ本体。ミステリー要素はおまけ」。レイの分析に「またそういう小難しい話を…」とツッコみつつ、直感で核心を突くタイプ。くだけた口調。

## 番組構成（この順番で進行する。セグメント見出しや区切り記号は出力しない）
1. **OPフック（2〜3ターン）**: 視聴者を掴む。あらすじ紹介から入るのは禁止。「〇〇だと思って敬遠してる人、損してます」のような引きで始める。
2. **テーマ1（6ターン前後）**: 「高峻（皇帝）が寿雪（烏妃）に執着するのは、愛か、利用か、それとも贖罪か」— 2人の解釈がはっきり割れること。決着はつけなくてよい。
3. **テーマ2（6ターン前後）**: 「『後宮にいるのに夜伽をしない妃』という烏妃の設定はなぜ巧いのか」— 設定の面白さを視聴者にプレゼンする。
4. **脱線コーナー（4〜6ターン）**: 下の「脱線ネタ」から1〜2個を使って本作からいったん離れ、また自然に本作へ戻ってくる。脱線こそ番組の華。
5. **締め（3ターン）**: それぞれ「どんな人に刺さる本か」を一言 + 例えや点数など印象に残る評価で終える。このセグメントに入るまで、総括・まとめの空気を出すことを禁止する。

## 脱線ネタ（この材料から選んで使う。無理に全部使わなくてよい）
- 『薬屋のひとりごと』のアニメ化以降、中華後宮ミステリーが一大ジャンルになった流れ。本作はその流れの中でどんな立ち位置か
- 『彩雲国物語』『十二国記』など中華風ファンタジーの系譜と、少女小説の伝統芸「与えられた役割から解放されていく少女」
- 「後宮×幽霊」という組み合わせの妙。閉鎖空間で女たちの愛憎が渦巻く後宮は、そもそもホラーと相性が良すぎる

## 会話のルール
- **安易な同意の禁止**: 「ええ、本当に！」「そうですよね！」のような全肯定で発言を始めない。各テーマで最低1回は反論・別解釈・茶々を入れる
- **あらすじの再演禁止**: シーンの引用は「自分の主張の証拠」として出すときだけ。物語を頭から順になぞらない
- ターンの長さは20〜300字で可変。短いツッコミや疑問形を混ぜて会話にリズムを作る
- 登場人物名は初出時に一言説明を添える
- 出力は日本語のみ。中国語の語彙・簡体字を混ぜない
- レイの発言は `[A]:`、ミオの発言は `[B]:` で始める。それ以外のヘッダー・前置き・後書きは一切出力しない
```

user メッセージ: 「番組台本をお願いします。構成の順番と各セグメントのターン数目安を守り、締めセグメントに入るまで総括しないでください。合計 21〜24 ターンを目安に。」

Phase 1 での改善ポイント（§6 Phase 0b 実測結果より）: マーカードリフト（`[A>:`）へのパーサ寛容化・脱線ネタカードへの正確な固有情報同梱（例: 薬屋の主人公名「猫猫」を明記）・セグメント別ターン数の増量（10 分尺到達）・英語リーク対策。

## 9. 次のアクション

- **2026-07-06 完了**: `docs/log/計画/バックログ.md` に Phase 単位で新規 ID を起票済み。
    - **B-28**: Phase 1（構成化台本 + 固定ホストキャラ永続化）
    - **B-29**: Phase 2（類似本トーク連携）
    - **B-30**: Phase 3（時事ネタ・教養小話の密度強化）
    - **C-14**: Phase 4（3 人以上パネル拡張、長期構想）
- **2026-07-06 追記**: DoD を §10 として確定（§5 論点5 解決）。同日のレビューで §5 の未決論点の棚卸しを修正し、論点8（UI/エクスポート → §4.10）・論点9（保存スキーマ・API 契約 → §4.11）も確定。残る未決論点は **論点3（トピックタグ設計）・論点4（蔵書外類似本の事実性）・論点7（有償 LLM 比較 + コスト試算）** の 3 件。うち 7 は B-28 着手時、3・4 は B-29/B-30 の設計時に判断する。**B-28 は要件レベルでは着手可能な状態**。
- **2026-07-07 追記: B-28 実装完了（コード）**: §4.6〜4.11・§10 の要件どおりバックエンド（2 段 LLM パイプライン・寛容パーサ・機械チェック M1〜M5・保存 v2・DELETE API）とフロントエンド（ページ刷新・セグメント表示・エクスポート・合否バッジ・削除）を実装（変更履歴 2026-07-07 参照）。現在形の設計は [小説RAG_検索QA設計 §8](../詳細設計/機能別/小説RAG_検索QA設計.md) が正本。**残るは DoD 層2/層3 の実機検証**（「後宮の烏」で機械チェック全合格 + 主観合格の台本 1 本）で、合格をもって B-28 完了。
- 本メモ（情報整理メモ）は今後 B-28〜B-30 の実装が進むにつれ役目を終える。実装の詳細判断はバックログ側の各 ID と、必要に応じて実装時の設計判断（ADR）に引き継ぐ。

---

## 10. 完成の定義（DoD）（2026-07-06 確定）

B-28（Phase 1）の完成判定基準。「面白さ」という主観品質を 3 層に分解して検証可能にする。

### 層1: 機械チェック（生成物 1 件ごと・スクリプト判定）

| # | 基準 | 判定方法 |
|---|---|---|
| M1 | 総発話量 3,000〜4,500 字（10 分尺） | 全ターンの文字数合計 |
| M2 | 5 セグメント（OP フック / テーマ1 / テーマ2 / 脱線 / 締め）が全て出現 | 生成時にセグメント境界を機械可読マーカーとして出力させ、パース時に検出・除去する（**この出力設計自体を Phase 1 の実装要件に含める**。本文内容からの推定判定は不安定なため採らない） |
| M3 | 全ターンが話者分割に成功（塊が残らない） | 寛容化パーサ（`[A>:` 等のマーカードリフト許容）で分割し、未分割の残余がないこと |
| M4 | 言語リーク 0 件 | 簡体字は Unicode 範囲で機械検出。英語リークは既知パターン検出 + 目視補完 |
| M5 | 脱線ネタカードの最低 1 枚が本文中で言及されている | カード内キーワードの出現判定 |

### 層2: 合格ライン（安定性の定義）

- **1 回でも「層1 全合格 + 層3 合格」の台本が生成できれば B-28 完了**とする。再生成運用（外れ回は捨てて引き直す）を前提とする個人ツールの割り切り。
- したがって層1 の機械チェックは DoD の一発判定用ではなく、**恒常的な生成物の合否ゲート**として実装する（生成のたびに自動判定し、不合格なら再生成を促す）。
- 運用ガイド: 5 回連続で層1 不合格が続く場合は再生成をやめてプロンプト改善に戻る（無限再生成の防止）。

### 層3: 主観評価（最終ゲート・ユーザー判定）

| # | 基準 |
|---|---|
| S1 | 笑える /「へえ」となる箇所が複数ある（目安 3 箇所以上） |
| S2 | キャラがブレていない（レイ＆ミオの口調・人格が全ターンで一貫し、§4.6 の設定と矛盾する発言がない） |

- チューニング反復中は**別 LLM に S1 / S2 をルーブリック採点させて高速に回し**、最終合格の判断だけユーザーが実際に読んで下す二段構え。
- Phase 0a への評価「だらだら話すだけで読む気が起きない」の裏返しとして、S1 / S2 を満たせば「読む気が起きる」とみなす。

### 検証範囲

- **「後宮の烏」1 冊で合格すれば B-28 完了**。複数書籍への汎化検証は DoD に含めない（プロンプトの過学習リスクは受容し、Phase 1 完了後の実運用で確認する）。
- 過学習の構造的な緩和策として、ホストキャラ設定・番組構成テンプレート・会話ルールは**書籍非依存**に設計し、書籍依存部分（テーマの問い・脱線ネタカード・推し解釈）だけを差し替え可変にする（§4.6 の 2 層分離と同じ原則）。
