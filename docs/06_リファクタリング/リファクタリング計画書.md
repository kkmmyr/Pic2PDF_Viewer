# リファクタリング計画書

最終更新: 2026-05-15（Phase 67 完了・Phase 68〜72 追加候補を記載）

未着手のリファクタリング候補・追加候補・対象外項目を管理する。完了済み Phase の詳細は [リファクタリング履歴.md](リファクタリング履歴.md) を参照。

---

## 概要

| 項目 | 値 |
|---|---|
| 完了 Phase 数 | 54 + Phase 55〜72 + Phase 57〜58（再実装）+ Phase 64〜65（DB/TQ刷新） |
| 未着手 Phase 数 | P3 候補 2 件のみ（下記参照）。P1/P2 はすべて完了またはスキップ判断済み |
| 直近完了 | Phase 58（2026-05-15）— ReaderPanel.tsx 5 層化（useReaderState / useReaderInput / ReaderPageView） |
| 次の動き | 新規候補発掘待ち。残 P3 は「router join_path 統一（判断保留）」「useOutsideClickAndEsc（スキップ寄り）」のみ |

優先度の判断基準:
- **P1**: 既に分離した責務が再膨張、または毎レンダー走る派生計算、緊急バグ修正など実害がある
- **P2**: 重複ロジック・読みづらさが顕在化しているが緊急性は中
- **P3**: 構造改善ではあるが現状で困っていない

---

## 未着手・追加候補

### Phase 51 完了後の追加候補（2026-05-05 抽出）

候補（2026-05-05 時点 / Phase 51 投入後の調査結果から）:
- **P2**: ジョブ状態管理が 3 系統（`GenerateJob` / `JobStateManager[T]` / `ocr_service` シングルトン）→ 単一基盤に統一 *(Phase 52 でスキップ判断 — 用途が異なるため)*
- ~~**P2**: router の `except Exception → logger.exception → HTTPException` 重複 → `_deps.py` にラッパー or デコレータ~~ *(Phase 52-1 で対応済み)*
- ~~**P2**: radio mode + reset パターン（`BulkAuthorDialog` / `BulkSeriesAssignDialog` / `SeriesEditDialog`）→ `useRadioMode<T>` 化~~ *(Phase 52-3 でスキップ判断)*
- ~~**P2**: タグ入力 UI（`addTag` / `removeTag` / Enter,Backspace）→ `useTagsInput` 化~~ *(Phase 52-2 で対応済み)*
- ~~**P2**: accent カラーが indigo / purple / red バラバラ → token に統一~~ *(Phase 52-4 で対応済み)*
- ~~**P3**: sort ロジック分散（`localeCompare('ja')`）→ `cmpJa` 化~~ *(Phase 53-2 で対応済み)*
- ~~**P3**: hooks の bash パース脆弱性（Windows パスの backslash）→ jq 化検討~~ *(Phase 53-3 で python3 化)*
- **P3（スキップ）**: `ReaderPanel.tsx` / `services/series_resolver.py` / `services/pdf_generator.py` の分割 *(Phase 53 調査でスキップ判断 — 詳細は [履歴.md § Phase 53（スキップ判断）](リファクタリング履歴.md))*
- ~~**P3**: SeriesEditDialog の独自 useState ベース送信ロジック → `useDialogSubmit` 統一~~ *(Phase 53-1 で対応済み)*

### Phase 54 後の追加候補（未着手）

- **P3**: backend ルーター間の `os.path.join` vs `join_path`（slash 統一）の使い分け。`routers/library.py` のみ `join_path` を使用、`routers/pdfs.py` / `thumbnails.py` は素の `os.path.join`。フロントから返す URL ではなくファイルシステム操作に使っている分は揃える必要はない。**判断保留**。
- **P3**: 外クリック + Esc 同時対応のドロップダウン制御（`ToolsMenu` / `SearchableSelect`）→ `useOutsideClickAndEsc` フック化。再利用箇所が 2 つしかなく、フック導入の利得は薄い。**スキップ寄り**。
- ~~`LibraryPanel.tsx` / `ReaderPanel.tsx` の presentation/container 分割~~ → **対応しない**（[バックログ § 対象外](../01_要件定義/バックログ.md) に移動。hook 抽出済みで JSX 主体の分割では認知負荷が下がらず、テスト容易性は hook 単体テストと将来の E2E で充足見込み）

### Phase 63〜67 — ライブラリ・DB 刷新（2026-05-14 追加）

---

#### Phase 64 — `meta_store` / `genre_store` → SQLite 移行（P1）✅

**実装**: `meta_store.py` / `genre_store.py` が `services/meta_db.py` 経由で SQLite（`meta.db`）に移行済み。`books_meta` テーブル + `genres` テーブル。JSON ファイルからの移行ロジックは `genre_store.py` 起動時に自動実行。詳細: [履歴.md § Phase 64](リファクタリング履歴.md)

---

#### Phase 65 — TanStack Query 導入（P2）✅

**実装**: `@tanstack/react-query` 導入済み。`usePolling.ts` 削除・`usePdfStatus` / `useGenerateJob` を `useQuery`（refetchInterval）パターンに置き換え。`frontend/src/lib/queryClient.ts` 新規作成。詳細: [履歴.md § Phase 65](リファクタリング履歴.md)

---

#### Phase 66 — Alembic 導入（P2）✅

**問題**: `backend/services/novel_db/schema.py` の `_migrate()` が手書きの `PRAGMA table_info` + `ALTER TABLE ADD COLUMN` チェックの連鎖になっており、カラム追加のたびに行数が増える。バージョン管理・ロールバック手順がコード外に存在しない。  
**実装**: `alembic` を本体依存に追加（runtime で upgrade_head() を呼ぶため）。`backend/alembic/` に env.py / script.py.mako / versions/ 配置。`_migrate()` を `0001_initial.py` revision に移管（idempotent）。`_ddl()` は全カラムを含む最新スキーマに更新。`schema.py` から `_migrate()` を削除し `init_schema()` を簡略化。`migrations.py` 新規作成で `upgrade_head()` ヘルパーを提供。`main.py` lifespan に組み込み（失敗で起動中断）。詳細: [履歴.md § Phase 66](../05_記録/変更履歴.md)

---

#### Phase 67 — React 18 → 19 アップグレード（P3）✅

**問題**: `react@18.2.0` / `react-dom@18.2.0`。React 19 の `use()` フック・Server Actions・改善された Suspense が使えない。`@types/react` / `@types/react-dom` もバージョンアップが必要。  
**実装**: `npm install react@19 react-dom@19 --legacy-peer-deps` で更新。`@types/react@19` / `@testing-library/react@16`（React 19 対応）も更新。`Button.tsx` の `forwardRef` を通常関数コンポーネント + `ref` prop に書き換え。`useBookMeta.test.ts` の `waitFor` 条件を強化（@testing-library/react@16 のタイミング変化対応）。vitest 852 passed・tsc エラーなし。詳細: [履歴.md § Phase 67](../05_記録/変更履歴.md)

---

### Phase 68〜72 — コードベース調査由来の追加候補（2026-05-15 抽出）

---

#### Phase 68 — `LibrarySection.tsx` 分割（P2）

**問題**: 431 行・グループ表示モード（flat / author / series）・選択モード・一括操作・ドリルダウンが 1 コンポーネントに混在。機能追加のたびに props が増える傾向。  
**改善案（grill-me 2026-05-15 確定）**:

| コンポーネント | 責務 | 主な props |
|---|---|---|
| `LibraryViewModeSelector` | グループ切替ボタン + ライブラリ件数ヘッダー | `groupMode`, `totalCount`, `onChangeMode` |
| `BulkActionsPanel` | 選択トグルボタン + 選択中アクションバー（全選択/解除・作者設定・シリーズ登録） | `isSelecting`, `onToggleSelecting`, `targetBooks`, `selectedNames`, `onToggleSelectAll`, `onOpenAuthorDialog`, `onOpenSeriesDialog` |
| `SeriesDrilldownPanel` | パンくず + カードグリッド（選択中）/ SeriesDrilldownView（通常） | `drilldown`, `isSelecting`, `selectedNames`, `renderCard`, `onBack` 他 |

**状態の場所**: `isSelecting` / `selectedNames` / `groupMode` / ダイアログ状態はすべて `LibrarySection` が保持・props 渡し。  
**スコープ外**: `SeriesDrilldownView` / `BookCard` / `SeriesGroupCard` / ダイアログ内部ロジックは変更しない。  
`LibrarySection` 本体は 3 コンポーネントのオーケストレーターに縮小（目標 150 行以下）。  
**変更対象**: `frontend/src/components/novel_db/LibrarySection.tsx`  
**新規作成**: 上記 3 コンポーネント（`components/novel_db/` 配下）+ 各コンポーネントの vitest テスト  
**完了条件**: `npm run lint` / `tsc --noEmit` クリーン・vitest 全通過

---

#### Phase 69 — `ChatSection.tsx` 分割（P2）

**問題**: 364 行・メッセージ描画・SSE ストリーミング・セッション管理・入力 UI が同居。新しい UI モード（音声入力等）追加時の影響範囲が大きい。  
**改善案**:
- `SessionSidebar.tsx` — セッション一覧・新規作成・切替（`SessionListPane` を独立ファイル化）
- `ChatMessageList.tsx` — scrollRef・メッセージバブル描画・ストリーミング表示。`MessageBubble` は非公開で同居
- `ChatInput.tsx` — textarea・送信/停止ボタン・Ctrl+Enter ショートカット

`ChatSection` 本体は state + handler をそのまま保持し、3 子コンポーネントを組み合わせるコンテナに縮小（目標 80 行以下）。  
追加変更: `handleDelete` 内の `window.confirm` を `ConfirmDialog` 共通コンポーネントに置き換え。  
**スコープ外**: モデル選択 UI の追加・テストの新規追加・Phase 68（LibrarySection 分割）  
**変更対象**: `frontend/src/components/novel_db/ChatSection.tsx`  
**新規作成**: `SessionSidebar.tsx` / `ChatMessageList.tsx` / `ChatInput.tsx`（`components/novel_db/` 配下）  
**完了条件**: vitest 全通過・tsc エラーなし・マルチターン QA・ストリーミング表示・削除確認（ConfirmDialog）が dev server で動作確認済み

---

#### Phase 70 — `search.py` FTS5 / ベクトル / RRF を分離（P2）✅

**実装**: `search.py`（458 行）を 4 ファイルに分割し後方互換エクスポートで既存インポートを維持。`_search_types.py`（共有型・ユーティリティ）/ `fts5_search.py`（FTS5 検索）/ `vector_search.py`（LanceDB KNN）/ `rrf_ranker.py`（RRF 統合）/ `search.py`（再エクスポートのみ、47 行）。テストの `patch` アドレスを `vector_search.embed_batch` に更新。pytest 819 passed。詳細: [変更履歴.md § Phase 70](../05_記録/変更履歴.md)

---

#### Phase 71 — `generate.py` threading → asyncio 化（P2）✅

**実装**: `threading.Thread` を廃止し `asyncio.create_task` + `run_in_executor` に置き換え。`generate_pdfs` を `async def` に変更。`_run_generate_job_async()` コルーチンを追加（スレッドプール経由で同期処理を実行）。`threading` import を削除。pytest 819 passed。詳細: [変更履歴.md § Phase 71](../05_記録/変更履歴.md)

---

#### Phase 72 — 主要サービスのテストカバレッジ追加（P2）✅

**実装**: 未テストの 2 サービスに対してテストを新設（外部依存はモック化）。既存 4 ファイル（job_queue / meta_store / qa_history / qa_sessions）は計 51 件のテストが既に存在。

| テストファイル | 件数 | 主要検証 |
|---|---|---|
| `test_novel_db_retrieval.py` | 7 | full_book_mode 分岐・scope 別 summaries・クエリ展開・デデュープ |
| `test_novel_db_full_builder.py` | 11 | スキップ条件・redo フラグ・キャラクター INSERT・コールバック |

pytest 837 passed（819 → +18）。詳細: [変更履歴.md § Phase 72](../05_記録/変更履歴.md)

---

### Phase 57〜61 — 0ベース再実装候補（2026-05-14 grill-me 確定）

> **並行投入方針**: Phase 57（BE）と Phase 58（FE）は独立しているため並行着手可。残り 59→60→61 は順番に。  
> **共通完了条件**: pytest/vitest 全通過 + 分割後の新テスト追加 + 設計書更新 + dev server で手動動作確認。

---

#### Phase 57 — `novel_db.py` ルーター 6 分割 + API パス REST 化（P1）✅

**実装**: `backend/routers/novel_db/` パッケージに 6 サブルーター（`lib.py` / `character.py` / `rebuild.py` / `search.py` / `qa.py` / `chat.py`）+ `schemas.py` + `_deps.py` を作成。API パス REST 化・`require_not_locked` Depends 抽出済み。詳細: [履歴.md § Phase 57](リファクタリング履歴.md)

---

#### Phase 58 — `ReaderPanel.tsx` 5 層化（P1）✅

**実装**: `ReaderPanel.tsx`（413 行）を state / input / view 3 層に分離。`useReaderState`（全 hook 集約）/ `useReaderInput`（ショートカット入力層）/ `ReaderPageView`（コンテンツ描画）を新規作成。本体は JSX オーケストレーター（168 行）に縮小。vitest 12 件追加。詳細: [変更履歴.md § Phase 58](../05_記録/変更履歴.md)

---

#### Phase 59 — `job_queue.py` worker 分離 + 旧 JobMode DB マイグレーション（P1）✅

**問題**: 374 行でキューイング/worker 管理/DB CRUD が混在。旧モード名（`pdf_text` / `reocr`）がコード・DB に残存。  
**実装**: `NovelDbJobWorker` を `job_worker.py` に抽出（`stop_event` / `wakeup` を queue から受け取る shared Event パターン）。`start()` 時に DB マイグレーション（`pdf_text→rebuild` / `reocr→ocr`）を実行。`schema.py` のデフォルト値も `rebuild` に修正。詳細: [履歴.md § Phase 59](リファクタリング履歴.md)

---

#### Phase 60 — `summarizer.py` prompts 抽出（P2）✅

**問題**: 479 行で 1-shot と map-reduce が同一関数内に場合分けで並存。プロンプト 3 種がロジックと密結合。  
**実装**: Strategy パターン（Summarizer 基底クラス）は ROI 薄と判断しスキップ。プロンプト定数・オプション dict・`parse_combined_output()` を `_prompts.py` に抽出し `summarizer.py` は 230 行に削減。詳細: [履歴.md § Phase 60](リファクタリング履歴.md)

---

#### Phase 61 — `NovelManagePage.tsx` Amazon インポート独立化 + hook 遅延起動（P2）✅

**問題**: 447 行・state 8 個で OCR/Build ジョブ監視・Amazon CSV インポートが混在。activeTab に関係なく全 hook が常時起動。  
**実装**:
- `AmazonImportButton.tsx` を `components/novel_build/` に抽出（Dialog 化は過剰と判断、Button 止まり）
- `useNovelBuildQueue(enabled)` / `useOcrStatus(enabled)` に `enabled` フラグを追加
- `NovelManagePage` で "once-activated stays active" パターン（`buildEnabled` が `false→true` の一方向遷移）を採用し SSE 接続を Build タブ初回訪問まで遅延
- `useNovelJobStats` 抽出は統計ロジックが存在しないためスキップ

詳細: [履歴.md § Phase 61](リファクタリング履歴.md)

---

---

#### Phase 62 — ベクトル DB を sqlite-vec → LanceDB に移行（P2）✅

**問題**: sqlite-vec の `vec0` 仮想テーブルは線形スキャン（O(n)）のため、チャンク数増加に比例して検索レイテンシが増大。ANN インデックスを後から追加できない設計制約があった。  
**実装**: `lance_store.py` 新規作成（chunks/summaries テーブル、IVF_PQ 自動構築）。`migrate_to_lancedb.py` で既存データ移行（chunks 2,781 件・summaries 13 件）。D: ドライブが exFAT のため `.env` で `NOVEL_DB_LANCE_PATH` を C: NTFS へリダイレクト。pytest 817 件全通過。

詳細: [履歴.md § Phase 62](リファクタリング履歴.md)

---

### スキップ判断（参考保持）

履歴に詳細記録あり、再検討時の参照ポイント:

| 項目 | スキップ理由 | 履歴参照 |
|---|---|---|
| Phase 52-3: `useRadioMode<T>` 化 | 各 Dialog で reset 内容が大きく異なり、抽象化コスト > 利得 | [履歴 § Phase 52-3](リファクタリング履歴.md) |
| Phase 53: 中位ファイル分割 | `ReaderPanel.tsx` / `series_resolver.py` / `pdf_generator.py` の 200〜296 行は許容範囲 | [履歴 § Phase 53（スキップ判断）](リファクタリング履歴.md) |
| Phase 54-9: `response_model` 統一 | OpenAPI スキーマ運用要件が出るまで保留 | [履歴 § Phase 54-9](リファクタリング履歴.md) |
| Phase 54-10: 日時取り扱いポリシー統一 | 用途が違うため統一動機が薄い | [履歴 § Phase 54-10](リファクタリング履歴.md) |
| Phase 54-11: `app.exception_handler` ファクトリ化 | 宣言的に並べた方が読みやすい | [履歴 § Phase 54-11](リファクタリング履歴.md) |
| Phase 55-5: `bench_llm_backend.py` 配置見直し | `scripts/` との同居が許容範囲。再実行場面が未確定 | [履歴 § Phase 55](リファクタリング履歴.md) |
| Phase 55-10: `ReaderPanel.tsx` 分割 | B-16 着手時に再評価。現状で困っていない | [履歴 § Phase 55](リファクタリング履歴.md) |
| Phase 56-5: `JobMode` Literal 旧名撤去 | 既存 DB の旧モード履歴を事前調査してから判断 | [履歴 § Phase 56](リファクタリング履歴.md) |
| Phase 56-18: 折りたたみカード共通化 | 内側差異が大きく、次の履歴系コンポーネント追加時に再判定 | [履歴 § Phase 56](リファクタリング履歴.md) |

---

## 対象外（現状維持）

リスク・工数に対してメリットが小さい、または現行設計で十分な項目。

| 項目 | 理由 |
|---|---|
| Pydantic ↔ TS 型自動生成（openapi-typescript 等） | 工数大。手書き同期で運用継続 |
| `pdf_generator.execute_moves` と `file_manager` のロールバック共通化 | 対象（処理ファイル種・タイミング）が異なり、共通化メリット小 |
| `GEMMA_TOOL_DIR` の Windows パス依存デフォルト値 | Phase 8 で `.env` で上書き可能。Linux/Mac 開発予定なし |
| SearXNG → DLsite 直接検索 | 既知の問題として記録済み（Phase 12 時点で対象外維持） |
| `useLibraryManagement` のリネーム | 影響範囲が広い割に効果小 |
| `types/index.ts` の `BookMetaEntry` 拡張 | サークル名のみ管理の現行設計で十分 |
| API バージョニング | 現状シングルクライアント構成のため不要 |

---

## 新規 Phase 案テンプレート

新規 Phase 案が浮かんだら以下フォーマットで「未着手・追加候補」配下に追記する：

````markdown
### Phase NN-X — <タイトル>（P1/P2/P3）

**問題**: <現状のどこが/なぜ困っているか>
**改善案**: <具体的な変更方針。コード例があれば短く>
**変更対象**: <ファイルパスのリスト>
**新規作成**: <あれば>
**完了条件**: <テスト通過など>
````

実装完了後は本文書から削除し、[リファクタリング履歴.md](リファクタリング履歴.md) に「実績」を加えて移管する。

---

## 関連ドキュメント

- [リファクタリング履歴.md](リファクタリング履歴.md) — 完了済み Phase 1〜54 の詳細記録
- [テスト整備計画書.md](テスト整備計画書.md) — バックエンドテスト整備計画
- [テスト整備計画書_フロントエンド編.md](テスト整備計画書_フロントエンド編.md) — フロントエンドテスト整備計画
- [変更履歴.md](../05_記録/変更履歴.md) — 全変更の時系列ログ
