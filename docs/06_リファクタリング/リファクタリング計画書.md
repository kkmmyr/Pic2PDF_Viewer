# リファクタリング計画書

最終更新: 2026-04-19

---

## 完了済みフェーズ

| Phase | 内容 | 完了日 |
|---|---|---|
| Phase 1 | 定数・ユーティリティ整備（config.py / file_naming.py / constants.ts / storage.ts） | 2026-04-19 |
| Phase 2 | バックエンド責務分離（job_manager.py / file_manager.py / exceptions.py） | 2026-04-19 |
| Phase 3 | フロントエンド責務分離（useGenerateJob / LibraryDialogs / EditModePanel） | 2026-04-19 |
| Phase 4 | エラーハンドリング統一（logger.exception / FastAPI exception_handler） | 2026-04-19 |
| Phase 5 | ViewerPage Props Drilling 解消（LibraryContext.tsx 導入） | 2026-04-19 |
| Phase 6 | pdf_generator.py リファクタリング（_collect_images 共通化） | 2026-04-19 |
| Phase 7 | AutoFillService 抽出（auto_fill_service.py / meta_store.py 新規作成） | 2026-04-19 |
| Phase 8 | 環境変数化（GEMMA_TOOL_DIR を .env で上書き可能に） | 2026-04-19 |
| Phase 9 | テスト追加（test_path_utils / test_meta / test_file_manager） | 2026-04-19 |

---

## 未着手フェーズ

（全フェーズ完了）

---

## Phase 4 — エラーハンドリング統一（低リスク）

### 課題

ルーター間でエラー処理が統一されておらず、`except Exception` でスタックトレースが消える。

| ファイル | 問題 |
|---|---|
| `routers/pdfs.py:69-72` | `except Exception as e: str(e)` のみ、ログなし |
| `routers/library.py:113-117` | 全例外を 500 に集約 |
| `routers/ocr.py` | エラーハンドリングほぼなし |

### 対処

**4-1. FastAPI の exception_handler を使ったミドルウェア統一**

`backend/main.py` に追加:
```python
@app.exception_handler(FileOperationError)
async def file_op_handler(request, exc):
    logger.error("FileOperationError: %s", exc, exc_info=True)
    return JSONResponse(status_code=500, content={"detail": str(exc)})
```

既存の `exceptions.py` に例外クラスを追加し、各ルーターで `raise FileOperationError(...)` に統一する。

**4-2. logger の活用**

`except Exception` → `logger.exception(...)` に置き換え（スタックトレース保存）。

**変更対象:** `backend/main.py`, `backend/routers/*.py`, `backend/exceptions.py`

---

## Phase 5 — ViewerPage の Props Drilling 解消（中リスク）

### 課題

`ViewerPage.tsx` → `LibraryPanel` に **21個の props** が渡されており保守困難。

```
ViewerPage
└── LibraryPanel (21 props)
    └── LibraryHeader (15 props)
```

### 対処方針

**Context API でライブラリ状態を一元管理する。**

```
frontend/src/contexts/LibraryContext.tsx (新規)
  - currentPath / currentSource / selectedItems / isSelectionMode
  - ダイアログ開閉状態 (isMoveDialogOpen etc.)
  - ハンドラ関数群
```

`ViewerPage` → `LibraryProvider` で包むだけにし、`LibraryPanel` は Context から必要な値を取得する。

**変更対象:** `frontend/src/pages/ViewerPage.tsx`, `frontend/src/components/viewer/LibraryPanel.tsx`  
**新規作成:** `frontend/src/contexts/LibraryContext.tsx`

> **注意:** 影響範囲が広いため、Phase 4 完了後に着手すること。

---

## Phase 6 — pdf_generator.py のリファクタリング（中リスク）

### 課題

`services/pdf_generator.py`（241行）で ZIP / ディレクトリ処理のスキャンロジックが重複し、単一クラスに6つの責務が混在している。

```python
# 95-119行 と 136-156行 で同じ walk ロジックが重複
for root, _, files in os.walk(...):
    for f in sorted(files):
        if f.lower().endswith(SUPPORTED_IMAGE_FORMATS):
```

### 対処

**6-1. 画像収集ロジックを共通関数に抽出**

```python
# services/pdf_generator.py 内
def _collect_images(source_dir: str) -> list[str]:
    """ディレクトリから対応画像ファイルを収集する（ZIP展開後・ディレクトリ共用）"""
```

**6-2. ZIP処理と Directory処理のフロー統一**

ZIP の場合は一時ディレクトリに展開 → `_collect_images` → PDF生成 → クリーンアップ  
Directory の場合も同じフローを通るよう統一する。

**変更対象:** `backend/services/pdf_generator.py`

---

## Phase 7 — サークル名自動登録サービスの整理（低リスク）

### 課題

`routers/meta.py` が370行超になっており、自動登録ジョブの状態管理・スレッド処理・PDF収集・保存が一ファイルに混在している。

### 対処

**7-1. AutoFillService の抽出**

```python
# backend/services/auto_fill_service.py (新規)
class AutoFillService:
    def start(source: str, overwrite: bool) -> None
    def get_state(source: str) -> AutoFillState
    def _collect_targets(source: str, meta: dict, overwrite: bool) -> list
    def _process_targets(targets: list, source: str) -> None
```

`routers/meta.py` は HTTP ハンドラだけに絞る。

**変更対象:** `backend/routers/meta.py`  
**新規作成:** `backend/services/auto_fill_service.py`

---

## Phase 8 — 環境変数化（低リスク）

### 課題

`config.py` に Windows パス依存のハードコードが残っている。

```python
GEMMA_TOOL_DIR = r"D:\61.tool\Gemma 4"  # 環境依存
```

### 対処

**`.env` による上書きを可能にする**（既存の `load_dotenv` を活用）:

```python
GEMMA_TOOL_DIR = os.environ.get("GEMMA_TOOL_DIR", r"D:\61.tool\Gemma 4")
```

`.env.example` に追記:
```
GEMMA_TOOL_DIR=D:\61.tool\Gemma 4
```

**変更対象:** `backend/config.py`, `.env.example`

---

## Phase 9 — テスト追加（低〜中リスク）

### 課題

バックエンドにテストが 0 ファイル。重要なロジックが無検証。

### 対処方針

フルテストは工数が大きいため、**壊れやすい箇所に絞って追加する**。

**9-1. バックエンド優先対象（pytest）**

| テスト対象 | 理由 |
|---|---|
| `utils/path_utils.py` | パストラバーサル防止のバリデーション — セキュリティ上最重要 |
| `services/file_manager.py` | ロールバックロジックが複雑 |
| `routers/meta.py` の `_make_key` / `_has_real_author` | 自動登録の判定ロジック |

**9-2. フロントエンド優先対象（vitest）**

| テスト対象 | 理由 |
|---|---|
| `hooks/useReaderNavigation.ts` | 見開きページ計算のバグが過去に発生 |
| `utils/storage.ts` | JSON パースエラー時の挙動 |

**新規作成:** `backend/tests/test_path_utils.py`, `backend/tests/test_file_manager.py`

---

## 対象外（現状維持）

以下はリスク・工数に対してメリットが小さいため対象外とする。

| 項目 | 理由 |
|---|---|
| `useLibraryManagement` のリネーム | 影響範囲が広い割に効果小 |
| `types/index.ts` の `BookMetaEntry` 拡張 | サークル名のみ管理の現行設計で十分 |
| SearXNG → DLsite 直接検索 | 既知の問題として記録済み。工数大のため別途判断 |
| API バージョニング | 現状シングルクライアント構成のため不要 |

---

## 実施優先度

| Phase | 内容 | リスク | 推奨タイミング |
|---|---|---|---|
| **Phase 8** | 環境変数化 | 低 | 最初（1時間以内） |
| **Phase 4** | エラーハンドリング統一 | 低 | Phase 8 後 |
| **Phase 7** | AutoFillService 抽出 | 低 | Phase 4 後 |
| **Phase 6** | pdf_generator.py 整理 | 中 | Phase 7 後 |
| **Phase 9** | テスト追加 | 低〜中 | いつでも（少量ずつ） |
| **Phase 5** | ViewerPage Props Drilling 解消 | 中〜高 | 最後（影響範囲最大） |
