# 変更履歴

実装・設計・運用などの全変更を時系列（新しい順）で記録する。直近 1 週間分は本ファイルにインライン記載、過去分は週単位で 変更履歴/YYYY-Www.md にアーカイブ。

---

## アーカイブ索引

| 週 | 期間 | 主な内容 |
|---|---|---|
| **直近 (W34)** | 2026-08-17 〜 | 本ファイル下部にインライン記載（Apple Silicon MLX対応ほか） |
| [W33](変更履歴/2026-W33.md) | 2026-08-10 〜 2026-08-16 | OCR worker分割 / 書籍カード・モバイルUI / Qwen3.8・Nemotron比較 |
| [W31](変更履歴/2026-W31.md) | 2026-07-27 〜 2026-08-02 | Sol主生成・独立評価 / Gemma4比較 / B-36 / 茉莉花官吏伝18巻分析 |
| [W30](変更履歴/2026-W30.md) | 2026-07-20 〜 2026-07-26 | Kindle自動撮影・購入カタログ・OCR品質改善・大規模リファクタ |
| [W29](変更履歴/2026-W29.md) | 2026-07-13 〜 2026-07-19 | Kindle撮影安定化・Surya OCR 2・CI/依存/バックアップ修正 |
| [W28](変更履歴/2026-W28.md) | 2026-07-06 〜 2026-07-12 | 読書会番組台本生成と品質修正 |
| [W27](変更履歴/2026-W27.md) | 2026-06-29 〜 2026-07-05 | 同人誌フォルダ監視自動取り込み / 全体コードレビュー修正 / ファイルマップ生成スクリプト化 / 読書会ロングフォーム要件整理 + Phase 0a/0b 実測 / hitomi Discord 通知 |
| [W26](変更履歴/2026-W26.md) | 2026-06-22 〜 2026-06-28 | 設計書整理（リファクタリング計画書スリム化・C12 アーカイブ・FE 設計書分割） |
| [W25](変更履歴/2026-W25.md) | 2026-06-15 〜 2026-06-21 | Kindle 新アプリ（Microsoft Store 版）ウィンドウタイトル対応（TM-1） |
| [W24](変更履歴/2026-W24.md) | 2026-06-08 〜 2026-06-14 | 大規模リファクタ Phase 0〜4 全実行（uv workspace 化・CI 導入・basedpyright・pre-commit・SQLModel・Alembic・React Query 全面移行・shadcn/ui 導入 ほか）/ TM-1 起票 / B-17 保留 / バックログ棚卸し |
| [W23](変更履歴/2026-W23.md) | 2026-06-01 〜 2026-06-07 | フロントエンド依存ライブラリ更新・Playwright E2E テスト導入 / Mac クロスプラットフォーム開発対応 / Gemma モデル統一 |
| [W22](変更履歴/2026-W22.md) | 2026-05-25 〜 2026-05-31 | doujin PDF 生成を Linux サーバー完結化・Samba 共有導入 / hitomi 監視を Linux systemd timer に移行 |
| [W21](変更履歴/2026-W21.md) | 2026-05-18 〜 2026-05-24 | Phase 83〜87 リファクタ（llm.py 分割・useNovelManage 統合・character_summarizer 分割）/ iPad・PWA 対応 / nginx リバースプロキシ導入 / localStorage→DB 移行 |
| [W20](変更履歴/2026-W20.md) | 2026-05-11 〜 2026-05-17 | Phase 55〜78 全実行 / LanceDB 移行 / React 19 + Zustand / Alembic 導入 / テスト整備 §10 全件完了 ほか |
| [W19](変更履歴/2026-W19.md) | 2026-05-04 〜 2026-05-10 | Phase 49〜54 共通化棚卸し / RAG 全文検索本実装 / B-5/B-8/B-9/B-11〜14 採用 / フロントテスト整備 / image-only モード |
| [W18](変更履歴/2026-W18.md) | 2026-04-27 〜 2026-05-03 | Phase 13-17 / Phase 19-38 / hitomi 監視機能 / .claude 拡充 / Phase D デザイントークン |
| [W17](変更履歴/2026-W17.md) | 2026-04-20 〜 2026-04-26 | Phase 10-12 リファクタ / uv 移行 / 最近見た順ソート / 閲覧回数機能 |
| [W16](変更履歴/2026-W16.md) | 2026-04-13 〜 2026-04-19 | Phase 1-9 リファクタ基盤 / 見開き 3 段階 / 書籍メタ機能 / OCR 品質改善 |
| [W15](../archive/変更履歴/2026-W15.md) | 2026-04-06 〜 2026-04-12 | リファクタリング フェーズ 4（品質向上・将来対応） |
| [W10](../archive/変更履歴/2026-W10.md) | 2026-03-02 〜 2026-03-08 | リファクタリング フェーズ 1-3 |
| [W03](../archive/変更履歴/2026-W03.md) | 2026-01-12 〜 2026-01-18 | Novel OCR 機能追加（プロジェクト初期） |

---

## 追加ガイド

新規エントリは本ファイル下部「直近の追記」セクションの**先頭**（最新が上）に `## YYYY-MM-DD: type — タイトル` 形式で追記する。`type` は `feat` / `fix` / `refactor` / `chore` / `docs` / `test` / `perf` / `style` / `revert` / `change` のいずれか。詳細フォーマットは `/changelog` スラッシュコマンドが直近コミットから草稿を生成する。

週境界（毎週月曜日）を跨いだら、本ファイル下部の前週分を切り出して `変更履歴/YYYY-Www.md` を新規作成し、上記「アーカイブ索引」テーブルに 1 行追加する。

---

## 直近の追記（2026-W34: 2026-08-17 〜）

## 2026-08-22: chore — Linux週次復元unitの共通backup lock反映を完了

- root所有の`/etc/systemd/system/backup-restore-test.service`をrepositoryの共通`flock`版へ更新し、配置元・配置先のSHA-256一致を確認した。
- `daemon-reload`後の手動復元試験は2026-08-22 21:24:59 JSTに開始し、約4秒で`Result=success`、`ExecMainStatus=0`となった。週次timerも`enabled / active`を維持した。
- B-37のroot gateを完了扱いとし、LanceDB ICU本番昇格の残条件を実利用shadow観測と利用者の別承認へ限定した。解消した項目は既知の問題から除外した。

## 2026-08-22: fix — Kindle package validatorの型安全性を復旧

- `warning_policy_version`が文字列でない不正manifestを明示的にfail closedで拒否し、未hashable値による内部例外を防いだ。
- 短時間overlay候補の最大MAD値を数値へ型絞り込みし、真偽値を許容せずに既存の上限検査を維持した。
- malformed policyの回帰テストを追加し、backendのbasedpyrightを0エラーへ戻した。

## 2026-08-22: docs — Kindleシリーズ失敗時の現行停止方針を確定

- `download_failed`は、最初の失敗でシリーズ全体を停止し、後続jobを作成しない現行挙動を当面の正式方針とした。
- 失敗巻だけを隔離して後続巻を継続する方式は将来候補として残し、再試行間隔・最大継続数・恒久的な非対応書籍・resume整合性を別変更で設計する。
- B-32の残作業を、キャプチャ品質contact sheetの独立人手確認と実陽性を含む追加holdoutへ絞り込んだ。

## 2026-08-22: docs — Qwen3.8 MLX-dsparkの実機知見を設計正本へ反映

- M1 Max 64GBで確認したruntime version、Qwen3.8 targetとDFlash2 cacheの規模、初回drafter取得・Metal kernel calibration、`/health`の期待値をADR-0019とGPU環境セットアップへ反映した。
- OpenAI互換短答、共通Backendの限定JSON、bge-m3の1024次元Embeddingというtransport smokeの結果を記録し、再起動時の判定材料を固定した。
- runtime疎通と小説品質を分離し、Ollama Q4_K_Mで得た既存のQwen3.8品質不合格を維持した。MLX-dsparkの採否更新には、同一固定本文・prompt・sampling・上限・seedと同じ機械ゲートによる再比較を必須とした。
- Mac開発環境とRAGパイプライン設計へ11439生成／11437 Embedding分離を追記し、Macのopt-in比較に限定してWindows/Linux既定値と自動公開を変更しない方針を明記した。

## 2026-08-22: feat — Qwen3.8のMac MLX-dspark経路を追加

- Qwen3.8-27B 4bitをMacで比較するため、`mlx-dspark`用の専用Backendと`mlx_dspark`設定を設計した。既存の`mlx_vlm.server`とはthinking契約が異なるため、nested `chat_template_kwargs.enable_thinking`とsampling名を共通層で扱う。
- `mlx-dspark`が`response_format`を生成制約に使わないことを踏まえ、`format="json"`を明示したJSON objectタスクは自然停止後の限定JSON adapterでfail closedにする。説明文、複数fence、重複キー、途中切れを受理しない。関係抽出のJSON配列は対象外とする。
- 生成を127.0.0.1:11439、bge-m3のEmbedding-only serverを127.0.0.1:11437へ分離し、Qwenの大きな重みを二重常駐させないMac運用値（131,072 context / KV 8bit / 同時生成1）を固定した。
- Windows/Linuxの既定backendとQwen3.8の本番品質判定は変更せず、固定小説品質ゲート合格まではMacの比較・QA用opt-inに限定する。`full_build` / `generate_relations`と、Gemma役をQwenへ寄せた`generate_contexts`はjob開始前に拒否し、公開SQLite行を更新しない。
- `.env.example`へ11437のMLX Embedding backend・modelを含む完全な組合せを記載し、dspark具象で同期・非同期JSON成功と未終端・重複キー・length終了の拒否を回帰testへ固定した。

## 2026-08-22: docs — 長時間作業後の設計実装整合を再監査

- `70d3219`から`1e91de0`までの81ファイルを横断確認し、Kindle品質warning、manifest v1/v2互換、
  シリーズ画面数外れ値、全active job抑止、Library / OCRのHTTP境界、LanceDB shadowの主要契約は
  要件・詳細設計・API・実装で一致していることを確認した。
- Linuxサーバーバックアップの対象に`kindle_catalog.db`が含まれる実装事実をバックエンド詳細設計へ
  追記し、購入カタログ、capture job、登録後画像QA warningも同一世代で保存・復元検査する契約を
  明確化した。Kindle要件書と共通詳細設計の検証日も2026-08-22へ更新した。
- B-32の状態をwarning永続化・確認導線・短時間右下通知・シリーズ画面数外れ値まで実装済みへ更新し、
  残gateをcontact sheetの独立確認、実陽性holdout、`download_failed`後続継続方針の確定に整理した。

## 2026-08-22: feat — Kindle撮影の短時間右下通知を警告

- 等間隔の反復標本では見逃し得る連続3画面だけの通知実例を再確認し、全ページをrolling走査する
  `transient_bottom_right_overlay_candidate`をwarning-onlyで追加する。
- 一般外周の近似一致は正常漫画・小説の繰り返し意匠を誤検出したため採用せず、実陽性2種に共通する
  右下2タイルへ責務を限定した。実陽性2種を検出し、対応正常203画面と封印holdout524画面の
  計727画面では誤警告0、MAD閾値を4から6へ緩めても同じ結果だった。
- 新agentはwarning / overlay policy v2を生成し、backendは段階更新用にv1組も受信する。
  新warningをv1へ混入したpackage、候補数・連続ページ・右下bounds・MADが整合しないpackageは
  拒否し、既存の登録・OCR・画像削除・後続撮影は変更しない。

## 2026-08-22: feat — Kindle撮影warningの確認導線を追加

- 成功jobのquality証跡を監査世代として`kindle_catalog.db`へ保存し、同一codeのfindingを
  要確認候補へ集約する。再撮影時は旧世代を削除せずsupersedeし、warning 0件の新世代も保存する。
- capture画面で未確認／確認済みを管理し、comic / novelの候補ページを既存readerで直接開ける。
  warningは登録成功、OCR、画像削除を変更しない補助情報として表示する。

## 2026-08-22: fix — 他ASIN処理中のKindle撮影開始を事前抑止

- 購入書籍詳細でactive capture jobをASINで絞らず確認し、他ASINが処理中の場合も
  「撮影して取り込む」を無効化する。同一ASINと他ASINの説明を分け、処理中jobへの
  導線を表示する。
- 確認ダイアログ表示後にpollingでactive jobが判明した場合も確定ボタンとhandlerを停止し、
  backendの全source・全ASIN最大1件transaction制約を競合時の最終防衛線として維持する。

## 2026-08-22: feat — Kindleシリーズ画面数外れ値を警告

- シリーズ直列撮影で同sourceの成功実績を3冊以上参照し、新しい画面数が中央値の
  0.5倍未満または2倍超の場合にwarning候補を生成する。comic / novelを混ぜず、
  長編・短編の正常差を考慮して登録や後続jobは止めない。
- policy version、ASIN、撮影数、reference統計、比率をsession stateへatomic保存し、
  resume後も画面数実績とwarning証跡を引き継ぐ。成功応答の画面数が不正な場合はfail closedする。
- session stateをschema v2へ更新し、完了ASINと画面数、warningのsourceと撮影数をresume時に
  再照合する。必須証跡がないv1または改変stateは暗黙補完せずresumeを拒否する。

## 2026-08-22: test — Kindle撮影警告の未調整holdoutを実測

- 検出結果を参照しない固定seed・タイトルfamily hashで本番画像4冊・524画面を選び、画像SHA、
  Codex視覚label、実測reportをprivate artifactとして封印した。実欠陥labelは0件、警告26件は
  すべて正常候補で、発火した4 codeの暫定precisionは0、recallは陽性不足により未測定となった。
- hardlink一時コピーへ完全重複、白紙化、微差重複、上端切れを固定レシピで注入し、5 codeすべて
  TP 1 / FN 0を確認した。制御故障のrecall 1.0は実陽性の人手labelと分離して記録する。
- 完全重複以外のraw warningはblocking化せず、将来UIを登録成功後の「要確認候補」に限定する。
  holdout評価器はlabel/source/fileの厳格検証、構造監査失敗時のfail-closed、specからの原子的な
  manifest生成CLIを追加し、不正caseを予測0件として集計しない。`provenance`で実画像・制御故障、
  reviewer種別、人手確認状態、関連digestをmanifestからreportへ固定する。
  contract検証と監査・指標集計は別moduleへ分け、複雑度と400行制限の範囲内に保つ。

## 2026-08-22: refactor — Library手動toolのHTTP境界を分離

- `AmazonImportButton`と`ToolsMenu`から`apiClient`、手動pending state、Blob download処理を除き、
  `features/library/api.ts`とTanStack mutation hookへ移す。Amazon取込はOpenAPI生成
  `AmazonImportResponse`を正本とし、interceptor後の本文を`.data`で二重unwrapして件数を0扱いする
  不整合を解消する。
- endpoint、`novel` / `comic`の独立実行、成功分の集約、toast文言、export filenameを維持し、
  hook/API境界のunit testで成功・部分失敗・全失敗・pending・Blob URL解放を固定する。
- OCR status / run / stopも`features/ocr/api.ts`へ集約し、生成API型に統一する。
  単冊`target_dir`を従来のrequest bodyからOpenAPI契約のquery parameterへ直す。
- OCR設計書のsource名を現行`doujin / comic / novel`へ直し、z-index tokenの正本をTailwind v4の
  `frontend/src/index.css`へ修正する。加えて現行job queueと矛盾するUI / OpenAPIのOCR説明、
  backendの旧source名comment、comic設定定数名の設計ドリフトを解消する。
  C-8とB-28は着手前監査で既に正しい状態だったため変更しない。
- 対象test 13件、Node.js 22.23.2でfrontend 1,089件、backend 61件、TypeScript、build、
  ESLint（既知13 warningのみ）、Knip、Ruff、OpenAPI 102 paths、docs / file map検査を通過した。
  実ブラウザは1440×900 / 390×844で横はみ出し0、tool menuのEscape閉じ、
  Amazon / OCR job queue表示、console error 0件を確認した。TM-4は完了履歴へ移した。

## 2026-08-22: fix — 本番LanceDBの全テーブルを固定パスへ復旧

- storage修正版commit `a035625`を含むclean generation
  `/opt/pic2pdf-viewer/backend-20260822163430-62582`をactive化し、LanceDBを
  `/opt/pic2pdf-viewer/data/novel.lancedb`へ固定した。activation smoke、service / public HTTP、
  CI run `32559772894`はすべて合格した。
- 移設前はpage ICUがactive release、`chunks` / `summaries`が直前releaseに分かれていたため、
  page-onlyコピーを完全版と扱わず、旧実体・検証済みbackupとのschema / 行multiset一致を確認した。
  共通backup lock、writer 0件、service一時停止下で`chunks=3,489`、`summaries=18`、page ICU `8,576`を
  統合してatomic swapし、dense 2 tableのvector検索、ICU検索、HTTP、再起動後error 0件を確認した。
- 復旧後snapshot `2026-08-22_164434_post-lance-repair`はSQLite 3種とLanceDB 3 tableの
  別directory復元試験に合格した。制御shadow 5件は成功5 / fallback 0。残る本番切替gateは
  root所有週次復元unitの共通lock反映、自然な実利用shadow観測、別承認の3点である。

## 2026-08-22: fix — LanceDBをversion付きbackend世代から分離

- AppleDouble修正版の新世代はCI・backup・venv・migration・HTTP probeまで合格したが、
  `NOVEL_DB_LANCE_PATH`省略時の既定値が新backend配下を指し、SQLite active pointerに対応する
  page ICU tableを失った。制御shadow smokeは5 / 5 `PageFtsUnavailable`となり、利用者返却はFTS5へ
  縮退した。writer 0件を確認して旧backend / commonへatomic rollbackし、ICU 10件取得、HTTP 200、
  warning 0件を再確認した。
- LanceDBの既定値を`NOVEL_DB_DIR`の兄弟へ変更し、通常開発pathは変えず、固定`NOVEL_DB_DIR`を持つ
  Linuxではrelease世代外へ永続化する。明示的な`NOVEL_DB_LANCE_PATH`は引き続き最優先する。
- activation smokeはLance pathが新backend配下なら切替を拒否し、`shadow` / `lance_icu`では
  active ICU tableのno-match検索まで実行する。固定pathへの全table統合と再deployは直上の復旧記録で完了した。

## 2026-08-22: fix — Mac由来AppleDoubleを本番切替前に排除

- holdout受入後の本番deployで、macOSのtarが生成した`._*` 453件をLinux stagingへ転送し、
  Alembic互換検査が`._0012_ocr_ground_truth_cer_cache.py`を未承認migrationとして拒否した。
  active symlink切替前にfail closedし、停止した旧service processを再開して旧backend世代の
  HTTP応答を維持できた。
- release archiveはACL・extended attribute・AppleDouble metadataを保存せず、明示的な
  `._*` / `.DS_Store`も除外する。さらにactivation preflightでbackend / common / workspaceを
  検査し、AppleDoubleが1件でもあればwriter停止・backup・venv構築・migrationより前に拒否する。
- archive helperの実行testで通常fileだけが格納されること、activation scriptの構文と
  preflight順序を回帰契約へ固定する。汚染した失敗世代はactive pathでないことを確認後に破棄し、
  cleanな新世代で再deployする。

## 2026-08-22: test — 未調整holdoutでLanceDB ICU採用ゲート合格

- 封印commit `89fc93e`をpush後、別3冊12問を一度だけ開封した。FTS5は12 / 12 0-hit、ICUは
  Recall@10 `.792`、MRR `.833`、nDCG `.748`、Recall@30 `.972`、0-hit 0件、p95 3.486ms、
  個別Recall@10回帰0件で、事前定義した品質・latencyゲートを通過した。
- ICUで正解が12位だった1問は修正せず失敗例として固定した。結果JSON SHA-256は
  `68d05ecf47d8e4f3f0f43ba8f37d09465966c4747be6f34f000403a6ca6ef669`。本文snapshotと隔離index
  396MBは評価後に削除し、本番返却はFTS5のまま維持した。
- backendファイルマップを生成器で更新し、shadow集計・holdout検証scriptをclean checkoutの
  freshness gateへ反映した。

## 2026-08-22: test — B-37未調整検索holdoutを評価前に封印

- 既存20問と異なる3シリーズ3冊について、本文位置20% / 45% / 70% / 85%を検索品質非参照で先に選び、
  本文と隣接pageだけから自然文query・関連page・relevanceを付けた12問fixtureを作成した。
- production page ICU source hashと全関連page本文hashをfixtureへ封印し、不一致、page欠落、集合差分では
  index構築・検索前にfail closedとする検査を追加した。このcommitをpushするまで方式別順位は開封しない。
- fixture SHA-256は`8e8b3ccde781e90b2a7a13af21da356163f29a510288a4dbe230e9fba55fc86b`。
  production read-only照合はsource一致、関連page 27件、本文hash不一致0件で、検索は未実行。

## 2026-08-22: feat — LanceDB ICU shadow観測をJSON集計

- production logの`lexical shadow` / `lexical shadow unavailable` / `lexical ICU fallback`から、
  成功・fallback率、unique query数、FTS5 / ICU latency、0-hit、top集合Jaccardを集計するCLIを追加した。
- query本文とhash一覧は出力せず、ISO日時で対象期間を限定できる。shadow観測0件は
  `status=insufficient_data`・終了code 2とし、無観測を受入合格にしない回帰testを追加した。
- productionのsmoke 10件と障害注入1件を別時間窓で正しく集計し、以後の実利用観測0件は
  `insufficient_data`になったため、運用観測ゲートは未合格として維持した。

## 2026-08-22: fix — FTS5の同点順位をcanonical page IDで固定

- productionのFTS5検索と検索評価CLIを`BM25 score ASC, pages.id ASC`へ統一し、同点時のtop-kが
  connectionやrestartを跨いで揺れない契約にした。BM25 scoreが異なる結果の順位は変更しない。
- 同一本文・同一scoreを持つpageを逆順に登録する回帰testで、canonical page ID順と複数回実行の
  完全一致を確認する。解消済みの項目を既知の問題から除外した。

## 2026-08-22: docs — TM-3をGitHub Actions全job greenで完了

- GitHub Actions run `32557048520`でsecrets / backend / frontend / docsの4 jobをgreenにし、依存監査、OpenAPI、import boundary、保守資産、format、basedpyright、backend coverage、Kindle、common LLM、Vitest、buildの全段が実行されたことを確認した。
- TM-3の起票理由・修正単位・検証値・commitをリファクタリング履歴へ凍結し、living計画から除いた。次の最優先はB-37 LanceDB ICUの本番受入とする。

## 2026-08-22: fix — OpenAPI contractをclean checkoutで決定的に生成

- GitHub Actionsのclean checkoutで`frontend/dist`がないと、SPAの`/`と`/{full_path}`がFastAPIから登録されず、release treeから作ったOpenAPI baselineと2 path差分になることを特定した。
- SPA handlerをbuild成果物と無関係に常時登録し、未build時は実行時に`404`でfail closedとする。`/assets`のstatic mountは実directoryがある場合だけを維持する。
- `FRONTEND_DIST_DIR`が存在する環境と存在しない環境で同じOpenAPI digest / 102 pathsになること、未build時のindex / catch-allが404になることを回帰testにした。

## 2026-08-22: fix — Kindle testのクロスプラットフォーム境界を復旧

- Windows専用の`windll` / `WINFUNCTYPE` / `COMError` / UI Automationと、ヘッドレス環境でimportできないPyAutoGUIを`kindle_platform.py`へ集約した。非WindowsでもKindleのポリシー・状態遷移・エラー契約を収集し、未モックの実機APIは明示的にfail closedとする。
- `uiautomation`依存をWindowsに限定し、GUI入力・Win32ハンドル・COM例外のtest doubleがmacOS / Linuxでも同じ呼び出し境界を使うようにした。Windows実機のKindle接続と撮影受入は引き続き非Windows testの対象外とする。
- macOSのKindle package全testとLinux CIの同一test stepを合格条件とし、import時のOS固有例外で後続gateが未実行になる状態を解消した。

## 2026-08-22: fix — backendの静的型gateを全件復旧

- Kindle Alembic migrationが`sqlmodel.sql` package属性経由で`AutoString`を参照していた76件を、同一具体型の直接importへ置き換えた。生成migrationを除外せず、DDLとrevision chainは変更していない。
- SQLite `lastrowid`の`None`、旧DB移行トークンの期限型、Novel書籍metaの空既定値を明示的に検査・宣言し、本番コードの6件をcastなしで解消した。
- `basedpyright`は82件から0件、Ruff lint / format、Kindle migration / import、Novel libraryの対象testは全件合格した。

## 2026-08-22: chore — Python依存監査と保守資産gateを復旧

- ONNXの公式advisory 2件とOSV上のalias重複を確認し、`uv.lock`を1.21.0から修正版1.22.0へ限定更新した。ONNX / onnxscriptのimport、model serialize / load、checkerとproject dependency auditを合格させた。
- 解消したONNX例外を`uv-audit-allowlist.toml`から撤去した。CUDA 12.1・yomitokuとの同時更新が必要なPyTorch例外だけを2026-10-18期限で維持し、未記録脆弱性0件を確認した。
- B-35 formal holdoutのmanifest・ledger・seal CLIを継続品質資産、Ornith MLX評価器4件と専用test 2件を2026-11-16期限の一時資産として、ownerと撤去判断を一意に登録した。
- 既存のPython format差分1件を解消し、maintenance asset、Ruff lint / format、対象testで回帰を確認した。

## 2026-08-22: chore — frontend CIのruntimeとclean installを再現可能化

- Node.js 22.23.2とnpm 10.9.8を`.node-version`、`packageManager`、`engines`で固定し、GitHub Actionsが同じ正本を読み取って実versionをfail-closedに検査するようにした。
- Node.js 22 + npm 10で`package-lock.json`を全再解決し、欠落していた`@emnapi` optional dependencyを回復した。`npm ci`はlockfileを書き換えずにclean installできる。
- `npm audit`が示した修正可能な脆弱性は`--force`を使わずに解消し、同じruntimeでlint、Knip、TypeScript、Vitest、production buildを再検証した。

## 2026-08-22: docs — CI復旧と残作業の優先順位を固定

- 全体監査に基づき、P0をCI・依存監査・runtime再現性の復旧、P1をLanceDB ICU本番受入、P2を設計ドリフト解消、P3以降をキャプチャ・OCR・生成文QAとする横断順序を固定した。
- GitHub Actionsの`npm ci`、dependency audit、保守script未分類、format、basedpyright、Windows COM依存testをTM-3の一連の復旧対象とし、clean checkoutの全job greenを完了条件にした。
- 設計ドリフト4件と計画状態2件をTM-4へ登録した。C-8はPlaywright E2E導入済みとして実装済み履歴へ移し、B-28は技術実装済み・主観受入待ちとして残作業を1回の評価へ限定した。
- B-37は共通lock版復元service、FTS5の決定的二次sort、shadow指標、未調整holdout、別承認での`lance_icu`切替の順に再整理した。
- frontendの正規検証runtimeをCIと同じNode.js 22 + npm 10とし、通常のclean installで`npm ci`を使う品質契約を追加した。ソース、DB、index、本番設定は変更していない。
## 2026-08-22: test — Google Document AIを小説縦書きOCR候補として不採用

- 開封済みtuning標本19冊30画面をEnterprise OCR `pretrained-ocr-v2.1.1-2025-01-31`で最大3並列処理し、30/30画面のsample集合、画像SHA、raw response、保存本文の対応を検証した。
- 独立画像checker採用参照のある8画面では既存OCRが8/8で最も近く、Googleはルビ混入、縦列読順崩壊、約物変形を示した。差分は人手ground truthによるCERではなく、開封済み調整用比較として記録した。
- 現versionの全冊展開・canonical昇格を不採用とし、Google結果はDBへimportしなかった。active publicationと既存公開本文は変更していない。
- OCR設計書、ADR-0021、Sol campaign実行計画、OCR技術知見へ、実測値、失敗形態、再評価境界を反映した。
- Sol campaign・holdout・import・CLIは本番APIではない期限付きtuning資産としてownerと2026-11-16再評価日を台帳へ登録した。publication履歴と通常OCR公開処理は継続的なsize/C901品質ゲート対象に残す。

## 2026-08-22: change — 個人利用前提でLanceDB ICUの本番shadow導入を承認

- productionを信頼済みoperator 1名の個人環境と位置付け、短時間停止、手動shadow監視、旧index / venv世代の手動整理、query hashの限定的な推測リスクを受容した。高可用性・自動alert・自動GCは初回導入条件から外した。
- 個人利用でもdependency lock、writer 0件、backup / restore検査、manifest整合、FTS5 fallbackは省略しない。ルートworkspace lockからsource・common・venvを含む別backend世代を構築し、smoke後にactive symlinkを切り替えるrollback可能なdeploy方針へ更新した。定期backup・復元試験・deploy前backupは共通lockで直列化する。
- additiveなAlembic `0014`だけを直前backend世代にも配置してから適用し、DB revision適用後も旧世代を起動可能にした。未承認migrationが増えた場合は自動deployを停止するfail-closed条件を追加した。
- 初回本番preflightでSSHの作業directoryから`config`をimportできない問題を検出した。DB操作・世代切替前に安全停止したことを確認し、writer検査をactive backend directory固定で実行するよう修正した。
- 2回目の本番試行では検証済みbackup後にactive appを再開し、staging workspaceのmember不足を`uv sync --locked`が拒否した。`kindle-pdf/pyproject.toml`もstagingへ配置して3 memberのlock構成を再現し、同じstaging上の`uv lock --check`で143 packageの整合を確認した。
- commit `c31822f`で3回目の本番deployに合格した。検証・復元済みbackup、LanceDB 0.34.0、世代別source / common / venv、Alembic 0014、FTS5 / HTTP smokeを確認し、旧backend releaseをrollback用に保持した。
- production page ICUを8,576行で完全構築し、source hash、active revision、row数、ICU index統計、SQLite integrityを検証した。`NOVEL_DB_LEXICAL_BACKEND=shadow`へ切り替え、5 probeのFTS5同一object返却とICU例外時fallbackを確認した。`lance_icu`への昇格は行っていない。
- 運用smokeで既存FTS5の同点順位に二次sortがなくcross-run digestが揺れること、週次復元serviceの共通lock定義はroot所有unitへの反映待ちであることを既知課題へ登録した。

## 2026-08-22: docs — LanceDB ICU BM25の運用状態とshadow導入手順を正本化

- 検索QA設計へ、本番未適用という現在地、バックアップ確認、構築CLI、manifest検査、shadow起動、観測、別承認までの順序を追記した。stale時の完全再構築、FTS5への即時rollback、旧世代GC未自動化も運用境界として明文化した。
- 本番read-only監査でLanceDBが`0.30.2`、実装契約が`>=0.34,<0.35`である一方、既存deploy scriptはPython dependencyを同期しないことを確認した。依存同期・import / FTS5 smokeを本番migration前のblocking preflightとし、LanceDB書込みとdirectory backupが重ならないmaintenance windowを必須化した。
- データ設計とバックエンド設計へ、ICU構築がGPU / embedding / LLMを使用しないこと、configured DB pathの事前確認、lexical選択境界、API / RRF非変更を反映した。
- 自動生成されたバックエンドファイルマップをAlembic 0012〜0014、`page_fts.py`、構築・評価CLIの現状へ同期し、非自明な運用ファイルを補足表へ追加した。ADR-0020にもproduction未適用状態を明記した。ソース、DB、index、環境変数は変更していない。

## 2026-08-22: feat — LanceDB ICU BM25の世代管理shadow経路を追加

- SQLite `pages`を正本に、`index_eligible=1`のpageを世代別LanceDB tableへ完全再構築するCLIを追加した。page ID一意性、件数、canonical SHA-256、FTS index行数、未索引0件を満たす場合だけSQLiteのactive pointerを条件付き更新する。
- Linux配置済みDBがAlembic 0013、作業ツリーが0011までというchain欠落を実snapshot試験で検出した。サーバー上の正式な0012（CER cache）/0013（OCR公開履歴）migrationを原文のまま復元し、対応するSQLModelを補完したうえで、`novel_search_index_state`をrevision 0014へ追加した。
- OCR QA公開、legacy OCR保存、補正ページ再構築がcanonical本文変更と同じtransactionでICU indexをstale化するようにした。同時更新・構築失敗では旧active pointerを維持する。
- lexical経路へ`fts5 / shadow / lance_icu`を追加した。初期既定値とshadowの返却順位はFTS5のまま、shadow logはquery hash・件数・上位重複・latencyだけを記録し、ICU欠落・stale・例外時はFTS5へ縮退する。
- 既存`chunks` / `summaries`、bge-m3、API response型は変更せず、LanceDBを検証済み`>=0.34,<0.35`へ制約した。世代構築・scope・snippet安全化・同時更新拒否・stale・shadow同値・障害fallbackを回帰testへ固定した。
- 配置済み0013 snapshotの別scratchで8,576ページを構築し、固定20問top 30が旧ICU評価と20/20完全一致、未索引0、SQLite integrity `ok`、canonical検索p95 13.239msであることを確認した。原本hashは不変で、約414MBのscratchは復元可能なゴミ箱へ移した。
- 対象test 75件とbackend全体1,338件（1件skip）、ruff全体、変更範囲basedpyright、docs整合、変更範囲format、差分検査が合格した。全体basedpyrightには今回と無関係な既存83件が残る。
- ADR-0020、基本設計、検索QA設計、データ設計、バックログB-37へ段階導入とrollback境界を反映した。production既定値の`lance_icu`切替は実運用shadowと未調整holdout後の別判断として保留する。

## 2026-08-22: docs — 日本語検索基盤の実測知見と移行境界を正本化

- 横断監査で見つかったGate Bの記述差を修正し、候補被覆はICU + ngram RRF top 30とbge-m3 top 30の和集合で診断した一方、bgeが正解ページを追加しなかったため実reranker入力はlexical top 30の600 pairだったことを明記した。
- lexical / denseは1 warmup + 3計測、rerankerは1 model warmup + 固定候補1巡という実測契約へ修正した。推論process排他はCLIが強制しないoperator前提であることも記録した。
- dense corpusがSQLite全81冊ではなく32冊・3,060ページに限られること、固定fixture / SQLite / 全モデルrevision、再現順序、一時SQLite・LanceDB・NPZの保持／再生成方針を検索QA設計へ追加した。
- 基本設計に現行FTS5 + bge-m3と未導入のpage-level ICU候補を分離して記載し、ADR-0019の「既存LanceDBを再構築しない」はbge-m3 runtime切替時の互換性判断であって保存済み索引の完全性保証ではないことを追補した。
- バックログB-37へ全対象pageの原子的再構築、SQLite整合、shadow評価、FTS5 fallback、rollback、API回帰testを起票した。Nemotron・reranker等は対象外とし、production DB / index / API設定とソースコードは変更していない。
- `docs-cross-checker`で設計・実測値・評価器を読み取り専用で横断確認し、評価CLIとfixtureを技術知見の関連ファイルへ追加した。

## 2026-08-22: test — 日本語検索基盤の段階比較ゲートを固定

- Linuxの検証済みバックアップをMacの一時領域へread-only複製し、現行コーパスが81冊・9,913ページ・SQLite 6,238チャンク、LanceDB 3,489チャンクで、SQLite整合性がokであることを確認した。本番DB・バックアップ原本・公開索引は変更していない。
- 現行FTS5、LanceDB ICU/ngram BM25、Qwen3 reranker、bge-m3/PPLX/Nemotron Embed/Qwen3 Embedding/Harrierの順で、同一正解集合とRecall@K・MRR・nDCG・latency・memoryを比較するfail-closed手順を検索QA設計へ追加した。
- 既知6問は診断専用とし、本番採否には3冊・20問以上のblind fixture、Recall@10非劣化、MRRまたはnDCGの相対5%以上改善、warm run 3回を要求する。モデル比較は別LanceDB tableとindex manifestを使い、現行1024次元索引を上書きしない。
- Harrier公式BF16 checkpointのスモークテストで、MLX BF16配列をNumPyへ直接変換するとPEP 3118 buffer形式エラーになり、FP32搬出後にも最大約0.5%のL2 norm誤差が残ることを確認した。MLX上でFP32へ明示変換し、shape・有限値・非ゼロnormを検証してFP32で再正規化する共通境界へ修正する方針を固定した。モデル重みや評価条件は変更しない。
- Harrier BF16は単体入力16件がすべて正常でも、同じ入力の特定2件以上のバッチで非有限値を返した。第三者8bit版のモデルカードには公式と異なるprefix例と誤った規模別スコアがあるため直接採用せず、immutable revisionを固定した公式重みからローカルFP16変換を作り、source・converter・dtype・hashを記録して同じ検索ゲートへ通す方針とした。
- Gate AではLanceDB ICUがR@10 0.886、MRR 0.720、nDCG 0.708、p95 2.12msで個別回帰なく合格した。現行FTS5はR@10 0.090・18/20問0-hit、ngramとICU+ngramはR@10回帰1件で不採用とした。
- Qwen3-RerankerはMRRを0.685から0.816へ改善したがR@10回帰2件で不採用とした。PPLX、Nemotron、Qwen3 Embedding、Harrierまで全6,238チャンクを切り詰め0件で比較し、Nemotronだけがbge比で個別R@10回帰0件・MRR +8.17%・nDCG +12.19%を同時に満たした。
- Nemotron top 30をlexical候補へ加えても新しい正解ページは0件でcandidate R@30は0.963のまま、平均候補数だけ43.05へ増えた。このため本番候補はLanceDB ICU単体、Nemotronはdense比較候補として保留し、reranker・PPLX・Qwen3・Harrierは配線しない。既存LanceDBは重複32行・SQLiteから2,781 ID欠落のため再利用せず、本番切替は8,576ページの完全再構築とfallbackを別変更で行う。
- Harrierは公式BF16からlocal FP16へ変換し、元BF16とのtoken ID一致・問題16件のcosine最小0.99991を確認して全件を10分20秒で完走したが品質ゲートは不合格だった。変換由来manifestのhash検証、BF16→FP32搬出・再正規化、検索・reranker・embedding評価器と回帰testを追加した。本番DB・索引・API設定は変更していない。
- 評価器の対象test 28件、backend全体1,327件（1件skip）、ruff lint、docs整合は合格した。全体format checkだけは今回未変更の`generated_content_audit.py`、`summary_grounding.py`、`test_generated_content_audit.py`に既存整形差があり、対象12ファイルが整形済みであることを確認して非対象3ファイルは変更しなかった。

## 2026-08-22: test — Ornith根拠引用診断を不合格とし根拠ID方式を設計

- Gate B3で残った主体混在、推薦人関係逆転、理由誤帰属、人物台帳誤リンクを、JSON形式ではなく意味グラウンディングの課題として設計書へ反映した。
- 公式資料・一次研究・利用者報告を分けて調査し、Ornithのreasoning既定、Thinking budgetの停止制御、grammarの構文制約、span単位の根拠照合を確認した。
- 不採用判定を変更しないGate B4として、固定6ページを単ページ・固定質問で処理し、連続原文引用と引用内主体を決定的に検査するプロトコルを実行前に固定した。
- 6/6ページが自然停止・raw厳格JSONへ到達した一方、12引用中の完全一致は5件、主体包含は6件、両方の合格は1件だけで、ページ単位0/6、固定意味条件10/14となった。推薦人関係、衝突主体、同一人物疑惑、荷物検査許可も欠落した。
- 人物台帳を同じ生成から除外すると既知の誤リンクは再発しなかったが、直接引用方式は不採用とした。次期候補はアプリが原文spanへ安定IDを付け、LLMにはSchema enumのIDだけを選ばせる方式とする。
- 評価器と4件のGate B4 unit testを追加し、既存3件を含む評価器7件、共通LLM 69件、backend 1,299件（1件skip）、docs整合、lint、format、差分検査が合格した。開始前後のswapは1,168.81MiBで不変、終了後memory freeは96%、server停止後のTCP 11440と関連processは0件だった。既存Qwen、本番DB、索引、公開成果物は変更していない。

## 2026-08-22: test — Ornith Gate B2/B3を不合格とし本番採用を終了

- 固定8〜27ページを4ページ×5窓、native JSON Schema、公式sampling、Thinking有効で実行し、2/5窓だけが`stop`した。残る3窓は8,192 tokenをreasoningへ使い、content 0字、全体coverage 12〜19ページで不合格だった。
- 成功24事実の原文照合でも、芳子星と皓茉莉花の科挙推薦人関係を逆転した重大誤り、page帰属違い、不自然な日本語を確認した。事前規則どおりOrnithを日本語小説RAGへ採用せず、Gate C / D、本番配線、公開更新へ進めない。
- 公式MLX-VLM 0.6.15の`thinking_budget=4096`を同一5窓へ追加すると、全窓が約98〜101秒、`stop`、各12事実、全page coverageへ改善し、停止ガードとしての有効性を確認した。
- Gate B3も簡体字混入、重要発言欠落、8ページの主体混在、18ページの推薦人逆転、25ページの理由誤帰属、26ページの華副三司使→`苑翔景`誤登録で不合格だった。追加GPU試験を終了し、checkpointだけをupstream更新時の比較用に保持する。
- 上記回帰を評価器と3件のunit testへ固定した。共通LLM 69件、backend 1,299件、評価器3件、docs整合、lint、変更ファイルformat、差分検査が合格した。
- MLX-VLM serverを正常停止し、TCP 11440と関連processが0件、終了後swapが1,176.81MiBであることを確認した。

## 2026-08-21: test — OrnithのGate A2/A3合格とGate B不合格を記録

- `mlx_lm.server`限定adapterのA2と`mlx_vlm 0.6.15` native JSON SchemaのA3は固定短窓へ各3/3で合格し、adapter不要のA3を第1ブロック候補に選んだ。
- 固定8〜27ページの現行事実抽出は、書籍事実・人物別事実とも8,192 tokenで`length`となり、118項目すべての空末尾page marker、21〜27ページ未到達、話者・主体誤り、中国語混入を確認した。
- 約19.05GiB常駐、swap増加0のため64GB不足ではなく、Gate B不合格として本番配線と残りブロックを停止した。現行Gate Bを変更せず、4ページ分割と件数・文字数制限付きnative SchemaのGate B2だけを隔離診断する方針を設計へ追加した。

## 2026-08-21: change — Ornithの構造化出力再評価プロトコルを固定

- 公式MLX-LMが`response_format`を生成制約へ使わない点と、MLX-VLMがThinking後のnative structured outputを実装する点を再確認した。
- 旧Gate Aの不合格は変更せず、`mlx_lm.server`向け限定JSON adapterのGate A2と、`mlx_vlm.server` native JSON SchemaのGate A3を独立試験として設計した。
- adapterは単独JSON object / 単独`json` fence以外、duplicate key、非有限数、未終端、`length`等を応答前に拒否し、A3は`--max-num-seqs 1`・追加KV量子化なしとするfail-closed条件を固定した。
- `NOVEL_DB_*`、公開物、索引、checkpointを変更せず、両経路不合格時だけ非公式runtimeを別検討する方針とした。
- MLX-VLM 0.6.13でtext-only checkpointへvision tower 393 parameterを要求する既知不具合を再現したため、修正済み公式0.6.15へ更新した。`mlx 0.32.0 / mlx-lm 0.31.3`とcheckpointは維持した。

## 2026-08-21: test — Ornith 1.5の本番採用Gate Aをfail closed

- Linux本番DBをread-onlyで取得した固定74〜75ページを評価し、non-thinking診断は厳格JSON・中核意味・正規名・自然停止へ1/1で合格した。一方、理由文に途中の帰牢意図を「仮の返事」とする軽微な過剰解釈があった。
- Thinking 3回はコードフェンスを除けば中核3判定と日本語正規名へ3/3で一致し自然停止したが、生contentをすべて`json`コードフェンスで囲んだため事前固定した厳格JSON契約は0/3だった。後付けで合格条件を緩めず、Gate A不合格・本番採用保留とした。
- fail closedにより8〜27ページ、backend契約、4ブロック・1冊・長文を実行せず、本番DB、公開物、checkpoint、LanceDB、`NOVEL_DB_*`、Qwen / bge-m3 / Gemmaの採用構成を変更しなかった。
- raw応答と判定結果をローカル実験ディレクトリへ保存した。評価serverを停止し、port `11440`と関連processがないこと、system-wide swapが試験前後とも1,208.81MiBであることを確認した。

## 2026-08-21: test — Ornith 1.5 35B-A3B MLXを64GB Macでsmoke

- 公式MLX 4bit版を固定revisionで18.2GiB取得し、全14ファイルの配布ハッシュ、4 shard・1,757 tensorのindex / header整合、stock `mlx-lm 0.31.3`の`qwen3_5_moe`対応を確認した。
- 公式通常samplingとThinking有効、prefix cache 0、同時実行1で、短答decode 50.911 token/s、benchmark 3回平均51.780 token/s、peak 20.176GB、swap増加0を確認した。
- OpenAI互換APIは256 output tokenで途中切れ、512でreasoning分離・自然停止し、既存SSE正規化も完走した。一方で1行出力契約へ説明を追加したため厳密形式は未合格とし、`NOVEL_DB_*`と既存採用構成を変更せず、固定小説ケースより前の比較候補に留めた。
- 公式モデルカード、MLX-LMのhybrid cache課題、M1 Max long-context報告、コミュニティ実測をADR、GPU環境セットアップ、比較計画、技術知見へ反映した。

## 2026-08-19: change — 天野美汐の原作寄り同定確認バッチを追加

- 公式公開立ち絵を生成時の外部参照としてのみ使用し、顔立ち・短いウェーブボブ・瞳の色味・制服・表情を固定した確認用画像10枚を追加した。
- 媒体変換用の画像とは別に「原作寄り・キャラクター再現確認」グループへ登録し、今後の媒体別生成の基準デザインとして扱う。

## 2026-08-19: feat — 天野美汐の媒体・年代別画像バッチを追加

- 1980年代白黒少女漫画、1990年代セル画、16-bitドット絵、2000年代ローポリ3D、粘土ストップモーション、水彩絵本、木版画、リソグラフ、油彩、紙コラージュの10媒体を各10枚、合計100枚生成した。
- 媒体ごとの必須質感と禁止条件を生成仕様JSON・カタログのpromptへ記録し、画像本体はGitHubではなくローカルサーバーのデータディレクトリへ同期した。

## 2026-08-20: test — Sol画像OCR再pilotをfail closedで再評価

- 初回失敗10画面を独立候補A/B、逆方向coverage checker、全標本集合検証で再処理し、5画面を確定、5画面を`needs_review`へ隔離。
- 原画像と誤り分類だけを使うbounded repairを5画面へ実施し、3画面を確定した一方、鉤括弧と二連ダッシュの欠落各1件を独立checkerが検出して停止。
- tuning全体は確定8/10、未解決2/10、誤採用0件。重大誤差0を満たさないためfresh holdout、全冊展開、canonical昇格を見送り、既存公開本文を維持。
- fresh holdoutはledgerの`opened`後だけ画像export可能とし、全画像SHA再照合と`retired_to_tuning`後の再export拒否を追加。
- v2候補・checker・resolved bundleのfail-closed import、fresh holdout選定・一方向ledgerを含むバックエンド全体で1,356 testの回帰を確認。
- rescue監査tarをmedaroserverへSHA一致で退避し、検証付きbackup後に初回Sol staging 27 runを`failed/rejected`へ終了。active publicationはlegacy 81冊、Sol 0冊、canonical 9,913行を維持。

## 2026-08-19: feat — Sol画像OCR campaignの版管理・並列実行契約を追加

- medaroserverの156冊・数値名PNG 19,406画面を固定manifestへ封印し、GPT-5.6 Solで最大3 workerの冊子単位OCRを行う計画を追加。
- 現行canonical本文を合成legacy runへsnapshotし、`ocr_publications`の追記履歴からSol昇格・legacy rollbackを同じ原子的公開処理で扱う方針を採用。
- 原画像外部送信の明示opt-in、認証情報非転送、artifactのschema・画像SHA検証、pilot 81画面のfail-closed条件をADR-0021と設計書へ記録。
- medaroserverでmanifest固定、検証付きbackup、81冊・9,911正式画面のlegacy snapshot、Sol pilot 81画面のstaging importまで実施。canonical本文は未変更。
- 本文比較可能な18画面を原画像で独立判定し、Sol優位8件、legacy優位10件を確認。legacy優位10件はいずれも重大な欠落・読順・固有名詞退行を含むためpilotを不合格とし、全冊展開と正規昇格を停止。
- 初回失敗を本文欠落8件、読順・発話構造崩れ6件などへ分類し、単一転記v1を監査専用へ固定。独立候補A/Bのoverlap列帯転記、第三sessionの逆方向coverage検査、全標本集合のfail-closed importを再pilot v2契約として追加。
- 初回pilotをtuningへ隔離し、B-35を含む既出標本を除外したfresh holdoutをprompt/policy凍結後に一度だけ評価する契約を追加。

## 2026-08-18: docs — 固有名詞候補支持とPP-OCRv6を追加評価

- 出版社公式のシリーズ語彙と同位置の独立OCR一致を要求する固有名詞補正を、開封済み30画面で評価
- 原画像・公式表記と異なる「鳥妃」5出現を封印済み成果物から分離した調整用errataへ記録
- errata適用後はCER 1.1346%から1.0622%、固有名詞再現率91.8605%から96.0465%へ改善し、21補正・10画面は全件改善、悪化0を確認
- PP-OCRv6 mediumは最大CER画面で全文33.6462%、既存行bbox再利用11.6968%のため拡大評価を見送り

## 2026-08-18: feat — ローカルサーバー公開と画像データ分離

- 画像をアプリ本体のGit管理から外し、`IMAGEGEN_CATALOG_DATA_DIR` で指定するローカルサーバー側のデータディレクトリを正本にした。
- FastAPIのリリース配信、systemd user service、Tailscale Serve経由のTailnet公開、初回データseedと反復デプロイの手順を追加した。

## 2026-08-18: feat — 作品・キャラクター絞り込みを追加

- カタログに作品名・キャラクター名のフィルターを追加し、詳細画面でも分類情報を確認できるようにした。
- 初回10枚の銀髪キャラクターを作品・キャラクター未設定のオリジナルとして再分類し、Kanon / 天野美汐と分離した。

## 2026-08-18: feat — 生成画像・プロンプト条件カタログを追加

- 生成画像と生成条件を `data/catalog.json` + `data/assets/` の相対パスで管理する独立 `imagegen-catalog/` アプリを追加した。
- React + FastAPI のローカル構成、別 PC へ `data/` を丸ごと移動できるポータビリティ、41枚の初期カタログ、検索・絞り込み・詳細条件・JSONコピー、パストラバーサル対策を実装した。

## 2026-08-18: test — Nemotron 30BのThinking利用を公式条件で再監査

- 前回評価器と保存成果物を監査し、Ollama固定試験はnative `think=true`、MLX比較は`chat_template_kwargs.enable_thinking=true`を実際に使っていたと確認した。初回第1ブロックのraw応答だけは未保存だったため、NVIDIA公式例の`temperature=1.0 / top_p=0.95 / max_tokens=16000`で再実行した。
- 74〜75ページはThinkingが中核判定3/3、directが0/1だった。一方、汎用事実抽出のThinkingは8〜27ページで559.465秒、24〜27ページでも414.988秒後に16,000 tokenを使い切り、必須形式へ到達しなかった。
- directは4ページ抽出を63.734秒で完走したが話者・時点誤りが残った。同じ誤りを26〜27ページ・4項目の列挙Schemaへ分解するとdirect 9.201秒、Thinking 132.680秒で双方正解したため、短block direct→型付き局所照合→難例のみThinkingの順を採用し、MLX切替と本番配線を見送った。
- 評価後はNemotronを停止し、通常のQwen / bge-m3 MLX serverについてhealth、短答、1,024次元embeddingを再確認した。

## 2026-08-18: test — Nemotron 3.5 Lightning 30BのMLX量子化を比較

- 公式情報とMLX-LMのhybrid cache・MTP対応状況を先に調査し、固定revisionの4bit / 6bit全shardとtokenizerをSHA-256照合した。prompt cacheの回答残留を再現したため、cache 0で全比較を取り直した。
- 74〜75ページ固定ケースはOllama Q4_K_M thinkingが中核判定3/3、MLX 4bitが0/3、MLX 6bitが8,192 tokenで3/3未終端だった。6bitは12,288上限の診断1回だけ9,205 token・268.377秒で正解した。
- 6bitは約24GB footprintで64GBへ収まり、試験後の空きメモリも回復したためメモリ不足ではない。品質・終端・速度からNemotronのMLX切替と長文拡大を見送り、Qwen / bge-m3構成へ復旧した。

## 2026-08-17: perf — OCR・生成文・撮影品質の既存監査契約を機械強制

- OCR ground truthの空白除去CERをページ単位SHA・編集距離cacheへ増分化し、API schemaを変えず本文変更時だけ再計算する契約を追加
- B-35 formal holdoutのmanifest digest、3シリーズ・20画面・固有名詞条件、全package SHA、一度だけの開封台帳をfail closedで検査する契約を追加
- B-36のE18単一修正範囲と41件完全一致reviewを決定的に検証し、合格前は公開DBを変更しない監査契約を追加
- B-32警告候補をprivate holdoutで評価し、code別TP/FP/FN・precision・recallを保存する非UI監査契約を追加
- 本番DBの検証コピー293件でCER初回0.619秒・cache hit 0.028秒、応答完全一致、1件変更時の単件無効化を確認

## 2026-08-17: fix — Kindle撮影の2画面往復とページ設定UI取得を有界化

- `A → B → A → B` を有界検出し、最低撮影数より前は誤方向として失敗、十分な前進後は重複を除いてKindle末尾として扱うことで無限往復を防止
- Kindleのページ設定ボタンが遅れて公開される場合に、ReadingArea再取得とAutomationId限定の有界再試行で復旧する契約を追加
- R1再受入はterminal jobの再開ではなく、同じASINを新規jobとして実行する安全契約へ表記を訂正
- Windows実機で漫画88画面・小説104画面の撮影、画像QA、転送、登録を完了し、両ASINの`capture_state=captured`とactive job 0件を確認してR1を合格とした

## 2026-08-17: fix — B-35正式holdoutの評価スコープをmanifestへ固定

- 正式評価で固有名詞注釈を封印済みmanifestから取得し、古い汎用corpus注釈を参照しない契約を追加
- manifestが保証する3シリーズ・normal prose 20画面へ、汎用corpusのページ種別・layout件数を二重適用しないよう修正
- CER、ページ最大値、列欠落、固有名詞再現率と最低文字数のpolicy閾値は変更せず、一度限りの開封前に回帰テストを追加
- 新規3シリーズ30画面を一度だけ評価し、現行primaryは通常散文CER 27.4038%、ページ最大496.7509%、列欠落疑い3画面、固有名詞324/430で総合FAILと記録
- 品質ゲート不合格の正式holdoutを台帳の`opened`から`retired_to_tuning`へ単方向遷移させる処理と再実行拒否テストを追加
- 既存の「externalが30文字・2%以上長い」QAリスク条件を純関数へ集約し、低信頼externalをQA必須のレビュー候補として選べるようworker候補選択へ適用
- 開封後の調整用集計で全文性候補選択はCER 2.1141%へ改善した一方、MangaOCR単体70.6753%、3候補合議2.4693%、3候補oracle 1.4761%で、現候補集合は正式基準未達と記録
- primaryの異常反復を候補本文だけで検出し、256文字以上かつ反復のないexternalへ必須QA付きで切り替えるfail-safe候補選択を追加
- 公式NDLOCR-Lite v1.2.3を開封後の調整用30画面へ再評価し、単体CER 3.1832%、3候補文字合議1.1174%、列欠落疑い0画面だが正式基準未達と記録
- 反復フォールバックが最大外れ値1画面をexternalへ切り替え、primary基準CER 27.4038%から4.1626%、ページ最大496.7509%から28.1128%へ低下することを確認
- llama.cppの既定random seedに依存しないようSurya OCRの全OpenAI互換requestへ`seed=0`を固定し、同一画像・promptの候補再現性を契約化
- `seed=0`後の4列部分再OCRで候補本文SHAが3回一致した一方、別難例はCER 38.6997%まで悪化したため、部分再OCRを自動採用せずQA補助に限定

## 2026-08-17: test — Nemotron Puzzle 75B MLXを64GB Macで早期評価

- mixed 4/6-bit checkpointを固定revisionで取得し、全9 shardとtokenizerのSHA-256、専用`mlx-lm` branchの差分、単体test 12件を確認して安全な排他切替scriptを追加した。
- M1 Max 64GBで排他serverのhealth・短答・OpenAI互換streamingを通したが、system-wide swapは最大33.79GB、空きメモリ指標は最小9%となった。
- 茉莉花官吏伝10巻74〜75ページはnon-thinking、thinking、`low_effort`の全条件で意味または終端・enumゲートに不合格となり、本番配線と長文試験を見送った。評価後はQwen / bge-m3構成へ復旧した。

## 2026-08-17: feat — Apple Silicon MLX推論経路を追加

- 共通LLM層へ`MlxBackend`を追加し、thinking指定、sampling名、OpenAI SSEを既存のOllama互換イベントへ変換する。
- 小説RAGの生成・検証backendとembeddingをenvでMLXへ切替可能にし、bge-m3の件数・index・1024次元をfail-closedで検証する。既定のWindows/Linux経路は維持する。
- M1 Max 64GBの同一入力比較でQwenとbge-m3（FP16+CLS）を採用し、Macの旧Ollama登録2件を削除した。Gemma MLXは誤答・`<image|>`大量出力・低速化で不採用とし、`gemma4:12b`を保持した。
