# 変更履歴

実装・設計・運用などの全変更を時系列（新しい順）で記録する。直近 1 週間分は本ファイルにインライン記載、過去分は週単位で 変更履歴/YYYY-Www.md にアーカイブ。

---

## アーカイブ索引

| 週 | 期間 | 主な内容 |
|---|---|---|
| **直近 (W30)** | 2026-07-20 〜 | 本ファイル下部にインライン記載（同人誌リーダー再オープン修正ほか） |
| [W27](変更履歴/2026-W27.md) | 2026-06-29 〜 2026-07-05 | 同人誌フォルダ監視自動取り込み / 全体コードレビュー修正 / ファイルマップ生成スクリプト化 / 読書会ロングフォーム要件整理 + Phase 0a/0b 実測 / hitomi Discord 通知 |
| [W26](変更履歴/2026-W26.md) | 2026-06-22 〜 2026-06-28 | 設計書整理（リファクタリング計画書スリム化・C12 アーカイブ・FE 設計書分割） |
| [W25](変更履歴/2026-W25.md) | 2026-06-15 〜 2026-06-21 | Kindle 新アプリ（Microsoft Store 版）ウィンドウタイトル対応（TM-1） |
| [W24](変更履歴/2026-W24.md) | 2026-06-08 〜 2026-06-14 | 大規模リファクタ Phase 0〜4 全実行（uv workspace 化・CI 導入・basedpyright・pre-commit・SQLModel・Alembic・React Query 全面移行・shadcn/ui 導入 ほか）/ TM-1 起票 / B-17 保留 / バックログ棚卸し |
| [W23](変更履歴/2026-W23.md) | 2026-06-01 〜 2026-06-07 | フロントエンド依存ライブラリ更新・Playwright E2E テスト導入 / Mac クロスプラットフォーム開発対応 / Gemma モデル統一 |
| [W22](変更履歴/2026-W22.md) | 2026-05-25 〜 2026-05-31 | doujin PDF 生成を Linux サーバー完結化・Samba 共有導入 / hitomi 監視を Linux systemd timer に移行 |
| [W21](変更履歴/2026-W21.md) | 2026-05-18 〜 2026-05-24 | Phase 83〜87 リファクタ（llm.py 分割・useNovelManage 統合・character_summarizer 分割）/ iPad・PWA 対応 / nginx リバースプロキシ導入 / localStorage→DB 移行 |
| [W20](変更履歴/2026-W20.md) | 2026-05-11 〜 2026-05-17 | Phase 55〜78 全実行 / LanceDB 移行 / React 19 + Zustand / Alembic 導入 / テスト整備 §10 全件完了 ほか |
| [W19](変更履歴/2026-W19.md) | 2026-05-04 〜 2026-05-10 | Phase 49〜54 共通化棚卸し / RAG 全文検索本実装 / B-5/B-8/B-9/B-11〜14 採用 / フロントテスト整備 / image-only モード |
| [W18](変更履歴/2026-W18.md) | 2026-04-27 〜 2026-05-03 | Phase 13-17 / Phase 19-38 / hitomi 監視機能 / .claude 拡充 / Phase D デザイントークン |
| [W17](変更履歴/2026-W17.md) | 2026-04-20 〜 2026-04-26 | Phase 10-12 リファクタ / uv 移行 / 最近見た順ソート / 閲覧回数機能 |
| [W16](変更履歴/2026-W16.md) | 2026-04-13 〜 2026-04-19 | Phase 1-9 リファクタ基盤 / 見開き 3 段階 / 書籍メタ機能 / OCR 品質改善 |
| [W15](../archive/変更履歴/2026-W15.md) | 2026-04-06 〜 2026-04-12 | リファクタリング フェーズ 4（品質向上・将来対応） |
| [W10](../archive/変更履歴/2026-W10.md) | 2026-03-02 〜 2026-03-08 | リファクタリング フェーズ 1-3 |
| [W03](../archive/変更履歴/2026-W03.md) | 2026-01-12 〜 2026-01-18 | Novel OCR 機能追加（プロジェクト初期） |

---

## 追加ガイド

新規エントリは本ファイル下部「直近の追記」セクションの**先頭**（最新が上）に `## YYYY-MM-DD: type — タイトル` 形式で追記する。`type` は `feat` / `fix` / `refactor` / `chore` / `docs` / `test` / `perf` / `style` / `revert` / `change` のいずれか。詳細フォーマットは `/changelog` スラッシュコマンドが直近コミットから草稿を生成する。

週境界（毎週月曜日）を跨いだら、本ファイル下部の前週分を切り出して `変更履歴/YYYY-Www.md` を新規作成し、上記「アーカイブ索引」テーブルに 1 行追加する。

---

## 直近の追記（2026-W30: 2026-07-20 〜）

## 2026-07-27: docs — 設計書と現行実装の横断整合

- OCR の公開条件を、機械合格直後ではなく `awaiting_qa`、必須ページ承認、
  run 承認を経る現行フローへ統一した。
- Full Build と Context 生成の分離、Kindle 漫画の見開き撮影方式、
  novel の image-only 編集範囲を実装に合わせた。
- Linux 本番の nginx 8090 → uvicorn 8091、DB バックアップ付きデプロイ、
  MkDocs の事前ビルド、Windows NSSM 運用を現行化した。
- uv workspace、Surya / yomitoku / Ollama GPU の役割、Mac 開発手順、
  起動方法、移動済みスクリプトパスとソースリンクを修正した。
- 安全要件を緩めずに残す実装差分として、シリーズ期待冊数、novel_db の
  パス境界、OpenAPI 型、Windows service の旧 venv 参照を既知の問題へ記録した。

## 2026-07-27: fix(ocr) — 終端本文の誤除外とQA見逃しを防止

- 目次判定を前付け位置と章見出しの組み合わせへ限定し、長い本文中の単独語で
  目次へ誤分類しない仕様へ変更した。
- 終章・あとがき等の本文優先規則と、奥付・広告を確定する位置・語彙・文字量の
  複合条件を追加した。
- 長文の非本文候補と、2つのOCR候補で敬称付き人名・カタカナ語に同じ文字数の
  1文字違いがあるページをリスクフラグ付きQA必須対象にする方針を追加した。
- 実データ914ページでは単純な語集合不一致が233ページを必須化したため採用せず、
  1文字違いの候補対がある92ページへ絞る高精度条件を採用した。
- `is_indexed`を`indexed_at IS NOT NULL`と同義に揃え、OCR公開状態と
  チャンク・Embedding構築状態を区別する契約へ修正した。
- 茉莉花官吏伝8〜17巻のチャンク・Embeddingを再構築し、10冊すべての
  `indexed_at`更新と、8巻・17巻の書籍内検索を確認した。
- EmbeddingはWindows RTX 5070利用時に各巻約6〜12秒で完了した。
  接続先と`NOVEL_DB_EMBED_NUM_GPU=99`は処理中だけ設定し、完了後に本番既定値へ戻した。
- 本番Qwen llama-serverが未起動のため、要約・人物抽出を含む`full_build`は避け、
  正式本文・検索索引に必要な`rebuild`のみを成功条件として実行した。

## 2026-07-27: docs — シリーズ撮影分のOCR引継ぎ・文字起こしを完了

- `capture_state`と小説DBのOCR・索引状態を別々に確認し、撮影完了だけで
  文字起こし完了と判定しない運用を明記した。
- シリーズの一部だけを処理する場合は書籍名を固定して1冊ずつOCRへ投入し、
  対象なしの全件実行で以前からの未OCR書籍を巻き込まない方針を追加した。
- 2026-07-27撮影分は茉莉花官吏伝8〜17巻の10冊を対象とし、
  QA承認まで正式本文へ公開しない境界を記録した。
- 10冊914ページのOCRとQAを完了し、本文806ページを公開、表紙・目次・挿絵・
  奥付・広告など108ページを画像のみ保持した。機械品質判定に失敗した本文15ページは
  Codexが原画像と段組みを照合して補助OCR候補を採用した。
- 終端付近の本文3ページが目次・奥付等へ誤分類される事例を補正し、位置ベース分類後も
  文字量のある非本文候補を画像確認する運用を追加した。
- `is_indexed`はDBレコード存在を表し得るため、文字起こしは`ocr_done_at`、
  チャンク・Embedding構築は`indexed_at`で判定する状態境界を明記した。

## 2026-07-27: feat(kindle) — シリーズ撮影の安全な直列実行を追加

- シリーズ対象をdry-runで棚卸しし、1冊の成功と正式登録を確認してから次冊を作成する
  運用補助スクリプトの契約を追加した。
- 失敗、タイムアウト、対象冊数不一致、別の未完了job検出時は次冊を作成せず、
  Kindle画面へ接続しないAPI監視だけで連続運用できるようにした。
- 茉莉花官吏伝シリーズは小説8〜17巻、漫画1〜12巻の順で再開する。
- バックグラウンド起動時にKindleの前面化が拒否されても背面座標をクリックしないよう、
  foreground threadとのinput queue接続と前面handleの一致確認を追加した。
- Sambaへのpackage公開renameで一時的なアクセス拒否が発生した場合、コピー済み成果物を
  保ったままrenameだけを有界再試行し、不要な全ページ再撮影を減らした。
- 茉莉花官吏伝の未撮影22冊（小説8〜17巻、漫画1〜12巻）を直列処理し、既存8冊と
  合わせた全30冊が`captured`であることをカタログAPIで確認した。

## 2026-07-27: docs — Kindle完了項目をバックログ履歴へ整理

- 完了済みのA-4、B-27、B-33、B-34を現役のA/B候補から削除し、
  「実装済み機能の履歴」へ完了日・成果概要付きで移動した。
- A階層は対応決定済みの未完了項目がない状態を明記し、
  バックログ本文を未着手・検討中の機能だけ確認しやすい構成へ戻した。

## 2026-07-27: test(quality) — 構造・契約ガードレールを通常運用化

- production code行数をbaseline化し、新規400行超と既存超過ファイルの肥大化を
  pre-commit・CIで防止するラチェットを追加した。
- FastAPI OpenAPI契約の正規化比較と、backend・frontend・Kindleの依存方向検査を
  blocking gateへ追加した。
- living設計書の800行上限をblockingへ移行し、月次に設計書、規模、複雑度、
  未使用依存、完了計画残存を一括確認する監査コマンドを追加した。

## 2026-07-27: docs — Kindle自動撮影の完了実績をバックログへ同期

- B-34をPhase 0条件付き合格からPhase 5実装・実機受入完了へ更新し、
  直接先頭移動、原子的置換、小説10巻・漫画1巻の実機結果を記録した。
- B-27の漫画確認待ちを解消し、単ページ・見開き切替、左右端、余白、UI混入、
  Pic2PDFViewerへの置換まで確認済みであることを記録した。

## 2026-07-26: test(quality) — C901複雑度ラチェットを導入

- backend 25件、kindle-pdf 3件の既存C901超過を関数単位のJSON baselineへ固定した。
- 新しい超過と既存関数の複雑度悪化を検出するscriptを追加し、
  pre-commitとCIのblocking gateへ接続した。
- parserと新規・悪化・改善判定をunit testで固定した。

## 2026-07-26: refactor(hitomi) — routerの警告ログを共通loggerへ統一

- hitomi監視対象追加時のNOZOMI取得失敗警告を共通`get_logger`へ移し、
  consoleとbackend永続ログへ集約した。
- 登録自体は成功させる既存挙動と警告出力をテストで固定した。

## 2026-07-26: chore(tools) — 使用不能な旧フラット移行ツールを撤去

- `source="generated"`と`meta.json`前提で現行環境では実行不能な
  `tools/migrate_flatten.py`を、誤実行防止のため撤去した。
- 運用ガイドから矛盾していた実行例を除き、将来の移行ツール要件を明記した。

## 2026-07-26: refactor(frontend) — Reader・Kindle query・OCR QAの状態境界を整理

- Readerの単ページ・見開き・RTL/LTRページ計算を純関数へ統一し、
  表紙、末尾、奇数最終ページを表形式テストへ固定した。
- Kindle購入一覧のURL query解析・既定値・page resetを専用featureへ移した。
- OCR QAのrun/page選択、Query、mutation、入力状態をcontroller hookへ分離した。
- Playwrightの接続先を環境変数で切替可能にし、取り込み画面E2Eを現行UIへ同期した。
- frontend 1,055テスト、TypeScript、Knip、production build、Chromium E2Eを通過した。

## 2026-07-26: refactor(ocr) — OCR処理を実行・品質・QA境界へ分割

- Surya OCRをtypes、prompt/parse、品質判定、server/clientへ分割し、
  `surya_ocr.py`は既存importを維持する互換facadeとした。
- OCR stagingをrun保存、ページ分類、QAレビュー・原子的公開へ分割し、
  `ocr_staging.py`は既存importを維持する互換facadeとした。
- novel DB job workerをqueue loop、状態永続化、対象選択、mode別executorへ分割した。
- OCR関連60テスト、backend 1,038テスト、target basedpyright、
  固定20画面のCER再計測で、
  本文・品質flag・分類・公開条件が変わらないことを確認した。

## 2026-07-26: refactor(kindle) — 自動撮影の操作・撮影・登録境界を分割

- `KindleAppController`を書誌照合、window/control、library、readerへ分割し、
  既存classを互換facadeとして維持した。
- capturerを基底window、撮影ループ、手動UI、漫画cropへ分割し、
  agentをAPI/heartbeat、package公開、工程制御へ分割した。
- Linux側を状態repository、ready package検証、正式登録・補償へ分割した。
- 正式画像配置後、meta更新後、package移動後、DB更新直前の障害注入テストを追加し、
  画像・meta・package・job状態がすべて元へ戻ることを確認した。
- 実機ゲートで確認したリフロー小説の表紙固有遷移に対応し、2画面目への遷移だけ
  通常キーが無反応なら反対キーを1回試す安全なフォールバックを追加した。
- Microsoft Store版Kindleの最大化後に読書領域へフォーカスを戻し、ページ送りキーが
  ウィンドウへ届かず1画面で停止する実機回帰を防止した。
- 同じASIN・書籍IDの再撮影は旧画像を世代バックアップへ退避して原子的に置換し、
  異なるASINの同名書籍は拒否する登録境界と障害注入テストを追加した。
- 小説の直接先頭遷移後に表示される `ページ1/N・0%` は表紙ではなく次の画面と
  実機確認し、この場合だけ逆方向へ1回送って `Location 1・0%` を再確認する。
- 直接遷移または結果検証に失敗した場合は `positioning_failed` とし、全ページを
  1画面ずつ戻る画像差分フォールバックを実装から削除した。
- 現行Kindleで「位置に移動」が「ページへ移動する」へ改名され、popoverが別の
  UI Automation rootへ公開される実機差に対応した。Name・AutomationIdと
  Kindleウィンドウ内座標をすべて検証してから操作する。
- 座標クリックを受け付けない現行版の `backButton` は、本人照合済みcontrolへ
  フォーカスを設定し、通常のEnterキーでライブラリへ戻す。
- 小説10巻は92画面（3516×1940）、漫画1巻は85画面（2936×2064）を
  同一ASIN置換で本番再登録した。両方とも連番欠落・SHA-256重複・全面白は0件で、
  metaのASINと世代バックアップを確認した。
- 漫画の表紙、P.9、P.10、最終画面を目視し、P.9から見開きで左右端が欠けず、
  表紙と最終の単ページも見開き安全幅内に保持されることを確認した。

## 2026-07-26: refactor(api) — レスポンススキーマをドメイン別に分割

- 948行の`routers/api_schemas.py`に集中していたPydanticモデルを、common、library、
  novel DB、OCR、Kindle catalogの機能別moduleへ分割した。
- `routers.schemas`を公開境界、旧`routers.api_schemas`を互換facadeとして維持し、
  既存ルーターのimportと外部API契約を変更しない構成にした。
- 正規化OpenAPIは変更前後で完全一致し、生成TypeScript型は1行の折り返し以外に
  差分がないこと、backend全テストとfrontend型チェックが通ることを確認した。

## 2026-07-26: docs — 設計書体系を現行仕様へ再編

- 完了済みKindle計画3件をarchiveへ移し、living文書とMkDocs navの参照を更新した。
- フロントエンド詳細設計を共通設計、ライブラリ・リーダーUI設計、
  小説RAGフロントエンド設計へ分割し、`design/`の800行超警告を0件にした。
- Kindle catalogのSQLModel/Alembicによるスキーマ管理と短命`sqlite3`接続による
  実行時アクセスをADR-0017として明文化した。
- OpenAPI生成型との差を説明するコメントへ改め、陳腐化したTODOを除去した。

## 2026-07-26: docs — リファクタリング再計画と健全性維持方針を策定

- 完了済み・スキップ・未評価候補が混在していたリファクタリング計画書をリセットし、
  設計書、API契約、Kindle、OCR、frontend、品質ゲートのPhase 1〜6へ再構成した。
- docs検査、ファイル規模、C901、テスト収集数、Knipの現状を基準線として記録した。
- 設計書とソースコードの将来的な崩れを防ぐため、正本の一元化、行数・複雑度の
  ラチェット、契約比較、依存方向、期限付き例外、段階的CI導入の計画を新設した。

## 2026-07-26: refactor(kindle) — 先頭移動をロケーション指定で高速化

- `go_to_start` は「位置に移動」でロケーション`1`を指定する高速経路を優先し、
  小説は`Location 1・0%`、2ページ表示漫画は`Location 1/2・0%`を確認する。
- UI Automationは限定探索と入力欄のフォーカス設定に限り、文字入力とクリックは
  通常操作を維持する。
- 高速経路の要素を取得できない、または移動結果を検証できない場合は、従来の
  画像差分による1画面ずつの先頭探索へフォールバックする。
- 実機で小説`2748/3304 → 1/3304`を2.38秒、2ページ表示漫画
  `150/169 → 2/169`を2.41秒で完了し、kindle-pdfテスト50件を通過した。

## 2026-07-26: docs — Kindle位置移動の高速化知見と一時停止状態を記録

- 茉莉花官吏伝シリーズは小説4〜7巻を追加登録し、一時停止時点で小説8冊が
  取り込み済み、残りは小説10冊・漫画12冊の計22冊であることを記録した。
- 小説8巻の途中jobを`failed / paused_by_user`として正式データを変更せず停止する
  手順と、再開時は途中画像を継ぎ足さず新規jobで表紙から撮り直す規則を追加した。
- Kindleの「位置に移動」は小説・漫画ともロケーション`1`を指定でき、小説は
  `Location 1`、2ページ表示漫画は表紙でも`Location 2`になる実機差を記録した。
- 1画面ずつ戻る現行方式を維持したまま、高速経路と画像差分フォールバックを
  技術メンテナンスP1候補として追加した。

## 2026-07-26: docs — Kindle active job中の診断接続を禁止

- 茉莉花官吏伝5巻の先頭移動中に別controllerで診断画像を取得した直後、
  撮影が3画面で終了した。結果を隔離し、API監視だけで再撮影すると91画面を完走した。
- 診断用controllerもウィンドウ復元・前面化を行うため、active job中はcapture agentだけが
  Kindleへ接続し、状態確認はcapture job APIとheartbeatに限定する排他規則を追加した。
- 画面キャプチャ、UI Automation診断、表紙・中盤・最終ページの目視はjob完了後に行う。

## 2026-07-26: fix(kindle) — マルチモニター環境の検索欄フォーカスを修正

- 表示倍率が異なるモニター上で検索欄の矩形クリックが外れ、以前の検索値のまま
  ASIN候補を探して`book_not_found`となる問題を実機で特定した。
- UI Automationは検索欄の`SetFocus`だけに使い、通常のキーボード入力後に
  `ValuePattern`を読み戻してASIN完全一致を確認する方式へ変更した。
- Kindleを不安定化させた`SetValue`／`Click`は引き続き使わず、検索値を確認できない
  場合は撮影前に安全停止する。

## 2026-07-26: docs — Kindleシリーズ全巻の直列取り込み手順を追加

- シリーズ検索、取り込み済み・active job除外、書名・レーベルによるsource確定、
  1冊ごとの成功確認を繰り返す安全な運用手順を実機知見へ追加した。
- 茉莉花官吏伝シリーズは利用者指定に従い、ビーズログ文庫を小説、
  プリンセス・コミックスを漫画として扱う。初期棚卸し30冊のうち既存4冊を除外し、
  小説14冊・漫画12冊の計26冊を新規取り込み対象とした。

## 2026-07-26: feat — 小説2冊のCodex補助QA・正式公開・索引を完了

- 茉莉花91画面と十三歳10巻92画面を全画面承認し、本文81画面ずつ、
  画像のみ10／11画面として正式公開した。機械候補147画面、Codex補正15画面を採用した。
- 本番Ollamaへ`bge-m3`を導入し、茉莉花177 chunks、十三歳168 chunksを生成した。
  公開本文とQA採用値の不一致、非本文への本文混入、空の本文ページはいずれも0件だった。
- 補正箇所4フレーズが期待ページへ検索ヒットし、結果画像がPNGとして取得できること、
  本番UIで十三歳P.82が1位・ハイライト表示されることを確認した。
- 機械OCRの通常散文CERは1.82%で目標0.5%に未達のため、
  無料・ローカルOCR単独の完全自動公開は行わず、Codex補助QAを継続する。
- Full Buildのサマリ・人物辞典は本番llama-server未起動のため未生成であり、
  公開・索引とは分離した運用課題として残した。

## 2026-07-26: fix — 短い本文ページを検索・QA対象へ復帰

- OCR QAで確定した`index_eligible`を検索対象判定の正本とし、旧来の文字数300未満除外と
  先頭・末尾5ページ除外を検索・QA取得へ重ねないよう変更した。
- 章間の短文や詩も検索可能にする一方、目次・挿絵・広告等は
  `index_eligible=0`と空本文で引き続き検索・Embedding対象外とする。

## 2026-07-26: fix — Full Build進捗更新のDBロック競合を無害化

- `rebuild_from_pages` の書込みトランザクション中に、進捗詳細を別接続で
  `rebuild_jobs` へ書こうとして `database is locked` で本処理まで失敗する既存不具合を修正した。
- `current_detail` は補助表示としてロック時だけ更新を省略し、チャンク・embedding・
  最終state/progressの更新を継続する。ロック以外のDB例外は従来どおり失敗として伝播する。

## 2026-07-26: change — レイアウト別OCR選択と画像のみ公開を設計

- 意味上のページ種別とOCRレイアウト種別を分離し、Surya・yomitoku両候補と
  Codex画像照合済み補正文を保持する設計を追加した。
- 目次・表紙・挿絵・奥付・広告はQAで明示承認後に画像のみ公開できる一方、
  本文は機械合格または非空補正文を必須とする受入ゲートへ更新した。
- 2冊のQA後にだけ正式公開・Full Build・索引確認へ進む運用順を固定した。

## 2026-07-26: change — OCR正解コーパスをページ種別別に評価

- 正式画像20画面をすべて原画像照合済みの`verified`とし、全体CER 16.42%、
  通常散文4画面のCER 1.82%を基準値として固定した。
- 全体値に表紙・目次・挿絵混在・漢文の誤りが埋もれないよう、
  正解コーパスAPIと管理画面へページ種別別の件数・文字数・加重CERを追加した。
- 固定SHA画像に対して現行OCR・Tesseract・yomitokuを再採点できる
  `scripts/maintenance/benchmark_ocr_ground_truth.py` を追加した。
- 20画面ベンチマークで、現行Surya系／yomitoku／Tesseractの全体CERが
  16.42%／11.51%／57.20%であることを確認した。通常散文は現行Surya系1.82%、
  本文＋挿絵はyomitoku 10.71%が最良であり、一律置換せずレイアウト別に選択する。
- 挿絵混在画面の本文全落とし、目次の読み順、漢文・書き下し・ルビの順序を
  通常本文モデルとは別の改善対象としてPhase 5bへ整理した。

## 2026-07-26: feat — 小説OCR正解コーパスとページ種別分離を実装

- 正式画像SHAに結び付く正解コーパスを`draft` / `verified`で管理し、
  人手確認済みだけのページ別・加重CERを計算するDB・API・管理UIを追加した。
- OCR QAで`unknown` / `narrative` / `toc` / `illustration` /
  `colophon_or_ad`を自動提案・手動確定し、
  本文以外は画像・OCR本文を保持したまま検索・Embedding・サマリ・人物抽出から
  除外するようDB公開処理と各読込経路を変更した。
- 既存ページを本文として維持するAlembic `0007`、画像差し替え検知、
  未分類ページの公開拒否、20画面の初期下書き登録手順を追加した。

## 2026-07-26: feat — 小説OCRの安全な再開・QA・Windows実行基盤を実装

- OCR serverの有限セッション化、失敗率による再起動、ページSHA-256チェックポイント、
  Suryaとyomitokuの全画面照合、confidence分布判定を追加した。
- OCR完了後も本文を公開しないQA承認状態、原画像・本文比較UI、ページ承認・却下、
  最終承認後だけのトランザクション公開を追加した。
- 本番Linuxがジョブ・画像・DBを保持し、Windows GPUエージェントが認証付きAPIで
  manifest取得・OCR・heartbeat・ページ結果返却を行う構成へ移行した。
- 正式2冊を本番実行し、茉莉花官吏伝は91/91合格だが73画面QA必須、
  十三歳10巻は88/92合格・4画面不一致となった。通常本文の人物名誤読も確認したため、
  両書籍の本文公開・Full Build・索引作成は安全側で実施していない。
- NDLOCR-Lite、manga-ocr、PaddleOCR PP-OCRv5、Tesseractを隔離比較し、
  現時点で既存主系を置換できる無料候補はないと判定した。

## 2026-07-26: change — 小説OCR品質改善の実装計画を開始

- 新規撮影小説2冊・全183画面を隔離実行し、合格106、失敗77、
  再試行98画面で、現状では正式公開・Full Build不可と判定した。
- 同一SHA-256画像でも単独実行952文字、全冊実行860文字となり双方が
  フラグなし合格になる再現性問題と、人物名誤読・短い縦列欠落の偽陰性を確認した。
- workerのserver世代管理、品質ゲート再設計、無料OCR候補比較、QA承認、
  Windows OCRエージェント、2冊の正式登録をPhase 1〜5として計画化した。

## 2026-07-26: docs — Kindle自動撮影の実機知見を運用記録へ集約

- Kindle UI Automation、未ダウンロード待機、先頭・終端判定、source別撮影範囲、
  原子的な正式配置、ブラウザーキャッシュの切り分けを、再検証可能な実機知見として
  `docs/log/技術知見/Kindle自動撮影_実機知見.md` へ集約した。
- 要件、完了済み実装計画、Kindleツール設計から実機知見の正本へリンクし、
  小説再撮影結果に残っていた旧寸法3516×2064を正式結果3516×1940へ修正した。
- Kindle更新時の再検証手順と、再撮影前にサーバー画像・APIの更新番号・
  ブラウザーの実URLを順に確認する障害切り分け手順を明文化した。

## 2026-07-26: fix(kindle) — 小説下端切れと漫画見開き余白を修正

- Phase 5正式画像351枚を監査し、小説は91枚中76枚・92枚中78枚で最下8行に
  本文または絵が残る下端切れ、漫画は168枚すべてで左右各900px以上の余白を確認した。
- Microsoft Store版 Kindle の `ReadingArea` 実矩形を上下クロップの正本とし、
  固定105pxの下マージンによる本文欠落を防ぐ設計へ変更した。
- 漫画は `aaOption-Split`、小説は `aaOption-Single` を毎ジョブ明示選択して
  ToggleStateを確認し、漫画は先頭ページ幅から2ページ分の安全幅を確保する。
- 再撮影時にリフロー型小説では `aaOption-Single` 自体が表示されず、フォント・余白・
  間隔設定だけが提供されることを実機確認した。小説は `aaOption-Single` があれば
  選択し、なければ `フォント-item` を確認して単ページ表示として継続するよう修正した。
- 対象3冊は連番欠落、SHA-256完全重複、意図しない全面白ページがないことも確認した。
- 初回の下端修正で対象3冊を再撮影して正式データを置換した。小説は91枚・92枚、
  全画像3516×2064で、
  旧画像の最下8行に本文が残るページ数76/78枚から、表紙の塗り足しだけの1/1枚へ改善した。
  漫画は168単ページから85見開き、全画像2936×2064へ変更し、通常見開きの外側余白中央値を
  左1021px・右1022pxから左15px・右17pxへ縮小した。
- 小説画像に残った上部書名と下部 `ページX/Y・進捗%` がOCRへ混入するため、
  `ReadingArea` 全体ではなく `TopChrome.bottom` から `Footer.top` までを
  UI Automationの実矩形で切り出す設計へ変更した。
- 漫画のP.9に旧単ページ画像が表示される報告を照合し、サーバー上の現行
  `009.png` は正常な見開きだが、同URLで置換する前の画像がブラウザーキャッシュに
  残っていたことを特定した。画像一覧APIが各ファイル更新時刻を `v` クエリへ付与し、
  再撮影後に旧・新ページが混在しない設計へ変更した。
- 上下UI除外後の小説2冊を再撮影し、91枚・92枚、全画像3516×1940、連番欠落・
  SHA-256完全重複・意図しない全面白ページなしを確認して正式データへ置換した。
  置換前の正式データは
  `/opt/pic2pdf-viewer/data/.capture-replacement-backup/20260726-092136` へ退避した。
- 本番画面で漫画P.9が更新番号付きURLから正常な見開きとして再読込されること、
  小説の表紙・本文・最終ページに上部書名と下部ページ・進捗表示が含まれないことを
  実機確認し、対応する受入条件を完了とした。
- 画像更新番号を小説Readerへ引き継ぐ際も、未OCR書籍の `page_count=null` では
  従来どおり画像列を探索してページ数を確定するよう判定を維持し、本番で
  `20 / 91` と `20 / 92` の表示を確認した。

## 2026-07-26: docs — Kindle自動撮影の静的設計書を通常運用へ同期

- 要件定義、基本設計、バックエンド・フロントエンド詳細、API、Kindleツール設計から
  Phase 2・3待ち、利用準備中、PageUp／FooterLabelText、端部クリック等の旧記述を除去した。
- 通常運用を購入書籍詳細でのjob作成、現行自動agent、ASIN照合、必要時ダウンロード、
  画像差分による先頭移動、Samba転送、Linux原子的登録の現在形へ統一した。
- `run_comic.bat` / `run_novel.bat` は通常経路ではなく、診断・手動互換経路として位置づけた。

## 2026-07-26: feat — Kindle自動撮影取込のPhase 5実機受入を完了

- Microsoft Store版 Kindle 1.0.18632.0で、ダウンロード済み漫画168画面、
  ダウンロード済み小説92画面、未ダウンロード小説の再取得後91画面を先頭から撮影し、
  Samba転送、Linux検証、正式画像配置、ASIN設定まで完了した。
- Kindle未起動の安全停止、ASIN完全一致、候補なし・複数、ダウンロードタイムアウト、
  heartbeat期限切れ回収を実機または自動試験で確認した。
- デスクトップとiPad相当幅、ライト／ダーク、loading・empty・error・disabled・success、
  キーボード操作を実ブラウザーで確認し、要件定義と実装計画を完了状態へ更新した。

## 2026-07-26: fix(kindle) — ページ方向・UI差分・Kindleクラッシュを修正

- 右開き漫画の先頭移動は次ページの `left` と逆の右キーを使い、固定 `PageUp` で巻末へ進む問題を修正した。
- Microsoft Store版 Kindle の64px chevron領域に対して120px内側をクリックしていた処理を廃止し、撮影は利用者が確定した左右矢印キーで送る。
- 完全一致だった安定判定を平均画素差へ変更し、本文ページ遷移6.82とchevron表示だけの変化0.38を閾値1.0で分離する。
- 誤判定した150画面は一時領域だけに存在し、Samba転送・正式画像配置・ASIN更新前に破棄された。
- WindowsイベントログでKindle 1.0.18632.0の `ucrtbase.dll / 0xc0000409` クラッシュを
  確認した。UI AutomationはcontrolのASIN照合・矩形取得へ限定し、検索入力・戻る・
  ダウンロード・書籍オープンを矩形中心への通常入力へ変更した。

## 2026-07-25: feat — Kindle自動撮影の開始・監視UIを通常運用化

- 購入書籍詳細にsourceとページ送り方向の選択、運用前提の確認、capture job作成、active jobの二重作成防止を追加。
- キャプチャページへ自動準備工程、経過時間、撮影枚数、成功・失敗、エラー別対処、failed jobの確認付き再実行を追加。
- デスクトップとiPad相当幅、ライト・ダーク、loading・empty・error・disabled・success、キーボード操作を受入対象としてテストを更新。

## 2026-07-25: feat — Kindle capture agentを自動操作へ統合

- 旧確認ダイアログを廃止し、claim後にASIN検索、必要時ダウンロード、先頭移動、全ページ撮影、原子的転送、登録を直列実行するagentフローへ更新。
- 長時間処理中のheartbeat送信と停止要求伝播、工程別エラーコード、途中jobを暗黙再開しない再起動規則を追加。
- capturerへ進捗callbackを追加し、撮影枚数の状態反映とheartbeat異常時の安全停止を可能にした。

## 2026-07-25: feat — Kindle自動撮影の操作コントローラーを追加

- Microsoft Store版 KindleへUI Automationで接続し、ASIN検索、候補一意性確認、未ダウンロード判定、取得待機、読書画面の先頭移動を担うcontrollerを追加。
- AutomationIdの限定探索と低頻度ポーリングを採用し、候補不一致時はカード操作とcapturer起動を行わない安全境界を設定。
- Windows UI Automation wrapperとして `uiautomation==2.0.29` をKindleツールの依存へ追加。

## 2026-07-25: feat — Kindle自動撮影取込のPhase 1ジョブ契約を実装

- capture job に `locating_book`、`downloading`、`positioning` と `heartbeat_at` を追加し、旧 `waiting_user` 経路を維持したまま自動工程の状態遷移を定義。
- claim 応答へASIN、正式・正規化タイトル、著者、シリーズ、巻の `identity` を追加し、書誌情報をカタログ正本から都度合成する契約へ更新。
- heartbeat API、既定300秒の期限設定、次回claim時のstale job回収を追加し、期限切れを `agent_heartbeat_timeout` で失敗として確定。
- Alembic migration、OpenAPI生成型、状態遷移・identity・heartbeat・stale回収テストを追加。

## 2026-07-25: docs — Kindle自動撮影取込のPhase 0追加実機調査を完了

- 指定された未ダウンロード書籍 `B0FTDGD2XV` で、ダウンロードボタンの進行中表示、約16秒後のボタン消失、10ファイル・15,162,028 bytesの正式保存先生成を確認し、UIとファイルの2系統で完了条件を確定。
- 指定された小説 `B0DJ3DYD7M` をASINで開き、`Location 1 of 2959 • 0%` の先頭境界、左右ページ要素による往復、既存 `NovelKindleCapturer` の先頭・本文クロップを確認。
- 実内容が小説でもカタログ上は `comic` となる例を確認したため、開始時の `source` はカタログ分類を初期値にとどめ、利用者が `comic` / `novel` を明示変更できる要件へ更新。
- 高頻度のUI Automation tree全走査後にKindleの異常終了を観測したため、直接locatorと低頻度ポーリング、プロセス消失エラーを実装条件に加え、Phase 0を条件付き合格とした。

## 2026-07-25: docs — Kindle自動撮影取込のPhase 0実機成立性を調査

- Windows Store 版 Kindle 1.0.18632.0 の UI Automation tree を実機確認し、検索欄、`library-more-menu-<ASIN>`、未ダウンロードボタン、読書画面の左右ページ要素と現在ページ表示を取得できることを確認。
- 既知 ASIN の検索から対象カードを開き、既存 capturer と同じウィンドウハンドル・タイトルを維持したまま読書画面へ遷移できた。読書画面タイトルはカタログ正式名と一致しないため、本人照合には使わない方針とした。
- 固定レイアウト漫画でフォーカス確保後に `PageUp` を反復し、114 ページ移動後、3 回連続無変化となる読書開始側の境界へ到達。単発無変化と描画待ち不足による誤終了を防ぐ再試行・安定待ち要件を追加した。
- ASIN 検索、漫画先頭移動、ライブラリ復帰は合格。新規ダウンロードの実遷移と `novel` は未確認のため Phase 0 を部分合格とし、利用者指定書籍での追加実機確認を残ゲートとした。

## 2026-07-25: docs — Kindle自動撮影取込の要件と段階実装計画を確定

- 購入書籍を 1 冊選択し、起動済み Kindle アプリでの検索・本人照合、未ダウンロード待機、先頭移動、全ページ撮影、Pic2PDFViewer 登録までを自動化する要件を確定。
- Kindle 自動起動・ログイン・画面ロック解除・複数冊処理は初期対象外とし、候補複数または本人照合不足では撮影せず失敗させる安全側の境界を明記。
- Windows UI Automation の成立性を Phase 0 で実機検証し、合格後にジョブ状態・heartbeat、操作コントローラー、capture agent、UI の順で実装する計画を追加。
- 現行の手動確認方式を現在仕様として維持し、要件・実装計画・バックログ B-34・関連設計書から後続拡張を相互参照できるよう更新。

## 2026-07-25: feat — Kindle購入カタログ画面を通常運用向け4ページへ再構成

- Kindle 領域を購入書籍、画像紐付け、キャプチャ、取込・管理の固有 URL 付き 4 ページへ分離し、共通サブナビゲーションを追加。
- 購入書籍を URL 同期検索、25 / 50 / 100 件ページング、レスポンシブ表、書籍詳細ダイアログへ再構成し、月次・初回管理操作と行内撮影ボタンを分離。
- ASIN 紐付けを対象・候補の比較 + 最終確認へ変更し、キャプチャ準備状態、既存ジョブ、3 系統の差分取込状態と履歴を専用ページで明示。
- API、DB、取込ロジックは変更せず、フロントエンド 1,019 テスト、lint、型検査、本番 build、デスクトップ・iPad 縦横・ライト/ダークの実機確認を完了。

## 2026-07-25: docs — Kindle購入カタログ画面のUI改善要件を確定

- 実データ入り画面をデスクトップ 1440px、iPad 縦 768px・横 1024px 相当で確認し、通常運用上の情報設計と横幅超過、操作優先度の課題を整理。
- Kindle 領域を購入書籍、画像紐付け、キャプチャ、取込・管理の固有 URL 付き 4 タブへ分離し、検索中心の高密度テーブル + 書籍詳細パネルを採用。
- ASIN 紐付けは左右比較 + 最終確認、キャプチャは運用設定完了まで利用準備中、月次取込は既存 3 API の順次・個別実行とする要件と受入条件を確定。
- API、DB、取込ロジック、旧アプリ画像・表紙・レビュー、ASIN 自動確定をスコープ外として維持。

## 2026-07-25: docs — Kindle購入カタログ画面のUI改善を次チャットへ引継ぎ

- 移行済み実データ、現行機能、変更禁止範囲、設計論点、受入条件のたたき台を UI/UX 改善引継ぎ文書へ整理。
- 現行仕様の正本と将来計画を分離し、機能別設計、実装計画、バックログから相互参照できるよう更新。
- 次チャットで現行画面確認、要件インタビュー、受入条件確定、UI案比較の順に進める貼り付け用プロンプトを用意。

## 2026-07-25: fix(kindle) — 既存Samba共有内に撮影受信箱を分離

- Samba管理設定を追加せず運用できるよう、`pic2pdf-input/.kindle-capture-inbox` をKindle撮影成果物の論理専用受信箱とした。
- 同人誌の自動監視はトップレベルの隠しディレクトリを無視し、Kindleの `.partial` / `.ready` packageを同人誌生成へ混入させない。
- 正式な `comic/images` / `kindle_novel/images` は従来どおり共有せず、Linux側の検証・原子的配置だけが書き込む。

## 2026-07-25: fix(kindle) — 漫画の先頭移動を読書領域の画像差分で判定

- Microsoft Store版 Kindle 1.0.18632.0 の漫画読書画面ではフッター文字列がUI Automationへ公開されない実測結果を反映した。
- `ReadingArea` の安定画像を取得し、`PageUp` 後に画像が変わらない状態を3回連続で確認して先頭境界とする方式へ変更した。
- フッターの有無に依存せず、漫画と小説で同じ安全な先頭移動条件を利用する回帰テストを追加した。
- ライブラリから読書画面への遷移中に発生する一時的なUI Automation `COMError` は
  control未検出として再試行し、単発の遷移例外でジョブを失敗させない。

## 2026-07-25: fix — 旧DB移行後のAmazon差分取込で履歴が重複する問題を修正

- 旧DBの日時表現とAmazon CSVのISO日時表現を意味的に比較し、同じKU借用・返品を再登録しないよう修正。
- 購入履歴は注文番号とASINが一致する既存行を優先し、タイトル表現差による重複と既存書名の上書きを防止。
- 既に追加された重複行はサーバーバックアップ後に対象を限定して除去する。

## 2026-07-25: fix — Kindle取込後の統計API応答を修正

- 取込履歴が存在する場合に、統計APIの `last_import` が応答モデル必須項目を欠いて500になる不具合を修正。
- 最新取込履歴へ処理ファイル数とエラー情報を含める回帰テストを追加。

## 2026-07-25: feat — Kindle購入カタログと画像取り込みを統合

- Linux 正本の専用 `kindle_catalog.db`、SQLModel スキーマ、専用 Alembic migration を追加。実測 11,419 冊・単一利用者の条件から SQLite を採用し、PostgreSQL 移行条件を ADR-0016 に記録した。
- `/kindle/catalog` に購入書籍の検索・所有状態・画像状態、旧 DB 移行 preview/commit、Amazon 注文/KU/返品、Kindle Info、シリーズ自動購入の差分取込を集約した。
- 旧 `kindle購入履歴` からは書籍・購入・借用・返品・シリーズだけを読み取り専用で移行し、画像、表紙キャッシュ、画像パス、感想・レビューは対象外とした。
- Pic2PDFViewer の既存 `comic` / `novel` だけを候補表示し、人が確定した ASIN のみ既存メタデータを保持して紐付ける API/UI を追加した。
- Windows キャプチャエージェント、トークン認証付きジョブ API、Samba inbox の `.partial` → `.ready` 転送、manifest・ハッシュ・パス・容量検証後の Linux 正式配置を実装した。
- Kindle カタログ DB をサーバーバックアップ・復元検証へ含め、専用 SQLModel metadata によって既存小説 DB の同名テーブル定義と隔離した。
- バックエンド 975 件、フロントエンド 1,016 件、Kindle ツール 8 件のテスト、型検査、lint、本番 build を通過した。

## 2026-07-25: docs — Kindle購入カタログ統合の実装計画を策定

- `kindle購入履歴` の購入書籍・注文・KU・返品・Kindle Info・シリーズ情報を Pic2PDF_Viewer 側へ移し、通常運用時の外部アプリ依存をなくす方針を確定。
- Linux サーバーを購入カタログ・取込ジョブ・画像正式配置の正本とし、Windows は Kindle キャプチャ専用エージェントに限定する設計とした。
- カタログ DB は件数だけで PostgreSQL を即採用せず、SQLite と10倍/100万行・同時アクセス条件で比較し、Phase 0 の ADR で採否を確定する。
- Windows の成果物は専用 Samba inbox へ `.partial` → `.ready` で転送し、Linux が安全に展開・検証してから正式画像領域へ公開する。
- `kindle購入履歴` 側の表紙・画像キャッシュ・画像パスは移行せず、ASIN紐付け対象の既存画像は Pic2PDF_Viewer 側の comic / novel に限定する。
- Amazon データ取り込み、既存画像の人手確認付き紐付け、Windows キャプチャエージェント、旧 Amazon CSV 機能の段階的撤去を Phase 0〜6 に分割。
- 感想・レビュー・レビュースクレイピング、外部書誌 API、購入金額ダッシュボードは初期スコープから除外。

## 2026-07-25: feat — Hitomi既読作品を履歴として永続保存

- Hitomi検出作品の正本を30日で削除するJSONから `meta2.db.hitomi_arrivals` へ移し、既読後も履歴を保持する方針へ変更。
- 旧 `new_arrivals.json` は主キー競合を無視する冪等移行で取り込み、既存の既読状態を保持する。
- 新着/履歴フィルター、件数、ページングをAPI契約に追加し、Hitomi画面から過去作品を再確認可能にする。

## 2026-07-23: fix — DnDチャンクの循環初期化による起動エラーを修正

- `@dnd-kit` を独立 `chunk-dnd` へ強制した際、CommonJS変換ヘルパーが `api_client` 側へ配置され、起動時に循環参照で未初期化の値を関数として呼ぶ本番ビルド障害を解消。
- DnD・React・Axios等の相互依存が強い共通依存をViteの自動分割へ戻し、独立 `chunk-dnd` と汎用 `chunk-vendor` を生成しない判定を回帰テストで固定。

## 2026-07-23: fix — 同人誌の再オープンで END のみ表示される不具合を修正

- 画像一覧が TanStack Query キャッシュから即時復元される再オープン時に、Reader の初期化 effect がページ数を 0 へ戻したままにする競合を解消。
- 書籍 state のリセット後に画像ページ数を同期し、同じページ数の別書籍への切替でも同期されるよう `selectedPdf` を依存に追加。
- キャッシュ済み画像書籍の初期化順序を固定するフロントエンド回帰テストを追加。

## 2026-07-19: fix — Surya OCR 2の縦書き実測フィードバック

- 実書籍97画面の初回OCRで、ページ全体HTMLを要求しても本文ページがlayout JSONを返すタスク種別ドリフトと、挿絵・飾り枠を本文欠落と誤判定するケースを確認。
- 公式プロンプトを改変せず、layout JSONを検出してbbox単位のblock OCRへフォールバックし、読み順を保ったHTMLへ再構成する設計へ更新。
- OpenAI互換APIへのcontent順序が実装では指示文→画像、公式では画像→指示文だったことを実ページで特定。公式順へ修正し、block OCRがlayout JSONを返した場合も本文として混入させない検査を追加。
- 黒背景を暗画素として全面カウントするcoverage判定を背景非依存の局所エッジ判定へ変更。ページ内の長文block完全重複も幻覚コピーとして不合格にし、検出済みbboxでblock再OCRする。
- 画像・表・目次等の構造ラベルがあり、本文・bboxが正常でcoverageだけが装飾により不足するページは、専用フラグ付きで許容する限定例外を追加。
- 256文字以下の疎な前付けページは、bbox単位のblock再OCR成功または独立画像候補2件の本文98%以上一致を条件に限定許容し、run完了後の原画像スポット確認対象としてフラグを保存。
- 3画面目の細い縦列でSurya block OCRを余白・拡大3条件で実測したが、中国語混入・幻覚が残ったため単独採用を禁止。同ページをyomitokuが3列ともconfidence 0.993以上で正読した結果に基づき、列重複・疎ページだけをyomitokuで補助照合するハイブリッドへ変更。
- 本文画面で同一文を大量反復した壊れたHTMLを実測し、12〜80文字の4回以上連続反復と6,000文字超を不合格条件へ追加。Suryaの全不合格ページをyomitoku高信頼度補助系へ回すよう対象を拡張。
- 非空なのにblockを解析できない壊れたHTMLは`malformed_output`としてSuryaの追加候補を打ち切り、補助系へ早期移行するよう変更。
- llama-serverのcontextを16,384、ページ全体の出力枠を12,288へ公式現行値に合わせ、長い縦書き本文の途中打ち切りを防止。正規化画像も公式の総画素数上限へ揃える。
- 品質閾値を一律に下げず、ページ全体OCR不合格時はlayout→block経路で文字・画像領域を分離して再検査し、全ページ合格時だけ公開する原則を維持。
- 実書籍runを81/97画面の永続チェックポイント（合格67・不合格14）でユーザー指示により停止。未完了runとジョブを中断理由付きで終了し、正式な`books` / `pages` / FTSには一切公開していない。次回は画像SHA-256が一致する合格済み67件を再利用できる。
- 長時間稼働の後半に壊れた出力が増え、新規llama-serverセッションでは同じ画像が合格へ戻る事象を確認。劣化開始ページは一定と断定せず、本文の連続不合格を検知した時点で所有serverを更新し、チェックポイント再開する運用とした。自動server更新は未実装。
- 構造品質ゲートを通過しても、表紙・挿絵・目次・通常本文に固有名詞、小書き仮名、濁点、読み順等の誤りが残ることを確認。全ページ合格時のDB確定は自動のまま、Full Build投入前に前付け・限定例外・本文標本を目視確認する運用を正本化した。
- 設計書と実装の再照合により、`/api/ocr/stop`が待機中ジョブだけをキャンセルする契約、`running`/`failed` runのページSHA単位の再開条件、`SURYA_MAX_ATTEMPTS`、Surya主系+yomitoku限定補助系のファイルマップ表記を正本へ追記。

## 2026-07-19: fix — 新Kindleの全頁キャプチャ安定化

- Microsoft Store版Kindleの実機で、F11中にページ送りすると本文が白画面のままになる一方、最大化ウィンドウでは正常描画されることを確認し、新版だけ最大化方式へ変更。
- 新版のページ送りを反応しない矢印キーから画面左右ボタンのクリックへ切り替え、旧Kindle for PCのキー操作は互換維持。
- ページ変化直後の遷移フレームを保存せず、同一画像が一定時間継続してから保存する安定判定を追加。
- 表紙などページ番号外の画像を含む期待撮影画面数を任意指定可能にし、指定数未満の無変化停止を正常完了と扱わない設計へ変更。Kindleの紙面ページ数と画面送り回数は一致しないため通常は自動終了を使う。
- 新版の上部ツールバー、下部進捗バー、左右ページ送りUIをキャプチャ範囲から除外。
- 実書籍（紙面ページ1〜265 + 表紙）を2回独立に全97画面取得し、同番号同士のpHash差が中央値0・最大2/64であることを確認。正式結果は最終画面がページ265/265、連番97件、SHA-256重複なし、全件3516×1918px（幅×高さ）、全面白画像なし。
- OCR/DBの`page_no`は紙面ページ番号ではなく連番PNGのキャプチャ画面番号であることを正本化し、診断・中断フォルダを`images/`外へ隔離してからOCR投入する運用を追加。

## 2026-07-19: feat — Surya OCR 2 品質保証パイプライン（Windows主系）

- 縦書き小説OCRの既定エンジンをSurya OCR 2とし、Windows RTX 5070上のCUDA対応llama-serverを主系にする設計へ更新。
- ページ単位の構造・文字領域カバレッジ検査、不合格ページの条件別再試行、raw HTML/bbox保持を追加。
- `ocr_runs` / `ocr_page_results` によるページ単位チェックポイントと、全ページ合格後だけ `pages` / FTS / `ocr_done_at` を更新する二段階確定を追加。
- OCR実行先は設定で分離し、将来Macを検証・予備系として利用できる境界を維持。

## 2026-07-19: fix — Kindle for Windows 小説キャプチャ・共通基盤更新（B-27 進行）

- Microsoft Store 版 Kindle for Windows の実機で、本文ウィンドウ検出とキャプチャ保護なしを確認。初期診断後の全頁実測で判明した F11 白画面・矢印キー無反応には、上記「新Kindleの全頁キャプチャ安定化」の最大化＋画面ボタン方式で対応。
- フルスクリーン撮影を対象ウィンドウの実座標基準へ変更し、マルチモニター配置に対応。起動時から全画面だった場合は終了時にその状態を維持する。
- F11 後の案内トーストが実測5秒で消えることを確認し、境界検出・初回画像への混入を防ぐ待機時間を追加。
- 診断スクリプトを現行Win32 HWND型・マルチモニター座標・本文ウィンドウ識別へ更新し、完了済みTM-1だけを案内する旧判定文を撤去。
- 小説の白背景境界検出で空白スキャン行を除外し、検出幅が安全領域の 90% 未満なら安全領域全体へフォールバック。章扉など幅の狭い開始ページによる後続本文の欠落を防止。
- 小説キャプチャ完了メッセージから削除済み `batch_ocr.py` の案内を除き、`/novel/manage` からの OCR・DB 構築へ更新。
- Kindle キャプチャ用の境界検出回帰テストを追加。

## 2026-07-19: refactor — 実装監査に基づく共通化と依存健全化

- POST SSEのストリーム解析をSSE仕様対応パーサーへ移し、CRLF・複数data行・分割チャンクを共通処理。
- Reader Contextをselector購読へ変更し、ページ描画・ヘッダー・ヘルプダイアログが無関係な状態更新を購読しない構成へ変更。
- hitomi監視のNOZOMI・ギャラリーメタ取得で1実行1HTTPクライアントを共有し、接続プールを再利用。
- キャラクター名解析を共通ヘルパーへ統合し、未使用UI依存・ファイルを削除。Knipによる依存検査を追加。
- バックエンドが直接importするPydantic・SQLAlchemy・PyArrowを直接依存として宣言。
- FastAPI・Uvicorn・LanceDB・PyMuPDF等とfrontend依存を互換範囲内で更新し、FastAPI 0.139の内部router表現に依存しないOpenAPI契約テストへ変更。
- backend 948 passed / 1 skipped、frontend 1008 passed、両言語のlint・型チェック・production build・Knip・docs checkを通過。

## 2026-07-19: perf — 小説チャンク文脈の再Embeddingをバッチ化

- Contextual Retrieval の文脈生成成功分を最大16件ずつまとめ、Embedding API・LanceDB delete/add・SQLite更新/commitの呼び出し回数を削減。
- チャンクの書籍名・ページ番号・文字数を最初の対象取得で同時に読み、チャンクごとの追加JOINを廃止。
- 外部ストア更新に失敗したバッチは `contextual_text` を未更新に保ち、次回ジョブで再試行できる回帰テストを追加。
- キャラクター関係生成は novel メタをジョブ開始時に1回だけ索引化し、対象書籍ごとのmeta2.db全件ロードを廃止。
- PDF生成のバックグラウンドTaskを完了まで強参照し、同期ワーカー委譲を `asyncio.to_thread()` へ更新。
- novel_db の一覧・検索・QA履歴・チャット・キャラクターAPIへ具体的なレスポンスモデルを適用し、OpenAPIで `unknown` だったネスト要素を型定義。
- フロントの書籍・検索・履歴・チャット型をOpenAPI生成型へ統一し、チャット詳細の架空フィールドなど手書き契約のずれを解消。
- novel_db のQuery KeyをFactoryへ集約し、books/seriesの共通`queryOptions`を管理画面・DB画面・画像リーダーで共有。画像リーダーの手動books取得を廃止。

## 2026-07-19: refactor — セキュリティ境界・データアクセス・フロント取得層を段階整理

- Windows drive絶対パスとsymlink escapeを防ぐ基準ディレクトリ境界検証を追加し、読書会履歴・OCR・ビルド投入を含むファイルI/Oへ適用。
- `meta2.db`接続のcloseを保証し、メタ更新をsource全行UPSERTから変更行だけのtransactionへ変更。
- novel系の生fetch/useEffectをTanStack Queryへ統一し、検索paginationは`useInfiniteQuery`へ移行。
- OCR/meta2.db/source/OpenAPI/SSE/ADR/フロントファイルパスの設計ドリフトを修正し、完了済みCI計画をarchiveへ移管。
- ライブラリ一括削除のネイティブ `confirm()` を共通 `ConfirmDialog` へ置換し、設計規約と実装を一致させた。
- 独立責務を大規模ファイルから抽出し、ファイルマップとテストを同期。

## 2026-07-18: fix — ライブラリ取得障害の可視化とJSON永続化の保護

- PDF一覧・メタデータ・ジャンル取得の例外を空データへ変換せず、ライブラリ画面に再試行可能なエラーとして表示。
- `useBookImages`をTanStack Queryへ移し、書籍切替時の旧リクエストキャンセルとページ更新時の画像保持を両立。
- Hitomiの`watchlist.json` / `state.json` / `new_arrivals.json`を、一時ファイルと`os.replace`を使うatomic writeへ統一。

## 2026-07-18: fix — Linuxバックアップを復元検証可能な方式へ統一

- Linuxの日次DBバックアップを単純コピーからSQLite Online Backup APIへ変更し、各保存後に`PRAGMA integrity_check`を実施。
- `meta2.db`に加えて`novel.db`とLanceDBを同一世代へ保存し、マニフェストと14日間の世代管理を追加。
- 最新世代を別ディレクトリへ復元して再検査する週次systemd timerを追加し、復元不能なバックアップを早期検出できるようにした。
- medaroserverへ配布し、日次02:00・日曜03:00の両timerをsystem-wideで`enabled / active`化。初回バックアップと別ディレクトリ復元で`meta2.db` / `novel.db`の整合性、LanceDB読み取り、復元service終了コード0を確認した。
- デプロイ前バックアップがSSHのホームディレクトリから起動されるとmoduleを解決できない問題を修正し、`backup-meta.sh`内で`backend/`へ移動してから実行するよう統一した（`590c952`）。

## 2026-07-18: fix — シークレット検査と期限付き依存監査をCIへ追加

- pre-commit / GitHub Actions に gitleaks を追加し、追跡ファイルや新規コミットへの秘密情報混入をブロック。
- backend CI に `uv audit --locked` ラッパー、frontend CI に `npm audit` を追加。ONNX / Torch のGPU互換検証待ちは、理由と2026-10-18期限を持つallowlistに限定した。
- Pillowを12.3.0以上、setuptoolsを83.0.0以上へ更新し、即時修正可能な9件を解消した。
- pre-commit全hook、`npm audit`、期限付きallowlist検証を含むPython依存監査を実行し、未受容脆弱性0件を確認した。

## 2026-07-18: fix — B-28 台本の笑いどころと字数上限を安定化

実機生成で、分析中心の真面目な台本になりやすい点と、リアクションを重ねた際に M1 上限を超えやすい点を調整した。

- **笑いどころの明示**: 「笑える / へえ」の山を最低 4 箇所、テーマごとのボケ / ツッコミ交代、トリビア直後の反応を台本プロンプトへ追加。レイの年下キャラへの甘さとミオのカップリング語りも、脱線限定の小ネタとして人格核へ反映した。
- **尺の上振れ抑制**: 1 ターンを 20〜200 字（大半は 80〜150 字）、全体目標を 3,000〜3,800 字へ調整。3,800 字以降の刈り込みと 4,500 字の絶対上限を明記した。
- **M4 誤検出の抑制**: 正規作品名 `Fate/Grand Order` を英語許可語へ追加し、登録作品名を許容する回帰テストを追加した。

## 2026-07-18: fix — CI frontend / backend / docs の3ジョブを復旧

GitHub Actions の3ジョブがそれぞれ独立した原因で失敗していたため、[CI修理計画書](../archive/CI修理計画書.md) に沿って最小差分で修正した。

- **frontend**: ESLint 10 と `eslint-plugin-jsx-a11y` の peer 競合を ESLint 9 系への整合で解消し、TypeScript 6 と `openapi-typescript` の peer 要求は npm `overrides` で限定的に吸収。npm 10 で lockfile を再生成し、npm 10 / 11 双方の `npm ci`、lint、tsc、998 tests が成功。
- **backend**: 空のサマリテーブルで Ollama embedding を呼ぶ前に `[]` を返すよう処理順を修正。Ollama 到達不能条件を含む全テスト、Ruff、basedpyright が成功。
- **docs**: 実行時生成物 `backend/.coverage` とローカル専用ディレクトリ `backend/data/` / `backend/input/` をファイルマップ生成対象から除外し、クリーン CI とローカル生成結果のドリフトを解消。

## 2026-07-07: fix — B-28 台本の尺・言語リーク安定化（ふつつかな悪女で層1全合格）

ライトノベル系「ふつつかな悪女ではございますが」での再検証 6 回で判明した 2 つの不安定性を修正し、層1全合格（M1 3,105 字 / M2-M5 合格）に到達。

- **尺の較正**: モデルが「長広舌モード（24〜31 ターン × 200 字超 → 5,600〜7,800 字）」と「相槌連打モード（59〜119 ターン × 70〜95 字）」の 2 つの引力圏を往復し M1 を外し続けたため、①ターン長目安を 100〜180 字へ引き下げ（28 ターン × 目安中央値で M1 中央 3,750 字に整合する算術に修正）②全体 3,200〜4,200 字 + 超過時は刈り込む対称指示 ③台本全体 25〜32 ターン厳守の総量上限を追加（`discussion_prompts.py`）。
- **M4 検出強化**: 簡体字セットに 评・级・绝・纯 を追加（「评分」が素通りした実測に基づく。いずれも日本語新字体に同形なし）。台本ステップの言語ルールにも「trope 等の英語批評用語は日本語で言い換え、作品タイトル固有名詞は可」を明記（`discussion_checks.py` / `discussion_prompts.py`）。テスト 55 件緑。

## 2026-07-07: fix — B-28 DoD 層3 フィードバック反映（OPフック導入変更・構成メモ JSON リトライ）

「後宮の烏」の層1全合格台本に対するユーザー層3判定は S2（キャラ一貫性）合格・S1（笑える/へえ 3+）は「内容がまじめすぎて人を選ぶ」→ ライトノベル系（ふつつかな悪女ではございますが）で再検証する方針に。あわせて 2 点を修正:

- **OPフックの導入方法を変更**（ユーザー要望）: 「〇〇だと思って敬遠してる人、損してます」型の書名直入りから、最初の 1〜2 ターンは本に触れない身近な話題・雑談から始めて自然に本へ接続する形へ（`discussion_prompts.py`）。
- **構成メモ JSON の自動リトライ**: 「ふつつかな悪女」初回生成で構成ステップの JSON 出力が崩れ、1 回のパース失敗で全体が error 終了する堅牢性ギャップが発覚。`generate_plan` にパース・バリデーション失敗時の同一プロンプト再試行（最大 3 回、`_PLAN_MAX_ATTEMPTS`）を実装。KV cache により再試行コストはほぼゼロ。テスト 2 件追加（リトライ成功・全滅時の試行回数）で計 38 件緑。

## 2026-07-07: feat — B-28 読書会 番組台本生成（構成化台本 + 固定ホストキャラ、B-20 を置き換え）

「だらだら話すだけ」だった B-20 読書会を、固定ホストキャラ 2 人（レイ＆ミオ）による 10 分尺の番組台本生成に置き換えた。要件・DoD・UI/API 契約は [読書会ロングフォーム拡張_要件.md](../design/要件定義/読書会ロングフォーム拡張_要件.md)（§4.6〜4.11・§10）で事前確定済み。

- **バックエンド（2 段 LLM パイプライン）**: `discussion_cast.py`（人格核定数）/ `discussion_prompts.py`（構成・台本テンプレート + `SEGMENTS`/`SEGMENT_TURNS`）/ `discussion_checks.py`（機械チェック M1〜M5）を新設。①構成ステップ `generate_plan`（対立する推し解釈・テーマ 2 件・脱線ネタカード [facts/keywords 付き] を JSON 生成、temperature 0.4）→ ②台本ステップ（5 セグメント構成 + `[S:id]` 境界マーカー、temperature 0.7）。両呼び出しの system 先頭（書籍本文）を完全一致させ llama-server の KV cache で 2 段目の prompt processing をほぼゼロ化。
- **パーサ寛容化**: Phase 0b で観測したマーカードリフト（`[A]:`→`[A>:` 等）を許容する `_TURN_RE` に差し替え。セグメントマーカーはチャンク分断による ID 誤検出ガード付きで逐次検出。
- **機械チェック（DoD 層1、恒常合否ゲート）**: M1 字数 3,000〜4,500 / M2 5 セグメント出現 / M3 話者分割成功 / M4 言語リーク 0（簡体字 64 字セットは全コードポイントを新字体と別字であることを検証済み）/ M5 ネタカード言及。結果は SSE done イベントと保存 JSON に含める。
- **保存 v2 / API**: `format_version: 2`（cast スナップショット・segments・cards・turns[segment 付き]・checks）。旧 v1 JSON は互換読み。generate API は `{book_name}` のみの破壊的変更（personas/num_turns 廃止）。`DELETE /history/{filename}` 新設（パストラバーサル対策）。JST タイムスタンプに UTC の `Z` を付けていた表記バグも修正（`+0900` に）。
- **フロントエンド**: 読書会ページを「読書会 番組台本」に刷新（書籍選択 + 生成のみ、PersonaPanel・往復数スライダー撤去）。段階表示（構成を考え中→台本を執筆中）・セグメント見出し付き表示（`script-view.tsx` に共通化）・合否バッジ + 不合格内訳 + 再生成ボタン・クリップボードコピー + Markdown ダウンロード（`script-export.ts`）・履歴削除（ConfirmDialog）。v1 旧履歴はフラット表示で閲覧可能のまま。
- **検証**: pytest 891 / vitest 998（108 ファイル）全緑、tsc -b / ruff / eslint クリーン。`generate:types` 再生成済み。
- **実機 DoD 層1 検証（同日追記）**: 「後宮の烏」で 4 回生成し層1機械チェック全合格に到達。1〜3 回目の不合格から判明した実装ギャップを `discussion_prompts.py` / `discussion_checks.py` に修正: ①構成ステップ（カード生成）に日本語限定ルールがなく、カード内の英単語（`trope` 等）が台本ステップにそのまま漏出 → カード生成プロンプトに日本語限定ルール + 言い換え例を追加。②ターン長の目標が緩く、平均字数が M1 下限（3,000字）を割り込む／逆に短い相槌応酬で 59 ターンに水増しされる回があった → ターン長の目標を強化し「短い相槌の応酬で水増ししない」指示を追加。③『BLEACH』等の正規作品タイトルが `_ENGLISH_ALLOWLIST`（意図的に空で用意されていた拡張ポイント）未登録で誤検出 → 追加。修正後の pytest 53 件（discussion 関連）通過確認済み。
- **未了（DoD 層3）**: 層1 全合格の台本 1 本をユーザーに提示し主観判定（S1 笑える/へえ 3+、S2 キャラのブレなし）待ち。合格をもって B-28 完了・バックログ履歴へ移動する。
