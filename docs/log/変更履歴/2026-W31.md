# 変更履歴 — 2026-W31 (2026-07-27 〜 2026-08-02)

[← 索引に戻る](../変更履歴.md)

---

## 2026-08-02: docs — Kindleエクスポートのバージョン選択ルールを追加

- 2026-07-30生成データで、UnifiedLibraryIndexの従来 `_FE` 名が
  `.1.1` / `.2.2` / `.3.1` のバージョン付き構成へ変わったことを確認した。
- RelationshipIndexは `.3.1` が11,727行の本体データで、`.1.1` の有効ASINは
  すべて `.3.1` に含まれ、`.2.2` は有効ASIN 0件だったため旧版を除外した。
- `.3.1` のRelationship / Genres / Authorsを従来の正規名へコピーする運用と、
  将来版では列・件数・Item Owner・ASIN差分を確認してから採用する規則を定めた。
- `SeriesRelation.csv` は自動購入JSONの代替にせず、新エクスポートに含まれない
  `kindle-series-autobuy.json` は既存版を維持する。
- medaroserverの既存入力17ファイルを
  `/opt/pic2pdf-viewer/import/kindle/backups/20260802-194839-pre-versioned-export`
  へ退避し、新エクスポートの対象10ファイルをSHA-256一致確認付きで配置した。
- 差分取込はKindle Info 7ファイル・31,088件、注文系3ファイル・287件が成功し、
  変更のない自動購入JSONは冪等管理によりスキップされた（run ID 11〜13）。

## 2026-08-02: change — Amazon購入データのWindows配置先を追加

- 既存の認証済み `pic2pdf-input` Samba共有に `.kindle-import` を追加し、
  Windowsから `\\medaroserver\pic2pdf-input\.kindle-import` へ展開済みCSV / JSONを
  直接配置できる運用へ変更した。
- Linuxの `AMAZON_DATA_DIR` は従来値を維持し、その参照先を共有内の実ディレクトリへ
  接続して既存データを移動した。
- 隠しトップレベルディレクトリとして同人誌監視から除外し、Sambaアクセスは
  既存どおり `amashio` のみに限定する。

## 2026-08-02: docs — 設計書リファクタリングD0の正本責務を固定

- 111件のMarkdownを棚卸しし、livingリンク、MkDocs nav、design文書サイズの
  blocking違反が0件であることを基準線として記録した。
- Kindle、OCR、B-35、RAGの横断事実について所有文書と競合時の優先順位を定め、
  MkDocs管理外のmodule設計が横断契約を再定義しない規則を追加した。
- D1〜D7の移行台帳を作成し、D0では本文を移動せず、各Phaseで旧記載を
  正本リンクへ置き換えてから所有権を移すfail-closedな手順とした。

## 2026-08-02: docs — Kindle撮影品質ゲートの設計正本を横断同期

- 基本設計とKindle購入カタログ設計へ、撮影前カナリア、version 2 manifest、
  Windows/Linux二重検証、読み取り専用QAだけを少数workerで並列化する境界を追記した。
- API共通仕様へ、完了APIが検証する撮影完了証跡・画像QA・反復オーバーレイ証跡と、
  agent再起動時に途中jobを暗黙再開しない規則を明記した。
- B-32の進捗を実装済み範囲と残作業へ更新し、`capture_overlay.py`分割後の責務記載を同期した。
- 横断監査で、Linux側が再計算できる項目と証跡の内部整合検査を区別し、現行runnerが
  `download_failed`も初回で停止すること、他ASINのactive jobはUIではなくbackendで拒否することを明記した。

## 2026-08-02: fix — Kindle撮影へ反復画面オーバーレイ検出を追加

- OneDrive通知が100画面へ固定表示された実機不良をもとに、通知文言へ依存せず、
  外周の同一座標に構造化タイルが複数の異なるページ間で反復することを検出条件とした。
- 標本の50%以上で隣接2タイルが反復する高信頼候補は登録前にfail closedとし、
  20%以上50%未満は`repeated_screen_overlay_candidate` warningとしてmanifestへ記録する。
- 完全重複ページ、無地余白、単独ページの意匠を除外し、汚染100画面で検出、
  通知除去後の再撮影100画面で非検出となることを回帰基準にした。

## 2026-08-02: feat — Kindle撮影品質ゲートとセッション安全制御を段階導入

- B-32 Phase 1として、撮影画面数、撮影矩形、終了理由、無変化確認回数、
  最終画像参照をversion 2 manifestへ記録する契約を定義した。
- 期待画面数への到達だけでは完了とせず、追加のページ送りで終端を確認する。
- Linux側で連番、復号、寸法、SHA-256、件数、撮影完了証跡を独立再計算する
  fail-closedゲートと、重複・低容量・白紙候補を警告に留める段階導入を定義した。
- Kindle UI操作と登録は直列を維持し、撮影後の不変画像に対するページ単位検査だけを
  少数workerで並列化する品質優先の境界を定めた。
- ローカル既存画像4冊392画面のshadow検証では3冊が厳格条件を通過し、旧debug成果物を
  混在させた1冊を拒否した。曖昧な白紙・低容量・重複候補は警告として保持した。
- Phase 2として、APIのjob作成を全ASIN対象の未完了1件制約へ強化し、シリーズ実行の
  Kindle復旧・連続download失敗をセッション単位で停止する契約を追加した。
- Phase 3として、正式撮影前に同じ矩形・方向で先頭2画面を確認し、先頭へ戻してから
  本撮影を始めるカナリアと、その証跡をLinux側でも再検証する契約を追加した。
- Phase 4として、隣接dHash近似重複と小説上下端の内容密度を警告専用で収集し、
  Codex画像QAの優先順位付けに使う段階導入を定義した。
- 392画面のshadow再評価で近似重複4組、端切れ候補1画面を抽出し、blockingへ
  昇格させず原画像QA対象として保持した。

## 2026-08-02: perf — Kindleクラッシュ復旧を即時・同一ASIN限定に自動化

- 9シリーズ夜間撮影63冊の実測を、正常処理7時間2分、失敗試行1時間11分、
  job間の待機・診断・復旧4時間58分へ分解した。正常時は平均6分43秒/冊、
  1画面約2.9秒であり、長時間化の主要な改善余地を障害後待機と特定した。
- シリーズ直列実行へ、撮影未開始・撮影枚数0・Kindleプロセス消失・同一ASIN再照合・
  未完了jobなし・最大1回を全て満たす場合だけ新しい同一書籍jobを作る復旧境界を追加した。
- 復旧処理を`kindle_capture_recovery.py`へ分離し、直列ランナーの10秒ポーリングから
  即時に呼び出すオプトイン経路と、同一書籍を2回以上復旧しない二重の上限制御を実装した。
- `download_failed`、書籍照合、位置決め、撮影、転送、登録の失敗、プロセスが残る
  UI不調は自動復旧せず、後続巻を作らないfail-closed方針を維持した。

## 2026-08-01: docs — B-35を第三シリーズ30画面で再評価

- 講談社タイガ版『虚構推理短編集　岩永琴子の密室』から、候補確認前に固定した通常散文
  10画面を原画像QAし、先行2シリーズと合わせて30画面・48,296字のground truthを確定した。
- 第三シリーズ単体は配布NDLOCR 1.485%、追加学習版1.214%だった。4候補合議＋固有名詞補正は
  CER 0.474%まで改善したが、ページ最大3.470%、固有名詞529/536で機械総合ゲートは不合格とした。
- 候補間最大距離6.04%でも片仮名・平仮名の共通反復誤りが残り、15%の距離閾値をSol省略条件から外した。
- 固定30画面をすべてCodex原画像QAした運用候補はCER 0.000%、ページ最大0.000%、列欠落0、
  固有名詞536/536で合格した。正解化実績を機械性能と分離し、自動公開とSol省略は継続する。

## 2026-08-01: feat — B-35固有名詞補正とページ昇格をfail-closed化

- シリーズ固有名詞台帳を正解本文から分離し、同位置の独立候補が支持する同長1文字置換だけを
  自動補正する方針を追加した。候補支持なし、競合、挿入・削除は本文を変えず監査提案へ残す。
- 未解決の反復近似、または4候補の最大正規化距離15%以上をCodex原画像QAへ送る条件を固定した。
- 機械補正候補とCodex補助込み候補を別レポートにし、DB・OCR run・公開本文を変更しない。
- 候補支持付き固有名詞11件の補正で機械CERを0.398%へ改善したが、ページ最大1.647%と
  固有名詞1件の残差により機械単独は不合格を維持した。
- 7画面をCodex QAへ昇格した補助込み候補はCER 0.23%、ページ最大0.714%、固有名詞278/278、
  列欠落0で2シリーズ用ゲートに合格した。完成済み機械レポートへQA結果だけを適用する軽量経路を追加した。

## 2026-08-02: test — 公開文完全一致のSol high監査を実施

- 修正後候補から高リスク語へ一致する詳細21文、一覧7文、人物説明13文を機械抽出し、公開物内の位置、
  公開物ハッシュ、文ハッシュを持つ41件の監査入力を再現固定した。
- 別の新規Sol highへ41文と確定OCR全文77ページを渡し、277.641秒、入力109,470 token、出力12,163 tokenで
  40件supported、E18の1件をunsupported / majorと判定した。ローカル側でも入力再現性と集計を検証した。
- r5で不合格となったR06/R10に対応する実際の公開文は支持され、監査用言い換え由来の偽陽性を除去できた。
  一方、詳細版が仁耀の「あとは頼んだ」の対象を白楼国の未来へ限定した実際の根拠不足を新たに検出した。
- 1回修正上限に従い再修正・公開DB反映は行っていない。主生成から今回の完全一致監査までのSol high合計は
  959.073秒で、次はE18だけを不確実性保持表現へ直す別承認の修正サイクルが必要である。

## 2026-08-02: test — Sol high限定修正と独立評価を検証

- high候補の4エラーと71〜79ページ・12,233文字だけを新規Sol highへ渡し、87.244秒、入力28,252 token、
  出力3,255 tokenで拘束fact、途中意向、最終決定、解放・離脱を限定修正した。基準ハッシュと変更範囲を
  決定的に検査して適用し、49 factの候補はローカルゲートへエラー0件で合格した。
- 別の新規Sol highへ14主張と48ページの原文窓だけを渡し、192.664秒で12件supported、重大2件contradictedを
  得た。R06は監査用言い換えだけが翔景を皇帝発見主体へ追加し、R10は将来の最長10年逃亡依頼を完了形へ変えた。
- 詳細版はR06の主体とR10の将来性を保持していたが、一覧版にはR10と同じ時制誤りが残った。監査用主張を
  モデルの言い換えで作ると公開文との対応が失われるため、今後は完成文の完全一致抜粋と正規化主張を分離する。
- 1回修正上限に従って再修正・再評価は行わず、公開DBも変更していない。3回のSol high合計は681.432秒だった。

## 2026-08-02: test — Sol highを同一中立条件で比較

- mediumと同じOCRハッシュ、プロンプト、Schemaを使い、reasoning effortだけをhighへ変更して1冊を再生成した。
  401.524秒、入力101,421 token、出力21,843 token、推論1,552 tokenで、事実47件、詳細1,641字、
  一覧414字、人物10名、高リスク主張14件を生成した。
- highはmediumで一覧版から欠落した「最長十年間」と黎天河を回復し、詳細版の網羅性も改善した。
  一方、牢へ戻る途中意向と最終決定を別factへ分離できず、珀陽を根拠なく戦術立案者・指揮承認者として断定し、
  拘束factへ仁耀の侵入行為と黎天河の拘束行為を混在させた。
- ローカルゲートは不合格とし、独立評価と公開DB反映は実施していない。表層語形だけを見る期間ゲートも補正した。
  highはmediumより良いが公開品質には未達で、次は高リスク場面の決定的な事実分割を検討する。

## 2026-08-02: test — 固定正解除去後のSol mediumを再検証

- 固定ケースの期待値を主生成から除外し、正規名enumと一般的な役割配列規則だけを与えて、同じ10巻OCRを
  Sol mediumで再生成した。364.542秒、入力101,421 token、出力19,937 tokenで、事実48件、詳細1,271字、
  一覧409字、人物10名、高リスク主張12件を生成した。
- 事実表は「最長10年間」と黎天河を保持したが、牢へ戻る途中意向と最終決定を別factへ分離せず、一覧版から
  限定期間と黎天河が欠落した。珀陽を拘束主体のように読める表現と、根拠を越えた役割推定も残った。
- ローカルゲートを3件のエラーで不合格とし、独立Sol評価、候補の自動修正、公開DB反映は実施していない。
  r2のローカル合格は正解誘導の影響を受けていたため、次は同じ中立契約をhighで比較する。

## 2026-08-02: test — Sol厳格化再生成と高リスク独立評価を実施

- 正規名を既存10名のSchema enumへ固定してSol mediumを再実行し、事実48件、詳細1,367字、一覧402字、
  人物10名、高リスク主張16件を410.709秒で生成した。ローカルゲートはエラー・警告0件で合格した。
- 別の新規Solセッションへ16主張と31ページの原文窓だけを渡し、102.854秒で12件supported、
  重大3件・軽微1件のunsupportedを得た。主生成の事実表、要約全文、理由、予測役割は評価入力へ含めていない。
- 固定ケースの期待役割を主生成へ与えたことで、原文より細かい立案・指揮分担が3成果物へ伝播した。
  固定正解を主生成から評価fixtureへ移し、主体役割を複数人物・根拠ページ付き配列へ改める方針に修正した。
- 独立評価はfailとし、候補の自動編集、再評価、公開DB反映、既存要約・人物辞典の置換は行っていない。

## 2026-08-02: test — Sol mediumの10巻1冊主生成を隔離検証

- ChatGPT認証の`gpt-5.6-sol` mediumへ、10巻のOCR確定本文77ページ・105,830文字を1回だけ入力した。
  333.657秒、入力101,054 token、出力15,499 tokenで、事実50件、詳細1,326字、一覧406字、人物11名を生成した。
- 牢へ戻る途中意向と最長10年の最終決定、拘束の物理的実行者、殺害未遂、戦争回避の未確定性を保持し、
  従来の期間断定化、中国字混入、主要な主体誤りを改善した。
- 正字を断定できない台帳外人物を人物辞典へ追加したためローカルゲートで停止した。一覧版の拘束実行者省略も
  警告とし、別Sol独立評価、公開DB反映、既存要約・人物辞典の置換は行っていない。
- 原文再確認に基づき、拘束作戦の役割を戦術立案=皓茉莉花、指揮・承認=珀陽、物理的実行=黎天河、対象=仁耀へ分割した。
  過去の巻全体ハッシュは算出式未記録で再現できなかったため、個別ブロック一致を確認し、新しい算出式を記録した。

## 2026-08-02: docs — Sol主生成・選択的独立評価の設計を追加

- OCR承認済み本文から、事実表・詳細あらすじ・一覧用要約・人物辞典をSolの単一実行で候補生成し、
  ローカル機械ゲートと高リスク主張だけの別セッションSol評価を通してから人間が公開承認する方針をADR 0018で採用した。
- Mac上のCodex CLIはChatGPTサブスクリプション認証を使い、書籍ごとに新規・一時セッションで実行する。
  API課金への自動切替、Linuxサーバーへの認証情報コピー、画像・PDF・識別子・不要なパス情報の送信は禁止する。
- Qwenは対話QAと外部送信しない代替経路として維持し、Sol失敗時にQwen生成物を自動公開しない。
  初期段階は10巻の隔離パイロットに限定し、現行コード、DB、公開要約・人物辞典は変更していない。

## 2026-08-02: test — Compact厳密化Luna抽出とQwen主生成の検証結果を記録

- 10巻の隔離スナップショットを4ブロックへ固定し、各Luna実行を独立コンテキストに分離して、
  状態契約、厳格Schema、先頭・末尾網羅、高リスク判定の機械ゲートを適用した。
- Compact後の会話要約を正本にせず、入力SHA-256、正規名、許可Schema、主体役割、完了状況、禁止事項を
  外部の状態契約JSONから復元する条件とした。Compact自体は発動せず、改善はブロック分離と外部契約の効果と判定した。
- Lunaは有界再試行後も4ブロック中3ブロックだけ機械合格し、自動経路はfail closedとした。比較完走用に限り、
  原文照合済みの別出力を手動救済として分離し、Qwen3.6 35B-A3Bへ渡した。
- Qwenは詳細1,637字、一覧529字を合計470.43秒で生成し、詳細版では拘束の作戦主体と実行者を改善した。
  一方、「最長10年」の断定化、中国字混入、一覧版の主体省略と戦争回避の過剰確定が残り、公開不可とした。
- 公開DB、OCR本文、画像、ページ索引、既存要約・人物辞典は変更せず、隔離領域だけで品質と時間を評価する。

## 2026-08-02: test — B-36のCodex Luna構造化再現計測を記録

- 前回と同じ10巻隔離スナップショット、入力SHA-256、4ブロック境界を使い、actor_role、fact/inference/unknown、
  開始・終了時刻、wall-clock、入出力文字数を工程別JSONへ保存した。
- 74〜75ページ固定ケースは初回意味判定失敗後、37.377秒で正しく再判定した。巻全体では事実抽出の許可外役割名、
  詳細・短縮版のSchema違反と主体誤り、人物辞典の人物同定課題、独立監査の重要事実欠落を検出し、fail closedとした。
- 詳細約1,100字、短縮502字、人物10名を得たが、Qwenの代替生成・補助初稿・自動公開には採用しない。固定ケースと
  少数の高リスク主張を人間確認付きで監査する候補に限定する。
- 実験結果は隔離一時領域へ保存し、公開DB、OCR本文、ページ索引、画像、既存要約・人物辞典は変更していない。

## 2026-08-02: docs — B-36のCodex Luna固定・巻全体比較を記録

- 10巻の隔離スナップショットで、Qwenと同じOCR本文・ページ境界・対象人物・評価軸を可能な範囲で揃え、
  `gpt-5.6-luna`の事実抽出、詳細あらすじ、一覧用短縮要約、人物辞典、事実確認を比較した。
- 74〜75ページの固定誤りでは、仁耀の「牢へ戻る」を途中状態、「最長10年間逃げ続ける」を最終状態として
  正しく判定した。巻全体も主要事実と最終状態を保持したが、拘束主体の単純化が残ったため、品質は条件付き合格、
  自動公開は不採用とした。
- Luna・Qwenとも役割別の再現可能な所要時間が今回の保存物にないため、速度の優劣は結論づけていない。
  公開DB、OCR本文、ページ索引、画像、既存生成物は変更せず、コミットも行っていない。

## 2026-08-02: docs — Gemma4 31BのQwen役割別比較を記録

- 10巻の隔離DBで、事実抽出、詳細あらすじ、一覧用短縮要約、人物辞典、完成文検証を
  Gemma4 31Bへ割り当て、Qwenとの品質・時間差を比較した。
- 詳細版は889字・28分31秒、短縮版は490字・22分30秒で自然な日本語を生成したが、
  正しい最終合意の省略と主体・時制の意味変更を確認した。
- 事実抽出、人物辞典、巻全文の一括検証は各20分の評価上限内に完了しなかったため、
  Qwenを生成の主系統として維持し、Gemma4 31Bは高リスク主張の局所的な独立検証へ限定する。
- 実験は隔離DBだけで行い、公開DB、OCR本文、ページ索引、画像は変更していない。

## 2026-08-02: docs — Gemma4 31BをB-36比較の主対象に変更

- `gemma4:31b`（19GB）を64GB Macへ導入し、10巻74〜75ページの固定誤りを12B・Qwenと
  同じ温度0・同一プロンプトで比較した。
- 31Bは最終行動と誤要約を正しく判定し、12Bが見落とした事実メモ内部の矛盾も
  `partially_contradicted`として検出した。実行時20GB・100% GPUで、監査と校正の合計は84秒だった。
- JSONコードフェンスと校正時の軽い意味変更は残ったため、31Bを独立第二検証役の主候補とする一方、
  既定検証モデル、執筆、校正の自動確定役には切り替えない。
- 正解固定10〜20件以上でQwen・Gemma4 12B・31Bを比較してから採用判断する方針を記録した。

## 2026-08-02: docs — B-36のQwen・Gemma4固定比較を記録

- 10巻で判明した仁耀の最終行動を固定ケースにし、Qwen3.6 35B-A3BとGemma4 12Bを
  温度0・同一本文・単独GPU常駐で比較した。
- Qwenは「牢へ戻る」をsupportedと誤判定したが、Gemmaは「最長10年間逃げ続ける」を選び、
  誤要約をcontradictedにできた。
- GemmaはJSONコードフェンスを付け、事実メモ内部の自己矛盾を完全には指摘できなかった。
  校正文章は表面上自然だったが、「宿へ急行」を「逃走し、隠れ家へ」と変える意味変更があった。
- Gemmaを既定モデルへ切り替えず、Qwen執筆後の独立第二検証役として、固定10〜20件以上の
  コーパスで事実判定・契約遵守・意味保存を追加比較する方針とした。

## 2026-08-01: feat — B-36人物正規名統合と削除回帰ゲート

- 同じ巻の公開済み人物名だけを事実抽出へ正規名ヒントとして渡し、本文に登場する場合の
  抽出漏れと見出し表記揺れを抑える設計にした。公開済み人物説明や他巻知識は入力しない。
- 公開版正規名との完全一致、一意な短縮形、事実メモ内で明示された別名だけを統合し、
  曖昧な部分一致による別人物の誤統合を避ける。
- 正規化後も現在本文に根拠がある公開済み人物が新候補から消えた場合は、要約・短縮要約・
  人物辞典の一括確定を中止して旧版を維持する削除回帰ゲートを追加する。
- 10巻隔離パイロットで35Bが出力した`[page 18, page 20]`形式を、根拠欠落と誤判定して
  安全停止する事例を確認した。複数ページを同じ角括弧へまとめた場合も全番号を抽出し、
  各番号を入力ブロックの許可ページと照合するよう事実パーサーを拡張した。
- 公開版人物名が当該ブロックに登場しないとき、35Bが`[CHARACTER_FACT:芳子星]`へ
  `該当事実なし`を返す事例を確認した。この明示的な空候補だけは人物事実から除外し、
  実際の事実文にページ根拠がない場合は従来どおりfail closedとする。
- 隔離パイロットの詳細あらすじは、検証モデルが主張1で許可候補外ページを引用したため
  fail closedとなり、旧版を維持した。構文・候補外引用で失敗した監査ログにも、候補要約の
  主張本文、主張別候補ページ、実提示ページを残し、検索候補とモデル引用を切り分け可能にした。
- 再試行した詳細あらすじは双方向根拠検証を通過した一方、一覧向け短縮要約で同じ候補外引用が
  発生した。検証判定を緩めず、JSON・主張ID・引用ページなど出力契約の違反だけを1回訂正させ、
  訂正版を同じ厳格検証へ通す有界再試行を追加する。内容上の不合格は再試行対象にしない。
- 有界訂正後も別主張で候補外引用が残る監査結果から、RAG候補を先に並べたことで、要約の
  直接材料だった事実表ページが全体文字数制限から落ちていることを特定した。候補順を事実表、
  RAG補完の順へ変更し、各主張の上位2候補を検証本文へ優先採用する。
- 候補優先後の監査では、モデルが引用したページ9は別主張の候補として検証プロンプトへ本文を
  全文提示済みであり、主張内容も直接裏付けていた。主張別候補は検索優先度として維持しつつ、
  引用許可は実際に本文を提示した全ページへ広げ、事実表に番号だけある未提示ページは拒否する。
- 詳細版合格後の短縮版監査で、逃亡説の直接候補ページ61に対して、その結論が始まる隣接ページ60が
  未提示となるページ境界の取りこぼしを確認した。直接候補の前後1ページを補助候補へ加え、
  本文が連続ページへ跨る場合も全文を照合できるようにする。
- 旧上限60,000文字・48ページでは隣接ページ60が候補追加後も落ちた。10巻短縮版で
  64ページ・90,000文字へ設定すると56ページ・80,419文字で保持でき、131kコンテキストにも
  余裕があることを確認して証拠本文上限を更新した。
- 後続パイロットで編集済み詳細版が確率的に87文へ膨らみ、64主張の検証上限で安全停止した。
  上限自体は維持し、モデル呼び出し前の停止でも候補全文と全主張を監査保存するようにした。
- 最終設定で詳細版は機械根拠検証に合格し、短縮版の保存済み実応答も9/9主張が再生検証に合格した。
  一方、Codex差分QAは本文75ページの「仁耀は最長10年間逃げ続けることに同意」を、候補が
  「牢へ戻る」と誤った重大矛盾を検出した。事実表自体の意味矛盾が原因のため候補を不採用とし、
  本文への事実表意味照合を全冊再生成前の残件とした。

## 2026-08-01: feat — 一覧の短い要約と詳細あらすじを画面上でも分離

- 通常の書籍一覧とシリーズ絞り込みで共通利用する`BookCard`に、
  `catalog_summary`を「短い要約」として最大4行表示する仕様を追加した。
- 書籍詳細は`summary`だけを「詳細あらすじ」として表示し、一覧向け短縮版を
  詳細版の代替にしない用途分離を明記した。
- 短縮要約が未生成の旧書籍では一覧の要約欄を省略し、既存カード表示を維持する。

## 2026-08-01: docs — B-35新規holdoutを原画像QAしCERを再評価

- run 70 / 71から固定した通常散文20画面を画像SHA照合後に原画像と全文比較し、132箇所を補正して
  正規化後27,629字のverified ground truthを確定した。選定外ページとrun全体は未承認・非公開を維持した。
- 機械候補はprimary 3.18%、external 2.24%、配布NDLOCR 1.38%、追加学習NDLOCR 0.81%、
  文字位置コンセンサス0.44%だった。集計CER目標は達成したが、ページ最大1.946%と固有名詞12件の
  誤りが残るため、総合ゲートとB-35は未完了とした。
- Codex原画像QA後の0.00%を正解化実績として機械性能から分離し、次工程を固有名詞台帳、
  ページ単位昇格、第三シリーズを含む正式holdout再評価とした。
- ground truth APIが全件CERを都度再計算して60秒を超える事例を、契約不変の増分集計候補TM-2へ起票した。

## 2026-08-01: docs — B-35新規2シリーズholdout候補とKindle実機知見を固定

- 購入済み小説から『悪役令嬢レベル99』120画面と『後宮一番の悪女』80画面を撮影し、
  画像QA後にOCR run 70 / 71を`awaiting_qa`で停止した事実を記録した。
- 各runの通常散文から候補品質を見ず等間隔に10画面ずつ選び、画像SHAを未調整holdout候補として
  固定した。文字単位画像QA前のground truth登録、run承認、公開、索引生成は禁止した。
- 『虚構推理（講談社文庫版）』のWindows版Kindle非対応と、ダウンロード・モーダル遷移後に同一UIA treeを
  再利用した際の`ucrtbase.dll / 0xc0000409`終了、Kindle再起動後の成功を実機知見へ追記した。
  作品単位では除外せず、取得可能な講談社タイガ版は別商品として後続確認する。

## 2026-08-01: feat — B-35のPARSeq復元・全量追加学習・最終holdout評価を実施

- 上流PARSeqのTiny定義と、NDLOCR-Lite配布日本語ONNXのinitializerから、互換PyTorch
  state dictを復元する隔離pilotへ進む方針を追加した。
- 入力・ソース・文字集合のSHA固定、全parameterの一意対応、形状・有限値、固定入力と
  実画像列におけるONNX Runtimeとのtop-1完全一致・logits誤差を復元合格条件にした。
- 復元checkpointは監査領域だけに出力し、同値性とrun分離pilot validationの両方が
  合格するまで、全量学習、本番ONNX差し替え、最終holdout開封を禁止した。
- ONNXから全175 tensorを復元する監査CLIと、537/185分割だけを使う追加学習CLIを追加した。
  復元は3,333 token位置のtop-1完全一致、短期学習は完全一致率61.0811%→71.3513%、
  NED 96.9124%→97.6639%となり、両ゲートへ合格した。
- 58 run・1,465画面から生成した49,779行を全件監査し、同一ラベル重複31行と画像確認済みの
  競合7行を除外した。確定49,741行はtrain 45,861／validation 3,880で、最終holdout混入と
  split間の画像SHA重複が0件であることを確認した。
- 全量LMDBの容量を入力バイト数から算出し、256 MiB以上・1.5倍の余裕・64 MiB単位で
  切り上げるようにした。pilot checkpointの検証漏洩を避け、全量学習はONNX同値復元checkpoint
  から開始する方針を明記した。
- 45,861行train／3,880行validationで全量追加学習し、3 epochの早期停止で完全一致率
  67.2680%→89.4072%、NED 97.9581%→99.4649%、loss 3.6022→0.5643へ改善した。
- 固定長101 tokenでONNXへ変換し、ランダム入力と32実画像・3,333 token位置でtop-1不一致0件を
  確認した。予約4 run・223画面・312,156字を一度だけ開封し、配布ONNXのCER 3.2843%から
  追加学習ONNXの2.6682%へ改善したが、B-35の0.5%には未達だった。
- ページ単位oracleは2.6471%、参照文字位置oracleの未回収率も1.5941%（4,976字）だった。
  bbox併用の列欠落疑いも4画面残ったため本番ONNXは差し替えず、同じ最終holdoutを見た再調整を
  禁止した。列欠落と文字認識誤りを分離し、固有名詞注釈付きの新holdoutで再評価する。
- 削除率だけの列欠落疑い5画面を原画像・bboxで照合し、内部列gapを伴う実欠落4画面と章見出し後の
  意図的な空白1画面へ分類した。削除率と縦列中央x間隔中央値比を併用する自動判定へ更新した。
- 開封済みholdoutで欠落列cropを全件診断し、白紙・UI等を除く36列を32画面へ補完すると
  全画面改善・悪化0、CER 2.6682%→2.2758%となった。holdoutを見て定めた濃色画素条件は
  本番採用せず、再現可能な診断manifest生成と新holdoutでの再評価へ分離した。SHA照合付き
  再生成でも77画面・172候補・除外136件・採用36列／32画面が一致した。
- 未使用runを再監査し、run 4 / 8 / 13の通常散文201画面を系列内事前検証用に予約した。
  各runから均等20画面、計60画面を固定した。3冊とも茉莉花官吏伝のため正式な3シリーズ条件には
  数えず、新規2シリーズ以上を待つ。原画像QA前のテキストはground truthへ登録しない。
- 60画面の候補差と両側アンカー付き補完提案から19画面を画像QA優先対象にした。初回3画面は
  すべて実欠落で、primary / externalが互いに別の列を落とす例も確認したため自動採用しない。
- 非公開の事前検証パッケージと優先キューを再生成するCLIを追加し、60画面・優先19画面の一致を
  確認した。委任時の対象取り違えを検出するため、件数とrun・ページ・画像SHA集合のdigest、同じCLIで
  検証する`--verify-queue`を追加した。
- digest固定の補助分類で19画面中16画面に候補間のまとまった欠落差を検出した。Solが全19原画像を
  確認し、すべて通常の縦書き散文と確定したが、正解文ではないため自動補完・公開更新は行っていない。
- 配布ONNXからの互換checkpoint復元条件、行認識validationとページCERを分離する判断、開封済み
  holdoutの診断値を新しい未調整holdoutの合否へ流用しない原則を技術知見へ整理した。
- 設計とCLIの強制範囲を再監査し、正式holdoutのシリーズ数、同じholdoutの再開封拒否、事前検証
  60画面全体のdigestは未実装の機械ガードと明記した。B-35完了前のfail-closed化へ残した。

## 2026-08-01: docs — B-35第三OCR・oracle CER評価手順を確定

- 現行機械候補、保存済みprimary / external、NDLOCR-Lite、隔離YomiToku最新版を
  固定コーパスで比較し、必要時だけPP-OCRv5へ進む順序を確定した。
- 単体CERに加えてページ単位oracle、参照位置oracle、bbox付き候補の列単位oracleを測り、
  0.3%以下なら列選択器、0.3〜0.5%ならCodex確認付き限定選択、0.5%超なら部分再OCR・
  新認識器・追加学習へ分岐する停止条件を追加した。
- 初期通常散文4画面は4,680字・編集距離85で、0.5%以下には編集距離23以下まで
  62文字以上の改善が必要であることを明記した。
- 調整用固定標本と3シリーズ以上・通常散文20画面以上の未調整holdoutを分離し、
  oracleを公開合格値やCodex補正文と混同しない運用を定めた。
- 再現可能なNDLOCR-Liteランナーと候補比較ランナーを追加し、固定20画面を実測した。
  NDLOCR-Liteの通常散文CERは4.00%、既存候補集合の参照位置oracleは1.218%であり、
  Tesseractを追加しても0.5%へ到達しないことを監査JSONと設計書へ記録した。
- 隔離YomiToku 0.14.0は通常散文3.74%でoracleを改善しないことを確認した。
  PP-OCRv5用の隔離workerも追加したが、通常散文1画面でCER 59.04%・約197秒だったため
  全画面評価を停止し、次工程を縦列切り出し・拡大再OCRへ変更した。
- 85編集を文字種と操作へ再分類し、56置換が全角 / 半角・横棒字形だけの差、14削除が
  ページ60の単一連続欠落であることを特定した。CERへNFKCと横棒統一の版付き評価規約を
  追加し、正規化後約0.62%、列欠落回復後約0.32%という到達経路を設計へ反映した。
- normal_prose限定のgap consensus評価器を追加した。独立候補2件以上と前後8文字アンカーを
  必須にしてページ60の欠落1件だけを補い、固定通常散文CER 0.342%、ページ最大0.443%、
  固有名詞100%を達成した。画像主体ページの誤挿入を確認し自動対象外へ固定した。
- 4シリーズ・通常散文20画面・29,509字のholdoutを固定し、保存済みprimary / externalを
  補正済み本文と混ぜずに再評価できる候補読出しと評価スコープ分離を追加した。
- holdout単体最良はNDLOCR-LiteのCER 3.61%、ページ単位oracleは1.484%、参照文字位置
  miss rateは0.285%だった。固定標本で合格したgap consensusは9.80%で汎化せず、
  次工程を列・文節単位のground truth非参照合議とした。
- 候補本文だけを使う文字位置合議を追加・評価したが、固定通常散文0.94%、holdout 2.49%で
  0.5%へ届かなかった。同系統誤りの多数決を避けるため本番採用せず、部分再OCRへ進めた。
- Surya OCR 2をNDLOCRのbboxで4列・2列・1列へ切り出して難例1画面を再認識したが、
  CER 31.69% / 16.74% / 17.49%で列省略が残った。全件実行を停止し追加学習評価へ進めた。
- 公開済みQAを読み取り専用で棚卸しし、追加学習候補となるCodex補正済み通常散文
  1,688画面・約233万字・62 runを確認した。現行Surya OCR 2ではなく、公式の学習・ONNX
  差し替え手順があるNDLOCR-Lite PARSeqをrun分離holdout付きでpilotする方針を追加した。
- 本文・画像を出力しない追加学習コーパス棚卸しツールを追加し、run 30 / 49 / 66 / 69を
  最終holdoutへ予約した。holdoutは4 run・223画面・312,156字、train候補は58 run・
  1,465画面・1,958,579字で、run単位の重複がないことを機械検証した。
- NDLOCR縦列bboxとCodex補正文を整列する隔離データ生成器を追加した。初回20画面の画像QAで、
  列末尾の1字欠落が隣接列へ誤配分される事例を検出し、境界許容を0字へ厳格化した。
- 厳格化後の20画面pilotでは833検出列中722列・24,658字を採用し、高CER上位列の画像QAと
  final holdout run未使用を確認した。次工程をrun分離LMDBによるPARSeq 1 epoch学習とした。
- RTX 5070 12GBで標準PARSeq 29.4Mの学習経路を確認した。1 epochは約47秒で完走したが、
  NDLOCR-Lite配布物には参照先`parseq-tiny`定義と初期checkpointがなく、標準モデルの新規学習に
  置換せざるを得なかった。20 epochでも別run検証は完全一致0%・最大NED 1.897%で不合格とした。
- 精度候補が成立しなかったため全量学習・ONNX変換・本番差し替え・最終holdout開封を停止した。
  run 30 / 49 / 66 / 69を未使用のまま維持し、互換初期重み入手まではCodex画像QAを残す。
## 2026-08-01: feat — 詳細あらすじと一覧向け短縮要約を分離

- 網羅性を優先する既存`books.summary`とは別に、選書用400〜700文字の
  `books.catalog_summary`と生成日時をAlembic revision 0011で追加した。
- 詳細版の双方向根拠検査合格後に短縮版を生成し、短縮版は意図した情報省略を許しつつ、
  書かれた全主張の本文根拠を検査する用途別品質ゲートを追加した。
- 詳細版、短縮版、人物辞典を単一トランザクションで公開し、監査スナップショット、
  差分、復元、書籍一覧・詳細API、OpenAPI生成型も両サマリ対応に更新した。

## 2026-08-01: feat — 要約検証モデルを独立backendへ切替可能にした

- 根拠検証は既定で執筆Qwenを直列再利用し、追加メモリを要求しない構成とした。
- 環境変数だけでOllamaまたは独立`llama-server`の別系統モデルへ切り替え、
  writer/verifierのモデル名を監査ログで区別できるようにした。
- 64GB Macでは大規模モデル同士を並行実行せず、独立モデル利用時も直列運用と
  メモリ・swap実測を採用条件とした。

## 2026-08-01: feat — 要約へ双方向RAG根拠検査を追加

- 要約を文単位の主張へ分解し、FTS5・bge-m3・RRFと事実表の根拠ページを併用して
  `supported / contradicted / unsupported`を判定する設計を追加した。
- 事実表から要約への逆方向検査で、主要な発端・対立・転機・結果・関係変化の欠落も
  同時に検出し、いずれかの不合格時は公開一括確定へ進まないfail-closedとした。
- 候補SHA-256、使用モデル、主張別判定、根拠ページ、欠落事実を
  `summary_grounding_reports`へ保存し、再生成の採否を追跡可能にした。

## 2026-08-01: feat — 小説事実抽出を細分化し再利用可能なチェックポイントを追加

- 要約前の本文を約3万文字のページ境界ブロックへ分割し、長文中央の事実取りこぼしを
  抑える品質優先方針へ変更した。
- ブロックごとのページ範囲、本文ハッシュ、モデル、抽出仕様、生の事実とページ根拠付き
  構造化レコードを`fact_extraction_blocks`へ保存する設計を追加した。
- 本文・モデル・抽出仕様が一致する完成ブロックを再実行時に利用し、後段失敗時も
  公開中の要約・人物辞典を変更せず、事実抽出だけを再利用できるようにした。
- M1 Max 64GBでは27B Dense 6bitの2プロセス常駐を標準構成とせず、同一サーバー直列実行か
  軽量検証モデルとの組み合わせを実測メモリ・swapで判定する方針を記録した。
## 2026-08-01: chore — Codexサブエージェントのモデル別運用を設定

- プロジェクト固有のサブエージェント運用設計を追加し、Lunaは読み取り専用の定型監査、
  Terraは所有範囲を限定した小規模実装、Solは要件・設計・統合・状態変更を担当すると定義した。
- Luna完結条件、Terraの書き込み委任条件、Solへの必須昇格条件、権限継承とfail closed、
  重大事故1件で全件Sol確認へ戻す段階導入基準を定義した。
- `.codex/config.toml`で同時実行数を3に制限し、`luna-triage`と
  `terra-focused-worker`を追加した。既存`docs-cross-checker`はTerra、
  `refactor-planner`はSolへ固定した。
- `AGENTS.md`へモデル選択、書き込み所有権、最終統合責任のルールを追記した。

## 2026-08-01: docs — OCR補助のLuna・Sol委任境界を定義

- B-35の代表通常散文1画面でLunaを予備評価し、low 8.39%、本文切り出し＋medium 1.63%、
  機械候補併用1.20%、highは4分で完成本文なしという実測を記録した。
- Lunaは差分要約、QA優先順位、修正案作成だけを担当し、主OCR、最終承認、
  `corrected_text`保存、run承認、FTS同期、公開を行わない権限境界を定義した。
- 修正提案、難レイアウト、固有名詞・括弧・約物、形式不正、タイムアウトをSolへ昇格する
  条件と、ChatGPT認証限定・APIキー存在時fail closed・1冊5画面の初期上限を定義した。
- 固定20画面とholdout、反復安定性、100画面連続試験を通過するまでLunaの`no_change`で
  Solを省略せず、将来解禁後も10%無作為監査と重大見逃し1件での即時ロールバックを必須にした。
- PowerShell経由の日本語JSON二重文字化を実測上の注意点として記録し、明示的UTF-8復号と
  入力SHA往復確認を必須にした。

## 2026-08-01: feat — B-35 OCR品質基準を機械判定可能に定量化

- 固定正解コーパスの最低件数・文字数・ページ構成、通常散文の加重CER 0.5%以下と
  ページ最大CER 1.0%以下を独立した受入ゲートとして定義した。
- 最小編集経路の削除40文字以上かつ5%以上を列欠落疑いとして計数し、0画面を必須とした。
- 画像SHA-256で固定した通常散文標本の指定固有名詞について、完全一致出現回数再現率100%と
  欠落0件を必須とした。
- 品質ポリシーをJSONで版管理し、ベンチマークCLIが項目別の実測値・閾値・未達ページを
  出力して、全合格0・品質未達1を返す仕様を追加した。
- 機械OCR候補の品質とCodex補助後の公開正本を別評価とし、機械ゲート未達時は
  自動公開不可を維持する方針を明記した。
- 固定20画面への初回実行は、コーパス構成と固有名詞121/121出現が合格、通常散文CER
  1.82%、ページ最大CER 2.07%、ページ上限超過4件、列欠落疑い2件が不合格となり、
  CLI終了コード1と測定JSONを確認した。

## 2026-08-01: fix — グリムコネクト3冊と46冊公開本文を原画像再監査

- グリムコネクト1〜3巻を通常の縦書きリフロー小説としてrun 67・68・69で再公開し、
  3巻141画面は本文112、目次1、画像保持・索引対象外28画面へ確定した。
- ページ単位の`layout_type=image_only`を書籍全体の固定レイアウト判定に使わない方針を明記し、
  通常本文だった3巻93・106・129画面を原画像基準で`normal_prose`へ修正した。
- 3巻105画面の主系OCRに黒四角55文字と本文欠落、補助OCRに行末欠落があったため、
  原画像を正本として本文を復元し、Codex補正文として公開した。
- 4シリーズ46冊の公開後監査で原画像確認済み41画面を修復し、DB整合性`ok`、
  ページ件数・公開本文・種別・索引・採用元の不一致、UI混入、隣接完全重複を各0件とした。
- 残った機械flagは意図的な台詞・叫び、短い章末、人物紹介・前付等と確認し、
  文字単位の完全性は引き続き固定コーパスとCodex原画像QAで評価する方針を記録した。

## 2026-08-01: fix — OCR入力完全性と通知UI混入を公開前QAへ追加

- グリムコネクト1〜3巻の旧入力20・18・14画面を原画像で再監査し、固定レイアウトではなく
  通常の縦書きリフロー小説が本文途中で終了していたことを確認した。
- 修正後の再撮影は168・168・141画面で、連番、復号、寸法、重複・白紙候補、
  先頭・中間・末尾を原画像で確認した。
- 2巻の完全重複3組は章間の「次のページへ」、3巻の完全重複は0件であり、
  白紙候補も扉・著作権表記・疎な本文・奥付として妥当と画像確認した。
- OCR runの全画面承認だけでは書籍全体の撮影欠落を保証できないため、Kindle終端到達と
  入力画像QAをOCR投入前の独立ゲートとして明記した。
- ふつつかな悪女2巻47〜49画面のCodex / ChatGPT通知重なりを確認し、公開本文へ混入した
  47・48画面を修復対象とした。
- `ChatGPT`、または`Kindleカタログ`と`UI改善` / `要件整理`の組み合わせを
  `ui_overlay_text_detected`としてQA必須にし、一般語だけでは検出しない方針を追加した。

## 2026-07-31: fix — Kindle検索欄のIME依存を解消

- グリムコネクト1巻の再撮影開始時、検索欄への通常キー入力が日本語IMEで全角化され、
  `Ctrl+A`も成立せず、再試行ごとにASINが連結される不具合を実機確認した。
- 検索欄はUI Automationの`ValuePattern.SetValue`で半角ASIN全体を置換し、
  読み戻し完全一致後だけASIN付きカードを探索する方式へ変更する。
- ページ番号入力も通常キーではIMEにより`１`へ全角化するため、型付き入力欄の
  `ValuePattern.SetValue("1")`と完全一致読み戻しへ統一する。
- 直接ページ遷移は型付きUIA controlで入力欄と確認ボタンを取得し、確認ボタンを
  通常クリックして入力モーダルが閉じたことを
  確認してから撮影へ進む。モーダルを含む1画面だけを成功登録しない。
- 右上メニューボタンは本人照合済みcontrolを通常クリックする。別UIA rootの
  ポップアップ項目はボタン矩形からの相対位置で選び、直後に型付き
  `go-to-page-input`が出た場合だけ続行する。
- 対象を開いた時点で表紙なら直接遷移を省略し、`ページ1/N・0%`なら1回だけ表紙へ戻す。
  入力エラーでも生じる画像差分は終了条件にせず、入力control消失を必須とする。
- 共有前の最低件数ガードを追加し、通常小説50画面未満・漫画10画面未満を
  `capture_incomplete`として正式登録しない。
- 検索結果の非同期描画を考慮し、半角ASIN完全一致後の候補カードをreader timeout内で
  有界に再取得する。
- グリムコネクト1巻を実機確認し、通常の縦書きリフロー小説・左送り・381ページと
  確定した。右送りで1画面となった結果は不完全として破棄した。
- ページ送りごとにKindleを前面化してハンドル一致を検証し、フォーカス喪失による
  本文途中の誤終端を防ぐ。前面一致だけでは内部WebViewのフォーカスを保証できないため、
  Microsoft Store版は取得済み`ReadingArea`中心をキー送信前に再フォーカスする。
- キーで変化しなかった場合の有界再試行は、同じキーの再送ではなく、取得済み
  `ReadingArea`に対する次ページ側の相対矢印クリックを1回だけ使用する。
- 白地の巻タイトルから著作権表記への遷移が全画面平均差分`0.643`で同一扱いされる
  不具合を実機画像で特定し、平均差分と輝度差画素比率を併用する判定へ変更する。
- グリムコネクト1巻は168撮影画面でKindle表示381/381へ到達したため、Kindleページ数を
  `expected_screens`へ指定しない運用へ修正する。
- 位置入力など他のcontrolは従来の通常キー入力を維持し、限定control探索と
  本人照合前の安全停止条件も変更しない。

## 2026-07-31: fix — 46冊OCRの補正文採用元を整合

- 4小説シリーズ46冊・5,585画面のOCRと全画面QAを完了し、本文4,625画面、
  挿絵等801画面、目次59画面、奥付・広告等100画面へ分類した。
- 薬屋11〜16巻の308画面で、補正文が保存されても採用元が`primary / external`のまま
  公開される不整合を検出した。非空の`corrected_text`は
  `selected_engine=codex`を必須とするAPIガードと回帰テストを追加した。
- 公開中6冊はSQLite Online Backup後に採用元・公開本文・FTSを再同期した。
  修復後は補正文あり・採用元非codex 0件、公開本文不一致0件、
  SQLite整合性`ok`を確認した。
- ふつつかな悪女1・2巻の旧ASINなし画像・OCR・DBはSHA-256付きバックアップに保持し、
  新ASIN版を正本、旧版を復旧専用とする移行方針を明文化した。

## 2026-07-29: fix — 未採用OCR候補の文字量差を列欠落QAへ追加

- ふつつかな悪女1巻120画面の全画面QAで、主系が合格した通常本文8画面に
  縦書き本文1列の全欠落があり、不合格の補助候補には該当列が残る事例を確認した。
- 通常本文で未採用補助候補が主系より2%以上かつ30文字以上長く、
  主系が256文字以上ある場合を`unselected_external_candidate_more_complete`として
  QA必須にする。補助候補は自動採用せず、原画像照合用の比較材料として扱う。
- 同runの人物名`堯明`に複数のOCR表記揺れがあった知見を記録し、
  固有名詞台帳は自動置換ではなく検出とQA優先順位付けに使う方針を明記した。

## 2026-07-29: fix — Kindle小説の先頭ロケーション実機差を許容

- `グリムコネクト1―赤ずきんの章―` で、ロケーション`1`への直接遷移後に
  表紙が表示されてもフッターが`Location 4 of 4006・0%`となる実機差を確認した。
- 入力値`1`を読み戻せた直接遷移に限り、小説の`Location 1〜4・0%`を
  先頭近傍として許可し、`Location 5`以降や進捗`1%`以上は引き続き拒否する。
- 数十画面以上を戻るフォールバックは追加せず、先頭確認に失敗した場合は
  従来どおり`positioning_failed`で後続書籍の作成前に停止する。

## 2026-07-28: docs — MacローカルLLM移行を固定比較計画へ分離

- M1 Max 64GBを将来のLLM推論ホストとして使うため、現行Qwen3.6-35B-A3B、
  Qwen3.6-27B Dense、Gemma 4-31Bを同じ10巻入力で3回ずつ比較する計画を追加した。
- Macは本番DBを持たず、推論APIだけをSSH reverse tunnelでLinux本番へ提供し、
  採用前はWindows推論環境と公開中の要約・人物辞典を維持する境界を定めた。
- 事実性、人物正規名、理由のない人物削除、実行時間、メモリを採否条件とし、
  10巻、1〜3巻、1〜18巻の順に段階導入する。
- Mac環境構築までの間も、Kindle撮影、OCR、検索索引構築は継続し、
  品質改善中の要約・人物辞典再生成だけを独立して保留する。

## 2026-07-28: fix — 長編の事実抽出で人物マーカーが欠落する問題を修正

- 10巻のパイロット再生成で、本文75ページから書籍事実6266字を生成した時点で
  出力上限へ近づき、人物事実が0件となって品質ゲートで停止する事象を確認した。
- 書籍事実と人物事実の同時生成を廃止し、本文から書籍事実を抽出した後、
  そのページ根拠付き事実を人物別に再編する2段階処理へ変更した。
- 初回失敗では一括確定前に停止し、既存の要約446字・人物10名が維持されたことを
  本番スナップショットとDBで確認した。
- 2段階化後は書籍事実7918字、要約1526字、人物6名を生成して機械品質ゲートを通過した。
  ただしCodex補助QAで、事実表にない行動、同一人物の正規名揺れ、根拠のない人物削除を
  検出したため新版を不採用とし、監査スナップショットから要約446字・人物10名と
  サマリembeddingを復元した。
- 全冊再生成は、正規名・別名統合、完成文と事実表の意味的照合、人物集合の削除回帰検査を
  追加するまでB-36の改善待ちとした。

## 2026-07-28: feat — 生成済み要約・人物辞典の再生成監査を追加

- 書籍単位で公開中の要約・人物辞典をJSONへ退避し、再生成後の全文差分、
  追加・削除・変更人物、生成日時、文字数、類似度、機械品質ゲート結果を
  JSONとMarkdownへ出力する監査CLIを追加した。
- QA不合格時は書名の完全一致確認を必須として、退避版を単一SQLite
  トランザクションで復元し、サマリembeddingも更新する復旧経路を追加した。
- 再生成は「退避 → 再生成 → 差分保存 → Codex補助QA → 採否」の順で行い、
  機械品質ゲートだけでは公開承認としない運用を明文化した。

## 2026-07-28: change — 人物説明と書籍要約を文字数優先から可読性優先へ変更

- 人物説明の400字・1段落固定、書籍サマリの1500字目標、分割要約の400字目標を
  廃止し、必要情報、主語、因果、時系列、関係変化を自然な文章で伝える方針へ変更した。
- 登場量に応じて説明量を調整し、重要人物は複数段落、情報の少ない人物は
  根拠のない水増しをしない受入条件を追加した。
- combined応答でサマリマーカーが欠けた場合の文字数切り捨てを廃止し、
  識別可能な自然文は全文採用、識別不能なら保存せず再実行する方針とした。
- `num_predict`は文字数目標ではなく、LLM暴走防止とcontext保護の技術上限として維持する。
- 既存の要約・人物辞典は即時更新せず、バックログB-36として夜間再生成、旧版との差分保存、
  機械検査、Codex補助QAを行ってから公開版へ確定する運用を記録した。
- 品質改善の次段として、ページ根拠付き事実抽出、要約と人物の個別執筆、全巻を覆う
  人物ページ選択、編集校正、品質ゲート、全生成物の一括確定を採用した。

## 2026-07-28: refactor — 品質ガードレールの正本と完了記録を分離

- 完了済みの`設計書・ソースコード健全性維持計画`を`archive/`へ移し、
  現行ルールだけを`design/詳細設計/品質ガードレール.md`へ抽出した。
- `docs/index.md`の品質ゲート説明を実装へ合わせ、Rule 1〜6がすべて
  blockingである現状、ファイルマップ注釈検査、uv経由のMkDocsコマンドを反映した。
- 詳細設計、運用ガイド、サーバー連携コマンドの責務を明記し、品質基準、
  運用判断、コピー用コマンドの重複定義をリンクへ置き換えた。
- `mkdocs.yml`のnavを現在の文書寿命に合わせ、品質ガードレールを設計へ追加し、
  完了済み計画を計画一覧から除外した。

## 2026-07-28: docs — 本番デプロイの検証手順と時計差警告の知見を追加

- Linux本番反映後の確認を、公開入口・設計書サイト・実在API・systemd・
  再起動後のerrorログ・更新HTML本文の組み合わせに統一した。
- 未定義の`/api/health`は死活確認に使用せず、実在する
  `/api/pdfs?source=doujin`を確認用APIとして利用することを明記した。
- Windows/Linux間の小さな時計差によるtarのfuture timestamp警告は、
  終了コードと配信結果が正常なら転送失敗ではないこと、差が大きい場合の
  時刻同期確認を運用ガイドへ反映した。
## 2026-07-28: docs — Mac OCR補助確認の将来評価方針を追加

- Mac専用OCRアプリを本番主系の代替ではなく、第二OCR・目視確認・比較評価に
  利用する位置づけを定義した。
- OwlOCRを初期評価の第一候補とし、縦書き比較ではABBYY FineReader、
  画像補正比較ではPrizmoを段階的に評価する方針を記録した。
- 固定20〜30画面の評価コーパス、文字精度・列欠落・読み順・ルビ・反復・
  再現性・機密性の評価指標、正式公開フローを迂回しない条件を明記した。
- 著作物の画像・全文OCRはGit管理せず、クラウドOCRは明示許可がある評価だけで
  使用するデータ取扱い方針を追加した。
## 2026-07-28: docs — OCR公開監査とページ単位索引の運用知見を統合

- `ocr_runs`の履歴と現在公開中runを区別し、書籍ごとの最新承認run選択と
  `selected_engine`に従う採用本文解決をデータ設計へ反映した。
- ページ単位再構築は公開`pages`を変更せず、キャプチャ画面番号の索引だけを同期すること、
  事前不整合では値にかかわらず、更新開始後の失敗では`indexed_at=NULL`を検知して
  書籍単位再構築へ戻すことを明記した。
- 本番6画面で確認した他ページchunk不変、SQLite–LanceDB chunk ID一致、
  採用本文反復0画面という実測結果を運用ガイドとシリーズ技術知見へ反映した。

## 2026-07-28: fix — 公開OCR監査条件を統一しページ単位索引再構築を追加

- 反復修正対象のrun IDを巻番号として記載していた設計書を訂正し、run 5・8・9・17・18・7が
  それぞれ9・12・13・4・5・11巻に対応することを明記した。
- 公開本文監査を、書籍ごとの最新承認済みrunだけに限定する共通条件へ統一した。
  未承認runと同じ書籍の旧承認runは履歴として保持しつつ、公開監査から除外する。
- 画像照合で1ページだけ補正した際、他ページのchunk IDとembeddingを維持して対象ページだけを
  再構築する経路を追加した。失敗時は旧ベクトルを補償復元し、`indexed_at=NULL`を
  書籍単位再構築が必要な不完全状態として残す。運用CLIは
  `build_novel_db.py --book "<書籍名>" --page <画面番号>`とする。
- 本番の4・5・9・11・12・13巻の補正済み6ページをページ単位で再構築し、
  全件で他ページのchunk件数・ID合計、書籍全体のchunk総数、`indexed_at`の正常性を確認した。
  最新承認runだけの再監査は採用本文反復0ページ、未採用primary候補20ページだった。

## 2026-07-28: fix — OCR反復誤検出を修正し公開本文6ページを画像照合

- 反復検出を空白・記号だけの行、短い擬音、`selected_engine`に応じた採用本文の解決へ対応し、
  未採用候補を公開本文の異常として数える誤検出を修正した。
- 茉莉花官吏伝1〜18巻の初期検出11ページを全件画像照合し、実反復5ページを
  `selected_engine=codex`の補正文へ置換した。残る6ページは本文を変更せず、
  改訂規則で追加検出した7巻1ページを含む計6ページを修正した。
- 修正した4・5・9・11・12・13巻（run ID 17・18・5・7・8・9）の全文検索索引を再構築し、改訂規則で
  公開中の採用本文に未解決の反復が0ページであることを確認した。

## 2026-07-28: fix — OCR後付け・反復・人物名の公開前ガードを追加

- 本の後半に現れる明示的な試し読み標識、またはあとがき後の第2目次から
  別作品境界を検出し、境界以後を画像保持・索引対象外へ分類する。
- OCR候補内の同一行・隣接行ブロック反復を品質フラグ化し、機械合格済みでも
  採用本文はQA必須、未採用候補は監査対象にする。
- 人物名の敬称・肩書・匿名役職を共通正規化し、外国人名の中黒を保持する。
  full buildでは当該巻本文に根拠のない人物を保存しない。初期dry-runでは
  4境界、採用本文反復11ページ、候補反復14ページを検出し、人物169件を維持した。
  反復件数は後続の全件画像照合で再評価した。

## 2026-07-28: feat — 茉莉花官吏伝18巻のOCR・索引・シリーズ分析を整備

- 小説1〜7巻を現行OCRとCodex補助QAで再処理し、別作品試し読み・広告・あとがきを画像保持のまま索引対象外にした。8〜18巻は承認済み本文を維持し、後付け23画面だけを索引対象外へ修正した。
- 全18巻の索引を再構築し、巻別サマリ18件、巻別人物169件、人物関係68件をページ根拠付きで生成・QA後にPic2PDFViewerへ登録した。APIで18冊、人物詳細、シリーズグラフの取得を確認した。
- OCR揺れ・肩書付き人物・匿名役職・関係端点・過剰な根拠ページをCodex補助QAで整理し、成果物の総合信頼度を暫定`medium`とした。
- ネタバレ込みの時系列、人物名統合、所属・関係、巻別事件、伏線・テーマ、根拠ページを「茉莉花官吏伝 シリーズ横断分析」へ記録した。漫画12冊は画像資料として維持し、解析は後続タスクとした。


## 2026-07-27: fix — 設計照合で判明した安全境界・型・環境差分を解消

- novel_db の書籍名パスへ共通バリデーションを適用し、画像参照は service 境界で
  `KINDLE_NOVEL_IMAGES_DIR` 配下へ安全に解決するよう統一した。
- Kindle シリーズ取込の `--apply` では `--expected-total` を必須とし、
  冊数未確認のまま最初の capture job を作成できないようにした。
- meta、build、discussion の OpenAPI ネスト型を具象化し、フロントエンドの
  rebuild / build / discussion 型を生成型参照へ統一した。
- Windows NSSM のセットアップと設計を uv workspace のルート `.venv` へ変更した。
  停止・無効化中の旧サービス登録は管理者再登録時に移行する運用として記録した。

## 2026-07-27: docs — 設計書と現行実装の横断整合

- OCR の公開条件を、機械合格直後ではなく `awaiting_qa`、必須ページ承認、
  run 承認を経る現行フローへ統一した。
- Full Build と Context 生成の分離、Kindle 漫画の見開き撮影方式、
  novel の image-only 編集範囲を実装に合わせた。
- Linux 本番の nginx 8090 → uvicorn 8091、DB バックアップ付きデプロイ、
  MkDocs の事前ビルド、Windows NSSM 運用を現行化した。
- uv workspace、Surya / yomitoku / Ollama GPU の役割、Mac 開発手順、
  起動方法、移動済みスクリプトパスとソースリンクを修正した。
- 安全要件を緩めずに残す実装差分として、シリーズ期待冊数、novel_db の
  パス境界、OpenAPI 型、Windows service の旧 venv 参照を既知の問題へ記録した。

## 2026-07-27: fix(ocr) — 終端本文の誤除外とQA見逃しを防止

- 目次判定を前付け位置と章見出しの組み合わせへ限定し、長い本文中の単独語で
  目次へ誤分類しない仕様へ変更した。
- 終章・あとがき等の本文優先規則と、奥付・広告を確定する位置・語彙・文字量の
  複合条件を追加した。
- 長文の非本文候補と、2つのOCR候補で敬称付き人名・カタカナ語に同じ文字数の
  1文字違いがあるページをリスクフラグ付きQA必須対象にする方針を追加した。
- 実データ914ページでは単純な語集合不一致が233ページを必須化したため採用せず、
  1文字違いの候補対がある92ページへ絞る高精度条件を採用した。
- `is_indexed`を`indexed_at IS NOT NULL`と同義に揃え、OCR公開状態と
  チャンク・Embedding構築状態を区別する契約へ修正した。
- 茉莉花官吏伝8〜17巻のチャンク・Embeddingを再構築し、10冊すべての
  `indexed_at`更新と、8巻・17巻の書籍内検索を確認した。
- EmbeddingはWindows RTX 5070利用時に各巻約6〜12秒で完了した。
  接続先と`NOVEL_DB_EMBED_NUM_GPU=99`は処理中だけ設定し、完了後に本番既定値へ戻した。
- 本番Qwen llama-serverが未起動のため、要約・人物抽出を含む`full_build`は避け、
  正式本文・検索索引に必要な`rebuild`のみを成功条件として実行した。

## 2026-07-27: docs — シリーズ撮影分のOCR引継ぎ・文字起こしを完了

- `capture_state`と小説DBのOCR・索引状態を別々に確認し、撮影完了だけで
  文字起こし完了と判定しない運用を明記した。
- シリーズの一部だけを処理する場合は書籍名を固定して1冊ずつOCRへ投入し、
  対象なしの全件実行で以前からの未OCR書籍を巻き込まない方針を追加した。
- 2026-07-27撮影分は茉莉花官吏伝8〜17巻の10冊を対象とし、
  QA承認まで正式本文へ公開しない境界を記録した。
- 10冊914ページのOCRとQAを完了し、本文806ページを公開、表紙・目次・挿絵・
  奥付・広告など108ページを画像のみ保持した。機械品質判定に失敗した本文15ページは
  Codexが原画像と段組みを照合して補助OCR候補を採用した。
- 終端付近の本文3ページが目次・奥付等へ誤分類される事例を補正し、位置ベース分類後も
  文字量のある非本文候補を画像確認する運用を追加した。
- `is_indexed`はDBレコード存在を表し得るため、文字起こしは`ocr_done_at`、
  チャンク・Embedding構築は`indexed_at`で判定する状態境界を明記した。

## 2026-07-27: feat(kindle) — シリーズ撮影の安全な直列実行を追加

- シリーズ対象をdry-runで棚卸しし、1冊の成功と正式登録を確認してから次冊を作成する
  運用補助スクリプトの契約を追加した。
- 失敗、タイムアウト、対象冊数不一致、別の未完了job検出時は次冊を作成せず、
  Kindle画面へ接続しないAPI監視だけで連続運用できるようにした。
- 茉莉花官吏伝シリーズは小説8〜17巻、漫画1〜12巻の順で再開する。
- バックグラウンド起動時にKindleの前面化が拒否されても背面座標をクリックしないよう、
  foreground threadとのinput queue接続と前面handleの一致確認を追加した。
- Sambaへのpackage公開renameで一時的なアクセス拒否が発生した場合、コピー済み成果物を
  保ったままrenameだけを有界再試行し、不要な全ページ再撮影を減らした。
- 茉莉花官吏伝の未撮影22冊（小説8〜17巻、漫画1〜12巻）を直列処理し、既存8冊と
  合わせた全30冊が`captured`であることをカタログAPIで確認した。

## 2026-07-27: docs — Kindle完了項目をバックログ履歴へ整理

- 完了済みのA-4、B-27、B-33、B-34を現役のA/B候補から削除し、
  「実装済み機能の履歴」へ完了日・成果概要付きで移動した。
- A階層は対応決定済みの未完了項目がない状態を明記し、
  バックログ本文を未着手・検討中の機能だけ確認しやすい構成へ戻した。

## 2026-07-27: test(quality) — 構造・契約ガードレールを通常運用化

- production code行数をbaseline化し、新規400行超と既存超過ファイルの肥大化を
  pre-commit・CIで防止するラチェットを追加した。
- FastAPI OpenAPI契約の正規化比較と、backend・frontend・Kindleの依存方向検査を
  blocking gateへ追加した。
- living設計書の800行上限をblockingへ移行し、月次に設計書、規模、複雑度、
  未使用依存、完了計画残存を一括確認する監査コマンドを追加した。

## 2026-07-27: docs — Kindle自動撮影の完了実績をバックログへ同期

- B-34をPhase 0条件付き合格からPhase 5実装・実機受入完了へ更新し、
  直接先頭移動、原子的置換、小説10巻・漫画1巻の実機結果を記録した。
- B-27の漫画確認待ちを解消し、単ページ・見開き切替、左右端、余白、UI混入、
  Pic2PDFViewerへの置換まで確認済みであることを記録した。
