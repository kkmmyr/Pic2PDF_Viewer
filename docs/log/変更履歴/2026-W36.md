# 変更履歴 2026-W36

[変更履歴索引](../変更履歴.md)

## 2026-09-05: docs — 現在の実装に合わせて未完了計画を再整理

- バックログ全項目へ現在地、次の一手、完了・再開条件を補い、汎用メタデータ取込、外部バックアップ復元、Level 2 PWAなどの部分実装境界を明記した。
- B-35 / B-36 / B-37の実測・判定値の重複を詳細設計と凍結記録への参照へ縮約し、OCRとSolの詳細計画は新候補・受入判断待ちのblocked状態へ更新した。
- リファクタリング計画はMac評価CLIの実モデル回帰だけを未完了として残し、完了済みのWindows・Node・責務分離は履歴参照へ統合した。

## 2026-09-05: docs — 凍結記録の旧リンク置換先を索引化

- 凍結本文と当時のURLは変更せず、情報表示されるリンク切れ30件・旧参照16種類から現行正本または保存先へ辿る対応表をアーカイブ索引へ追加した。

## 2026-09-05: docs — Kindle module設計書の実装同期と管理を強化

- `run_comic.bat` / `run_novel.bat`はページ設定を自動変更せず、capture jobのagent経路だけがcomic=2ページ、novel=1ページを設定・検証する現行実装へ処理フローを同期した。
- CI指定Node.js 22.23.2でのfrontend再確認をリファクタリング計画の完了扱いとし、環境待ちをMac MLX実推論だけへ限定した。
- `kindle-pdf/docs/`の基本・詳細設計をliving文書として明示し、状態ヘッダ、行数、相対Markdownリンクを`check_docs.py`のblocking検査へ追加した。

## 2026-09-05: fix — フロントエンドの狭幅Dialogとアクセシビリティを修正

- CIと同じNode.js 22.23.2 / npm 10.9.8でfrontendの依存導入、監査、lint、未使用依存検査、型・全1,112単体test、production buildを再確認した。
- 共通`Dialog`が390px幅で左右16pxを確保するよう幅計算を修正し、click操作を持つ6か所へnative要素またはkeyboard・ARIA契約を追加する。
- 解消済みC901 4件をbaselineから削除し、縮小済みcode-size 6件の上限を現在値へ下げた。390px / 1280pxの実ブラウザで配置、Tab / Escape、console errorなしを確認した。

## 2026-09-05: refactor — ページ全文索引の状態・構築・検索責務を分離

- page-level LanceDB ICU索引を`page_fts_state`、`page_fts_builder`、`page_fts_query`へ分離し、`page_fts`は既存importを維持する公開窓口にした。
- source revision、active pointer、manifest、失敗table除去、prefilter、canonical SQLite再照合、snippetの既存契約を回帰テストで維持した。
- API、DB schema、CLI manifest、FTS5 fallback条件を変更せず、解消した`page_fts`のcode-size・C901 baselineを削除した。

## 2026-09-05: refactor — Windows全体検査と文脈生成責務を整理

- Windowsで再現したbackend全体検査9件を、利用できないBash・symlinkの明示skip、停止済みprocess判定、directory fsync、UTF-8読込の移植性対応で正常化した。
- チャンク文脈生成の対象選定・バッチ処理・LanceDB更新・SQLite確定を`context_generation`へ分離し、`full_builder.build_book_contexts`の既存importとジョブ契約を維持した。
- API、DB schema、ジョブmode、保存順序を変更せず、失敗した生成・embeddingバッチを次回再試行できる挙動を回帰テストで固定した。
- 分離で解消した`full_builder`のcode-size・C901 baselineを削除し、品質基準を現行構造へ追従させた。

## 2026-09-05: refactor — 評価ツール・小説一括操作・読書会生成の責務整理

- 評価CLIのOS依存と共通計算、OCR QAの型付き入力、小説一覧の一括操作、読書会生成のservice境界を整理。
- 既存挙動を先に回帰テストで固定し、API・保存形式・SSE・QA承認条件を維持。設計書・ファイルマップも同期した。
- 対象回帰、frontend全1,110件、desktop/mobile操作とAPI定義一致を確認。backend全体は1,625件成功、既存Windows依存の9件失敗、2件skip、coverage 72.49%。
- 5件の実施結果を[検証記録](../../archive/検証/実装責務整理_2026-09-05.md)へ移し、Windows全体検査の環境課題を計画書へ分離した。

## 2026-09-05: docs — 設計書配備とOCR入力契約の本番確認

- PR #16の検査・取り込み後、設計書HTMLとLinux backendを配備。backup / 復元検証、ソース・主要HTML照合、公開入口を確認した。
- Windows agentの新規run 192で57ページの実行環境情報保存とQA待ちへの移行を確認し、既知の問題から本番反映待ちを解消した。
- 公開本文・検索用本文・既存OCR/QAは全行一致。候補は全57ページ未承認（機械判定failed 3ページを含む）として保持した。詳細は[本番確認記録](../../archive/検証/設計書配備・OCR入力契約_本番確認_2026-09-05.md)を参照する。

## 2026-09-05: docs — 設計書の入口・責務・計画と履歴の再整理

- README・起動方法・設計書トップの役割を整理し、更新ルールとHTMLビルド手順を正本へ集約。
- 未完了計画と実測・完了記録を分離し、Kindleの購入カタログと自動撮影ジョブ契約を分割。
- 共通設計から機能別正本への参照を揃え、目的別ナビゲーションとアーカイブ索引を整備。
- 既存の文書URLを維持して必要な本文を移管し、AGENTS・開発ガイド・モジュール設計の参照も更新。
- 検証: 設計書11ルール、ファイルマップ一致、check_docsテスト3件、MkDocs strict buildが合格。

## 2026-09-05: test — Granite 4.2 30BをM1 Maxで実機評価

- Ollama公式Q4_K_Mを取得し、M1 Max 64GBで22GB・GPU 100%・32K contextのロードを確認した。
- 固定小説ケース3 seedは、公式の思考なし0/3、低思考2/3、プロジェクト低温設定0/3で、途中発言と最終合意の時系列誤認が残った。
- 標準ベンチはキャッシュ後5.8〜9.3 token/秒、15,617 tokenの長文初回は約360秒だった。
- 現行Qwen3.6の置換とアプリ設定変更は見送り、比較用モデルとしてのみ保持する。
- ADR-0019と小説RAGパイプライン設計へ、32K明示・実ロード確認・3 seed早期ゲート・利用禁止範囲を反映した。

## 2026-09-05: docs — 未照合章の同期と稼働環境の確認

- 要件・API・バックエンド責務・セキュリティ・運用手順の残存差分を修正し、全63設計文書の確認範囲を記録した。
- RAG/Kindle/Mac OCR/環境構築の実測をarchiveへ分離し、他プロジェクトの設計と旧ライセンス表は正本参照へ整理した。
- Linuxのアプリ・バックアップ・監視・Samba、Windows旧サービス、本番の主要画面とモバイル操作を読み取り確認した。MCPのTailscale直結とSPA応答を区別した。
- [照合・稼働確認記録](../../archive/検証/設計書最新化_照合・稼働確認_2026-09-05.md)へ実施範囲・テスト結果・本番未反映・未実測項目を明記した。

## 2026-09-05: fix — OCRの新規ページ保存とQA移行で実行環境情報を必須化

- agent APIとworker共通保存でruntime manifestを必須とし、schema・engine・model revision・run内不変性を検証する。
- QA移行時に欠損を拒否し、manifestなしの保存ページがある旧runは再利用せず、新runで処理する。
- Qwen＋dotsの両推論環境と調停workerの監査情報を保存する。旧runの閲覧・既存QA承認・公開rollbackは互換性を維持する。
- 本番DB・公開本文・稼働サービスの設定変更は含まない。

## 2026-09-05: chore — 設計書更新PRの依存監査失敗を解消

- Browserslistを4.28.6から4.28.9へ更新し、必要なブラウザ互換性データの依存5件を合わせて更新した。
- アプリの直接依存・画面仕様は変更せず、lockfileの対象6件に変更を限定した。
- 検証: npm 10.9.8での依存監査は脆弱性0件。型・テスト・ビルドはPRのCIで確認する。

## 2026-09-05: docs — 現行実装に合わせた設計説明の整理と検証履歴の分離

- 撤去済みSearXNG自動補完の対処手順・推奨件数を除き、旧ファイル名とPDF操作の説明を修正した。
- 要件の対象範囲、Linux配信・agent認証の信頼境界、パス検証条件、関係グラフAPI、検索・QAの画面配置、
  Kindleの5ページ構成、Context・SSE・OCR agentの責務を静的な実装照合に基づき更新した。
- OCRのrun 184・Windows run 189/190・候補screeningと、RAGのモデル比較・索引復旧の実測を
  archiveへ分離し、元の数値・失敗例・保留・採否・公開前後の区別を保存した。現行仕様には利用境界を残した。
- Windows OCR agentのruntime manifest省略受理を既知の問題として記録した。必須化・互換性の採否は未決で、
  実装、API、QA承認条件、公開データは変更していない。
- 先行変更で整理済みのW34を保持し、MCP連携・候補解析改善を含むW35の11件を週別へ分離した。索引・MkDocs nav・相対リンクを更新した。
- 設計書の確認日は今回照合した節の更新を示す。本番DB・実ブラウザ・OCR実行の再検証は含まない。
- 検証: 文書構造11項目、ファイルマップ鮮度、差分の空白検査、MkDocs strict buildに合格。
  実測本文の原文保持、既存週別記録と新規移管11件の保持を照合し、新規リンク先のHTMLアンカーも確認した。
