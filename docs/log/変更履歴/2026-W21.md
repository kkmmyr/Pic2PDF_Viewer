# 変更履歴 — 2026-W21 (2026-05-18 〜 2026-05-24)

[← 索引に戻る](../変更履歴.md)

---
## 2026-05-23: refactor — Phase 85/86/87 — character_summarizer 分割・job_worker クエリ分離・useNovelDetailData 抽出

### Phase 85: `character_summarizer.py` → `character_db.py` + 縮小 `character_summarizer.py`
`services/novel_db/character_summarizer.py`（302 行）を DB 層と LLM 層に分離。
- `character_db.py` 新規作成（`CharacterStat` / `CharacterRow` / `_parse_main_characters` / DB CRUD 6 関数）
- `character_summarizer.py` は `summarize_character` + プロンプト定数のみに縮小（90 行）
- `routers/novel_db/character.py` / `scripts/build_character_summaries.py` / tests 2 ファイルの import 先を更新

### Phase 86: `job_worker.py` のクエリ関数を `_book_queries.py` に分離
`NovelDbJobWorker` クラスと書籍一覧クエリが混在していた `job_worker.py`（293 行）を分割。
- `_book_queries.py` 新規作成（`_list_books_needing_ocr` 等 7 関数）
- `job_worker.py` は `NovelDbJobWorker` のみに集約（220 行）

### Phase 87: `NovelDetailPage.tsx` の data fetch を `useNovelDetailData` hook に抽出
読書会履歴・類似書籍の fetch ロジックをページコンポーネントから分離。
- `hooks/novel_db/useNovelDetailData.ts` 新規作成（state 4 個 + useEffect 2 個）
- `NovelDetailPage.tsx` を 353 → 310 行に縮小

### fix: useGenerateJob / usePdfStatus のテストモック対象を修正
フックが `generate_api_client` を使用しているにも関わらず `api_client` をモックしていたため 8 件が偽失敗。モック対象を修正し全通過。

**テスト**: pytest 908 件 / vitest 966 件 全通過

---

## 2026-05-22: refactor — Phase 83/84 — llm.py 分割・useNovelManage 3連パターン統合

### Phase 83: `llm.py` → `prompt_builder.py` + 縮小 `llm.py`
`services/novel_db/llm.py`（297 行）をプロンプト組み立てと LLM 呼び出しに分離。
- `prompt_builder.py` 新規作成（`PROMPT_TEMPLATE` / `CHAT_SYSTEM_TEMPLATE` / `build_prompt` / `build_chat_context_block` / `build_chat_system_message` 等）
- `llm.py` を LLM 呼び出し 4 関数 + `LLM_OPTIONS` のみに縮小（89 行）
- `routers/novel_db/qa.py` / `chat.py` / テスト 2 ファイルの import 先を更新

### Phase 84: `useNovelManage.ts` の 3 連パターンを `useBuildTarget` hook に統合
Full Build / コンテキスト生成 / 関係グラフ生成で重複していた state 管理を共通化。
- `hooks/useBuildTarget.ts` 新規作成（`mode / books / enqueue` → `all / selected / showBuilt / filtered / handleEnqueue`）
- `useNovelManage.ts` を 276 行 → 190 行に削減。公開インターフェース（`UseNovelManage`）は変更なし
- `buildUnifiedRows` を private 関数として切り出し

### 変更ファイル
- `backend/services/novel_db/prompt_builder.py` — 新規
- `backend/services/novel_db/llm.py` — 縮小
- `backend/routers/novel_db/qa.py` / `chat.py` — import 更新
- `backend/tests/test_novel_db_llm_prompt.py` / `test_novel_db_llm_backend.py` — import 更新
- `frontend/src/hooks/useBuildTarget.ts` — 新規
- `frontend/src/hooks/useNovelManage.ts` — リファクタ
- pytest 908 件 / vitest useNovelManage 20 件全通過

---

## 2026-05-22: docs — 設計書最新化（useLibraryPins/useLibrarySettings DB移行・linux_sync.py 追加）

### 概要
2026-05-19 の localStorage→DB 移行と 2026-05-18 の linux_sync.py 追加が設計書に未反映だったため修正。

### 変更ファイル
- `docs/03_詳細設計/詳細設計書_フロントエンド編.md` — `useLibraryPins`（localStorage→API 永続化）・`useLibrarySettings`（source 引数追加・readStateFilter/genreFilter が API 経由）の記述を更新
- `docs/03_詳細設計/詳細設計書_バックエンド編.md` — `services/linux_sync.py` をディレクトリツリー・ファイルテーブルに追加

---

## 2026-05-22: fix — Generate ジョブ後の meta.db 自動送信を廃止（★初期化バグ修正）

### 概要
`linux_sync.sync_after_generate` が Generate ジョブ完了のたびに Windows の `meta.db` を
Linux へ上書き送信していた。これにより Linux 側でつけた★（シリーズ/作者ピン）が次回
Generate ジョブ時に消えるバグが発生していた。

`sync_to_linux.sh` ではすでに「meta.db はサーバー側が正」方針に従って DB 同期を削除
済みだったが、`linux_sync.py` の自動同期だけ残存していたため、方針を統一した。

### 変更ファイル
- `backend/services/linux_sync.py` — `sync_after_generate` の `meta_db_path` 引数と送信処理を削除
- `backend/routers/generate.py` — 呼び出し側の引数削除・`META_DB_DIR` import 削除
- `backend/tests/test_linux_sync.py` — 期待 SSH 呼び出し回数を 3→2、5→4 に更新

## 2026-05-19: fix — sync_to_linux.sh から DB 同期機能を削除

### 概要
`sync_to_linux.sh` の `sync_db()` 関数・`db` ターゲット・`DB_DATA` 変数を完全削除。
Linux サーバーの `meta.db` を正として、ローカルからの誤上書きリスクをなくす。
（`sync_db()` が `$TARGET` に関わらず無条件実行されていたため、リリース時に Windows の空 DB でサーバーデータを上書きする事故が発生したことへの対処）

### 変更内容
- **`sync_to_linux.sh`**: `sync_db()` 関数・`db` ターゲット・`DB_DATA` 変数を削除。対象引数は `doujin | comic | novel | hitomi | all` のみ
- **`docs/04_環境構築/サーバー連携よく利用するコマンド.md`**: `db` ターゲット使用例・注意書きを削除。メタ設定はサーバー上で直接行う旨に更新

### 運用変更
- ジャンル・シリーズ・作者の設定は **サーバー（`http://100.107.238.88:8090`）上で直接行う**
- Windows 側の `meta.db` 編集 → サーバーへの反映という運用は廃止

---

## 2026-05-19: feat — localStorage キャッシュの DB 移行（フィルター / グループピン）

### 概要
`readStateFilter` / `genreFilter`（ライブラリフィルター）と `pins_series_*` / `pins_author_*`（グループカード代表ピン）を localStorage から `meta.db` に移行。デバイス間共有・永続化の信頼性向上が目的。

### 変更内容
- **DB スキーマ**（`meta_db.py`）: `ui_filters(source, read_state_filter, genre_filter)` / `group_pins(source, pin_type, group_id, book_name)` テーブルを `_CREATE_DDL` に追加。起動時 `init_db()` で自動作成
- **新規サービス**（`services/prefs_store.py`）: `get_prefs` / `update_filters` / `set_pin` / `delete_pin` の 4 関数
- **新規ルーター**（`routers/prefs.py`）: `GET /api/prefs` / `PATCH /api/prefs/filters` / `PUT /api/prefs/pins` / `DELETE /api/prefs/pins`（§12）
- **フロント `useLibrarySettings`**: `source` パラメータを追加。`readStateFilter` / `genreFilter` を React Query + API に差し替え。localStorage から DB への一回限り移行処理を内包
- **フロント `useLibraryPins`**: localStorage → React Query + API に差し替え。一回限り移行処理を内包
- **`LibraryPanel.tsx`**: `useLibrarySettings(currentSource)` に変更

### 変更ファイル
- `backend/services/meta_db.py` — DDL 追加
- `backend/services/prefs_store.py` — 新規
- `backend/routers/prefs.py` — 新規
- `backend/main.py` — prefs ルーター登録
- `frontend/src/config/api.ts` — PREFS エンドポイント追加
- `frontend/src/hooks/useLibrarySettings.ts` — API 連携に変更
- `frontend/src/hooks/useLibraryPins.ts` — API 連携に変更
- `frontend/src/components/viewer/LibraryPanel.tsx` — source 引数追加
- `frontend/src/test/useLibrarySettings.test.ts` — テスト更新
- `frontend/src/test/useLibraryPins.test.ts` — テスト更新

---

## 2026-05-19: fix — リーダーナビゲーション 3 件修正

### 概要
- **最終ページ（関連書籍確認）の黒枠常時表示**: `isOnRelatedPage` 時にヘッダー・スライダーを auto-hide させず常時表示
- **最終ページのスワイプ戻り（LTR/RTL 対応）**: `direction` に応じた「戻る方向」スワイプのみ前ページへ戻る（LTR: 右スワイプ、RTL: 左スワイプ）。逆方向は no-op
- **本文スワイプの LTR/RTL 対応**: 従来 LTR 固定だったスワイプを `direction` で分岐（LTR: 左→次・右→前、RTL: 右→次・左→前）
- **戻るボタンがシリーズ/作者一覧に戻る**: `selectPdf` が全 URL パラメータを上書きしていた問題を修正。関数形式 `setSearchParams(prev => ...)` に変更し `author`/`series` を保持。書籍を閉じると元のドリルダウン一覧に戻れる

### 変更ファイル
- `frontend/src/components/viewer/ReaderPanel.tsx` — 上記 3 項目を実装
- `frontend/src/hooks/useUrlState.ts` — `selectPdf` を関数形式に変更
- `frontend/src/test/useUrlState.test.tsx` — `selectPdf` で `author`/`series` が保持されることを検証するテスト追加

---

## 2026-05-18: chore — grill-me スキル改修（目的・体験中心の質問構成に変更）

### 概要
- 質問カテゴリをユーザーの目的・利用シーン・完了条件に集中するよう再構成
- データモデル/スキーマ影響・影響範囲（router/service/component）・UI/API 変更有無など実装寄りのカテゴリを削除
- Kindle・meta.json・特定ファイルパスなどプロジェクト固有記述を全削除
- 出力先を `docs/01_要件定義/<機能名>_要件.md` に固定

---

## 2026-05-18: feat — リーダーUI表示ロジック改善（トグル化・EdgeHoverZones 削除・ドラッグ中両タイマー停止）

### 概要
- 中央ゾーンタップ：ヘッダー+スライダーの **表示/非表示トグル**（両方表示中なら非表示、それ以外なら表示+3s auto-hide）
- 上下エッジホバー/タッチによる表示ロジック（`EdgeHoverZones`）を削除
- スライダードラッグ中はヘッダー・スライダー**両方**のタイマーを停止（ドラッグ完了で両方再開）

### 変更ファイル
- `frontend/src/hooks/useReaderUIState.ts` — `showBothOnTouch`→`toggleBothUI`（トグル）、`showHeaderOn/Off/OnTouch`/`showSliderOn/OnTouch` 削除、`pauseSliderTimer`/`resumeSliderTimer` 両タイマー対応
- `frontend/src/hooks/useReaderState.ts` — 削除関数の pass-through を撤去、`toggleBothUI` を公開
- `frontend/src/components/viewer/ReaderPanel.tsx` — `EdgeHoverZones` 削除、`toggleBothUI(showHeader, showSlider)` 呼び出しに変更

---

## 2026-05-18: feat — リーダークリック3ゾーン方式への変更

### 概要
コンテンツエリアのタップ/クリック動作を左1/3・中央1/3・右1/3の3ゾーンに統一。

### 変更内容
- **左 1/3**: 戻る（RTL モードでは進む）
- **中央 1/3**: ヘッダー＋スライダーを同時表示（3秒後に自動非表示）
- **右 1/3**: 進む（RTL モードでは戻る）
- 単ページ・見開き両モードで同一ルールを適用
- `PageRenderer` からナビゲーション用 `onClick`/`onNext`/`onPrev` を削除し、`ReaderPanel` の `handleContentClick` で一元管理

### 変更ファイル
- `frontend/src/hooks/useReaderUIState.ts` — `showBothOnTouch` 追加
- `frontend/src/hooks/useReaderState.ts` — `showBothOnTouch` を公開
- `frontend/src/components/reader/PageRenderer.tsx` — `onNext`/`onPrev`/`side`/`direction` props 削除、onClick 削除
- `frontend/src/components/viewer/ReaderPageView.tsx` — `onNext`/`onPrev` props 削除
- `frontend/src/components/viewer/ReaderPanel.tsx` — `handleContentClick`（3ゾーン判定）追加

---

## 2026-05-18: docs — 設计書（バックエンド編/フロントエンド編/API仕様書）の実装差分を修正

## 2026-05-18: fix — iPad iOS safe area 完全対応（エッジゾーン・ヘッダー・スライダー・ドラッグ中 auto-hide 停止）

- **EdgeHoverZones**: `top:0 / bottom:0` から開始し `safe-area-inset + 4rem / 3.5rem` の高さに変更。画面端タッチで確実に反応するようになった。
- **ReaderHeader**: `paddingTop: env(safe-area-inset-top)` を追加しステータスバーと重なりを解消。コンテンツ高さを `h-16`（64px）に拡大。
- **PageSlider**: `paddingBottom: env(safe-area-inset-bottom)` を追加しホームインジケーターと重なりを解消。コンテンツ高さを `h-14`（56px）に拡大。
- **スライダードラッグ中 auto-hide 停止**: `useReaderUIState` に `pauseSliderTimer` / `resumeSliderTimer` を追加。`PageSlider` の `onDragStart` / `onDragEnd` 経由でドラッグ中はタイマーを停止し、離したタイミングで再開する。
- **useTouchSwipe**: `onTap` / `showBothOnTouch` を撤去。コンテンツタップはページ送りのみに限定。
- **test**: `useBookMeta.test.ts` の `makeKey` テストで `waitFor(mockedGet called)` → `waitFor(state反映)` に修正（断続的失敗の解消）。

## 2026-05-18: feat — iPad タップで UI 表示 + エッジゾーン iOS セーフエリア対応

- `useTouchSwipe`: `onTap` コールバック追加。`|dx| < 10px && |dy| < 10px` をタップと判定。
- `useReaderUIState`: `showBothOnTouch` 追加。ヘッダー・スライダーを同時に表示し 3 秒後に両方自動非表示。
- `useReaderState` / `ReaderPanel`: `showBothOnTouch` を `useTouchSwipe` の `onTap` に接続。画面中央タップ → UI 同時表示の操作フローが成立。
- `EdgeHoverZones`: 上ゾーンを `env(safe-area-inset-top)` 下端から、下ゾーンを `env(safe-area-inset-bottom)` 上端から配置。iOS ホームインジケーター・ステータスバーエリアのシステムジェスチャーと競合しなくなった。

## 2026-05-18: feat — Level 1 PWA 対応（ホーム画面追加・スタンドアロン起動）

- `frontend/public/manifest.json` 追加（`display: standalone`、テーマカラー `#1e2236`）。
- `icon-192.png` / `icon-512.png` / `apple-touch-icon.png` をダークネイビー＋本アイコンで生成。
- `index.html`: `<link rel="manifest">`・`apple-touch-icon`・`theme-color`・`apple-mobile-web-app-*` メタタグ・`viewport-fit=cover` を追加。
- iPad Safari で「ホーム画面に追加」→ アドレスバーなしのスタンドアロンモードで起動可能。
- Service Worker なし（HTTPS 不要、LAN 内 HTTP 環境でも動作）。

## 2026-05-18: docs — サーバー連携よく利用するコマンド.md 追加

- `docs/04_環境構築/サーバー連携よく利用するコマンド.md` 新規作成。
- sync / deploy / SSH / サービス管理 / 動作確認コマンドを用途別に整理。
- よくある作業フロー（新刊追加・メタ編集後・コード修正後）を記載。

## 2026-05-18: chore — sync_to_linux.sh db ターゲット追記・空フォルダ再送バグ修正

- `sync_to_linux.sh` のコメントに `db` ターゲットを追記。`bash sync_to_linux.sh db` で meta.db + novel_db のみを高速同期可能。
- `sync_new_books()`: サーバー側の既存判定を「ディレクトリが存在する」→「ファイルが 1 件以上ある」に変更。転送失敗で空ディレクトリが残った場合でも次回に再送される。
- `grep -qx` を `grep -Fqx` に変更（正規表現メタ文字を含む書籍名で誤マッチしないよう固定文字列比較に統一）。
- 背景: `ここころみだらにそまりて（コッコロ嚮導幼君NTRafter` が空フォルダとして残りスキップされ続けた問題を修正。

## 2026-05-18: chore — Linux デプロイスクリプト追加・移行計画を稼働中に更新

- `deploy_to_linux.sh` 新規作成。`bash deploy_to_linux.sh` 1コマンドでフロントビルド → tar+ssh 転送 → `systemctl restart` を実行。
- `find -prune` でディレクトリを除外（Git Bash の `tar --exclude` が効かない問題を回避）。`.venv`/`data`/`complete`/`__pycache__` 等を除外、転送サイズは約 350KB。
- `sudoers` に `NOPASSWD: /usr/bin/systemctl restart pic2pdf-viewer` を設定。
- `docs/04_環境構築/Linuxサーバー移行計画.md` のステータスを「計画中」→「稼働中（2026-05-18）」に更新、現在の運用状態テーブルと D-1/D-2 手順を整備。

## 2026-05-18: chore — sync_to_linux.sh に images/ 差分同期・hitomi ターゲットを追加

- `sync_new_books()` 関数を追加: SSH で既存ディレクトリ一覧を取得し、未転送の書籍ディレクトリのみ tar+ssh で転送。
- doujin/comic/kindle_novel 各カテゴリの `images/` を差分同期対象に追加。
- `hitomi` を単独ターゲットとして追加（`bash sync_to_linux.sh hitomi` で hitomi データのみ同期）。

## 2026-05-18: feat — iPad/タッチデバイス向けビューアー操作対応

- `hooks/useTouchSwipe.ts` 新規作成: 左右スワイプでページ送り（閾値 50px、縦スワイプは無視）。
- `hooks/useReaderUIState.ts`: `showHeaderOnTouch` / `showSliderOnTouch` 追加（表示後 3 秒で自動非表示）。
- `components/reader/EdgeHoverZones.tsx`: `onTouchStart` 追加（上端タッチ=ヘッダー表示、下端タッチ=スライダー表示）。
- `hooks/useReaderState.ts`: 新 touch ハンドラを return に追加。
- `components/viewer/ReaderPanel.tsx`: swipe フックと touch ハンドラを接続。
- 既存のマウス・キーボード操作は変更なし。

## 2026-05-18: chore — nginx リバースプロキシ導入（Linux サーバー静的ファイル高速化）

- `deploy/nginx-pic2pdf-viewer.conf` 新規作成。
- nginx が静的ファイル（images/thumbnails/assets）を直接ディスク配信、uvicorn は API 専任に。
- uvicorn を `0.0.0.0:8090` → `127.0.0.1:8091` に変更（nginx が外部 8090 を受け持つ）。
- ブラウザキャッシュ: 画像・アセットは 30 日 immutable、HTML は no-cache。
- `deploy/pic2pdf-viewer.service` のポートを 8091 に更新。
- `docs/04_環境構築/Linuxサーバー移行計画.md` § F-8 を Caddy → nginx に更新。

## 2026-05-18: fix — comic/novel ライブラリ一覧を images/ 走査に統一

- `routers/library.py:_list_from_images()` の画像フィルタを `.webp` 限定から `is_image_file()`（WebP/PNG/JPG/JPEG 対応）に変更。
- `list_pdfs()` で doujin 専用だった `_list_from_images()` を comic / novel にも適用。従来の `pdfs/` スキャンコードを削除。
- 背景: comic/novel の実コンテンツは Kindle キャプチャ PNG（`images/{book}/*.png`）に格納されており、PDF は存在しない。PDF スキャンのままでは Windows・Linux ともに空一覧になっていた。

---
