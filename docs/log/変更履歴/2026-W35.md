# 変更履歴 2026-W35

[変更履歴索引](../変更履歴.md)

## 2026-08-29: feat — Mac・Windows CodexのMCP連携基盤を追加

- medaroserver上の専用SQLiteを正本とする非同期メッセージ・topic・確認・返信契約を追加した
- `send_message`等7ツールをStreamable HTTP MCPで公開し、OCR本番DBと公開操作から分離した
- 冪等再送、参加者照合、closed topic拒否、比較文脈SHA検証を回帰テスト対象として固定した
- medaroserverではTailscale IPv4に限定したsystemd user unitで常駐し、通常デプロイ後に新世代へ自動追従する

## 2026-08-29: docs — Windows OCR監査run 190の実測を設計へ反映

- 138画面のWindows CUDA実行でruntime・model SHA、原候補、工程時間を全件保存し、51分11秒で`awaiting_qa`へ完走した
- `passed` 117件・`failed` 21件、全画面`required`とし、同一入力run 185から改善3件・逆方向0件を確認した
- 先行run 189の候補SHA不一致は19画面時点でfail closedとなり、修正後も公開版・canonical本文・検索索引を変更していない

## 2026-08-29: fix — 空のOCR主候補を外部採用本文で上書きしない

- Surya主候補が空でYomiToku外部候補を採用したページでも、空の主候補を実測値として保持するよう修正した
- 原候補本文とcandidate manifestのSHA不一致によるWindows OCR checkpoint拒否を回帰テストで固定した
- 監査不一致は引き続きfail closedとし、既存公開版へ自動反映しない

## 2026-08-29: fix — Kindle価格監視のDB行型をfail closedで検証

- SQLite行のID・閾値を型検証してからAPI値へ変換し、欠損した`lastrowid`を明示的に失敗させるようにした
- 本番系列をmasterへ統合した際のbasedpyright 11件を解消し、既存CRUD・通知判定の挙動を維持した

## 2026-08-29: fix — OCR監査migrationをrollback可能なデプロイへ追加

- additiveなAlembic `0016`を明示的な後方互換migrationとして直前backend世代へ先行配置し、未知migrationのfail-closed拒否は維持する。
- migration後にOCR run manifest、候補manifest、工程時間の必須列が存在することを世代切替前に検査する。
- 現行DB headとMac/Windows OCR技術知見への設計リンクをrevision `0016`・§29へ同期する。

## 2026-08-29: feat — YomiTokuのWindows CUDA / Mac MPS実行を分離

- `OCR_YOMITOKU_DEVICE`を追加し、`auto`でCUDA → MPS → CPUを選択する実行契約を追加した
- `kindle-pdf`のYomiToku 0.11.0 / PyTorch 2.6.0取得先をOS markerで分け、Windows/LinuxはCUDA 12.4、MacはApple Silicon MPSへ切り替えるようにした
- YomiTokuのMPS対応版と外部OCRラッパーのデバイス指定を要求し、Macでの意図しないCPU実行を検出できるようにした
- デバイス検証と新旧ラッパーAPI変換を専用moduleへ分離し、OCR workerのサイズ上限を維持した

## 2026-08-29: feat — OCR版情報・原候補・工程別時間を監査保存

- 実OCR workerのOS・Python・package・device・source/model SHA・Git dirty状態をrun manifestへ固定し、新規runの空版・`unversioned`とrun途中の環境混在をfail closedで拒否する。
- primary/external双方の原本文・raw出力・自己SHA付きcandidate manifestを採否とは独立に保存し、後から非採用候補を監査できるようにした。
- worker初期化、画像読込、両OCR、候補選択、ページ総時間、QA開始・完了、ページ確認・Codex補正時間を分離保存し、QA APIと画面から確認できるようにした。

## 2026-08-29: fix — OCR公開前QAを全ページ画像照合へ変更

- Windows Surya + yomitoku runとMac原画像照合済みrunを同一画像SHAで比較し、
  Windowsで`cross_engine_consensus / not_required`だった20ページ中16ページに補正が必要だったため、
  B-35正式合格前の選別QAを廃止して全ページを`required`にする。
- selected本文が30文字未満でも独立候補が300文字以上ある矛盾を
  `candidate_content_conflict`として記録し、非本文分類との衝突も画像QA理由へ残す。
- OCR QA画面は品質flagを日本語化し、Surya / yomitoku候補の文字数・本文を並列表示する。
  ページ判定を`OK / 修正 / 保留`として明示し、全ページ解消まで公開を許可しない。

## 2026-08-26: fix — dots OCRの表HTMLを可視本文に正規化

- dots.mocrの`Table`候補に含まれる`table` / `tr` / `td`タグを本文反復と誤検知し、selectorが停止する問題を修正した
- raw JSONは改変せず保存し、選択候補と`external_text`だけをDOM順の可視文字列へ正規化する回帰テストを追加した

## 2026-08-26: fix — Qwen OCRの不正bboxを空候補として継続

- レビュー前提のQwen＋dots OCRでQwenが不正bboxを返した場合、raw応答と`candidate_error`をcheckpointし、runを停止せずdots候補へ切り替えるよう修正した
- predictorとselectorの両方に回帰テストを追加し、不正bboxを派生シグナルで再解析しない契約を固定した

## 2026-08-26: fix — CIの複雑度と設計書ファイルマップを同期

- Kindle価格監視とサーバーバックアップの内部責務を分割し、C901 complexity ratchetの新規違反を解消した
- frontend / backendの生成済みファイルマップを実ファイル構成へ再同期し、clean checkoutのdocsチェックを回復した
