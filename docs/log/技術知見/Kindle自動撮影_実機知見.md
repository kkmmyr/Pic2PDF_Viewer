# Kindle 自動撮影 実機知見

最終更新: 2026-08-17

対象: Microsoft Store 版 Kindle 1.0.18632.0 / Windows / 3840×2160

関連:

- [Kindle 自動撮影取込 要件](../../design/要件定義/Kindle自動撮影取込_要件.md)
- [Kindle 自動撮影取込 完了記録](../../archive/Kindle自動撮影取込_実装計画.md)
- [バックエンド詳細設計](../../design/詳細設計/詳細設計書_バックエンド編.md)
- [フロントエンド詳細設計](../../design/詳細設計/詳細設計書_フロントエンド編.md)
- Kindle キャプチャツール詳細設計: `kindle-pdf/docs/detailed_design.md`

## 1. この文書の役割

この文書は、Kindle 自動撮影を実機で成立させる過程で判明した、次回の障害調査や
Kindle 更新時の再検証に必要な事実を集約する運用記録である。

- 現在守るべき仕様は要件定義・詳細設計を正本とする。
- Phase ごとの実装経緯と完了判定は実装計画を参照する。
- 本文書には、環境依存の観測値、失敗しやすい操作、切り分け順序、再撮影後の
  品質確認を記録する。
- 画像寸法や UI Automation の矩形は実測例であり、固定値の仕様ではない。

## 2. 実行前提

- Kindle アプリは起動済み、ログイン済みとする。capture agent は自動起動しない。
  撮影開始前のプロセス消失に対するオプトインのシリーズ復旧だけを例外とする。
- Windows は画面ロックなしとし、撮影中はマウス・キーボードを操作しない。
- active capture job 中は、診断スクリプトや別プロセスの
  `KindleAppController.attach_running_app()`を実行しない。状態確認はcapture job APIと
  heartbeatだけを使い、画面キャプチャ・UI Automation診断・目視確認はジョブ完了後に行う。
- 初期版の処理単位は 1 冊で、複数冊の連続処理や並列撮影は行わない。
- 書籍を ASIN で一意に照合できない場合は、安全側に倒して撮影を開始しない。
- Windows は撮影と転送だけを担当し、Linux の正式画像領域へ直接書き込まない。

### 2.1 シリーズ単位で複数冊を取り込む運用

シリーズ全巻を取り込む場合も、capture agent の処理単位と安全境界は 1 冊のままとする。
複数ジョブをまとめて投入せず、次の順序を各冊について直列に繰り返す。

1. 購入カタログをシリーズ名で検索し、所有中の対象 ASIN を確定する。
2. `capture_state=captured` の書籍と active capture job がある書籍を除外する。
3. カタログの `book_type` は誤分類を含むため、書名・レーベルと利用者指定を照合して
   capture job の `source` を確定する。
4. 1 冊だけ capture job を作成し、`succeeded` と正式画像の登録を確認する。
5. 失敗時は次冊へ進まず、エラー原因と既存正式画像が不変であることを確認する。
6. 成功後に次の 1 冊を投入する。

この直列手順は`scripts/capture_kindle_series.py`で自動化する。既定はdry-runで、
`--apply`と期待総冊数を明示した場合だけ実行する。スクリプトはKindle画面へ接続せず、
capture job APIだけを監視するため、active job中のcontroller排他規則に抵触しない。
全対象を先にqueueへ入れる方式は、共通原因の失敗が後続巻へ連鎖し、一時停止も困難に
なるため採用しない。

2026-07-26 の「茉莉花官吏伝」シリーズ取り込みでは、利用者指定に基づき
`ビーズログ文庫`を `novel`、`プリンセス・コミックス`を `comic` とする。
初期棚卸しは小説 18 冊・漫画 12 冊の計 30 冊で、小説 1・2・3・18 巻の 4 冊は
取り込み済み、新規対象は小説 14 冊・漫画 12 冊の計 26 冊である。

2026-07-26 の一時停止時点では、小説 4・5・6・7 巻を追加登録済みで、
既存分と合わせて小説 1〜7・18 巻の 8 冊が取り込み済みである。小説 8 巻は
未ダウンロード状態からの初回試行が `download_failed`、再試行は撮影 10 画面の時点で
利用者指示により停止し、job を `failed / paused_by_user` とした。正式画像と
`meta2.db` は更新されていない。残りは小説 8〜17 巻の 10 冊と漫画 1〜12 巻の
12 冊、計 22 冊である。

シリーズ処理の途中で画面を確認する場合も、現在の1冊が`failed`または`succeeded`に
なってから行う。茉莉花官吏伝5巻では、先頭移動中に別controllerから診断画像を取得した
直後の撮影が3画面で終了した。3画面結果を隔離して、API監視だけで再撮影すると91画面を
完走したため、active job中の別controller接続を禁止する排他規則とした。

### 2.2 連続処理を一時停止する手順

1. 次冊の job を作成するバッチ監視プロセスを先に停止する。
2. capture agent を停止し、新しい job が claim されないことを確認する。
3. 実行中 job は agent 専用 state API で `failed / paused_by_user` とし、
   `captured_screens` を停止時点の値で保持する。
4. active / queued job が 0 件であることと、対象書籍が `not_captured` のままで
   正式画像パスを持たないことを API で確認する。
5. Windows の job 専用一時ディレクトリは正式成果物ではない。再開時は途中画像を
   継ぎ足さず、新しい job で表紙から撮り直す。

再開順序は、停止した小説 8 巻を新規 job で再実行した後、小説 9〜17 巻、
漫画 1〜12 巻とする。停止時の一時画像 10 枚は品質確認・正式登録の対象にしない。

2026-07-27の再開では、小説8〜17巻10冊と漫画1〜12巻12冊の計22冊を正式登録し、
既存8冊と合わせてシリーズ全30冊が`capture_state=captured`となった。完了時点で
`not_captured`、`capture_pending`、`multiple_links`はいずれも0冊である。新規成功jobの
画面数は小説86〜102画面、漫画82〜100画面で、固定枚数ではなく各書籍の終端判定を
完了条件とした。

途中では小説10巻と漫画8巻のKindleダウンロードが一度失敗したが、Kindle画面で
「ダウンロードに失敗しました。もう一度試してください」と正式コンテンツ未作成を
確認後、新規jobによる1回の再試行で回復した。Windowsロック中の漫画4巻は撮影前、
漫画6巻の初回は80画面撮影後のSamba公開renameで停止した。いずれも正式データを
変更せず、原因解消後に新規jobで表紙から再実行して登録した。

事前の効率調査では、直接先頭移動は小説約2.38秒、漫画約2.41秒（安定確認を含む）で、
すでに全体のボトルネックではなかった。ページ描画安定待ちを短縮すると中間フレームや
重複画像の混入リスクが増えるため変更せず、各冊の完了後に人手で次jobを作成していた
待ち時間だけを直列オーケストレーターで削減する。

バックグラウンドでcapture agentを起動した最初の小説8巻では、Windowsが
`SetForegroundWindow`を拒否してChromeが前面に残ったが、旧実装は成功確認をせず
背面にあるKindle書籍カードの座標をクリックした。結果としてKindleはライブラリ画面の
ままで、`kindle_ui_unavailable`により撮影前に安全停止した。以後はforeground threadへ
input queueを一時接続して前面化し、前面handleがKindleと一致した場合だけクリックする。

漫画6巻では80画面撮影後、Samba inbox上のjob専用ディレクトリを
`.partial`から`.ready`へrenameする瞬間だけ`WinError 5`となった。直前の複数jobは同じ
共有先へ正常公開でき、失敗後に`.partial`・`.ready`とも残らなかったため、恒常的な
権限不備ではなくSMBの一時的な共有違反として扱う。画像コピーとmanifest生成後の
renameだけを短いバックオフ付きで有界再試行し、失敗し続ける場合は従来どおり
`transfer_failed`で正式登録せず停止する。

## 3. Kindle 操作で得た知見

### 3.1 接続と UI Automation

- 対象プロセスは `Kindle.exe`、ウィンドウタイトルは `Kindle` として検出できる。
- 検索結果カードの `library-more-menu-<ASIN>` を利用すると、タイトルの表記揺れだけに
  依存せず ASIN 完全一致で本人確認できる。
- 表示倍率が異なるマルチモニター環境では、検索欄のUI Automation矩形と
  pyautoguiのクリック座標が一致せず、検索値が更新されない場合がある。
  2026-07-26の茉莉花官吏伝4巻では、以前の検索値`プリンセス・コミックス`が残ったまま
  18巻の「最後に読んだ本」カードだけを照合でき、4巻は`book_not_found`で安全停止した。
- 2026-07-31の再検証では、検索欄へ`SetFocus`後に`Ctrl+A`と通常キー入力を行うと、
  日本語IMEがASINを全角化し、選択も成立せず、再試行ごとに同じASINが10回連結された。
  ライブラリ一覧は絞り込まれず、`kindle_ui_unavailable`で撮影前に安全停止した。
- 検索欄は`ValuePattern.SetValue`で半角ASIN全体を置換し、同じpatternから完全一致を
  読み戻してから候補探索へ進む。実機では連結済み全角値を半角ASINへ置換でき、
  ASIN固有カードを1件取得した後もKindleプロセスが応答することを確認した。
  以前の不安定化調査はUI Automation tree全descendants走査と`SetValue`を同時に
  行っており原因を分離できていなかったため、限定controlの`SetValue`自体を
  禁止する根拠にはしない。全descendants走査とUI Automation `Click`は引き続き使わない。
- 2026-08-01のグリムコネクト1巻では、検索欄の半角ASIN完全一致後も最初の候補取得が
  0件となり`book_not_found`で停止したが、実画面には約20秒後に対象が1件表示され、
  同じAutomationIdからASIN付き候補を取得できた。検索結果は非同期描画されるため、
  検索値確認後はreader timeout内で候補出現を有界に再確認する。
- UI Automation tree の全 descendants を約 100 ms 間隔で走査した調査では、
  完了後に Kindle が `ucrtbase.dll / 0xc0000409` で異常終了した。
  因果関係は断定できないが、通常処理では AutomationId を指定した `FindFirst` と
  低頻度ポーリングだけを使う。
- Kindle プロセスの消失は再接続で隠さず、`kindle_app_exited` としてジョブを失敗させる。

### 3.2 未ダウンロード書籍

- ダウンロードボタンの状態だけではなく、Kindle の正式コンテンツフォルダが安定した
  ことも確認してから読書画面へ進む。
- タイムアウトはダウンロード失敗と区別して利用者へ返す。
- 検証書籍 `茉莉花官吏伝 十八　青龍の睛を点ずる` で、未ダウンロード状態から
  ダウンロード完了を待って撮影へ進めることを確認した。

### 3.3 先頭移動とページ送り

- `go_to_start(source, direction)` は「位置に移動」でロケーション `1` を指定し、
  source 別のフッター表示を検証する。直接遷移または結果検証に失敗した場合は
  `positioning_failed` とし、数十画面を逆方向へ送る先頭探索は行わない。
- 漫画ではツールバー非表示時に `FooterLabelText` を取得できない。ツールバーや
  「位置に移動」ダイアログを表示した後は取得できる実測もあるが、常時存在しないため
  フッター文字列を通常の先頭判定の必須条件にできない。
- `ReadingArea` を縮小グレースケール化した画像差分でページ変化と端部を判定する。
  実測では本文ページ遷移の差分が平均 `6.82`、端部 chevron だけの変化が `0.38` で、
  本文変化と装飾変化を分離できた。
- Microsoft Store 版では画面端クリックより矢印キーの方が安定した。右綴じ・左綴じから
  決定した次ページ方向に対応するキーを利用し、撮影ループと先頭移動で同じ規則を使う。
- リフロー小説では、表紙の `Location 1` から本文1ページ目へ進む最初の遷移だけ、
  通常の左送りでは変化せず右キーで進む実機例がある。本文開始後は左キーで進むため、
  撮影ループは2画面目への遷移に限り、通常キーで無変化なら反対キーを1回だけ試す。
  終端では反対キーを試さず、既撮影ページへ戻ることを防ぐ。
- 最終ページでは同じ画像が返ることがあるため、無変化を複数回確認して終了する。
  一時的な描画遅延は直ちに終端とせず、再試行後に判定する。

#### 「位置に移動」による高速化（2026-07-26）

小説・漫画とも、ツールバー右端の `moreMenuButton` から「ページへ移動する」
（旧版は「位置に移動」）を開ける。
限定探索で確認した UI Automation 要素は次のとおり。

| 用途 | AutomationId / Name | 操作方式 |
|---|---|---|
| その他メニュー | `moreMenuButton` / `もっと` | control 矩形を通常クリック |
| メニュー項目 | `btn-popover-menu-item` / `ページへ移動する`（旧版は `位置に移動`） | Name も併用して限定探索し、通常クリック |
| 入力欄 | `go-to-page-input` / `ページ番号入力`（旧版は `ロケーション番号入力`） | 型付きcontrolの`ValuePattern.SetValue`で半角値を設定し完全一致を読み戻す |
| 確定 | `modal-confirm` / `ページへ移動する`（旧版は `位置に移動`） | control 矩形を通常クリック |
| 結果確認 | `FooterLabelText` | 読み取り専用 |

入力対象は現行版では**ページ番号**、旧版では**ロケーション番号**として
表示される。ただし、どちらも撮影画像の連番とは一致しない。UI Automationの
`Invoke`はKindleを不安定化させたため使用せず、ボタンは本人照合済み矩形を通常クリックする。
2026-07-26時点の実機では、同項目のName変更に加えてpopoverがKindle本体とは別の
UI Automation rootへ公開された。デスクトップからNameとAutomationIdを限定探索し、
control中心がKindleウィンドウ内にあることを確認すれば安全に取得できる。
同版では読書画面の `backButton` が座標クリックを受け付けない実機例もあるため、
本人照合済みcontrolへ `SetFocus` した後、通常のEnterキーでライブラリへ戻る。

実測結果:

| source / 表示 | 移動前 | 入力 | 移動後 | 確認時間 |
|---|---:|---:|---:|---:|
| リフロー小説・単ページ | `Location 2748 of 3304・82%` | `1` | `Location 1 of 3304・0%`、表紙 | 約 0.12 秒 |
| 小説・固定画像の先頭領域 | `Location 381 of 4006・9%` | `1` | `Location 4 of 4006・0%`、表紙 | 未計測 |
| 固定レイアウト漫画・2ページ | `Location 150 of 169・88%` | `1` | `Location 2 of 169・0%`、表紙 | 約 0.17 秒 |

上表はダイアログ確定からフッター更新までの時間である。実装後の
`go_to_start(source, direction)`を、書籍オープン・レイアウト確定後の実機で再試験すると、
読書画面安定待ちと結果検証を含め、小説`2748 → 1`は約2.38秒、
漫画`150 → 2`は約2.41秒で完了した。

漫画は入力値 `1` と移動後の表示値が一致しない。右綴じ・2ページ表示の先頭では
表紙が表示されても `Location 2` となるため、`Location 1` 完全一致を成功条件に
してはならない。小説と漫画の操作部品は共通だが、先頭確認条件を source 別にする。

同じリフロー小説でも、直接遷移後のフッターが
`Location 1 of 2959・0%` または `ページ1/233・0%` となる実機例がある。
実機で後者は表紙ではなく本文側の2画面目であることを確認した。この場合だけ、
ダイアログ確定後に `ReadingArea` 中央へフォーカスを戻してから、利用者が指定した
次ページ方向と逆のキーを1回送り、`Location 1 ...・0%` を再確認して表紙とする。
漫画にはこの補正を適用しない。
ページ本体の切替後も `FooterLabelText.Name` の更新が数秒遅れる実機例があるため、
補正後は追加のページ送りをせず、同じフッター control の更新だけを有界回数待つ。

2026-07-29 の `グリムコネクト1―赤ずきんの章―` 実機確認では、入力欄へ
`1` を設定して確定した後、表紙画像が表示されていてもフッターは
`Location 4 of 4006・0%` となった。旧条件の `Location 1` 完全一致では
`positioning_failed` となる偽陰性であり、連続ページ送りを行わず停止した点は正しい。
入力値 `1` の読み戻しと直接遷移が成立している場合だけ、小説の
`Location 1〜4・0%` を先頭近傍として扱う。丸めによって `0%` となり得る
本文を広く許容しないため、`Location 5` 以降と進捗 `1%` 以上は拒否する。

`go_to_start(source, direction)` は `set_page_layout(source)` の後に、
「位置に移動」で `1` を指定して読書画面の安定を待つ。小説は
`Location 1〜4 ...・0%` を表紙・先頭近傍として確認し、`ページ1/N・0%` の場合は上記の
1回だけの補正後に同じ表紙条件を確認する。漫画は表紙表示と `0%`
（実測では `Location 2`）を確認する。メニュー、入力欄、直接遷移結果、
補正後の表紙を取得または検証できない場合は `positioning_failed` として停止する。

2026-07-31のグリムコネクト1巻の右送り再撮影では、直接遷移の確定ボタンを
座標クリックした後もページ番号入力ダイアログが残った。背後のフッターは
`Location 4 of 4006・0%`だったため旧実装は位置決め成功と判定し、モーダルを含む
表紙1画面だけを保存して`success`として登録した。確定ボタンはcontrolへ
`SetFocus`後にEnterで実行し、`go-to-page-input`が消えたことを確認してから
フッター検証と撮影へ進む。モーダルが残る場合は`positioning_failed`とし、
1画面成果物を正式配置しない。

同巻の安全停止後に再確認すると、移動メニュー項目のcontrol自体は取得できる一方、
その直後の`BoundingRectangle`取得が一時的なCOMエラーとなり、座標クリック前に
`positioning_failed`となる場合があった。`SetFocus`も同じCOMエラーとなるため、
取得済みポップアップcontrolだけに依存した操作は安定しない。さらに右上メニューを
`SetFocus`＋Enterで開く方法もポップアップが出ない場合があり、Home＋Enterの選択も
再現性が不足した。

2026-08-01の実機再調査では、右上の`moreMenuButton`自体は安定して矩形取得・通常クリック
できた。ポップアップは別UI Automation rootのため項目controlを安定取得できないが、
先頭の「ページへ移動する」はメニューボタンにアンカーされた一定の相対位置に表示される。
本人照合済みメニューボタンの中心から相対位置をクリックし、その結果として型付き
`EditControl`の`go-to-page-input`が現れた場合だけ続行する。入力欄と確定ボタンは、
汎用`Control`検索ではValuePatternや矩形を取得できない場合でも、型付き`EditControl`と
`ButtonControl`なら安定して取得できた。`modal-confirm`のcontrol中心を通常クリックし、
入力controlの消失を確認する。実機では`ページ23/381`から`1`へ直接移動し、
`ページ1/381・0%`を確認後、逆方向キー1回で`Location 4 of 4006・0%`の表紙へ戻れた。
同じ入力欄へ通常キーで`1`を送ると日本語IMEにより`１`となり、確定してもモーダルが
閉じなかった。型付き入力欄の`ValuePattern.SetValue("1")`と完全一致読み戻しを使う。

グリムコネクト1巻では、表紙のフッターが`Location 4 of 4006・0%`で、移動画面にも
「現在1ページ」と表示される。この状態で1ページへ再移動すると、Kindleは
「有効なページ番号を入力してください」と表示して確定を拒否する。赤いエラー文の
出現だけでも画像差分は生じるため、画像変化をダイアログ終了条件にしてはならない。
対象を開いた時点でフッターが表紙なら直接遷移を省略し、`ページ1/N・0%`なら
1回だけ表紙へ戻す。その他の位置だけ直接遷移を使い、入力controlの消失を必須とする。

同巻の表紙から左右キーを実測すると、左キー1回で`ページ1/381・0%`、さらに左で
`ページ2/381・0%`へ進み、右キー2回で`Location 4 of 4006・0%`の表紙へ戻った。
グリムコネクトは固定レイアウトではなく通常の縦書きリフロー小説であり、
次ページ方向は`left`である。旧20画面結果は方向ではなく、不正な開始位置から
逆側境界まで撮影した不完全成果物だった。

修正後の左送り撮影でも20画面目（Kindle表示は`ページ22/381`）で2回のキー入力が
届かず停止した。手動の左キーでは直ちに`ページ23/381`へ進み、エージェントと同じ
最大化・クロップで隣接画像差を測ると平均5.93で閾値1.0を十分超えたため、
画像変化判定ではなくキーボードフォーカス喪失が原因と判断した。撮影開始時だけでなく
各ページ送り直前にKindleを前面化し、`GetForegroundWindow`の一致後だけキーを送る。

前面ハンドル一致を追加した再試行でも20画面目で再現した。トップレベルウィンドウが
Kindleであることと、内部WebViewの読書領域がキー入力先であることは同一ではない。
取得済み`ReadingArea`中心を各キー送信前にクリックする実験では、`ページ23/381`から
30回連続で`ページ58/381`まで進み、20回目付近も通過した。フッター表示だけは一時的に
1回遅れて更新される場合があったが、本文は次ページへ進んでいた。Microsoft Store版の
撮影ループでは、前面ハンドル確認後に`ReadingArea`中心へフォーカスを戻してからキーを送る。

ただしエージェント再試行では、上記を加えても同じ20画面目でキーが届かない事象が
再現した。手動で同じ`keyDown/keyUp`を25回送ると`ページ22/381`から`ページ50/381`まで
進めたため、送信方式そのものやページ差分閾値ではなく、外部ウィンドウ切替との競合を含む
一時的な入力先喪失が残っている。`ReadingArea`中心から左右120px内側にある
「次のページ」矢印を通常クリックすると`ページ50/381`から`ページ51/381`へ進むことも
実機確認した。通常キーで変化しなかった場合の1回だけの再試行は、取得済み
`ReadingArea`に対する相対矢印クリックへ切り替える。固定画面座標は使わない。

表紙からの30画面限定ローカル再現でも20画面で停止したため、保存画像と停止後の画面を
同じ`1940×3516`クロップで直接比較した。20枚目は白地に小さな巻タイトルだけ、次画面は
白地に小さな著作権表記だけであり、ページ送り自体は成立していた。両画像の全チャンネル
平均差分は`0.643`で旧閾値`1.0`未満だった一方、輝度差20超の画素は全体の`0.3647%`あり、
内容は明確に異なった。全画面平均だけでは疎な文字ページを同一と誤判定する。
平均差分に加え、輝度差20超の画素比率が`0.1%`以上ならページ変化として扱う。
1画素だけのUIノイズ等は従来どおり無視する。

修正後のグリムコネクト1巻は、168画面の撮影後にKindleフッター
`ページ381/381・100%`へ到達した。Kindle表示の381ページは現在の表示設定での
撮影画面数と一致せず、`expected_screens=381`を指定したため完全撮影を未完了と
誤判定した。既知のKindleページ数を`expected_screens`へ転用せず、画面数の実測が
ない書籍はsource別最低件数ガードと終端画像無変化で完了判定する。

同じ条件でグリムコネクト2・3巻も再撮影し、2巻168画面、3巻141画面で終端へ到達した。
3冊とも通常の縦書きリフロー小説であり、固定レイアウト用の処理は不要だった。
全画像の連番・復号・3516×1940寸法を確認し、先頭・中間・末尾と白紙候補を
原解像度で確認した。2巻の完全重複3組は章間の「次のページへ」で、白紙候補は
扉・著作権表記・疎な本文・奥付だった。3巻は完全重複0件で、白紙候補は目次・扉・
著作権表記・疎な本文・奥付だった。画像統計だけで重複や白紙を欠落扱いせず、
候補画像を直接開いて意味を確認する。

## 4. 表示レイアウトと撮影範囲

### 4.1 共通

- Microsoft Store 版 Kindle は F11 中にページ送りすると本文が白画面のままになる
  場合があるため、F11 ではなくウィンドウ最大化を使う。
- 最大化後に `ReadingArea` を取得し、画面座標から Kindle ウィンドウ相対座標へ変換する。
- 最大化により読書領域からキーボードフォーカスが外れる実機例があるため、
  撮影矩形の確定後に `ReadingArea` 中央を通常クリックしてフォーカスを戻す。
  このクリックはページ送り用の端部ではなく、ツールバー表示切替領域で行う。
- 固定クロップ値は controller を使わない手動・互換経路だけのフォールバックとする。
  自動 agent は UI Automation の矩形を取得・検証できなければ撮影を開始しない。
- ジョブ開始時に `source` に対応するレイアウトを毎回選択し、前回読書時の設定を
  引き継がない。

### 4.2 漫画

- `aaMenuButton` から `aaOption-Split` を選択し、`ToggleState` で 2 ページ表示を確認する。
- `ReadingArea` の上下全体を保存する。横幅は先頭の単ページ幅だけに固定せず、
  後続の見開き 2 ページ分を含む安全幅を確保する。
- 表紙や奥付の単ページは安全幅内で中央配置される。単ページの左右に余白があることは
  欠陥ではなく、後続見開きの欠落を防ぐための意図した挙動である。
- 検証漫画は 85 画面、全画像 2936×2064 となり、通常見開きの外側余白中央値は
  左 15 px、右 17 px だった。

### 4.3 小説

- 固定レイアウトで `aaOption-Single` が存在する場合は選択して状態を確認する。
- リフロー型小説ではページ数 option 自体がなく、`フォント-item`、フォントサイズ、
  余白、間隔だけが表示される。この構成を単ページ表示として受け入れる。
- 横方向は `ReadingArea` の左右端を使う。
- 縦方向は `TopChrome.bottom` から `Footer.top` までとし、上部書名と下部の
  `ページ X/Y・進捗 %` を保存画像から除外する。
- `TopChrome` または `Footer` を取得できない、または包含関係が不正な場合は
  `kindle_ui_unavailable` として撮影前に停止する。自動 agent は `ReadingArea`
  全体へ暗黙にフォールバックしない。固定 105 px の下マージンは本文を切るため
  正本にしない。
- 検証小説 2 冊は 91 画面・92 画面、全画像 3516×1940 となり、本文下端を残しながら
  Kindle の上下 UI を除外できた。

## 5. 再撮影後の公開とブラウザーキャッシュ

### 5.1 判明した問題

画像は `Cache-Control: public, immutable, max-age=2592000` で配信される。
同じ書籍ディレクトリ、同じ `009.png` のような名前へ再撮影結果を置換しただけでは、
ブラウザーが最大 30 日間、置換前の画像を表示し続ける。

漫画 P.9 が単ページに見えた事例では、次を確認した。

1. サーバー上の現行 `009.png` は正常な見開きだった。
2. 利用者の画面は、置換前データの単ページ内容と一致した。
3. 現行データを再撮影しても解決せず、画像 URL の世代を変える必要があった。

### 5.2 現在の対策

- `GET /api/books/{path}/images` は各画像の `st_mtime_ns` を `v` クエリへ付与する。
- 小説一覧の `thumbnail_url` は `001.png`、未生成時は書籍ディレクトリの
  `st_mtime_ns` を `v` として返す。
- `NovelReaderPage` は一覧で受け取った `v` を同じ書籍の全ページへ引き継ぐ。
- 小説の世代番号は、正式配置で `001.png` を含む画像列全体を置換することを前提とする。
- 未 OCR 書籍で `page_count=null` の場合も、画像への HEAD 探索でページ数を確定する。

表示不良を報告された場合は、再撮影を始める前に、サーバー上の現行画像、API が返す
`v`、ブラウザーの実際の画像 URL の順で照合する。

## 6. 成果物の転送と置換

1. Windows のジョブ専用一時ディレクトリへ連番 PNG と manifest を作る。
2. Samba inbox へ `.partial` 名で転送する。
3. 転送完了後だけ `.ready` へ原子的に rename する。
4. Linux で manifest、枚数、連番、安全なパスを検証する。
5. 既存正式データを世代バックアップへ退避する。
6. 新しい画像ディレクトリを正式名へ切り替える。
7. 正式配置成功後だけ `meta2.db` の ASIN とジョブ状態を更新する。

Windows 側の転送失敗、Linux 側の検証失敗、正式配置失敗では、既存正式画像と
`meta2.db` を変更しない。2026-07-26 の小説再置換前データは次へ退避した。

`/opt/pic2pdf-viewer/data/.capture-replacement-backup/20260726-092136`

バックアップを削除する時期は自動化せず、一定期間の閲覧・OCR確認後に運用判断する。

## 7. 品質確認手順

### 7.1 画像列の機械確認

- 期待枚数と実ファイル数が一致する。
- `001.png` から最終番号まで連番欠落がない。
- 全画像の寸法が同じである。ただし寸法値そのものは環境依存とする。
- SHA-256 が完全一致する重複画像がない。
- 意図しない全面白画像がない。
- 表紙、本文中盤、最終ページを目視する。

### 7.2 source 別の目視確認

漫画:

- 通常ページが最初から見開きである。
- 左右ページの外側が切れていない。
- 中央の綴じ目が不自然に重複していない。
- 表紙・奥付の単ページが中央に収まる。

小説:

- 本文の最上部と最下部が切れていない。
- 上部に Kindle の書名がない。
- 下部にページ数・進捗率がない。
- 表紙や挿絵を固定クロップで欠損していない。

### 7.3 公開後の確認

- API の画像 URL に `v` が含まれる。
- 同じブラウザーで再読込し、旧ページと新ページが混在しない。
- 漫画はページ数と見開き表示を確認する。
- 小説は OCR 未実施でも画像列探索により総ページ数を表示できる。
- 問題時に戻せるバックアップの枚数を確認する。

### 7.4 OCRへの引継ぎ

- 撮影完了とOCR完了は別の状態として扱う。購入カタログで`captured`でも、
  小説DBの`ocr_done_at=null`なら文字起こしは未完了である。`is_indexed`は
  `indexed_at IS NOT NULL`と同義とし、Embedding構築完了だけを表す。
- シリーズ撮影後は、今回追加した書籍名を固定して1冊ずつOCRへ投入する。
  対象なしの全件OCRは、以前から画像だけ存在する別巻も巻き込むため使用しない。
- 茉莉花官吏伝の2026-07-27撮影分では小説8〜17巻の10冊が対象であり、
  撮影済み全18巻や未OCR書籍全体を対象にしない。
- OCR完了後もQA承認前のrunは正式本文ではない。画像のみ保持する目次・挿絵・
  奥付・広告を分類し、本文ページの必須QAと必要なCodex補正を終えてから公開する。
- 実行結果は10冊914ページ、本文806ページ、画像のみ108ページ、Codexによる
  原画像照合・補助候補採用15ページで、全runを`completed / approved`として公開した。
- 終端位置を使う自動分類だけでは、本文・あとがきを奥付や広告と誤認し得る。
  今回は14巻p79、16巻p95、17巻p97を画像確認で本文へ戻した。終端付近の
  非本文候補に長いOCR結果がある場合は、索引除外前に原画像を確認する。
- 文字起こし公開後、8〜17巻のチャンク・Embeddingを`rebuild`し、10冊すべての
  `indexed_at`更新を確認した。検索は8巻の「赤奏国 コウマツリカ」がp80ほか、
  17巻の「茉莉花」がp12ほかへ命中した。
- Embeddingの実測は、サーバーCPUで8巻が約8分14秒、Windows CPUで9巻が
  約1分32秒、Windows RTX 5070で10巻が約12秒、11〜17巻が各約6〜8秒だった。
  リモートOllamaへ切り替える場合、`NOVEL_DB_OLLAMA_BASE_URL`だけでは
  サーバー既定の`NOVEL_DB_EMBED_NUM_GPU=0`が送信されてCPU実行になる。
  GPU利用時は`NOVEL_DB_EMBED_NUM_GPU=99`も一時設定し、完了後は両設定を戻す。
- 本番Qwen llama-serverが未起動のため、要約・人物抽出を含む`full_build`は実行せず、
  `rebuild`で検索索引だけを構築した。要約・人物抽出は正式本文・検索索引の公開を
  ブロックしない独立工程として扱う。

## 8. 2026-07-26 受入実績

| source | 書籍 | ASIN | 画面数 | 実測画像寸法 |
|---|---|---|---:|---:|
| `novel` | 茉莉花官吏伝 十八　青龍の睛を点ずる | `B0FTDGD2XV` | 91 | 3516×1940 |
| `novel` | 十三歳の誕生日、皇后になりました。10 | `B0DJ3DYD7M` | 92 | 3516×1940 |
| `comic` | 十三歳の誕生日、皇后になりました。【電子特別版】 1 | `B08M8XZWM7` | 85 | 2936×2064 |

- 3 冊とも連番欠落、SHA-256 完全重複、意図しない全面白画像は 0 件だった。
- 小説は上部書名・下部ページ進捗を除外し、本文下端が残ることを確認した。
- 漫画 P.9 は更新番号付き URL で正常な見開きを表示した。
- 未 OCR 小説は本番画面で `20 / 91`、`20 / 92` を表示した。

### 8.2 Phase 3 境界分割後の再検証

2026-07-26、現行Microsoft Store版Kindleで次を再検証した。

- 小説10巻: ジョブ`92b33d22-261e-45b5-8ab3-063eff46d6bf`、
  92画面、全件3516×1940。連番欠落、SHA-256重複、全面白はいずれも0件。
- 小説のページ直接遷移: `ページ1/233・0%`は表紙の次の画面であり、
  `ReadingArea`へフォーカスを戻して右キーを1回だけ送ると
  `Location 1 of 2959・0%`の表紙となった。
- 漫画1巻: ジョブ`bca6326e-e7d9-431b-8884-a178a9e415bc`、
  85画面、全件2936×2064。連番欠落、SHA-256重複、全面白はいずれも0件。
- 漫画はページ/ロケーション`1`への直接遷移だけで
  `Location 2 of 169・0%`の表紙となり、逆方向キーは不要だった。
- 漫画の表紙、P.9、P.10、最終画面を目視し、P.9から見開きで左右端が欠けず、
  単ページの表紙・最終画面も見開き安全幅内に中央保持されることを確認した。
- 両書籍のmeta ASINは指定値と一致し、同一ASIN置換の旧画像は
  `.capture-replacement-backup/<timestamp>_<job-prefix>/`へ世代退避された。

### 8.3 未ダウンロード書籍とWindows非対応書籍の再検証（2026-08-01）

- 『悪役令嬢レベル99 ～私は裏ボスですが魔王ではありません～』は未ダウンロード状態から取得し、
  Kindleを新規起動した状態での制御付き再試行により120画面を正常撮影した。
- 『後宮一番の悪女』もダウンロード完了後にKindleを新規起動してから撮影し、80画面を正常取得した。
  両書籍とも全画像3516×1940、連番欠落・書籍内完全重複・白紙は0件だった。
- ダウンロード完了直後またはモーダルを閉じた直後に、同じKindleプロセスと既存UI Automation treeを
  再利用すると、`ucrtbase.dll / 0xc0000409`でKindleが終了し、ジョブは`kindle_ui_unavailable`となった。
  Kindleを再起動してUI treeを取り直した後は2冊とも成功した。UIA参照をプロセス境界を越えて再利用せず、
  ダウンロード・モーダル遷移後は状態を再取得する。自動再起動を実装するまではfail closedで停止し、
  同一対象の再試行前にプロセスと検索候補を直接確認する。
- 『虚構推理（講談社文庫版）』はダウンロード操作後に「この本はWindows版Kindleではサポートされていません」と表示された。
  このモーダルは`download_failed`とは別の恒久的な環境非対応として扱い、間隔を空けた再試行対象にしない。
  撮影・OCRジョブは作成せず、別の購入済み小説へ選定を切り替えた。ただし作品全体を除外せず、
  Windows版Kindleで取得可能な講談社タイガ版は別商品・別ASINとして後続確認する。

### 8.4 106冊夜間撮影の時間内訳と復旧境界（2026-08-02）

- 9シリーズ106冊のうち停止時点で63冊・8,258画面を撮影した。開始から最終成功まで
  13時間11分で、正常な1冊は平均6分43秒、中央値6分27秒、平均131画面だった。
- 成功処理は7時間2分で、そのうちページ撮影・保存・転送が6時間35分を占めた。
  1画面あたりは約2.9秒であり、直列ページ送り自体が正常系の下限になる。
- 失敗試行は78件・1時間11分、job間の待機・診断・復旧は4時間58分だった。
  5分以上の11間隔だけで4時間33分を占め、30分周期の外部監視による再開待ちが
  総時間を大きくした。`kindle_ui_unavailable`は70件、復旧記録は60件だった。
- 改善の主対象はページ送り速度ではなく、撮影開始前のKindleクラッシュを直列ランナーが
  即時検知することとする。ただし自動復旧は、失敗jobが撮影未開始、Kindleプロセス消失、
  同じASINの一意な再照合、別の未完了jobなし、1回の上限をすべて満たす場合だけ許可する。
  `download_failed`や位置決め・撮影・転送・登録失敗は自動復旧へ混ぜない。

### 8.5 登録前画像QAのshadow検証（2026-08-02）

- ローカル既存画像から小説2冊・漫画2冊、計392画面を新しい登録前QAで検査した。
  3冊は連番、全画像復号、寸法一貫性、SHA-256、件数の厳格条件を通過した。
- 旧方式の小説1冊は画像ディレクトリに`008_debug.json`と`008_debug_vis.png`が
  混在しており、撮影packageとしては意図どおり厳格拒否された。新agentの一時出力は
  連番PNGだけを許可し、診断成果物を正式画像へ混在させない。
- 警告候補は白紙・疎7件、低容量15件、完全重複2組、隣接dHash近似重複4組、
  小説上下端の内容密度1件だった。章扉・挿絵・奥付等の正常画面を含み得るため、
  自動削除や登録拒否には使わず、Codex画像QAの優先順位付けに留める。
- OneDrive上の全件走査は画像のオンデマンド読出しにより2分を超えてタイムアウトした。
  全件shadow監査は画像がローカルにあるWindows側、または正式配置後のLinux側で実行し、
  ネットワーク同期領域を性能基準にしない。

### 8.6 OS通知オーバーレイの実機検出（2026-08-02）

- 『この仮面の悪魔に相談を！』初回成功100画面には、右下のOneDrive
  「ファイルの自動ダウンロード」通知が全標本へ重なっていた。連番、復号、寸法、重複、
  白紙、端密度だけでは異常を検出できず、登録後の目視QAで判明した。
- 通知を閉じて同一書籍を再撮影した100画面には同じ固定領域が存在しなかった。
  汚染世代と正常世代を比較すると、幅1024へ正規化した外周タイルのうち、汚染世代では
  隣接2タイルが23〜25/32標本で完全一致し、正常世代では条件を満たすタイルが0件だった。
- 文言OCRや特定通知のテンプレートではなく、異なるページに同一座標の構造化画素が
  反復する性質を検出根拠とする。全画像hashが同じページ群は除外し、無地余白も
  輝度分散・エッジ条件で除外する。高信頼だけをblocking、中信頼をwarningとする。
- この方式は一度だけ現れる短時間通知を保証できない。単発混入はOCR側の
  `ui_overlay_text_detected`と原画像QAを併用し、撮影ゲート単独で完全検出を主張しない。

### 8.7 設計・実装横断監査で確定した検証境界（2026-08-02）

- Linux側は正式画像の連番、復号、寸法、SHA-256、件数を実ファイルから再計算する。
  一方、終端観測フレームとカナリア2画像はpackageへ転送しないため、最終ページ到達、
  無変化回数、カナリアSHA・差分指標そのものは再計算できない。Linux側の責務は、
  これらのpolicy version、型、範囲、件数、最終画像、正式撮影との矩形・寸法一致を
  fail closedで検査することであり、Windows側の意味判定を再実行することではない。
- 反復オーバーレイもWindows側で画像から検出し、Linux側は画像SHA-256でpackage内容を固定した上で、
  `kindle-repeated-overlay-v1`の合格、標本数、候補数、blocking候補0件を検証する。
  Linux側で検出器を再実行する二重実装ではない。
- `SessionSafetyGuard`は連続`download_failed`を記録し上限3冊を持つが、現行
  `execute_series()`は`record_failure()`直後に全エラーで停止する。このため実運用は
  1冊目の`download_failed`で後続jobを作らない、設計値より厳しいfail closedである。
  2026-08-22の運用判断でこの初回停止を当面の正式方針とした。隔離・間隔付き継続は将来候補であり、
  導入するまでは上限3冊を「2冊目まで自動継続済み」と解釈しない。
- 全source・全ASINで未完了job最大1件の制約はbackend transactionが最終防衛線として強制する。
  2026-08-22以降の書籍詳細UIはASINで絞らずactive jobを事前判定し、他ASINが実行中でも
  開始と確認ダイアログの確定を無効化する。同一ASIN・他ASINそれぞれの理由を表示し、
  処理中jobへ運ぶ。UIの5秒poll間に生じる競合はbackend制約が引き続き拒否する。

### 8.8 R1 境界分割後の Windows 実機受入（2026-08-16、未合格）

対象漫画を『おとなになっても（1）』(`B07YKJ6VSB`)、対象小説を
『悪役令嬢の中の人【特典SS付】』(`B08V8CDCR3`) として再受入を開始した。
漫画で次の未解決事象を確認したため、安全規則に従って小説は投入していない。

- 初回job `a99ec1df-6982-49ea-95b8-e56a1976ec68`（`comic / left`）は、
  未ダウンロード書籍の取得後にKindleプロセスが終了し、検索欄の再取得に失敗して
  `kindle_ui_unavailable`となった。撮影と正式登録には進まなかった。
- Kindle再起動後、同じASINのコンテンツ26ファイル・60,673,214 bytesが安定して存在し、
  検索欄の`ValuePattern`が書込み可能であることを限定control探索で確認した。
- 新規job `e6338c6b-840a-4eb8-b42c-068af7fcb1ac`（`comic / left`）は、ASIN照合、
  見開き設定、先頭移動、カナリアを通過したが、既往漫画実績82〜100画面を大幅に超えて
  620画面まで終了しなかったため手動停止した。停止後のKindleは表紙直後の奥付画面を
  表示していた。capture loopは直前1画面との無変化だけを終端条件にするため、選択方向と
  2画面目の反対方向fallbackが表紙・奥付の往復を作った可能性が高い。2周期の再訪を
  検出する防護がない現状では正常完走と判定できない。
- 停止jobは`failed / paused_by_user / captured_screens=620`とし、job一時ディレクトリが
  0件、active jobが0件、カタログが`not_captured`のままであることを確認した。
  正式画像とmetaは更新されておらず、fail-closedとcleanupは成立した。
- 方向を`right`へ変えた新規job `bbef4335-d01c-4046-b971-abb8f5c8d21c`は、
  撮影前のページ設定UIを開けず`positioning_failed`で停止した。正式領域は更新していない。

したがってR1受入は未合格とする。再試験前に、誤方向時の2周期再訪を有界に検出して
正式登録前に停止することと、ページ設定UIの一時取得失敗を再現・切り分けることが必要である。

### 8.9 R1 修正後の Windows 実機再受入（2026-08-17、合格）

8.8で確認した2事象へ、2画面周期の有界検出と`aaMenuButton`の期限付き再取得を追加し、
同じ漫画・小説で再受入した。修正後のKindleテストは116件成功した。

- 漫画の誤方向`right` job `6728f56d-f8f4-40fb-8b3b-6721ba3757c2`は、
  `A → B → A → B`の4画面目を保存前に検出し、3画面の一時出力だけで
  `capture_failed`となった。カタログは`not_captured`のままで正式領域を更新しなかった。
- 正方向は`left`である。job `59aff8d7-b06f-446e-9b27-800d01e190ba`は、ASIN照合、
  見開きページ設定、先頭移動、カナリアを通過し、末尾の2画面周期で重複`089.png`を
  一時領域から破棄した。88画面の画像QA、転送、登録を完了して`succeeded`となった。
- 小説job `ea823c02-4ae6-4075-b7f8-4ff6d8f5ca9f`は、カタログ上の`book_type`に依存せず
  `source=novel / direction=left`で単ページ設定、先頭移動、カナリアを通過した。
  104画面で通常の無変化終端を確認し、画像QA、転送、登録を完了して`succeeded`となった。
- 両書籍の初回ダウンロード完了時はStore版Kindleが終了した。terminal jobを再開せず、
  `.azw`と`.voucher`を含むローカルコンテンツの完全性、Kindle再起動、ASIN検索欄の
  ValuePattern読み戻しを確認してから新規jobを作成する既存のfail-closed契約で復旧した。
- 最終確認は漫画・小説とも`capture_state=captured`、active job 0件で、capture agentを
  停止した。以上によりR1 Windows実機受入を合格とする。

### 8.10 B-32 未調整holdoutと警告運用判断（2026-08-22）

- 現行本番画像から検出結果を見ず、固定seed、source、タイトルfamily hashだけで漫画2冊・
  小説2冊を選んだ。全524画面のSHA-256を先に封印し、選定digestは
  `90cf0775f6672ce61af945251ea3572257c79756a93691a326f6b3c5c1787c0c`となった。
  既知のOneDrive通知混入／正常再撮影pairは検出器の校正に使ったため、このholdoutから除外した。
- Codexによる全画面の縮小画像QAでは、欠落、重複、白紙化、上下端切れ、Kindle / OS UI混入を
  0件とlabelした。章扉、表紙、挿絵、奥付、意図的な余白は正常とした。label digestは
  `1f46d57bee94c395aab209a1603250a3b7935e1aee237817542fe737d8ebb4be`だが、
  `human_confirmation=pending`であり、人手確定labelとは扱わない。
- `kindle-image-warning-v1`の実測は近似重複5、白紙・疎6、低容量13、小説端密度2の計26件で、
  すべて正常画面への候補だった。完全重複と反復overlayは0件、構造監査errorも0件だった。
  欠陥label基準の暫定precisionは発火した4 codeすべて0、陽性0件のためrecallは`null`である。
- 同じ4冊をhardlinkした一時領域へ、固定seedで完全重複、白紙化、1 pixelだけ異なる重複、
  25%上端切れを各1件注入し、レシピdigest
  `706e8c48e8f89638b9c35692f9a9986545a6f7503a7a9647b2ebe3cddffbf62e`を
  評価前に固定した。5 codeのTPは各1、FNは0でrecall 1.0だった。ただし実機故障分布ではなく、
  detector回帰を補う制御故障としてのみ利用する。
- 以上から、完全重複以外の警告をblocking、失敗表示、自動削除へ昇格しない。将来のUIは
  登録成功後の「要確認候補」として書籍・code単位に集約し、ページを開ける補助導線に限定する。
  private artifact一式は
  `/opt/pic2pdf-viewer/data/.capture-quality-holdout/b32-20260822-v2/`へ保存し、
  digestと全contact sheetを再照合済みである。
- warningの運用正本は成功jobごとの監査世代とし、同一codeを1候補へ集約する。再撮影または
  新policyでの再監査は旧世代を削除せずsupersedeし、新世代を未確認で開始する。warning 0件の
  新世代も保存することで、旧画像の候補を現在のreaderへ誤って残さない。

### 8.11 シリーズ内画面数外れ値の初期policy（2026-08-22）

- 単冊の最低画面数（novel 50 / comic 10）は明らかな早期終了を拒否できるが、
  最低値を超えた部分的な撮影終了は検出できない。一方、巻ごとの長さには正常差があるため、
  未校正の絶対画面数をblockingにすると誤拒否リスクが高い。
- 初期policyは同じseriesの中でも`comic` / `novel`を分け、登録済みの最新成功jobと
  実行中sessionの完了冊から正の画面数を収集する。3冊以上でのみ中央値を使い、
  0.5倍未満または2倍超を`series_screen_count_outlier_candidate`とする。中央値は
  少数の過去外れ値に引きずられにくく、幅広い比率は短編・特典巻の正常差を誤検知しにくい。
- 候補は登録済み撮影を取り消さず、後続jobも止めない。policy version、ASIN、撮影数、
  reference冊数・範囲・中央値、比率をatomic session stateへ残し、実機の長さ分布と見逃しを
  蓄積してから閾値やblocking化を再判断する。
- 画面数証跡追加後のsession stateはschema v2とし、完了ASINと画面数の完全対応、warningの
  source・撮影数の一致をresume前に検証する。証跡を持たなschema v1は自動補完せず、
  稼働状態の目視確認後に新規sessionとして開始する。

### 8.12 短時間右下通知の追加検出（2026-08-22）

- 既知のOneDrive通知100画面は現行の等間隔反復検出でblockingとなり、通知除去後100画面は
  候補0を再確認した。一方、別書籍の47〜49画面だけに残ったChatGPT / Codex通知は、
  最大32画面の等間隔標本では原理上見逃し得ることを原画像で再確認した。
- 全ページの任意外周を「連続3画面・近似一致」だけで調べる案は、正常holdoutで漫画14件・
  小説6件の繰り返し意匠を誤検出したため不採用とした。実陽性2種に共通し、正常候補と
  分離できた右下隅へ初期責務を限定する。
- 幅1024へ正規化し、異なる連続3画面の右下2タイルについて、各画面の輝度分散8以上、
  Canny edge率1.5%以上、3画面間の最大MAD 6以下を同時に要求する。実陽性2種を検出し、
  対応する正常203画面と封印holdout4冊524画面の計727画面では候補0だった。
- 実陽性は検出器校正に使ったため未調整holdoutの性能値とは混ぜない。初期codeは
  `transient_bottom_right_overlay_candidate`、severityはwarningとし、登録、OCR、画像削除、
  後続jobを止めない。単一・2画面、右下以外、移動する通知の見逃しは残存リスクとして維持する。
- 新agentは`kindle-image-warning-v2` / `kindle-repeated-overlay-v2`を出力し、Linux backendは
  段階更新中のv1組も受信する。新codeをv1へ混入したmanifestはfail closedで拒否する。

## 9. Kindle 更新時の再検証

Kindle 更新後は、いきなり正式撮影せず `diagnose_new_kindle.py` と短い書籍で次を確認する。

1. プロセス名、ウィンドウタイトル、主要 AutomationId が取得できる。
2. ASIN 検索と候補一意性を確認できる。
3. ダウンロード済み・未ダウンロードを区別できる。
4. `ReadingArea`、`TopChrome`、`Footer` の矩形が妥当である。
5. 漫画の `aaOption-Split` と小説の表示設定を選択できる。
6. 最大化後にページ送りしても白画面にならない。
7. 直接先頭遷移が source 別のフッター条件を満たし、小説の `ページ1/N・0%` だけは
   1回戻した後に `Location 1・0%` となる。直接遷移を失敗させても連続ページ送りを
   開始せず `positioning_failed` で停止する。
8. 数ページの試験画像で上下・左右の欠落がない。

AutomationId や矩形取得が変わった場合は固定座標を先に増やさず、診断出力と
UI Automation tree の差分を記録してから controller の locator を更新する。
