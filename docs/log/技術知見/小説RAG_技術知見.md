# 小説 RAG 機能 — 技術知見の蓄積

実機検証・モデル選定・回避策・ベンチマークの記録。設計書（How を書く場所）/ ADR（単発判断）とは別に、運用で蓄えた「経験則」を残す。

最終更新: 2026-08-13（§9.34 Muse Glimmer / Qwen比較と運用境界を追加）

関連:
- 設計: [パイプライン設計](../../design/詳細設計/機能別/小説RAG_パイプライン設計.md) / [検索QA設計](../../design/詳細設計/機能別/小説RAG_検索QA設計.md) / [データ設計](../../design/詳細設計/機能別/小説RAG_データ.md)
- 機能候補: [バックログ](../計画/バックログ.md)
- LLM 切替の経緯: [ADR-0007](../../design/基本設計/ADR/0007_llm-extraction-qwen-adoption.md)
- 共通 LLM モジュール: `D:\61.tool\common\llm`（A-0 リネーム前は `Qwen/`）

---

## 0. ハードウェア前提（重要、再発防止用）

実機は次のスペック。**VRAM を 22GB と誤記していた箇所が複数あったため、2026-05-11 に全面訂正**:

| 項目 | スペック |
|---|---|
| GPU | RTX 5070、**VRAM 12GB（dedicated）** |
| 共有 GPU メモリ（Windows 表示）| 15.6GB | システム RAM の一部、PCIe 経由で低帯域。LLM 推論ではほぼ使えない |
| システム RAM | 32GB |

**重要な事実**:
- `qwen3.6:35b-a3b`（Q4_K_M デフォルト）は **27GB** あり、12GB VRAM に乗り切らない
- Ollama は自動で **61% を CPU 側にオフロード**（システム RAM 使用）
- `ollama ps` で確認できる: `PROCESSOR    61%/39% CPU/GPU`
- 「共有 GPU メモリ 15.6GB」は Windows の仕組み上の表示。Ollama を含む LLM 推論エンジンは速度的に使わない（PCIe 帯域 ~30GB/s vs 専用 VRAM ~700GB/s で 1/20）。CPU offload 部分は通常のシステム RAM + CPU 演算

**性能への影響**（旧 Q4_K_M 27GB / Ollama 時代の値、参考）:
- CPU 計算と CPU↔GPU 通信が速度律速
- 実測生成速度: ~5.4 t/s（ctx=131k）/ ~13.8 t/s（ctx=8k）
- num_ctx を上げても OOM しないのは、足りない分が CPU 側に流れるため（ただし遅くなる）

**現状（2026-05-13、B-12 + B-14 + B-13 段階 C + bge-m3 CPU 化後）**:

採用した高速化策:
- **B-12**: IQ4_XS 量子化（21GB）に切替で CPU offload を 61% → 49% に削減
- **B-14**: 推論エンジンを Ollama → llama-server に切替。`-ncmoe / -ctk q8_0 / -ctv q8_0 / -fa 1` の組合せで応答時間 5× 短縮（[ADR-0009](../../design/基本設計/ADR/0009_llm-backend-llama-server.md)）
- **B-13 段階 C**: `-c 131072` で書籍 1 冊（最大 87k tokens）丸読みに対応、`-ncmoe 28` で VRAM 70% 使用 + 30% ヘッドルームを確保
- **bge-m3 CPU 化（2026-05-13）**: `NOVEL_DB_EMBED_NUM_GPU=0`（既定）で bge-m3 を Ollama の CPU 推論に固定。Full Build 中の VRAM 配分（概算）:

  | プロセス | VRAM |
  |---|---|
  | llama-server（Qwen 35B IQ4_XS, ngl=28） | ~10.0〜10.5 GB |
  | Ollama（bge-m3 CPU 化後）| ~0 GB（CPU で処理） |
  | CUDA バッファ等 | ~0.3 GB |
  | **合計** | **~10.5 GB 以下**（旧 ~11.5 GB → 約 1 GB 解放） |

  ロールバック: `NOVEL_DB_EMBED_NUM_GPU=99` → uvicorn 再起動

結果として、scope=book の 1 冊丸読み（in_tok 78k）で **170 秒 / 9.8 t/s end-to-end**、短文 warm では **tg ~46 t/s** まで改善。詳細は §9.3 採用最適設定を参照。

---

## 1. ローカル LLM の制約と回避策

### 1.1 Qwen3.x thinking モデルの地雷

`qwen3.6:35b-a3b` を含む Qwen3.x 系は thinking モデル。以下の事故が起きる:

| 事故 | 症状 | 対処 |
|---|---|---|
| `response` 空返り | `done_reason='stop'` だが応答 0 字 | `stream=True` 必須 |
| `num_predict` 食い潰し | thinking ブロックで予算消化、本文 0 字 | `think=False` を必ず送る |
| 途中切断 | `done_reason='length'`、回答が中途半端 | `num_predict` を 4096 以上に |

これらは `D:\61.tool\common\llm\local_llm\` の各 Backend 実装で `_build_body()` がデフォルトで `stream=True` / `think=False` を強制している。利用側プロジェクトはこれを書かなくてよい設計。

### 1.2 num_ctx と入力サイズの実機計測（2026-05-10）

書籍 1（本文 113,357 字 ≒ ~73k tokens）を 1-shot 要約させた結果:

| num_ctx | prompt_eval | response | elapsed | 結果 |
|---|---|---|---|---|
| 8,192 | 6,497（入力 10k 字）| 1,262 chars | 91s | ✅ baseline |
| 16,384 | **16,384（切詰）** | 1,146 chars | 127s | ⚠️ 入力切詰 |
| 32,768 | **32,768（切詰）** | 1,010 chars | 171s | ⚠️ 入力切詰 |
| 65,536 | **65,536（切詰）** | 653 chars | 268s | ⚠️ ぎりぎり切詰 |
| **131,072** | **70,964（完全）** | 984 chars | 287s | ✅ 完走 |

**示唆:**
- VRAM 12GB（RTX 5070）+ システム RAM 32GB の環境で `num_ctx=131,072` まで OOM なく動作する（モデルの大部分が CPU 側に offload されるため、KV cache 拡大も主にシステム RAM を使う）
- Qwen のトークナイザは **~1.54〜1.6 chars/token**（日本語）。10,000 字 → 6,497 token、113,357 字 → 70,964 token
- 大きな num_ctx でも、入力サイズが num_ctx 以下なら不利益なし（速度は ctx に比例して伸びるが、応答品質は維持）

**結論:** 一発で全文を読ませたいときは `num_ctx=131,072` を採用する（`summarizer.py` 既定）。

### 1.3 トークン換算の経験則

| 言語 | ~ chars/token (Qwen) |
|---|---|
| 日本語（漢字・カナ混在）| 1.5〜1.6 |
| 英語 | 4〜5 |

簡易計算: 日本語 1 冊 100,000 字 ≒ 60,000〜65,000 tokens。

---

## 2. モデル選定の指針

実機で確認した「タスクと適したモデルの組合せ」。

| タスク種別 | 推奨モデル | 理由 |
|---|---|---|
| 短答型（人物名抽出 / カンマ区切り出力 / 80 字位置説明） | **gemma4:e4b** | 1 件 2〜3 秒。重量モデルは過剰 |
| 構造的要約（書籍俯瞰サマリ）| **qwen3.6-iq4xs**（IQ4_XS、2026-05-11〜）| 1500 字に因果連鎖を組み込める文章生成力 + 12GB VRAM への適合 |
| QA（RAG 質問応答）| **qwen3.6-iq4xs**（IQ4_XS、2026-05-11〜）| 同上 |
| Embedding（多言語意味検索） | **bge-m3**（1024 次元）| 日本語意味距離が `nomic-embed-text` より明確に良い |

**経験則**: 「単純な短答 = 軽量、構造分析 = 重量」。Anthropic の Contextual Retrieval blog も同様の指針（位置説明は Claude Haiku で十分と推奨）。

### 2.1 Qwen 量子化グレードの選定（B-12、2026-05-11 採用）

旧 `qwen3.6:35b-a3b`（Q4_K_M、27GB）は本機 VRAM 12GB に収まり切らず、61% を CPU 側にオフロードしていた。`bartowski/Qwen_Qwen3.6-35B-A3B-GGUF` から **IQ4_XS（21GB）** を取得・登録（`ollama create qwen3.6-iq4xs`）して切替。

**実測効果**: 書籍サマリ 287s → 229s（-20%）、QA 93s → 80s（-14%）、CPU/GPU 比 61/39 → 49/51。詳細は変更履歴 2026-05-11 エントリ参照。

**選定の経緯**:
- 当初は MTP（Multi-Token Prediction）変種で 1.5〜2x 高速化を期待
- 調査の結果、MTP は Aman Gupta の patched llama.cpp 必須 → Ollama では動作しない
- IQ4_XS のみで採用、結果として 14〜20% 速度改善（モデルが GPU により多く乗ったため）

**Modelfile**（`D:\models\qwen3.6-35b-a3b-iq4_xs\Modelfile`）:

```
FROM D:/models/qwen3.6-35b-a3b-iq4_xs/Qwen_Qwen3.6-35B-A3B-IQ4_XS.gguf
TEMPLATE {{ .Prompt }}
RENDERER qwen3.5
PARSER qwen3.5
PARAMETER min_p 0
PARAMETER presence_penalty 1.5
PARAMETER repeat_penalty 1
PARAMETER temperature 1
PARAMETER top_k 20
PARAMETER top_p 0.95
```

`RENDERER qwen3.5` / `PARSER qwen3.5` は Ollama 内部の Qwen 3.5 系プロンプト整形に必須（既存 `qwen3.6:35b-a3b` から継承）。

**ロールバック**: `NOVEL_DB_LLM_MODEL=qwen3.6:35b-a3b` を環境変数で指定。旧モデルは保険として削除しない。

### 2.2 QA の num_ctx・top_k 段階拡大（B-13 段階 A → B → C、2026-05-11 採用）

`llm.py:LLM_OPTIONS` の `num_ctx=8192` は PoC 当時の値だったが、B-5（書籍俯瞰サマリ）/ B-8（サマリ検索インデックス）追加で **scope=all の QA プロンプトが ~25k 字 / ~15k tokens** に膨らんでいた。実機ベンチマークで `prompt_eval_count` が **num_ctx 上限の 8,192 にぴったり張り付いている** ことが判明 = **入力時点で切り詰め発生**。

これを契機に段階的に context を拡大した:

| 段階 | num_ctx | top_k | max_per_book | scope=book 挙動 | llama-server `-c` / `-ncmoe` |
|---|---:|---:|---:|---|---|
| PoC | 8,192 | 16 | 2 | hybrid_search | (Ollama 時代) |
| A | 16,384 | 32 | 2 | hybrid_search | -c 18432 / -ncmoe 16 |
| B | 32,768 | 64 | 5 | hybrid_search | -c 36864 / -ncmoe 18 |
| **C（既定）** | **32,768**（scope=all/series）<br/>**131,072**（scope=book） | **64** | **5** | **全 page 読み**（`NOVEL_DB_QA_FULL_BOOK_MODE`） | **-c 131072 / -ncmoe 28** |

**段階 A の効果実測**:

| 設定 | scope=all のプロンプト | 実 prompt_eval | 状況 |
|---|---|---|---|
| 旧（num_ctx=8192）| 16,676〜16,809 chars | **8,192 tok**（切詰）| 後半が捨てられ、Qwen が全文を見ていない |
| 新（num_ctx=16384、段階 A）| 16,676〜16,809 chars | **10,833〜10,915 tok**（完全）| 全文を Qwen が受け取れる |

応答時間の変動は想定 +20〜30% に対し、実測 -3% 〜 +12% でほぼ誤差範囲。切り詰め解消なのに遅くならない理由は、prompt processing が 680 t/s と高速 + そもそも切り詰め前の 8192 tok でも処理時間は近い（無駄に処理してた）から。

**段階 B（B-14 後）**: B-14 の llama-server 切替で応答が 5× 速くなった分の余裕を使って `num_ctx=32768` / `top_k=64` / `max_per_book=5` に。同書籍に集中する質問への深さが向上。

**段階 C（品質優先で本採用）**: scope=book で `load_all_pages_of_book()` が hybrid_search を bypass し、書籍の全 page を page_no 順で LLM に投げる。実測で 11 巻 = 87k tokens の本に対し 78k tokens 入力 / 170 秒 / 9.8 t/s end-to-end。本文 9 箇所以上から具体的セリフ引用付き分析が得られる（段階 B では 16 page = 2.8k tokens / 37 秒で浅め）。ロールバックは `NOVEL_DB_QA_FULL_BOOK_MODE=false` の env で段階 B 相当の hybrid_search に戻る。

**注意**: `num_ctx=8192` 想定で動いていた頃の質問履歴（`qa_history.options_json`）と新設定後の履歴は同列に比較できない（前者は context が一部欠落している可能性）。

### 2.3 軽量モデル採用で時間がどう変わるか

| 用途 | qwen3.6:35b-a3b | gemma4:e4b | 倍率 |
|---|---|---|---|
| 1 チャンクの位置説明（B-9）| ~30 秒 | **~2.8 秒** | **10.7×** |
| 1 ページの主要登場人物抽出 | ~10 秒 | ~2〜3 秒 | 4× |
| 1 冊の俯瞰要約（1-shot）| ~5 分 | ❌ ctx 不足 | — |

**判断:** thinking モデルの精度が不要な「定型タスク」では gemma4:e4b を積極採用。RAG QA や構造的要約は Qwen 一択。

---

## 3. 品質改善のレイヤー構造

検索ヒットの S/N 改善と回答品質を、独立した層で重ねて積んでいる。

```
質問
  ↓
[Query Expansion]                  ← B-11（未着手、応答時間 +30s）
  ↓
[ハイブリッド検索]
  ├ FTS5 OR 検索（pages_fts）
  ├ ベクトル検索（LanceDB chunks）  ← B-9 で contextual_text を含めて再 embedding（Phase 62 で chunks_vec から移行）
  └ 書籍サマリベクトル検索（LanceDB summaries） ← B-8（Phase 62 で book_summaries_vec から移行）
  ↓
[RRF 融合 + フィルタ]
  ├ min_chars（薄いページ除外）
  ├ body_page_margin（前付け・後付け除外）
  └ max_per_book（書籍偏り抑制）
  ↓
[プロンプト構築]
  ├ 書籍俯瞰サマリブロック        ← B-5 / B-8 で関連書籍を充実
  ├ 主要登場人物ヒント            ← character_extractor
  └ 引用ルール / 構造的分析要請
  ↓
[qwen3.6-iq4xs（num_ctx=16384、B-12 + B-13 段階 A、2026-05-11）]
  ↓
SSE ストリーミング応答
```

**直交関係:** どの層も独立に効くため、組合せると累乗的に改善する。

| 層 | 効くケース | 効果実測 |
|---|---|---|
| min_chars / body_page_margin | 章扉 / 目次 / あとがきが混入 | ノイズ激減 |
| max_per_book | scope=all で特定冊に偏る | 書籍均等化 |
| 主要登場人物ヒント | キャラ帰属の誤統合 | ハルシネ率 ~18% に抑制 |
| 書籍俯瞰サマリ（プロンプト）| 概括質問の浅さ | 因果連鎖を含む構造的回答に |
| サマリ検索インデックス | ページに引っかからない書籍も拾う | scope=all の recall 改善 |
| Contextual Retrieval | 検索の precision/recall | Anthropic 計測で 35〜49% 改善 |

---

## 4. ベンチマーク結果

### 4.1 各処理の所要時間（実機）

| 処理 | 単位 | 単価 | 全件（11 冊 / 2,230 chunks）|
|---|---|---|---|
| OCR テキスト抽出（PyMuPDF）| 1 冊 | < 1 分 | 数分 |
| チャンキング + bge-m3 embedding | 1 冊 | < 5 分 | < 30 分 |
| 主要登場人物抽出（gemma4:e4b）| 1 ページ | 1〜3 秒 | 30〜60 分 |
| 書籍俯瞰サマリ（qwen3.6:35b-a3b 1-shot）| 1 冊 | 4〜6 分 | 約 40 分 |
| サマリ vec embedding（bge-m3）| 1 冊 | 数秒 | 数十秒 |
| チャンクコンテキスト生成（gemma4:e4b）| 1 チャンク | 2.7〜2.8 秒 | **実測 98.6 分**（2,230 チャンク、失敗 0 件）|
| チャンク再 embedding（bge-m3 バッチ 16）| 1 冊 ~200 chunks | ~30 秒 | 数分 |

### 4.2 ハルシネーション率

書籍 1 巻のサマリ生成で実機検証:

| 方式 | ハルシネーション率 | 備考 |
|---|---|---|
| map-reduce 経路（旧、num_ctx=16384）| **0%**（10 主張を検証）| 個別シーン中心、因果連鎖は弱い |
| 1-shot 経路（新、num_ctx=131072）| **0%**（17 主張を検証）| 因果連鎖・派閥構造を捕捉、構造把握深い |
| scope=all QA（B-5/8/9/11/12/13A 全採用後）| **0%**（13 主張中 12 完全一致 + 1 言い換え）| 「シリーズ全体のテーマ」質問。逐語一致 92% |

**観察:** 1-shot のほうが文章は短くなる傾向（984 vs 1233 chars）が、密度・構造把握は深い。両方とも完全に本文に依拠している。

#### ベースライン比較（retrieval なしとの差分、2026-05-11）

「シリーズ全体のテーマ」質問について、retrieval なし（Claude の事前知識のみ）と RAG QA を比較:

| 観点 | retrieval なし | RAG QA |
|---|---|---|
| 粒度 | 「中世風 / 政治劇 / 王女主人公」程度の上位カテゴリ抽象論 | 3 軸（王権の脱構築 / 円卓の再生 / 思想の越境継承）× 巻横断引用 |
| 具体性 | 主要キャラ 1 人（レティーツィア）のみ | 5〜6 人 + 各巻象徴事件を実引用 |
| 検証可能性 | 「推察される」「であろう」 | 巻番号 + ページ番号 + 原文引用 |
| ハルシネーション率 | 検証されていない断定をすれば 100% 近い | 0% |
| 書ける文字数 | ~300 字（汎用テンプレ） | 2,000 字超の構造化テキスト |

→ retrieval なしでは具体的なシリーズ分析は物理的に不可能。RAG の存在価値が定量的に確認できた。

### 4.3 retrieval 精度（B-9 パイロット後の実測）

「父王が次期女王を発表する場面」(scope=book で 1 巻) のクエリで:

```
top 1: p11   score=0.0318  ← 正解（「父親は…奇策を講じた…レティーツィアに継がせる」）
top 2: p100  score=0.0288
top 3: p 28  score=0.0276
top 4: p  8  score=0.0254
top 5: p  9  score=0.0253
```

contextual_text が「該当場面の位置説明」を embedding に含むため、抽象的なクエリでも該当ページが top に来る。

### 4.4 B-9 ctx の弱点パターン（2026-05-11 発見）

データ層品質チェックで、ctx 生成（gemma4:e4b）が **本文の重要な固有名詞・引用句を含まず、物語的解釈に流れる**事例を確認した。

**実例: Vol 9 p113 chunk 1**

本文に「王族は国の金で生きている。ならば王族は国のために尽くす義務がある」「幼い頃にベルナードが教えたことはソレスに深く染み」を含むが、生成された ctx は:

> "ソルヴェール国、ソレスとレティの対話場面で、ソレスが自らの人生と夢について内省し、自由な未来を選ぶ決意を固める場面。"

「ベルナード」「教え」「義務」「王族」が一切含まれない。

結果として、クエリ「ベルナードの教え 国民への義務」のベクトル検索で本来 1〜2 位に来るべき chunk が **6 位** (dist=0.6041) まで沈降する。直接引用に近いクエリ「王族は国のために尽くす義務 ベルナード」でも 9 位 (dist=0.5549)。

**同型: 表紙・タイトルページの ctx 捏造**

`char_count<300` のページ（表紙・著作権・タイトル）に対し、本文がほぼ空でも ctx 生成が呼ばれた結果、物語の本筋を捏造する事例が頻発（例: Vol 1 p1 タイトルカバーの ctx が「レティがデュークに真の騎士としての覚悟を確信させる場面」）。これらは `min_chars` フィルタで検索対象からは除外されているため実害は出ていないが、embedding 空間にノイズとして残る。

**救済される理由**

それでも前回の RAG QA で p113 引用が正しく出たのは、top_k=32（B-13 段階 A）+ Query Expansion（B-11）+ summary 検索（B-8）の多層防御で 6 位まで含めて拾えるため。最終 QA 出力は 92% 逐語一致 + 0% ハルシネーション。

**改善 TODO**（GPU 空き待ち、`pending_tasks.md` 高優先）

1. プロンプトに「ctx には本文に登場する重要な固有名詞・特徴フレーズを必ず含めてください」追記
2. `char_count<300` / body_page_margin 範囲のチャンクは ctx 生成 skip
3. 全 2,230 chunk 再生成（gemma4:e4b で ~100 分の見込み）

### 4.5 FTS5 vs Vector の単体精度（2026-05-11）

ハイブリッド検索のうち FTS5 単独は **抽象的な助詞混じりクエリで 0 ヒット** を起こす（`tokenize='trigram'` 設定 + `build_fts5_or_query` で min_len=2 トークン抽出の組み合わせが原因）:

| クエリ | FTS5 | Vector | Hybrid |
|---|---|---|---|
| ベルナードの教え 国民への義務 | **0 hits** | 5 hits | 5 hits ← Vector が救う |
| 戦争を起こさない国 王の誓い | **0 hits** | 5 hits | 5 hits ← 同上 |
| マティアス殺害事件の真相 | **0 hits** | 5 hits | 5 hits ← 同上 |
| グイード 取り替えっ子 出生 | 5 hits | 5 hits | 5 hits |
| おこぼれ姫 上等 国一番 | 5 hits | 5 hits | 5 hits |

具体名詞があれば FTS5 もちゃんと働く、抽象 + 助詞混じりだとマッチしないことがある — ハイブリッド検索の RRF で Vector 側が常にカバーするので最終結果は問題なし。Vector 側の品質改善（§4.4 の B-9 ctx 改良）が ROI 最大。

---

## 5. クラウド LLM 比較（採用しなかった分析）

DeepSeek V3.x 級のクラウド API を使う場合の試算（2026-05-10 時点）:

### コスト

| 用途 | 入力 tokens | 出力 tokens | コスト |
|---|---|---|---|
| 書籍サマリ全 11 冊 | 627k | 22k | **~¥30〜50** |
| ページ単位キャラ抽出（1,359 ページ）| 1M | 20k | …2060 tokens truncated…ity 12.0, VMM: yes, VRAM: 12226 MiB`

#### Phase 2: llama-bench 投稿設定再現

```
-ncmoe 18 -t 9 -ctk q8_0 -ctv q8_0 -fa 1 -p 512 -n 128
```

結果: **pp512 = 766.6 t/s / tg128 = 76.8 t/s**（投稿 46.8 を 64% 超過、本機 GPU 世代差で説明可能）

#### Phase 2a: -ncmoe スイープ（最適点探索）

| ncmoe | pp512 t/s | tg128 t/s |
|---:|---:|---:|
| 14 | 166 | 21.5 ← GPU 過密で大幅低下 |
| **16** | **965** | **81.7** |
| 18 | 919 | 77.3 |
| 20 | 768 | 72.5 |
| 22 | 724 | 69.0 |

→ **本機の最適は `-ncmoe 16`**（投稿の 18 より 1 ステップ GPU 寄り）。

#### Phase 2b: depth スイープ（実運用シナリオ近似）

ncmoe=16 固定、`-d` で KV cache 既存 token 数を変動:

| depth | pp512 t/s | tg128 t/s |
|---:|---:|---:|
| 0 | 987 | 81.5 |
| 2,048 | 1,109 | 82.2 |
| 4,096 | 1,082 | 81.3 |
| 8,192 | 1,059 | 79.6 |
| **16,384** | **749** | **76.3** |

→ scope=all 想定の **depth=16k でも tg 76 t/s 維持**（KV cache q8_0 量子化の効果）。

#### Phase 4: E2E 比較（Ollama vs llama-server）

llama-server を `:11435` に起動（Ollama `:11434` と共存、`-np 1` 重要）。novel_db の PROMPT_TEMPLATE を模した 3 種類で実走。

**最初の試行で問題判明**: llama-server デフォルトの `n_parallel=4` で KV cache が 4 分割されて性能 1/3 に低下（tg 23 t/s）。`-np 1` を指定して再起動で解決。

#### Phase 4b: thinking 抑制（chat_template_kwargs）

Phase 4 で「llama-server の `/completion` 直叩きだと Qwen3.x thinking モデルが `<think>` ブロックで `num_predict` を食い潰す」事象を確認（B_mid で 256 tok を全部 thinking 消費、実回答ゼロ）。

解決策: `/v1/chat/completions` + `chat_template_kwargs: {enable_thinking: false}` + llama-server 側に `--jinja` フラグ。

最終結果（同一プロンプト・同一モデル・thinking 抑制済み）:

| ケース | Ollama tg | llama-server tg | tg 倍率 | Ollama 応答 | llama-server 応答 | 時間倍率 |
|---|---:|---:|---:|---:|---:|---:|
| A_short (~120 tok in) | 17.1 | **80.7** | **4.72×** | 3.75 s | **0.71 s** | **5.28×** |
| B_mid (~2k tok in) | 14.6 | **80.5** | **5.51×** | 21.25 s | **3.55 s** | **5.99×** |
| C_long (~8k tok in) | 13.4 | **78.0** | **5.82×** | 24.22 s | **14.03 s** | **1.73×★** |

★ C_long の時間倍率が小さいのは Ollama 側 warm-up で KV cache hit していたため。cold start 比なら llama-server 圧勝。

**応答品質**: 同等。Ollama 416 chars vs llama-server 147 chars の差は冗長性のみで論旨は一致（[backend/tmp_bench_phase4_*.json](../../backend/) 参照）。

### 9.3 採用最適設定

**B-14 採用時（2026-05-11、num_ctx=16384 想定）**:

```
-ncmoe 16 -c 18432   # VRAM 11.8 / 12.2 GiB (97%)
```

**B-13 段階 B（2026-05-11、num_ctx=32768、過去設定）**:

```
-ncmoe 18 -c 36864   # VRAM 11.7 / 12.2 GiB (95.8%)
```

**B-13 段階 C 本採用（2026-05-11、num_ctx=131072、現在の canonical 設定）**:

```
llama-server.exe ^
  -m D:\models\qwen3.6-35b-a3b-iq4_xs\Qwen_Qwen3.6-35B-A3B-IQ4_XS.gguf ^
  -ncmoe 28 -t 9 ^
  -ctk q8_0 -ctv q8_0 ^
  -fa 1 ^
  -ngl 99 ^
  -c 131072 ^
  -np 1 ^
  --jinja ^
  --port 11435 --host 127.0.0.1
```

起動 bat: `D:\61.tool\common\llama.cpp\b9101\start-qwen-server.bat`（タスクスケジューラ `llama-server-qwen` で自動起動）。

**ncmoe スイープ結果（2026-05-11、bench warm KV measure）**:

| -ncmoe | VRAM | A_short tg* | B_mid tg* | C_long tg* |
|---:|---:|---:|---:|---:|
| 32 | 56% (6.9 GB) | 21.4 | 55.0 | 59.4 |
| 30 | 63% (7.7 GB) | 42.9 | 53.3 | 54.2 |
| **28（現行・本番）** | **70% (8.5 GB)** | **46.3** | **55.6** | **56.2** |
| 26 | 未計測 | 47.4 | 59.6 | 56.0 |
| **24（Ollama 非同時稼働時推奨）** | **83% (10.1 GB)** | **50.2** | **60.8** | **59.7** |

**2026-05-12 追加 — ncmoe 26 / 24 実測（B-13 最適化検証）**:
- ncmoe 24 は 28 比 **A_short +8.4% / B_mid +9.4% / C_long +6.2%** の改善
- 検証スクリプト: `D:\61.tool\common\llama.cpp\b9101\start-qwen-server-ncmoe26.bat` / `start-qwen-server-ncmoe24.bat`

**2026-05-12 追加 — ncmoe 24 VRAM 実測（B-13 採用判断）**:

| -ncmoe | VRAM 使用 | VRAM 空き | Gemma 同時稼働 |
|---:|---:|---:|---|
| **28（本番）** | 8,488 MiB (69%) | 3,739 MiB | **可**（余裕あり）|
| **24** | 10,124 MiB (83%) | **1,820 MiB** | **不可**（空き不足）|

- ncmoe 24 の空き 1,820 MiB では gemma4:e4b（Ollama）の VRAM を確保できない
- Query Expansion（B-11）は gemma4:e4b を常時使用するため、通常運用では ncmoe 28 が必須
- **ncmoe 24 推奨シナリオ**: Ollama を停止した状態での単独 QA（夜間バッチ後の手動操作など）
- **結論: 本番設定は ncmoe 28 を維持。判断完了。**

**書籍丸読み実測（2026-05-11、87k tokens の書籍 1 冊に深い質問、-ncmoe 28）**:
- VRAM: 8.5 / 12.2 GiB（70% 使用、ヘッドルーム 3.7 GiB）
- 生成速度: end-to-end 9.8 t/s（pp が depth=78k で支配的）
- 応答時間: 170 秒

### 9.4 検証で得られた重要な地雷リスト

1. **`-np 1` 必須**: llama-server のデフォルト `n_parallel=4` で KV cache が 4 分割され性能 1/3 に低下
2. **`-c <n>` は llama-bench にはない**: llama-server だけのオプション。llama-bench は GGUF のデフォルト ctx を使う
3. **thinking 抑制は `chat_template_kwargs` + `--jinja` の組合せが必須**: 片方欠けると thinking が出続ける
4. **`-ncmoe` の最適値は GPU と RAM 速度に依存**: 投稿 (3060) は 18、本機 (5070) は 16
5. **CUDA バージョン**: 本機 CUDA 13.1 ドライバには公式 `llama-b9101-bin-win-cuda-13.1-x64` が完全フィット。CUDA 12.x ビルドを使うとフォワード互換で動くが微妙な性能差が出る可能性

### 9.5 検証スクリプト

| ファイル | 役割 |
|---|---|
| `tmp_bench_qwen_phase0.py` | Phase 0 ベースライン取得（Ollama 経由） |
| `tmp_bench_phase4_compare.py` | Phase 4 / 4b E2E 比較（`--backend ollama` / `--backend llama` 切替） |
| `tmp_bench_phase0_result.txt` | Phase 0 ログ |
| `tmp_bench_phase2_*.txt` | Phase 2 / 2a / 2b の llama-bench ログ |
| `backend/tmp_bench_phase4_*.json` | Phase 4 / 4b の比較結果 JSON |

採用実装後、これらは `backend/scripts/bench_llm_backend.py` 等に整理して残す予定。

### 9.6 B-14b: ngram Speculative Decoding 採用（2026-05-12）

**目的**: decode フェーズの高速化。B-14（llama-server 切替）の延長上で「MTP や speculative decoding で追加の速度改善ができないか」の評価。

#### 調査: 真の MTP（Multi-Token Prediction）の現状

B-12 メモに「Aman Gupta の patched llama.cpp + MTP GGUF」と記録していた手法を再調査した。

- GitHub で確認できた最も近い PR: **#22673 by am17an** (`llama + spec: MTP Support`)
  - Qwen3.6-35B-A3B を明示的にテスト済み
  - フラグ: `--spec-draft-model mtp.gguf --spec-type mtp`
  - 必要なもの: MTP テンソル入り専用 GGUF（bartowski 標準 IQ4_XS には含まれない）
  - **ステータス: Open（未マージ、2026-05-12 時点）**
- 現行 b9101 にはこの MTP 実装は含まれない

→ 本物の MTP はビルドから要するため、PR マージまで保留。

#### 採用: ngram-cache Speculative Decoding（b9101 既存機能）

b9101 の `llama-server.exe` に `--spec-type` フラグが既存実装として存在することを発見:

```
--spec-type [none|ngram-cache|ngram-simple|ngram-map-k|ngram-map-k4v|ngram-mod]
```

`ngram-cache` は KV cache 内の n-gram マッチを使って draft トークンを提案し、main モデルで並列 verify する仕組み。別モデル不要、GGUF 変更不要、API 透過的。

**変更内容**: `start-qwen-server.bat` 他 2 ファイルに `--spec-type ngram-cache` を追記。

```bat
llama-server.exe ^
  ... (既存フラグ) ... ^
  --spec-type ngram-cache ^   ← B-14b 追加
  --port 11435 --host 127.0.0.1
```

#### 速度改善の適用範囲と限界

| フェーズ | 現況 | ngram-cache の効果 |
|---|---|---|
| Prefill（書籍丸読み 78k tok） | ~170 秒 | **効かない**（prefill は並列化済み、spec dec は decode のみ） |
| Decode warm KV（2問目以降） | 46 t/s | **5〜20% 改善期待**（日本語小説のリピートパターン次第） |
| Decode cold（サーバ再起動後初回） | 46 t/s（短文のみ） | 同上（prefill 部分は不変） |

**170 秒短縮の本命は `--slot-save-path`（B-14c）**: サーバ再起動後も書籍 KV キャッシュをディスク永続化すれば初回 prefill をスキップできる。B-14b 完了後の次のタスクとして分類。

#### 実測結果（2026-05-12）

`--metrics` を追加して5リクエスト実行後に `/metrics` を確認。

| 指標 | 値 |
|---|---|
| tokens_predicted_total | 572 |
| n_decode_total | 571 |
| **tokens / decode** | **1.002**（≈ 1.0） |
| predicted_tokens_seconds | 47.5 t/s |
| baseline（spec dec なし） | 46.3 t/s |

**tokens/decode ≈ 1.0 = ngram ヒット率ほぼ 0%**。日本語小説の自由回答は n-gram 反復率が低く、ドラフトがほぼ全てリジェクトされていた。効果ゼロでオーバーヘッドだけ払う状態のため `--spec-type ngram-cache` は **削除**（2026-05-12 revert）。

`--metrics` フラグは `/metrics` 監視用として維持。

**結論**: ngram Speculative Decoding は日本語散文生成に対して無効。コード補完・定型文向けの手法。本来の MTP（PR #22673 マージ待ち）か、scope=book 初回 prefill の根本改善（B-14c は却下済み）が次の速度改善候補。

---

### 9.7 Gemma4 MTP（contextualizer Step5）実機検証（2026-05-13）

**目的**: Step5（contextualizer、B-9）の速度改善。`google/gemma-4-E4B` の MTP が E4B 長文で 2.10× を達成したとの報告（DGX Spark、vLLM 使用）を受けて Ollama での適用可否を検証。

#### 検証対象

| 項目 | 内容 |
|---|---|
| 現行モデル | `gemma4:e4b`（Ollama 0.23.2、GGUF: `4c27e0f5b5ad`、Q4_K_M、9.6 GB）|
| MTP 候補 | `bjoernb/gemma4-e4b-fast`（Ollama Hub より pull）|
| プロンプト | contextualizer の `_CONTEXT_PROMPT`（514 tok）|
| 設定 | `temperature=0.2`, `repeat_penalty=1.15`, `num_predict=256`, `num_ctx=8192`, `think=False`|

#### 実測結果（各 3 回中央値）

| モデル | 生成速度 | 生成トークン数 | 備考 |
|---|---|---|---|
| `gemma4:e4b` | **31.3 t/s** | ~71 tok | |
| `bjoernb/gemma4-e4b-fast` | **31.3 t/s** | ~71 tok | 速度差ゼロ |

出力品質は両モデルとも同等（固有名詞・場面種別を正しく含む位置説明を生成）。

#### 原因分析

`ollama show` で確認したところ、`bjoernb/gemma4-e4b-fast` は **同一 GGUF ウェイト**（`sha256-4c27e0f5b5ad`）を使っており、System プロンプト違いのラッパーに過ぎなかった。`RENDERER gemma4` / `PARSER gemma4` フラグは Thinking トークンのフィルタリング用で MTP 投機デコードとは無関係。

**Ollama 0.23.2 時点では Gemma4 MTP は非対応**。

#### 正規 MTP の条件（今後の参考）

DGX Spark 記事（classmethod, 2026-05）によると:

- フレームワーク: **vLLM 必須**
- モデル: `google/gemma-4-E4B-it-assistant`（MTP ドラフトヘッド込み HF モデル）
- 仕組み: 本体の埋め込み層を共有する 4 層の軽量ドラフターで投機デコード
- E4B 256 tok: **2.10× 実測**（短文 8 tok では効果ほぼなし）

→ 本番適用には `local_llm` に vLLM 対応 Backend を追加し、Ollama → vLLM に切替が必要（中規模の作業）。

---

### 9.8 Gemma4 MTP 再確認・対応見送り（2026-06-07）

**背景**: §9.7 から約 1 ヶ月後、Ollama 大幅更新（0.23.2 → 0.30.6）・モデル変更（`gemma4:e4b` → `gemma4:12b`）を受けて状況を再確認。

#### 環境変化のまとめ

| 項目 | §9.7 時点（2026-05-13） | 再確認時（2026-06-07） |
|---|---|---|
| Ollama バージョン | 0.23.2 | **0.30.6** |
| contextualizer モデル | `gemma4:e4b`（MoE, 9.6 GB） | **`gemma4:12b`**（Dense 12B, 7.6 GB） |
| Ollama の MTP 対応 | 非対応 | **非対応**（Capabilities に未掲載） |
| llama.cpp バージョン | b9101（2026-05-10） | 同じ b9101 |

#### llama-server 経由での Gemma4 MTP 可否

- llama.cpp の MTP PR（§9.6 記録の `#22673`）は Qwen3 向け専用。Gemma4 向け MTP の llama.cpp PR は未確認
- Gemma4:12b の GGUF（Ollama blob `sha256-1278394b...`, 6.9 GB）は `llama-server -m` に渡して動作させること自体は技術的に可能
- ただし標準 Q4_K_M ウェイトには MTP ドラフトヘッドテンソルが含まれていない可能性が高い（§9.7 の `e4b-fast` がラッパーに過ぎなかったのと同じ構造）
- `-fa 1 / -ctk q8_0 / -ctv q8_0` によるチューニングで微改善は期待できるが、MTP 効果は得られない

#### 対応見送りの判断

- Step 5（contextualizer）は `num_predict=256` と出力が短く、MTP の恩恵が最も薄い用途
- 現運用で速度に不満なし
- 本物の MTP（vLLM + MTP ドラフトヘッド入り HF モデル）は中規模の作業が必要
- **→ 対応見送り**。速度面で実運用上の問題が生じた場合に改めて検討する

---

### 9.9 Qwen長文serverの起動停滞と`--no-warmup`回避（2026-07-28）

茉莉花官吏伝18巻の`full_build`前に、タスクスケジューラ
`llama-server-qwen`からcanonical設定を起動したところ、`/health`が503のまま続き、
GPU使用量が162 MiBから増えず、モデル層ロード前で停滞した。原因切り分けのため、
同じ引数を標準エラー記録付きで直接起動すると17.50 GiBのGGUFロード、
CUDA層6.24 GiB、131,072 contextのKV cache 1.36 GiB確保までは完了したが、
空入力ウォームアップで再び進行が止まった。

同じモデル、`-ncmoe 28 -ctk q8_0 -ctv q8_0 -fa 1 -ngl 99 -c 131072 -np 1
--jinja --metrics`を維持し、`--no-warmup`だけを追加して再起動したところ、
`/health={"status":"ok"}`、GPU使用量8.45 GiBとなった。実APIへ
`Reply with only OK.`を送った初回生成も23.7秒で`OK`を返し、
WindowsからLinux本番へのreverse SSH経由でもhealthを確認した。

運用上は次の順で判定する。

1. 起動中の503は直ちに失敗とみなさず、ログの`load_tensors`進捗とGPU使用量を確認する。
2. 数分待ってもGPU使用量が増えない、または
   `warming up the model with an empty run`から進まない場合だけ、対象PIDを1回再起動する。
3. 回避時は他の推論設定を変えず`--no-warmup`を追加し、healthだけでなく短い実生成も通す。
4. `--no-warmup`は空入力の事前実行を省くだけで、モデル重み・量子化・context長・
   生成プロンプトを変えない。ただしb9101またはGPUドライバ側の根本原因は未確定なので、
   常時設定化は再発実測後に判断する。

---

### 9.10 茉莉花官吏伝18巻の派生データ生成QA（2026-07-28）

小説1〜18巻を対象に`full_build`を完了し、その後に巻別サマリ・人物辞典・
人物関係をページ根拠付きで再生成した。最終的にPic2PDFViewerへ登録した件数は、
サマリ18件、巻別人物169件、人物関係68件である。APIで18冊すべての索引状態、
巻別サマリ、人物一覧・人物詳細、シリーズ人物グラフを取得できることを確認した。

ローカルLLMの生成結果だけでは次のノイズが残った。

- 匿名役職を固有人物として扱う。
- OCR揺れや「第一皇子守伸」のような肩書付き表記を別人物として扱う。
- 当該巻の索引本文に名前がない人物を、シリーズ知識から補って出力する。
- 人物辞典に存在しない端点を持つ関係を作る。
- 連続ページ全体を根拠として列挙し、検証箇所を絞れない。
- LLMの自己評価が`high`でも、事件順・因果・人物の変化を取り違える。

今回はCodex補助QAで、索引本文への固有名完全一致、既知OCR揺れの正規化、
匿名人物の除外、関係端点の存在検査、根拠ページの代表5画面以内への圧縮を行った。
さらに誤読リスクの高かった4・6・12・18巻は該当画像と本文を直接確認して補正した。
派生データ全体の信頼度は`medium`とし、探索の起点には使えるが重要判断では画像確認を
必要とする。LLMの自己評価値は品質合格の根拠にしない。

この実績から、今後の自動生成では少なくとも次を機械検査する。

1. 人物名が当該巻の索引本文に存在する。
2. 肩書付き表記と短縮名を正規名へ統合する。
3. 関係の両端が同じ巻の人物辞典に存在する。
4. 根拠ページが索引対象であり、別作品試し読みや広告ではない。
5. 再生成前後の件数・名前・根拠ページ差分を保存し、Codex補助QA対象を絞る。

シリーズ横断の時系列・人物名統合・事件・伏線・テーマ・根拠ページは
[茉莉花官吏伝 シリーズ横断分析](茉莉花官吏伝_シリーズ横断分析.md)へ記録した。

---

### 9.11 可読性優先生成の10巻パイロットと復元判断（2026-07-28）

『茉莉花官吏伝 十　中原の鹿を逐わず』で、公開版をJSONスナップショットへ退避してから
新しい事実抽出・個別執筆・編集校正パイプラインを試した。最初の試行では、書籍事実と
人物事実を同じ応答で生成したため、書籍事実6266字で出力上限へ近づき、人物マーカーが
0件となって一括確定前に停止した。このため、書籍事実の抽出と人物事実への再編を
2回のLLM呼び出しへ分離した。再試行では本文75ページから書籍事実7918字、人物6名、
要約1526字を生成し、文章形状の機械品質ゲートは通過した。

ただし、旧版との差分をCodex補助QAした結果、次の理由で新版を不採用とした。

- 完成要約に「仁耀が黒の皇帝を殺して戦争を誘発しようとする」という、
  入力したページ根拠付き事実表では確認できない行動が追加された。
- `皓茉莉花`と`茉莉花`、`芳子星`と`子星`、`封大虎`と`冬虎皇子`を
  同一人物として扱えず、人物の削除・追加に見える正規名の揺れが発生した。
- 旧版にいた`影傑`、`望来現`、`鉦春雪`、`黎天河`が、根拠ある除外理由を示さず
  人物集合から失われた。

監査CLIの`restore`により、公開版の要約446字・人物10名と生成日時を
単一SQLiteトランザクションで復元し、サマリembeddingも再indexした。
したがって、この試行による不採用データは現在の公開版へ残っていない。

この実測から、反復、空出力、生成マーカー、人物名明示のような文章形状検査は必要だが、
事実性と人物同一性の合格判定には不十分と分かった。全冊の夜間再生成を再開する前に、
公開版またはシリーズ人物台帳を使った別名・正規名統合、完成文の主張と事実表の照合、
正規化後の人物集合に対する削除回帰検査が必要である。それまでは
「スナップショット → 再生成 → 全文差分 → Codex補助QA → 採否」を必須運用とする。

---


### 9.12 OCR品質改善の知見

OCR候補の列欠落、46冊QA、入力途中終了、補正文採用元、B-35複合ゲート、
第三OCR、PARSeq追加学習、holdout、固有名詞補正、Codex委任境界の実測記録は、
責務分離のため [OCR品質改善 技術知見](OCR品質改善_技術知見.md) へ移した。

RAG側はQA承認済みの `pages.full_text` を入力とし、OCR候補選択やground truth評価を
再実装しない。OCR未承認・非索引ページを検索、要約、人物抽出へ混入させない。

---

### 9.20 B-36では要約検証より前の事実表も意味照合が必要（2026-08-02）

茉莉花官吏伝10巻の本番コピーを隔離し、Qwen3.6 35B-A3Bを131k context・100% GPUで直列実行した。
事実抽出75ページは4ブロックのチェックポイントとして再利用でき、詳細版の機械根拠検証は合格した。
短縮版では検索候補と引用ページの割当、ページ境界、証拠上限による偽陰性が順に判明したため、
事実表候補の優先、全文提示済みページを引用境界とする判定、前後1ページ補完、最大64ページ・
90,000文字の証拠本文へ修正した。直前の短縮版実応答は56ページ・80,419文字の集合で9/9主張が
再生検証に合格した。

それでもCodex差分QAでは不採用となった。候補は仁耀の結末を「牢へ戻る」としたが、本文75ページの
最終合意は、珀陽の依頼に応じて「最長10年間逃げ続け、必要時に淑太上皇后の手引きを証言する」
ことである。構造化事実表の1件が「十年間逃げ続けてほしい」という依頼と「牢に戻ることを決意」
という矛盾した結果を同居させ、要約執筆と同じ35Bによる検証も後者を支持してしまった。

この事例から、ページ番号が入力範囲内にあることは事実内容の正しさを保証しない。同系統モデルで
事実表から執筆し、その事実表を再び参照して要約を検証すると誤りが相関する。全冊自動公開の前に、
各事実を記載ページ本文へ独立に意味照合し、途中状態と最終結果の衝突を検出する必要がある。
それまでは旧版・新版の全文差分とCodex補助QAを公開条件として残し、今回候補と本番データは
置換しない。

また再試行の1回では編集済み詳細版が87文へ膨らみ、64主張上限で安全停止した。確率的な冗長化を
後から診断できるよう、上限超過時も候補全文と分解済み主張を監査保存する。35B単独は64GB Macで
安定したが、長文検証が所要時間の支配項であり、夜間直列・巻単位チェックポイント再開を前提とする。

---

### 9.21 Gemma4は日本語事実検証の第二意見として有望だが、校正確定には使わない（2026-08-02）

B-36で確定した誤りを固定ケースとし、Qwen3.6 35B-A3B（23GB）とGemma4 12B（7.6GB、
実行時8.1GB）をOllamaで1台ずつ100% GPUへ載せ、温度0で比較した。入力は10巻74〜75ページの
同一本文、矛盾した事実メモ、「仁耀は牢へ戻る道を選んだ」という誤要約である。

Qwenは最終行動を`return_to_prison`、誤要約を`supported`と判定した。「わかった」が直前の
「最長10年間逃げ続けてほしい」への返答である時系列を追わず、さらに前の「私は牢に戻る」という
発言へ誤って結び付けた。Gemmaは`continue_fleeing_up_to_10_years`と誤要約`contradicted`を選び、
この固定ケースの中心判定は正しかった。日本語の長距離照応と最終状態の選択では、モデル規模が
小さくてもGemmaを独立検証役として試す価値がある。

ただしGemmaも完全合格ではない。JSONだけを要求したのにコードフェンスを付け、事実メモ末尾の
「牢に戻ることを決意」という自己矛盾を`fact_note_verdict=supported`として残した。日本語校正では
Qwenが「州庁舎へ安全な場所へ」という助詞重複を残したのに対し、Gemmaは表面上自然に直したが、
「宿へ急行」を「逃走し、隠れ家へ」と変えて主体と事実を動かした。流暢さだけで意味保存を判定すると
危険である。

したがってGemma4はQwen執筆後の第二検証役候補とし、執筆・校正の自動確定には使わない。
64GB MacではQwenを停止してからGemmaを直列ロードする。既定検証モデルの変更前に、人物の最終行動、
否定、時系列、指示語、途中状態と最終状態の衝突を含む正解固定10〜20件以上で、事実判定正解率、
重大見逃し、JSON契約成功率、意味保存率、所要時間を測る。コードフェンス除去などの構文正規化は、
意味判定の誤りを隠さない範囲に限定する。

---

### 9.22 Gemma4 31Bは12Bより事実検証が改善するが、意味保存とJSON契約は未解決（2026-08-02）

Gemma系の主比較対象を12Bから`gemma4:31b`へ変更し、§9.21と同じ10巻74〜75ページ、同じ矛盾した
事実メモと誤要約、温度0のプロンプトを再実行した。モデルは19GB、実行時20GBで、64GB Macの
GPUへ100%配置できた。事実監査は16,384 context、校正は8,192 contextで直列実行し、初回ロードを
含む2リクエスト合計は84.05秒だった。

31Bは`final_action=continue_fleeing_up_to_10_years`、
`summary_claim_verdict=contradicted`を返し、中心判定は12Bと同じく正解した。さらに12Bが
`fact_note_verdict=supported`として見落とした事実メモ末尾の「牢に戻ることを決意」も、31Bは
`partially_contradicted`として検出し、途中の発言と最終的な同意を説明できた。この固定ケースでは、
31Bへの拡大によって日本語の時系列・最終状態・入力内自己矛盾の監査が明確に改善した。

一方、JSONだけを要求してもコードフェンスを付ける契約違反は31Bでも残った。校正文は助詞重複を
解消して自然だったが、「逃げ隠れしている宿」を「逃げ込んだ宿」、「『尊い方』がいること」を
「『尊い方』であることに間違いがない」と変えた。12Bの大きな主体変更より軽いものの、元文にない
移動状態や確信度を加えるため、意味保存の自動合格にはできない。

したがって31BをGemma系の主比較対象かつ独立第二検証役の主候補とするが、既定検証モデル、執筆、
校正の自動確定役にはまだ切り替えない。正解固定10〜20件以上で、12B・31B・Qwenの事実判定正解率、
重大見逃し、JSON契約成功率、意味保存率、所要時間を比較してから採用を判断する。64GB Macでは
引き続きQwenとGemmaを同時常駐させず、モデルを停止してから直列に切り替える。

初回の`ollama pull`は19GB blobの並列チャンクが8.8GB付近で失速し、未完了部分が巻き戻った。
同一レジストリblobを再開可能な単一ストリームで一時領域へ取得し、期待するSHA-256との一致を
確認してからOllamaへ登録すると完了した。失敗した一時チャンク21個は、正式モデル登録後に
8.9GB分を削除した。大容量モデル取得で進捗が繰り返し減る場合は、破損blobを採用せず、単一取得と
digest検証を必須にする。

---

### 9.23 Gemma4 31Bは全文生成の代替より高リスク主張の独立検証に向く（2026-08-02）

10巻の隔離DBとQwenが抽出した同一事実表を使い、Qwenが担当していた事実抽出、詳細あらすじ、
一覧用短縮要約、人物辞典、完成文検証を`gemma4:31b`で役割別に試行した。公開DB、OCR本文、
ページ索引、画像は変更していない。夜間バッチでも1工程が無制限に長時間化しないよう、
応答が返らない工程は20分を今回の評価上限として停止した。この上限到達はモデル障害ではなく、
現行Qwen向けコンテキスト・出力上限をそのまま適用した場合の運用不適合として扱う。

| 役割 | Gemma4 31Bの結果 | 判断 |
|---|---|---|
| 構造化事実抽出 | 第1ブロックで書籍事実応答後の人物事実が20分以内に完了せず、チェックポイント未保存 | Qwenを継続。31Bへの一括置換はしない |
| 詳細あらすじ執筆・校正 | 889字・15文、28分31秒。日本語は自然で「牢へ戻る」の誤断定を回避したが、最長10年間逃げる最終合意と帰宅後の結末を省略し、将来の証言を取得済みのように時制変更した | 網羅性と時間の両面でQwenを主執筆役とする |
| 一覧用短縮要約 | 490字、22分30秒。400〜700字の契約内で読みやすく、重大誤断定を避けた。一方、拘束主体を珀陽へ単純化し、最終合意を省略した | 任意の第二案候補。既定生成にはしない |
| 人物辞典 | 最重要人物の茉莉花1名の初稿が20分以内に完了せず、人物出力なし | Qwenを継続。証拠量削減後に再検討する |
| 巻全体の完成文検証 | 64ページ・約9万字を一括提示した検証は20分以内に応答なし | 単一巨大プロンプトでは運用しない |
| 局所事実検証 | 74〜75ページの固定ケースは事実監査と校正を合計84.05秒で完了し、Qwenの最終状態誤りと事実メモ内部矛盾を正しく検出 | Gemma4 31Bの主用途とする |

詳細版・短縮版で重大な「牢へ戻る」を出さなかったことは改善だが、正しい最終状態を書いたのではなく
難しい箇所を省いた結果でもある。生成文の誤り率だけでなく、重要な発端・転機・結果・関係変化の
欠落率を同時に測らなければ、保守的な省略を品質向上と誤認する。また校正後も主体、時制、確信度が
動くため、日本語の流暢さを意味保存の代替指標にしない。

当面の構成は、Qwenで事実抽出・詳細版・短縮版・人物辞典を生成し、機械ゲート後にGemma4 31Bへ
高リスク主張だけを小さな本文窓と共に渡す直列ハイブリッドとする。高リスク対象は、否定、最終行動、
途中状態と最終状態の衝突、時系列、代名詞・主語、死亡・拘束・離脱など不可逆な状態変化を優先する。
巻全文を一度に検証せず主張単位または少数主張の束へ分割し、Gemmaの不合格判定を自動で本文へ
反映せず、再生成またはCodex補助QAへ送る。短縮要約の第二案生成は夜間時間に余裕がある場合だけ
実験的に残し、固定コーパスで欠落率と所要時間を評価してから採用を判断する。

---

### 9.24 Codex Lunaは10巻全体で主要事実を保持したが、時間計測と主体精度が不足している（2026-08-02）

B-36の比較を続けるため、Qwenが担当したOCR後処理の役割を`gpt-5.6-luna`でも実行した。公開DBの
生成物を入力へ混ぜないよう、10巻の隔離スナップショットから`index_eligible=1`の74ページを含む
8〜84ページ、77ページ・105,830文字を読み取り専用で提示した。事実抽出はQwenと同じ4ブロック境界
（8〜27、28〜49、50〜69、70〜84）を使い、既存のQwen事実表・要約・人物辞典は証拠入力から除外した。
同じOCR本文、ページ境界、対象人物10名、評価軸（最終状態、否定、時系列、主体、重要事実の欠落）を
揃えたが、Codexの実行環境とQwenのプロジェクト用プロンプト・推論ランタイムを完全一致させた比較ではない。

最初に行った74〜75ページの固定誤りケースでは、Lunaは次のように判定した。

- 最終行動: `continue_fleeing_up_to_10_years`
- 事実メモ: `partially_contradicted`
- 「牢へ戻る道を選んだ」という要約主張: `contradicted`
- 根拠: 74ページの「牢に戻る」は途中の発言であり、75ページで珀陽が10年間逃げ続けるよう求め、
  仁耀が「わかった」と最終同意している

固定ケースは、途中状態と最終状態を分離し、否定と時系列も正しく扱えたため合格とした。この合格後に
だけ巻全体の生成へ進み、出力はメモリ上の比較結果として扱った。SQLite、OCR本文、ページ索引、画像、
公開要約・人物辞典には書き込んでいない。

巻全体の結果をQwenの保存済み結果と比較すると、次の通りだった。

| 役割 | Qwenの保存済み結果 | Codex Lunaの結果 | 主評価 |
|---|---|---|---|
| 事実抽出 | 4チェックポイントを保存したが、固定箇所で「牢へ戻る」を最終行動として混在 | 約30件の出来事を抽出し、固定箇所の最終行動は正しい。瑠璃天目に関する一部の推測は事実と仮説の表示を要確認 | Lunaは意味判定が改善。ただし構造化表の独立機械検証なしでは公開不可 |
| 詳細あらすじ | 公開版446字。主要な流れと10年間の逃走は含むが、Qwen事実表の誤りが再生成へ波及し得る | 約1,050字。戦争論、捜索、黒の皇帝の逃亡、外交的帰結、仁耀の最終同意まで保持 | 網羅性と最終状態はLunaが優位。拘束主体の表現は修正待ち |
| 一覧用短縮要約 | `catalog_summary`は根拠検証失敗のため未公開 | 約560字。発端・転機・結果・10年間の逃走を保持 | Qwenの未公開状態から前進。ただし「珀陽に拘束」は「珀陽の作戦で天河が拘束」へ直す |
| 人物辞典 | 10名。役割・目的に根拠の薄い断定が一部混在 | 同じ10名。本文事実と推測を分け、推測であることを明記 | Lunaが保守的。ただし心理・役割の推測は公開前に個別根拠確認が必要 |
| 事実検証 | 同一Qwen検証では固定誤りを再承認 | 固定ケースの独立したCodex確認は正解。巻全体は自己監査までで、別モデルによる独立検証工程は未完了 | 固定ケースはLuna合格。巻全体の自動公開ゲートには未採用 |

Codex自身の品質評価は「固定ケース合格、巻全体は条件付き合格・公開不可」とした。Lunaは主要な発端、
転機、結果、関係変化を落とさず、仁耀の「牢へ戻る」という途中発言を最終状態へ反転しなかった。一方、
詳細版・短縮版で珀陽を拘束主体のように書いた点は、作戦主体（珀陽）と物理的実行者（天河）の区別を
弱める。これは日本語の自然さでは見えにくいが、ユーザーが指定した主体評価では不合格要素である。
また、全体出力に対する独立検証を別工程として完了していないため、Luna自身の自己監査を合格根拠へ
置き換えない。

所要時間は比較不能だった。今回のLuna実行では役割別の推論開始・終了時刻を保存しておらず、固定ケースも
読取り監査の結果だけを記録してモデル推論時間を計測していない。Qwenについても、保存済み10巻成果物から
役割別の再現可能な所要時間を取り出せない。したがって「Lunaの方が速い／遅い」という結論は出さず、次回は
同一入力ハッシュ、モデル、プロンプト版、温度、開始・終了時刻、出力文字数を役割ごとに記録する。

当面は公開DBの主系統を変更せず、Qwen生成・既存機械ゲート・高リスク主張のCodex補助QAを維持する。Lunaを
次の候補として再評価する場合は、(1) 拘束・死亡・離脱などの主体を明示する構造化出力、(2) 事実と推測の
ラベル、(3) 巻全体の独立検証、(4) 役割別時間計測を追加し、固定ケースを10〜20件へ拡張する。

---

### 9.25 構造化条件を追加した再現計測では固定ケースだけ合格し、巻全体はfail closedとなった（2026-08-02）

前回の「条件付き合格」を再現可能にするため、実験ID`b36-codex-luna-2026-08-02-r2`で再計測した。入力は
前回と同じ隔離スナップショットの10巻、`index_eligible=1`の8〜84ページ77ページ・105,830文字、入力SHA-256
`21401728fc9694de29f25c32e2ff5e81f7188d06fc0a956562edf4c5dfe3cf13`で固定した。4ブロック境界（8〜27、
28〜49、50〜69、70〜84）、対象人物10名、Qwen生成物を入力へ混ぜない条件を維持した。モデルは
`gpt-5.6-luna`、プロンプト版は`b36-luna-structured-v2.1-fixed-definition`、temperature相当値は
Codexでは外部設定できないため「決定的な構造化出力指示」と記録した。入力マニフェスト、設定、Schema、全試行の
JSON envelope、Codexレビューは隔離一時領域へ保存し、OCR本文はgit管理対象へ追加していない。

固定ケースは、初回の構文有効応答が`fact_note=contradicted`・誤要約`supported`となり意味判定に失敗した。
判定定義をプロンプトへ明記して再実行したところ、37.377秒、3,191文字入力、1,278文字出力で、
`final_action=continue_fleeing_up_to_10_years`、事実メモ`partially_contradicted`、誤要約`contradicted`を返した。
拘束も珀陽=`plan_owner`、黎天河=`physical_actor`、仁耀=`target`へ分離できたため、固定ケースだけ合格とした。

巻全体の工程別結果は次の通りである。時間はエージェント起動から完了までのwall-clockであり、Qwenの保存済み成果物には
同じ形式の役割別時間がないため、速度差の結論は出さない。

| 工程 | Lunaの計測結果 | Schema・品質判定 |
|---|---|---|
| 事実抽出 | 175.928秒、入力105,830字、JSON 11,209字、36件 | 最終状態と拘束3役は正しいが、`decision_maker`等の許可外`actor_role`が混入。fail |
| 詳細あらすじ | 132.908秒、入力105,830字、JSON 5,881字、本文約1,100字 | 独立監査では18主張の本文内容を支持し、最終状態も正しい。一方、出力の`raw_ocr`・日本語時制ラベルが契約外で、主語1件と欠落1件を指摘。fail |
| 一覧用短縮要約 | 144.333秒、入力105,830字、JSON 3,217字、本文502字 | 内容10主張は概ね支持。任務発端・帰国後課題を省略し、仁耀を`plan_owner`、珀陽を`witness`とする等6件の主体誤り。fail |
| 人物辞典 | 113.770秒、入力105,830字、JSON 5,399字、10名 | 10名集合は出力したが、影傑と黒の皇帝の混同、望来現/来現、皓茉莉花/晧茉莉花の正規名課題を独立監査で検出。公開不可 |
| 完成文の根拠監査 | 157.030秒、入力127,184字、JSON 8,669字、詳細18＋短縮10主張 | 最終状態・時系列は概ね支持したが、主体誤りと重要事実欠落で`fail`。監査モデル自身の応答も要求Schemaに違反し、自己監査だけで合格できないことを再確認 |

Qwenとの比較では、Qwenの事実抽出は4チェックポイントに保存されたものの固定箇所で途中状態と最終状態を混在させた。
Qwenの公開詳細版は446字、一覧版は根拠検証失敗で未公開、人物辞典は10名だった。今回のLunaは詳細版の本文量と
最終状態の明示では前回の約1,050字版と同等以上、短縮版は前回の約560字版より短い502字だった。しかし、
厳格な主体語彙・事実ラベル・正規名・監査Schemaを工程全体で通過できないため、意味的に自然な文章を補助生成へ
自動採用する根拠にはならない。

Codex自身の最終判定は「固定ケース合格、巻全体fail」である。LunaをQwenの代替生成、一覧・詳細・人物の補助初稿、
自動公開役へは進めない。採用候補として残す範囲は、入力ページを限定した固定ケースおよび、主体・否定・最終状態・
不可逆な状態変化を少数主張単位で人間または別検証器が再確認する補助QAだけである。次回必要なのは、許可外役割名を
生成時点で拒否するJSON制約、正規名の事前辞書、感謝状の「交渉中」など確信度の固定、監査Schemaの修正、Qwen側も
同じタイマーで再実行できる比較基盤である。

---

### 9.26 Compact厳密化より外部状態契約とブロック直接入力が有効で、Qwen主生成にも意味ゲートが必要（2026-08-02）

実験ID`b36-luna-compact-qwen-2026-08-02-r1`で、10巻の隔離スナップショットを再利用した。
入力は`index_eligible=1`の8〜84ページ77ページ・105,830文字、全体SHA-256
`21401728fc9694de29f25c32e2ff5e81f7188d06fc0a956562edf4c5dfe3cf13`である。Lunaへ巻全体を
一括提示せず、8〜27、28〜49、50〜69、70〜84ページの4つを独立した新規セッションへ直接入力した。
実験目的、ブロックSHA-256、正規名10名、許可された主体役割、途中状態と最終状態、禁止事項、再開条件を
外部状態契約JSONに固定し、厳格JSON Schemaとローカル検証器でページ範囲、先頭・末尾網羅、参照整合、
74〜75ページの最終行動、拘束の3役を検査した。

Lunaの機械判定は次の通りだった。

| ブロック | 抽出結果 | 判定 |
|---|---|---|
| 1（8〜27） | 初回32件は25ページで上限へ達し末尾欠落。有界再試行は25件で27ページまで保持 | 再試行でpass |
| 2（28〜49） | 29件で冒頭・末尾、Schema、高リスク非該当を保持 | 初回pass |
| 3（50〜69） | 28件で68〜69ページを含み、Schemaと参照整合を保持 | 初回pass |
| 4（70〜84） | 32件。牢へ戻る発言を途中状態、最長10年逃走を最終状態としたが、`plan_owner=皓茉莉花`とし、82ページで上限へ達した | fail |

有界再試行後も自動合格は4ブロック中3ブロックであり、本番条件ならQwenへ渡さず停止する。
比較を完走するためだけに、同じ原文を使った別Luna出力のブロック4をCodexが照合し、
`plan_owner=珀陽`、`physical_actor=黎天河`、`target=仁耀`、84ページまでの26件を
`human_reviewed_salvage`として分離採用した。機械合格と手動救済を同じ成功率へ数えない。

Compact後の復元用プロンプトも設定したが、1セッション1ブロックの直接実行ではCompactが発動しなかった。
したがって今回の改善をCompact要約の厳密化へ帰属できない。ツール経由でLuna自身にSQLite探索やハッシュ計算を
させた試行は、誤ったハッシュ、確定前の幻覚出力、トークン増加を生じた。原文OCRを直接標準入力へ渡し、
状態を外部契約から復元する方が安定した。重要なのはCompact文章を詳しくすることではなく、会話要約を証拠にせず、
工程を小さく分離し、原文と機械検証済み成果物だけで再開することである。

手動救済を含む108件の事実をQwen3.6 35B-A3Bへ渡し、詳細版と一覧版を初稿・校正の2段階で生成した。
初回は事実表の`[page N]`が初稿・校正版の双方へ漏れ、既存品質ゲートが正しく拒否した。ページ根拠を通常の
日本語注記へ変えた有界再試行は、Ollama上で25GB、100% GPU、131,072 contextを使い、詳細版1,637字を
292.65秒、一覧版529字を177.77秒、合計470.43秒で生成し、形式ゲートを通過した。

詳細版は、従来のQwen保存版とLuna単独版が誤った拘束主体を、珀陽が用意した作戦・警備と黎天河による
物理的拘束へ分離し、仁耀の「牢へ戻る」を最終結果にしなかった。戦争論、黒槐国任務、黒の皇帝の逃亡、
仁耀の襲撃、珀陽の決断、帰国後の課題まで保持し、網羅性も公開済みQwen版446字より改善した。
一方、正しい「最長10年間」を「十年間」と断定し、`黑`、`纸条`、`血迹`などLuna抽出由来の中国字を
正規化できなかった。一覧版は529字の契約内だが黎天河を省略し、戦争回避の方針を「成功」と完了済みに強め、
逃走期間も同じく10年へ固定した。自然な文章と形式ゲート合格だけでは、期間上限、主体、確信度、字形の
意味・表記回帰を防げない。

結論は「品質改善あり、自動公開はfail」である。Lunaの役割は、全ブロックが機械合格した場合に限る
構造化抽出候補として再評価できるが、手動救済前提では本番化しない。QwenはGemma4 31Bより大幅に速く
網羅的な主生成役として維持するが、事実表インターフェースで生成マーカーを除去し、正規日本語表記を固定し、
最大・最小期間、主体3役、未確定方針を完成文へ引き継ぐ意味ゲートが必要である。公開DB、OCR本文、ページ索引、
画像、既存要約・人物辞典は変更していない。

---

### 9.27 Sol mediumの1冊主生成は固定誤りを改善したが、未承認正規名でfail closedとなった（2026-08-02）

ADR-0018のPhase 1として、10巻の同じ読取り専用隔離DB、`index_eligible=1`の8〜84ページ77ページ・
105,830文字を`gpt-5.6-sol`のmediumへ1回だけ渡した。ChatGPTログイン、`--ephemeral`、read-only sandbox、
ユーザー設定・exec policy除外、APIキー環境変数除去、JSON Schema出力を固定し、公開DBへの書込みを無効にした。
主生成は333.657秒、入力101,054 token、出力15,499 token、推論510 tokenで完了した。

出力は事実50件、詳細あらすじ1,326字、一覧用要約406字、人物11名、高リスク主張12件だった。
「牢へ戻る」を途中の意向、「長くても十年間逃亡」を最終決定として分離し、期間を`up_to`のまま保持した。
黒の皇帝殺害を未遂、戦争回避を今後の外交課題として扱い、`黑`、`纸条`、`血迹`等の中国字も公開文へ
混入しなかった。詳細版では黎天河を物理的拘束者として明示し、Qwen/Luna混成結果に残った主要な意味回帰を改善した。

一方、正規名台帳10名にない`礎犀輿`を人物辞典へ追加しながら、同じ出力の`unresolved`ではOCR揺れのため
正字を断定できないと記した。未確定名を公開候補へ入れる自己矛盾のためローカルゲートは不合格とした。
一覧用要約も拘束の物理的実行者である黎天河を省略した。候補は自然で主要筋も強いが、公開可能とは判定しない。
設計どおり、ローカル不合格後の別Sol独立評価は実行せず、既存公開版を維持した。

原文再確認では、拘束作戦の戦術は茉莉花が考案し、珀陽が到着後に説明・指揮し、天河が拘束していた。
従来の固定条件`珀陽=plan_owner`だけでは役割を単純化しすぎる。今後は`tactical_planner=皓茉莉花`、
`command_approver=珀陽`、`physical_actor=黎天河`、`target=仁耀`へ契約を分割する。

> **後続訂正**: §9.28の独立評価により、茉莉花単独の戦術立案と珀陽による罠全体の指揮・承認は
> 提示原文から確定できないと判明した。本段落の役割仮説を本番fixtureとして使用しない。

なお、過去記録の巻全体SHA-256には算出式が保存されておらず、同じDBから再現できなかった。4ブロックの個別ハッシュ、
ページ集合、文字数はすべて一致したため本文変更ではない。今回から
`sha256(0x1d + repeated(page_no + 0x1f + full_text + 0x1e))`を明記し、巻全体ハッシュ
`1567094cbffd9de015e7fa63ec67f9de12c1deb539f0f874d4cba4dd348e12a9`を監査値とする。

次回は人物辞典の`canonical_name`を正規名台帳のJSON Schema enumへ固定し、不明人物は`unresolved`だけへ出す。
一覧用要約には高リスク事件の計画者・指揮者・物理的実行者を必要範囲で残す。これらを直したmediumの再試行後にだけ、
高リスク主張を原文ページ窓付きの新規Solセッションへ送る。

---

### 9.28 正規名厳格化後はローカル合格したが、正解誘導による役割過剰特定を独立Solが検出した（2026-08-02）

§9.27の不合格を受け、人物辞典の`canonical_name`を既存10名のJSON Schema enumへ固定し、一覧用要約にも
黎天河の物理的拘束を残す契約でSol mediumを再実行した。主生成は410.709秒、入力102,487 token
（cache 100,096）、出力14,431 tokenで、事実48件、詳細1,367字、一覧402字、人物10名、高リスク主張16件を
生成した。正規名、文字数、末尾網羅、途中状態、期間上限、戦争回避の未確定性、拘束者を含むローカルゲートは
エラー・警告とも0件で合格した。

続いて新規Sol mediumセッションへ、高リスク主張16件と候補ページ前後の31ページ・40,242文字だけを渡した。
主生成の事実表、要約全文、理由、予測役割は渡していない。独立評価は102.854秒、入力48,181 token、
出力4,410 tokenで、12件を`supported`、4件を`unsupported`とした。重大3件は同じ根本原因で、主生成へ
`tactical_planner=皓茉莉花`、`command_approver=珀陽`という期待値を与えたため、詳細版、一覧版、人物辞典の
3箇所が原文より細かい役割分担を断定した。原文は「茉莉花たち」が罠を用意し、黎天河が仁耀を拘束したことを
支持するが、茉莉花単独の設計と珀陽による罠全体の指揮・承認までは確定できない。

軽微1件は、一覧版が「外交交渉と政策形成はこれから」としたうち、政策形成が今後であることは支持される一方、
提示原文から外交交渉という具体工程までは確定できない点だった。独立評価の総合判定は`fail`であり、候補編集、
再評価、公開DB反映は行わず既存版を維持した。主生成と独立評価の合計wall-clockは513.563秒である。

この結果は、固定ケースの正解を主生成プロンプトへ含めるとベンチマーク自体を汚染することを示す。
今後は正解fixtureをローカル評価・独立評価だけが参照し、主生成へは正規名enum、一般的な役割分離規則、
断定回避規則だけを渡す。主体役割は単一文字列ではなく複数候補と根拠ページを持つ配列にし、直接根拠がなければ
空配列または`unknown`とする。次の再試行はこのプロンプト汚染を除いた後に行い、highへの変更はその後に判断する。

### 9.29 固定正解除去後のSol mediumは一覧版の限定条件と拘束主体を保持できなかった（2026-08-02）

§9.28で見つかったベンチマーク汚染を除くため、主生成から牢、最長10年、拘束作戦の人物役割など固定ケースの
期待値をすべて除外した。入力は同じ読取り専用隔離DBとOCRハッシュ、既存10名の正規名enum、一般的な
役割分離・断定回避規則だけとし、役割を根拠ページと確度を持つ配列へ変更した`sol-publication-candidate-v2`で
Sol mediumを再実行した。

主生成は364.542秒、入力101,421 token、出力19,937 token（reasoning 393）で、事実48件、詳細1,271字、
一覧409字、人物10名、高リスク主張12件を生成した。事実表のF42には`最長十年間`と`up_to`が残り、
黎天河による拘束も別の事実内には残った。一方で「牢へ戻る」という途中意向と「最長十年間逃げ続ける」という
最終決定を別factへ分離せず、遷移・矛盾関係を表現できなかった。さらに一覧版は期間上限を丸ごと省略し、
黎天河を物理的拘束者として残さず、「茉莉花の罠で仁耀を捕らえた珀陽」と読める主体表現へ変化した。
役割配列にも、同一factへ複数行為と関与人物を詰めたことによる過剰推定が残った。

ローカルゲートは`catalog:up_to_duration_missing`、`fixed_case:prison_intermediate_missing`、
`catalog_summary_omits_physical_arrest_actor`の3件で不合格とした。設計どおり独立Sol評価、候補の自動修正、
公開DB反映は実行していない。この結果から、§9.28のローカル合格は固定正解を主生成へ与えた影響を受けており、
中立条件のmediumは現行受入基準を満たさないと判断する。

次はmediumへ固定ケース固有の文言を足さず、同じ中立プロンプト、Schema、ローカルfixtureでhighを比較する。
同時に一般契約として、1件のfactへ複数の中心行為を混在させず、実行者が変わる場合はfactを分割する。
highも同じ3条件を満たさない場合は、モデル強度だけで解く経路を見直し、事実分割と完成文転記を決定的な
中間処理へ寄せる。

### 9.30 Sol highは一覧版を改善したが、事実分割と役割推定の重大誤りは残った（2026-08-02）

§9.29のmediumと同じOCRハッシュ`1567094cbffd9de015e7fa63ec67f9de12c1deb539f0f874d4cba4dd348e12a9`、
同じ中立プロンプトSHA-256`51e96025ef0d8a68be43a10b43f668f821ec7082953175b332074f0721731125`、
同じ`sol-publication-candidate-v2`を使い、reasoning effortだけをhighへ変更した。highは401.524秒、
入力101,421 token、出力21,843 token、推論1,552 tokenで、事実47件、詳細1,641字、一覧414字、
人物10名、高リスク主張14件を生成した。medium比では実行時間が36.982秒（10.1%）、出力が1,906 token
（9.6%）増え、推論tokenは393から1,552へ増加した。

品質面ではhighが優位だった。mediumの一覧版で欠落した「最長十年間」と黎天河による拘束を保持し、
詳細版も発端から帰国後課題までより厚く記述した。期間ゲートが原文と同義の「長くて十年間」を認識しない
表層一致バグも見つかったため、これはモデル誤りから除外し、限定意味`up_to`と値で扱う設計へ補正した。

ただしhighも受入条件には達しなかった。「私は牢に戻り、裁きを受ける」という仁耀の途中意向と、珀陽が示した
最長十年間の逃亡・将来証言という最終決定を1件のfactへ統合し、遷移・矛盾関係を持たせなかった。また72ページの
原文は罠の準備主体を「茉莉花たち」とだけ示すが、highは`皓茉莉花と珀陽`を戦術立案者、珀陽を
指揮承認者として`fact`断定した。さらに同じ拘束factへ、侵入者である仁耀と拘束者である黎天河を
`physical_actors`として混在させた。これは§9.28の独立評価で検出した役割過剰特定と同じ根本原因である。

補正後の同一ローカルfixtureでは、mediumが6件、highが4件のエラーとなった。highは明確に改善しているが、
重大誤り0件の公開基準を満たさないため、独立Sol評価、候補の自動修正、公開DB反映は行っていない。
品質優先でSolを使う場合はmediumよりhighを選ぶ価値があるものの、reasoning effortの増加だけでは解決しない。
次は高リスクページ窓を行為単位へ分割して検証済み事実グラフを決定的に作り、主生成にはそのグラフから
詳細あらすじ・一覧用要約・人物説明を文章化させる二段構成を検討する。

### 9.31 Sol highの限定修正は有効だが、監査主張を言い換える設計では公開文との対応が失われる（2026-08-02）

§9.30のhigh候補を全文再生成せず、4件のローカルエラー、該当fact、詳細版・一覧版、71〜79ページ9件・
12,233文字だけを新規Sol highへ渡した。修正出力はF38、F41〜F43、R08、詳細版と一覧版の完全一致置換2件に
限定した。基準候補SHA-256、JSON Schema、変更許可範囲、fact ID、参照更新をローカルで検査してから適用した。
修正は87.244秒、入力28,252 token、出力3,255 token、推論1,907 tokenで完了した。

Sol highは、拘束factから仁耀の侵入行為を除き、物理的拘束者を黎天河、対象を仁耀へ限定した。罠の準備主体は
原文どおり「茉莉花たち」とし、珀陽の戦術立案・指揮承認を空配列へ戻した。また仁耀の「牢へ戻る」途中意向、
珀陽の最長10年間逃亡・将来証言依頼と仁耀の承諾、黎天河による解放と仁耀の離脱を別factへ分割した。
適用後は47件から49件となり、候補全体のSchema、参照、固定fixture、完成文転記を再検査してエラー0件で合格した。
「牢へ」と同義の「牢に」を認識しないゲートの表層語形バグも期間表現と同じ方針で補正した。

続いて別の新規Sol highへ、高リスク主張14件と候補ページ前後の48ページ・64,965文字だけを渡した。
修正出力、事実表、完成文全体、生成理由、固定fixtureは渡していない。評価は192.664秒、入力68,223 token、
出力7,600 token、推論3,734 tokenで、12件を`supported`、R06とR10を`contradicted / major`とした。
R06は翔景が仁耀を引き離す陽動へ移ったのに、監査用主張が翔景まで黒の皇帝の発見・保護主体へ含めた誤り、
R10は将来の「長くて十年間逃げ続けてほしい」という依頼を、既に最長10年間逃亡した完了結果へ変えた誤りである。

独立評価自体の判定は原文に照らして正しい。ただし完成物との照合では、詳細版は「茉莉花と大虎」が発見したことと
「最長十年間逃げてほしい」という依頼を正しく保持していた。R06とR10は、主生成が詳細版を監査用に言い換えた際に
新しく作った誤りだった。一方、一覧版の「仁耀は最長十年間逃亡し」はR10と同じ完了形の問題を実際に含むが、
R10の`artifact`は詳細版であり、一覧版の当該文を正確には監査できていなかった。

したがって、Sol highを生成者、限定修正者、独立評価者へ分ける方式は、修正と追加誤り検出の両方で有効である。
失敗の主因はモデル能力だけではなく、`risk_claims.text`を完成文の言い換えとして生成した監査境界にある。
次は`artifact_text`を完成文から完全一致で抽出し、出現位置と文ハッシュを付け、評価用の正規化主張を別フィールドへ
分離する。ローカル規則が拾った全高リスク文との完全被覆を確認し、評価者には両方を渡す。1回修正上限に従い、
今回の候補への2回目修正、再評価、公開DB反映は行っていない。主生成、限定修正、独立評価の合計は681.432秒である。

### 9.32 公開文の完全一致監査で言い換え偽陽性を除き、実際の根拠不足1件を検出した（2026-08-02）

§9.31の監査境界を修正し、修正後候補の詳細あらすじ、一覧用要約、人物説明を句点・疑問符・感嘆符単位で
決定的に分割した。戦争、死亡・暴力、拘束・解放、逃亡・保護、主体・計画、不確実性・否定、期間・将来、
人物関係、将来失敗の版付きトリガーへ一致した文を和集合にし、詳細21文、一覧7文、人物説明13文の合計41文を
抽出した。各項目は公開物内の開始・終了位置、公開物SHA-256、完全一致文、文SHA-256、カテゴリ、トリガー語を持つ。
同じ候補から再抽出したJSONと完全一致することをローカルゲートで確認した。

候補ページ対応や過去の監査用言い換えを使わず、41件の`artifact_text`と確定OCR全文77ページ・105,830文字を
新規Sol highへ渡した。候補事実表、生成・修正理由、前回レビュー、固定fixtureは渡していない。評価は277.641秒、
入力109,470 token、出力12,163 token、推論6,421 tokenで、40件を`supported`、詳細版E18を
`unsupported / major`とした。入力再現性、41件の順序・文ハッシュ、判定集計もローカルで再検算した。

R06に対応する公開文E12は、翔景側を陽動へ回し、茉莉花と大虎が黒の皇帝を発見・保護した主体を正しく保持して
`supported`となった。R10に関連する詳細版E17、一覧版E27、仁耀の人物説明E34も、最長10年間の逃亡と将来証言を
珀陽の依頼内容として保持していると評価された。これにより、§9.31のR06/R10は公開文ではなく監査用言い換えを
判定した偽陽性だったことを確認できた。

一方、E18の「仁耀は承諾して解放され、茉莉花へ白楼国の未来を託して去った」は実際の公開文である。原文は
「あとは頼んだ」に続けて、託した対象を「白楼国の未来か、玉霞のような若者たちか、それとも……」と複数候補の
まま未確定にしている。公開文が白楼国の未来だけへ限定したため、不確実性を失う重大な根拠不足という評価は正しい。

完全一致監査は言い換え由来の偽陽性を除き、実際に公開される文の新しい誤りを検出できたため採用価値が高い。
ただし「41件完全被覆」は現行トリガー規則が抽出した集合に対する完全性であり、未知表現を含む意味上の全リスクを
保証しない。固定コーパスで規則を拡張する必要がある。1回修正上限に従ってE18の再修正・再評価・公開は行っていない。
主生成、限定修正、2回の独立評価を含むSol highの合計wall-clockは959.073秒である。

### 9.33 Muse Glimmer 30B Q6はM1 Max 64GBで動くが、Ollama 0.30.11と短文reasoningに制約がある（2026-08-13）

Meta系の新しい比較候補として、Unsloth配布の
`hf.co/unsloth/Muse-Glimmer-30B-GGUF:UD-Q6_K_XL`をM1 Max 64GBへ隔離導入した。
Ollama表示は言語モデル27.9B、Q6_K、context 131,072で、言語ウェイト26GBにvision projector
3.8GBを加えた登録サイズは30GBだった。既存Qwen、Gemma、bge-m3は削除・上書きしていない。

Ollama 0.30.11はmanifest取得とSHA-256検証、モデル情報の解析までは成功したが、推論開始時に
`Failed to load CLIP model`、`unknown model architecture: 'muse-glimmer'`で停止した。projectorを外した
Ollamaモデルの再作成も内部`llama-quantize`の互換性検査で失敗した。したがって、Ollama上の登録成功を
推論成功とみなさず、対応版を実機確認できるまでこのモデルをOllama経路へ設定しない。

Homebrewの`llama.cpp` b10360では、同じ登録に含まれる言語blobだけを直接ロードできた。4,096 context、
`-ngl 99`、単一slot、`127.0.0.1:11436`限定で初回ロードは約30.9秒、常駐RSSは26,006,096KiB
（約24.8GiB）だった。公式special tokenで回答先を`user`へ固定した短い生成は「起動確認成功」を返し、
入力49.2 token/s、生成11.9 token/sだった。確認後はserverを停止し、LAN公開と常駐を残していない。

一方、標準chat templateは`reasoning_strength`の既定値がhighである。誤って`reasoning_effort`を渡すと
既定highのままとなり、正しい`chat_template_kwargs.reasoning_strength=low`でも、単純な3キーJSONを
256 token考え続けて本文へ到達しない事例があった。`--reasoning off`は出力解析を変えてもモデルの自己推論を
止めなかった。Qwenの`think=false`と同じ挙動を前提にせず、固定10巻比較ではreasoning本文、最終本文、
finish reason、出力上限、速度を分離保存する。長文品質と131,072 contextは後続の9.34で検証した。
既存`local_llm`とのAPI互換は未検証であり、Qwenの既定値と本番DBは変更しない。

### 9.34 MuseとQwenは64GBで共存できるが、同時生成せずローカルQwen主生成 + Muse短窓検証とする（2026-08-13）

茉莉花官吏伝10巻の読み取り専用隔離コピーを使い、Qwen3.6 35B-A3BとMuse Glimmer 30B Q6を
固定ケース、77ページ一括、同じ4ブロック方式で比較した。固定ケースの正解は、仁耀の最終行動が
`continue_fleeing_up_to_10_years`、途中の牢へ戻る記録が`partially_contradicted`、牢へ戻ったという
要約が`contradicted`である。Qwenは3フィールドすべてを誤ったが、Museは3フィールドとも正解した。
所要時間はQwen 18.25秒、Muse 45.63秒で、短い意味判定ではMuseに独立検証候補としての価値がある。

本節は各条件1回の予備試験であり、seed 42、temperature 0、同じOCRページ・意味上同じ指示・
JSON Schemaを使った。Qwenは`think=false`、Museは`Reasoning strength: high`と回答先`user`を固定した
直接completionである。Muse公式モデルカードが推奨するtemperature 1.0 / top_p 0.95 / top_k 64と
DFlash speculative decodingは使っていない。したがって速度はM1 Max上の`llama.cpp`通常decode実測であり、
最適化済みMuseの上限ではない。Museの直接promptは先頭BOSを含む一方でserverもBOSを追加し、
duplicate BOS警告が記録された。次回はchat template経路を統一し、BOS重複を解消して3回再測定する。
QwenとMuseではtokenizerも異なるため、token/sは同一モデル内の単独・並列比較には使えるが、
モデル間の品質効率をtoken/sだけで順位付けしない。

8,192 contextでMuse約24.5GiB、Qwen約23.0GiBを同時常駐させてもswapは発生しなかった。しかし、
cacheを分離した逐次実行はQwen 8.28秒 + Muse 44.40秒 = 52.68秒だったのに対し、同時生成は
Qwen 49.62秒、Muse 63.46秒となり、完了時刻は約20.5%悪化した。Qwenの出力速度は34.24から
2.67 token/sへ低下した。メモリ容量ではなく、共有メモリ帯域とGPU資源の競合が支配的である。
131,072 contextでも合計約50.0GiB、空き約7%、swap 0で同時常駐はできたが、長文生成を2本走らせる
安全余裕はない。したがって「両方をインストールできる」「短時間は同時常駐できる」「同時生成が有効」
を別の条件として扱い、通常運用は片方を停止してからもう片方を起動する。

77ページ一括では、Qwenは639.39秒で完走したものの詳細730字、一覧208字で文字数契約に違反し、
終盤の将来証言を欠落させた。Museは63,488入力token、約89%までのprompt処理に約53分を要したため、
生成開始前に評価上限で停止した。長文一括はMuseの実用経路にしない。

同じ4ブロック方式の結果は次のとおりである。

| 指標 | Qwen3.6 35B-A3B | Muse Glimmer 30B Q6 |
|---|---:|---:|
| 4ブロック抽出 | 7分33秒 | 32分32秒 |
| 最終統合 | 1分45秒 | 10分15秒 |
| 合計 | 9分18秒 | 42分47秒 |
| ブロック要約文字数 | 387 / 327 / 253 / 270 | 188 / 433 / 331 / 597 |
| ブロック事実件数 | 7 / 8 / 8 / 8 | 12 / 9 / 18 / 10 |
| 詳細 / 一覧文字数 | 1,280 / 299 | 1,542 / 314 |
| 高リスク事実 / 不確実性 | 14 / 7 | 14 / 20 |

両モデルともJSON Schemaの型は守ったが、400〜700字の一覧要約は下限未達だった。Qwenは短く圧縮し、
Museは事実を多く残して件数上限を超える傾向がある。原文の最終合意は「長くて十年間逃げ、必要時に
淑太上皇后の手引きによる脱獄と証言する」である。Qwenは一覧版へ十年逃亡を残したが証言を落とし、
Museは詳細版と高リスク事実へ逃亡と証言を残したが一覧版で十年を落とした。さらに両モデルとも
「長くて」という期間上限を失い、Museは将来の約束を`completed`にした。固定短窓でのMuse正解を、
巻全体生成の自動合格へ一般化してはならない。

初回仮結論として、ローカルLLM系統の主生成はQwenを維持する。Museは短い根拠窓と期待ラベルを渡す
高リスク主張の第二検証候補に限定し、機械ゲートの代替や自動公開の確定役にはしない。両者を使う場合も、
Qwen生成、アンロード、Muse検証、
Museアンロードの順に直列実行する。Ollama 0.30.11がMuse projector非対応のため、Museは当面
localhost限定の`llama-server`経路を使い、本番既定値へ配線しない。OCR後の公開成果物は
[ADR-0018](../../design/基本設計/ADR/0018_sol-primary-post-ocr-generation.md)のSol主生成へ段階移行中であり、
ここでいうQwen主生成は対話QA・オフライン代替を担うローカル経路内の役割である。3回再現性試験と
固定10〜20件への拡張までは、Museの本番採用または恒久不採用を確定しない。

## 10. 関連ファイル

| パス | 役割 |
|---|---|
| `backend/services/novel_db/llm.py` | Qwen SSE 呼び出し（共通モジュール経由）|
| `backend/services/novel_db/summarizer.py` | 1-shot 書籍要約（B-5 / B-6）|
| `backend/services/novel_db/contextualizer.py` | チャンク位置説明生成（B-9）|
| `backend/services/novel_db/character_extractor.py` | 主要登場人物抽出 |
| `backend/services/novel_db/search.py` | ハイブリッド検索 + フィルタ + サマリ検索（B-8）|
| `backend/scripts/build_novel_summaries.py` | サマリ一括生成 CLI |
| `backend/scripts/extract_characters.py` | キャラ一括抽出 CLI |
| `backend/scripts/build_chunk_contexts.py` | チャンク contextualize 一括 CLI |
| `D:\61.tool\common\llm\local_llm\` | 共通 LLM クライアント（Backend ABC + 2 つの具象） |

---

## 追加ガイド

新しい知見が出てきたら、本ファイルに追記する。古い記述は「（旧）」と注記して残し、消さない（後から経緯を追えるように）。
