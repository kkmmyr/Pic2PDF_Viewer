# リファクタリング計画書

最終更新: 2026-06-27（全 Phase + TM-1 完了。完了記録は archive/ に移管済み）

未着手のリファクタリング候補・追加候補・対象外項目を管理する。完了済み Phase の詳細は [リファクタリング履歴.md](../../archive/リファクタリング履歴.md) を参照。

---

## このファイルの範囲

**書くもの:**
- **リファクタリング** — 外部挙動を変えずに内部構造を改善する（ファイル分割・hook 抽出・重複統合等）
- **技術メンテナンス** — 依存ライブラリ更新・インフラ/DB ツール導入・性能改善（例: React 19 化・TanStack Query 導入・LanceDB 移行）

**書かないもの:** ユーザーから見た新機能・新体験の追加 → [バックログ.md](バックログ.md) へ

| 判定 | 行き先 |
|---|---|
| ユーザー向けの振る舞いが新しく増える / 変わる | `バックログ.md`（A/B/C 階層） |
| 外部挙動を変えない内部構造改善 | 本ファイル **§ 未着手候補 — リファクタリング** |
| 依存・インフラ・性能特性が変わる（外部振る舞いは同等） | 本ファイル **§ 未着手候補 — 技術メンテナンス** |

迷ったら: ユーザーが画面で気づく差分があれば機能（バックログ）、なければ本ファイル。

---

## 概要

| 項目 | 値 |
|---|---|
| 完了 Phase 数 | 54 + Phase 55〜92（全 Phase 完了）— Phase 89/91 はスキップ判断 |
| 未着手 Phase 数 | 0 件（すべて完了またはスキップ） |
| 直近完了 | TM-1（2026-06-27）— Kindle 新アプリ（Microsoft Store 版）ウィンドウタイトル対応 |
| 次の動き | 新候補が浮かんだら「未着手候補」セクションに追記 |

優先度の判断基準:
- **P1**: 既に分離した責務が再膨張、または毎レンダー走る派生計算、緊急バグ修正など実害がある
- **P2**: 重複ロジック・読みづらさが顕在化しているが緊急性は中
- **P3**: 構造改善ではあるが現状で困っていない

---

## 未着手・追加候補

### 未着手候補 — リファクタリング

_新規のリファクタリング候補はここへ。外部挙動を変えない内部構造改善（ファイル分割・hook 抽出・重複統合等）。_

**Phase 51 完了後の追加候補（2026-05-05 抽出）:**

候補（2026-05-05 時点 / Phase 51 投入後の調査結果から）:
- **P2**: ジョブ状態管理が 3 系統（`GenerateJob` / `JobStateManager[T]` / `ocr_service` シングルトン）→ 単一基盤に統一 *(Phase 52 でスキップ判断 — 用途が異なるため)*
- ~~**P2**: router の `except Exception → logger.exception → HTTPException` 重複 → `_deps.py` にラッパー or デコレータ~~ *(Phase 52-1 で対応済み)*
- ~~**P2**: radio mode + reset パターン（`BulkAuthorDialog` / `BulkSeriesAssignDialog` / `SeriesEditDialog`）→ `useRadioMode<T>` 化~~ *(Phase 52-3 でスキップ判断)*
- ~~**P2**: タグ入力 UI（`addTag` / `removeTag` / Enter,Backspace）→ `useTagsInput` 化~~ *(Phase 52-2 で対応済み)*
- ~~**P2**: accent カラーが indigo / purple / red バラバラ → token に統一~~ *(Phase 52-4 で対応済み)*
- ~~**P3**: sort ロジック分散（`localeCompare('ja')`）→ `cmpJa` 化~~ *(Phase 53-2 で対応済み)*
- ~~**P3**: hooks の bash パース脆弱性（Windows パスの backslash）→ jq 化検討~~ *(Phase 53-3 で python3 化)*
- **P3（スキップ）**: `ReaderPanel.tsx` / `services/series_resolver.py` / `services/pdf_generator.py` の分割 *(Phase 53 調査でスキップ判断 — 詳細は [履歴.md § Phase 53（スキップ判断）](../../archive/リファクタリング履歴.md))*
- ~~**P3**: SeriesEditDialog の独自 useState ベース送信ロジック → `useDialogSubmit` 統一~~ *(Phase 53-1 で対応済み)*

**Phase 54 後の追加候補（未着手）:**

- **P3（判断保留）**: backend ルーター間の `os.path.join` vs `join_path`（slash 統一）の使い分け。`routers/library.py` のみ `join_path` を使用、`routers/pdfs.py` / `thumbnails.py` は素の `os.path.join`。フロントから返す URL ではなくファイルシステム操作に使っている分は揃える必要はない。
- **P3（スキップ寄り）**: 外クリック + Esc 同時対応のドロップダウン制御（`ToolsMenu` / `SearchableSelect`）→ `useOutsideClickAndEsc` フック化。再利用箇所が 2 つしかなく、フック導入の利得は薄い。
- ~~`LibraryPanel.tsx` / `ReaderPanel.tsx` の presentation/container 分割~~ → **対応しない**（[バックログ § 対象外](バックログ.md) に移動。hook 抽出済みで JSX 主体の分割では認知負荷が下がらず、テスト容易性は hook 単体テストと将来の E2E で充足見込み）

**2026-07-04 コードレビュー由来の追加候補（9並列エージェントによる backend/frontend 全体調査、178件中「安全に自動修正可」56件+バグ3件は即日対応済み。残り122件から重複統合した候補）:**

_backend:_
- **P2**: novel_db の LLM オプション（temperature/repeat_penalty/num_predict/num_ctx）dict が `character_extractor.py`/`character_summarizer.py`/`llm.py`/`contextualizer.py`/`query_expander.py`/`relation_extractor.py`/`discussion_service.py` の7ファイルで個別直書き → config 化 or 共通ビルダー関数化
- **P2**: `series_id → 書籍名` 解決ロジックが `scripts/build_character_summaries.py`/`build_novel_summaries.py`/`build_chunk_contexts.py`/`services/novel_db/job_worker.py` の4箇所で重複 → 共通ヘルパー化
- **P2**: キャラ名正規化ロジックが `character_db.py:_parse_main_characters` と `relation_extractor.py:_parse_char_names` で二重実装 → 共通化
- **P2**: `services/hitomi/nozomi.py`/`metadata.py` で httpx.Client 生成/close のボイラープレートが3箇所重複、プロキシドメインも2ファイルに個別ハードコード → contextmanager 抽出 + config 集約
- **P2**: `routers/library.py` の `delete_pdfs` が削除件数分 `update_meta_locked` を個別呼び出し（毎回全件ロード）。`series.py` 方式（1回のロック内でループ）に揃える
- **P3**: JST変換が SQL 内マジック文字列 `datetime('now','+9 hours')`（10箇所超）と Python 側 `utils.dt.JST`（`relation_extractor.py` 等）で方式不統一 → どちらかに統一
- **P3**: `amazon_csv_parser.py`/`amazon_csv_importer.py` のデコード処理重複（`shift_jis` vs `cp932` の差異あり、統合前に挙動差の実害確認が必要）
- **P3**: `routers/hitomi.py` が共通 `get_logger` を使わず標準 `logging.getLogger` 直書き（ログレベル未設定の地雷）→ `get_logger` 化
- **P3（判断保留）**: `services/novel_db/lance_store.py` の `maybe_create_index()` が本番構築フローから未呼び出し。docstring の「チャンク数50,000件超で自動構築」という説明と実態が乖離 → 呼び出し配線を追加するか docstring を訂正するか要判断

_frontend:_
- **P2**: `reader/PageSlider.tsx:normalizeSpreadPage` と `hooks/reader/useReaderNavigation.ts` のページ送り判定ロジックが二重実装。将来ページ送り規則を変更した際に不整合を起こすリスク → 共通化
- **P2**: `contexts/ReaderContext.tsx` の Provider value が `useMemo` 未使用で、無関係な状態変化のたびに全 reader 系コンポーネントが再レンダーされる根本原因になっている → `useMemo` 化
- **P2**: `components/library/PdfGrid.tsx`/`PdfCard.tsx` 系が `React.memo` 未使用でカード枚数が多い場面の再レンダーコストが懸念 → memo化を検討（`LibraryPanelContext` 側の値安定性とセットで要検討）
- **P2**: `components/novel_db/QuestionSection`（フックへ集約済み）と `ChatSection`（コンポーネント内にSSE処理をベタ書き）で抽象化レベルが不揃い → 共通の SSE 用フックへ統一
- **P2**: `hooks/library/useLibraryPdfs.ts`/`useBookMetaCore.ts`/`useGenres.ts` 等、catch で空データ（`[]`/`{}`）を返し TanStack Query の `isError` が発火しないフックが複数存在。通信障害が「0件」としか見えない → エラー時は throw して `isError` に委ねる方針へ横断的に統一
- **P3**: `components/novel_db` 系ダイアログの削除確認フロー実装場所（親 or 子コンポーネント）が不統一
- **P3**: `RelatedBooksPage.tsx` の「同シリーズ」「同じ作者」カードグリッド JSX がほぼ完全重複 → `BookCard` サブコンポーネント抽出
- **P3（再確認）**: ラジオ選択UIパターン（`BulkGenreDialog`/`BulkAuthorDialog`/`SeriesEditDialog`/`BulkSeriesAssignDialog`）の重複が今回のレビューでも再指摘された。Phase 52-3 のスキップ理由（reset内容の差異が大きく抽象化コスト＞利得）は変わっていないため、引き続き P3 据え置き

---

### 未着手候補 — 技術メンテナンス

_依存更新・ライブラリ移行・インフラ/DB ツール導入・性能改善はここへ。_

**2026-07-04 skill-audit 追加指摘（多視点並列監査の実験で検出。対応する）:**

- **P2**: `.claude/skills` の実態乖離 6 件 — ①test-writing/references/backend-patterns.md の fixture 例が存在しない `meta_store.DATA_DIR` を patch（コピペで AttributeError。実際は `config.META_DB_DIR`。4 体中 3 体が独立発見）②architecture-overview「viewer/ と reader/ の階層分離」が viewer/→library/ リネームに未追従（姉妹 skill と矛盾）③frontend-patterns.md「コンポーネントテストは原則書かない」vs 実際は .test.tsx 28 本存在 ④worktree-workflow が uv workspace 化（ADR-0010）以前の手順のまま（`backend/ && uv sync`・backend/.venv 残存の説明が現構成と不一致）⑤test-writing のテスト対象表に削除済み機能（`move_with_assets` / `JobStore.get_active_current_item`）が残存 ⑥「meta.json 更新」表記の実体は meta.db（SQLite）移行済み
- **P3**: backend-conventions「`import config; config.X` を基本」と実態（`from config import X` 直 import が主流 26 箇所）の乖離 — 規約を直すかコードを直すか要裁定
  → **✅ 2026-07-06 裁定=規約側を精密化**（コードは無変更）。実査の結果: 直 import 26+ 箇所はほぼ全て静的値（モデル名・閾値・定数集合）で、テストが monkeypatch するパス系は helper/settings/call-time 経由 — 乖離ではなく暗黙の使い分けだった。skill を「テストで差し替わる値=call-time 参照 / 静的値=直 import 可」に書き換え。patch 対象キーの直 import は `main.py`（StaticFiles mount）と `discussion_service.py`（DISCUSSIONS_DIR）の 2 箇所のみで、いずれも import 時 1 回評価の構造上 conftest の「patch→main import」順序で成立しており現状 green・記法変更では改善しないため触らない
- → **✅ P2 の 6 件は 2026-07-06 全修正済み**（skill 6 ファイル。各件実コードで事実確認のうえ修正、check_skills.py 通過。代表 1 件 = fixture 例は「旧記述 AttributeError / 新記述 1 passed」の実 pytest TDD で検証）。P3 は裁定待ちのまま未接触
- **P3（修正時の副産物 2 件・要判断）**: ① `tools/migrate_flatten.py:67` が削除済み `FileManager.move_with_assets` を今も呼ぶ（再実行すると AttributeError になる一回性移行スクリプトの陳腐化 — 削除 or archive へ）② `backend/.venv/` がルート `.venv/`（uv workspace, ADR-0010）と並存する移行前の遺物 — 削除候補

**2026-07-04 skill-audit 副産物（CI 常時赤の事象記録）— 対応する:**

- **P1**: CI が直近 5 run 連続 failure（2026-07-04 時点、skill-audit 以前の run から）。原因は job ごとに独立:
    1. **frontend**: `npm ci` が ERESOLVE で失敗 — `eslint@10.4.1` × `eslint-plugin-jsx-a11y@6.10.2`（peer は eslint ≤9）の競合。**テスト step 未到達のため、型ゲート（`3fe8d35` で復活させた tsc -b）・vitest・eslint がすべて CI 上で休眠中**
    2. **backend**: `tests/test_novel_db_search_summary.py::test_search_book_summaries_handles_empty_table` が Ollama embed API へ実接続して Connection refused（1 failed / 847 passed。外部依存が CI に漏れているテスト）
    3. **docs**: failure（原因未特定・要調査）
    - → **2026-07-06 修理計画書を作成済み（実装は未着手）**: [CI修理計画書](CI修理計画書.md)。3 事象とも根本原因特定済み（docs は `generate_file_map.py` の `.coverage` 除外漏れ、frontend は ERESOLVE が 2 段重ね）。実行者への指示文つき自己完結形式

**2026-07-04 コードレビュー由来（詳細は上記と同じ調査）:**

- **P1**: TanStack Query 未使用の生 fetch/useEffect パターンが8箇所に存在（`useBookImages`, `useCharacterGraph`, `useNovelManage.fetchBooks`, `useBookDetail`/`useCharacterDetail`/`useNovelDetailData`/`useDiscussion`/`useNovelDbSearch`）。キャッシュ非共有・エラー握りつぶしに加え `useBookImages` はレース条件（旧リクエストが新状態を上書き）の実害あり → 順次 `useQuery`化
- **P2**: `services/meta_store.py` の `save_meta`/`update_meta_locked` が1フィールド更新でも source 全書籍を無条件 UPSERT（O(N)）。書籍数が多いライブラリでは1回の編集がO(N)書き込みになる → 差分検出 or `executemany` バッチ化
- **P2**: `services/meta_db.py` が API 呼び出しのたびに新規 SQLite 接続 + `PRAGMA` 再実行。`with connect() as conn:` パターンはコミット/ロールバックのみでクローズされず接続がリークし続ける → 接続の使い回し設計を検討
- **P2**: `services/hitomi/state_store.py`/`watchlist.py` の `json.dump` 直書きが atomic write（一時ファイル+`os.replace`）未対応。書き込み中のクラッシュでJSONが破損しうる → 3箇所共通のヘルパー化とセットで対応
- **P2**: `services/novel_db/full_builder.py:_run_generate_contexts` がチャンク毎に冗長 JOIN クエリ + `embed_batch` を要素数1で都度呼び出し + 都度 `conn.commit()`（N+1寄り）→ バッチ化
- **P3（要ADR）**: `services/novel_db/`・`services/meta_db.py` 全体で `models.py` に SQLModel 定義があるが実運用は生 sqlite3 + 文字列 SQL。意図的な設計であれば ADR として明文化し、以後の「規約違反っぽいが実は意図通り」の再調査コストを省く
- **P3**: `services/hitomi/nozomi.py`/`metadata.py` が同期 `httpx.Client` でネットワーク I/O（async規約から外れるが呼び出し元も同期のため実害小）→ 将来的な非同期化を検討

---

### スキップ判断（参考保持）

履歴に詳細記録あり、再検討時の参照ポイント:

| 項目 | スキップ理由 | 履歴参照 |
|---|---|---|
| Phase 75: `BaseSummarizer` 基底クラス化 | grill-me（2026-05-15）でコード分析した結果、共通部分は `_BACKEND = build_qwen_backend()` の 1 行のみ。Phase 74 完了後はその行も消えるため ROI ゼロ。`summarizer.py` は map-reduce あり、`character_summarizer.py` は DB 管理主体で設計思想が全く異なる。Phase 60 の判断を踏襲 | — |
| Phase 52-3: `useRadioMode<T>` 化 | 各 Dialog で reset 内容が大きく異なり、抽象化コスト > 利得 | [履歴 § Phase 52-3](../../archive/リファクタリング履歴.md) |
| Phase 53: 中位ファイル分割 | `ReaderPanel.tsx` / `series_resolver.py` / `pdf_generator.py` の 200〜296 行は許容範囲 | [履歴 § Phase 53（スキップ判断）](../../archive/リファクタリング履歴.md) |
| Phase 54-9: `response_model` 統一 | OpenAPI スキーマ運用要件が出るまで保留 | [履歴 § Phase 54-9](../../archive/リファクタリング履歴.md) |
| Phase 54-10: 日時取り扱いポリシー統一 | 用途が違うため統一動機が薄い | [履歴 § Phase 54-10](../../archive/リファクタリング履歴.md) |
| Phase 54-11: `app.exception_handler` ファクトリ化 | 宣言的に並べた方が読みやすい | [履歴 § Phase 54-11](../../archive/リファクタリング履歴.md) |
| Phase 55-5: `bench_llm_backend.py` 配置見直し | `scripts/` との同居が許容範囲。再実行場面が未確定 | [履歴 § Phase 55](../../archive/リファクタリング履歴.md) |
| Phase 55-10: `ReaderPanel.tsx` 分割 | B-16 着手時に再評価。現状で困っていない | [履歴 § Phase 55](../../archive/リファクタリング履歴.md) |
| Phase 56-5: `JobMode` Literal 旧名撤去 | 既存 DB の旧モード履歴を事前調査してから判断 | [履歴 § Phase 56](../../archive/リファクタリング履歴.md) |
| Phase 56-18: 折りたたみカード共通化 | 内側差異が大きく、次の履歴系コンポーネント追加時に再判定 | [履歴 § Phase 56](../../archive/リファクタリング履歴.md) |

---

## 対象外（現状維持）

リスク・工数に対してメリットが小さい、または現行設計で十分な項目。

| 項目 | 理由 |
|---|---|
| Phase 76: `axios` → `fetch` 移行 | grill-me（2026-05-15）で確認。`apiClient` インターフェースを維持する限り fetch に変えても「内部で fetch を呼ぶ axios 相当のラッパー」になるだけで実質的利得なし。動機も「モダン化」のみで弱い。axios に脆弱性が出た時点で再検討 |
| Pydantic ↔ TS 型自動生成（openapi-typescript 等） | 工数大。手書き同期で運用継続 |
| `pdf_generator.execute_moves` と `file_manager` のロールバック共通化 | 対象（処理ファイル種・タイミング）が異なり、共通化メリット小 |
| `GEMMA_TOOL_DIR` の Windows パス依存デフォルト値 | Phase 8 で `.env` で上書き可能。Linux/Mac 開発予定なし |
| SearXNG → DLsite 直接検索 | 既知の問題として記録済み（Phase 12 時点で対象外維持） |
| `useLibraryManagement` のリネーム | 影響範囲が広い割に効果小 |
| `types/index.ts` の `BookMetaEntry` 拡張 | サークル名のみ管理の現行設計で十分 |
| API バージョニング | 現状シングルクライアント構成のため不要 |

---

## 新規 Phase 案テンプレート

新規 Phase 案が浮かんだら以下フォーマットで「未着手・追加候補」配下に追記する：

````markdown
### Phase NN-X — <タイトル>（P1/P2/P3）

**問題**: <現状のどこが/なぜ困っているか>
**改善案**: <具体的な変更方針。コード例があれば短く>
**変更対象**: <ファイルパスのリスト>
**新規作成**: <あれば>
**完了条件**: <テスト通過など>
````

実装完了後は本文書から削除し、[リファクタリング履歴.md](../../archive/リファクタリング履歴.md) に「実績」を加えて移管する。

---

## 関連ドキュメント

- [リファクタリング履歴.md](../../archive/リファクタリング履歴.md) — 完了済み Phase 1〜92 + TM-1 の詳細記録
- [変更履歴.md](../変更履歴.md) — 全変更の時系列ログ
