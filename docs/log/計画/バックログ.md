# バックログ（機能追加候補）

最終更新: 2026-08-22（B-37固定storage復旧完了、残る外部ゲートと次作業を再整理）

将来の機能追加候補を整理した棚卸しリスト。**個人 LAN ソロツール / 同人誌特化** という制約下で何を作り、何を作らないかを明示する。

> 内部構造改善・依存更新・DB/インフラ移行は本ファイルではなく [リファクタリング計画書](リファクタリング計画書.md) へ。

優先度の判断基準:
- **A（高）**: 新方針の核機能。すぐに着手する
- **B（中）**: 工数中〜大、設計の見直しが必要、一定の利得あり
- **C（低）**: 工数大、利得が中程度、要望が出てから検討
- **対象外**: 信頼モデル / 同人誌特化方針と整合しない、または重複機能で意図的に排除

小説 RAG 関連の検討時は実機ベンチマーク・モデル選定の知見を [小説RAG_技術知見.md](../技術知見/小説RAG_技術知見.md) に蓄積している。判断材料として参照する。

---

## 実装済み機能の履歴

実装内容の詳細・設計判断・実機ベンチマークは [変更履歴.md](../変更履歴.md) と各設計書を参照。

| ID | 機能 | 完了日 |
|---|---|---|
| A-1 | AI 提案：既存シリーズへの紐付け候補 | 2026-05-09 |
| A-2 | 同サークル次の作品ジャンプ（リーダー内） | 2026-05-09 |
| A-3 | 見開き原稿（センターカラー等）の表示崩れ修正 | 2026-05-09 |
| B-3 | PDF のページ並び替え（@dnd-kit、即時保存） | 2026-05-09 |
| B-5 | 小説 RAG: 書籍サマリ事前生成（俯瞰質問の品質向上） | 2026-05-10 |
| B-6 | 小説 RAG: ローカル `num_ctx` 拡大による map-reduce 廃止 | 2026-05-10 |
| B-8 | 小説 RAG: 書籍サマリを検索インデックスに追加 | 2026-05-10 |
| B-9 | 小説 RAG: Contextual Retrieval（チャンクごとの文脈付与、gemma4:e4b 採用） | 2026-05-11 |
| B-11 | 小説 RAG: Query Expansion（質問前処理、gemma4:e4b、デフォルト ON） | 2026-05-11 |
| B-12 | 小説 RAG: Qwen 35B の高速化（IQ4_XS 量子化への切替） | 2026-05-11 |
| B-13 | 小説 RAG: QA の大量コンテキスト読み込み（段階 A → B → C、scope=book で全 page 読み既定有効化） | 2026-05-11 |
| B-14 | 小説 RAG: Qwen 推論バックエンドを llama-server に切替（応答時間 5× 短縮、[ADR-0009](../../design/基本設計/ADR/0009_llm-backend-llama-server.md)） | 2026-05-11 |
| B-15 | 小説 RAG: キャラクター辞典（書籍 × キャラ単位の人物像サマリ + フロント登場人物セクション + 主要シーン top 5） | 2026-05-12 |
| B-16 | 小説 RAG: マルチターン会話 QA（qa_sessions / qa_messages + ChatGPT 風 UI + local_llm.stream_chat） | 2026-05-12 |
| B-20 | 読書会ディスカッション生成（小説 × 2 ペルソナ SSE ストリーミング + JSON 保存） | 2026-05-13 |
| B-21 | 小説 DB: 作者・シリーズ UI 管理（グループ表示 + 一括操作） | 2026-05-13 |
| B-22 | Build 画面 進捗詳細化（`current_detail` カラム + スピナー + DetailLabel） | 2026-05-13 |
| B-23 | Full Build Step 3（Contextual Retrieval）を `generate_contexts` モードとして分離 | 2026-05-13 |
| B-24 | Build + OCR 管理画面集約（`/novel/manage`、タブ統合 + 全ジョブ履歴セクション） | 2026-05-13 |
| B-19 | 小説 RAG: 書籍類似度推薦（サマリ embedding KNN、`find_similar_books` + 書籍詳細ページ表示。`027533c`。棚卸しで実装済みと判明し 2026-07-04 に履歴へ移動） | 2026-05-17 |
| C-12 | 小説 RAG: キャラクター関係グラフ（vis-network、`/novel/graph`、登場人物関係 + エッジラベル可視化） | 2026-05-17 |
| C-8 | E2Eテスト導入（Playwright、golden path 8 scenario + CI用script） | 2026-06-05 |
| A-4 | Kindle 購入カタログと画像取り込みの統合（専用SQLite、差分取込、ASIN紐付け、capture agent、原子的登録） | 2026-07-25 |
| B-33 | Kindle 購入カタログ画面のUI/UX改善（固有URL付き4ページ、URL同期検索、左右比較紐付け、月次取込分離） | 2026-07-25 |
| B-27 | Kindle新アプリの漫画・小説キャプチャ完全対応（本文領域、見開き安全幅、実機再撮影・置換） | 2026-07-26 |
| B-34 | Kindle自動撮影取込（本人照合、未DL待機、直接先頭移動、全画面撮影、原子的登録・置換） | 2026-07-26 |

---

## A. 対応決定（高優先）

現在、対応決定済みの未完了項目はない。

## B. 中規模の機能追加（中優先・中工数）

### B-26. 書籍類似度推薦（novel 専用）

**動機**: 読書会で「この本に似ている」という表現を自然に使えるようにしたい。書籍詳細ページから直接「次に読む本」を探せる体験を作る。

**詳細要件**: [書籍類似度推薦_要件.md](../../design/要件定義/書籍類似度推薦_要件.md)

**概要**:
- 類似度: bge-m3 全文 embedding + サマリ embedding + タグ/著者メタ情報の 3 軸ハイブリッド（均等重み、調整可）
- 表示: 書籍詳細ページ下部パネル、閾値 cos ≥ 0.7 の最大 5 件
- 永続化: `book_similarities` テーブルにプリコンピュート
- トリガー: 管理画面から手動バッチ実行
- 対象: novel のみ（comic / doujin は対象外）

**工数**: 中〜大。DB スキーマ変更・バッチ処理・新規 API 2 本・フロントエンドコンポーネント追加

### B-28. 読書会ロングフォーム化 Phase 1 — 技術実装済み・主観受入待ち

**動機**: 既存の読書会ディスカッション生成（B-20）は本の内容をだらだら話すだけで「読む気が起きない」というユーザー評価だった。10 分尺相当の構成化された番組台本にし、固定の司会キャラ（レイ＆ミオ）を本を跨いで使い回せるようにしたい。「やる夫スレ／ゆっくり解説」的な、キャラクターが本について語る動画コンテンツの土台を作る。

**詳細要件**: [読書会ロングフォーム拡張_要件.md](../../design/要件定義/読書会ロングフォーム拡張_要件.md)（Phase 0a/0b 技術検証済み、ホストキャラ設定・関係性・境界線確定済み）

**概要**:
- 番組構成テンプレート（OP フック → テーマ 2 本 → 脱線コーナー → 締め）をプロンプトに導入し、セグメント別ターン数指定で 10 分尺（3,000〜4,500 字）に到達させる
- ホストキャラ（レイ＆ミオ）の人格核（属性・得意分野・口調ベース）を永続データ化し、本ごとの推し解釈・スタンスのみ都度生成する設計に分離
- ボケ・ツッコミ役割をテーマごとに入れ替える会話ルールを追加
- マーカーパーサの寛容化（`[A>:` 等のドリフト許容）、中国語/英語リーク対策
- 生成バックエンドはローカル LLM（Qwen）を既定としつつ、有償 LLM API への切替も可能な設計にしておく（品質比較は別途実施）
- **UI（要件メモ §4.10 確定）**: 読書会ページを番組台本生成に置き換え（自由ペルソナ UI・往復数スライダーは廃止、旧履歴は閲覧可能のまま）。セグメント見出し付き区切り表示・クリップボードコピー + Markdown ダウンロード・機械チェック合否バッジ + 不合格理由 + 再生成ボタン
- **保存スキーマ / API 契約（要件メモ §4.11 確定）**: 保存 JSON v2（turn 単位 `segment` フィールド + キャラ設定スナップショット埋め込み + `checks` + `format_version`）。generate API は `{book_name}` のみの破壊的変更、SSE に `segment` / `checks` イベント追加、履歴は v1/v2 互換読み、DELETE API 新設（パストラバーサル対策必須）。全保存 + 削除ボタン方式

**DoD**: 要件メモ §10 に確定済み。機械チェック 5 項目（字数 3,000〜4,500 / 5 セグメント出現 / 話者分割 100% / 言語リーク 0 / ネタカード言及）を恒常的な合否ゲートとして実装し、「後宮の烏」1 冊で層1 全合格 + ユーザー主観合格（笑える・へえ箇所 3+ / キャラのブレなし）の台本が 1 本生成できれば完了

**現在地（2026-08-22再監査）**: 2026-07-07にAPI、保存JSON v2、固定ホスト、
機械check、UI、export、履歴削除とPlaywright/Vitest回帰を実装済みである。
「ふつつかな悪女」では層1を全合格した。残作業はユーザー主観DoDの1回だけであり、
新規実装として扱わない。主観合格なら実装済み履歴へ移し、不合格なら具体的な評価項目だけを
別改善として起票する。

**残工数**: 小。既存合格台本または同条件の再生成1件を主観評価する。

### B-29. 読書会ロングフォーム化 Phase 2（類似本トーク連携）

**動機**: 脱線コーナーで「似た本」を引き合いに出した比較トークを自然に挟みたい。B-28 の脱線コーナーに接続する後続タスク。

**詳細要件**: [読書会ロングフォーム拡張_要件.md](../../design/要件定義/読書会ロングフォーム拡張_要件.md) §4.2

**概要**: 既存 `find_similar_books`（B-19、実装済み）+ `books.summary` を脱線ネタカード生成に接続。蔵書外の本は LLM 一般知識で補い、事実として扱わない体裁にする。B-26（ハイブリッド類似度フル版）は前提にしない

**工数**: 小〜中。B-28 完了後の結線が中心

### B-30. 読書会ロングフォーム化 Phase 3（時事ネタ・教養小話の密度強化）

**動機**: 時事ネタ・過去の流行・教養小話を高密度で自然に混ぜたい。レイ（日本史・英語）・ミオ（世界史・数学）の得意分野を脱線の資源として活用する。

**詳細要件**: [読書会ロングフォーム拡張_要件.md](../../design/要件定義/読書会ロングフォーム拡張_要件.md) §4.3, §4.7

**概要**: 脱線ネタカードをその場 1 回生成ではなく、専用の下準備ステップで複数候補を先出し→選別する二段構成を検討。ネタカードに正確な固有情報を同梱してハルシネーション抑制（Phase 0b で「薬屋の主人公名」誤称の実例あり）

**工数**: 中。ネタカード生成ステップの切り出し要否は B-28 の実測次第

### B-31. 重複作品検出（doujin）— 検討段階

**動機**: フォルダ監視自動取り込み（2026-07-04 導入）は同一 ZIP の二重投入を防げない。重複に気づくのは閲覧時で、削除の手間と閲覧メタの分散が起きる

**案**: 取り込み時に表紙（または先頭数ページ）の perceptual hash を算出して保存し、既存作品との一致・近似を検知したら警告（取り込み自体はブロックしない）。既存ライブラリへの一括バックフィルもセットで必要

**工数**: 中

**状態**: 検討（2026-07-07 起票。閾値・警告 UI の要件は未確定）

### B-32. キャプチャ品質チェック（comic / novel）— 進行中

**動機**: Kindle キャプチャの重複ページ・真っ白ページ・欠落は、現状 OCR / RAG 構築後や閲覧時に発覚し手戻りが大きい

**案**: キャプチャ直後（または取り込み時）に、隣接ページの画像ハッシュ比較で重複を、輝度ヒストグラムで白紙を検知してレポート。novel は OCR 前段のゲートとして組み込む

**工数**: 中

**状態**: 進行中（2026-08-22 warning永続化・確認導線・短時間右下通知・シリーズ画面数外れ値まで実装し、独立人手確認も合格。実陽性holdout待ち）

- Phase 1: 撮影完了証跡付きmanifest v2、登録前の連番・復号・寸法・hash・件数の
  独立再検証を厳格ゲートとして導入する。
- 完全重複、低容量、白紙・疎な画面は既存63冊・8,258画面を含む実画像で
  shadow計測し、正当な章扉・挿絵・奥付を除外できるまでは警告に留める。
- 撮影前カナリアとセッションサーキットブレーカーは実装済み。シリーズ実行では
  Kindle復旧を全冊で最大1回に制限し、エラー種別を問わず最初のfailed jobで後続jobを作らない。
  連続`download_failed`上限3冊はセッション状態へ記録するが、2冊目までを自動継続する制御は
  未実装である。セッション状態は対象manifest digest付きのatomic JSONへ保存する。
- 2026-08-02 shadow検証: ローカル既存画像4冊を対象に392画面を検査し、3冊は
  厳格条件を通過した。旧方式1冊はページ画像と同じディレクトリに
  `008_debug.json` / `008_debug_vis.png`があり、意図どおり厳格拒否した。
  白紙・疎7件、低容量15件、完全重複2組は正常ページの可能性を残す警告となった。
  OneDrive上の全件検査はI/Oが2分を超えたため、ローカルまたはサーバー側での
  バッチ監査として継続する。
- Phase 4: 隣接dHash近似重複と小説上下端の内容密度を警告専用で実装し、Codex画像QAの
  優先ページ抽出へ利用する。未調整holdoutで再現率・適合率を確定するまでは自動拒否しない。
  同じ392画面のshadow再評価では近似重複4組、端切れ候補1画面を抽出した。いずれも
  警告であり、原画像QAによる真偽確認を残す。
- 反復画面オーバーレイは、実機汚染packageと正常な再撮影packageで校正済み。最大32ページの
  外周を比較し、隣接2タイル以上が標本の50%以上で反復する高信頼候補だけをblockingへ昇格した。
  それ未満の候補はwarningとし、通知文言・OS種類に依存しない画素構造で判定する。
- private manifestの画像SHAと確定labelを照合し、完全重複・低容量・白紙・近似重複・端切れ等の
  code別TP/FP/FN・precision・recallを出す非UI評価器は実装済み。2026-08-22に検出結果を見ない
  family hash選定で4冊・524画面を封印し、Codex画像QAと実測を完了した。実画像の欠陥labelは0件、
  raw warning 26件はすべて正常候補で、制御故障5 codeは各TP 1 / FN 0だった。このためraw warningは
  blocking化せず、将来UIも登録成功後の「要確認候補」へ限定する。プロジェクトオーナーは
  contact sheet全35枚を独立確認し、欠陥なしと判断したため`human_confirmation=confirmed`とした。
  残作業は実陽性を含む追加holdoutである。短時間の右下UI混入は、実陽性2種と正常727画面で
  校正した連続3画面検出をwarning-onlyで実装済みとする。単一・2画面、右下以外、移動するUIは
  引き続き未検出である。warning証跡の監査世代保存、
  既読状態、ページ導線は実装済み。シリーズ内画面数外れ値は、同sourceの先行3冊以上の
  中央値に対して0.5倍未満または2倍超をwarningとし、session stateへ根拠付きで保存する。
  併せて、
  `download_failed`は、2026-08-22の運用判断により最初の失敗でシリーズ全体を停止する現行挙動を
  正式仕様とした。失敗巻だけを隔離・間隔付きで後続継続する方式は将来候補として残すが、
  再試行間隔、最大継続数、恒久的な非対応書籍の除外、resume整合性を設計・検証してから別変更で
  導入する。他ASINの未完了jobを含む全体active判定と開始操作の事前無効化は実装済み。

### B-2. メタデータの一括インポート（CSV / JSON）

**動機**: 現在エクスポートは `/api/meta/export` で実装済みだが、インポートが無い。他環境から移行する手段が無い

**案**: `/api/meta/import` を新設し、JSON ファイルアップロード → 既存メタとマージ（衝突解決ポリシーは upsert / overwrite / skip 選択）

**工数**: 中。バリデーション + マージロジック + UI

### B-4. キーボードショートカット カスタマイズ

**動機**: 現在のショートカットは固定。利用頻度や好みに応じてカスタマイズできない

**案**: localStorage に key-action マップを保存。設定画面で編集

**工数**: 中。useReaderShortcuts の汎化が必要

### B-17. 漫画ソースへの画像キャプション検索（パイロット先行）

**動機**: ローカル VL モデルを活かして、絵の内容を自然言語で検索できるようにする。同人誌特化ツールの目玉機能候補。クラウドを使わずローカル完結で性的内容を扱える点が方針と整合（旧「AI タグ自動生成」とは異なり、自由文クエリ前提）。

**案**:
- ~~**Step 1 パイロット**: 1 冊（20〜30 頁）を Gemma4:e4b で全頁キャプション → 品質判定~~
- ~~**Step 2 比較**（任意）: Qwen-VL 系との比較~~
- **Step 3 本実装**: `pages.caption TEXT` + `pages_caption_vec USING vec0(embedding FLOAT[1024])`（bge-m3 で再利用）+ バッチ CLI + 検索エンドポイント + 検索 UI

**パイロット結果（2026-05-14 完了）**: 詳細 → `docs/archive/検証/漫画AI検証.md`

| モデル | T1 シーン | T2 パネル | T3 ストーリー | T4 OCR | T5 キャラ |
|---|:---:|:---:|:---:|:---:|:---:|
| gemma4:e4b | △ | ◯ | × | ×（30%） | △ |
| **qwen3-vl:8b** | **◯** | **◯** | **△** | **◯（4/5）** | **△** |

**採用モデル**: `qwen3-vl:8b`（OCR 精度が 30% → 80%+ に改善）

**技術的注意点**:
- qwen3-vl:8b は Ollama の `images` 配列に複数画像を渡すと **空文字列を返す（サイレント失敗）**
- 複数ページ送信は **PIL 縦連結で 1 枚 PNG にまとめる**回避策で対応済み
- 1 ページあたり処理時間の計測は Step 3 着手時に行う

**規模試算**: 1 冊平均 25 頁仮定で約 12,100 頁。qwen3-vl:8b の速度は実測次第。許容できなければサンプル頁（表紙 + 中扉 + 数頁）モードに切替

**工数**: Step 3 本実装 中

**判断**: **保留**（2026-06-12）: 現状の qwen3-vl:8b の品質が期待を下回るため現状は対応しない。ローカル VLM の精度向上（新モデル等）を確認した段階で再判断する。

### B-18. 小説 RAG: リーダー統合「ここまでのあらすじ」生成（優先度低）

**動機**: 続巻を読み始める前や、間が空いたときに「ここまで何があったっけ」を即座に思い出したい。Qwen 131k ctx で 1 冊丸ごと読めるので、任意ページまでの本文を input に「現時点までのあらすじ」を生成できる。

**案**:
- 小説リーダーに「ここまでのあらすじを生成」ボタン
- `GET /api/novel_db/books/{name}/recap?up_to_page=N` 新設、1〜N ページの本文を Qwen に投入し 800〜1500 字で要約
- 結果は `book_recaps(book_id, up_to_page, text, generated_at)` にキャッシュ（同じ `up_to_page` での再要求はキャッシュ返却）
- 既存 `summarizer.py`（B-5）が骨格としてほぼ流用可能

**工数**: 小〜中

### B-25. meta.db クラウド同期（SQLite Online Backup API + ファイルストレージ）

**動機**: 閲覧回数・ジャンル・シリーズ等の書誌メタを複数端末で共有したい。SQLite は WAL モードで頻繁に書き込みが発生するため、ライブファイルを OneDrive に置くと同期競合でDB破損リスクがある。

**方針**: `sqlite3.backup()`（SQLite Online Backup API）でスナップショットを安全に作成し、そのファイルだけを OneDrive / S3 等に配置する。ライブDB は常にローカルのまま。

**案**:
- `GET /api/meta/backup` — `sqlite3.backup()` でスナップショット（`meta.backup.db`）を作成し、指定ディレクトリに保存
- `POST /api/meta/restore` — 指定パスの `meta.backup.db` を取り込んで現在の DB に upsert マージ（衝突時は `last_viewed_at` が新しい方を優先）
- バックアップ先は env `META_BACKUP_DIR` で設定（OneDrive の共有パスを指定）
- Windows タスクスケジューラで定期実行（例: 1 日 1 回 `POST /api/meta/backup`）

**なぜ DynamoDB でなく SQLite backup か**:
- データモデルの移行が不要（SQLite のままファイルコピー）
- 追加クラウド費用ゼロ（OneDrive / ローカル NAS に置くだけ）
- `sqlite3.backup()` は API 標準なのでロック競合なし

**工数**: 小。`sqlite3.backup()` の呼び出し 1 関数 + API エンドポイント 2 本 + タスクスケジューラ登録手順

**関連**: 前段となるローカル自動バックアップは実装済み。Linux 本番では
`backup-meta.timer` による日次世代バックアップ、`integrity_check` /
LanceDB 読み取り、`backup-restore-test.timer` による別ディレクトリ復元試験を
運用している。B-25 はこの安全なローカル世代をクラウドへ複製・復元する
追加体験として扱う。

### B-37. 小説RAG page-level LanceDB ICU検索への段階移行 — shadow安定稼働・本番切替保留

**動機**: 固定20問では現行FTS5 queryが18/20問で0-hitとなった一方、隔離page-level
LanceDB ICU BM25はR@10 `.886`、MRR `.720`、nDCG `.708`、個別R@10回帰0件だった。
検索結果というユーザー体験を改善できるが、現行LanceDB snapshotには重複32行とSQLiteからの
ID欠落2,781件があるため、そのまま切り替えない。

**対象と完了条件**:

1. `index_eligible=1`の全pageから新しい隔離table / ICU FTS indexを原子的に構築する。検証snapshotでは
   8,576ページだが固定値にせず、build時の実対象件数をSQLiteから再計算する。
2. unique page ID、件数、重複0件、source SQLite SHA-256、tokenizer設定、build時刻をmanifestへ記録し、
   SQLiteとの不一致時は公開しない。
3. 現行検索とのshadow dual-readで固定20問の個別R@10非劣化とAPI response互換を確認する。最終切替前には、
   可能なら検索結果を見ていない未調整holdoutも追加する。
4. config / feature switchで段階導入し、LanceDB障害・不整合時は現行FTS5へ縮退する。旧indexを保持し、
   1操作でrollbackできることを障害注入testで確認する。
5. 既存API型、`rrf_score`、scope、page filter契約を変えず、backend全体と検索API回帰testを通過した後だけ
   本番既定値を切り替える。

**対象外**: Nemotron等へのdense置換、Qwen3 reranker、PPLX、Qwen3 Embedding、Harrierの本番配線は
この項目へ含めない。現fixtureではlexical候補へ新しい正解ページを追加しなかったため、別の正解集合で
追加価値が確認できるまで比較候補に留める。

**2026-08-22実装範囲**: SQLite revisionとactive pointer、世代別完全再構築、件数・ID・hash・
index統計のfail-closed検査、`fts5 / shadow / lance_icu`切替、stale検知、FTS5 fallback、障害注入test、
構築CLIまで完了した。初期既定値は`fts5`のままで、`shadow`もレスポンス順位を変更しない。
固定snapshot 8,576ページでは旧ICU順位と20/20完全一致、未索引0、canonical検索p95 13.239msを確認した。
残作業を含む実施順は次のとおり。

1. **完了**: LanceDBの省略時pathをversion付きbackend世代外へ固定し、active ICU tableを
   release smokeで開けない世代は切替前に拒否する。clean generation
   `backend-20260822163430-62582`をactive化し、分離していたdense 2 tableも共通lock下で固定pathへ統合した。
   現在は`chunks=3,489` / `summaries=18` / page ICU `8,576`、制御shadowは成功5 / fallback 0、
   復旧後snapshotの別directory復元試験も合格している。
2. **完了**: `/etc/systemd/system/backup-restore-test.service`を配置済みの共通`flock`版へ更新し、
   `daemon-reload`後の2026-08-22 21:24:59 JSTに手動復元試験を実行した。`ExecMainStatus=0`、
   `Result=success`、timerの`enabled / active`を確認し、repository配置元とsystemd配置先のSHA-256も
   `a26b5594328d29776b74b332bb6bed648f36a2da6c8f13c030311c39a862cf96`で一致した。
3. **完了**: FTS5同点時はcanonical `pages.id`昇順で決定的に並べ、production検索・評価CLI・
   回帰testを同じ契約へ統一してshadow比較のcross-run揺れを除く。
4. **集計基盤完了・実運用観測待ち**: production logからshadow成功率、fallback、FTS5 / ICU latency、
   0-hit、top集合Jaccardをquery本文・hash一覧なしでJSON集計するCLIを追加した。smokeと障害注入の
   分離集計は合格したが、その後の実利用queryは0件で終了code 2のため、観測ゲートは未合格。
5. **完了**: commit `89fc93e`で評価前封印した別3冊12問を一度だけ開封した。FTS5は12 / 12 0-hit、
   ICUはRecall@10 `.792`、MRR `.833`、nDCG `.748`、0-hit 0件、p95 3.486ms、個別Recall@10回帰0件で
   採用ゲート合格。正解が12位の1問を含め、同じholdoutで調整・再採用判定しない。
6. **別承認待ち**: 2と4の完了後、別commit・別承認でproduction既定値を`lance_icu`へ切り替え、
   障害注入とFTS5 rollbackを再確認する。自動昇格はしない。

B-37の実装・復旧・root unit・holdoutは完了した。残る順序は4（実利用queryの運用観測）、
6（利用者承認を伴う既定値切替）である。TM-4の設計ドリフト解消は完了したため、
外部ゲートを待つ間の次の自律実行対象は[B-32](#b-32-comic-novel)とする。

**正本**: [小説RAG 検索・QA設計 §10](../../design/詳細設計/機能別/小説RAG_検索QA設計.md#10-日本語検索基盤の比較検証ゲート)、[ADR-0020](../../design/基本設計/ADR/0020_page-level-lancedb-icu-shadow.md)

## C. 大規模 / 要望待ち（低優先・大工数）

### C-1. 表紙ザッピングモード

**動機**: 「今日は何を読もう」状態を解消したい。表紙だけを次々送って気分の本を探すモード

**案**: フルスクリーンで表紙のみを表示し、左右キーでザッピング → クリックで開く

**工数**: 中〜大。新しい閲覧モードの追加

**判断**: 現状のサムネイル一覧で実質的にザッピングできているため、優先度低のまま保留

### C-2. DLsite 作品名検索によるキーワードタグ取得

**動機**: 作品 ID は基本的に取れないが、作品名で公開ページを検索できればキーワードタグを取得できる可能性がある

**案**: タイトル → DLsite / Fanza の公開検索ページ → 候補提示 → ユーザー確認 → タグ反映

**工数**: 中〜大。スクレイピング + 候補提示 UI + 精度評価

**判断**: 実用精度が出る見込みが立った時点で着手判断。タグ機能は撤去済みのため、復活時に併せて検討

### C-3. PWA 対応 / オフライン閲覧

**動機**: PC を移動する際にライブラリをローカルにキャッシュしたい

**案**: Service Worker でメタデータと一部 PDF をオフラインキャッシュ

**工数**: 大


### B-35. 小説OCRの正解コーパス・ページ種別・第三判定 — 継続中

**動機**: 構造・coverage上は合格した通常散文にも、縦列欠落、反復、固有名詞の
共通誤読が残る。OCR完走や候補一致と、文字単位の正しさを分離して判定する必要がある。

**現在地**:

- ページ種別、QA承認、補正文採用元、原子的公開、固定コーパス複合ゲートは実装済み。
- 第三OCR、oracle、PARSeq追加学習pilotは診断済みだが、単独0.5%に未達。
- 3シリーズ30画面では機械合議の集計CER 0.474%に達したが、ページ最大3.470%、
  固有名詞529/536のため総合ゲートは `FAIL`。
- Codex画像QA込みの結果は運用品質として合格したが、機械性能やCodex省略の根拠にはしない。
- 正式holdoutの3シリーズ、通常散文20画面、再開封拒否、全package digestは
  `b35-holdout-v1` manifestと開封台帳でfail-closed強制済み。
- 2026-08-17に新規3シリーズ30画面を一度だけ開封し、現行primaryは通常散文CER
  27.4038%、ページ最大496.7509%、列欠落疑い3画面、固有名詞324/430で総合 `FAIL`。
  最大外れ値を除いてもCER 3.8608%であり、同じholdoutは再評価しない。
- API表示用CERはAlembic 0012のページcacheへ増分化し、B-35 benchmarkのNFKC正規化と
  非互換であることをテスト・設計で固定した。
- 開封後の調整用診断では、既存の全文性QA条件を候補選択へ共有した固定版が通常散文CER
  2.1141%まで改善した。MangaOCR追加後の3候補合議は2.4693%、正解参照oracleも1.4761%で、
  現候補集合だけでは0.5%へ届かない。追加閾値で同じholdoutへ過学習せず、新holdoutへ進む。
- 公開最新版NDLOCR-Lite v1.2.3は単体CER 3.1832%、primary・externalとの文字合議1.1174%、
  列欠落疑い0画面だった。primary異常反復からexternalへの必須QAフォールバックは実装済みだが、
  ページ最大5.3345%と固有名詞誤りが残るため、NDLOCR常時追加だけでは正式候補にしない。
- NDLOCR行bboxによるSurya部分再OCRは最悪ページ3.1769%まで改善したが、別難例38.6997%と
  不安定なためQA補助に限定する。Surya requestの`seed=0`固定後は同一候補本文が3回一致した。
- 公式シリーズ台帳と同位置の独立OCR一致を必須とする固有名詞補正は、転記errata適用後の
  調整値でCER 1.1346%から1.0622%、固有名詞再現率91.8605%から96.0465%へ改善した。
  21補正・10画面は全件改善したが、ページ最大5.3345%は変わらず総合 `FAIL`。
- PP-OCRv6 mediumは最大CER画面で全文CER 33.6462%、NDLOCR行bbox再利用でも11.6968%で、
  現行合議5.3345%を下回るため拡大評価しない。

**残作業**:

1. 候補支持付き固有名詞補正は固定候補として維持し、部分再OCRとPP-OCRv6は自動採用せず、
   小書き文字・約物を保持してページ最大CERを下げる独立候補を作る。
2. 全文性判定を含む固定版について、新しい未調整holdoutを候補品質を見ずに封印し、一度だけ再評価する。
3. 機械総合合格後にのみ、Codex確認縮小の反復・100画面試験へ進む。
4. subprocess、QA DB更新、FTS同期の障害注入で旧公開状態の保持を確認する。

**完了条件**: [小説OCR品質改善 実装計画](小説OCR品質改善_実装計画.md) のH1〜H5を
完了し、正式holdoutで `scripts/maintenance/ocr_quality_policy.json` の全ゲートに
機械候補が合格すること。QA未承認・品質未達・障害時は旧公開本文と索引を保持する。

現在の契約は [OCR設計書](../../design/詳細設計/機能別/OCR設計書.md)、実測経緯は
[OCR品質改善 技術知見](../技術知見/OCR品質改善_技術知見.md) を参照する。

### B-36. 可読性優先プロンプトによる既存要約・人物辞典の夜間再生成とQA — 進行中

**動機**: 2026-07-28に人物説明・書籍要約の固定文字数と固定段落数を廃止したが、
既に保存・公開されている生成物は旧プロンプトのままである。日中のローカルLLM利用を
妨げないよう、再生成は夜間にまとめて実行する。

**対象と手順**:

- 対象書籍と既存の要約・人物辞典を実行前に記録し、失敗時に比較・復旧できるようにする。
- 事実抽出、要約・人物の個別執筆、編集校正、品質ゲートを通る新パイプラインで
  巻別要約と人物辞典を再生成する。OCR本文・ページ索引・画像は変更しない。
- 空出力、生成マーカー混入、根拠のない人物、同一人物の重複を機械検査する。
- 旧版との差分を保存し、主要人物の立場・関係・動機・変化と、要約の因果関係・時系列を
  Codex補助QAで確認する。
- QA不合格の生成物は公開版へ確定せず、旧版を維持する。

**完了条件**: 対象全冊の再生成結果について成否と差分が記録され、機械検査と
Codex補助QAを通過した要約・人物辞典だけが公開データとして確定している。

**2026-07-28 10巻パイロット**:

- 公開版の要約446字・人物10名を退避し、新パイプラインで要約1526字・人物6名を生成した。
  機械品質ゲートは通過したが、Codex補助QAで事実表にない
  「仁耀が黒の皇帝を殺して戦争を誘発しようとする」という行動を検出した。
- `皓茉莉花`→`茉莉花`、`芳子星`→`子星`、`封大虎`→`冬虎皇子`が
  削除・追加として扱われ、同一人物の正規名を維持できなかった。
  `影傑`、`望来現`、`鉦春雪`、`黎天河`も根拠ある除外理由なしに失われた。
- 新版は不採用とし、監査スナップショットから要約446字・人物10名と
  サマリembeddingを復元した。OCR本文・ページ索引・画像は変更していない。
- 全冊再生成の再開前に、(1) 公開版またはシリーズ人物台帳による正規名・別名統合、
  (2) 完成文の各主張を事実表へ照合する意味的根拠検査、
  (3) 正規化後の人物集合について理由のない削除を不合格にする回帰検査を実装する。
  機械的な文章形状検査のPASSだけで公開承認しない。
- M1 Max 64GB上で現行35B-A3B、Qwen3.6-27B Dense、Gemma 4-31Bを
  同じ10巻入力で比較する作業は
  [MacローカルLLM移行・比較計画](MacローカルLLM移行・比較計画.md)へ分離した。
  環境構築と固定比較は2026-08-01〜02を目安とし、それまでは撮影、OCR、
  検索索引構築を継続する。要約・人物辞典の全冊再生成だけを保留する。

**2026-08-01 品質基盤の進捗**:

- 本文を約3万文字のページ境界ブロックへ分け、本文ハッシュ・モデル・抽出仕様で
  再利用判定する構造化事実チェックポイントを実装した。
- 完成要約の全主張をハイブリッドRAGの本文ページへ照合し、事実表から要約への
  重要事実欠落も検査する双方向ゲートを実装した。不合格候補は監査保存して公開しない。
- 検証役は既定で執筆Qwenを直列再利用し、設定によりOllamaまたは独立llama-serverの
  別モデルへ切り替えられる。
- 同じ巻の公開済み人物名を事実抽出の正規名ヒントへ使い、一意な短縮形と事実メモ内で
  明示された別名だけを公開版正規名へ統合した。
- 正規化後も現在本文に根拠がある公開済み人物が新候補から消えた場合は、一括確定を止める
  削除回帰ゲートを実装した。
- 10巻の隔離再パイロットでは構造化事実4ブロックと詳細あらすじの双方向検証が合格した。
  一覧向け短縮要約は主張6について、検証モデルが明示された候補ページ外を引用して安全停止した。
  内容不合格と出力契約違反を区別するため、JSON・主張ID・候補外引用だけを1回訂正させて
  同じ厳格検証へ通す有界再試行を追加した。再試行でも直接根拠ページが文字数上限から落ちる
  問題を確認したため、事実表候補の優先採用を追加した。さらに主張別候補の割当誤差と本文未提示
  ページ引用を区別し、実際に全文提示したページだけを引用許可範囲として再パイロットと
  旧版差分QAを継続する。短縮版で直接候補61の直前ページ60が未提示となった事例を受け、
  候補の前後1ページを補助証拠へ追加し、実測に基づき証拠本文上限を64ページ・90,000文字へ
  拡張する。
- 最終設定では詳細版の機械根拠検証が合格し、直前の短縮版実応答も再生検証で9/9主張が合格した。
  ただしCodex差分QAで、本文上の最終結果「仁耀は最長10年間逃げ続けることに同意」を
  「牢へ戻る」とした重大な矛盾を検出した。構造化事実表自体が両結果を同じ事実へ混在させており、
  今回候補は不採用とする。次の必須作業は、事実表を該当ページ本文へ意味照合するゲート、
  10巻人物辞典の実モデル完走、修正後の旧版差分QAである。
- Qwen3.6 35B-A3B、Gemma4 12B、Gemma4 31Bの固定1件比較では、Gemmaだけが上記の最終行動を
  正しく判定した。主比較対象の31Bは、12Bが見落とした事実メモ内部の矛盾も検出でき、64GB Macで
  実行時20GB・100% GPU、事実監査と校正の合計84秒だった。一方でJSONコードフェンスと校正時の
  軽い意味変更は31Bにも残ったため、現時点では独立第二検証役の主候補に留め、既定モデルは変えない。
  次は正解固定済み10〜20件以上で、事実判定正解率、重大見逃し、JSON契約成功率、意味保存、
  所要時間を同一条件で比較する。
- 10巻の隔離DBで31BをQwenの全役割へ適用したところ、詳細あらすじは889字・28分31秒、一覧用要約は
  490字・22分30秒で完了した。文章は自然で「牢へ戻る」の誤断定を避けたが、最長10年間逃げる
  最終合意を省略し、主体・時制の意味変更も残った。事実抽出、人物辞典、64ページ一括検証は、
  各20分の評価上限内に完了しなかった。Gemmaによる全工程置換は行わず、Qwenを生成の主系統にする。
  次は否定、最終行動、時系列、主語、不可逆な状態変化を含む主張を抽出し、小さな本文窓でGemmaへ
  直列照合するリスクベース検証を固定コーパスで評価する。短縮要約のGemma第二案は任意比較に留める。
- 2026-08-02にCodex Lunaを同じ10巻の隔離入力へ適用した。固定した74〜75ページでは、
  「牢へ戻る」を途中状態、「最長10年間逃げ続ける」を最終状態として正しく判定し、事実メモを
  `partially_contradicted`、誤要約を`contradicted`としたため、固定ケースは合格とした。巻全体では
  事実約30件、詳細版約1,050字、短縮版約560字、人物10名を生成し、主要な否定・時系列・最終状態・
  重要転機を保持した。一方、詳細版・短縮版の拘束主体を「珀陽」と単純化したため、作戦主体と物理的
  実行者（天河）の区別を修正する必要がある。全体の独立検証と役割別時間計測も未実施であり、自己監査だけを
  根拠に公開せず、巻全体は条件付き合格・公開不可と判定した。
- QwenとLunaの役割別所要時間は、今回の保存物から同じ条件で再現可能な数値を取得できなかった。
  次の比較では、入力ハッシュ、プロンプト版、モデル、温度、開始・終了時刻、出力文字数を役割別に記録する。
  それまではQwenを生成の主系統、Codex Lunaを固定ケースと高リスク主張の補助QA候補として扱い、既存の
  公開DB・OCR本文・ページ索引・画像・生成物は変更しない。
- 前回の条件付き合格を再現するため、入力SHA-256、プロンプト版、Schema、工程別開始・終了時刻を隔離JSONへ保存した。
  固定ケースは初回意味判定失敗後の再試行が37.377秒で合格したが、巻全体はfail closedとなった。事実抽出は36件を
  生成したものの許可外actor_role、詳細版は約1,100字で内容は概ね正しいものの契約外ラベル、短縮版は502字で
  主体6件の誤りと帰国後課題の欠落、人物辞典は10名だが影傑・正規名の照合課題、完成文監査は28主張中の主体誤りを
  検出した。Lunaの計測値は事実175.928秒、詳細132.908秒、短縮144.333秒、人物113.770秒、監査157.030秒である。
- Qwenは詳細446字、一覧用は未公開、人物10名、事実4ブロックという既存結果で、役割別時間は保存されていない。
  よってLunaを代替生成・補助初稿・自動公開へ採用せず、固定ケースと少数の高リスク主張を人間確認付きで補助QAする
  候補に限定する。隔離JSONは実験ID`b36-codex-luna-2026-08-02-r2`と入力ハッシュを正本とし、公開DB等へ反映しない。
- Compact復元条件を外部状態契約へ固定し、Lunaへ4ブロックを独立・直接入力した再検証では、
  有界再試行後も機械合格は3/4だった。最終ブロックは最長10年逃走を正しく選んだが、拘束の計画主体と
  末尾網羅に失敗したため、自動経路はfail closedとする。Compact自体は発動しておらず、改善は外部契約、
  新規コンテキスト、OCR直接入力、件数抑制、末尾網羅ゲートの効果として扱う。
- 比較完走用の手動救済事実を明示的に分離してQwen3.6 35B-A3Bへ渡すと、詳細1,637字、一覧529字を
  合計470.43秒で生成した。詳細版は珀陽の作戦と黎天河の物理的拘束を正しく分離し、従来より網羅的だった。
  ただし「最長10年」の断定化、中国字混入、一覧版の拘束実行者省略と戦争回避の過剰確定が残るため公開しない。
  次の実装候補は、全ブロック機械合格、事実表の日本語正規化、期間上限・主体・未確定状態の完成文意味ゲートである。
- 2026-08-02の設計判断として、公開候補の主生成をSolへ寄せ、1冊につき巻全文を渡す主生成1回で
  事実表・詳細あらすじ・一覧用要約・人物辞典を同時に得る。ローカルでSchema、ページ参照、正規名、
  文字数、期間上限、主体役割、未確定状態を機械検査し、否定・最終状態・主体・時系列などの高リスク主張だけを
  原文ページ窓付きの新規Solセッションで独立評価する。合格後も人間承認までは公開版を置換しない。
- 初期実装はMac上のCodex CLIとChatGPTサブスクリプション認証を使う1冊パイロットに限定する。
  APIへの自動切替、並列実行、Compactへの状態依存、認証情報のサーバー複製、失敗時のQwen自動公開を禁止する。
  Qwenは対話QAと外部送信しない代替経路として維持する。詳細は
  [Sol生成・評価設計](../../design/詳細設計/機能別/小説RAG_Sol生成・評価設計.md)と
  [ADR 0018](../../design/基本設計/ADR/0018_sol-primary-post-ocr-generation.md)を正本とする。
- 次の作業は、(1) 10巻固定入力でSol medium/highを同一契約で比較、(2) 10〜20件の固定ケースと3冊で
  重大誤りゼロ・固定ケース100%・機械契約成功率95%以上・人手修正量・総作業時間を測定、
  (3) 合格時に限りCLIランナー、隔離成果物、承認導線を実装する順とする。API、画面、定期バッチ、
  新規DBテーブルはパイロット完了後に別途設計する。
- Sol mediumの10巻1冊主生成は333.657秒で、事実50件、詳細1,326字、一覧406字、人物11名を生成した。
  途中状態と最終状態、期間上限、殺害未遂、戦争回避の未確定性、中国字正規化は合格した。一方、正字を
  断定できない`礎犀輿`を正規名台帳外から人物辞典へ追加したため、ローカルゲートでfail closedとした。
  一覧版の黎天河省略も改善対象とし、別Sol評価と公開DB反映は行っていない。
- 次は人物名をSchema enumへ固定し、未同定人物を`unresolved`へ分離する。拘束の役割契約も、原文に合わせて
  戦術立案=皓茉莉花、指揮・承認=珀陽、物理的実行=黎天河、対象=仁耀へ分割する。修正後のmedium再試行が
  ローカル合格した場合だけ、12件の高リスク主張を別の新規Solセッションで評価する。
- 正規名enumと一覧版の拘束者保持を追加した再生成は、事実48件、詳細1,367字、一覧402字、人物10名で
  ローカルゲートを完全合格した。主生成410.709秒に続き、16件・31ページ窓の別Sol評価を102.854秒で実行した。
  12件は支持されたが、拘束作戦の役割を主生成へ正解として与えたことで、同じ過剰特定が3成果物へ伝播し、
  重大3件となった。外交交渉という工程の過剰具体化も軽微1件あり、独立評価はfail、公開しない。
- 次は固定ケースの期待値を主生成入力から完全に外し、評価fixtureだけへ隔離する。主体役割は複数人物と
  根拠ページを持つ配列へ変更し、原文の「茉莉花たち」を単独立案者へ分解せず、珀陽の指揮・承認も
  明示根拠なしに補わない。汚染除去後のmedium再試行を先に行い、high比較はその結果を見て判断する。
- 固定正解を完全に除去したmedium再試行は、事実表では期間上限と黎天河を保持したが、途中状態／最終状態の
  fact分離、一覧版の期間上限・物理的実行者保持でローカルゲートに不合格となった。独立評価と公開は実施していない。
  mediumの固定ケース向け調整を重ねず、次は同じ中立プロンプト、Schema、評価fixtureのままhighを比較する。
- reasoning effortだけをhighへ変更した比較では、一覧版の期間上限と黎天河、詳細版の網羅性が改善したが、
  途中状態／最終状態のfact分離と、根拠を越えた珀陽の戦術立案・指揮承認の断定は改善しなかった。
  highもローカル不合格として独立評価と公開を停止した。次は高リスク場面を行為単位へ決定的に分割した
  検証済み事実グラフを先に作り、Solは文章化へ限定する方式を設計する。
- 4エラーと根拠9ページだけのSol high限定修正は87.244秒で完了し、ローカルゲートへ完全合格した。
  別Sol highの14件独立評価も新たな重大2件を検出でき、生成・修正・評価の役割分担は有効だった。一方、
  重大2件のうちR06は公開文ではなく監査用言い換えが作った誤りで、R10は言い換えと一覧版に時制誤りがあった。
  次はrisk claimを完成文の完全一致抜粋、位置、文ハッシュ、別の正規化主張に分け、ローカル規則抽出との
  完全被覆を確認してから同じ候補で再パイロットする。
- 完成文から高リスク語へ一致する41文を位置・ハッシュ付きで完全一致抽出し、確定OCR全文を使う別Sol highで
  40件supported、1件unsupported / majorとなった。R06/R10の言い換え由来偽陽性は消えた一方、詳細版が
  仁耀の「あとは頼んだ」の対象を白楼国の未来へ限定した根拠不足を検出した。完全一致監査方式を採用し、
  次はE18だけを不確実性保持表現へ直す別承認の修正サイクル後、41件すべてを新規セッションで再評価する。
- E18だけの完全一致置換、他40件不変、candidate/source/extractor SHA、別runの41件全件supportedを
  決定的に検証するCLIを実装した。元のr6 private candidateと新規独立Sol sessionがない環境では
  公開DBを変更せず`awaiting_review`で停止する。

**再生成後に判断する低優先改善**: 独立した複数初稿の比較選択、シリーズ横断人物台帳の
自動入力、承認済み生成文を使う固定ゴールデン評価は、今回の旧版・新版比較で残る問題を
確認してから着手する。巻単位の説明へ後続巻の知識を混ぜるとネタバレと事実混同が起きるため、
シリーズ横断入力は「当該巻まで」に限定する要件を先に確定する。

**工数**: 中。夜間バッチとして実行する。

### C-5. 読書統計ダッシュボード

**動機**: 「今月何冊読んだ」「どの作者を多く読んでいるか」のサマリ画面がない

**案**: `/api/stats` エンドポイント + ダッシュボードページ

**工数**: 中

### C-10. 小説 RAG: 新規キャプチャ後の自動 DB 構築

**動機**: 現状は管理画面から手動で OCR → ビルドを投入する必要がある。新しい書籍をキャプチャするたびに手順が発生する。

**案**: キャプチャ完了イベントを hook して job_queue に自動投入

**工数**: 中。イベント連携の設計が必要

### C-11. 小説 RAG: 検索結果エクスポート

**動機**: 良い回答を手元のメモに残したい。コピーボタン以上の構造化エクスポートが欲しい。

**案**: JSON / Markdown 形式でダウンロード（回答 + 引用チャンク + ページ番号）

**工数**: 小

### C-14. 読書会ロングフォーム化 Phase 4（3 人以上パネル拡張）— 長期構想

**動機**: 将来的に 2 人構成から 3 人以上のパネル形式に拡張したい。

**詳細要件**: [読書会ロングフォーム拡張_要件.md](../../design/要件定義/読書会ロングフォーム拡張_要件.md) §4.5

**概要**: `discussion_service.py` の `[A]:`/`[B]:` 2 話者決め打ちマーカー体系・システムプロンプトの一般化が必要。B-28〜B-30（2 人構成）完成後の後続タスク

**工数**: 大

### C-13. 小説 RAG: 複数モデルでの回答比較 UI

**動機**: モデル更新時の品質比較を画面上でできるようにしたい。現状はログを見て判断。

**案**: 同一クエリを複数モデルに投げて並列表示。NOVEL_DB_LLM_MODEL を実行時に切り替え可能にする。

**工数**: 中

---

## 保留領域（要件未確定）

### Kindle キャプチャ / OCR

[要件定義書 §5](../../design/要件定義/要件定義書.md) の保留領域。2026-05-13 時点の状況：

- **漫画 (`comic` ソース、旧 kindle ソース)**: `run_comic.bat` / `main_auto.py` で継続利用中。ソース名は `kindle` から `comic` に移行済み。
- **小説 (`kindle_novel` ソース)**: `run_novel.bat` / `main_novel.py` でスキャン、管理画面（`/novel/manage`）から OCR・RAG 構築を行う方式で稼働中。
- **OCR（novel.db 直接格納）**: スタンドアロン `batch_ocr.py` は廃止。backend job queue 経由（管理画面の「OCR」タブ）で Surya OCR 2 と品質ゲートを実行し、全ページ合格時だけ `novel.db` へ確定する方式に移行済み。yomitoku は限定補助・比較・後方互換用。

---

## 対象外（信頼モデル / 同人誌特化方針との非整合）

以下は **個人 LAN シングルユーザー × 同人誌特化** の前提と整合しないため意図的に作らない。

| 項目 | 理由 |
|---|---|
| ユーザーアカウント / 認証 / 認可 | LAN 内シングルユーザー前提 |
| マルチテナント・データ分離 | 同上 |
| API レート制限 | 単一クライアント前提 |
| WebSocket / SSE による進捗通知 | ポーリングで十分 |
| GraphQL API | REST で要件を満たす |
| API バージョニング | クライアント単一 |
| マイクロサービス分割 | 規模に対し過剰 |
| Kubernetes / Docker Compose 化 | LAN ローカル起動で十分 |
| 公開 PR テンプレート / Issue テンプレート | ソロ開発 |
| Storybook | コンポーネント数が爆発しない範囲 |
| サークル名 Web 検索による自動取得 | 撤去済み（精度が実用に達しない） |
| シリーズの自動発見（新規シリーズ生成） | 撤去済み（命名揺れに対し精度不足）。代替: AI による既存シリーズへの紐付け提案（A-1） |
| 多言語 OCR 対応 | OCR 機能自体が保留領域。現状日本語のみで十分 |
| ePub サポート | スコープ外（同人誌は ZIP / 画像が主） |
| AI タグ自動生成（表紙画像 + LLM） | タグ機能自体を撤去。復活時に再検討 |
| レーティング（★1〜5） | 旧バックログで実装候補だったがコード化されたことはなく、利用習慣もないため恒久的に対象外 |
| 小説 RAG: クラウド LLM 移行（B-7） | B-13/B-14 完了でローカル QA 品質・速度が要件を充足（llama-server で応答 5× 短縮、全文読み込み実現）。「性的描写を含む可能性があるためクラウド AI を使用しない」方針とも整合するため恒久的に対象外（2026-06-12 判断） |
| 小説 RAG: ページ単位シーン要約（B-10） | B-9（Contextual Retrieval / gemma4:e4b）で当初 19 時間想定 → 実機 100 分に短縮され存在意義消失。B-9 で品質充足（2026-06-12 判断） |
| 大規模 Panel の presentation/container 分割 | hook 抽出済みで本体ロジック層は既に薄く、JSX 主体の分割では行数は減っても認知負荷は下がらない。テスト容易性の利得は hook 単体テストと導入済みPlaywright E2E（C-8）で充足するため対応しない |
| 小説 RAG: LLM 補助チャンキング（Qwen でシーン境界検出 → 再チャンク） | 文献的に確定的な評価が薄く、B-9（Contextual Retrieval）のほうが ROI 高い。再チャンク + 再 embedding のコストも大きい。B-9 で十分な改善が得られると見込み、本項目は対応しない |
| PDF 分割（B-1） | merge の逆処理は工数に対して利用頻度が低く、手動ツールで代替可能 |
| 全画面一括のモバイル UI 最適化（旧 C-4） | 個人 LAN PC 中心のため全画面一括対応は行わない。グローバルナビゲーション、ライブラリ絞り込みなどユーザー指定の主要導線は個別に対応する |
| 全画面一括の a11y 強化（C-6） | LAN 内シングルユーザー前提のため一括対応しない。書籍カードなどユーザー指定の主要操作は、44px操作領域・読み上げ名・keyboard focusを個別に対応する |
| 小説 RAG: 横断検索（C-9） | スキーマ変更コストが大きく、ソース別閲覧で利用実態として不要 |

---

## 運用方針

- **新規候補の振り分け**: ユーザーから見た新機能・新体験 → 本ファイル（A/B/C 階層）。内部改善・依存更新 → [リファクタリング計画書](リファクタリング計画書.md)。
- 候補が **A-N → 対応決定** になったら新機能として `feat:` プレフィックスのコミットで実装
- 実装後は本ファイルから対応済み項目を削除し、上部「実装済み機能の履歴」テーブルに 1 行追加 + 詳細は変更履歴 / 設計書側に記録
- 棚上げ判断（C → 対象外）になった項目は対象外テーブルに移動

候補追加時のフォーマット:

````markdown
### {Tier}-{N}. {タイトル}

**動機**: {現在の不便 / 欲しい価値}
**案**: {具体的な実装方針}
**工数**: 小 / 中 / 大
````
