# 詳細設計書（バックエンド編）

バックエンド（FastAPI / Python）と、キャプチャ・OCR エンジンの詳細設計をまとめる。
ディレクトリ構成全体図・運用メモ・リリースビルド構成は [詳細設計書_共通.md](詳細設計書_共通.md) を参照。
フロントエンドの詳細設計は [詳細設計書_フロントエンド編.md](詳細設計書_フロントエンド編.md) を参照。

機能別の独立した設計書（hub から独立、本ファイルからは概要のみリンク）:
- [hitomi.la 新着監視設計書](機能別/hitomi新着監視設計書.md) — 監視スクリプト + JSON 連携
- [小説テキスト検索・RAG機能 バックエンド設計](機能別/小説テキスト検索・RAG機能_バックエンド設計.md) — novel タブの SQLite + FTS5 + sqlite-vec / bge-m3 / qwen3.6:35b-a3b SSE（共通 Qwen モジュール経由）
- [OCR設計書](機能別/OCR設計書.md) — yomitoku ベースの OCR。novel ソース（`kindle_novel/`）の job queue 経由 OCR で利用。旧 `batch_ocr.py` / `start_batch_ocr.bat` は Phase 5 で削除済み。

`backend/routers/novel_build.py`（4.6）: Full Build 専用管理画面の API ルーター。`novel_db` の `job_queue` シングルトンを薄くラップし、`mode=full_build` ジョブに特化した enqueue / status / cancel / SSE stream を提供する（[API 仕様書 §8](API仕様書.md#8-本構築管理novel_build)）。

---

## 1. ディレクトリ構成（バックエンド側）

```
backend/
├── data/               # データ格納用
│   ├── doujin/         # 同人誌データ（source='doujin'）
│   │   ├── images/         # 閲覧用WebP画像の保存・配信場所 (Static Mount: /images)。書籍一覧の走査元
│   │   # ※ pdfs_compressed/ は 2026-05-05 に廃止。/pdfs 静的マウントも削除済み。
│   │   #   書籍一覧は images/ サブディレクトリを走査して構築する（PDF ファイルは不要）
│   │   ├── thumbnails/     # サムネイル画像の保存・配信場所 (Static Mount: /thumbnails)
│   │   └── complete/       # 処理済みソースファイルの移動先
│   ├── comic/          # 漫画データ（source='comic'、旧 kindle/）(Static Mount: /comic/...)
│   │   ├── pdfs/
│   │   ├── thumbnails/
│   │   └── images/
│   ├── kindle_novel/   # 小説データ (Static Mount: /kindle_novel/...)
│   │   ├── thumbnails/
│   │   └── images/     # キャプチャ生画像
│   ├── meta/           # 旧 JSON メタデータ（Phase 64 移行後は .bak として残存）
│   └── meta.db         # 書籍メタデータ一元管理（Phase 64 以降、SQLite WAL モード）
├── routers/            # APIルーター
│   ├── _deps.py        # 共通 FastAPI 依存ヘルパー (validated_source / assert_valid_source / log_and_raise_500 / cancel_job_response / sse_event)
│   ├── library.py      # PDFs一覧・書籍画像・リネーム
│   ├── pdfs.py         # ページ削除・PDF結合（生成系は generate.py に分離）
│   ├── generate.py     # PDF生成（非同期ジョブ）・status・batch_compress
│   ├── thumbnails.py   # サムネイル再生成（単発 / 一括）
│   ├── ocr.py          # OCR実行・停止・ステータス
│   ├── meta.py         # 書籍メタデータ取得・更新（authors / hidden / genre / read_state）・閲覧記録・自動登録ジョブ
│   ├── series.py       # シリーズ判定ジョブ・手動割当（単一/配列 index）/ 解除 / 並べ替え（reorder）/ 未分類候補レポート
│   ├── genres.py       # ジャンルリスト取得・追加・削除・並べ替え
│   ├── hitomi.py       # hitomi.la 新着監視 API（薄いラッパー）
│   ├── novel_build.py  # Full Build 専用管理画面 API（enqueue / status / cancel / SSE stream, 4.6）
│   ├── novel_discussion.py # 読書会ディスカッション生成・履歴 API（B-20）
│   ├── novel_graph.py  # キャラクタ関係グラフ API（series一覧 / books / graph, C-12）
│   ├── meta_db_backup.py   # meta.db バックアップ API（B-25）
│   ├── amazon_import.py    # Amazon CSV インポート API（4.3）
│   └── novel_db/       # 小説テキスト検索・RAG サブパッケージ（Phase 57 で 6 分割）
│       ├── __init__.py     # サブルーターの include_router まとめ
│       ├── schemas.py      # 共通 Pydantic スキーマ（RebuildRequest / ScopeModel 等）
│       ├── _deps.py        # novel_db 専用依存ヘルパー
│       ├── lib.py          # 書籍一覧・シリーズ一覧・作者一覧・詳細・メタインポート API
│       ├── search.py       # ハイブリッド検索・QA 履歴 API
│       ├── rebuild.py      # 再構築ジョブ管理 API（/builds/*）
│       ├── character.py    # キャラクター一覧・詳細 API
│       ├── qa.py           # RAG 質問応答（SSE）API
│       └── chat.py         # マルチターン会話セッション API（/sessions/*）
├── services/           # ビジネスロジック
│   ├── ocr_service.py  # OCRプロセス管理 (OCRService)
│   ├── pdf_generator.py # PDF生成ロジック (PdfGenerator Class / scan_and_generate / batch_compress)
│   ├── pdf_service.py  # PDF操作 (PdfService)
│   ├── thumbnail_service.py # サムネイル生成 (ThumbnailService)
│   ├── file_manager.py # PDF・サムネイル・画像の3点セット操作 (FileManager)
│   ├── job_manager.py  # 非同期PDF生成ジョブ管理 (GenerateJob, JobStore)
│   ├── meta_db.py      # SQLite 接続管理・テーブル定義・JSON 自動マイグレーション（Phase 64）
│   ├── meta_store.py   # meta.db CRUD・ソース別ロック管理 (update_meta_locked / load_meta / save_meta)
│   ├── genre_store.py  # meta.db genres テーブル CRUD・(load_genres / save_genres)
│   ├── series_detector.py # シリーズ ID / 作者キーの純関数（authors_key / stable_series_id）。手動 series API 用
│   ├── series_suggester.py # 既存シリーズへの紐付け候補提案（ルールベース、書き込みなし、A-1）
│   ├── batch_compressor.py # 既存画像からの一括圧縮 PDF 生成（Phase 46 で分離）
│   ├── meta_db_backup.py   # meta.db OneDrive スナップショットバックアップ（sqlite3.backup()、B-25）
│   ├── volume_parser.py   # 巻数文字列のパース（整数/小数/vol.N/漢数字/括弧/巻数なし1巻ルール）
│   └── novel_db/
│       ├── _llm_backend.py        # LLM Backend シングルトン管理（QWEN_BACKEND / GEMMA_BACKEND / QUERY_BACKEND、Phase 79）
│       ├── graph_query.py         # character_relations 読み取り専用クエリ（Phase 82）
│       └── relation_extractor.py  # キャラ共起カウント + Qwen 関係タイプ抽出 → character_relations UPSERT（C-12）
├── utils/
│   ├── path_utils.py   # パスバリデーション一元化
│   ├── file_naming.py  # サムネイル名生成ユーティリティ
│   ├── file_utils.py   # 拡張子チェックユーティリティ (is_pdf_file / is_webp_file / is_zip_file / is_image_file)
│   ├── locks.py        # ソース別 threading.Lock の遅延生成・キャッシュ (SourceLockManager)
│   └── logger.py       # ロギング設定（StreamHandler + RotatingFileHandler、[ADR-0006](../02_基本設計/ADR/0006_backend-log-file-persistence.md)）
├── tests/              # pytest テスト（詳細は docs/06_リファクタリング/テスト整備計画書.md §1）
│   ├── conftest.py                  # 共通フィクスチャ (tmp_data_dir / client / make_pdf / make_webp)
│   ├── test_router_deps.py          # validated_source / log_and_raise_500 等
│   ├── test_router_library.py       # GET /api/pdfs / books images / rename / delete
│   ├── test_router_pdfs.py          # delete_pages / merge
│   ├── test_router_generate.py      # 生成ジョブ起動・進捗・status・batch_compress
│   ├── test_router_thumbnails.py    # ページサムネイルオンデマンド・再生成・一括
│   ├── test_router_meta.py          # GET /api/meta / export / genre
│   ├── test_router_ocr.py           # OCR run / stop / status の HTTP 層
│   ├── test_router_genres.py        # ジャンル CRUD / reorder
│   ├── test_router_hitomi.py        # Hitomi 監視 API（new-arrivals / dismiss / watchlist / run-now）
│   ├── test_pdf_service.py          # delete_pages / get_page_count
│   ├── test_thumbnail_service.py    # generate_thumbnail
│   ├── test_pdf_generator.py        # _collect_images / scan_and_generate / batch_compress
│   ├── test_meta_store.py           # merge_entry_fields / has_meaningful_value / 並行 update
│   ├── test_meta.py                 # record_view / update_meta（authors / hidden / genre / read_state）
│   ├── test_file_manager.py         # rename / delete (rollback / image-only / 部分失敗)
│   ├── test_job_manager.py          # JobStore CRUD / 並行性 / eviction 境界
│   ├── test_ocr_service.py          # subprocess.Popen モック化・ライフサイクル
│   ├── test_genre_store.py          # genres テーブル永続化・meta.db からの初期生成
│   ├── test_series.py               # series_detector の手動 API ヘルパー / routers/series 手動 API
│   ├── test_series_suggester.py     # 既存シリーズ候補提案（スコア計算・閾値・複数作者）
│   ├── test_volume_parser.py        # 巻数パターン全種
│   ├── test_image_service.py        # list_book_images / delete_book_image_pages / reorder_book_image_pages
│   ├── test_path_utils.py           # validate_safe_path / validate_safe_name / join_path
│   ├── test_file_utils.py           # is_*_file 群
│   ├── test_file_naming.py          # get_thumbnail_name
│   ├── test_locks.py                # SourceLockManager 並行性
│   ├── test_logger.py               # get_logger スモーク
│   ├── test_main.py                 # 例外ハンドラ・CORS・静的マウント
│   ├── test_config.py               # get_dirs_by_source
│   ├── test_hitomi_*.py             # services/hitomi/* と tools/hitomi_monitor
│   ├── test_novel_db_*.py           # novel_db サービス群（extractor/search/builder/summarizer/embedder/chunker/retrieval/full_builder 等）
│   ├── test_router_novel_db.py      # novel_db 書籍・シリーズ・作者・メタインポート API
│   ├── test_router_novel_db_search.py  # novel_db 検索・QA API
│   ├── test_router_novel_db_characters.py # novel_db キャラクター API
│   ├── test_router_novel_db_chat.py # novel_db チャットセッション API
│   ├── test_novel_discussion.py     # ディスカッション生成サービス・履歴
│   ├── test_router_novel_build.py   # novel_build Full Build エンキュー・キャンセル
│   └── test_build_chunk_contexts.py # チャンクコンテキスト生成のユニットテスト
├── exceptions.py       # カスタム例外クラス (FileOperationError / OcrProcessError)
├── config/             # パス定数・OCR起動設定・サポートフォーマット定数（Phase 55-7 でパッケージ化）
│   ├── __init__.py     # 従来の config.py 内容（DATA_DIR / get_dirs_by_source() 等）+ from .novel_db import *
│   └── novel_db.py     # NOVEL_DB_* 設定（21 件）を分離
├── main.py             # FastAPIエントリーポイント（exception_handler 登録含む）
└── requirements.txt    # Python依存関係
```

```
kindle-pdf/             # Kindle自動化ツール（漫画・小説キャプチャ専用）
├── main_auto.py        # 漫画/雑誌用エントリーポイント
├── main_novel.py       # 小説用エントリーポイント（画像保存のみ）
├── main_manual.py      # 手動撮影用エントリーポイント
├── capturer.py         # 共通キャプチャロジック (Manga)
├── novel_capturer.py   # 小説用キャプチャロジック (Novel)
├── run_comic.bat       # 漫画キャプチャ起動ランチャー（.venv\Scripts\python.exe main_auto.py）
├── run_novel.bat       # 小説キャプチャ起動ランチャー（.venv\Scripts\python.exe main_novel.py）
└── pyproject.toml      # uv 管理の依存定義
```
※ `batch_ocr.py` / `searchable_pdf.py` / `start_batch_ocr.bat` は Phase 5（旧 PDF 経路撤去）で削除済み。OCR（yomitoku）は `D:\61.tool\common\ocr\` の独立 venv で動き、novel_db rebuild（`services/novel_db/extractor.py`）から subprocess 呼び出しされる。

### 1.2 データ配置 (Backend)
- `backend/data/`: データ格納ルート
    - `doujin/`: 同人誌データ（source='doujin'、旧 `main/`）
    - `comic/`: 漫画データ（source='comic'、旧 `kindle/`）
    - `kindle_novel/`: 小説キャプチャデータ（source='novel'）
    - `meta.db`: 書籍メタデータ + ジャンルリスト（Phase 64 で JSON → SQLite 移行）
        - `meta` テーブル: キー `"{path}/{filename}"` / 値カラム `authors`, `hidden`, `genre`, `read_state`, `view_count`, `last_viewed_at`, `series_id`, `series_order`, `series_title`, `asin`
        - `genres` テーブル: ソース別の表示順ジャンルリスト
        - 旧 `meta/` / `genres/` ディレクトリの JSON は移行後 `.bak` にリネームされ残置
    - `logs/`: バックエンドログ（`app.log` + `app.log.1`〜`app.log.5`、`RotatingFileHandler` で自動世代管理）。詳細は [ADR-0006](../02_基本設計/ADR/0006_backend-log-file-persistence.md)

### 1.3 構成設定 (config パッケージ) [Phase 55-7 でパッケージ化]

`backend/config/` パッケージ（旧 `backend/config.py`）。Python のパッケージ優先により既存の `from config import X` は変更不要。

**`config/__init__.py`**:
- **パス定数**: `DATA_DIR`（env: `PIC2PDF_DATA_DIR` で OneDrive 等に変更可）, `MAIN_DATA_DIR`, `PDF_COMPRESSED_DIR`, `THUMBNAIL_DIR`, `IMAGES_DIR`, `COMPLETE_DIR`（`PDF_DIR` は Phase 19 の `/pdfs` マウント廃止で削除済み）
- **Kindle**: `KINDLE_PDF_DIR`, `KINDLE_THUMBNAIL_DIR`, `KINDLE_IMAGES_DIR`
- **Kindle Novel**: `KINDLE_NOVEL_PDF_DIR`, `KINDLE_NOVEL_THUMBNAIL_DIR`, `KINDLE_NOVEL_IMAGES_DIR`
- **hitomi**: `HITOMI_DATA_DIR`（`DATA_DIR/hitomi/`。`routers/hitomi.py` と `tools/hitomi_monitor.py` が参照）
- **書誌メタ DB（ローカル固定）**: `META_DB_DIR`（env: `META_DB_DIR`、デフォルトは `backend/data/`）。`services/meta_db.py` がモジュール変数 `DATA_DIR` として公開し、テストは `monkeypatch.setattr("services.meta_db.DATA_DIR", ...)` で切り替える
- **Novel DB（ローカル固定）**: `NOVEL_DB_DIR`（env: `NOVEL_DB_DIR` で個別上書き可、デフォルトは `backend/data/novel_db/`）。SQLite / LanceDB は OneDrive 同期競合でDB破損リスクがあるためローカルに固定
- **OCR起動設定**: `BATCH_OCR_LAUNCHER` 定数は Phase 5 で削除済み。現在の OCR は `services/ocr_service.py` のスレッドベース方式（`_run_ocr` メソッド）で実行され、`POST /api/ocr/run` は動作する。novel ソースの OCR は `services/novel_db/extractor.py` 経由で `D:\61.tool\common\ocr\` の独立 venv で実行される。
- **フロントエンド配信**: `FRONTEND_DIST_DIR` — `frontend/dist/` の絶対パス。リリースモード起動時に存在すれば `main.py` が SPA を同一オリジンで配信する。
- **meta.db バックアップ先**: `META_DB_BACKUP_DIR`（env: `META_DB_BACKUP_DIR`、デフォルト `OneDrive/61.tool/meta_db_backup/`）。`services/meta_db_backup.py` が `sqlite3.backup()` でスナップショットを保存する宛先ディレクトリ（B-25）
- **サポートフォーマット**: `SUPPORTED_IMAGE_FORMATS`, `SUPPORTED_WEBP_FORMAT`, `SUPPORTED_ZIP_FORMAT`
- **数値定数**: `THUMBNAIL_HEIGHT`, `JOB_MAX_JOBS`, `OCR_LOG_MAXLEN`, `ZIP_MAX_ENTRIES`, `ZIP_MAX_PER_FILE_BYTES`, `ZIP_MAX_TOTAL_UNCOMPRESSED_BYTES`（後 3 つは zip bomb 対策の上限値、[セキュリティ設計書 §2-5](セキュリティ設計書.md) 参照）

**`config/novel_db.py`**: `NOVEL_DB_*` 設定 21 件（Ollama URL / embedding / LLM モデル / QA パラメータ等）を env から読む。`__init__.py` が `from .novel_db import *` で再エクスポートするため、利用側の import は変わらない。

### 1.4 ロギング (utils/logger.py)

設計判断の詳細は [ADR-0006](../02_基本設計/ADR/0006_backend-log-file-persistence.md)。

- **取得方法**: 各モジュールで `from utils.logger import get_logger; logger = get_logger(__name__)`
- **ハンドラ構成**:
  - **StreamHandler** (`sys.stdout`): 各モジュール logger に直接付与。`get_logger(__name__)` 初回呼び出し時にセット。開発時のターミナル可視性を確保
  - **RotatingFileHandler** (`backend/data/logs/app.log`): root logger に 1 度だけ付与。`logger.propagate=True`（デフォルト）経由で全モジュール logger のログがファイルに集約される。ファイルハンドルを 1 個に集約することで Windows でのローテーション競合を回避
- **ローテーション**: 10MB × 5 世代（最大合計 50MB）、UTF-8 エンコーディング
- **ログレベル**:
  - 自前モジュール logger: `INFO`（既定）
  - root logger: `WARNING`（サードパーティ DEBUG 混入防止、`get_logger` 呼び出し時に未設定なら自動セット）
- **フォーマット**: `%(asctime)s [%(levelname)s] %(name)s: %(message)s`（`asctime` は `%Y-%m-%d %H:%M:%S`）
- **セキュリティ監査ログ**: `Security:` 接頭辞で grep 可能。詳細は [セキュリティ設計書 §2-5](セキュリティ設計書.md)

---

### 1.5 ファイル詳細マップ（タスク起点）

コード変更タスクの起点として、関連ファイルを特定するために参照する。`§1.1` のツリーが「構成」、本節が「ファイル → 役割」。

#### バックエンド (FastAPI / Python)

| ファイル | 役割 |
|---|---|
| [backend/main.py](../../backend/main.py) | アプリ起動・ルーター登録・静的ファイルマウント |
| [backend/config/\_\_init\_\_.py](../../backend/config/__init__.py) | データディレクトリ定数・`get_dirs_by_source()`（旧 `backend/config.py`、Phase 55-7 でパッケージ化） |
| [backend/config/novel\_db.py](../../backend/config/novel_db.py) | `NOVEL_DB_*` 設定 21 件（Ollama URL / embedding / LLM モデル / QA パラメータ等） |
| [backend/routers/library.py](../../backend/routers/library.py) | ライブラリ一覧・リネーム API |
| [backend/routers/pdfs.py](../../backend/routers/pdfs.py) | ページ削除・PDF 結合 API（生成系は `generate.py` に分離） |
| [backend/routers/generate.py](../../backend/routers/generate.py) | PDF 生成（非同期ジョブ）・進捗・status 一覧・batch_compress API |
| [backend/routers/thumbnails.py](../../backend/routers/thumbnails.py) | サムネイル再生成 API（単発 / 一括） |
| [backend/routers/ocr.py](../../backend/routers/ocr.py) | OCR 実行・停止・ステータス API |
| [backend/routers/meta.py](../../backend/routers/meta.py) | 書籍メタデータ取得・更新（authors / hidden / genre / read_state）・閲覧記録・サークル名自動登録ジョブ |
| [backend/routers/series.py](../../backend/routers/series.py) | シリーズ判定ジョブの実行・進捗、シリーズ手動割り当て（単一 / 配列 index）/ 解除 / DnD 並べ替え API |
| [backend/routers/genres.py](../../backend/routers/genres.py) | ジャンルリスト取得・追加・削除・並べ替え API |
| [backend/routers/novel_graph.py](../../backend/routers/novel_graph.py) | キャラクタ関係グラフ API（`GET /api/novel_graph/series` / `/series/{id}/books` / `/series/{id}/graph`、C-12） |
| [backend/routers/meta_db_backup.py](../../backend/routers/meta_db_backup.py) | meta.db バックアップ API（`POST /api/meta_db/backup` / `GET /api/meta_db/backup/status`、B-25） |
| [backend/routers/_deps.py](../../backend/routers/_deps.py) | ルーター共通の FastAPI 依存ヘルパー（`validated_source` Depends / `assert_valid_source` ボディ検証 / `validate_request_targets` パスと names 一括検証 / `log_and_raise_500` 例外デコレータ） |
| [backend/services/meta_db.py](../../backend/services/meta_db.py) | `meta.db` 接続・テーブル定義・起動時初期化（`init_db()`）・JSON 自動移行（Phase 64） |
| [backend/services/genre_store.py](../../backend/services/genre_store.py) | ジャンルリスト CRUD（`meta.db` の `genres` テーブル経由）。公開 API は変わらず |
| [backend/services/pdf_service.py](../../backend/services/pdf_service.py) | PDFページ削除・ページ数取得ロジック |
| [backend/services/image_service.py](../../backend/services/image_service.py) | image-only モード（generated ソース）のページ列操作（`list_book_images` で natsort 走査、`delete_book_image_pages` で N 番目 WebP を削除） |
| [backend/services/pdf_generator.py](../../backend/services/pdf_generator.py) | WebP/ZIP → PDF 変換（`scan_and_generate` / `_collect_images` 共通フロー / generate_thumbnail）。`batch_compress` は `batch_compressor.py` に分離（後方互換 re-export あり） |
| [backend/services/batch_compressor.py](../../backend/services/batch_compressor.py) | 既存画像から一括で圧縮 PDF を生成する独立サービス（`batch_compress` / `_write_compressed_pdf`） |
| [backend/services/thumbnail_service.py](../../backend/services/thumbnail_service.py) | サムネイル生成 |
| [backend/services/ocr_service.py](../../backend/services/ocr_service.py) | OCRバックグラウンドプロセス管理 |
| [backend/services/meta_store.py](../../backend/services/meta_store.py) | メタデータ CRUD（`meta.db` の `meta` テーブル経由）。`update_meta_locked()` / `merge_entry_fields()` / `has_meaningful_value()` / `MetaEntry` — 公開 API は変わらず（Phase 64 で内部実装を SQLite に移行） |
| [backend/services/series_detector.py](../../backend/services/series_detector.py) | シリーズ手動編集 API 用ヘルパー（`authors_key` / `stable_series_id`）。状態を持たない純関数 |
| [backend/services/series_suggester.py](../../backend/services/series_suggester.py) | 既存シリーズへの紐付け候補をルールベースで提案する純関数群（A-1）。書き込み副作用なし |
| [backend/services/volume_parser.py](../../backend/services/volume_parser.py) | 巻数文字列のパース（整数 / 小数 / `vol.N` / 漢数字 / `(上中下前後)` / 巻数なし1巻ルール） |
| [backend/services/meta_db_backup.py](../../backend/services/meta_db_backup.py) | meta.db の OneDrive スナップショットバックアップ（`sqlite3.backup()` によるホットバックアップ、B-25） |
| [backend/services/novel_db/relation_extractor.py](../../backend/services/novel_db/relation_extractor.py) | キャラ共起カウント（`count_cooccurrences`）+ Qwen 関係タイプ抽出（`extract_relations_with_qwen`）+ `character_relations` テーブル UPSERT（`store_relations`）+ オーケストレーター（`generate_book_relations`）（C-12） |
| [backend/services/novel_db/graph_query.py](../../backend/services/novel_db/graph_query.py) | `character_relations` 読み取り専用クエリ（Phase 82）。`get_graph_for_series`（nodes/edges 組み立て）/ `list_series_with_relations` / `list_books_in_relation_series`。`novel_graph` ルーターから利用 |
| [backend/services/amazon_csv_parser.py](../../backend/services/amazon_csv_parser.py) | Amazon CSV の行レベルパース（`Digital Content Orders.csv` / `amazon-order_digital/*.csv`）。CSV 読み込みと正規化を担う純関数群（4.3） |
| [backend/services/amazon_csv_importer.py](../../backend/services/amazon_csv_importer.py) | Amazon CSV インポート統合ロジック：固定パス CSV から lookup テーブル構築・タイトル正規化マッチング・meta.json の authors / asin 空欄補完（上書きなし）。novel / comic 両対応（4.3） |
| [backend/services/file_manager.py](../../backend/services/file_manager.py) | PDF・サムネイル・画像 3 点セットのリネーム・削除（ロールバック付き） |
| [backend/services/job_manager.py](../../backend/services/job_manager.py) | PDF 生成ジョブの管理（`GenerateJob` / `JobStore` / `get_active_current_item`） |
| [backend/utils/file_utils.py](../../backend/utils/file_utils.py) | 拡張子チェックユーティリティ（is_pdf_file / is_webp_file / is_zip_file / is_image_file） |
| [backend/utils/file_naming.py](../../backend/utils/file_naming.py) | サムネイルファイル名生成ユーティリティ |
| [backend/utils/path_utils.py](../../backend/utils/path_utils.py) | パスバリデーション（`validate_safe_path` / `validate_safe_name`） |
| [backend/utils/locks.py](../../backend/utils/locks.py) | ソース別 `threading.Lock` の遅延生成・キャッシュ（`SourceLockManager`）。`meta_store` / `genre_store` が利用 |
| [backend/utils/logger.py](../../backend/utils/logger.py) | ロギング設定（StreamHandler + RotatingFileHandler、[ADR-0006](../02_基本設計/ADR/0006_backend-log-file-persistence.md)） |
| [backend/exceptions.py](../../backend/exceptions.py) | カスタム例外クラス（FileOperationError / OcrProcessError） |

**主要ライブラリ:** `fastapi`, `uvicorn`, `img2pdf`, `Pillow`, `pymupdf`, `natsort`

#### Kindleキャプチャツール (kindle-pdf/)

| ファイル | 用途 |
|---|---|
| [kindle-pdf/main_auto.py](../../kindle-pdf/main_auto.py) | 漫画自動キャプチャ |
| [kindle-pdf/main_manual.py](../../kindle-pdf/main_manual.py) | 漫画手動キャプチャ |
| [kindle-pdf/main_novel.py](../../kindle-pdf/main_novel.py) | 小説キャプチャ（画像保存のみ） |
| [kindle-pdf/run_comic.bat](../../kindle-pdf/run_comic.bat) | 漫画キャプチャ起動ランチャー（`.venv\Scripts\python.exe main_auto.py`） |
| [kindle-pdf/run_novel.bat](../../kindle-pdf/run_novel.bat) | 小説キャプチャ起動ランチャー（`.venv\Scripts\python.exe main_novel.py`） |

設計詳細: [kindle-pdf/docs/basic_design.md](../../kindle-pdf/docs/basic_design.md)

#### hitomi.la 新着監視 (backend/services/hitomi/, backend/tools/)

| ファイル | 役割 |
|---|---|
| [backend/routers/hitomi.py](../../backend/routers/hitomi.py) | `/api/hitomi/*` API（薄いラッパー） |
| [backend/tools/hitomi_monitor.py](../../backend/tools/hitomi_monitor.py) | 監視 CLI のエントリ（Task Scheduler から起動） |
| [backend/services/hitomi/nozomi.py](../../backend/services/hitomi/nozomi.py) | NOZOMI 取得・big-endian デコード |
| [backend/services/hitomi/metadata.py](../../backend/services/hitomi/metadata.py) | `/galleries/<id>.js` 取得・パース |
| [backend/services/hitomi/watchlist.py](../../backend/services/hitomi/watchlist.py) | watchlist.json CRUD・作者名正規化 |
| [backend/services/hitomi/state_store.py](../../backend/services/hitomi/state_store.py) | state.json / new_arrivals.json 操作・30 日 purge |

設計詳細: [機能別/hitomi新着監視設計書.md](機能別/hitomi新着監視設計書.md)

---

## 2. クラス設計

### バックエンド (`backend/`)

#### `OCRService` (`backend/services/ocr_service.py`)
- **役割**: OCR スレッドの管理、ログ収集、ステータス管理。シングルトン。
- **主要メソッド**:
    - `start_ocr(target_dir?)`: スレッドを起動し `_run_ocr` を実行（`run_ocr_subprocess` + `_store_ocr_pages` を直接呼ぶ。旧 `start_batch_ocr.bat` は不使用）。スレッド ID を返す。
    - `stop_ocr()`: ステータスを `idle` にリセット（現在書籍完了後に停止。強制終了ではない）。
    - `get_status()`: 現在の状態と直近のログを返却。

#### `GenerateJob` / `JobStore` (`backend/services/job_manager.py`)
- **役割**: 非同期PDF生成ジョブのライフサイクル管理。利用元は `routers/generate.py`（旧 `routers/pdfs.py` から分離）。
- **設計**:
    - `GenerateJob`: UUID・ステータス・進捗アイテム・ファイルリスト・エラーを保持。`threading.Lock` で状態更新をスレッドセーフに。
    - `JobStore`: 最大 `JOB_MAX_JOBS` 件のジョブをメモリ管理。FIFO方式で古いジョブを自動削除。
    - `JobStore.get_active_current_item()`: 最新の RUNNING ジョブの `current_item` を返す。`/api/status` エンドポイントで「処理中アイテム」を判定するために使用（旧 `GenerateState` は廃止）。
- **エンドポイント** (`backend/routers/generate.py`):
    - `POST /api/generate` → `asyncio.create_task + run_in_executor` でバックグラウンド非同期実行 + `{job_id}` を即返却（Phase 71 で `threading.Thread` から移行）。
    - `GET /api/generate/job/{job_id}` → ジョブ進捗取得（クライアントが1500msでポーリング）。
    - `GET /api/status` → ソースディレクトリ内の各アイテムの変換状態（`not_started` / `in_progress` / `completed`）を返す。
    - `POST /api/batch_compress` → `data/doujin/images/` 配下の WebP を一括圧縮 PDF 化。

#### ルーター共通依存 (`backend/routers/_deps.py`)
- **役割**: ルーターで共通利用する FastAPI 依存関数を集約。
- **関数**:
    - `validated_source(source: str = "doujin") -> str`: クエリパラメータ `source` を `VALID_SOURCES` で検証する `Depends` 用関数。無効値は 400。
    - `assert_valid_source(source: str) -> None`: リクエストボディ内 `source` フィールドを検証するヘルパー（`Depends` を経由しないボディ POST 用）。無効値は 400。
    - `validate_request_targets(path: str, names: list[str]) -> None`: `path` を `validate_safe_path`、各 name を `validate_safe_name` で検証する。多くのエンドポイントで重複していた 2 行検証を 1 行に圧縮（Phase 47）。
    - `log_and_raise_500(operation: str)`（Phase 52）: エンドポイントデコレータ。想定外例外を `logger.exception(operation)` で記録し `HTTPException(500)` に変換する。`HTTPException` は素通し、明示的な `RuntimeError → HTTPException(400)` などの内部 try/except も先に評価される。
- **適用範囲**: `series.py` / `meta.py` / `genres.py` のクエリ 8 エンドポイントが `Depends(validated_source)` を、ボディ POST 7 箇所が `assert_valid_source(request.source)` を呼ぶ。`log_and_raise_500` は `ocr.py` / `generate.py` / `library.py` / `thumbnails.py` / `pdfs.py` の 6 エンドポイントで重複していた `except Exception → logger.exception → HTTPException(500)` を集約（Phase 52）。

#### `FileManager` (`backend/services/file_manager.py`)
- **役割**: PDF・サムネイル・画像ディレクトリの3点セット操作を一元管理。`routers/library.py` の重複コードを集約。
- **主要メソッド**:
    - `rename_with_assets(path, old_name, new_name, is_folder, dirs)`: PDF+サムネイル+画像ディレクトリをリネーム。失敗時は自動ロールバック。PDF が存在しない場合（generated image-only モード）はスキップ。存在チェックは「PDF または images ディレクトリのどちらか一方が存在すること」。
    - `delete_with_assets(item, path, dirs)`: PDF+サムネイル+画像ディレクトリをディスクから完全削除。`DELETE /api/pdfs` からのみ呼ばれる。PDF が存在しない場合はスキップ（images ディレクトリのみ削除）。存在チェックは「PDF または images ディレクトリのどちらか一方が存在すること」。

#### `PdfGenerator` (`backend/services/pdf_generator.py`)
- **役割**: ディレクトリやZIPファイルのスキャン、画像からのサムネイル生成・ファイル移動を一元管理。
- **設計**:
    - ZIP・ディレクトリのどちらも「`images_dir` にファイルを配置 → `_collect_images` → `_generate_outputs`」という共通フローで処理する。
    - `output_dir=None` を渡すと image-only モード（PDF 生成をスキップ）。`generated` ソースはこのモードで呼び出す。
    - `process_zip` / `process_directory` は書籍単位の例外を `self.failed_items: list[(name, error)]` に集約する。`scan_and_generate` の戻り値 `GenerateResult` 経由でジョブ層に伝播し、`GET /api/generate/job/{id}` の `failed_items` でフロントに可視化する（**サイレント失敗防止**）。
- **戻り値型**:
    - `GenerateResult(generated: list[str], failed_items: list[tuple[str, str]])` — `scan_and_generate` の戻り値。`failed_items` は `(item_name, error_message)` の組。
- **主要メソッド・関数**:
    - `_collect_images(images_dir)`: ディレクトリ内の WebP を natsorted で収集するモジュールレベル関数。
    - `_check_zip_safety(webp_infos, zip_filename)`: zip bomb 対策のモジュールレベル関数。`process_zip()` が ZIP を開いた直後・解凍前に呼び、`ZIP_MAX_ENTRIES` / `ZIP_MAX_PER_FILE_BYTES` / `ZIP_MAX_TOTAL_UNCOMPRESSED_BYTES` を超える場合は `ValueError` を投げる。違反時は `logger.warning("Security: ZIP rejected ...")` で監査ログも残す（[セキュリティ設計書 §2-5](セキュリティ設計書.md)）。
    - `process_directory()`: WebP を `images_dir` にコピーして共通フローへ。失敗時は `failed_items` に `(folder_name, error)` を追加。
    - `process_zip()`: ZIP を `images_dir` に展開して共通フローへ。展開前に `_check_zip_safety()` で安全検査。失敗時は `failed_items` に `(item_name, error)` を追加。
    - `_generate_outputs(item_name, item_images_dir)`: サムネイル生成 + PDF 生成（`output_dir` が `None` の場合はスキップ）。
    - `run()`: 処理の実行と、完了ファイルの `complete/` への移動・空ディレクトリ削除の制御。
- **生成後のメタ初期化** (`backend/routers/generate.py` `_run_generate_job`):
    - `scan_and_generate` で新規生成された各書籍について、`meta.json` にエントリが存在しない場合のみ `genre: "オリジナル"` を初期書き込みする（再生成時は既存エントリを保持）。
- **`GET /api/status` の完了判定**:
    - `pdfs_compressed/` 廃止にともない、`IMAGES_DIR/{item_name}/` ディレクトリが存在するかどうかで `completed` を判定する。

#### `PdfService` (`backend/services/pdf_service.py`)
- **役割**: 既存PDFの編集操作を一元管理。
- **主要メソッド**:
    - `delete_pages()`: 指定されたページの削除とPDFの再保存。
    - `reorder_pages()`: `Document.select(page_indices)` で PDF を再構築し、上書き保存する（B-3）。`page_indices` は `[0..N-1]` の完全なパーミュテーションであることを要求し、違反時は `ValueError`。
    - `get_page_count()`: PDFの総ページ数を取得。

#### `ImageService` (`backend/services/image_service.py`)
- **役割**: image-only モード（`generated` ソース）の書籍ページ列を操作するヘルパー。`PdfService` の image-only 版に相当。
- **主要関数**:
    - `list_book_images(img_dir, book_name, path) -> list[str]`: `images/{path}/{book_name}/` 配下の WebP を natsort 順で絶対パスのリストとして返す。ディレクトリ不在時は空リスト。
    - `delete_book_image_pages(book_dir, page_indices) -> int`: `book_dir` 配下の WebP を natsort 順に並べ、`page_indices`（0 始まり）に該当する WebP ファイルを削除する。重複排除 + 降順ソートでインデックスずれを防ぐ。範囲外インデックスは `ValueError`。戻り値は削除後の総ページ数。
    - `reorder_book_image_pages(book_dir, page_indices) -> int`: `book_dir` 配下の WebP を `page_indices` の指す順序で `page_0001.webp` / `page_0002.webp` / ... と再採番する（B-3）。一時名 `__reorder_tmp_NNNN.webp` 経由の 2 段階リネームで、入力順と既存ファイル名の衝突を回避する。`page_indices` は `[0..N-1]` の完全なパーミュテーションであることを要求し、違反時は `ValueError`。戻り値は総ページ数（並び替え前後で不変）。
- **設計判断**: `routers/thumbnails.py` の private `_get_webps` ヘルパーをここに移して `list_book_images` として公開。`routers/thumbnails.py` も同関数を使うようにリファクタ。

#### `routers/pdfs.py:delete_pages` のソース別分岐
- `doujin`: `ImageService.delete_book_image_pages` を呼び、削除後は `pdf_generator.generate_thumbnail`（PIL ベース）で先頭画像から表紙を再生成。
- `comic` / `novel`: 従来通り `PdfService.delete_pages` を呼び、`ThumbnailService.generate_thumbnail`（fitz ベース）で表紙を再生成。
- 404 判定: `doujin` は `images/{book_name}/` ディレクトリが存在しないとき、`comic`/`novel` は PDF が存在しないとき。

#### `routers/pdfs.py:reorder_pages` のソース別分岐（B-3）
- `generated`: `ImageService.reorder_book_image_pages` を呼び、再採番後の先頭 WebP から表紙を再生成。
- `kindle` / `novel`: `PdfService.reorder_pages` を呼び、再構築後の PDF 先頭ページから表紙を再生成。
- 404 判定は `delete_pages` と同じ規則。
- バリデーション: `page_indices` の長さが現在の総ページ数と一致し、`[0..N-1]` の完全なパーミュテーションになっていることを `_validate_permutation` ヘルパーで先にチェックして 400 を返す（service 層に到達する前に拒否してエラーメッセージを明確化）。

#### `ThumbnailService` (`backend/services/thumbnail_service.py`)
- **役割**: **PDF からの**サムネイル画像生成（fitz ベース）。
- **主要メソッド**:
    - `generate_thumbnail(pdf_path, thumbnail_path, scale=0.5)`: PDF 先頭ページをスケール指定でレンダリングし JPEG 出力。
- **画像（WebP / JPEG / PNG）からのサムネイル生成は別関数**:
    - `services/pdf_generator.py:generate_thumbnail(image_path, output_path) -> bool` を使う（PIL ベース）。
    - 現行 PyMuPDF ビルドが WebP を読めない (`FileDataError`) ため、`generated` ソース（image-only モード）の経路では fitz を使ってはならない。
    - 役割分担: **PDF → ThumbnailService（fitz）** / **画像 → pdf_generator.generate_thumbnail（PIL）**。
- **呼び出しパス**:
    - `routers/pdfs.py:delete_pages` / `merge_pdfs`: PDF 操作後の再生成 → fitz
    - `routers/thumbnails.py:_regenerate_one`: PDF があれば fitz、PDF 不在 + WebP のみなら PIL（image-only モード対応）
    - `routers/library.py:_list_from_images`: BackgroundTasks で WebP → PIL（generated 専用）
    - `services/pdf_generator.py:_generate_outputs`: PDF 生成と同時に PIL で 1 枚目から作成

#### `GenreStore` (`backend/services/genre_store.py`)
- **役割**: ジャンルリストの読み書き・ソース別ロック管理を一元化。`routers/genres.py` が使用するインフラ層。
- **保存先**: `backend/data/meta.db` の `genres` テーブル（Phase 64 で `data/genres/{source}.json` から SQLite WAL に移行）。
- **主要関数**:
    - `load_genres(source)`: ソース別ロックを取得して `genres` テーブルから読み込んで返す。テーブル未登録時は `_derive_from_meta` で初期リストを生成してから保存して返す。
    - `save_genres(source, genres)`: ソース別ロックを取得して `genres` テーブルに書き込む。
    - `_derive_from_meta(source)`: 初回ロード時の migration 用関数。`meta` テーブルの `genre` カラムを収集して名前順にソートした初期 list を返す。並び順は UI からの `PATCH /api/genres/reorder` で更新されるため、ここでは単純なソートに留める。
- **ロック**: `utils.locks.SourceLockManager` のインスタンスを保持し、source 単位で `load_genres` / `save_genres` を直列化する（読み書き双方をロック下で行うことで、初期生成中の不整合を防ぐ）。

#### `GenreAPI` (`backend/routers/genres.py`)
- **役割**: ジャンルリストの CRUD と並べ替えエンドポイント。
- **エンドポイント**:
    - `GET /api/genres?source=` → 順序付きジャンルリストを返す。
    - `POST /api/genres` → `{source, name}` でジャンルを追加。重複時は 409。
    - `DELETE /api/genres/{name}?source=` → ジャンルをリストから削除（書籍の `genre` フィールドは変更しない）。
    - `PATCH /api/genres/reorder` → `{source, genres}` で順序を更新（集合が一致しない場合は 400）。

#### `MetaStore` (`backend/services/meta_store.py`)
- **役割**: meta.json の読み書き・ソース別ロック管理を一元化。`routers/meta.py` のインフラ層。
- **型定義**:
    - `MetaEntry(TypedDict)`: `authors: list[str]` / `view_count?: int` / `last_viewed_at?: float` / `series_id?: str` / `series_title?: str` / `series_index?: float` / `hidden?: bool` / `genre?: str` / `read_state?: ReadState` / `volume?: int | None` / `publisher?: str` / `asin?: str` / `isbn?: str` / `release_date?: str`（任意フィールド。`series_index` は doujin/comic の小数巻用 float。`volume` は novel 向け整数巻番号）
    - `ReadState = Literal['unread', 'reading', 'done']` / `VALID_READ_STATES` 定数で列挙を一元管理
    - `MetaDict = dict[str, MetaEntry]`
- **主要関数**:
    - `get_lock(source)`: ソース別 `threading.Lock` を返す（`utils.locks.SourceLockManager` への薄いプロキシ）。
    - `load_meta(source)` / `save_meta(source, data)`: JSON の読み書き。
    - `make_key(path, name)`: `"{path}/{name}"` または `"{name}"` のキーを生成。
    - `update_meta_locked(source, updater)`: ロック取得→ロード→updater適用→保存を一括で行うユーティリティ。
- **ロック**: `utils.locks.SourceLockManager` のインスタンスを保持。`MetaStore` と `GenreStore` は別インスタンスを持つため、両者のロックは独立する。

#### シリーズ手動編集 API (`backend/routers/series.py`)
- **役割**: 書籍を既存または新規シリーズに手動で割り当てる API。自動グループ化は撤去済み（2026-05-09、Phase 6）。
- **書き込み**:
    - `series_id`（共通プレフィックス + 作者をハッシュ化した安定 ID、`stable_series_id` で生成）/ `series_title` / `series_index`（`float`、小数巻 `2.5` 等に対応）を `meta.json` に書き戻す。
- **API**:
    - `POST /api/series/assign` → 書籍に既存または新規シリーズを設定。`index` は `float` または `float[]`（配列指定で `names[i]` に `index[i]` を割り当てる、複数選択からの一括登録用）。
    - `POST /api/series/unassign` → 書籍をシリーズから外す。
    - `POST /api/series/reorder` → 同シリーズに属する書籍の `series_index` を `names` の順序で `1.0, 2.0, ...` に振り直す（DnD 並べ替え用）。`series_id` 不一致の書籍が混在すれば 400（中途半端な書き込みなし）。
- **`services/series_detector.py` のヘルパー（純関数）**:
    - `authors_key(authors)`: 作者集合を順序非依存のキーにする。
    - `stable_series_id(prefix, authors_key)`: シリーズ名 + 作者集合から安定 ID を生成。

#### `SeriesSuggester` (`backend/services/series_suggester.py`)
- **役割**: 既存シリーズへの紐付け候補をルールベースで提案する純関数群（A-1）。`POST /api/series/suggest` から呼ばれる。書き込み副作用なし。撤去された自動グループ化（Phase 6）の代替で、**「新規シリーズの発見」ではなく「既存シリーズへの追加候補」** に絞ることで誤検出（取りこぼし・誤登録名）を回避する。
- **マッチング戦略**:
    - 既存シリーズの `series_title` から末尾の巻数表記（数字 / 漢数字 / `vol.N` / `(上中下)`）を除いた本体で比較。
    - 各選択書籍タイトルとの **共通プレフィックス長 / min(タイトル長, シリーズ本体長)** をスコア化。
    - 全選択書籍の平均スコアを採用（複数冊が同一シリーズに属することを評価）。
    - 全書籍で作者集合が一致した場合のみ `AUTHOR_MATCH_BONUS=0.2` 加点。最大 1.0 にクランプ。
    - 閾値 `SUGGEST_MIN_SCORE=0.4` 未満は除外、スコア降順で `SUGGEST_MAX_CANDIDATES=5` 件まで返す。
- **主要関数**:
    - `suggest_series(meta, path, names) -> list[SuggestedSeries]`: メイン関数。
    - `_strip_volume_suffix(title)`: シリーズ表示名末尾の巻数表記を除去（純関数）。
    - `_aggregate_existing_series(meta)`: meta から既存シリーズを集計（series_id 単位）。
- **方針**: 自動実行しない。フロントエンドのダイアログから明示的に呼ばれた時のみ動く。

#### メタデータAPI (`backend/routers/meta.py`)
- **役割**: 書籍メタデータ（作者名）の HTTP ハンドラのみを担う。ロジックは `MetaStore` に委譲。
- **エンドポイント**:
    - `GET /api/meta?source=` → meta.json 全体を返す。
    - `GET /api/meta/export?source=` → meta.json 全体を JSON ファイルとしてダウンロード（`Content-Disposition: attachment`）。バックアップ用。
    - `PATCH /api/meta` → `names`（ファイル名リスト）と `authors` / `hidden` / `genre` / `read_state` を受け取り、指定されたフィールドのみ更新。他は保持される。`hidden=true` で非表示化、`hidden=false` で再表示（フィールド削除）、`genre` は文字列で上書き（空文字でフィールド削除）、未指定なら変更しない。
    - `POST /api/meta/view` → 閲覧記録（`view_count` / `last_viewed_at` 更新、連打抑制 5 分）。

#### 閲覧回数 / 最近見た順ソート（バックエンド側）
- **保存先**: `meta.json` の各エントリに `view_count: int`（任意、デフォルト 0）と `last_viewed_at: float`（任意、UNIX 時刻）を追加。既存エントリには無いため読み込み時に 0 として扱う。
- **連打抑制**: `view_count` は前回 `last_viewed_at` から `VIEW_COUNT_DEBOUNCE_SEC = 300`（5分）以上経過した場合のみ +1 する。`last_viewed_at` は呼び出し毎に常に現在時刻で更新する（「最近見た順」ソートに反映するため）。
- **メタデータマージ規則**: `PATCH /api/meta`（作者更新）と `POST /api/meta/view`（閲覧記録）は互いに干渉しない。authors 更新時は既存の `view_count` / `last_viewed_at` を保持し、閲覧記録時は既存の `authors` を保持する。`authors` を空配列で更新した場合、他のフィールドが残っていれば `authors: []` で残し、何も残らなければエントリ自体を削除する。

#### 読書状態 (`read_state`)
- **保存先**: `meta.json` の各エントリ任意フィールド `read_state: 'unread' | 'reading' | 'done'`。未設定の既存エントリは `view_count` から派生（フロント側で 0→unread / >0→reading として扱う、バックエンドは値を返すだけ）。
- **自動遷移**: `POST /api/meta/view` で `view_count` が +1 されるとき（連打抑制でカウント据え置きの場合は除く）、既存 `read_state` が `'done'` でなければ `'reading'` に書き換える。`done` を維持するのは「読了済みの再読」を区別したいため。
- **手動遷移**: `PATCH /api/meta` の `read_state` フィールドで `'unread'` / `'reading'` / `'done'` 設定 or 空文字で削除。空文字削除後は再び `view_count` 由来の派生で扱われる（unread に戻すなら view_count を 0 にしないと派生で `reading` になる点に注意）。
- **最終ページ到達**: フロントエンドの `ReaderPanel` で `isAtLastSpread && readState !== 'done'` のときに 1 度だけ `PATCH /api/meta` で `done` に書き換える（多重発火は `useRef` でガード、選択 PDF が変わったらリセット）。詳細は [詳細設計書_フロントエンド編](詳細設計書_フロントエンド編.md)。
- **バリデーション**: `routers/meta.update_meta` で `meta_store.VALID_READ_STATES` に含まれない値（空文字以外）が来たら 400。`merge_entry_fields` の `read_state` パラメータは `None`（変更しない）/ `''`（削除）/ `'unread'|'reading'|'done'`（設定）の 3 系統。
- **API**:
    - `POST /api/meta/view` — body: `{ path: string, name: string, source: string }`。レスポンス `{ view_count: number, last_viewed_at: number, incremented: boolean }`。`incremented` は連打抑制に該当した場合 `false`。
    - `GET /api/meta` のレスポンスにも `view_count` / `last_viewed_at` が含まれる。

UI 側のソート挙動（`view_desc` / `recent_view`）は [詳細設計書_フロントエンド編.md](詳細設計書_フロントエンド編.md) の「閲覧回数 / 最近見た順ソート（フロントエンド側）」を参照。

---

### Kindleキャプチャツール (`kindle-pdf/`)

#### `KindleCapturer` (`kindle-pdf/capturer.py`)
- **役割**: 基本的なKindleウィンドウ操作、スクリーンショット撮影、PDF作成。
- **出力先解決** (`_resolve_comic_dir(subdir)`): `PIC2PDF_DATA_DIR/comic/<subdir>` → なければ `backend/data/comic/<subdir>`。`.env` を起動時に読み込む。
- **主要メソッド**:
    - `find_window()`: Kindleウィンドウのハンドル取得。
    - `get_book_title()`: タイトル入力ダイアログ（`BookInfoDialog`）の表示。
    - `capture_loop()`: ページめくりと撮影のループ。

#### `AutoKindleCapturer` (`capturer.py`)
- **継承**: `KindleCapturer`
- **役割**: 漫画/雑誌用の自動クロップ（黒帯検出）。フルスクリーンモードを使用。

#### `NovelKindleCapturer` (`novel_capturer.py`)
- **継承**: `AutoKindleCapturer`
- **役割**: 小説用の自動クロップ（X軸白背景検出）と画像保存。
- **出力先解決** (`_resolve_images_dir()`): `KINDLE_NOVEL_IMAGES_DIR` → `PIC2PDF_DATA_DIR/kindle_novel/images` → `backend/data/kindle_novel/images` の優先順。
- **特徴**: OCR/PDF生成は行わず画像保存のみ（OCR は job queue 経由）。撮影速度を優先。

#### `BookInfoDialog` (`capturer.py`)
- **役割**: 書籍情報入力ダイアログ。
- **入力**: タイトル、ページめくり方向（左キー/右キー）。

#### `SearchablePdfGenerator` / `BatchOCR`（Phase 5 で削除済み）
- `kindle-pdf/searchable_pdf.py` および `kindle-pdf/batch_ocr.py` は Phase 5（旧 PDF 経路撤去）で削除済み。
- novel ソースの OCR → DB 取り込みは `services/novel_db/extractor.py`（`run_ocr_subprocess`）が担い、`D:\61.tool\common\ocr\` の独立 venv を subprocess 呼び出しする。
- 詳細設計: [機能別/OCR設計書.md](機能別/OCR設計書.md)

---

### OCRエンジン (`D:\61.tool\common\ocr\`)

#### `YomitokuEngine` (`ocr_engine.py`)
- **役割**: `yomitoku` ライブラリを用いたOCR処理。
- **主要メソッド**:
    - `extract_text()`: 画像からテキストを抽出。段落判定または単語判定に分岐。
    - `_process_words()`: 単語情報の処理。thickness統一・aspect比判定・フリガナ除去・正規化。
    - `filter_ruby_text()`: ヒストグラム谷（valley）自動検出によるフリガナ除去。
    - `normalize_text()`: 3点リーダー・特殊記号の正規化。
- 詳細設計: [機能別/OCR設計書.md](機能別/OCR設計書.md)
