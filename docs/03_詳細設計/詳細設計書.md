# 詳細設計書

## 1. ディレクトリ構成
```
Pic2PDF_Viewer/
├── backend/
│   ├── data/               # データ格納用
│   │   ├── main/           # メイン（生成済）データ格納用
│   │   │   ├── pdfs/           # PDFファイルの保存・配信場所 (Static Mount: /pdfs)
│   │   │   ├── pdfs_compressed/ # 圧縮版PDFの保存場所（静的マウントなし、必要なら別途参照）
│   │   │   ├── thumbnails/     # サムネイル画像の保存・配信場所 (Static Mount: /thumbnails)
│   │   │   ├── images/         # 閲覧用WebP画像の保存・配信場所 (Static Mount: /images)
│   │   │   └── complete/       # 処理済みソースファイルの移動先
│   │   ├── kindle/         # Kindle専用データ (Static Mount: /kindle/...)
│   │   │   ├── pdfs/
│   │   │   ├── thumbnails/
│   │   │   └── images/
│   │   ├── kindle_novel/   # Kindle小説用データ (Static Mount: /kindle_novel/...)
│   │   │   ├── pdfs/       # Searchable PDF
│   │   │   ├── thumbnails/
│   │   │   └── images/     # キャプチャ生画像
│   │   └── meta/           # 書籍メタデータ（作者名等）
│   │       ├── generated/meta.json
│   │       ├── kindle/meta.json
│   │       └── novel/meta.json
│   ├── routers/            # APIルーター
│   │   ├── library.py      # PDFs一覧・書籍画像・フォルダ管理・移動・リネーム
│   │   ├── pdfs.py         # PDF生成・ページ削除・圧縮・PDF結合
│   │   ├── thumbnails.py   # サムネイル再生成（単発 / 一括）
│   │   ├── ocr.py          # OCR実行・停止・ステータス
│   │   └── meta.py         # 書籍メタデータ取得・更新・閲覧記録・自動登録ジョブ
│   ├── services/           # ビジネスロジック
│   │   ├── ocr_service.py  # OCRプロセス管理 (OCRService)
│   │   ├── pdf_generator.py # PDF生成ロジック (PdfGenerator Class / scan_and_generate / batch_compress)
│   │   ├── pdf_service.py  # PDF操作 (PdfService)
│   │   ├── thumbnail_service.py # サムネイル生成 (ThumbnailService)
│   │   ├── file_manager.py # PDF・サムネイル・画像の3点セット操作 (FileManager)
│   │   ├── job_manager.py  # 非同期PDF生成ジョブ管理 (GenerateJob, JobStore)
│   │   ├── meta_store.py   # meta.json CRUD・ソース別ロック管理 (update_meta_locked)
│   │   ├── auto_fill_service.py # サークル名自動登録ジョブ管理 (AutoFillState)
│   │   └── author_resolver.py # タイトルからサークル名/著者名を推定 (Web検索 + Gemma)
│   ├── utils/
│   │   ├── path_utils.py   # パスバリデーション一元化
│   │   ├── file_naming.py  # サムネイル名生成ユーティリティ
│   │   ├── file_utils.py   # 拡張子チェックユーティリティ (is_pdf_file / is_webp_file / is_zip_file / is_image_file)
│   │   └── logger.py       # ロギング設定
│   ├── tests/              # pytestテスト
│   │   ├── test_path_utils.py    # パスバリデーションのユニットテスト
│   │   ├── test_meta.py          # make_key / record_view / update_authors / run_auto_fill のテスト
│   │   ├── test_file_manager.py  # FileManager 移動・リネーム・ロールバックのテスト
│   │   ├── test_pdf_generator.py # _collect_images / scan_and_generate / batch_compress のテスト
│   │   └── test_img2pdf.py       # img2pdf 動作確認
│   ├── exceptions.py       # カスタム例外クラス (FileOperationError / OcrProcessError / AutoFillError)
│   ├── config.py           # パス定数・OCR起動設定・サポートフォーマット定数・GEMMA_TOOL_DIR
│   ├── main.py             # FastAPIエントリーポイント（exception_handler 登録含む）
│   └── requirements.txt    # Python依存関係
├── kindle-pdf/             # Kindle自動化ツール
│   ├── main_auto.py        # 漫画/雑誌用エントリーポイント
│   ├── main_novel.py       # 小説用エントリーポイント
│   ├── main_manual.py      # 手動撮影用エントリーポイント
│   ├── capturer.py         # 共通キャプチャロジック (Manga)
│   ├── novel_capturer.py   # 小説用キャプチャロジック (Novel)
│   ├── batch_ocr.py        # OCRバッチ処理スクリプト
│   ├── searchable_pdf.py   # SearchablePdfGenerator
│   ├── start_batch_ocr.bat # OCR起動ランチャー (.bat経由)
│   └── requirements.txt
├── start.bat               # バックエンド・フロントエンド同時起動スクリプト
├── 起動方法.md              # 起動手順メモ
├── frontend/
│   ├── src/
│   │   ├── components/     # 共通コンポーネント
│   │   │   ├── Layout.tsx  # グローバルレイアウト
│   │   │   ├── ErrorBoundary.tsx # エラーバウンダリ
│   │   │   ├── ui/         # 共通UIプリミティブ
│   │   │   │   ├── Dialog.tsx        # ダイアログ共通シェル（オーバーレイ・Header/Body/Footer・ボタン）
│   │   │   │   └── ConfirmDialog.tsx # 汎用確認ダイアログ（confirm() の置換）
│   │   │   ├── generator/  # PDF生成ページ専用コンポーネント
│   │   │   │   ├── CompressionOptions.tsx # 圧縮版チェックボックス + 品質スライダー
│   │   │   │   ├── JobProgress.tsx        # 生成ジョブ進捗パネル
│   │   │   │   └── StatusTable.tsx        # 生成対象ステータス一覧表
│   │   │   ├── reader/     # リーダー・ライブラリ関連コンポーネント（プレゼンテーション層）
│   │   │   │   ├── index.ts
│   │   │   │   ├── FolderGrid.tsx
│   │   │   │   ├── HeaderSearchBar.tsx # タイトル検索入力・作者フィルター選択
│   │   │   │   ├── HeaderSortSelect.tsx # ソート順選択ドロップダウン
│   │   │   │   ├── LazyThumbnail.tsx   # IntersectionObserver遅延読み込み
│   │   │   │   ├── LibraryHeader.tsx
│   │   │   │   ├── MoveDialog.tsx
│   │   │   │   ├── PageRenderer.tsx
│   │   │   │   ├── PdfGrid.tsx
│   │   │   │   ├── PdfSearchBar.tsx    # PDF内テキスト検索バー
│   │   │   │   ├── ReaderHeader.tsx
│   │   │   │   ├── SourceSelector.tsx  # ソース切り替えタブ（generated / kindle / novel）
│   │   │   │   └── ToastContainer.tsx  # トースト通知表示（右下固定・種別色分け）
│   │   │   └── viewer/     # ライブラリ・リーダー統合コンポーネント（状態管理層）
│   │   │       ├── index.ts
│   │   │       ├── LibraryPanel.tsx    # ライブラリUI（一覧・ソート・お気に入り・作者フィルター）
│   │   │       ├── LibraryDialogs.tsx  # ライブラリ内ダイアログ群を集約（Move/Rename/CreateFolder/BulkAuthor/Merge）
│   │   │       ├── ReaderPanel.tsx     # PDFリーダーUI（ページ表示・検索）
│   │   │       ├── CreateFolderDialog.tsx # フォルダ作成ダイアログ（Tailwind実装）
│   │   │       ├── RenameDialog.tsx    # PDF/フォルダ共用リネームダイアログ
│   │   │       ├── BulkAuthorDialog.tsx   # 複数書籍への作者名一括設定ダイアログ
│   │   │       ├── MergeDialog.tsx     # 複数PDFの結合ダイアログ
│   │   │       └── AutoFillAuthorsBar.tsx # サークル名自動登録バー（実行・モード選択・進捗表示）
│   │   ├── features/       # 機能別モジュール
│   │   │   └── ocr/
│   │   │       └── OCRPanel.tsx  # OCR実行UI
│   │   ├── config/         # 設定ファイル
│   │   │   ├── api.ts      # API URL設定
│   │   │   └── api_client.ts # 共通APIクライアント (axiosベース)
│   │   ├── utils/          # ユーティリティ
│   │   │   ├── storage.ts    # localStorage の JSON 読み書き
│   │   │   └── validation.ts # ファイル名/フォルダ名バリデーション (validateFilename)
│   │   ├── constants.ts    # マジックナンバー定数（STORAGE_KEYS / API_CONFIG / UI_CONFIG）
│   │   ├── hooks/          # カスタムフック
│   │   │   ├── index.ts
│   │   │   ├── useWindowSize.ts
│   │   │   ├── useReaderNavigation.ts
│   │   │   ├── useBookImages.ts
│   │   │   ├── useImagePreloader.ts    # 画像先読みフック
│   │   │   ├── useLibraryManagement.ts # ライブラリ操作フック（フォルダ作成・移動・リネーム）
│   │   │   ├── usePolling.ts           # 共通ポーリングフック
│   │   │   ├── usePdfStatus.ts         # PDF生成ステータス監視
│   │   │   ├── useOcrStatus.ts         # OCRステータス監視
│   │   │   ├── useDarkMode.ts          # ダークモード管理 (localStorage永続化)
│   │   │   ├── useFavorites.ts         # お気に入り管理 (source別localStorage)
│   │   │   ├── useSortedPdfs.ts        # PDF並び替え (useMemo)
│   │   │   ├── useUrlState.ts          # URLパラメータ同期 (path/file/source の読み書き)
│   │   │   ├── useBookMeta.ts          # 書籍メタデータ（作者名）取得・更新・集計
│   │   │   ├── useGenerateJob.ts       # PDF生成ジョブ管理（localStorage永続化・ポーリング）
│   │   │   ├── useAutoFillAuthors.ts   # サークル名自動登録ジョブ進捗管理・実行制御
│   │   │   ├── useLibraryFilter.ts     # タイトル検索・作者フィルタリング (useMemo)
│   │   │   ├── usePdfSearch.ts         # PDF内テキスト検索（全ページ走査・ハイライト）
│   │   │   ├── useToast.ts             # トースト通知管理（4秒自動消去）
│   │   │   ├── useSpreadMode.ts        # 見開きモード循環 + ページサイズによる Auto 判定
│   │   │   ├── useEditMode.ts          # リーダー編集モード（ページ選択・削除リクエスト・確認）
│   │   │   ├── useFullscreen.ts        # ブラウザのフルスクリーン API ラッパー
│   │   │   └── useReaderShortcuts.ts   # リーダー画面のキーボードショートカット集約
│   │   ├── types/          # 型定義 (PdfFile, LibrarySource, SortOrder, GenerateJob, BookMetaMap, SpreadMode 等)
│   │   │   └── index.ts
│   │   ├── contexts/       # React Context
│   │   │   └── LibraryContext.tsx  # ライブラリ状態一元管理 (LibraryProvider + useLibraryContext)
│   │   ├── pages/          # ページコンポーネント
│   │   │   ├── ViewerPage.tsx  # LibraryProvider でラップするだけのシンプルな構造
│   │   │   ├── GeneratorPage.tsx
│   │   │   └── OCRPage.tsx  # OCR実行ページ (/ocr)
│   │   └── App.tsx         # ルーティング定義
│   └── package.json
└── docs/
    ├── 01_要件定義/
    │   └── 要件定義書.md
    ├── 02_基本設計/
    │   └── 基本設計書.md
    ├── 03_詳細設計/
    │   ├── 詳細設計書.md       # 本詳細設計書（ディレクトリ構成・クラス設計）
    │   ├── API仕様書.md
    │   └── OCR設計書.md
    ├── 04_環境構築/
    │   └── GPU環境セットアップ.md
    └── 05_記録/
        ├── 変更履歴.md
        └── 実装ログ.md
```

### 1.2 データ配置 (Backend)
- `backend/data/`: データ格納ルート
    - `main/`: メイン（生成済）データ格納用
    - `kindle/`: Kindleキャプチャ (漫画)
    - `kindle_novel/`: Kindleキャプチャ (小説)
    - `meta/`: 書籍メタデータ（ソース別 `meta.json`）
        - キー形式: `"{path}/{filename}"` または `"{filename}"`（path が空の場合）
        - 値形式: `{"authors": ["作者A", "作者B"]}`

### 1.3 構成設定 (config.py)
- **パス定数**: `PDF_DIR`, `PDF_COMPRESSED_DIR`, `THUMBNAIL_DIR`, `IMAGES_DIR`, `COMPLETE_DIR`
- **Kindle**: `KINDLE_PDF_DIR`, `KINDLE_THUMBNAIL_DIR`, `KINDLE_IMAGES_DIR`
- **Kindle Novel**: `KINDLE_NOVEL_PDF_DIR`, `KINDLE_NOVEL_THUMBNAIL_DIR`, `KINDLE_NOVEL_IMAGES_DIR`
- **OCR起動設定**: `BATCH_OCR_LAUNCHER` — `kindle-pdf/start_batch_ocr.bat` へのパス。`.bat` ファイル経由で正しいPython環境とPATHを設定してOCRを起動する。
- **フロントエンド配信**: `FRONTEND_DIST_DIR` — `frontend/dist/` の絶対パス。リリースモード起動時に存在すれば `main.py` が SPA を同一オリジンで配信する。
- **サポートフォーマット**: `SUPPORTED_IMAGE_FORMATS`, `SUPPORTED_WEBP_FORMAT`, `SUPPORTED_ZIP_FORMAT`
- **数値定数**: `THUMBNAIL_HEIGHT`, `JOB_MAX_JOBS`, `OCR_LOG_MAXLEN`

---

## 2. クラス設計

### バックエンド (`backend/`)

#### `OCRService` (`backend/services/ocr_service.py`)
- **役割**: OCRバックグラウンドプロセスの管理、ログ収集、ステータス管理。
- **主要メソッド**:
    - `start_ocr()`: `batch_ocr.py` をサブプロセスとして起動。UTF-8エンコーディングを強制。
    - `stop_ocr()`: 実行中のプロセスを停止 (Terminate/Kill)。
    - `get_status()`: 現在の状態と直近のログを返却。

#### `GenerateJob` / `JobStore` (`backend/services/job_manager.py`)
- **役割**: 非同期PDF生成ジョブのライフサイクル管理。`routers/pdfs.py` から分離。
- **設計**:
    - `GenerateJob`: UUID・ステータス・進捗アイテム・ファイルリスト・エラーを保持。`threading.Lock` で状態更新をスレッドセーフに。
    - `JobStore`: 最大 `JOB_MAX_JOBS` 件のジョブをメモリ管理。FIFO方式で古いジョブを自動削除。
    - `JobStore.get_active_current_item()`: 最新の RUNNING ジョブの `current_item` を返す。`/api/status` エンドポイントで「処理中アイテム」を判定するために使用（旧 `GenerateState` は廃止）。
- **エンドポイント**:
    - `POST /api/generate` → バックグラウンドスレッド起動 + `{job_id}` を即返却。
    - `GET /api/generate/job/{job_id}` → ジョブ進捗取得（クライアントが1500msでポーリング）。

#### `FileManager` (`backend/services/file_manager.py`)
- **役割**: PDF・サムネイル・画像ディレクトリの3点セット操作を一元管理。`routers/library.py` の重複コードを集約。
- **主要メソッド**:
    - `move_with_assets(item, src_path, dst_path, dirs)`: PDF+サムネイル+画像ディレクトリを移動。失敗時は自動ロールバック。
    - `rename_with_assets(request, dirs)`: PDF+サムネイル+画像ディレクトリをリネーム。失敗時は自動ロールバック。

#### `PdfGenerator` (`backend/services/pdf_generator.py`)
- **役割**: ディレクトリやZIPファイルのスキャン、画像からのPDF生成、ファイル移動を一元管理。
- **設計**:
    - ZIP・ディレクトリのどちらも「`images_dir` にファイルを配置 → `_collect_images` → `_generate_outputs`」という共通フローで処理する。bytes/ファイルパスの二重実装を排除。
- **主要メソッド・関数**:
    - `_collect_images(images_dir)`: ディレクトリ内の WebP を natsorted で収集するモジュールレベル関数。
    - `process_directory()`: WebP を `images_dir` にコピーして共通フローへ。
    - `process_zip()`: ZIP を `images_dir` に展開して共通フローへ。
    - `_generate_outputs(item_name, item_images_dir)`: サムネイル・PDF・圧縮PDFの共通生成処理。
    - `run()`: 処理の実行と、完了ファイルの移動・空ディレクトリ削除の制御。

#### `PdfService` (`backend/services/pdf_service.py`)
- **役割**: 既存PDFの編集操作を一元管理。
- **主要メソッド**:
    - `delete_pages()`: 指定されたページの削除とPDFの再保存。
    - `get_page_count()`: PDFの総ページ数を取得。

#### `ThumbnailService` (`backend/services/thumbnail_service.py`)
- **役割**: PDFからのサムネイル画像生成。
- **主要メソッド**:
    - `generate_thumbnail()`: 最初のページを座標指定またはスケール指定で画像出力。

#### `MetaStore` (`backend/services/meta_store.py`)
- **役割**: meta.json の読み書き・ソース別ロック管理を一元化。`routers/meta.py` と `auto_fill_service.py` が共用するインフラ層。
- **型定義**:
    - `MetaEntry(TypedDict)`: `authors: list[str]` / `tags?: list[str]` / `view_count?: int` / `last_viewed_at?: float`（`tags` 等は任意フィールド）
    - `MetaDict = dict[str, MetaEntry]`
- **主要関数**:
    - `get_lock(source)`: ソース別 `threading.Lock` を返す（遅延初期化）。
    - `load_meta(source)` / `save_meta(source, data)`: JSON の読み書き。
    - `make_key(path, name)`: `"{path}/{name}"` または `"{name}"` のキーを生成。
    - `update_meta_locked(source, updater)`: ロック取得→ロード→updater適用→保存を一括で行うユーティリティ。

#### `AutoFillService` (`backend/services/auto_fill_service.py`)
- **役割**: サークル名自動登録ジョブの状態管理・スレッド起動。`routers/meta.py` から抽出。
- **設計**:
    - `AutoFillState` dataclass: `status / total / done / skipped / current / results / error` を保持。
    - `mode` パラメータ（`missing_only` / `unknown_only` / `overwrite_all`）で処理対象を制御。
        - `missing_only`: 作者名エントリが未登録の書籍のみ処理。
        - `unknown_only`: 「作者不明」または未登録の書籍を処理（デフォルト）。
        - `overwrite_all`: 全件を上書き処理。
    - `state.total` は対象件数（スキップ分を除く）で設定する。
    - SearXNG 上限対策として件数間に `AUTOFILL_REQUEST_DELAY_SEC`（5.0秒）の sleep を挿入。
    - **既存値の保持**: 各書籍のメタデータを更新する際、既存の `view_count` / `last_viewed_at` 等の他フィールドは保持し、`authors` のみを上書きする（`{**existing, "authors": [a]}` パターン）。`PATCH /api/meta` と同じ挙動。
- **主要関数**:
    - `get_auto_fill_state(source)` / `reset_auto_fill_state(source)`: ソース別状態管理。
    - `run_auto_fill(source, mode)`: バックグラウンドで実行するジョブ本体。
    - `start_auto_fill_job(source, mode)`: バックグラウンドスレッドを起動。

#### メタデータAPI (`backend/routers/meta.py`)
- **役割**: 書籍メタデータ（作者名）の HTTP ハンドラのみを担う。ロジックは `MetaStore` / `AutoFillService` に委譲。
- **エンドポイント**:
    - `GET /api/meta?source=` → meta.json 全体を返す。
    - `PATCH /api/meta` → `names`（ファイル名リスト）と `authors` / `tags`（いずれかまたは両方）を受け取り、複数冊を一括上書き。指定されなかったフィールドは保持される。
    - `POST /api/meta/view` → 閲覧記録（`view_count` / `last_viewed_at` 更新、連打抑制 5 分）。
    - `POST /api/meta/auto-fill` → 自動登録ジョブを起動。
    - `GET /api/meta/auto-fill/status` → ジョブ進捗を返す。
    - `GET /api/meta/auto-fill/test` → 1件分をデバッグ実行して中間結果を返す。

---

### Kindleキャプチャツール (`kindle-pdf/`)

#### `KindleCapturer` (`kindle-pdf/capturer.py`)
- **役割**: 基本的なKindleウィンドウ操作、スクリーンショット撮影、PDF作成。
- **主要メソッド**:
    - `find_window()`: Kindleウィンドウのハンドル取得。
    - `get_book_title()`: タイトル入力ダイアログ（`BookInfoDialog`）の表示。
    - `capture_loop()`: ページめくりと撮影のループ。

#### `AutoKindleCapturer` (`capturer.py`)
- **継承**: `KindleCapturer`
- **役割**: 漫画/雑誌用の自動クロップ（黒帯検出）。フルスクリーンモードを使用。

#### `NovelKindleCapturer` (`novel_capturer.py`)
- **継承**: `AutoKindleCapturer`
- **役割**: 小説用の自動クロップ（X軸白背景検出）と画像保存。
- **特徴**: OCR/PDF生成機能は削除（`batch_ocr.py`へ委譲）。撮影速度を優先。

#### `BookInfoDialog` (`capturer.py`)
- **役割**: 書籍情報入力ダイアログ。
- **入力**: タイトル、ページめくり方向（左キー/右キー）。

#### `SearchablePdfGenerator` (`kindle-pdf/searchable_pdf.py`)
- **役割**: 画像とOCR結果から「透明テキスト付きPDF」を生成する。
- **使用ライブラリ**: `ReportLab`
- **主要メソッド**:
    - `add_page(image_path, ocr_results)`: 画像を描画し、その上に検索用テキスト（透明）を配置する。
    - `_draw_vertical_text`: **1文字ずつ個別の `TextObject` で縦に配置**（旧: `-90°` 回転+`textOut(全文)` 方式は廃止）。
    - `_draw_horizontal_text`: 横書き（ルビ等）の配置。
- 詳細設計: [OCR設計書.md](OCR設計書.md)

#### `BatchOCR` (`kindle-pdf/batch_ocr.py`)
- **役割**: `kindle_novel/images` 配下の未処理フォルダを走査し、自動的にSearchable PDF化する。
- **フロー**: フォルダ検知 → OCR (`yomitoku`) → PDF生成 (`SearchablePdfGenerator`)

---

### OCRエンジン (`D:\61.tool\common\ocr\`)

#### `YomitokuEngine` (`ocr_engine.py`)
- **役割**: `yomitoku` ライブラリを用いたOCR処理。
- **主要メソッド**:
    - `extract_text()`: 画像からテキストを抽出。段落判定または単語判定に分岐。
    - `_process_words()`: 単語情報の処理。thickness統一・aspect比判定・フリガナ除去・正規化。
    - `filter_ruby_text()`: ヒストグラム谷（valley）自動検出によるフリガナ除去。
    - `normalize_text()`: 3点リーダー・特殊記号の正規化。
- 詳細設計: [OCR設計書.md](OCR設計書.md)

---

### フロントエンドContext (`frontend/src/contexts/`)

#### `LibraryContext` (`frontend/src/contexts/LibraryContext.tsx`)
- **役割**: ViewerPage が LibraryPanel に渡していた 21 個の props を Context に集約し、Props Drilling を解消する。
- **設計**:
    - `LibraryProvider`: `useUrlState` + PDF フェッチ + `useLibraryManagement` を内包し、`LibraryContext.Provider` で子にデータを提供。
    - `useLibraryContext()`: Provider 外で呼ばれると `throw Error` するカスタムフック。
    - `ViewerPage` は `LibraryProvider` でラップするだけの薄い構造になる。
    - `LibraryPanel` は props を受け取らず `useLibraryContext()` で必要な値・ハンドラを取得する。
- **提供する値 (LibraryContextValue)**:
    - 状態: `pdfs`, `directories`, `selectedPdf`, `currentPath`, `currentSource`, `isSelectionMode`, `selectedItems`, `isMoveDialogOpen`, `isCreateFolderOpen`, `renameTarget`
    - ナビゲーション: `onPdfClick`, `onFolderClick`, `onUpClick`, `onSourceChange`, `onClosePdf`, `onPdfUpdated`
    - ダイアログ操作: フォルダ作成・移動・リネームの開閉・実行ハンドラ群
    - `onRefresh`: PDF 一覧の再取得

---

### フロントエンドフック (`frontend/src/hooks/`)

#### `useDarkMode`
- **役割**: ダークモード状態の管理と永続化。
- **設計**:
    - `index.html` インラインスクリプトと連携し、React マウント前に `<html class="dark">` を適用してフラッシュを防止。
    - `useState` 初期化フェーズで即座に `classList` を操作（`useEffect` 待ちのラグを排除）。
    - `localStorage` キー `darkMode` に `true/false` 文字列で永続化。
    - システムカラースキーム (`prefers-color-scheme: dark`) をフォールバックとして使用。

#### `useFavorites`
- **役割**: お気に入りPDFの管理（ソース別）。
- **設計**:
    - `localStorage` キー `favorites_{source}` に `string[]` (JSON) で永続化。
    - `source` が切り替わった際に `useEffect` で該当ソースのデータを再ロード。
    - `toggle(name)` はimmutableなSet更新 + 即時永続化。

#### `useSortedPdfs`
- **役割**: PDF一覧の並び替えロジック（`useMemo` でメモ化）。
- **ソート順**: `name_asc` / `name_desc` / `date_asc` / `date_desc` / `favorites_first`
    - `favorites_first`: お気に入りを先頭に、同グループ内は名前昇順。
    - 日付ソートは `created_at` (Unix timestamp) を使用。

#### `useBookMeta`
- **役割**: 書籍メタデータ（作者名）の取得・更新・全作者名集計。
- **設計**:
    - `source` が変わると `GET /api/meta` を再取得。
    - `updateAuthors(path, names, authors)` で1冊または複数冊を一括更新し、ローカル状態も即時反映（再フェッチ不要）。
    - `allAuthors`: このソースに登録されている全作者名の重複排除・日本語ソート済みリスト。
- **主要戻り値**:
    - `getAuthors(path, name)`: キーを構築して作者名リストを返す。
    - `updateAuthors(path, names, authors)`: PATCH API を呼び出し、ローカル状態を更新。
    - `allAuthors`: フィルタードロップダウンの選択肢として使用。

#### `useAutoFillAuthors`
- **役割**: サークル名自動登録ジョブの実行制御・進捗ポーリング。
- **設計**:
    - `startAutoFill(mode)` で `POST /api/meta/auto-fill` を呼び出し、`JOB_POLL_INTERVAL_MS`（1500ms）でポーリング開始。
    - `mode`: `'missing_only'` / `'unknown_only'` / `'overwrite_all'`。
    - ジョブ完了時に `onComplete` コールバックを呼び出してメタデータを再フェッチ。
    - `startAutoFill` はエラーを throw するため、呼び出し側でキャッチしてトースト表示する。

#### `useLibraryFilter`
- **役割**: PDF一覧・フォルダ一覧のフィルタリングロジック（`useMemo` でメモ化）。
- **設計**:
    - `searchText`（タイトル前方一致）/ `authorFilter`（作者名一致）/ `currentPath` の変化でフィルタを再計算。
    - `LibraryPanel` から切り出すことでコンポーネントの責務を分離。

#### `usePdfSearch`
- **役割**: PDF内テキスト検索ロジック。`ReaderPanel` から切り出し。
- **設計**:
    - `searchAllPages(text, pdf)` で全ページのテキストを走査してマッチ数・先頭マッチページを算出。
    - `customTextRenderer` で一致箇所を `<mark>` タグでハイライト表示。
    - `onDocumentLoaded(pdf)` でロード直後に検索テキストが入力済みの場合は即座に再検索。

#### `useToast`
- **役割**: トースト通知の状態管理。`alert()` の代替。
- **設計**:
    - `showToast(message, type)` で通知を追加。4秒後に自動消去。`type`: `'success'` / `'error'` / `'info'`。
    - `dismissToast(id)` で手動消去。
    - `ToastContainer` コンポーネントと組み合わせて右下に固定表示。

#### `useSpreadMode`
- **役割**: リーダーの見開きモード（Auto / Spread / Single）と Auto 時のページサイズ判定を一元管理。`ReaderPanel` から切り出し。
- **戻り値**: `spreadMode`, `isSpread`, `cycleSpreadMode`, `handlePageSize`, `resetAutoSpread`
- **挙動**:
    - `spreadMode === 'auto'` 時のみ `handlePageSize(width, height)` で `autoIsSpread` を更新（縦長なら見開き、横長なら 1 ページ）。
    - `spreadMode === 'spread' / 'single'` の場合は `handlePageSize` は no-op。

#### `useEditMode`
- **役割**: リーダーの編集モード（ページ選択 + 削除）を一元管理。`ReaderPanel` から切り出し。
- **設計**:
    - 削除確認は OS ネイティブ `confirm()` を使わず、フック側で「削除待ち件数」(`pendingDeleteCount`) を保持し、呼び出し側 (`ReaderPanel`) が `<ConfirmDialog>` を表示する。
    - `requestDeletePages()` で確認ダイアログを開く → `confirmDeletePages()` で API 削除実行、`cancelDeletePages()` でキャンセル。
- **戻り値**: `isEditMode`, `selectedPages`, `toggleEditMode`, `togglePageSelection`, `resetEditMode`, `requestDeletePages`, `confirmDeletePages`, `cancelDeletePages`, `pendingDeleteCount`

#### `useFullscreen`
- **役割**: Document Fullscreen API のラッパー。リーダー画面のフルスクリーン切り替え。
- **設計**:
    - `document.fullscreenElement` の有無で `isFullscreen` を判定。
    - `fullscreenchange` イベントを購読してブラウザ UI / Esc キー経由の解除も追従する。
    - `toggleFullscreen()` は失敗時（ユーザー拒否・iframe 等）は黙ってスキップ。
- **戻り値**: `isFullscreen`, `toggleFullscreen`

#### `useReaderShortcuts`
- **役割**: リーダー画面のキーボードショートカットを 1 か所に集約する。
- **キー割り当て**:
    - `f` — フルスクリーン切替
    - `e` — 編集モード切替
    - `g` — ページジャンプ入力にフォーカス（`focusPageJump` ハンドラを呼ぶ）
    - `?` — ショートカット一覧モーダル表示
    - `Ctrl+F` / `Cmd+F` — 検索バーを開く
- **設計**:
    - 入力中（`<input>` / `<textarea>` / `contentEditable`）の場合はキー割り当てを抑制する。
    - `isActive` フラグで PDF リーダーがアクティブな間だけ動かす。
    - 矢印キーは既存の `useReaderNavigation` が管理（重複を避ける）。
- **引数**: `{ isActive, onToggleFullscreen, onToggleEditMode, onFocusPageJump, onOpenHelp, onToggleSearch }`

#### `useBookImages`
- **役割**: 書籍の画像URL一覧を取得し、画像モード/PDFモードを判定する。
- **設計**:
    - `selectedPdf` が変わると `GET /api/books/{bookPath}/images` を呼び出して画像URLを取得。
    - 画像が取得できた場合は `isImageMode=true`（画像モード）、失敗した場合は `isImageMode=false`（PDFモード）。
    - **`novel` ソースは画像取得をスキップして PDFモードを強制する**。`kindle_novel/images/` に PNG ファイルが存在するが、このソースの目的は Searchable PDF のテキスト検索にあるため、PDF モードで表示する必要がある。

#### `useReaderNavigation`
- **役割**: リーダーのページナビゲーション（前/次ページ、ページジャンプ、リセット）を管理。
- **依存**: `numPages`, `isSpread`, `direction`, `isActive`
- **主要戻り値**: `pageNumber`, `setPageNumber`, `handleNext`, `handlePrev`, `resetPage`

#### `useUrlState`
- **役割**: ViewerPage の URLパラメータ (`?path=`, `?file=`, `?source=`) 同期を一元管理。
- **設計**:
    - `useSearchParams` ラッパー。`currentPath` / `selectedPdf` / `currentSource` を導出して返す。
    - ナビゲーション操作を関数として提供し、ViewerPage での状態二重管理を排除。
    - `source` を URL に含めることでリロード・直接アクセス時にも正しいソースが復元される。
    - `VALID_SOURCES` で無効なソース値を `'generated'` にフォールバック。
- **主要メソッド**:
    - `navigateIntoFolder(dirName, basePath, source)`: フォルダ内に移動（`?path=` を更新）。
    - `navigateUp(currentPath, source)`: 親階層へ戻る。
    - `selectPdf(pdfName, currentPath, source)`: PDF を選択（`?file=` を追加）。
    - `clearPdf(currentPath, source)`: PDF選択を解除してライブラリに戻る。
    - `setSource(source)`: ソース切り替え時に `?source=` だけ変更し、`path` / `file` をリセット。

### フロントエンド共通UIプリミティブ (`frontend/src/components/ui/`)

#### `Dialog` (`frontend/src/components/ui/Dialog.tsx`)
- **役割**: ダイアログ共通シェル。5 ダイアログ（CreateFolder / Rename / BulkAuthor / Move / Merge）が共通で使う。
- **提供サブコンポーネント**: `Dialog`, `DialogBody`, `DialogFooter`, `DialogCancelButton`, `DialogPrimaryButton`
- **共通機能**:
    - 黒オーバーレイ + 中央配置 + 外クリック / Esc キーで閉じる
    - ヘッダー（タイトル + サブタイトル + X ボタン）/ Body / Footer の3パート構成
    - `nested` prop で z-index を切り替え（`z-dialog` / `z-dialog-nested`）
- **z-index**: `tailwind.config.js` の階層クラス (`z-dialog` = 100, `z-dialog-nested` = 200) を使用。任意値構文 `z-[100]` は使わない。

#### `ConfirmDialog` (`frontend/src/components/ui/ConfirmDialog.tsx`)
- **役割**: 汎用確認ダイアログ。OS ネイティブ `confirm()` の置換用。
- **設計**:
    - `<Dialog>` 共通シェルをベースに、確認ボタン + キャンセルボタンを提供。
    - `danger` prop で危険操作（削除等）を赤系ボタンで表示。
    - 使用例: `ReaderPanel` のページ削除確認、`useEditMode` と組み合わせて使う。

---

### フロントエンドコンポーネント

#### `LazyThumbnail` (`frontend/src/components/reader/LazyThumbnail.tsx`)
- **役割**: サムネイル画像の遅延読み込み。
- **設計**: `IntersectionObserver` で viewport から200px前に入った時点で `src` をセット。エラー時は `FileText` アイコンにフォールバック。

#### `PdfSearchBar` (`frontend/src/components/reader/PdfSearchBar.tsx`)
- **役割**: PDF内テキスト検索UI。`ReaderHeader` 直下に固定表示。
- **設計**: 300msデバウンスで親へ検索テキストを通知。Enter/Shift+Enterで次/前へ移動。マッチ数 "X / Y" 表示。

#### `PageRenderer` (`frontend/src/components/reader/PageRenderer.tsx`)
- **役割**: 単一ページのレンダリング（PDFモード・画像モード両対応）。
- **設計**:
    - `onPageSize?: (width, height) => void` コールバックを持ち、ページサイズが確定した時点で親に通知する（自動見開き判定用）。
    - PDFモード: `<Page onRenderSuccess={(page) => onPageSize?.(page.width, page.height)}>` で通知。
    - 画像モード: `<img onLoad={(e) => onPageSize?.(img.naturalWidth, img.naturalHeight)}>` で通知。
    - `ReaderPanel` は右ページ（`side === 'right'`）またはシングルページ（`side === 'single'`）にのみ `onPageSize` を渡し、見開き時の二重発火を防ぐ。

#### `ReaderHeader` (`frontend/src/components/reader/ReaderHeader.tsx`)
- **役割**: リーダー上部コントロールバー。マウスオーバー時のみ表示（没入型モード）。
- **設計**:
    - **見開きモードボタン**: `spreadMode: SpreadMode` (`'auto' | 'spread' | 'single'`) を `onCycleSpreadMode` で循環（Auto → Spread → Single → Auto）。
    - `SPREAD_MODE_CONFIG` 定数でモードごとのラベル・アイコン・次モードを管理。
    - Auto モードは indigo 色で強調表示。他は gray。
    - ページ番号表示はクリックで直接入力（数値入力 → Enter/blur で `onPageJump` 呼び出し）。

#### `CreateFolderDialog` (`frontend/src/components/viewer/CreateFolderDialog.tsx`)
- **役割**: フォルダ作成ダイアログ。`<Dialog>` 共通シェルを使用。
- **設計**: バリデーションは `utils/validation.ts` の `validateFilename(value, 'folder')` に委譲。Enter でサブミット、Esc/外クリックで閉じる挙動は `<Dialog>` 側で吸収。

#### `RenameDialog` (`frontend/src/components/viewer/RenameDialog.tsx`)
- **役割**: PDF/フォルダ共用のリネームダイアログ。`<Dialog>` 共通シェルを使用。
- **設計**:
    - PDF の場合は拡張子 `.pdf` を入力欄から省き、確定時に自動付加。フォルダの場合はそのまま扱う。
    - バリデーションは `validateFilename(value, isFolder ? 'folder' : 'file')`。
    - `isFolder` prop で PDF/フォルダを切り替える。

#### `ReaderPanel` (`frontend/src/components/viewer/ReaderPanel.tsx`)
- **役割**: PDFリーダーUI。ページ表示・見開き制御・検索・ページ削除の **統合（合成）** を担う。各種ロジックはカスタムフックに委譲。
- **Worker設定**:
    - `pdfjs.GlobalWorkerOptions.workerSrc` を `new URL('pdfjs-dist/build/pdf.worker.min.mjs', import.meta.url).toString()` で設定。
    - `<Document>` コンポーネントと同一ファイルで設定する必要がある（react-pdf の要件）。Vite がビルド時にワーカーファイルを自動バンドルする。
    - `react-pdf/dist/Page/TextLayer.css` をインポートしてテキストレイヤーのスタイルを適用（ハイライト表示に必要）。
- **委譲先フック**:
    - `useSpreadMode()` — `spreadMode` / `isSpread` / `cycleSpreadMode` / `handlePageSize` / `resetAutoSpread` を提供。
    - `useEditMode({...})` — `isEditMode` / `selectedPages` / `toggleEditMode` / `togglePageSelection` / `requestDeletePages` / `confirmDeletePages` / `cancelDeletePages` / `pendingDeleteCount` を提供。
    - `useReaderNavigation()` — ページナビゲーション。
    - `usePdfSearch()` — PDF テキスト検索ロジック（マッチ数・ハイライト・カスタムレンダラー）。
- **見開きモード制御** (`useSpreadMode` 内):
    - `spreadMode: SpreadMode` — `'auto' | 'spread' | 'single'` の3状態。
    - 実効 `isSpread` = `spreadMode === 'auto' ? autoIsSpread : spreadMode === 'spread'`
    - `cycleSpreadMode()`: Auto → Spread → Single → Auto の順で循環。
    - `handlePageSize(width, height)`: `spreadMode === 'auto'` 時のみ動作。`width <= height`（縦長）なら見開き、横長なら1ページに設定。
    - PDF切り替え時は `resetAutoSpread()` で `autoIsSpread` を `true` にリセット。
- **テキスト検索**:
    - `pdfRef` に `PDFDocumentProxy` を保持し、全ページのテキストを走査してマッチ数を算出・ハイライト表示。
    - `searchAllPages(text, pdf)` は `pdfRef.current` ではなく引数の `pdf` を直接受け取り、ロード直後の null 参照問題を回避。
    - `onDocumentLoadSuccess`: PDF ロード完了時点で `searchText` が入力済みの場合は即座に `searchAllPages` を呼び出す。
    - 検索バー (`PdfSearchBar`) は `isSearchOpen === true` の間は常に表示（`showHeader` とは独立）。
    - `ReaderHeader` は `showHeader || isSearchOpen` で検索中は常に表示。
- **ページ削除**: `useEditMode` の `requestDeletePages()` で確認状態を作り、`<ConfirmDialog>` で削除確認。`confirmDeletePages()` で実際の削除 API 呼び出し → 完了後 `pdfVersion` をインクリメントして PDF を再ロード。
- **エラー通知**: `alert()` / `confirm()` は使用せず、エラーは `useToast`、確認は `<ConfirmDialog>`（`components/ui/ConfirmDialog.tsx`）を使用する。

#### 閲覧回数 / 最近見た順ソート
- **保存先**: `meta.json` の各エントリに `view_count: int`（任意、デフォルト 0）と `last_viewed_at: float`（任意、UNIX 時刻）を追加。既存エントリには無いため読み込み時に 0 として扱う。
- **記録タイミング**: ライブラリのカードクリックで PDF を開いたタイミング（`LibraryPanel` 内で `onPdfClick` をラップ）で `POST /api/meta/view` を呼ぶ。
- **連打抑制**: `view_count` は前回 `last_viewed_at` から `VIEW_COUNT_DEBOUNCE_SEC = 300`（5分）以上経過した場合のみ +1 する。`last_viewed_at` は呼び出し毎に常に現在時刻で更新する（「最近見た順」ソートに反映するため）。
- **メタデータマージ規則**: `PATCH /api/meta`（作者更新）と `POST /api/meta/view`（閲覧記録）は互いに干渉しない。authors 更新時は既存の `view_count` / `last_viewed_at` を保持し、閲覧記録時は既存の `authors` を保持する。`authors` を空配列で更新した場合、他のフィールドが残っていれば `authors: []` で残し、何も残らなければエントリ自体を削除する。フロントエンドの `useBookMeta` ローカル状態も同じマージ規則で更新する。
- **API**:
    - `POST /api/meta/view` — body: `{ path: string, name: string, source: string }`。レスポンス `{ view_count: number, last_viewed_at: number, incremented: boolean }`。`incremented` は連打抑制に該当した場合 `false`。
    - `GET /api/meta` のレスポンスにも `view_count` / `last_viewed_at` が含まれる。
- **ソート挙動**:
    - `'view_desc'` — 閲覧回数の降順、同数の場合は名前昇順（`localeCompare('ja')`）。
    - `'recent_view'` — 最終閲覧時刻 (`last_viewed_at`) の降順。未閲覧（`undefined`）は末尾、同時刻は名前昇順。
- **UI**: ソートドロップダウン（`HeaderSortSelect`）に「よく見る順」「最近見た順」を追加。カード上には数字を表示しない（ソートのみ）。

#### `BulkAuthorDialog` (`frontend/src/components/viewer/BulkAuthorDialog.tsx`)
- **役割**: 選択モードで複数書籍を選択したときに作者名を一括設定するダイアログ。`<Dialog>` 共通シェルを使用。
- **設計**:
    - タグ形式の入力（Enter/カンマで確定、Backspace で削除）。
    - 「一括適用」で選択中の全書籍の作者名を上書き（既存の作者名は破棄、ただし `view_count` / `last_viewed_at` 等の他フィールドは保持）。
    - `targetCount` prop で対象冊数をヘッダーに表示。
    - 選択モード中にヘッダーの「作者を設定」ボタンから開く。

#### `AutoFillAuthorsBar` (`frontend/src/components/viewer/AutoFillAuthorsBar.tsx`)
- **役割**: サークル名自動登録ジョブの実行ボタン・モード選択ラジオ・進捗バーを 1 か所に集約。`LibraryPanel` から切り出し。
- **設計**:
    - `useAutoFillAuthors` フックを内部で使い、ジョブの起動・進捗ポーリングを管理。
    - props は `source: LibrarySource` と `onComplete: () => void` のみ。
    - エラー時は内蔵のトーストで通知（ヘッダー側のトーストとは別系統）。

#### 選択モードの視覚スタイル（PdfGrid / FolderGrid）
- **選択中カード**: `border-amber-400` の枠 + `bg-amber-50 dark:bg-amber-900/20` の薄い琥珀色背景で表示。
- **PdfGrid のカード本体パネル**（サムネイル下部の書名・作者・日付エリア）も同じ条件で背景色を切り替える。サムネイル領域は画像で覆われるため枠と本体パネルの両方を着色することで選択状態を明確化する。
- **チェックアイコン**: 選択時は `text-amber-500` の `CheckSquare`、未選択時は `text-gray-400` の `Square`。
- **採用理由**: 従来の `border-blue-500` はダークモード（`bg-gray-800` 背景）で視認性が低く、UI 内の他の青系要素（作者タグ等）とも紛らわしかったため、アクセント色として琥珀（amber）に変更。両モードで高コントラスト。

---

## 3. 運用・開発メモ

- **起動方法**: [起動方法.md](../../起動方法.md) を参照
    - 開発モード: Backend `uv run uvicorn main:app --reload` (Port 8000) / Frontend `npm run dev` (Port 5173)
    - リリースモード: `start_release.bat` で Backend (Port 8090) が dist を同一オリジン配信
- **注意点**:
    - `react-pdf` のWorker設定は `new URL('pdfjs-dist/build/pdf.worker.min.mjs', import.meta.url)` で Vite バンドル経由で読み込む。CDN不要。
    - `<Document>` コンポーネントを使うファイルと同じモジュールで `workerSrc` を設定すること（`ReaderPanel.tsx`）。
    - `novel` ソースは Searchable PDF なので PDF モードで表示する（`useBookImages` が画像取得をスキップ）。
    - `data/pdfs` / `data/thumbnails` は静的ファイルとして `/pdfs` / `/thumbnails` パスでマウントされている。
- **環境設定**:
    - Frontend: `frontend/.env` に `VITE_DEFAULT_SOURCE_DIR` を設定することで生成画面のデフォルトパスを変更可能。
    - Backend: `python-dotenv` 導入済み。`.env` ファイルから設定を読み込む。

---

## 4. リリースビルド構成

開発時とは別ポートで動作する「ビルド済み配信」モード。修正時は通常通り dev サーバーで作業し、確定したものを `build_release.bat` でビルドする。

### 4.1 ポート割り当て
| モード | Backend | Frontend | 起動方法 |
|---|---|---|---|
| 開発 | 8000 (uvicorn --reload) | 5173 (vite dev) | `start.bat` |
| リリース | 8090 (uvicorn 単独) | Backend に統合（dist 配信） | `start_release.bat` |

### 4.2 動作原理
1. `npm run build` で `frontend/dist/` に静的アセットを出力。
2. リリース起動時、`backend/main.py` が `FRONTEND_DIST_DIR` の存在をチェックし、存在すれば以下のルートを最後に登録する:
    - `/` → `dist/index.html`
    - `/{full_path:path}` → `dist/` 配下にファイルがあれば返す。なければ `index.html`（SPA fallback）
    - `/api`, `/pdfs`, `/thumbnails`, `/images`, `/kindle/...` などの既存マウントが優先されるよう、SPA 用 catch-all は **最後** に登録する。
3. リリースモードでは同一オリジンで動くため、フロントエンドは `BASE_URL=''` （相対パス）で API を叩く。

### 4.3 フロント側の API URL 切り替え
`frontend/src/config/api.ts` で `import.meta.env.PROD` を判定:
- `true`（`vite build`）→ `BASE_URL = ''`（同一オリジンで相対パス）
- `false`（`vite dev`）→ `VITE_API_URL` または `http://localhost:8000`

### 4.4 起動スクリプト
| ファイル | 役割 |
|---|---|
| `build_release.bat` | `cd frontend && npm run build` を実行（dist 再生成） |
| `start_release.bat` | uvicorn を Port 8090 で起動し、ブラウザで `http://localhost:8090` を開く |

### 4.5 Windows ログイン時の自動起動 / クラッシュ時自動再起動

**タスクスケジューラ** に `Pic2PDF_Viewer` タスクとして登録（`Register-ScheduledTask`）：
- トリガー: `AtLogOn`（対象ユーザーのログオン時）
- アクション: `powershell -Command Start-Process cmd -ArgumentList '/c start_release.bat' -WindowStyle Minimized` で最小化起動
- 設定: `RestartCount=3` / `RestartInterval=1分` / `ExecutionTimeLimit=0`（無制限）
- 二重防御: `start_release.bat` 内にも `:loop` で uvicorn 再起動ループを実装。終了コード 0 のときだけ抜ける

登録・解除コマンドは [起動方法.md](../../起動方法.md) を参照。

### 4.6 文字エンコーディング上の注意
`start_release.bat` は **ASCII のみ** で記述する。日本語コメントを含むと UTF-8 として保存され、`chcp 65001` 後でも cmd.exe のパーサが行をまたいで誤動作する場合がある（実際にハマったので残しておく）。
