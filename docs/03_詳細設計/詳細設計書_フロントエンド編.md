# 詳細設計書（フロントエンド編）

フロントエンド（React + TypeScript + Vite）の詳細設計をまとめる。
ディレクトリ構成全体図・運用メモ・リリースビルド構成は [詳細設計書_共通.md](詳細設計書_共通.md) を参照。
バックエンドの詳細設計は [詳細設計書_バックエンド編.md](詳細設計書_バックエンド編.md) を参照。

---

## 1. ディレクトリ構成（フロントエンド側）

```
frontend/
├── src/
│   ├── components/     # 共通コンポーネント
│   │   ├── Layout.tsx  # グローバルレイアウト
│   │   ├── ErrorBoundary.tsx # エラーバウンダリ
│   │   ├── ui/         # 共通UIプリミティブ
│   │   │   ├── Dialog.tsx        # ダイアログ共通シェル（オーバーレイ・Header/Body/Footer・ボタン）
│   │   │   └── ConfirmDialog.tsx # 汎用確認ダイアログ（confirm() の置換）
│   │   ├── generator/  # PDF生成ページ専用コンポーネント
│   │   │   ├── JobProgress.tsx        # 生成ジョブ進捗パネル
│   │   │   └── StatusTable.tsx        # 生成対象ステータス一覧表
│   │   ├── reader/     # リーダー・ライブラリ関連コンポーネント（プレゼンテーション層）
│   │   │   ├── index.ts
│   │   │   ├── FolderGrid.tsx
│   │   │   ├── HeaderSearchBar.tsx # タイトル/作者/タグ部分一致検索 + 作者・タグフィルター select
│   │   │   ├── HeaderSortSelect.tsx # ソート順選択ドロップダウン
│   │   │   ├── LazyThumbnail.tsx   # IntersectionObserver遅延読み込み
│   │   │   ├── LibraryHeader.tsx   # 検索/ソート/作者・タグフィルター/グループ化セレクト/非表示トグル/シリーズチップ/選択モード（一括「シリーズに登録」含む）
│   │   │   ├── MoveDialog.tsx       # 書籍移動ダイアログ（<Dialog> ベース）
│   │   │   ├── PageRenderer.tsx
│   │   │   ├── PageSlider.tsx      # 下部フローティングページスライダー（ドラッグ遅延コミット・RTL 対応）
│   │   │   ├── PdfCard.tsx          # 書籍カード本体（PdfCardBadge / PdfCardProps 型定義含む）
│   │   │   ├── SortablePdfCard.tsx  # @dnd-kit useSortable ラッパー（ドラッグハンドル付きカード）
│   │   │   ├── PdfGrid.tsx          # 書籍カードグリッドコンテナ（DnD コンテキスト・センサ設定・カード配置）
│   │   │   ├── PdfSearchBar.tsx    # PDF内テキスト検索バー
│   │   │   ├── ReaderHeader.tsx    # 見開き/方向/検索/編集/フルスクリーン/ヘルプ
│   │   │   ├── ShortcutsHelpDialog.tsx # キーボードショートカット一覧モーダル
│   │   │   ├── SourceSelector.tsx  # ソース切り替えタブ（generated / kindle / novel）
│   │   │   └── ToastContainer.tsx  # トースト通知表示（右下固定・種別色分け）
│   │   └── viewer/     # ライブラリ・リーダー統合コンポーネント（状態管理層）
│   │       ├── index.ts
│   │       ├── LibraryPanel.tsx     # ライブラリUI 全体合成（一覧・ソート・お気に入り・フィルター URL 同期・グループ化・非表示）
│   │       ├── LibraryDialogs.tsx   # ライブラリ内ダイアログ群を集約（Move/Rename/CreateFolder/BulkAuthor/BulkTag/BulkSeriesAssign/Merge）
│   │       ├── ReaderPanel.tsx      # PDFリーダーUI（useEditMode/useSpreadMode/useFullscreen/useReaderShortcuts に委譲、シリーズ「次の巻へ」フローティングボタン含む）
│   │       ├── CreateFolderDialog.tsx # フォルダ作成ダイアログ（<Dialog> ベース）
│   │       ├── RenameDialog.tsx     # PDF/フォルダ共用リネームダイアログ（<Dialog> ベース）
│   │       ├── BulkAuthorDialog.tsx # 複数書籍への作者名一括設定ダイアログ（<Dialog> ベース）
│   │       ├── BulkTagDialog.tsx    # 複数書籍へのタグ一括設定ダイアログ（emerald 系）
│   │       ├── BulkSeriesAssignDialog.tsx # 複数書籍を一度にシリーズへ登録（既存追加/新規作成、選択順に自動採番）
│   │       ├── MergeDialog.tsx      # 複数PDFの結合ダイアログ（<Dialog> ベース）
│   │       ├── SeriesEditDialog.tsx # シリーズ手動編集（既存追加/新規作成/解除、小数巻入力可）
│   │       ├── AutoFillAuthorsBar.tsx # サークル名自動登録バー（実行・モード選択・進捗表示）
│   │       └── SeriesResolveBar.tsx # シリーズ判定ジョブ実行バー（Gemma 補助併用チェック・進捗表示）
│   ├── features/       # 機能別モジュール
│   │   └── ocr/
│   │       └── OCRPanel.tsx  # OCR実行UI
│   ├── config/         # 設定ファイル
│   │   ├── api.ts      # API URL設定
│   │   └── api_client.ts # 共通APIクライアント (axiosベース)
│   ├── utils/          # ユーティリティ
│   │   ├── storage.ts    # localStorage の JSON 読み書き
│   │   └── validation.ts # ファイル名/フォルダ名バリデーション (validateFilename)
│   ├── constants.ts    # マジックナンバー定数（STORAGE_KEYS / API_CONFIG / UI_CONFIG）
│   ├── hooks/          # カスタムフック
│   │   ├── index.ts
│   │   ├── useWindowSize.ts
│   │   ├── useReaderNavigation.ts
│   │   ├── useBookImages.ts
│   │   ├── useImagePreloader.ts    # 画像先読みフック
│   │   ├── useLibraryManagement.ts # ライブラリ操作フック（フォルダ作成・移動・リネーム）
│   │   ├── usePolling.ts           # 共通ポーリングフック
│   │   ├── usePdfStatus.ts         # PDF生成ステータス監視
│   │   ├── useOcrStatus.ts         # OCRステータス監視
│   │   ├── useDarkMode.ts          # ダークモード管理 (localStorage永続化)
│   │   ├── useFavorites.ts         # お気に入り管理 (source別localStorage)
│   │   ├── useSortedPdfs.ts        # PDF並び替え (useMemo)
│   │   ├── useUrlState.ts          # URLパラメータ同期 (path/file/source の読み書き)
│   │   ├── useBookMeta.ts          # 書籍メタデータ（作者名）取得・更新・集計
│   │   ├── useGenerateJob.ts       # PDF生成ジョブ管理（localStorage永続化・ポーリング）
│   │   ├── useAutoFillAuthors.ts   # サークル名自動登録ジョブ進捗管理・実行制御
│   │   ├── useLibraryFilter.ts     # タイトル検索・作者フィルタリング (useMemo)
│   │   ├── usePdfSearch.ts         # PDF内テキスト検索（全ページ走査・ハイライト）
│   │   ├── useToast.ts             # トースト通知管理（4秒自動消去）
│   │   ├── useSpreadMode.ts        # 見開きモード循環 + ページサイズによる Auto 判定
│   │   ├── useEditMode.ts          # リーダー編集モード（ページ選択・削除リクエスト・確認）
│   │   ├── useFullscreen.ts        # ブラウザのフルスクリーン API ラッパー
│   │   ├── useReaderShortcuts.ts   # リーダー画面のキーボードショートカット集約
│   │   ├── useSeriesResolve.ts     # シリーズ判定ジョブの実行制御・進捗ポーリング
│   │   ├── useNextSeriesVolume.ts  # 次巻/前巻探索（usePrevSeriesVolume も同ファイルに定義）
│   │   ├── useLibraryGrouping.ts   # ライブラリの集約表示（none / series / author）
│   │   ├── useUrlFilters.ts        # author/tag/series フィルターの URL クエリ同期 + clearAllDrilldown
│   │   ├── useLibrarySettings.ts   # sort/groupMode/showHidden/showUnreadOnly の localStorage 永続化
│   │   ├── useLibraryBulkActions.ts # ライブラリ一括操作（authors/tags/hidden/thumbnail/merge/series）
│   │   └── useLibraryDisplay.ts    # effectiveGroupMode / displayPdfs / breadcrumbs の派生計算
│   ├── types/          # 型定義 (PdfFile, LibrarySource, SortOrder, GenerateJob, BookMetaMap, SpreadMode 等)
│   │   └── index.ts
│   ├── contexts/       # React Context
│   │   └── LibraryContext.tsx  # ライブラリ状態一元管理 (LibraryProvider + useLibraryContext)
│   ├── pages/          # ページコンポーネント
│   │   ├── ViewerPage.tsx  # LibraryProvider でラップするだけのシンプルな構造
│   │   ├── GeneratorPage.tsx
│   │   └── OCRPage.tsx  # OCR実行ページ (/ocr)
│   └── App.tsx         # ルーティング定義
└── package.json
```

---

## 2. クラス設計

### フロントエンドContext (`frontend/src/contexts/`)

#### `LibraryContext` (`frontend/src/contexts/LibraryContext.tsx`)
- **役割**: ViewerPage が LibraryPanel に渡していた 21 個の props を Context に集約し、Props Drilling を解消する。
- **設計**:
    - `LibraryProvider`: `useUrlState` + PDF フェッチ + `useLibraryManagement` を内包し、`LibraryContext.Provider` で子にデータを提供。
    - `useLibraryContext()`: Provider 外で呼ばれると `throw Error` するカスタムフック。
    - `ViewerPage` は `LibraryProvider` でラップするだけの薄い構造になる。
    - `LibraryPanel` は props を受け取らず `useLibraryContext()` で必要な値・ハンドラを取得する。
- **提供する値 (LibraryContextValue)**:
    - 状態: `pdfs`, `directories`, `selectedPdf`, `currentPath`, `currentSource`, `isSelectionMode`, `selectedItems`, `isMoveDialogOpen`, `isCreateFolderOpen`, `renameTarget`
    - ナビゲーション: `onPdfClick`, `onFolderClick`, `onUpClick`, `onSourceChange`, `onClosePdf`, `onPdfUpdated`
    - ダイアログ操作: フォルダ作成・移動・リネームの開閉・実行ハンドラ群
    - `onRefresh`: PDF 一覧の再取得

---

### フロントエンドフック (`frontend/src/hooks/`)

#### `useDarkMode`
- **役割**: ダークモード状態の管理と永続化。
- **設計**:
    - `index.html` インラインスクリプトと連携し、React マウント前に `<html class="dark">` を適用してフラッシュを防止。
    - `useState` 初期化フェーズで即座に `classList` を操作（`useEffect` 待ちのラグを排除）。
    - `localStorage` キー `darkMode` に `true/false` 文字列で永続化。
    - システムカラースキーム (`prefers-color-scheme: dark`) をフォールバックとして使用。
    - **`index.css` で `html.dark` に `color-scheme: dark` を当てる**ことで `<select>` のネイティブドロップダウンメニュー（option）等もダーク表示にする（Tailwind の `dark:` 修飾子は子要素のスタイルしか効かないため、ネイティブ UI 部分はこの設定で対応）。

#### `useFavorites`
- **役割**: お気に入りPDFの管理（ソース別）。
- **設計**:
    - `localStorage` キー `favorites_{source}` に `string[]` (JSON) で永続化。
    - `source` が切り替わった際に `useEffect` で該当ソースのデータを再ロード。
    - `toggle(name)` はimmutableなSet更新 + 即時永続化。

#### `useSortedPdfs`
- **役割**: PDF一覧の並び替えロジック（`useMemo` でメモ化）。
- **ソート順**: `name_asc` / `name_desc` / `date_asc` / `date_desc` / `favorites_first`
    - `favorites_first`: お気に入りを先頭に、同グループ内は名前昇順。
    - 日付ソートは `created_at` (Unix timestamp) を使用。

#### `useBookMeta`
- **役割**: 書籍メタデータ（作者名・タグ・シリーズ・閲覧履歴・非表示）の取得・更新・集計。
- **設計**:
    - `source` が変わると `GET /api/meta` を再取得。
    - `updateMeta` / `updateAuthors` / `updateTags` / `setHidden` でローカル状態も即時反映（再フェッチ不要）。
    - `allAuthors` / `allTags` / `allSeries`: このソースに登録されている全項目の重複排除・日本語ソート済みリスト（フィルター select の選択肢に使用）。
    - `allSeriesWithStats`: `allSeries` に `maxIndex`（シリーズ内最大巻数）と `count`（冊数）を加えた拡張版。`ExistingSeriesOption[]` として一括登録ダイアログの選択肢に使用。
- **シリーズ関連 API**:
    - `assignSeries(path, names, { title, index, id? })`: 既存または新規シリーズに割り当て。`index` は `number | number[]`（配列で複数書籍に個別の巻数を当てられる）。`id` 省略時はバックエンドで `title + 作者集合` のハッシュから生成。
    - `unassignSeries(path, names)`: シリーズから外す。
    - `reorderSeries(path, names, seriesId)`: `names` の順序で `series_index` を `1, 2, 3, ...` に振り直す（DnD ドロップ時に呼ぶ）。**楽観的更新 + 失敗時ロールバック**: ローカル `meta` を即時に書き換えてから API を投げ、失敗時はスナップショットから元の `series_index` を復元する（ドロップ後にカードが元位置に戻ってチラつくのを防ぐ）。
- **その他主要戻り値**:
    - `getSeries(path, name)`: 1 冊のシリーズ情報 `{id, title, index}` を返す（未割当なら null）。「次の巻へ」遷移の探索元として使用。
    - `recordView(path, name)`: 閲覧記録（`view_count` / `last_viewed_at` を加算）。失敗時は黙ってスキップ。

#### `useAutoFillAuthors`
- **役割**: サークル名自動登録ジョブの実行制御・進捗ポーリング。
- **設計**:
    - `startAutoFill(mode)` で `POST /api/meta/auto-fill` を呼び出し、`JOB_POLL_INTERVAL_MS`（1500ms）でポーリング開始。
    - `mode`: `'missing_only'` / `'unknown_only'` / `'overwrite_all'`。
    - ジョブ完了時に `onComplete` コールバックを呼び出してメタデータを再フェッチ。
    - `startAutoFill` はエラーを throw するため、呼び出し側でキャッチしてトースト表示する。

#### `useLibraryFilter`
- **役割**: PDF一覧・フォルダ一覧のフィルタリングロジック（`useMemo` でメモ化）。
- **設計**:
    - `searchText`（タイトル / 作者 / タグ部分一致）/ `authorFilter`（完全一致）/ `tagFilter`（完全一致）/ `seriesFilter`（series_id 完全一致）/ `currentPath` / `showHidden`（ゴミ箱モード）/ `showUnreadOnly`（未読フィルター）の変化でフィルタを再計算。
    - **ゴミ箱方式**: `showHidden=false` のときは `hidden=true` を全件除外、`showHidden=true` のときは `hidden=true` の書籍**のみ** を表示する。
    - **未読フィルター**: `showUnreadOnly=true` のとき `meta[key].view_count === 0`（または未記録）の書籍のみを表示する。`view_count` は `meta` から直接参照する。
    - `LibraryPanel` から切り出すことでコンポーネントの責務を分離。

#### `useLibraryGrouping`
- **役割**: 書籍リストをシリーズ単位 / 作者単位で集約して仮想ディレクトリ的なカードを生成する。
- **モード** (`GroupMode = 'none' | 'series' | 'author' | 'author-then-series'`):
    - `none`: 集約しない。入力 `pdfs` をそのまま返す。
    - `series`: 同 `series_id` の書籍を集約。代表サムネイルは **最終巻**（`series_index` 最大）。displayTitle は `series_title`。
    - `author`: 同じ作者集合の書籍を集約。代表サムネイルは **入力順の 1 番目**（呼び出し側がソート済みなら「ソート 1 位」）。displayTitle は `{作者名} コレクション`。
    - `author-then-series`: 2 階層モード。`useLibraryGrouping` 自体は **`'series' | 'author' | 'none'` のいずれかに変換された値** を受け取る（モードの解釈は `LibraryPanel` 側で実施）:
        - 階層 1（authorFilter なし）: `author` として集約
        - 階層 2（authorFilter 適用中）: `series` として集約
        - 階層 3（seriesFilter 適用中）: `none` でフラット表示
- **戻り値**: `items` / `badgeByRepresentativeName` (count, kind, groupId, displayTitle) / `membersByRepresentativeName`。
- **共通**:
    - 集約対象だがメンバーが 1 冊だけのグループは集約しない（単独本扱い）。
    - 元の `pdfs` 順序を可能な限り保つ（代表書籍の元位置に集約カードを表示）。
    - シリーズ・作者ともに `kind` プロパティで PdfGrid 側の装飾（紫枠・バッジ・タイトル差し替え）を切り替える。

**LibraryPanel 側のパンくず計算**:
- ドリルダウン中（`authorFilter` または `seriesFilter` がセット）はパンくずを構築:
    1. 「ライブラリ」（全フィルタークリア）
    2. authorFilter があれば「作者: {名前}」
    3. seriesFilter があれば「シリーズ: {タイトル}」
- 各要素は `ChevronRight` で区切り、最後の要素以外はクリックで該当階層へ戻る。
- パンくず表示中は通常の作者・シリーズフィルター select は隠れる（重複防止）。

#### `usePdfSearch`
- **役割**: PDF内テキスト検索ロジック。`ReaderPanel` から切り出し。
- **設計**:
    - `searchAllPages(text, pdf)` で全ページのテキストを走査してマッチ数・先頭マッチページを算出。
    - `customTextRenderer` で一致箇所を `<mark>` タグでハイライト表示。
    - `onDocumentLoaded(pdf)` でロード直後に検索テキストが入力済みの場合は即座に再検索。

#### `useToast`
- **役割**: トースト通知の状態管理。`alert()` の代替。
- **設計**:
    - `showToast(message, type)` で通知を追加。4秒後に自動消去。`type`: `'success'` / `'error'` / `'info'`。
    - `dismissToast(id)` で手動消去。
    - `ToastContainer` コンポーネントと組み合わせて右下に固定表示。

#### `useSpreadMode`
- **役割**: リーダーの見開きモード（Auto / Spread / Single）と Auto 時のページサイズ判定を一元管理。`ReaderPanel` から切り出し。
- **戻り値**: `spreadMode`, `isSpread`, `cycleSpreadMode`, `handlePageSize`, `resetAutoSpread`
- **挙動**:
    - `spreadMode === 'auto'` 時のみ `handlePageSize(width, height)` で `autoIsSpread` を更新(縦長なら見開き、横長なら 1 ページ）。
    - `spreadMode === 'spread' / 'single'` の場合は `handlePageSize` は no-op。

#### `useEditMode`
- **役割**: リーダーの編集モード（ページ選択 + 削除）を一元管理。`ReaderPanel` から切り出し。
- **設計**:
    - 削除確認は OS ネイティブ `confirm()` を使わず、フック側で「削除待ち件数」(`pendingDeleteCount`) を保持し、呼び出し側 (`ReaderPanel`) が `<ConfirmDialog>` を表示する。
    - `requestDeletePages()` で確認ダイアログを開く → `confirmDeletePages()` で API 削除実行、`cancelDeletePages()` でキャンセル。
- **戻り値**: `isEditMode`, `selectedPages`, `toggleEditMode`, `togglePageSelection`, `resetEditMode`, `requestDeletePages`, `confirmDeletePages`, `cancelDeletePages`, `pendingDeleteCount`

#### `useFullscreen`
- **役割**: Document Fullscreen API のラッパー。リーダー画面のフルスクリーン切り替え。
- **設計**:
    - `document.fullscreenElement` の有無で `isFullscreen` を判定。
    - `fullscreenchange` イベントを購読してブラウザ UI / Esc キー経由の解除も追従する。
    - `toggleFullscreen()` は失敗時（ユーザー拒否・iframe 等）は黙ってスキップ。
- **戻り値**: `isFullscreen`, `toggleFullscreen`

#### `useReaderShortcuts`
- **役割**: リーダー画面のキーボードショートカットを 1 か所に集約する。
- **キー割り当て**:
    - `f` — フルスクリーン切替
    - `e` — 編集モード切替
    - `?` — ショートカット一覧モーダル表示
    - `Ctrl+F` / `Cmd+F` — 検索バーを開く
    - `↓` — 次の巻へ（シリーズ未所属または次巻なしの場合は無効）
    - `↑` — 前の巻へ（シリーズ未所属または前巻なしの場合は無効）
- **設計**:
    - 入力中（`<input>` / `<textarea>` / `contentEditable`）の場合はキー割り当てを抑制する。
    - `isActive` フラグで PDF リーダーがアクティブな間だけ動かす。
    - `←` / `→` のページ送りは `useReaderNavigation` が管理。`↓` / `↑` のシリーズ巻間移動は本フックが管理。
    - `onNavigateNextVolume` / `onNavigatePrevVolume` が `null` のとき（次巻/前巻なし）は何もしない。
- **引数**: `{ isActive, onToggleFullscreen, onToggleEditMode, onOpenHelp, onToggleSearch, onNavigateNextVolume, onNavigatePrevVolume }`

#### `useBookImages`
- **役割**: 書籍の画像URL一覧を取得し、画像モード/PDFモードを判定する。
- **設計**:
    - `selectedPdf` が変わると `GET /api/books/{bookPath}/images` を呼び出して画像URLを取得。
    - 画像が取得できた場合は `isImageMode=true`（画像モード）、失敗した場合は `isImageMode=false`（PDFモード）。
    - **`novel` ソースは画像取得をスキップして PDFモードを強制する**。`kindle_novel/images/` に PNG ファイルが存在するが、このソースの目的は Searchable PDF のテキスト検索にあるため、PDF モードで表示する必要がある。

#### `useReaderNavigation`
- **役割**: リーダーのページナビゲーション（前/次ページ、ページジャンプ、リセット）を管理。
- **依存**: `numPages`, `isSpread`, `direction`, `isActive`
- **主要戻り値**: `pageNumber`, `setPageNumber`, `handleNext`, `handlePrev`, `resetPage`

#### `useUrlState`
- **役割**: ViewerPage の URLパラメータ (`?path=`, `?file=`, `?source=`) 同期を一元管理。
- **URL に同期する LibraryPanel 側のフィルター**（`useUrlState` 直下ではなく `LibraryPanel` で `useSearchParams` を直接利用）:
    - `?author=` — 作者フィルター
    - `?series=` — シリーズフィルター（series_id）
    - `?tag=` — タグフィルター
    - これらにより、シリーズ / 作者カードでドリルダウンした後にブラウザの戻るボタンで元の一覧に戻れる。
    - `searchText` は入力ごとに履歴汚染を避けるため URL 同期しない。
    - フォルダ移動・ソース切替・PDF 選択時には `useUrlState` の navigate メソッドが `setSearchParams({ path, source, ... })` 形式で**全パラメータを置き換え**るため、これらフィルターは自動的に消える。
- **設計**:
    - `useSearchParams` ラッパー。`currentPath` / `selectedPdf` / `currentSource` を導出して返す。
    - ナビゲーション操作を関数として提供し、ViewerPage での状態二重管理を排除。
    - `source` を URL に含めることでリロード・直接アクセス時にも正しいソースが復元される。
    - `VALID_SOURCES` で無効なソース値を `'generated'` にフォールバック。
- **主要メソッド**:
    - `navigateIntoFolder(dirName, basePath, source)`: フォルダ内に移動（`?path=` を更新）。
    - `navigateUp(currentPath, source)`: 親階層へ戻る。
    - `selectPdf(pdfName, currentPath, source)`: PDF を選択（`?file=` を追加）。
    - `clearPdf(currentPath, source)`: PDF選択を解除してライブラリに戻る。
    - `setSource(source)`: ソース切り替え時に `?source=` だけ変更し、`path` / `file` をリセット。

---

### フロントエンド共通UIプリミティブ (`frontend/src/components/ui/`)

#### `Dialog` (`frontend/src/components/ui/Dialog.tsx`)
- **役割**: ダイアログ共通シェル。5 ダイアログ（CreateFolder / Rename / BulkAuthor / Move / Merge）が共通で使う。
- **提供サブコンポーネント**: `Dialog`, `DialogBody`, `DialogFooter`, `DialogCancelButton`, `DialogPrimaryButton`
- **共通機能**:
    - 黒オーバーレイ + 中央配置 + 外クリック / Esc キーで閉じる
    - ヘッダー（タイトル + サブタイトル + X ボタン）/ Body / Footer の3パート構成
    - `nested` prop で z-index を切り替え（`z-dialog` / `z-dialog-nested`）
- **z-index**: `tailwind.config.js` の階層クラス (`z-dialog` = 100, `z-dialog-nested` = 200) を使用。任意値構文 `z-[100]` は使わない。

#### `ConfirmDialog` (`frontend/src/components/ui/ConfirmDialog.tsx`)
- **役割**: 汎用確認ダイアログ。OS ネイティブ `confirm()` の置換用。
- **設計**:
    - `<Dialog>` 共通シェルをベースに、確認ボタン + キャンセルボタンを提供。
    - `danger` prop で危険操作（削除等）を赤系ボタンで表示。
    - 使用例: `ReaderPanel` のページ削除確認、`useEditMode` と組み合わせて使う。

---

### フロントエンドコンポーネント

#### `LazyThumbnail` (`frontend/src/components/reader/LazyThumbnail.tsx`)
- **役割**: サムネイル画像の遅延読み込み。
- **設計**: `IntersectionObserver` で viewport から200px前に入った時点で `src` をセット。エラー時は `FileText` アイコンにフォールバック。

#### `PdfSearchBar` (`frontend/src/components/reader/PdfSearchBar.tsx`)
- **役割**: PDF内テキスト検索UI。`ReaderHeader` 直下に固定表示。
- **設計**: 300msデバウンスで親へ検索テキストを通知。Enter/Shift+Enterで次/前へ移動。マッチ数 "X / Y" 表示。

#### `PageRenderer` (`frontend/src/components/reader/PageRenderer.tsx`)
- **役割**: 単一ページのレンダリング（PDFモード・画像モード両対応）。
- **設計**:
    - `onPageSize?: (width, height) => void` コールバックを持ち、ページサイズが確定した時点で親に通知する（自動見開き判定用）。
    - PDFモード: `<Page onRenderSuccess={(page) => onPageSize?.(page.width, page.height)}>` で通知。
    - 画像モード: `<img onLoad={(e) => onPageSize?.(img.naturalWidth, img.naturalHeight)}>` で通知。
    - `ReaderPanel` は右ページ（`side === 'right'`）またはシングルページ（`side === 'single'`）にのみ `onPageSize` を渡し、見開き時の二重発火を防ぐ。

#### `PageSlider` (`frontend/src/components/reader/PageSlider.tsx`)
- **役割**: リーダー画面下部に固定表示するページスライダーバー。上部ヘッダーとは独立した表示制御を持つ。
- **設計**:
    - `input[type=range]` で全ページをスクラブ。
    - **表示制御**: `show` prop で表示/非表示をフェード（`translate-y-full` ↔ `translate-y-0`）。`ReaderPanel` 内で `showSlider` state を `showHeader` とは独立して管理し、画面下端ホバーゾーン（`fixed bottom-0 h-12`）で表示・`onMouseLeave` で非表示にする。
    - **矢印キー対策**: `tabIndex={-1}` でタブフォーカスを防止し、さらに `onFocus={(e) => e.target.blur()}` でクリック後のフォーカスも即座に解放する。これにより矢印キーは常に `useReaderNavigation`（RTL/LTR 対応済み）が処理する。
    - **遅延コミット**: `onChange` → `pendingPage` (ローカル state) のみ更新。`onPointerUp` 時に `normalizeSpreadPage()` を適用し `onPageJump` 呼び出し。react-pdf の描画コストをドラッグ終了時だけ発生させる。
    - **RTL 対応**: `direction === 'rtl'` の場合、slider 要素に `transform: scaleX(-1)` を適用してビジュアルを反転。ツールチップ位置計算も `1 - thumbRatio` で補正。
    - **見開き正規化** (`normalizeSpreadPage`):
        - RTL spread: 有効開始ページは 1, 2, 4, 6, … (奇数 >1 → page-1)
        - LTR spread: 有効開始ページは 1, 3, 5, … (偶数 → page-1)
    - ドラッグ中はサムネイル画像 + ページ番号テキストをツールチップとして thumb 位置に表示。
        - `thumbPage` state を 150ms デバウンスで `pendingPage` から更新し、`GET /api/thumbnails/page?width=400` で画像取得。
        - 画像取得失敗時（`onError`）は画像を非表示にしてページ番号テキストのみ表示。
    - 必要な追加 props: `selectedPdf`, `currentPath`, `currentSource`（サムネイル URL 構築用）。

#### `ReaderHeader` (`frontend/src/components/reader/ReaderHeader.tsx`)
- **役割**: リーダー上部コントロールバー。マウスオーバー時のみ表示（没入型モード）。
- **設計**:
    - **見開きモードボタン**: `spreadMode: SpreadMode` (`'auto' | 'spread' | 'single'`) を `onCycleSpreadMode` で循環（Auto → Spread → Single → Auto）。
    - `SPREAD_MODE_CONFIG` 定数でモードごとのラベル・アイコン・次モードを管理。
    - Auto モードは indigo 色で強調表示。他は gray。
    - ページ番号表示はクリックで直接入力（数値入力 → Enter/blur で `onPageJump` 呼び出し）。

#### `CreateFolderDialog` (`frontend/src/components/viewer/CreateFolderDialog.tsx`)
- **役割**: フォルダ作成ダイアログ。`<Dialog>` 共通シェルを使用。
- **設計**: バリデーションは `utils/validation.ts` の `validateFilename(value, 'folder')` に委譲。Enter でサブミット、Esc/外クリックで閉じる挙動は `<Dialog>` 側で吸収。

#### `RenameDialog` (`frontend/src/components/viewer/RenameDialog.tsx`)
- **役割**: PDF/フォルダ共用のリネームダイアログ。`<Dialog>` 共通シェルを使用。
- **設計**:
    - PDF の場合は拡張子 `.pdf` を入力欄から省き、確定時に自動付加。フォルダの場合はそのまま扱う。
    - バリデーションは `validateFilename(value, isFolder ? 'folder' : 'file')`。
    - `isFolder` prop で PDF/フォルダを切り替える。

#### `ReaderPanel` (`frontend/src/components/viewer/ReaderPanel.tsx`)
- **役割**: PDFリーダーUI。ページ表示・見開き制御・検索・ページ削除の **統合（合成）** を担う。各種ロジックはカスタムフックに委譲。
- **Worker設定**:
    - `pdfjs.GlobalWorkerOptions.workerSrc` を `new URL('pdfjs-dist/build/pdf.worker.min.mjs', import.meta.url).toString()` で設定。
    - `<Document>` コンポーネントと同一ファイルで設定する必要がある（react-pdf の要件）。Vite がビルド時にワーカーファイルを自動バンドルする。
    - `react-pdf/dist/Page/TextLayer.css` をインポートしてテキストレイヤーのスタイルを適用（ハイライト表示に必要）。
- **委譲先フック**:
    - `useSpreadMode()` — `spreadMode` / `isSpread` / `cycleSpreadMode` / `handlePageSize` / `resetAutoSpread` を提供。
    - `useEditMode({...})` — `isEditMode` / `selectedPages` / `toggleEditMode` / `togglePageSelection` / `requestDeletePages` / `confirmDeletePages` / `cancelDeletePages` / `pendingDeleteCount` を提供。
    - `useReaderNavigation()` — ページナビゲーション。
    - `usePdfSearch()` — PDF テキスト検索ロジック（マッチ数・ハイライト・カスタムレンダラー）。
- **見開きモード制御** (`useSpreadMode` 内):
    - `spreadMode: SpreadMode` — `'auto' | 'spread' | 'single'` の3状態。
    - 実効 `isSpread` = `spreadMode === 'auto' ? autoIsSpread : spreadMode === 'spread'`
    - `cycleSpreadMode()`: Auto → Spread → Single → Auto の順で循環。
    - `handlePageSize(width, height)`: `spreadMode === 'auto'` 時のみ動作。`width <= height`（縦長）なら見開き、横長なら1ページに設定。
    - PDF切り替え時は `resetAutoSpread()` で `autoIsSpread` を `true` にリセット。
- **テキスト検索**:
    - `pdfRef` に `PDFDocumentProxy` を保持し、全ページのテキストを走査してマッチ数を算出・ハイライト表示。
    - `searchAllPages(text, pdf)` は `pdfRef.current` ではなく引数の `pdf` を直接受け取り、ロード直後の null 参照問題を回避。
    - `onDocumentLoadSuccess`: PDF ロード完了時点で `searchText` が入力済みの場合は即座に `searchAllPages` を呼び出す。
    - 検索バー (`PdfSearchBar`) は `isSearchOpen === true` の間は常に表示（`showHeader` とは独立）。
    - `ReaderHeader` は `showHeader || isSearchOpen` で検索中は常に表示。
- **ページ削除**: `useEditMode` の `requestDeletePages()` で確認状態を作り、`<ConfirmDialog>` で削除確認。`confirmDeletePages()` で実際の削除 API 呼び出し → 完了後 `pdfVersion` をインクリメントして PDF を再ロード。
- **エラー通知**: `alert()` / `confirm()` は使用せず、エラーは `useToast`、確認は `<ConfirmDialog>`（`components/ui/ConfirmDialog.tsx`）を使用する。

#### 閲覧回数 / 最近見た順ソート（フロントエンド側）
- **記録タイミング**: ライブラリのカードクリックで PDF を開いたタイミング（`LibraryPanel` 内で `onPdfClick` をラップ）で `POST /api/meta/view` を呼ぶ。
- **ローカル状態の更新**: `useBookMeta` の `recordView` でローカル状態も即時反映。`PATCH /api/meta`（作者更新）と `POST /api/meta/view`（閲覧記録）は互いに干渉しないマージ規則で動く（バックエンド側の規則は [詳細設計書_バックエンド編.md](詳細設計書_バックエンド編.md) の「閲覧回数 / 最近見た順ソート（バックエンド側）」参照）。
- **ソート挙動**:
    - `'view_desc'` — 閲覧回数の降順、同数の場合は名前昇順（`localeCompare('ja')`）。
    - `'recent_view'` — 最終閲覧時刻 (`last_viewed_at`) の降順。未閲覧（`undefined`）は末尾、同時刻は名前昇順。
- **UI**: ソートドロップダウン（`HeaderSortSelect`）に「よく見る順」「最近見た順」を追加。カード上には数字を表示しない（ソートのみ）。

#### `BulkAuthorDialog` (`frontend/src/components/viewer/BulkAuthorDialog.tsx`)
- **役割**: 選択モードで複数書籍を選択したときに作者名を一括設定するダイアログ。`<Dialog>` 共通シェルを使用。
- **設計**:
    - タグ形式の入力（Enter/カンマで確定、Backspace で削除）。
    - 「一括適用」で選択中の全書籍の作者名を上書き（既存の作者名は破棄、ただし `view_count` / `last_viewed_at` 等の他フィールドは保持）。
    - `targetCount` prop で対象冊数をヘッダーに表示。
    - 選択モード中にヘッダーの「作者を設定」ボタンから開く。

#### `BulkSeriesAssignDialog` (`frontend/src/components/viewer/BulkSeriesAssignDialog.tsx`)
- **役割**: 選択モードで複数書籍を選択し、一度にシリーズへ登録するダイアログ。`<Dialog>` 共通シェルを使用、紫系。
- **設計**:
    - 2 モードのラジオ:
        - **既存に追加**: `existingSeries: ExistingSeriesOption[]`（`{id, title, maxIndex}`）から `SearchableSelect` で選択（テキスト絞り込み付き）。選択後に `maxIndex`（現在の最大 `series_index`）を小テキストで表示。`BulkAuthorDialog` の既存モードと同じ UX。
        - **新規作成**: 自由入力でシリーズタイトルを設定。
    - **採番プレビュー**: 選択中の書籍を上から順に表示し、各冊に振られる巻数（既存追加なら `floor(maxIndex)+1` から、新規なら `1` から）をリアルタイムで見せる。
    - `onAssign({ title, indexes, id? })` で確定。`LibraryPanel` 側で `useBookMeta.assignSeries` の `index: number[]` 形式で API を呼ぶ。
    - 既存シリーズが 0 件のソースでは「既存に追加」モードは disable。
- **採番ルール（`LibraryPanel` 側で計算）**: 選択順を保持するため `Array.from(selectedItems)`（Set のイテレーション順 = 選択順）をそのまま使用。

#### `PdfGrid` の DnD 並べ替えサポート (`frontend/src/components/reader/PdfGrid.tsx`)
- **役割**: 通常のグリッド表示に加え、`dndEnabled=true` のときは各カードを `@dnd-kit/sortable` で並べ替え可能にする。
- **設計**:
    - `dndEnabled && !isSelectionMode` のときだけ `<DndContext>` + `<SortableContext>` でラップする（選択モード中はチェックボックス操作と競合するため無効）。
    - 各カードを `SortablePdfCard`（`SortablePdfCard.tsx`）で包み、**右上**に `GripVertical` のドラッグハンドルを表示（左上のお気に入り星との位置競合を避けるため右上配置）。`useSortable` の `setActivatorNodeRef` をハンドルにのみ付け、カード本体のクリックは通常の遷移として残す。
    - `PointerSensor` の `activationConstraint.distance: 8` で 8px 未満のクリックは drag 開始しない（誤検知防止）。
    - ドロップ時に `arrayMove` で新順序を計算し、`onReorder(newOrder: string[])` を呼ぶ。
- **シリーズドリルダウンとの連動 (`LibraryPanel`)**:
    - `seriesFilter` がセットされている間はユーザー選択ソートに関係なく `series_index` 昇順を強制（`displayPdfs` で再ソート）。
    - 同条件で `dndEnabled` を `true` にし、ドロップ時に `useBookMeta.reorderSeries` を呼ぶ。

#### `ReaderPanel` のシリーズ巻間ナビゲーション (`frontend/src/components/viewer/ReaderPanel.tsx`)
- **役割**: シリーズ内の前巻・次巻への遷移を「フローティングボタン」と「キーボードショートカット（↓/↑）」の 2 経路で提供する。
- **設計**:
    - `useBookMeta(currentSource)` から `meta` / `getSeries` / `recordView` を取得。
    - **次巻探索**: `useNextSeriesVolume(meta, getSeries, currentPath, selectedPdf)`。`series_index` が現在より大きい中で最小のエントリを選ぶ。
    - **前巻探索**: `usePrevSeriesVolume(meta, getSeries, currentPath, selectedPdf)`。`series_index` が現在より小さい中で最大のエントリを選ぶ。
    - 判定範囲はともに **同フォルダ内のみ**（meta キーの `prefix` 一致と残部分にスラッシュなしでチェック）。
    - **フローティングボタン（次巻のみ）**: `numPages > 0` かつ最終ページ／最終スプレッド到達かつ次巻あり かつ `onSelectPdf` 提供時に表示。
    - **キーボードショートカット**: `↓` で次巻、`↑` で前巻。最終ページでなくても動作する（意図的な巻間ジャンプのため）。次巻/前巻が存在しない場合は何もしない。
    - **遷移時**: `recordView(currentPath, name)` で閲覧記録を加算してから `onSelectPdf(name)` を呼ぶ。`selectedPdf` 変化で `resetPage()` が走るため遷移先は 1 ページ目から開始。
    - `onSelectPdf` は `ViewerPage` から `LibraryContext.onPdfClick` を渡す。

#### 一括操作後の選択モード自動解除（`useLibraryManagement` / `LibraryPanel`）
- **役割**: 作者・タグ・シリーズ・非表示・結合・サムネイル再生成・移動の各バルク操作が **成功** したら、選択モードを抜けてチェックボックスを消し通常表示に戻す。
- **実装**:
    - `useLibraryManagement.clearSelection()`: `setIsSelectionMode(false)` + `setSelectedItems(new Set())`。
    - `LibraryContext.onClearSelection` で公開。
    - `LibraryPanel` の `handleBulkApplyAuthors` / `handleBulkApplyTags` / `handleBulkAssignSeries` / `handleBulkToggleHidden` / `handleMergePdfs` / `handleRegenThumbnailBulk` の **成功パス末尾** で `onClearSelection()` を呼ぶ。失敗時は throw 経路でスキップされるため選択は残りリトライ可能。
    - `handleMoveItems`（`useLibraryManagement` 内）は元から同等処理を持つ。

#### `AutoFillAuthorsBar` (`frontend/src/components/viewer/AutoFillAuthorsBar.tsx`)
- **役割**: サークル名自動登録ジョブの実行ボタン・モード選択ラジオ・進捗バーを 1 か所に集約。`LibraryPanel` から切り出し。
- **設計**:
    - `useAutoFillAuthors` フックを内部で使い、ジョブの起動・進捗ポーリングを管理。
    - props は `source: LibrarySource` と `onComplete: () => void` のみ。
    - エラー時は内蔵のトーストで通知（ヘッダー側のトーストとは別系統）。
    - **配置**: `LibraryPanel` 直下から `ToolsMenu` のドロップダウン内に移動（2026-04-29）。月 1 回程度しか触らないため画面占有を避ける。

#### `SeriesResolveBar` (`frontend/src/components/viewer/SeriesResolveBar.tsx`)
- **役割**: シリーズ判定ジョブの実行ボタン・Gemma 補助併用チェック・進捗バーを集約。
- **設計**:
    - `useSeriesResolve` フックを内部で使い、ジョブの起動・進捗ポーリングを管理。
    - props は `source: LibrarySource` と `onComplete: () => void` のみ。
    - **配置**: `AutoFillAuthorsBar` と同じく `ToolsMenu` のドロップダウン内に移動（2026-04-29）。

#### `ToolsMenu` (`frontend/src/components/viewer/ToolsMenu.tsx`)
- **役割**: 滅多に使わないジョブ起動 UI（サークル名自動登録・シリーズ判定）を `LibraryHeader` の「ツール ▼」ボタンから開くドロップダウンに集約する。常時表示で画面を圧迫することを防ぐ。
- **設計**:
    - 「ツール」ボタン（Settings アイコン）クリックでドロップダウンを開閉。
    - ドロップダウン内に `<AutoFillAuthorsBar>` と `<SeriesResolveBar>` を縦に並べる（既存コンポーネントの border-b が区切り線として機能）。
    - **閉じる契機**: 外クリック（`mousedown` リスナー）/ Esc キー / もう一度ボタンクリック。
    - props は `source: LibrarySource` と `onComplete: () => void` を受け取り、両バーに同じものを渡す。
    - z-index は `z-header` 階層内（親が `z-header` の `LibraryHeader` 子要素なので相対配置で自然に上に出る）。

### LibraryHeader の 3 段構成（2026-04-29 再編）
- **役割**: ライブラリ画面のヘッダー。**責務分離**のため 3 段に再編。
- **構成**:
    1. **1 段目（h-12, ナビゲーション + ソース）**: 戻る ← / パスタイトル / パンくず（ドリルダウン中のみ） / 右端に `SourceSelector`
    2. **2 段目（h-12, 検索 + フィルター + 表示設定 + アクション）**: 検索 input / 作者 select / タグ select / グループ化 select / 未読フィルタートグル / 非表示モードトグル / ソート select / `ToolsMenu` / + フォルダ作成 / 選択モード起動
    3. **3 段目（h-11, 選択モード時のみ動的）**: 「N 選択中」/ 一括アクション群（作者・タグ・シリーズ・非表示・結合・サムネイル再生成・移動）/ キャンセル
- **キーボードショートカット**:
    - `s` キー: 選択モードをトグル（`LibraryPanel` の `useEffect` で登録。リーダーが開いている間 / 入力フォーカス中 / 修飾キー付きは無効）
- **設計判断**:
    - 1 段目に「常に変わらない情報」（現在地・ソース）、2 段目に「絞り込み・表示設定・アクション起動」、3 段目に「選択モード時のみのコンテキストアクション」を置く。これにより通常モードと選択モードの**視覚的衝撃**を抑え、文脈の連続性を保つ。
    - 旧設計（1 段に 9〜10 要素詰め込み + 選択時にヘッダー全置換）の問題を解消。
    - 通常時の高さは 96px、選択時は 140px。

### ボタン色規約（全画面共通）

全画面で以下の 3 色規約を統一して使用する。

- **Primary** (`bg-indigo-600 text-white hover:bg-indigo-700`): 「実行する」操作。
    - Library: 一括アクション群（作者設定 / タグ設定 / シリーズ登録 / 非表示 / 結合 / サムネイル再生成 / 移動）
    - Dialog: `DialogPrimaryButton`（全ダイアログの確定ボタン）
    - OCR: Start OCR ボタン
- **Secondary** (`bg-gray-100 dark:bg-gray-800 text-gray-700 dark:text-gray-300 hover:bg-gray-200 dark:hover:bg-gray-700`): 中立・補助・取消。フォルダ作成 / 選択モード起動 / ツール開閉 / キャンセル / `DialogCancelButton`。
- **Danger** (`bg-red-600 text-white hover:bg-red-700`): 破壊的・回復不能な操作。OCR Stop ボタン / `ConfirmDialog` の `danger=true` 確認ボタン / Reader のページ削除ボタン。
- **Active tint (トグル ON)**: 現在 ON のトグルは indigo tint（`bg-indigo-100 dark:bg-indigo-900/40 text-indigo-700 dark:text-indigo-300`）または `bg-gray-700 text-white` で表示。
    - Reader: Search / Edit モード ON 時は indigo tint
    - Library: 非表示モード ON 時は `bg-gray-700 text-white`（Secondary active）
- **ナビゲーション**: `bg-indigo-600` を使用（例: リーダーの「次の巻へ」フローティングボタン）。
- **方針**: 旧設計の多色（blue / purple / emerald / amber など）を上記 3 色に集約。色ではなくアイコン + 文字で操作を区別する。`<select>` はネイティブスタイルを維持。

#### 未読バッジ（PdfCard）
- **条件**: `isGroup=false` かつ `isUnread=true`（view_count === 0）のカードに表示
- **配置**: カード下部の日付・アクション行（`flex items-center gap-1`）内、お気に入り星アイコンの隣に inline 表示
- **スタイル**: `bg-sky-500 text-white text-xs font-semibold px-1.5 py-0.5 rounded-full`、表示テキスト "NEW"
- **渡し方**: `LibraryPanel` → `PdfGrid`（`getIsUnread` prop）→ `buildCardProps` → `PdfCard`（`isUnread` prop）
- **サムネイル領域に置かない理由**: シリーズ DnD 時のドラッグハンドルや集約バッジと重なるため

#### フォルダカードの未読カウントバッジ（FolderGrid）
- **条件**: `getUnreadCount(dirName) > 0` のフォルダアイコン右上に表示（選択モード中は非表示）
- **カウント対象**: `currentPath/dirName/` 直下の書籍（再帰なし）で `view_count === 0` のもの
- **スタイル**: `absolute -top-2 -right-2`、`bg-sky-500 text-white text-xs font-semibold min-w-[1.25rem] h-5 rounded-full flex items-center justify-center px-1`
- **渡し方**: `LibraryPanel` で `meta` を走査し `getUnreadCount={(dir) => ...}` として `FolderGrid` へ渡す

#### 選択モードの視覚スタイル（PdfGrid / FolderGrid）
- **選択中カード**: `border-amber-400` の枠 + `bg-amber-50 dark:bg-amber-900/20` の薄い琥珀色背景で表示。
- **PdfGrid のカード本体パネル**（サムネイル下部の書名・作者・日付エリア）も同じ条件で背景色を切り替える。サムネイル領域は画像で覆われるため枠と本体パネルの両方を着色することで選択状態を明確化する。
- **チェックアイコン**: 選択時は `text-amber-500` の `CheckSquare`、未選択時は `text-gray-400` の `Square`。
- **採用理由**: 従来の `border-blue-500` はダークモード（`bg-gray-800` 背景）で視認性が低く、UI 内の他の青系要素（作者タグ等）とも紛らわしかったため、アクセント色として琥珀（amber）に変更。両モードで高コントラスト。
