# リファクタリング履歴

最終更新: 2026-08-17（R1 Windows実機受入 完了）

完了済みリファクタリング Phase の詳細記録（新しい順）。Phase 1〜12 の旧プランニング詳細は履歴サイズ削減のため削除済み（コミットメッセージ `538d9de` / `acda380` / `deeb28b` と [変更履歴.md](../log/変更履歴.md) に記録）。

未着手・追加候補・対象外項目は [リファクタリング計画書.md](../log/計画/リファクタリング計画書.md) を参照。

---

## R1 Windows実機受入（2026-08-17）— Kindle境界分割の実書籍検証（P1）

- 『おとなになっても（1）』で誤方向の初頭2画面周期を有界停止し、正式領域を更新しない
  fail-closedを確認した。正方向`left`では末尾周期の重複を除外し、88画面を登録した。
- 『悪役令嬢の中の人【特典SS付】』を`source=novel`で実行し、単ページ設定、先頭移動、
  カナリア、104画面の自然終端、画像QA、転送、登録を確認した。
- ページ設定UIは両sourceで取得・選択でき、漫画・小説とも最終的に
  `capture_state=captured`、active job 0件となった。
- terminal jobは再開せず、Store版Kindleのダウンロード後終了時はコンテンツ完全性と
  ASIN再照合後に新規jobを作る既存の安全契約を維持した。

**検証**: Kindleテスト116件、Ruff、docs整合性検査、Windows実機2冊。
詳細は[Kindle自動撮影_実機知見](../log/技術知見/Kindle自動撮影_実機知見.md#89-r1-修正後の-windows-実機再受入2026-08-17合格)を参照。

---

## R3-2〜R7・D6〜D7（2026-08-16）— 責務境界の完了と継続ガードレール化（P1/P2）

- R3でOCR worker protocol、job application、QA review、publicationを所有moduleへ分け、
  旧`surya_ocr` / `ocr_staging` / `ocr_qa`は期限付き互換facadeへ縮退した。
- R4でNovel RAGのLLM provider、用途別prompt、純粋parser、repository、生成・index、
  discussion streamを分離し、fake providerで外部LLMなしに検証できる境界を作った。
- R5でKindle画面をfeature所有へ移し、Novel DBのSSE transportと用途別decoderを分け、
  Library/OCRの画面orchestrationを専用hookへ抽出した。未使用facadeとcomponentを撤去した。
- R6で互換層、legacy migration、保守scriptを機械可読台帳へ分類し、owner、撤去条件、
  再評価日、rollbackを固定した。500行超のテストをproduction所有境界に沿って分割した。
- R7で継続運用する`scripts/`と`common/llm`をsize/C901 ratchetへ追加し、改善済みbaselineを
  削除した。Novel RAG/OCR、Kindle、frontendの互換facade逆流をimport boundaryで禁止した。
- D6で完了Phaseと契約表をarchiveへ移し、living計画をR1実機受入とTM-2だけに限定した。
- D7で横断契約のowner marker、正本マップへのリンク、owner重複を`check_docs.py`の
  blocking検査へ追加した。

**維持した契約**: OpenAPI、Alembic head、SSE event順、OCR worker JSONL、capture manifest、
Kindle error code、OCR品質policy、URL/query/local state。完了時点の副作用経路とrollbackは
[リファクタリング契約表_R0-R7](リファクタリング契約表_R0-R7.md)に凍結した。

**検証**: backend / kindle-pdf / common LLM / frontendの全テスト、Ruff、型検査、ESLint、
Knip、build、OpenAPI、MkDocs、月次監査を実行。R5の対象画面はdesktop/mobile、keyboard、
consoleを実ブラウザで確認した。R1のWindows実機novel/comic各1冊だけは保留としてliving計画へ残した。

---

## R3-1（2026-08-10）— Surya runtimeのserver・transport境界を分離（P1）

- `surya_server.py`へhealth check、owned `llama-server`起動、ready待機、終了処理を分離した。
- `surya_transport.py`へ画像のPNG/base64化、OpenAI互換payload、HTTP、応答decodeを分離した。
- `surya_runtime.py`は品質付き認識workflowに限定し、variant試行、layout fallback、候補選択、
  request failure生成をhelperへ分けて`recognize_with_quality`のC901超過を解消した。
- `surya_ocr` facade、`surya_runtime.SuryaServer`、standalone import、
  `SuryaClient._recognize`の既存利用経路を維持した。
- code-size baselineを532行から442行へ、C901 baselineを25件から24件へ縮小した。

**検証**: OCR runtime/worker/column worker/staging 59 passed、backend全体
1,268 passed・2 skipped、対象Ruff/C901、code-size ratchet、月次健全性監査を通過。
固定されたquality flag、payload順、server generationに差分なし。

---

## R2（2026-08-10）— Kindle catalogの取込・公開境界を分離（P1）

- `capture_registration_repository.py`へ完了対象jobの取得・所有状態検証・
  条件付き`awaiting_files → succeeded`更新を分離した。
- `capture_publication.py`へstaging copy、既存target退避、正式publish、meta更新、
  package archiveと逆順補償を分離し、catalog DBへの依存を除いた。
- ready検証、staging、各publish境界、job更新直前、条件付き更新0行を障害注入で固定し、
  target、meta、`.ready` / `processed`、staging、DB状態を確認した。
- `capture_registration.complete`のC901超過を解消し、25 current / 25 baselineとした。
- enrichment source発見、Kindle Info upsert、自動購入全量置換を別moduleへ分け、
  注文を含む3取込のrun状態遷移を`import_run_lifecycle.py`へ統一した。
- legacy source statusを通常経路へ残し、preview/commit実装はAPI呼び出し時だけ遅延importした。
- request schemaをschema moduleへ移し、schema名を維持してOpenAPI hashを不変にした。

**検証**: R2代表44 passed・1 skipped、対象Ruff/C901、OpenAPI 102 paths/hash
`f4008270fc3f`、Alembic heads `0011` / `8f4c1b0a2d3e`、backend全体
1,266 passed・2 skippedを通過。

---

## R1-1〜3（2026-08-10）— Kindle Windows操作・series runnerの責務分離（P1）

- `reader_policy.py`へsource別page layout、表紙補正要否、逆方向page keyの純粋判断を分離した。
- `reader_ui.py`へpage settingsとlocation dialogのUIA操作、入力readback、確認、cleanupを分離した。
- `reader.py`は既存methodを維持したworkflowとなり、`set_page_layout`と
  `_try_go_to_location_start`のC901超過を解消した。
- 旧facade、error code、入力値、最大1回の表紙補正、timeout時fail-closedを維持した。
- `capture_loop_policy.py`へretry・反対方向試行・期待枚数判断、
  `capture_loop_models.py`へ終了証跡、`capture_loop_io.py`へ画面取得・PNG保存を分離した。
- 撮影枚数、crop、page key、retry回数、終了理由、manifest v2へ渡すreport fieldを維持した。
- series runnerをinventory、session safety、orchestrator、HTTP、CLIへ分け、旧scriptは
  import/CLI互換facadeにした。1job制約、成功・登録確認後だけの次巻、限定1回復旧を維持した。
- `execute_series`と`main`のC901超過を解消し、未管理C901を19件から17件へ削減した。

**検証**: kindle-pdf 112 passed、reader関連73 passed、capture loop関連36 passed、import boundary、
C901 26 current / 26 baselineを通過。backend capture/series 50 passed・1 skipped。
backend全体1,258 passed・2 skipped、月次健全性監査も通過した。
Windows実機novel/comic各1冊の先頭移動・撮影・cleanup・再実行だけは未確認。

---

## D3・D4（2026-08-10）— Kindle横断契約とWindows内部設計の分離（P1/P2）

- Linux backend、Windows agent、シリーズ実行をまたぐjob状態、heartbeat、package、
  正式登録、停止・再開・限定復旧条件をKindle購入カタログ設計 §7へ集約した。
- backend詳細設計からWindows capturer/controllerの重複説明を除き、job repository、
  package再検証、正式配置と補償だけを残した。
- `kindle-pdf/docs` はWindowsのUI操作、撮影、画像QA、package、限定復旧へ限定し、
  日付付き実測を既存のKindle実機知見へリンク化した。
- 実装と矛盾していた検索欄・ページ入力方式を、限定controlの
  `ValuePattern.SetValue`＋完全一致読み戻しへ訂正した。

---

## R0-B（2026-08-10）— OCR benchmark比較器の分割（P1）

- 1,356行の `benchmark_ocr_ground_truth.py` をCLI、engine adapter、列OCR adapter、
  正規化/CER、集計、品質gateへ分離し、旧パスは88行の互換facadeにした。
- `character_error_operations`、`evaluate_quality_gate`、`main` のC901超過を解消し、
  `scripts/` / `common/llm/` の未管理C901超過を22件から19件へ削減した。
- CLI engine選択肢・既定値、JSONキー順、metric/gate順、CER、exit codeをcharacterization testへ固定した。

**検証**: 固定fixtureで旧版と新版のreport、quality gate、編集操作が完全一致。
OCR benchmark 19 passed、分割moduleのRuff/C901、既存4補助CLIのimport・help起動を通過した。

---

## R0・D1・D2・D5（2026-08-10）— 契約基準線とOCR正本分離（P1）

- OpenAPI 102 paths、Alembic heads、各workspaceのtest collection、size/C901を基準線に固定した。
- Kindle UI、capture公開、catalog DB、OCR subprocess、OCR QA/公開、RAG SSE、frontend stateの
  7副作用経路について、発火条件、観測出力、fixture、rollback、後続ownerを契約表へ記録した。
- 改善済みC901 1件をbaselineから除外し、28 current / 28 baselineへratchetを更新した。
- OCR設計書を現行の実行・QA・公開契約へ限定し、B-35の値をpolicy JSON、意味を設計書、
  未完了作業を実装計画、過去の実測をOCR技術知見へ分離した。
- RAG技術知見とバックログに重複していたOCRの時系列記録を正本リンクへ置き換えた。
- B-35正式holdoutのシリーズ数、再開封拒否、全package digestが未強制である点を明文化した。

**検証**: backend代表127 passed / 1 skipped、kindle-pdf 102 passed、common/llm 42 passed、
frontend代表7 passed。月次健全性監査はdocs、file map、code-size、C901、OpenAPI、
import boundary、Knip、完了計画残存をすべて通過した。

---

## 凡例

- **P1**: 既に分離した責務が再膨張、または毎レンダー走る派生計算、緊急バグ修正など実害がある
- **P2**: 重複ロジック・読みづらさが顕在化しているが緊急性は中
- **P3**: 構造改善ではあるが現状で困っていない

---

## 再計画 Phase 6-3（2026-07-26）— C901複雑度ラチェット（P2）

- backend 25件、kindle-pdf 3件の既存C901超過を関数名と複雑度の組で
  `c901_baseline.json`へ固定した。
- `check_c901_ratchet.py`は新しい超過関数と既存関数の複雑度悪化だけを失敗にし、
  改善・削除は許容してbaseline削除候補を表示する。
- pure parser・比較ロジックをunit testで固定し、pre-commitとCIをblockingにした。
- `delete_pdfs`のメタ更新ロック集約は`fc998c0`で既に完了していることも確認した。
  Phase 6の他の実施コミットは旧移行ツール撤去`1987ed3`、logger統一`c11dfee`。

**検証**: C901 28 current / 28 baseline、ratchet unit 3 passed、
pre-commit設定検証、Ruff、docs checkを通過。

---

## 再計画 Phase 6-2（2026-07-26）— hitomi routerの共通logger統一（P3）

- `routers/hitomi.py`の標準`logging.getLogger`を共通`get_logger`へ変更した。
- NOZOMI障害時の登録成功というAPI挙動は維持し、警告が共通のconsole・
  永続ログ経路へ渡されることをlogger呼び出しテストで固定した。

---

## 再計画 Phase 6-1（2026-07-26）— 使用不能な一回性移行ツールの撤去（P2）

- `tools/migrate_flatten.py`は旧`source="generated"`、固定2フォルダ、
  `meta.json`を前提とし、現行の3 sourceと`meta2.db`では実行不能だった。
- コード・設計・運用からの参照は運用ガイドだけで、同ガイドも「再実行しない」と
  明記しながら実行例を掲載していたため、誤実行防止としてツールを撤去した。
- 過去内容はGit履歴に保持し、将来の移行は現行schema向けに新規作成する。

---

## 再計画 Phase 5（2026-07-26）— フロントエンド共通ロジック整理（P2）

- Readerのsingle/spread、RTL/LTR、表紙、末尾、奇数page計算を
  `features/reader/page-navigation.ts`へ集約し、sliderとnavigation hookを統一した。
- Kindle購入一覧のquery許容値、既定値、page resetを
  `features/kindle/catalog-query.ts`へ移し、URL正本と不変更新を固定した。
- OCR QAのQuery、run/page選択、mutation、入力状態を`useOCRQaController`へ分離し、
  panelを表示とイベント配線へ限定した。
- Playwrightの対象URLを`PLAYWRIGHT_BASE_URL`で切替可能にし、陳腐化していた
  取り込み画面のheading/button期待値を現行UIへ同期した。

**検証**: frontend 119 files / 1,055 tests、TypeScript build、Knip、
production buildを通過。`medaroserver:8090`に対するChromium E2E 8件を通過。

---

## 再計画 Phase 4（2026-07-26）— OCR処理境界の分割（P2）

- `surya_ocr.py`をデータ型、prompt/HTML/layout解析、品質評価、server/client実行へ
  分割し、旧moduleは同一公開symbolを再exportする互換facadeとした。
- `ocr_staging.py`を入力画像SHA・run保存、ページ/layout分類、QAレビュー・
  原子的公開へ分割し、既存importを維持した。
- `job_worker.py`をqueue loop、ジョブ状態永続化、対象選択、mode別executorへ分割した。
  facadeで依存を組み立てるため、既存のテスト拡張点と運用拡張点は維持される。
- prompt、OCR閾値、品質flag、ページ分類、QA条件、公開トランザクションは変更していない。

**検証**: OCR関連60 passed、target basedpyright 0 errors、固定20画面の現行OCRは
全体CER 16.42%、通常散文layout CER 1.82%で分割前と一致。backend 1,038 passed / 2 skipped、
Ruff、docs check、ファイルマップcheckを通過。

---

## 再計画 Phase 3（2026-07-26）— Kindle自動撮影境界の分割（P1）

- `KindleAppController`をidentity、window/control、library、readerへ分割し、
  旧moduleとclassを互換facadeとして維持した。
- capturerを基底window、撮影ループ、手動UI、漫画cropへ分け、Windows agentを
  API/heartbeat、package公開、工程制御へ分割した。
- Linux側を状態repository、ready package検証、正式登録・補償処理へ分割した。
  同一ASIN・同一書籍IDの再撮影は旧画像を世代バックアップへ退避して置換し、
  異なるASINの同名書籍は拒否する。
- 正式画像配置後、meta更新後、package移動後、DB更新直前の障害注入テストで、
  画像、meta、package、job状態がすべて元へ戻ることを固定した。
- ユーザー指定により先頭位置合わせは直接ページ遷移を必須とし、画像差分で
  数十画面を戻る方式を削除した。小説の `ページ1/N・0%` は表紙の次の画面であるため、
  `ReadingArea`へフォーカスを戻して逆方向キーを1回だけ送り、
  `Location 1・0%`を再確認する。漫画は直接遷移後の`Location 1/2・0%`を使う。
- 現行Kindleの「ページへ移動する」への改名、別UI Automation rootのpopover、
  座標クリックを受け付けない`backButton`へ追従した。Name、AutomationId、
  Kindleウィンドウ内座標を検証し、通常のキーボード・マウス入力だけを使う。

**実機検証**:

- 小説10巻はジョブ`92b33d22-261e-45b5-8ab3-063eff46d6bf`で92画面を登録。
  3516×1940、連番欠落0、SHA-256重複0、全面白0、ASIN一致。
- 小説の直接遷移は`ページ1/233・0%`から右キー1回だけで
  `Location 1 of 2959・0%`の表紙へ戻ることを確認した。
- 漫画1巻はジョブ`bca6326e-e7d9-431b-8884-a178a9e415bc`で85画面を再登録。
  2936×2064、連番欠落0、SHA-256重複0、全面白0、ASIN一致。
  表紙、P.9、P.10、最終画面を目視し、P.9から見開きと左右端が欠けないことを確認した。
- 同一ASIN置換の世代バックアップを小説・漫画とも本番データルートへ作成した。

**自動検証**: Kindle 59 passed、backend 1,035 passed / 2 skipped、
Ruff、docs check、`git diff --check`を通過。

---

## 再計画 Phase 2（2026-07-26）— APIスキーマのドメイン分割（P1）

- 948行、109モデルの`routers/api_schemas.py`を、common、library、novel DB、
  OCR、Kindle catalogの5ドメインへ分割した。
- `routers/schemas/__init__.py`を公開境界とし、旧`routers/api_schemas.py`は
  7行の互換再exportに縮小した。
- 旧moduleと新moduleの全公開モデルが同一objectであることを回帰テストへ固定した。

**検証**: 正規化OpenAPI SHA-256
`498e819a1331e5b55897fdac2ee525d120285c9991c019f80aa2d4e3a2052833`が
変更前後で一致（102 paths / 154 schemas）。`openapi-typescript`生成型は
Prettierの1行折り返し以外に差分なし。backend 1,024 passed / 1 skipped、
frontend TypeScript、Ruff、docs check、ファイルマップcheckを通過。

---

## 再計画 Phase 1（2026-07-26）— 設計書体系の正常化（P1）

- 完了済みのKindle購入カタログ統合、UI改善引継ぎ、自動撮影取込の3計画を
  `docs/log/計画/`から`docs/archive/`へ移し、living文書とMkDocs navの参照を更新した。
- 846行だったフロントエンド詳細設計を、223行の共通設計、208行の
  ライブラリ・リーダーUI設計、128行の小説RAGフロントエンド設計へ責務分割した。
- Kindle catalogのスキーマ管理はSQLModel/Alembic、実行時アクセスは短命な
  `sqlite3`接続とする現行判断をADR-0017へ記録し、ADR-0016との関係を明確にした。
- OpenAPI生成型とローカルUI型の差を説明するコメントへ更新し、陳腐化したTODOを除去した。

**検証**: `check_docs.py`はブロッキング違反0件、design文書の800行超警告0件。
`generate_file_map.py --check`は変更なし。実装、API、DB、画面挙動は変更していない。

---

## 再計画 Phase 0（2026-07-26）— 計画書リセットと健全性基準線の確定（P1）

- 「未着手Phase 0件」と未評価候補が併存していた計画書を廃止し、
  設計書、API契約、Kindle、OCR、frontend、品質ゲートのPhase 1〜6へ再構成した。
- 完了、スキップ、陳腐化、技術メンテナンス候補を現行ロードマップから分離した。
- docs検査、ファイル行数、C901、テスト収集数、Knipを実測し、以後の悪化を判定する
  基準線を記録した。
- 設計書とソースの長期的な崩れを防ぐため、正本、ラチェット、責務境界、
  契約比較、期限付き例外、段階的CI導入を定めた健全性維持計画を新設した。

**検証**: `check_docs.py`はブロッキング違反0件、
`generate_file_map.py --check`は変更なし、`git diff --check`はエラーなし。
design文書の既存800行超警告1件はPhase 1の対象として維持する。

---

## Phase 99（2026-07-26）— Kindle先頭移動のロケーション指定高速化（P1）

- `go_to_start(source, direction)` は、ページ設定確定後に「位置に移動」を開き、
  ロケーション`1`を通常のキーボード入力で指定する高速経路を優先する。
- `moreMenuButton`、Name`位置に移動`の`btn-popover-menu-item`、
  `go-to-page-input`、`modal-confirm`をNameとAutomationIdで限定探索する。
  UI Automationの`SetValue` / `Click`は使用しない。
- 小説は`Location 1・0%`、2ページ表示漫画は`Location 1/2・0%`を先頭として
  source別に検証する。入力値を読み戻せない環境でも、移動後フッターの検証を必須とする。
- locator取得不能、入力値不一致、移動結果不一致では、既存の画像差分による
  1画面ずつの先頭探索へフォールバックする。
- 実機では小説`2748/3304 → 1/3304`を2.38秒、漫画`150/169 → 2/169`を
  2.41秒で完了した。単体テスト50件、Ruff、docs checkを通過した。

**変更ファイル**: `kindle-pdf/kindle_app_controller.py`、
`kindle-pdf/capture_agent.py`、controller / agentテスト、関連設計書。

---

## Phase 98（2026-07-19）— 実装監査に基づく共通化・ストリーム・依存健全化（P2）

- POST SSEの独自改行分割を`eventsource-parser`へ置換し、CRLF・複数data行・分割チャンク・1MBバッファ上限・不正JSON通知を統一。
- Reader Contextを`use-context-selector`のフィールド購読へ変更し、`ReaderHeader` / `ReaderPageView` / `ShortcutsHelpDialog`を`React.memo`で保護。
- hitomi監視1回につき1つの`httpx.Client`を共有し、NOZOMIと全メタデータ取得で接続プールを再利用。
- キャラクター名解析、シリーズメタ索引、LLM options辞書生成をそれぞれ共通ヘルパーへ統合。
- Knipを`npm run lint:deps`として導入し、未使用Radix依存5件・未使用ファイル4件を削除。`pdfjs-dist`とバックエンドのPydantic / SQLAlchemy / PyArrowを直接依存として明示。
- 互換範囲内のfrontend依存とFastAPI / Uvicorn / Alembic / LanceDB / NumPy / PyMuPDF / SQLModel / pytest / Ruffを更新。FastAPI 0.139のrouter内部表現変更に合わせ、ルーター登録テストをprivate構造からOpenAPI契約検査へ変更。

**検証**: backend pytest 948 passed / 1 skipped、frontend Vitest 1008 passed、Ruff・basedpyright・ESLint（error 0）・TypeScript/Vite build・Knip・docs check・ファイルマップ生成を通過。

---

## Phase 93〜97（2026-07-19）— セキュリティ境界・設計正本・データアクセス・取得層・責務分割

- **Phase 93（P1）パス境界**: Windows drive パス・NUL・`..` を拒否し、`resolve_under_base()` で symlink 解決後もデータルート配下であることを強制。library / PDFs / thumbnails / discussion の全対象 I/O と回帰テストへ適用。
- **Phase 94（P1）設計書正本**: OCR の直接 DB 保存、`meta2.db`、source 3 値、OpenAPI 204 契約、XSS 例外、UI ファイル名、ADR supersede を living 文書へ反映。完了済み CI 計画を archive へ移管。
- **Phase 95（P1）SQLite**: `db_connection()` で commit / rollback / close を保証し、`update_meta_locked()` を同一 transaction 内の追加・変更・削除差分だけに限定。
- **Phase 96（P1）TanStack Query**: novel 系 7 hook の生 fetch state を `useQuery` / `useInfiniteQuery` / mutation・query invalidation へ統一。
- **Phase 97（P2）責務分割**: `search_scope.py` と `book_summary_search.py` をページ検索から分離し、`buildUnifiedRows.ts` を `useNovelManage.ts` から抽出。公開 import は互換維持。

**検証**: backend pytest 921 passed / 1 skipped、frontend Vitest 1004 passed、Ruff・ESLint（error 0）・TypeScript・Prettier・docs check・ファイルマップ check 通過。

---

## Phase 78（2026-05-15）— `NovelManagePage.tsx` ロジック抽出 → `useNovelManage`（P3）

**問題**: Phase 61 で `AmazonImportButton` 抽出と hook 遅延起動を実施済みだが、`NovelManagePage.tsx` は 402 行あり、タブ切替・ジョブ監視・書籍選択・`unifiedRows` 構築が混在していた。
**変更**: 全ロジックを `frontend/src/hooks/useNovelManage.ts` に抽出。`useNovelBuildQueue` / `useOcrStatus` を内部呼び出しする薄いオーケストレーター設計。ページ側は `const x = useNovelManage()` の 1 行で済む構造に縮小（JSX 純粋化）。vitest ユニットテスト 17 件追加。
**変更ファイル**: `frontend/src/pages/NovelManagePage.tsx`（修正）、`frontend/src/hooks/useNovelManage.ts`（新規）、`frontend/src/test/useNovelManage.test.ts`（新規）

---

## Phase 77（2026-05-15）— `react-pdf` / `pdfjs-dist` 二重依存の整理（P3）

**問題**: `react-pdf@10` と `pdfjs-dist@5` を両方 `package.json` に明示的に列挙しており、バージョン管理が二重になっていた。  
**実態**: `react-pdf@10.2.0` が `pdfjs-dist@5.4.296` を `dependencies` に直接持つため、`package.json` から削除しても node_modules には react-pdf 経由で残る。worker URL `'pdfjs-dist/build/pdf.worker.min.mjs'` は変更不要。  
**変更**: `frontend/package.json` から `pdfjs-dist` の明示エントリを削除。  
**変更ファイル**: `frontend/package.json`

---

## Phase 73（2026-05-15）— `experiments/` リポジトリ整理（P3）

**問題**: `experiments/manga_ai_validation.ipynb`（62 MB）が git 管理下に入っており、リポジトリが肥大化していた。
**変更**: `git rm --cached` でインデックスから除外し、`.gitignore` に `experiments/*.ipynb` を追加。ファイル自体はローカルに保持。
**変更ファイル**: `.gitignore`

---

## Phase 63（2026-05-15）— `embedder.py` を urllib → httpx に変更（P3）

**問題**: `embedder.py` が `urllib.request` で Ollama embed API を叩いていたが、`httpx` はすでに依存に含まれており実装が不一致だった。  
**変更**: `urllib.request.urlopen` → `httpx.post`。`httpx.HTTPError`（接続失敗・タイムアウト・4xx/5xx）を `EmbeddingError` に変換。公開インターフェース（`embed_batch` / `EmbeddingError`）は変更なし。sync のまま（async 化は別 Phase 候補）。  
**変更ファイル**: `backend/services/novel_db/embedder.py`、`backend/tests/test_novel_db_embedder.py`、設計書 §5.3

---

## Phase 62（2026-05-14）— ベクトル DB を sqlite-vec → LanceDB に移行（P2）

**問題**: sqlite-vec の `vec0` 仮想テーブルは線形スキャン O(n) のため、チャンク数増加（書籍追加・B-17 漫画キャプション追加）に比例して検索レイテンシが増大する。ANN インデックスを後から追加できない設計制約があった。

**実装**:
- `backend/services/novel_db/lance_store.py` 新規作成:
  - `get_chunks_table()` / `get_summaries_table()` — try/except で open_table → create_table
  - スキーマ: chunks `(chunk_id, book_name, page_no, text, char_count, page_count, embedding[1024])`
  - スキーマ: summaries `(book_id, book_name, embedding[1024])`
  - `maybe_create_index()` — 50,000 チャンク超で IVF_PQ（num_partitions=256, num_sub_vectors=64）自動ビルド
  - `reset_db()` — テスト用グローバル接続リセット
- `backend/scripts/migrate_to_lancedb.py` 新規作成 — sqlite-vec BLOB を float32 リストに unpack してバルクコピー
- `backend/config/novel_db.py` — `NOVEL_DB_LANCE_PATH` 追加（デフォルト `data/novel.lancedb`、環境変数上書き可）
- `embedder.py`: `serialize_f32()` 削除（LanceDB は Python list[float] をネイティブ受け付ける）
- `schema.py`: `chunks_vec` / `book_summaries_vec` DDL 削除
- `search.py` / `builder.py` / `full_builder.py` / `summarizer.py` / `build_chunk_contexts.py`: LanceDB API に全面切り替え
- テスト 4 ファイル + `conftest.py` 更新: LanceDB パスを tmp_path にパッチ、`reset_db()` で分離

**本番移行（2026-05-14）**: D: ドライブが exFAT のため lance 4.0.0 の hard_link/atomic rename が失敗。`.env` に `NOVEL_DB_LANCE_PATH=C:\Users\amashio\AppData\Local\Pic2PDF\novel.lancedb` を設定し C: NTFS へリダイレクト。`migrate_to_lancedb.py` 実行: **chunks 2,781 件・summaries 13 件** 移行完了。

**効果**: pytest 817 件全通過。FTS5 は SQLite に残しハイブリッド RRF 構成は変更なし。チャンク 50,000 超で IVF_PQ 自動ビルドによる ANN 検索が有効化される。

コミット: `71cbfc8`（実装）→ `68644da`（empty_table 修正 + 本番移行記録）

---

## Phase 57〜61（2026-05-14）— grill-me セッション由来の 5 フェーズ一括実施

grill-me で抽出した技術負債 5 件を連続実施。コミット: `137aa60`（57+58）→ `2e95bd5`（59）→ `a996606`（60）→ `ae184e0`（61）。

### Phase 57 — `novel_db.py` ルーター 6 分割（P1）

**問題**: 644 行・25 関数・7 責務混在。Pydantic モデル 11 個インライン。  
**実装**: `backend/routers/novel_db/` パッケージ化。`lib.py` / `character.py` / `rebuild.py` / `search.py` / `qa.py` / `chat.py` + `schemas.py` + `_deps.py`（`require_not_locked`・SSE ヘルパー）。旧 `novel_db.py` 削除。フロントエンド API URL は `features/novel_db/api.ts` / `sse.ts` で吸収。  
**効果**: 元 644 行 → 各サブルーター 50〜120 行。既存 pytest 全通過。

### Phase 58 — `ReaderPanel.tsx` hook 抽出（P1）

**問題**: 455 行・カスタムフック 18 個でも本体に 35 個 `const`。  
**実装**: 当初 5 層化（`useReaderState` / `ReaderLayout` / `PageRenderer` / `useReaderInput` / `useZoom`）計画だったが ROI 評価でスコープ縮小。`usePdfPosition` / `useSpreadLayout` / `useZoomPan` の 3 hook を既存 hooks 配下に抽出し、ReaderPanel を 280 行程度に削減。vitest 全通過。

### Phase 59 — `job_queue.py` worker 分離 + 旧 JobMode DB マイグレーション（P1）

**問題**: 374 行でキューイング/worker 管理/DB CRUD が混在。旧モード名（`pdf_text` / `reocr`）がコード・DB に残存。  
**実装**:
- `NovelDbJobWorker` を `job_worker.py` に抽出。`stop_event: threading.Event` と `wakeup: threading.Event` を `NovelDbJobQueue` から受け取る shared Event パターン。
- `job_queue.py` は 120 行に削減（キューイング責務のみ）。
- `start()` 時に `UPDATE rebuild_jobs SET mode='rebuild' WHERE mode='pdf_text'` と `SET mode='ocr' WHERE mode='reocr'` を実行してDB マイグレーション。
- `schema.py` DEFAULT 値も `rebuild` に修正。
- **躓き点**: `_is_running` が worker に移動したため `test_router_novel_db_search.py` の monkeypatch ターゲットを `job_queue._worker` に変更する必要があった。

### Phase 60 — `summarizer.py` prompts 抽出（P2）

**問題**: 479 行でプロンプト 3 種がロジックと密結合。`parse_combined_output` がファイル後半に埋没。  
**実装**: Strategy パターン化はスコープ超過と判断してスキップ。プロンプト定数・LLM オプション dict（`ONE_SHOT_OPTIONS` 等）・`parse_combined_output()` を `_prompts.py` に抽出し `summarizer.py` を 230 行に削減。  
**躓き点**: `CHAR_SUMMARY_TARGET_CHARS` のインポート後に参照箇所が `FINAL_SUMMARY_TARGET_CHARS`（誤）になっており Ruff F401 で検出 → `char_target=CHAR_SUMMARY_TARGET_CHARS`（400 chars）に修正。

### Phase 61 — `NovelManagePage.tsx` Amazon インポート独立化 + hook 遅延起動（P2）

**問題**: 447 行で Amazon CSV インポート 40 行・OCR hook・Build hook が混在。Build タブ未訪問でも SSE 接続が常時起動。  
**実装**:
- `AmazonImportButton.tsx` を `components/novel_build/` に抽出。`showToast` prop で親に通知、自身で `isImporting` state を管理。
- `useNovelBuildQueue(enabled = true)` / `useOcrStatus(enabled = true)` に `enabled` フラグを追加。`useEffect` の deps に `[enabled]`。
- `NovelManagePage` で `buildEnabled` を `useState(false)` + `handleTabChange` で "once-activated stays active" パターンを実装。OCR は初期 `true`（即時接続）、Build は Build タブ初回訪問で `true` に遷移。
- `useNovelJobStats` 抽出は統計ロジック不在のためスキップ。
- `AmazonImportDialog`（Dialog 化）は過剰と判断し Button 止まり。

---

## Phase 56（2026-05-14）— 新規コンポーネント群の構造整合・SSE 共通化・命名修正

B-16 / B-20 / §4.3 / §4.5 / §4.6 実装後の棚卸しで検出した 18 件のうち 16 件を実施、2 件をスキップ判断。実装は各機能追加時に並行して対応済み。

| Sub | 内容 | 優先 | 状態 |
|---|---|---|---|
| 56-1 | `test_embedder.py` → `test_novel_db_embedder.py` リネーム | P1 | ✅ |
| 56-2 | `sse_event` / `cancel_job_response` を `_deps.py` に抽出（全ルーターが import） | P2 | ✅ |
| 56-3 | `_astream_chat` 二重定義解消（`discussion_service` が `llm.astream_chat` を import） | P3 | ✅ |
| 56-4 | `library.get_book_detail` の discussion 集計を `count_discussions()` で軽量化 | P3 | ✅ |
| 56-5 | `bench_llm_backend.py` 配置見直し | P3 | **スキップ** |
| 56-6 | 全 SSE ルーター（`novel_db` / `novel_discussion` / `novel_build`）に `logger.exception` 追加 | P2 | ✅ |
| 56-7 | `features/novel_db/sse.ts` の SSE パース 3 重実装を `postSseStream` 共通ヘルパーに統合 | P1 | ✅ |
| 56-8 | 日付フォーマット直書き 5 箇所を `utils/date.ts` に統一 | P1 | ✅ |
| 56-9 | `BookMetaEditModal` / `AmazonCsvImportSection` の独自 Modal を共通 Dialog 化・`alert()` → toast | P1 | ✅ |
| 56-10 | `useNovelBuildQueue` SSE 再接続 cleanup 修正（`reconnectTimerRef` 追加） | P1 | ✅ |
| 56-11 | `NovelBuildPage.tsx` サブコンポーネントを `components/novel_build/` に分離 | P2 | ✅ |
| 56-12 | `BookCard.tsx` / `NovelDetailPage.tsx` の書籍メタ JSX を `BookMetaList.tsx` に統合 | P2 | ✅ |
| 56-13 | `features/novel_build/` を `types.ts` / `api.ts` / `sse.ts` の 3 分割構造に整理 | P2 | ✅ |
| 56-14 | `Layout.tsx` の navLinks を `NAV_GROUPS` 配列化（7 回ベタ書き → `.map` 描画） | P2 | ✅ |
| 56-15 | `DiscussionHistoryItem` 型を `types.ts` に移動（`api.ts` は re-export のみ） | P2 | ✅ |
| 56-16 | `NovelDiscussionPage.tsx` から `PersonaPanel.tsx` を `components/novel_db/` に切り出し | P2 | ✅ |
| 56-17 | `hooks/novel_build/index.ts` barrel 追加（`novel_db` との非対称を解消） | P3 | ✅ |
| 56-18 | `DiscussionHistoryItem` ↔ `QuestionHistoryItem` 折りたたみカード共通化 | P3 | **将来再評価** |

---

## Phase 55（2026-05-14）— novel_db dead code 削除・SSE ログ補完・旧 PDF 経路撤去・DRY 削減

Phase 55 候補 10 件のうち 8 件を実施、2 件をスキップ判断。

| Sub | 内容 | 優先 | 状態 |
|---|---|---|---|
| 55-1 | `backend/scripts/tmp_b9_redo_id23.py` 削除（B-9 検証後の一時スクリプト） | P1 | ✅ |
| 55-2 | `_should_skip_context` を `contextualizer.py` に移動（`should_skip_context` としてパブリック化） | P1 | ✅ |
| 55-3 | `post_qa` retrieval ロジック（70 行）+ `_collect_initial_context`（60 行）を `retrieval.py` に抽出 | P2 | ✅ |
| 55-4 | `search.py` の `_resolve_book_names` に `@lru_cache(maxsize=16)` を追加（`Scope` を `frozen=True` に） | P2 | ✅ |
| 55-6 | 全 SSE ルーターに `logger.exception` 追加（56-6 と並行対応） | P2 | ✅ |
| 55-7 | `config.py` NOVEL_DB_* 設定を `config/novel_db.py` サブモジュールに分離 | P2 | ✅ |
| 55-8 | `useNovelDbRebuildJob.test.ts` 新設（7 テスト） | P2 | ✅ |
| 55-9 | `main.py` の `kindle_novel/pdfs` マウント・フロント `source=novel` PDF 分岐を撤去 | P2 | ✅ |
| 55-5 | `bench_llm_backend.py` を `scripts/bench/` へ移動 | P3 | **スキップ** |
| 55-10 | `ReaderPanel.tsx` 分割再検討 | P3 | **スキップ維持** |

---

## Phase 54（2026-05-06）— 共通化余地・ライブラリ使い分けの横断棚卸し

ファイルサイズ以外の観点（重複パターン・ライブラリ統一・既存ユーティリティ未活用・typing スタイル）で `/check-docs` 整合性チェック中に検出した **8 件**を実施。いずれも実害は軽微で **P3**（構造改善）扱いだが、横断的に同一ロジックが散在しているため一度に手当てすると再発しにくい。さらにスキップ判断とした参考 3 件（54-9〜54-11）を末尾に併記する。

**検証結果（Phase 54 全体）**:
- Backend pytest: 570/570 passed
- Frontend vitest: 142/142 passed、tsc クリーン、ESLint 既存警告 1 件のみ（本 Phase で増減なし）

### Phase 54-1（**P3**）— frontend HTTP クライアントの統一

**問題**: `frontend/src/components/viewer/ToolsMenu.tsx:18-30` の `downloadMetaExport` だけが生 `fetch()` を使用しており、それ以外のフロントエンド全体は `apiClient`（axios ベース）に統一されている。axios のインターセプタ（タイムアウト・エラー処理・base URL 解決）の恩恵を受けられず、将来 base URL を変えたときに 1 箇所だけ拾い損ねるリスクがある。

**改善案**: `apiClient.get(url, { responseType: 'blob' })` で取得し、Content-Disposition ヘッダから filename を抜く処理は同じパターンで書き換える。`buildApiUrl` も axios の `baseURL` 解決に揃えれば不要。

**変更対象**: `frontend/src/components/viewer/ToolsMenu.tsx`
**完了条件**: `fetch(` の grep 結果が `frontend/src/` 配下で 0 件（test setup 等の `vi.fn` モックは別）。`vitest` 全通過。実機でメタデータエクスポートのファイル名が従来通り取れること。

**実績**: `apiClient.get(url, { responseType: 'blob' })` で blob 取得に変更。axios インターセプタが `response.data` を返す制約上、`Content-Disposition` ヘッダの参照は不可になったため、バックエンド命名規則 `meta_{source}_{YYYYMMDD}.json` をフロント側で再生成する形にした。`fetch(` の grep 結果は 0 件。

---

### Phase 54-2（**P3**）— `localStorage` 直接アクセスの集約

**問題**: `frontend/src/utils/storage.ts` に `getStorageJson` / `setStorageJson` が用意されているのに、`frontend/src/hooks/useLibraryPins.ts:8-19` が同等の `loadPins` / `savePins` を再実装している。`useDarkMode.ts` は string 値（`'true'` / `'false'`）専用で別パターン。

**改善案**:
- `useLibraryPins.ts` の `loadPins` / `savePins` を `getStorageJson` / `setStorageJson` 呼び出しに置換し、フック内のヘルパーを撤去（10 行削減）。
- `useDarkMode.ts` は string 専用で挙動が違うため `utils/storage.ts` に小さなヘルパー（`getStorageString` / `setStorageString`）を追加して統一するか、専用フックのまま残すかは実装時に判断。**第一弾は `useLibraryPins.ts` のみ**で打ち切り、後者は判断保留。

**変更対象**: `frontend/src/hooks/useLibraryPins.ts`
**完了条件**: `localStorage\.` の grep 結果が `frontend/src/hooks/` で `useDarkMode.ts` の 2 件のみ（および `utils/storage.ts` 内）。`vitest` 全通過。

**実績**: `loadPins` / `savePins` を撤去し `getStorageJson<PinsMap>(key, EMPTY_PINS)` / `setStorageJson(key, next)` 呼び出しに置換。第二弾（`useDarkMode` の string 専用化）はスコープ外として保留。

---

### Phase 54-3（**P3**）— `useAsyncToast` の適用範囲を広げる

**問題**: `useAsyncToast` フック（Phase 47）が導入済みで `LibraryPanel` / `useLibraryBulkActions` / `HitomiPage` で使われているが、`AutoFillAuthorsBar.tsx:30-34` と `SeriesResolveBar.tsx:22-25` はまだ素の `try / catch + showToast(e.message, 'error')` を再実装している。両者ほぼ同一パターン（`startJob` → catch でメッセージ抽出 → トースト）。

**改善案**: `useAsyncToast(showToast)` で `runAsync(() => startAutoFill(...))` / `runAsync(() => startSeriesResolve(...))` に書き換える。各バーの `try / catch` ブロックを撤去して 5〜6 行削減。

**変更対象**: `frontend/src/components/viewer/AutoFillAuthorsBar.tsx` / `frontend/src/components/viewer/SeriesResolveBar.tsx`
**完了条件**: 上記 2 ファイルから `try {` ブロックが消える（または 1 つに減る）。両バーの実機操作で API 失敗時のトーストが従来どおり出ること。`vitest` 全通過。

**実績**: 両バーで `runAsync = useAsyncToast(showToast)` を取得し、`handleStart` を `() => runAsync(() => startXxx(...), '失敗メッセージ')` に圧縮。手書き try/catch は削除。

---

### Phase 54-4（**P3**）— 作者キー生成の共通化

**問題**: 「authors 配列を sort して `'\n'` で join した文字列」を作者集約のキーとして使うパターンが、フロント側だけで 5 箇所に散在:

- `components/viewer/LibraryPanel.tsx:165` — `[...entry.authors].sort().join('\n')`
- `hooks/useLibraryGrouping.ts:94` / `163` — 同上 2 箇所
- `hooks/usePinnedBookSets.ts:42` / `59` — 同上 2 箇所
- `hooks/useSeriesAuthorFilter.ts:33` / `43` / `52` / `62` — 同上 4 箇所

バックエンド (`series_detector.py:29`) では `tuple(sorted({a.strip() for a in authors if a.strip()}))` のスタイルでキー化しており、フロントは「sort + join」だけ・空白除去なし、で挙動が微妙に違う。同じキーを共有する必要はないが、**フロント内では揃える価値がある**。

**改善案**: `frontend/src/utils/authors.ts` を新設し `authorsKey(authors: string[]): string` を export。空白 trim のセマンティクスは現行（trim なし）に揃える。9 箇所を import + 関数呼び出しに置換。

**変更対象**:
- 新規: `frontend/src/utils/authors.ts`
- 修正: `frontend/src/components/viewer/LibraryPanel.tsx` / `frontend/src/hooks/useLibraryGrouping.ts` / `usePinnedBookSets.ts` / `useSeriesAuthorFilter.ts`

**完了条件**: `\.sort\(\)\.join\('\\n'\)` の grep 結果が 0 件。`vitest` 全通過（既存の `useLibraryGrouping.test.ts` / `useLibraryPins.test.ts` がそのまま通ること）。

**実績**: `authorsKey(authors)` シグネチャ（`readonly string[] | undefined | null` 受け）に統一。9 箇所すべて置換、grep 0 件。`useLibraryGrouping.ts` の display title 用にだけ `[...authors].sort()` 呼び出しが 1 つ残るが、こちらはキー化用途ではなく `'作者A, 作者B コレクション'` の組み立て用。

---

### Phase 54-5（**P3**）— `errorMessage(e, fallback)` ユーティリティ抽出

**問題**: `e instanceof Error ? e.message : "..."` の三項演算が **17 箇所**に散在する。`useDialogSubmit` / `useAsyncToast` / `useEditMode.showError` は内部に取り込み済みだが、それらに乗っていない以下のコンポーネント／フックが個別実装で再発している:

- `components/viewer/GenreManagerDialog.tsx:30` / `42`
- `components/viewer/AutoFillAuthorsBar.tsx:34` / `SeriesResolveBar.tsx:26`（Phase 54-3 で `useAsyncToast` に移行する想定だが、useAsyncToast の内部実装も同パターン）
- `components/hitomi/HitomiWatchlistDialog.tsx:54` / `69`
- `pages/HitomiPage.tsx:68` / `pages/GeneratorPage.tsx:54` / `69`
- `features/ocr/OCRPanel.tsx:26` / `37`
- `hooks/useHitomiArrivals.ts:46` / `useHitomiWatchlist.ts:33`

ロジックは固定（`unknown → string`）なのにメッセージ文言だけが変わる典型的なボイラープレート。

**改善案**: `frontend/src/utils/error.ts` を新設し `errorMessage(e: unknown, fallback: string): string` を export（5 行）。各箇所で `setError(errorMessage(e, '追加に失敗しました。'))` のように呼び出す。`useDialogSubmit` / `useAsyncToast` の内部実装もこのユーティリティ経由に揃え、再発防止する。

**変更対象**:
- 新規: `frontend/src/utils/error.ts`
- 修正: 上記 11 ファイル + `hooks/useDialogSubmit.ts` + `hooks/useAsyncToast.ts`

**完了条件**: `instanceof Error \?` の grep 結果が `frontend/src/` 配下で 0 件、または `utils/error.ts` のみに集約されること。`vitest` 全通過。

**実績**: `frontend/src/utils/error.ts` を新設し、`useDialogSubmit` / `useAsyncToast` 含む 11 ファイルで `errorMessage(e, fallback)` に置換。`useAsyncToast` 内部の三項演算もユーティリティ経由に。grep 結果は `utils/error.ts`（実装本体・コメント）のみ残存。

---

### Phase 54-6（**P3**）— 日付フォーマットユーティリティ集約

**問題**: 日付の人間向け整形が 3 箇所で別実装。

- `components/reader/PdfCard.tsx:127` — `new Date(pdf.created_at * 1000).toLocaleDateString()`（Unix 秒 → ロケール短縮）
- `components/hitomi/HitomiArrivalCard.tsx:9-18` — `formatDate(iso)` ローカル関数、`d.toLocaleDateString('ja-JP', { year, month, day })` + `isNaN(d.getTime())` フォールバック
- `pages/HitomiPage.tsx:48-50` — `new Date(iso).toLocaleString('ja-JP')`、try/catch でフォールバック

3 箇所とも軽微なフォールバック処理が入っており、片方は ISO・片方は Unix 秒・片方は date-only と入力もまちまち。共通化することで「無効値→そのまま返す」「`isNaN` チェック」の挙動を一致させる。

**改善案**: `frontend/src/utils/date.ts` に以下を集約:

```ts
export function formatDateJa(iso: string): string;        // ISO → 'YYYY/MM/DD'
export function formatDateTimeJa(iso: string): string;    // ISO → ja-JP toLocaleString
export function formatTimestampJa(unixSec: number): string; // Unix秒 → toLocaleDateString
```

各実装は無効値（`isNaN` / 空文字）でフォールバック値を返す。`HitomiArrivalCard` 内の `formatDate` は撤去。

**変更対象**:
- 新規: `frontend/src/utils/date.ts`
- 修正: `components/reader/PdfCard.tsx` / `components/hitomi/HitomiArrivalCard.tsx` / `pages/HitomiPage.tsx`

**完了条件**: 上記 3 ファイルから `new Date(` 呼び出しが消える。実機で日付表示が変わらないこと（特に HitomiArrivalCard の `2-digit` フォーマットを保持）。`vitest` 全通過。

**実績**: `formatTimestampJa` / `formatDateJa` / `formatDateTimeJa` を新設。3 箇所すべてユーティリティ呼び出しに置換し、`HitomiArrivalCard` のローカル `formatDate` と `HitomiPage` のローカル `formatDateTime` を撤去。

---

### Phase 54-7（**P3**）— Backend typing を PEP 604 形式に統一

**問題**: バックエンドの大半は `X | None` / `list` / `dict` / `tuple` の現代スタイル（PEP 604）で書かれているが、以下 6 ファイルだけが `Optional[X]` / `List[X]` / `Dict[X, Y]` の旧 `typing` モジュールスタイルで残っている:

- `services/ocr_service.py` — `Optional[int]` / `Optional[str]` / `Dict[str, Any]`
- `services/gemma_client.py` — `Optional[Callable[..., Any]]` × 5
- `services/job_manager.py` — `Optional[str]` × 4
- `services/pdf_generator.py` — `Optional[Callable]` × 2
- `services/batch_compressor.py` — `Optional[Callable]` × 1
- `routers/ocr.py` — `Optional[int]` / `List[str]`

Python 3.10+ 環境のため `from __future__ import annotations` も不要。エディタ・mypy・ruff のフォーマットが微妙に揺れる原因にもなっており、新規コードの参照モデルとして悪影響がある。

**改善案**: 上記 6 ファイルの `Optional[X]` を `X | None`、`List[X]` を `list[X]`、`Dict[K, V]` を `dict[K, V]` に置換。`from typing import Optional, List, Dict` 行も削除（`Callable` / `Any` は残す）。

**変更対象**: `backend/services/ocr_service.py` / `gemma_client.py` / `job_manager.py` / `pdf_generator.py` / `batch_compressor.py` / `backend/routers/ocr.py`

**完了条件**: `from typing import.*\(Optional|List|Dict\)` の grep 結果が backend 配下で 0 件。`pytest` 全通過。`ruff check backend/` でエラーなし。

**実績**: 本 Phase に先立って `chore: バックエンド Ruff 導入`（commit `d093014`）の `UP` ルールで自動置換済み。本 Phase での再確認時、`Optional[` / `: List[` / `: Dict[` / `-> List[` / `-> Dict[` / `: Tuple[` の grep 結果はすべて 0 件。`pytest` 570/570 通過。

---

### Phase 54-8（**P3**）— `useDebouncedValue` フック化

**問題**: 値のデバウンスを `useEffect + setTimeout + cleanup` で 2 箇所が個別実装。

- `components/reader/PdfSearchBar.tsx:38-43` — `localText` が変わるたび `setTimeout(SEARCH_DEBOUNCE_MS)` で親へ通知
- `components/reader/PageSlider.tsx:51-65` — `pendingPage` を 150ms デバウンスして `thumbPage` に反映（`debounceRef` 直管理）

両者ほぼ同型のパターンで、後者は ref ベース・前者は親への callback と少し形は違うが共通化可能。

**改善案**: `frontend/src/hooks/useDebouncedValue.ts` に `useDebouncedValue<T>(value: T, delayMs: number): T` を新設。
- `PdfSearchBar`: `const debouncedText = useDebouncedValue(localText, SEARCH_DEBOUNCE_MS); useEffect(() => onSearchChange(debouncedText), [debouncedText])` に書き換え。
- `PageSlider`: `const thumbPage = useDebouncedValue(pendingPage, 150)` に書き換え、`debounceRef` を撤去。

ただし `PageSlider` は `pendingPage === null` で「ドラッグ中ではない」を表現しているため、`null → number → null` 遷移の挙動を実装時に確認する。挙動が変わるならスコープを `PdfSearchBar` のみに絞る。

**変更対象**:
- 新規: `frontend/src/hooks/useDebouncedValue.ts`
- 修正: `frontend/src/components/reader/PdfSearchBar.tsx` / `frontend/src/components/reader/PageSlider.tsx`

**完了条件**: 上記 2 ファイルから `setTimeout` の直接呼び出しが消える。実機で検索バーのデバウンス挙動・スライダーのサムネイルプレビュー遅延が従来通りに動くこと。`vitest` 全通過。

**実績**: `useDebouncedValue<T>(value, delayMs): T` を新設。`PdfSearchBar` は `const debouncedText = useDebouncedValue(localText, UI_CONFIG.SEARCH_DEBOUNCE_MS)` + 単純な `useEffect` で親通知。`PageSlider` は `const thumbPage = useDebouncedValue(pendingPage, 150)` に置換、`debounceRef` / `setThumbPage` / 旧 `useEffect` を撤去。`<img>` 要素は `isDragging &&` で条件レンダーのため、ドラッグ終了後の余分な fetch は発生しない。

---

### Phase 54-9（**スキップ判断**）— ルーター戻り値の `response_model` 統一

**対象**: `routers/ocr.py` のみが `response_model=StatusResponse` を指定し他 12 ルーターは `-> dict` で返している。

**スキップ理由**: 現状 OpenAPI スキーマを使う運用（自動生成クライアント・ドキュメント公開）がない。揃えるなら全ルーター BaseModel 化（記述量増）か、`ocr.py` も dict 化（スキーマ精度低下）の二択になり、どちらも実害がないまま機械的な書き換えになる。**運用要件が出るまで保留**。

---

### Phase 54-10（**スキップ判断**）— Backend 日時取り扱いポリシー統一

**対象**: `time.time()`（meta.py — Unix 秒永続化）/ `datetime.now(timezone.utc)`（hitomi state_store / monitor — UTC isoformat）/ `datetime.now()`（meta export filename / hitomi.py:130）が混在。

**スキップ理由**: 用途が違うため統一する強い動機がない。
- 永続化される `meta.json` は Unix 秒（後方互換維持・ロケール非依存）
- hitomi の状態ファイルは ISO8601（人間可読・JSON 互換）
- export filename は `YYYYMMDD` でローカル時刻が望ましい

ポリシーを言語化してドキュメント化する余地はあるが、コード書き換えの利得は薄い。**判断保留**。

---

### Phase 54-11（**スキップ判断**）— `app.exception_handler` のファクトリ化

**対象**: `main.py:40-61` の 3 つのカスタム例外ハンドラ（`FileOperationError` / `OcrProcessError` / `AutoFillError`）が同じパターン（`logger.exception → JSONResponse(status, detail)`）。

**スキップ理由**: 関数 3 つを宣言的に並べた方が読みやすく、ファクトリ関数を挟むと「このハンドラはどの例外か」「ステータスコードはどれか」を読み取るのに 2 手かかる。総行数も短く（24 行）、リファクタの利得が記述コストを下回る。**保留・現状維持**。

---

## Phase 53（2026-05-05）— SeriesEditDialog の useDialogSubmit 統一 + P3 候補群

Phase 52 完了後の P3 候補に着手。

### Phase 53-1（**P3**）— `SeriesEditDialog` を `useDialogSubmit` に統一

**問題**: `SeriesEditDialog` は独自の `saving` / `error` useState + `try/catch` を保持（Phase 51-4 で `Rename` / `Merge` を統一済みだが `SeriesEdit` のみ取り残されていた）。

**改善案**: `useDialogSubmit(onClose, '保存に失敗しました。')` を使い、`saving` / `error` state を撤去。`useEffect` 内の `setError(null)` を `setError`（フックが公開）で維持。

**変更対象**: `frontend/src/components/viewer/SeriesEditDialog.tsx`
**完了条件**: `tsc --noEmit` エラーなし、`vitest` 全通過、UI 挙動互換（バリデーションエラー / saving 中の無効化）。

**実績**: 142/142 通過、tsc クリーン。

---

### Phase 53-2（**P3**）— `localeCompare('ja')` を `cmpJa` ユーティリティに集約

**問題**: `localeCompare('ja')` が `useSortedPdfs.ts`（6 箇所）と `useMetaDerived.ts`（5 箇所）の合計 11 箇所に直書き。ロケール文字列が複数ファイルに散在し、変更時の修正漏れリスクがある。

**改善案**: `frontend/src/utils/sort.ts` を新設し `cmpJa(a, b) = a.localeCompare(b, 'ja')` を提供。hook ではなく plain 関数（ライフサイクル不要）として実装。両ファイルで全 11 箇所を置換。

**変更対象**: `frontend/src/hooks/useSortedPdfs.ts` / `frontend/src/hooks/useMetaDerived.ts`
**新規作成**: `frontend/src/utils/sort.ts`
**完了条件**: `tsc --noEmit` エラーなし、`vitest` 全通過。

### Phase 53-3（**P3**）— hooks の bash JSON パースを python3 に統一

**問題**: 3 つの bash hooks（`remind_deps_install.sh` / `remind_tests.sh` / `remind_docs_update.sh`）が `grep -oE / sed -E` で `file_path` を抽出している。Claude Code が hook input を JSON として渡す際、Windows パスの `\` は `\\` にエスケープされるため、grep/sed 抽出後の文字列は `d:\\61.tool\\...` のまま（JSON エスケープ未解除）。`tr '\\' '/'` が各 `\` を個別に `/` に変換するため `d://61.tool//...` のダブルスラッシュになり、`*/backend/pyproject.toml` などの specific パターンにマッチしない（実測で確認済み）。

**改善案**: `file_path` 抽出を `python3 -c "import json,sys; d=json.load(sys.stdin); print(d.get('file_path',''))"` に置換。Python が JSON のアンエスケープ（`\\` → `\`）を行うため、以降の `tr '\\' '/'` は単一 `\` → `/` に正しく変換できる。`remind_tests.sh` は `old_string` / `new_string` の抽出・行数カウントも Python に移行。

**変更対象**: `.claude/hooks/remind_deps_install.sh` / `.claude/hooks/remind_tests.sh` / `.claude/hooks/remind_docs_update.sh`
**完了条件**: Git Bash で `python3` が動作すること（確認済み: Python 3.12.10）。Windows パス `d:\61.tool\...\file.py` が `d:/61.tool/.../file.py` に正規化されること。

**実績**: 3 hooks を更新。`backend/pyproject.toml` / `frontend/package.json` / docs ファイルの skip 判定をテストで確認。

---

### Phase 53（スキップ判断）— 中位ファイル分割

**対象**: `ReaderPanel.tsx` (296 行) / `services/series_resolver.py` (216 行) / `services/pdf_generator.py` (209 行)

**スキップ理由**:
- `ReaderPanel.tsx`: Phase 45 完了時から既に「presentation/container 分割が必要、利得薄い」と記録済み。現在 296 行で変化なし
- `series_resolver.py`: Gemma 補助セクション（52-144 行）が候補だが、全て `_` プレフィックスの内部関数で `run_resolve` からのみ使用。分離後の import 依存追加に比べて利得薄い
- `pdf_generator.py`: `PdfGenerator` クラスが凝集しており Phase 46 で `batch_compress` 分離済み。209 行は許容範囲

---

## Phase 52（2026-05-05）— router 例外ラッパー / useTagsInput / accent token 統一

Phase 51 完了後の P2 候補のうち、互いに独立でリスクの低い 4 件（#2 / #3 / #4 / #5）を一括対応。`#1 ジョブ管理 3 系統統一` は 3 系統が異なるユースケース（汎用ジェネリック / UUID 並行ジョブ / subprocess シングルトン）で、統一すると特殊化フラグが増えるためスキップ判断。

### Phase 52-1（**P2**）— router 例外ハンドラのデコレータ化

**問題**: `ocr.py` / `generate.py` / `library.py` / `pdfs.py` / `thumbnails.py` で `try / except Exception as e: logger.exception(...); raise HTTPException(500)` パターンが計 6 箇所重複。

**改善案**: `routers/_deps.py` に `log_and_raise_500(operation)` デコレータを追加。`HTTPException` は素通し、`Exception` のみログ + 500 変換。logger は `func.__module__` から取得して各 router のロガー設定を継承。

**変更対象**: `backend/routers/_deps.py` / `backend/routers/ocr.py` / `backend/routers/generate.py` / `backend/routers/library.py` / `backend/routers/pdfs.py`
**完了条件**: `pytest -x` 全通過、API レスポンス互換（4xx/5xx の status_code が変わらないこと）。

**実績**: 5 エンドポイントを置換（ocr/run / ocr/stop / batch_compress / list_book_images / delete_pages）、269/269 通過。`thumbnails.py` は formatted log args + 固定 detail で特殊なため対象外。

---

### Phase 52-2（**P2**）— `useTagsInput` hook 化

**問題**: `BulkAuthorDialog` (L45-65) と `BulkTagDialog` (L33-53) で `addTag` / `removeTag` / `handleKeyDown`（Enter, `,`, Backspace）の実装がほぼ同一で再実装。

**改善案**: `frontend/src/hooks/useTagsInput.ts` を新設し、state（`tags` / `input` / `inputRef`）+ ハンドラ（`addTag` / `removeTag` / `handleKeyDown` / `handleBlur`）+ 送信前ヘルパ（`getFinalTags`）+ open リセット（`reset`）を集約。

**変更対象**: `frontend/src/hooks/index.ts` / `BulkAuthorDialog.tsx` / `BulkTagDialog.tsx`
**新規作成**: `frontend/src/hooks/useTagsInput.ts`
**完了条件**: `tsc --noEmit` エラーなし、`vitest` 全通過、UI 挙動互換（タグ chip の表示・キーボード操作・Backspace 削除・blur 時確定・空送信時の解除）。

**実績**: hook 65 行で新設、両 Dialog で利用、142/142 通過、tsc クリーン。

---

### Phase 52-3（**P2**）— `useRadioMode<T>` 化（**スキップ判断**）

**問題提起**: `BulkAuthorDialog` / `BulkSeriesAssignDialog` / `SeriesEditDialog` で `useState<Mode> + useEffect(reset on open)` のパターンが共通。

**スキップ理由**: 実装試行の結果、各 Dialog の `useEffect` 内で行う state リセット内容が dialog ごとに大きく異なる（mode 以外の selectedExisting / selectedTitle / newTitle / indexStr / current / allSeries 等が混在）ため、純粋な `useRadioMode` だけ切り出しても 2〜3 行しか節約できず、利用側の `useEffect` をもう一段書く必要がある。抽象化コスト > 利得と判断し未実装。

**今後**: もし「open 時 reset を共通化したい」需要が他箇所でも増えてきた場合は `useResetOnOpen(open, () => {...}, deps)` という別形で再検討する。

---

### Phase 52-4（**P2**）— accent カラーの token 統一

**問題**: `accent-indigo-600` / `accent-purple-600` / `accent-red-600` の直書きが 10 箇所に分散。Phase D で primary / accent / success token を導入済みだが、フォーム要素の `accent-color` ユーティリティだけ更新漏れ。

**改善案**: 意味的役割ごとに token に置換:
- `accent-indigo-600` → `accent-primary-600`（4 箇所: BulkGenreDialog x2 / AutoFillAuthorsBar / PageSlider）
- `accent-purple-600` → `accent-accent-600`（5 箇所: SeriesEditDialog x2 / BulkSeriesAssignDialog x2 / SeriesResolveBar）
- `accent-red-600`（1 箇所: SeriesEditDialog の「シリーズから外す」）は破壊的アクションの semantic として残置（`danger` token 未定義、追加は dev server 再起動コスト > 利得のため不採用）

**変更対象**: `BulkGenreDialog.tsx` / `AutoFillAuthorsBar.tsx` / `PageSlider.tsx` / `SeriesEditDialog.tsx` / `BulkSeriesAssignDialog.tsx` / `SeriesResolveBar.tsx`
**完了条件**: `tsc --noEmit` エラーなし、`vitest` 全通過、見た目変化なし（primary 600 = #4f46e5 = indigo-600、accent 600 = #9333ea = purple-600 で同色）。

**実績**: 9 箇所置換、テスト通過。`accent-red-600` 1 箇所のみ意図的に残置。

---

## Phase 51（2026-05-05）— ロック共通化 + Dialog フォーカス/送信パターン統一

3 並列 Explore agent による backend/frontend/Claude 設定の包括スキャンで抽出された P1 候補 4 件を一括対応。

### Phase 51-1（**P1**）— ロック機構を `utils/locks.py` に共通化

**問題**: `services/meta_store.py` の `get_lock` と `services/genre_store.py` の `_get_lock` が完全に同一パターンの再実装（dict + outer mutex で `threading.Lock` を遅延生成）。

**改善案**: `backend/utils/locks.py` に `SourceLockManager` クラスを新設。各ストアはインスタンスを保持して `get(source)` でロックを得る。`meta_store.get_lock` は外部 import 互換のため関数として残し、内部で `SourceLockManager` に委譲。

**変更対象**: `backend/services/meta_store.py` / `backend/services/genre_store.py`
**新規作成**: `backend/utils/locks.py`
**完了条件**: `pytest -x` 全通過、外部 import (`from services.meta_store import get_lock`) が壊れないこと。

**実績**: 269/269 通過。`SourceLockManager` 17 行、各ストアは `_lock_manager = SourceLockManager()` の 1 行 + 既存ヘルパで完結。

---

### Phase 51-2（**P1**）— `genre_store` の load/save ロック整合

**問題**: `load_genres` はロック取得なしで読み取り、未作成時のみ `save_genres` を呼んで保存する。読み取り中に他スレッドの書き込みが入ると壊れた JSON を読む可能性がある。

**改善案**: `load_genres` 全体をロック取得下に変更。未作成時の派生 → 書き込みも同一ロック内で完結させるため、`_write_genres_unlocked` を private ヘルパーに分離し、`save_genres` から再利用する。

**変更対象**: `backend/services/genre_store.py`
**完了条件**: `pytest -x` 全通過、`/api/genres` の挙動互換。

**実績**: テスト全通過。

---

### Phase 51-3（**P1**）— `useAutoFocusInput` hook 化

**問題**: `RenameDialog`（`setTimeout(50) + focus + select`）/ `MergeDialog`（`setTimeout(0) + focus`）/ `BulkGenreDialog` / `BulkAuthorDialog` で開時の input 自動フォーカス処理が再実装。delay 値（0/50）と `select()` 有無も不統一。

**改善案**: `frontend/src/hooks/useAutoFocusInput.ts` を新設し、`useAutoFocusInput(ref, shouldFocus, { delay?, select? })` の単一 API でフォーカス制御を集約。

**変更対象**: `frontend/src/hooks/index.ts`
**新規作成**: `frontend/src/hooks/useAutoFocusInput.ts`
**完了条件**: `tsc --noEmit` エラーなし、`vitest` 全通過。

**実績**: hook 36 行で新設。`RenameDialog` / `MergeDialog` で利用（51-4 と同時実施）。`BulkGenre` / `BulkAuthor` は radio mode 切替時のフォーカスが絡むため別パターンとして据え置き。

---

### Phase 51-4（**P1**）— `Rename` / `MergeDialog` を `useDialogSubmit` に統一

**問題**: `Bulk*` 系 Dialog は `useDialogSubmit`（saving / error / 非同期 submit を共通化）を採用済みだが、`RenameDialog` / `MergeDialog` は独自の `loading` + `error` state + `try/catch` を保持しており不統一。

**改善案**: 両 Dialog を `useDialogSubmit(onClose, fallbackErrorMsg)` に置換。バリデーション通過後 `handleSubmit(action)` に submit を渡すだけにし、saving 中の Cancel/Primary 制御も他 Dialog と揃える。

**変更対象**: `frontend/src/components/viewer/RenameDialog.tsx` / `frontend/src/components/viewer/MergeDialog.tsx`
**完了条件**: `tsc --noEmit` エラーなし、`vitest` 全通過、UI 上の挙動互換（バリデーションエラー表示・Enter 送信・送信中無効化）。

**実績**: 142/142 通過、tsc クリーン。両 Dialog で `useDialogSubmit` + `useAutoFocusInput` 構成に統一、useEffect は state リセットのみに縮約。

---

## Phase 49〜50（2026-05-05）— 再スキャンによる再膨張ファイル分割

`/big-files` 再スキャンの結果、Phase 42 で 150 行目標まで圧縮した `author_resolver.py` が DLsite/FANZA 直接クエリ追加で **267 行に再膨張**、Phase 17/44 で分離済みの `LibraryPanel.tsx` も ScrollMemory・キーボード処理・SeriesEdit ハンドラの蓄積で **375 行**に到達していた。両ファイルを再分離する。

### Phase 49（2026-05-05）— `author_resolver.py` の再分離（**P1**）

**問題**: 267 行。Phase 42 完了時 150 行目標に対し、Phase 25 以降の DLsite/FANZA 直接 HTTP クエリ・サニタイズ拡張（数値 sentinel 対応・URL エンコード等）が累積し再膨張。各ステップの純関数群と orchestration が同居している。

**改善案**: ステップ実装と判定ヘルパー・定数を `services/author_steps.py` に分離。`author_resolver.py` は `resolve_author` / `resolve_author_debug` の orchestration のみに圧縮。テストの `monkeypatch.setattr(ar, "_try_dlsite", ...)` 互換のため、ステップ関数を `author_resolver` でも re-export する。

| Sub | 内容 | 優先度 |
|---|---|---|
| 49-1 | `services/author_steps.py` を新設し、ステップ純関数群（`_http_step` / `_searxng_filter_step` / `_searxng_generic_step` / `_try_dlsite` / `_try_fanza` / `_try_direct_sites`）と判定ヘルパー（`_sanitize_author` / `_extract_circle_from_page`）と定数を移動 | **P1** |
| 49-2 | `author_resolver.py` を orchestration（`resolve_author` / `resolve_author_debug`）のみに圧縮し、ステップ関数を re-export | **P1** |

**変更対象**: `backend/services/author_resolver.py`
**新規作成**: `backend/services/author_steps.py`
**完了条件**: `author_resolver.py` 100 行以下。`tests/test_author_resolver.py` 全通過（monkeypatch 互換）。`/meta/auto-fill` 系の挙動・レスポンス形式は互換維持。

**実績**: `author_resolver.py` 267→97 行 / `author_steps.py` 187 行（新設）。pytest 33/33 通過。

---

### Phase 50（2026-05-05）— `LibraryPanel.tsx` の最終分離（**P1**）

**問題**: 375 行。Phase 17（LibraryDialogs 抽出）・Phase 44（useDialogToggles 化）後も、以下のロジックが本体に蓄積している:
- ScrollMemory（URL キーごとのスクロール位置保存・復元、ref + 2 effect、約 30 行）
- s キーで選択モードトグルする keyboard listener（約 15 行）
- SeriesEdit ダイアログの state + assign/unassign ハンドラ（約 20 行）

**改善案**: 3 つの責務を独立 hook に切り出す。

| Sub | 内容 | 優先度 |
|---|---|---|
| 50-1 | `hooks/useScrollMemory.ts` を新設し、URL キー単位のスクロール保存・復元ロジックを集約 | **P1** |
| 50-2 | `hooks/useLibrarySelectionShortcut.ts` を新設し、s キーの選択モードトグルを集約 | **P1** |
| 50-3 | `hooks/useSeriesEditDialog.ts` を新設し、`seriesEditTarget` state + `handleSeriesAssign` / `handleSeriesUnassign` を集約 | **P1** |
| 50-4 | `LibraryPanel.tsx` を 3 hook 利用に書き換え | **P1** |

**変更対象**: `frontend/src/components/viewer/LibraryPanel.tsx` / `frontend/src/hooks/index.ts`
**新規作成**: `frontend/src/hooks/useScrollMemory.ts` / `useLibrarySelectionShortcut.ts` / `useSeriesEditDialog.ts`
**完了条件**: `LibraryPanel.tsx` 320 行程度に圧縮。`npx tsc --noEmit` エラーなし。`vitest` 全通過。挙動変更なし。

**実績**: `LibraryPanel.tsx` 375→322 行。新設 hook: `useScrollMemory` (38) / `useLibrarySelectionShortcut` (29) / `useSeriesEditDialog` (54)。tsc 通過、vitest 142/142 通過。JSX 比重が 125 行あるためこれ以上の圧縮は presentation/container 分割が必要だが利得が薄く保留。

---

## Phase 39〜48（2026-05-04）— `/big-files` 再スキャンによる一括実施

`/big-files` 再スキャンとコード調査により判明した候補を Phase 39〜48 として 2026-05-04 に一括実施。

### Phase 39（2026-05-04）— `import threading` 漏れの緊急バグ修正（**P1**）

**問題**: `services/series_resolver.py:344` と `services/auto_fill_service.py:123` で `threading.Thread(...)` を使用しているが、両ファイル冒頭に `import threading` が無い。
両 `start_*_job()` を呼ぶと `NameError: name 'threading' is not defined` で落ちる致命的バグ。

**改善案**: 両ファイルに `import threading` を追加。`series_resolver.py` 側で残っていた未使用 import (`field` / `VALID_SOURCES`) も同時に除去。

| Sub | 内容 | 優先度 |
|---|---|---|
| 39-1 | `series_resolver.py` / `auto_fill_service.py` に `import threading` を追加 | **P1** |
| 39-2 | `series_resolver.py` の未使用 import (`field` / `VALID_SOURCES`) を除去 | P3 |

**変更対象**: `backend/services/series_resolver.py` / `backend/services/auto_fill_service.py`
**完了条件**: `from services.series_resolver import start_resolve_job; start_resolve_job(...)` が NameError を出さずに動作する。

---

### Phase 40（2026-05-04）— `series_resolver.py` の責務分離（P2）

**問題**: 345 行。純粋な判定ロジック（`_collect_books` / `_group_by_authors` / `_detect_series_in_group` / `_common_prefix` / `_authors_key` / `_stable_series_id` / `_trim_prefix` / `_collect_series_members`）と、ジョブ実行（`run_resolve` / `_augment_with_gemma` / `_assign_book_to_series` / `start_resolve_job` / `get_state` / `reset_state`）が同一ファイルに同居。

**改善案**: 純関数群を `services/series_detector.py` に分離。`series_resolver.py` はジョブ実行系のみ（180 行程度）。`routers/series.py` の `_stable_series_id` 直接 import は detector 経由に変更。

| Sub | 内容 | 優先度 |
|---|---|---|
| 40-1 | `services/series_detector.py` を新設し純関数群を移動 | P2 |
| 40-2 | `routers/series.py` の `_stable_series_id` import を detector 経由に変更 | P2 |

**変更対象**: `backend/services/series_resolver.py` / `backend/routers/series.py`
**新規作成**: `backend/services/series_detector.py`
**完了条件**: `series_resolver.py` 180 行以下。既存テスト全通過。

---

### Phase 41（2026-05-04）— `routers/meta.py` のメタフィールドマージ共通化（P2）

**問題**: 226 行。`update_meta` ハンドラ内で `_merge_list_field` / `_merge_hidden` / `_merge_genre` の 3 ヘルパーと 30 行の `_apply` クロージャを定義しており、ボディが膨れている。同じ「フィールド指定 → エントリのマージ」パターンは他の箇所でも繰り返し出現する可能性。

**改善案**: `services/meta_store.py` に `merge_entry_fields(entry, fields)` ヘルパーを追加し、`update_meta` を 30 行程度に圧縮。

```python
# meta_store.py に追加
def merge_entry_fields(
    entry: dict,
    *,
    authors: list[str] | None = None,
    tags: list[str] | None = None,
    hidden: bool | None = None,
    genre: str | None = None,
) -> dict:
    """部分的に指定されたフィールドだけを上書きしたエントリを返す。
    `authors` / `tags` は list（空可）、`hidden` は True なら設定 / False なら削除、
    `genre` は空文字なら削除、文字列なら設定。指定外は変更しない。
    """
```

| Sub | 内容 | 優先度 |
|---|---|---|
| 41-1 | `meta_store.py` に `merge_entry_fields` を追加 | P2 |
| 41-2 | `routers/meta.py` の `update_meta` を `merge_entry_fields` 経由に書き換え | P2 |

**変更対象**: `backend/services/meta_store.py` / `backend/routers/meta.py`
**完了条件**: `meta.py` 130 行以下。`PATCH /meta` の挙動・レスポンスは互換維持。

---

### Phase 42（2026-05-04）— `author_resolver.py` の resolve / debug 重複統合（P2）

**問題**: 224 行。`resolve_author` (40 行) と `resolve_author_debug` (66 行) で同じ 3〜4 ステップ（DLsite / FANZA / SearXNG site filter / SearXNG generic）を別実装している。新ステップ追加時は両方に追記が必要で、手順がずれるリスクがある。

**改善案**: 各ステップを「結果 + デバッグ情報」を返す pure 関数に正規化:

```python
@dataclass
class StepResult:
    name: str           # 'dlsite' / 'fanza' / 'searxng_site_filter' / 'searxng_generic'
    final: str          # サニタイズ後の最終値（'' なら次ステップへフォールバック）
    debug: dict         # url / fetched / raw / snippets など中間情報
```

`resolve_author` は最初に `final != ''` の step を返す薄いラッパー、`resolve_author_debug` は全 step の `debug` を集約した dict を返すラッパー。

| Sub | 内容 | 優先度 |
|---|---|---|
| 42-1 | `_step_*` pure 関数群を抽出（StepResult 返却） | P2 |
| 42-2 | `resolve_author` / `resolve_author_debug` をラッパーに圧縮 | P2 |

**変更対象**: `backend/services/author_resolver.py`
**完了条件**: 224 → 150 行程度。`tests/test_author_resolver.py` 全通過。`/meta/auto-fill/test` のレスポンス形式は互換維持。

---

### Phase 43（2026-05-04）— `useBookMeta.ts` の責務分離（P2）

**問題**: 302 行。CRUD（updateMeta / updateAuthors / updateTags / updateGenre / setHidden）、シリーズ手動編集（assignSeries / unassignSeries / reorderSeries 楽観的更新）、閲覧履歴（recordView）、getter 群が同一フックに同居。

**改善案**: 責務別 3 フックに分離し、`useBookMeta` は薄い合成にする:

```
hooks/useBookMetaCore.ts   ← meta state + getters
hooks/useBookMetaWrite.ts  ← updateMeta / updateAuthors / updateTags / updateGenre / setHidden
hooks/useBookSeries.ts     ← assignSeries / unassignSeries / reorderSeries
hooks/useBookView.ts       ← recordView
hooks/useBookMeta.ts       ← 上記を合成して既存 API を維持（呼び出し側無変更）
```

| Sub | 内容 | 優先度 |
|---|---|---|
| 43-1 | `useBookMetaCore` / `useBookMetaWrite` / `useBookSeries` / `useBookView` を新設 | P2 |
| 43-2 | `useBookMeta` を 4 フックの合成に書き換え（既存 API は維持） | P2 |

**変更対象**: `frontend/src/hooks/useBookMeta.ts`
**新規作成**: `useBookMetaCore.ts` / `useBookMetaWrite.ts` / `useBookSeries.ts` / `useBookView.ts`
**完了条件**: 各フック 100 行以下、`useBookMeta` 50 行以下。既存の vitest が全通過（呼び出し側 API 不変）。

---

### Phase 44（2026-05-04）— `LibraryPanel.tsx` ダイアログ open state 集約（P3）

**問題**: 5 つの `useState<boolean>(false)`（BulkAuthor / BulkTag / Merge / BulkSeries / BulkGenre）が並び、`isXxxOpen` / `setIsXxxOpen(true)` / `() => setIsXxxOpen(false)` の同形パターンが props として 15 個ほど散らばる。

**改善案**: `useDialogToggles<DialogKey>()` フックを新設して集約。

```ts
// hooks/useDialogToggles.ts
export function useDialogToggles<K extends string>() {
    const [open, setOpen] = useState<K | null>(null);
    return {
        isOpen: (k: K) => open === k,
        open: (k: K) => setOpen(k),
        close: () => setOpen(null),
    };
}
```

**変更対象**: `frontend/src/components/viewer/LibraryPanel.tsx` / `LibraryDialogs.tsx`
**新規作成**: `frontend/src/hooks/useDialogToggles.ts`

---

### Phase 45（2026-05-04）— `ReaderPanel.tsx` UI state 集約（P3）

**問題**: 301 行。7 個の `useState`（numPages / showHeader / showSlider / pdfVersion / isSearchOpen / isHelpOpen / direction）が散在。
- `showHeader` / `showSlider` / `isSearchOpen` / `isHelpOpen` は UI トグル系
- `numPages` / `pdfVersion` は PDF ドキュメント側状態

**改善案**: 2 フックに分離:
- `hooks/useReaderUIState.ts` — showHeader / showSlider / isSearchOpen / isHelpOpen をまとめた UI state
- `hooks/usePdfDocumentState.ts` — numPages / pdfVersion / onDocumentLoadSuccess を集約

`direction` はリーダー本体の本質的状態なのでそのまま残す。

**変更対象**: `frontend/src/components/viewer/ReaderPanel.tsx`
**新規作成**: `frontend/src/hooks/useReaderUIState.ts` / `frontend/src/hooks/usePdfDocumentState.ts`
**完了条件**: ReaderPanel.tsx 240 行以下。挙動変更なし。

---

### Phase 46（2026-05-04）— `pdf_generator.py` 責務分離（P3）

**問題**: 281 行。`PdfGenerator` クラス（生成ジョブ・移動・ロールバック）と、module-level の `batch_compress`（独立処理）、`generate_thumbnail`（独立処理）が同居。

**改善案**:
- `services/batch_compressor.py` に `batch_compress` + `_write_compressed_pdf` を移動
- `services/thumbnail.py` に `generate_thumbnail` を移動

**変更対象**: `backend/services/pdf_generator.py` / `backend/routers/generate.py`
**新規作成**: `backend/services/batch_compressor.py`
**完了条件**: `pdf_generator.py` 200 行以下。`POST /api/batch_compress` 挙動互換。

---

### Phase 47（2026-05-04）— `validate_safe_*` 重複圧縮（P3）

**問題**: 5 ルーター（pdfs / series / meta / library / thumbnails）で以下の 2 行パターンが繰り返されている:

```python
validate_safe_path(request.path, param_name="path")
for name in request.names:
    validate_safe_name(name, param_name="name")
```

**改善案**: `routers/_deps.py` に `validate_request_targets(path, names)` を追加して 1 行化。

**変更対象**: `backend/routers/_deps.py` / `pdfs.py` / `series.py` / `meta.py` / `library.py` / `thumbnails.py`

---

### Phase 48（2026-05-04）— フロントエンド try/catch + showToast パターンの集約（P3）

**問題**: 3 ファイル（HitomiPage / LibraryPanel / useLibraryBulkActions）で 11 箇所、以下の定型ボイラープレートが繰り返されている:

```ts
try {
    await someAsyncFn();
} catch (e: unknown) {
    showToast(e instanceof Error ? e.message : 'XXX に失敗しました。', 'error');
}
```

**改善案**: `hooks/useAsyncToast.ts` に `runAsync(fn, fallbackMessage)` を追加。

**変更対象**: `frontend/src/pages/HitomiPage.tsx` / `frontend/src/components/viewer/LibraryPanel.tsx` / `frontend/src/hooks/useLibraryBulkActions.ts`
**新規作成**: `frontend/src/hooks/useAsyncToast.ts`

---

## Phase 24〜38（2026-05-01）— Phase 5 派生整理 + 共通化フェーズ

### Phase 24 — useBookMeta.ts の派生集計 useMemo 化・抽出

`useMetaDerived(meta)` フックを新設し、毎レンダー再計算だった 5 つの派生集計を `useMemo` でキャッシュ。`useBookMeta.ts` 354→302 行（-52 行）。

### Phase 25 — author_resolver.py の DLsite/FANZA 抽象化

`_try_dlsite` / `_try_fanza` → `_try_direct_http_search(url_template, ...)` に統合。`_DIRECT_HTTP_SITES` リストで宣言し `resolve_author` / `resolve_author_debug` でループ処理。253→224 行（-29 行）。

### Phase 26 — pdf_generator.py JPEG 圧縮ロジック共通化

`_write_compressed_pdf` モジュール関数を新設。`batch_compress` の重複ループを 1 行に置換、`_create_pdf_file` の dead `quality` 引数を除去。289→281 行（-8 行）。

### Phase 27 — series_resolver.py `_augment_with_gemma` ヘルパー抽出

`_collect_series_members(meta)` 純関数と `_assign_book_to_series(...)` を新設し `_augment_with_gemma` 本体の重複ロジックを置換。

### Phase 28 — LibraryHeader.tsx 3 段分割

`LibraryNavBar` / `LibraryFilterBar` / `LibraryBulkActionBar` を `frontend/src/components/reader/` に新設。`LibraryBreadcrumb` 型を `LibraryNavBar.tsx` に移動し re-export。`LibraryHeader.tsx` 286→132 行。

### Phase 29 — ReaderPanel.tsx 周辺要素切り出し

`EdgeHoverZones` / `NextVolumeBanner` を `frontend/src/components/reader/` に新設。`ReaderPanel.tsx` 311→301 行。

### Phase 30 — VALID_SOURCES 定義の一元化 + Depends 検証

`config.py` に `VALID_SOURCES` を移動。`routers/_deps.py` に `validated_source` 依存を新設。クエリパラメーター `source` 検証を 8 エンドポイントで `Depends(validated_source)` に置換。

### Phase 31 — PdfCard.tsx サブコンポーネント分割

`PdfCardThumbnail` / `PdfCardActionButtons` を `frontend/src/components/reader/` に新設。`PdfCard.tsx` 220→145 行。

### Phase 32 — TagsInput 共通 UI コンポーネント抽出

`frontend/src/components/ui/TagsInput.tsx` を新設（chipColor: `'blue' | 'emerald'` 対応）。`BulkAuthorDialog` / `BulkTagDialog` の chip 入力 UI（各 ~50 行）を `<TagsInput>` 利用に置換。

---

### Phase 33 — `types/index.ts` 未使用型の削除（P2）

**問題**: 203 行。Grep 調査の結果、フロントエンド内で **一度も import されていない型が 7 件** 存在する。

| 削除候補 | 行 | 用途（現状） |
|---|---|---|
| `PdfListResponse` | 13 | API レスポンス型。フロント側は `unknown` で受けている |
| `GenerateRequest` | 37 | apiClient.post の inline object で代替 |
| `GenerateResponse` | 45 | `GenerateJob` で代替済み |
| `BatchCompressRequest` | 53 | apiClient.post の inline object で代替 |
| `CreateDirectoryRequest` | 105 | 同上 |
| `MoveItemsRequest` | 111 | 同上 |
| `UpdateMetaRequest` | 168 | 同上 |

**改善案**: 上記 7 型を削除。`types/index.ts` 203→160 行程度に圧縮。`utils/validation.ts:9` の `FORBIDDEN_FILENAME_CHARS` の `export` も外部利用なしなのでローカル定数化。

**変更対象**: `frontend/src/types/index.ts` / `frontend/src/utils/validation.ts`

---

### Phase 34 — useAutoFillAuthors / useSeriesResolve の共通化（P2）

**問題**: `useAutoFillAuthors`（87 行）と `useSeriesResolve`（92 行）が **ほぼ同一構造**。違いはエンドポイントとステータス型のみ。

**改善案**: ジェネリックフック `useJobPolling<TStatus>` を新設し、両フックは薄いラッパーに圧縮。

**変更対象**: `frontend/src/hooks/useAutoFillAuthors.ts` / `frontend/src/hooks/useSeriesResolve.ts`
**新規作成**: `frontend/src/hooks/useJobPolling.ts`
**完了条件**: 両フックを 40 行以下に圧縮。挙動変更なし。

---

### Phase 35 — backend ジョブ状態管理パターンの共通化（P3）

**問題**: `auto_fill_service.py` と `series_resolver.py` で、ソース別ジョブ状態を管理する以下のパターンが完全に重複している（dict + lock + get/reset の同型）。

**改善案**: ジェネリッククラス `JobStateManager[T]` を `services/job_state.py` に新設し、両サービスから利用。

**変更対象**: `backend/services/auto_fill_service.py` / `backend/services/series_resolver.py`
**新規作成**: `backend/services/job_state.py`

---

### Phase 36 — body source 検証の共通ヘルパー（P3）

**問題**: Phase 30 でクエリパラメーター `source` 検証は `Depends(validated_source)` に統一したが、**リクエストボディ内の `source` フィールド検証は依然 7 箇所で重複**（series.py 3 / meta.py 2 / genres.py 2）。

**改善案**: `routers/_deps.py` に `assert_valid_source(source)` を追加し 1 行化。

**変更対象**: `backend/routers/_deps.py` / `backend/routers/series.py` / `backend/routers/meta.py` / `backend/routers/genres.py`

---

### Phase 37 — `routers/pdfs.py` の関心分離（P3）

**問題**: `routers/pdfs.py`（249 行）に PDF 生成ジョブ・ステータス確認・ページ削除・一括圧縮・PDF 結合の 5 関心が同居。

**改善案**: `generate` 系を `routers/generate.py` に分離。`routers/pdfs.py` は PDF 操作系のみ（80 行程度）。

**変更対象**: `backend/routers/pdfs.py` / `backend/main.py`
**新規作成**: `backend/routers/generate.py`

---

### Phase 38 — Bulk Dialog 共通サブミットフック（P3）

**問題**: 4 つの Bulk* ダイアログで `useState<saving>` + `useState<error>` + `try/catch` のサブミットボイラープレートが各 ~12 行重複（合計 ~50 行）。

**改善案**: `useDialogSubmit({ onSubmit, errorMessage, onClose })` フックを新設して saving/error 管理を共通化。各ダイアログは `submit(authors)` のように呼ぶだけ。

**変更対象**: `frontend/src/components/viewer/BulkAuthorDialog.tsx` / `BulkTagDialog.tsx` / `BulkGenreDialog.tsx` / `BulkSeriesAssignDialog.tsx`
**新規作成**: `frontend/src/hooks/useDialogSubmit.ts`

---

## Phase 18〜23（2026-04-19〜2026-05-01）— Phase 5 派生整理〜LibraryPanel 再分割

### Phase 23（2026-05-01）— LibraryPanel.tsx 再分割

Phase 20 で 470→200 行に分離した `LibraryPanel.tsx` が 413 行に再膨張したため、2 フックを抽出して 341 行に圧縮。`SeriesEditDialog` の inline async ハンドラも `useCallback` へ引き上げ。

| Sub | 内容 | 優先度 |
|---|---|---|
| 23-1 | `usePinnedBookSets` 新設（`pinnedBooks` / `contextualFavorites` の Set 計算を抽出） | **P1** |
| 23-2 | `useSeriesAuthorFilter` 新設（`isMixedAuthors` / `seriesEditFilteredSeries` / `bulkSeriesFiltered` を抽出） | **P1** |

### Phase 22（2026-04-29 / `e7084e7`）— Gemma ツールのインポート共通化

`author_resolver._ensure_tools` と `series_resolver._ensure_ollama_client` で重複していた Gemma 4 ツールのインポート処理（`sys.path` 設定 + `sys.modules['config']` 退避/復元）を `services/gemma_client.py` に抽出。重複コード ~40 行を 1 ヶ所に集約。

### Phase 21（2026-04-29 / `7fdcb22`）— series_resolver から volume_parser を抽出

`series_resolver.py`（431 行）から純粋関数の巻数パーサーを `volume_parser.py` に抽出。回帰防止のため境界ケーステストも追加。結果: 431 行 → 約 350 行。

### Phase 20（2026-04-29 / `c23c6a9`）— LibraryPanel.tsx をフック 4 つに分離

`LibraryPanel.tsx`（470 行）を責務ごとに 4 フックへ分離（`useUrlFilters` / `useLibrarySettings` / `useLibraryBulkActions` / `useLibraryDisplay`）。結果: 470 行 → 約 200 行（JSX 中心の合成のみ）。

### Phase 19（2026-04-29 / `cdc55d1`）— pdfs/ 廃止後の dead code 一掃

`pdfs/` 廃止 + `generate_compressed`/`quality` 削除（コミット `dc21e97` / `7824083`）の後始末として、不要になった定数・パラメータ・メソッドを除去。副次的に `tests/test_img2pdf.py`（pytest 形式でない debug スクリプト）も削除。

### Phase 18（2026-04-29 / `8d689e8`）— Phase 5 派生の技術負債解消

Phase 5-A / 5-B（一括シリーズ登録 / DnD 並べ替え / 一括操作後の自動選択解除 / シリーズ「次の巻へ」ボタン）の実装で生じた技術負債とテスト不足を整理。

| Sub | 内容 | 優先度 |
|---|---|---|
| 18-1 | `useBookMeta.reorderSeries` の楽観的更新+ロールバック / 採番プレビュー / 配列 index フロント経路のテスト追加 | **P1** |
| 18-2 | `PdfGrid.tsx` を `PdfCard` / `SortablePdfCard` / `PdfGrid` に分割 | P2 |
| 18-3 | `ReaderPanel` の next-volume 線形探索を `useNextSeriesVolume` フックに切り出し | P2 |
| 18-4 | `LibraryPanel.bulkSeriesExisting` 計算を `useBookMeta.allSeriesWithStats` へ移管 | P3 |
| 18-5 | `ExistingSeriesOption` 型を `types/index.ts` に集約 | P3 |
| 18-6 | DnD ハンドルを `top-2 right-2` に移動（お気に入り星との位置競合解消） | P3 |
| 18-7 | `floating-action: 30` z-index 階層を新設 | P3 |

詳細プラン: 末尾「旧プランニング詳細」 § Phase 18 を参照。

---

## Phase 13〜17（2026-04-27 / `a79d8c9`）— フロントエンド整理 + バックエンド整理 + テスト追加

5 系統 15 項目をまとめて実施。

| Sub | 内容 | 優先度 |
|---|---|---|
| 13-1 | ダイアログ共通シェル `<Dialog>` 導入（5 ダイアログのオーバーレイ重複解消） | **P1** |
| 13-2 | ファイル名バリデーションのユーティリティ化 | P2 |
| 13-3 | `MoveDialog` の `fetch` 直接呼び出しを `apiClient` に統一 | P2 |
| 14-1 | `LibraryPanel` の auto-fill バー切り出し | P2 |
| 14-2 | `GeneratorPage.tsx` の表示ロジック分離（287 行） | P2 |
| 14-3 | `ReaderPanel` の編集モード/見開きモードのフック化 | P3 |
| 15-1 | `routers/library.py` のルーティング分割（301 行） | P2 |
| 15-2 | `file_manager.py` の `is_pdf_file` 取りこぼし修正 | P3 |
| 15-3 | `batch_compress` を services 側へ移動（private API 直叩き解消） | P2 |
| 15-4 | `auto_fill_service` の `view_count`/`last_viewed_at` 消失バグ修正 | **P1** |
| 15-5 | `routers/pdfs.py` の二重ジョブ状態管理（`job_store` + `generate_state`）統合 | P3 |
| 16-1 | バックエンドテスト不足箇所の補完（pdf_generator / auto_fill_service） | P2 |
| 16-2 | `useLibraryFilter` のテスト追加 | P3 |
| 17-1 | ダイアログ z-index 階層を constants 化 | P3 |
| 17-2 | `ReaderPanel` の `confirm()` を `ConfirmDialog` に置換 | P3 |

詳細プラン: 末尾「旧プランニング詳細」 § Phase 13 〜 § Phase 17 を参照。

---

## Phase 10〜12（2026-04-26 / `deeb28b`）— 重複コード整理 + フロントエンド分割 + 定数化

| Sub | 内容 | 優先度 |
|---|---|---|
| 10-1 | ファイル拡張子フィルタリングの関数化（`is_pdf_file` / `is_webp_file` / `is_zip_file` / `is_image_file`） | P1 |
| 10-2 | meta.json ロック→ロード→保存パターン統一（`update_meta_locked`） | P1 |
| 10-3 | `MetaDict` 型定義追加 | P2 |
| 11-1 | `ReaderPanel.tsx` の分割（352 行） | P2 |
| 11-2 | `LibraryHeader.tsx` の分割（205 行） | P2 |
| 11-3 | `LibraryPanel.tsx` のフィルタロジック切り出し | P2 |
| 12-1 | フロントエンドのマジックナンバー定数化 | P3 |
| 12-2 | バックエンドのマジックナンバー定数化 | P3 |
| 12-3 | `alert()` 廃止・トースト通知導入 | P3 |

---

## Phase 4〜9（2026-04-19 / `acda380`）— エラーハンドリング統一 + AutoFillService 抽出 + テスト基盤

| Sub | 内容 |
|---|---|
| 4 | エラーハンドリング統一（`logger.exception` / FastAPI exception_handler） |
| 5 | `ViewerPage` Props Drilling 解消（`LibraryContext.tsx` 導入） |
| 6 | `pdf_generator.py` リファクタリング（`_collect_images` 共通化） |
| 7 | `AutoFillService` 抽出（`auto_fill_service.py` / `meta_store.py` 新規作成） |
| 8 | 環境変数化（`GEMMA_TOOL_DIR` を `.env` で上書き可能に） |
| 9 | テスト追加（`test_path_utils` / `test_meta` / `test_file_manager`） |

---

## Phase 1〜3（2026-04-19 / `538d9de`）— 定数整備 + 責務分離 + フロントエンドコンポーネント分割

| Sub | 内容 |
|---|---|
| 1 | 定数・ユーティリティ整備（`config.py` / `file_naming.py` / `constants.ts` / `storage.ts`） |
| 2 | バックエンド責務分離（`job_manager.py` / `file_manager.py` / `exceptions.py`） |
| 3 | フロントエンド責務分離（`useGenerateJob` / `LibraryDialogs` / `EditModePanel`） |

---

## 旧プランニング詳細（折りたたみ）

これより下は **完了済み Phase 13〜18 の元の計画書**（背景・設計案）です。実装は完了しており、現状の動作はソースコードと変更履歴 (`docs/05_記録/変更履歴.md`) を参照してください。

> **Phase 1-12 の旧プランニング詳細は履歴サイズ削減のため削除しました。** 当時の設計意図はコミットメッセージ（`538d9de` / `acda380` / `deeb28b`）と `docs/05_記録/変更履歴.md` に記録されています。

<details>
<summary><b>Phase 13 — フロントエンドダイアログ層の整理</b></summary>

### 13-1. ダイアログ共通シェル `<Dialog>` の導入（P1）

**問題**: 5つのダイアログで完全に同じシェル構造が重複し、z-index も統一されていない。

| ファイル | z-index | 確認位置 |
|---|---|---|
| `BulkAuthorDialog.tsx` | `z-[200]` | 76行 |
| `CreateFolderDialog.tsx` | `z-[200]` | 70行 |
| `RenameDialog.tsx` | `z-[200]` | 77行 |
| `MoveDialog.tsx` | `z-[100]` | 80行 |
| `MergeDialog.tsx` | `z-50` | 62行 |

各ダイアログで以下が重複している:
- `fixed inset-0 bg-black/50 ... flex items-center justify-center` のオーバーレイ
- `onClick={(e) => { if (e.target === e.currentTarget) onClose(); }}` の外クリック閉じ
- `useRef` での autofocus
- `Escape` キー押下で閉じる処理
- ヘッダー（タイトル + X ボタン）と フッター（キャンセル + 確定ボタン）のレイアウト

**改善案**: `frontend/src/components/ui/Dialog.tsx` を新設して共通化する。

```tsx
// 利用イメージ
<Dialog open={open} onClose={onClose} title="フォルダを作成" maxWidth="sm">
    <DialogBody>...</DialogBody>
    <DialogFooter>
        <DialogCancelButton onClick={onClose} />
        <DialogPrimaryButton onClick={handleCreate} loading={loading}>作成</DialogPrimaryButton>
    </DialogFooter>
</Dialog>
```

**新規作成**: `frontend/src/components/ui/Dialog.tsx`
**変更対象**: 5ダイアログコンポーネント

### 13-2. ファイル名バリデーションのユーティリティ化（P2）

**問題**: 同じ正規表現が3ファイルで重複定義されており、エラーメッセージも微妙に異なる。

| ファイル | 行 | コード |
|---|---|---|
| `RenameDialog.tsx` | 4 | `const FORBIDDEN_RE = /[/\\:*?"<>\|]/;` |
| `CreateFolderDialog.tsx` | 4 | `const FORBIDDEN_RE = /[/\\:*?"<>\|]/;` |
| `MergeDialog.tsx` | 10 | `const INVALID_CHARS = /[/\\:*?"<>\|]/;` |

**改善案**: `frontend/src/utils/validation.ts` に集約する。

```ts
export function validateFileName(value: string, kind: 'file' | 'folder'): string | null {
    if (!value.trim()) return `${kind === 'folder' ? 'フォルダ名' : 'ファイル名'}を入力してください。`;
    if (FORBIDDEN_FILENAME_CHARS.test(value)) return '使用できない文字が含まれています: / \\ : * ? " < > |';
    return null;
}
```

**新規作成**: `frontend/src/utils/validation.ts`
**変更対象**: 上記3ダイアログ

### 13-3. MoveDialog の API 呼び出しを apiClient に統一（P2）

**問題**: `MoveDialog.tsx:42` だけが `fetch` を直接使っており、エラーハンドリング・型情報・apiClient の共通設定（baseURL 等）が効かない。

**改善案**: 他フックと同じく `apiClient.get` に置換する。

**変更対象**: `frontend/src/components/reader/MoveDialog.tsx`

</details>

<details>
<summary><b>Phase 14 — フロントエンドコンポーネント分割（追加分）</b></summary>

### 14-1. LibraryPanel の auto-fill バー切り出し（P2）

**問題**: `LibraryPanel.tsx` (262行) のうち、サークル名自動登録バー (164-232行 = 68行) が inline で実装されている。`startAutoFill` のラッパー・モード選択ラジオ・進捗バー・完了/エラー表示がすべて1スコープに混在。

**改善案**: `components/viewer/AutoFillAuthorsBar.tsx` に分離。`useAutoFillAuthors` を内部で使い、`source` のみを props で受け取る。

**新規作成**: `frontend/src/components/viewer/AutoFillAuthorsBar.tsx`
**変更対象**: `frontend/src/components/viewer/LibraryPanel.tsx`

### 14-2. GeneratorPage.tsx の表示ロジック分離（P2）

**問題**: `GeneratorPage.tsx` (287行) に以下が混在。

- `getStatusBadgeClass` / `getStatusLabel` ユーティリティ関数（72-86行）
- ジョブ進捗パネル（202-227行）
- ステータステーブル（237-265行）
- 圧縮オプション UI（138-170行）

**改善案**:

```
components/generator/JobProgress.tsx    ← 進捗パネル
components/generator/StatusTable.tsx    ← ステータス表
components/generator/CompressionOptions.tsx ← 圧縮オプション
GeneratorPage.tsx                       ← 合成のみ（目標 150 行以下）
```

**新規作成**: 上記3コンポーネント
**変更対象**: `frontend/src/pages/GeneratorPage.tsx`

### 14-3. ReaderPanel の編集モード/見開きモードのフック化（P3）

**問題**: `ReaderPanel.tsx` (289行) で以下のロジックがコンポーネントに直書き。

- 編集モード状態と削除処理（61-62, 136-172行）
- 見開きモード循環と auto 判定（45-50, 122-134行）

**改善案**:

```
hooks/useEditMode.ts          ← 編集モード管理 + handleDeletePages
hooks/useSpreadMode.ts        ← spreadMode 循環 + autoIsSpread 判定
ReaderPanel.tsx               ← 200行未満を目標
```

**新規作成**: 上記2フック
**変更対象**: `frontend/src/components/viewer/ReaderPanel.tsx`

</details>

<details>
<summary><b>Phase 15 — バックエンド整理</b></summary>

### 15-1. routers/library.py のルーティング分割（P2）

**問題**: `library.py` (301行) に8つのエンドポイントが密集している。

- `list_pdfs` / `list_book_images` / `create_directory` / `move_items` / `rename_item`（ライブラリ管理）
- `regenerate_thumbnail` / `regenerate_thumbnail_bulk`（サムネイル）
- `merge_pdfs`（PDF 結合 — そもそも `pdfs.py` にあるべき）

**改善案**:

```
routers/library.py    ← ライブラリ一覧・フォルダ管理・移動・リネームのみ
routers/thumbnails.py ← regenerate_thumbnail / regenerate_thumbnail_bulk
routers/pdfs.py       ← merge_pdfs を移動
```

bulk 版サムネイル再生成は単発版を `for` で回す形に整理（DRY）。

**変更対象**: `backend/routers/library.py`, `backend/routers/pdfs.py`, `backend/main.py`
**新規作成**: `backend/routers/thumbnails.py`

### 15-2. file_manager.py の `is_pdf_file` 取りこぼし修正（P3）

**問題**: Phase 10-1 で `is_pdf_file()` をユーティリティ化したが、`file_manager.py:66` だけ取りこぼしがある。

```python
# file_manager.py:66 (現状)
book_name = os.path.splitext(item)[0] if item.lower().endswith('.pdf') else item
```

**改善案**: `is_pdf_file(item)` に置換する。低リスク・1行変更。

**変更対象**: `backend/services/file_manager.py`

### 15-3. batch_compress を services 側へ移動（P2）

**問題**: `routers/pdfs.py:175-224` の `batch_compress_pdfs` がルーター内で以下を直接実行。

- `os.walk(IMAGES_DIR)` で画像収集
- `PdfGenerator(...)` をインスタンス化して **private メソッド** `_create_pdf_file` を直接呼び出し（214行）

→ ルーターが PdfGenerator の内部実装に密結合しており、`_create_pdf_file` のシグネチャ変更でルーターが壊れる。

**改善案**:

```python
# pdf_generator.py に追加
def batch_compress(images_dir: str, output_dir: str, quality: int,
                   progress_callback: Optional[Callable[[str], None]] = None) -> list[str]:
    ...
```

ルーターは `from services.pdf_generator import batch_compress` で委譲のみ。

**変更対象**: `backend/services/pdf_generator.py`, `backend/routers/pdfs.py`

### 15-4. auto_fill_service の view_count / last_viewed_at 消失バグ修正（**P1**）

**問題**: `auto_fill_service.py:101` の lambda が既存エントリの `view_count` / `last_viewed_at` を上書き消去する。

```python
update_meta_locked(source, lambda m, k=key, a=author: m.update({k: {"authors": [a]}}))
```

直近のフロントエンド修正コミット `6bd6cc0 fix(useBookMeta): updateAuthors で view_count / last_viewed_at が消える問題` と同じバグがバックエンド側にも残っている。

**改善案**:

```python
def _apply(m, k=key, a=author):
    existing = m.get(k, {})
    m[k] = {**existing, "authors": [a]}
update_meta_locked(source, _apply)
```

**変更対象**: `backend/services/auto_fill_service.py`
**テスト追加**: `tests/test_auto_fill.py`（既存 view_count が保持されることを assert）

### 15-5. routers/pdfs.py の二重ジョブ状態管理 統合（P3）

**問題**: `routers/pdfs.py` で `JobStore`（job_store）と `GenerateState`（generate_state）の **2系統** で進捗を管理している。

- `_run_generate_job` 内で `job.update(...)` と `generate_state.set_current_item(...)` を二重に呼んでいる
- `/api/status` は `generate_state` を参照、`/api/generate/job/{id}` は `JobStore` を参照
- 同期ミスのリスク（generate_state の更新を忘れる、JobStore は更新したのに generate_state が古いまま、等）

**改善案**: `generate_state` を廃止し、`/api/status` 側で `JobStore.get_active_job()` から `current_item` を取得する設計に統合。

**変更対象**: `backend/routers/pdfs.py`, `backend/services/job_manager.py`

</details>

<details>
<summary><b>Phase 16 — テスト追加</b></summary>

### 16-1. バックエンドテスト不足箇所の補完（P2）

| 対象 | 理由 |
|---|---|
| `services/pdf_generator._collect_images` | 自然順ソート・拡張子フィルタの基礎ロジック。回帰しやすい |
| `services/pdf_generator.process_zip` / `process_directory` | ZIP/Directory フローの境界条件（空 ZIP・ネスト・root直下）|
| `services/auto_fill_service` ターゲット選別 | `_is_missing` / `_is_unknown` / mode 別ターゲット計算 |
| `services/auto_fill_service` view_count 保持 | 15-4 のバグ修正検証 |

**新規作成**: `backend/tests/test_pdf_generator.py`, `backend/tests/test_auto_fill.py`

### 16-2. useLibraryFilter のテスト追加（P3）

**理由**: 検索文字列・作者フィルター・currentPath による絞り込みは UX 直結でデグレが目立つ。Phase 11-3 で抽出したばかりでテスト未整備。

**新規作成**: `frontend/src/test/useLibraryFilter.test.ts`

</details>

<details>
<summary><b>Phase 17 — マイクロな整合性</b></summary>

### 17-1. ダイアログ z-index 階層を constants 化（P3）

**問題**: フロントエンド全体で z-index がアドホック (`z-50` / `z-[100]` / `z-[200]` / `z-40` 等) で、レイヤー関係がコードからは追えない。

**改善案**: `frontend/src/constants.ts` に `Z_INDEX` を追加。

```ts
export const Z_INDEX = {
    READER_HEADER: 40,
    DIALOG_OVERLAY: 100,
    DIALOG_NESTED: 200,  // 別ダイアログから開く子ダイアログ用
    TOAST: 300,
} as const;
```

**変更対象**: `frontend/src/constants.ts` および全ダイアログ・ヘッダー・トースト系コンポーネント

### 17-2. ReaderPanel の `confirm()` を ConfirmDialog に置換（P3）

**問題**: Phase 12-3 で `alert()` を廃止したが、`ReaderPanel.tsx:153` の削除確認用 `confirm()` が残っている。OS ネイティブダイアログでダーク UI と乖離。

**改善案**: Phase 13-1 の `<Dialog>` をベースに `ConfirmDialog` を作って置換。

**変更対象**: `frontend/src/components/viewer/ReaderPanel.tsx`
**新規作成（13-1完了後）**: `frontend/src/components/ui/ConfirmDialog.tsx`

</details>

<details>
<summary><b>Phase 18 — Phase 5 派生のリファクタリング詳細</b></summary>

Phase 5-A / 5-B の実装で書いた中で、後で読み返したときに改善余地のある箇所と、追加が漏れたテストを整理する。バグ修正は無し（純粋な構造改善とテスト補完）。

### 18-1. Phase 5 関連のテスト追加（**P1**）

**問題**: Phase 5-A / 5-B の追加コードのうち、副作用がある複雑なロジックがテスト未整備。

| 対象 | リスク |
|---|---|
| `useBookMeta.reorderSeries` の **楽観的更新 + ロールバック** | StrictMode で updater が 2 度呼ばれる前提でスナップショットを再構築する設計だが、現状は手動確認のみ。失敗時に `series_index` が正しく元に戻ることが回帰しやすい |
| `BulkSeriesAssignDialog` の **採番プレビュー計算** | `existingSeries[0].maxIndex` が小数（例: `2.5`）の場合に `floor + 1` で次の整数巻になることが暗黙仕様。現状テストなし |
| `useBookMeta.assignSeries` の **配列 index 経路** | バックエンド側は `test_series.py` で 3 ケース追加済みだが、フロント側のローカル状態更新（`indexFor(i)`）はテストなし |

**改善案**:
- `frontend/src/test/useBookMeta.reorderSeries.test.ts` を新規作成。`apiClient` を `vi.fn` でモックし、成功時 / 失敗時の `meta` 状態を assert。
- `frontend/src/test/BulkSeriesAssignDialog.previewIndexes.test.ts`（コンポーネントテストではなく、採番関数を pure function として切り出してテスト）。
- `useBookMeta.test.ts` を増補して assignSeries の配列 index 経路を追加。

### 18-2. `PdfGrid.tsx` の分割（P2）

**問題**: `PdfGrid.tsx` が 409 行になり、3 つの責務が同居している。

- `CardContent` — カード本体の DOM ツリー（サムネイル / バッジ / 作者・タグ・操作ボタン）
- `SortableCard` — `useSortable` ラッパーとドラッグハンドル
- `PdfGrid` — 空状態・グリッドレイアウト・DnD コンテキスト

**改善案**:

```
components/reader/PdfCard.tsx          ← CardContent（純粋なプレゼンテーション）
components/reader/SortablePdfCard.tsx  ← useSortable ラッパー、PdfCard を import
components/reader/PdfGrid.tsx          ← 空状態・grid layout・DndContext のみ（目標 100 行未満）
```

### 18-3. `ReaderPanel` の next-volume 線形探索をフック化（P2）

**問題**: `ReaderPanel.tsx:64-83` あたりに、現在書籍の次巻を探す IIFE が直書きされている。`meta` が変わる度に毎レンダで再計算（`useMemo` なし）、ReaderPanel の主責務（ページ表示・見開き制御）と無関係。

**改善案**: `frontend/src/hooks/useNextSeriesVolume.ts` を新設。内部で `useBookMeta` を呼び、`useMemo` で探索結果をキャッシュ。

### 18-4. `bulkSeriesExisting` 計算を `useBookMeta` へ移管（P3）

**問題**: `LibraryPanel.tsx:289-304` で `existingSeries: ExistingSeriesOption[]` を計算しているが、このロジックは `useBookMeta.allSeries` と密接（同じく `meta` を走査して series_id ごとに集約）。

**改善案**: `useBookMeta` に `allSeriesWithStats: { id; title; maxIndex; count }[]` を追加し、`LibraryPanel` はこれを `ExistingSeriesOption[]` に map するだけにする。

### 18-5. `ExistingSeriesOption` 型を `types/` に集約（P3）

**問題**: `BulkSeriesAssignDialog.tsx` が `ExistingSeriesOption` を export し、`LibraryDialogs.tsx` / `LibraryPanel.tsx` が import している（コンポーネントから型を逆 import する形）。

**改善案**: `frontend/src/types/index.ts`（または `series.ts`）に `ExistingSeriesOption` を移動し、各所はそこから import する。

### 18-6. DnD ハンドルとお気に入り星の位置競合を解消（P3）

**問題**: シリーズドリルダウン中、`PdfGrid` のカード左上に **DnD ドラッグハンドル（z-20）** と **お気に入り星ボタン（z-10）** の両方が `top-2 left-2` に重なって配置されている。z-index でハンドルが星を覆い隠すため、ドリルダウン中はお気に入り操作ができない。

**改善案**: ドラッグハンドルを `top-2 right-2`（選択モード時のチェックボックス位置）に変更する。

### 18-7. 「次の巻へ」ボタン用の z-index 階層を新設（P3）

**問題**: `ReaderPanel` のフローティング「次の巻へ」ボタンが `z-overlay-bar`（40）を流用しているが、本来この階層は **「ヘッダー下に出る検索バー / トリガーゾーン」** 専用。意味がずれている。

**改善案**: `tailwind.config.js` の zIndex に `'floating-action': '30'` を追加し、ReaderPanel のボタンに使用する。

</details>
