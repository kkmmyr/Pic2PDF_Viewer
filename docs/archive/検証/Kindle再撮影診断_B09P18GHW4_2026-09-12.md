# Kindle再撮影診断 B09P18GHW4（2026-09-12）

> status: archived | recorded-at: 2026-09-12 | baseline-revision: e4e94cd

[初回183画面の撮影](Kindle実撮影_B09P18GHW4_2026-09-12.md)で見つかった通知混入を解消するため、
ユーザーがWindowsの「応答不可」をオンにした後の再撮影・診断結果を保存する。
未完了作業は[リファクタリング計画書](../../log/計画/リファクタリング計画書.md)を正本とする。

## 結果

再撮影は未完了。通常の撮影ジョブを順に実行し、各失敗後に準備操作を確認したが、
前面喪失またはUI操作位置の取得失敗で停止した。成功済みジョブの状態は変更していない。

| job ID先頭 | 結果 |
|---|---|
| dc50ea74 | 9画像保存後のページ送りで前面喪失。DBの最終進捗は5画面 |
| a86a8b4e | 撮影開始前の操作位置取得失敗 |
| 7c76c7a4 | 撮影開始前の操作位置取得失敗 |
| fc197d38 | 準備成功後、カナリアのページ送りで前面喪失 |
| ce93d304 | 同じ前面喪失時にDocker Desktopへの切り替わりを記録 |

ジョブを作らない診断では、ASIN・書名・著者の照合、ダウンロード済み判定、書籍表示、
1ページ設定、開始位置検証、カナリアの成功も確認した。再現は常時ではない。
最後の試行は例外時の前面ウィンドウを記録するだけの一時診断を加え、既存の停止判定を維持した。

- 最後のjob: `ce93d304-b91d-4e44-9336-0c1a6eeb147c`。
- 前面: `Docker Desktop`、PID 16408、HWND 264926。
- 撮影対象KindleのHWND: 11928716。
- 例外: `Kindle window lost foreground while restoring reading focus.`。

最後の前面喪失は切り替わり先まで実測した。Dockerが前面になった理由や、先行する位置取得失敗の
根本原因は確定していない。Docker・コンテナーの停止や撮影側の安全判定の無効化は行っていない。
ユーザーへDocker Desktopのウィンドウ最小化と、撮影中にウィンドウを切り替えない準備を依頼した。

## 保全確認

- 再撮影前のSQLite 3 DBをbackupし、integrity検査に成功。
- 初回登録済み183画像のSHA-256は再試行前後で全件一致。Libraryメタも同一。
- 初回の品質監査は有効なまま。通知を含む警告を確認済みに変更していない。
- 未完了job 0件、復旧記録0件。失敗した5ジョブは履歴として保持。
- ソース変更・画像置換・OCR・本文公開は実施していない。
- 証跡: `/opt/pic2pdf-viewer/backups/2026-09-12_B09P18GHW4_pre-recapture/` の
  `before.json`、`recapture-diagnostic.json`とSQLite backup。

初回の低情報量候補に加え、003/004のカラー口絵と白黒扉、010/011の人物紹介と黒い扉を目視した。
類似警告が撮影重複を意味するとは限らない。全品質候補の承認を代替するものではない。
