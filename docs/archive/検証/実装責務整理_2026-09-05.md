# 実装責務整理 — 2026-09-05検証記録

> status: historical | last-verified: 2026-09-05

開始時点は`f632f2f`。実装調査で承認された優先5件の分離と、既存契約の照合を記録する。
現行の責務は[検索QA設計](../../design/詳細設計/機能別/小説RAG_検索QA設計.md)、
[小説フロントエンド設計](../../design/詳細設計/機能別/小説RAG_フロントエンド設計.md)、
[OCR設計書](../../design/詳細設計/機能別/OCR設計書.md)を参照する。

## 実施範囲と不変条件

| 対象 | 分離した責務 | 維持した契約 |
|---|---|---|
| 評価CLIのOS依存 | 共通型・fixture・NPZを`novel_embedding_eval_common.py`、RSSを`novel_eval_runtime.py`へ | 公開import、CLI引数、read-only SQLite、モデルhash、NPZ/report形式、Mac byte/Linux KiB換算 |
| 順位評価 | 3方式の指標を`novel_eval_metrics.py`へ | Recall@5/10/30、MRR@10、graded nDCG、重複hitの既存計算、方式別計測単位 |
| OCR QA | API生成型のrequestオブジェクトと応答型 | endpoint、null/任意項目省略、レビュー時間・補正時間・修正文、承認条件 |
| 小説一覧 | 候補取得と逐次更新を`useNovelLibraryBulkActions`へ | 作者明示選択、書籍順、途中失敗時の停止、成功時だけ再取得/選択解除、URL `dd` |
| 読書会 | 本文ロード・2段生成・検査・保存を`discussion_service`へ | HTTP開始前のDB例外、SSE順序、空/失敗/切断時の保存抑止、保存JSON v2、history API |

MLX importは引数解析後、`resource` importはRSS取得時に限定する。
Windowsでも共通処理と`--help`を利用でき、未対応OSでRSSの架空値は返さない。
実モデル、公開DB、封印holdout、OCR本文公開、配備は実行していない。
文脈生成ジョブとpage全文索引の分割は今回の対象外。

## 挙動固定と対象回帰

- 評価: 変更前のsearch/dense 14件が成功。OS境界2件は`resource`のimportで失敗することを再現。
  変更後はexport/rerankerを含む41件が成功。3方式のadapter比較とRSS単位換算を含む。
- 読書会: 切断位置0/1/2、空stream、部分生成後の例外、HTTP開始前のDB例外を追加し、
  分離前後とも49件成功。既存のv2保存・history互換も確認した。
- 小説一覧・OCR QA: 変更前の挙動固定5件成功。変更後の対象5ファイル10件成功。
  候補取得失敗、逐次更新と途中失敗、QAのHTTP payloadを含む。
- 評価CLI 6本のmodule/standalone `--help`は12通りすべて成功。
- 独立レビューで読書会、フロントエンド、評価CLIの今回差分に回帰指摘なし。

## 全体検査

- Frontend: 130ファイル、1,110 tests成功。型検査とproduction build成功。
- Ruff lint/format成功、basedpyrightはerror/warningとも0。
- ESLintはerror 0、未変更箇所の既存accessibility warning 13件。
- OpenAPIは109 paths、digest `ed33f214ffed`で変更前と一致。
- 最終月次監査は全項目成功。設計書11ルール、ファイルマップ、code-size、C901、
  OpenAPI、import境界、保守資産、未使用依存を照合した。C901は39件で既存baseline43件を超えない。
  MkDocs strict buildも成功。検査閾値・baselineは変更していない。
- Knipは標準実行でoxc-parserのArrayBuffer確保に失敗したが、同梱parserが提供する
  `KNIP_DISABLE_RAW_TRANSFER=1`を検査processだけに指定して成功。依存・恒久設定は変更していない。
- Backend全体: 1,625 passed / 9 failed / 2 skipped、coverage 72.49%（下限65%を通過）。
  この全体実行後に追加した評価境界テストとPPLX import整理は、上記対象41件で再検証した。
  失敗9件の原因は下記の未変更箇所のWindows依存であり、全件成功とは扱わない。

## ブラウザ確認

変更前後に実Chromiumで1280×850、390×850を確認。ローカルViteと合成API応答を使い、
実データへ通信しない構成で次を実行した。

- 作者未選択での拒否、選択後の逐次PATCHと選択解除を各幅2回。
- シリーズ登録の巻数1/2、series drilldownと一覧への戻り。
- Tab / Shift+Tab / Enter / Escape、dialog開閉、コンソールと未処理例外0。
- 390幅の作者dialogスクリーンショットは変更前後で同一SHA-256。

共通Dialogの390幅で右端が16px画面外になる既存レイアウトを確認した。
主要操作は実行でき、今回変更前と同一である。共通UIの修正は本リファクタへ混在させていない。

## 環境上の制約

- 未変更の`test_deploy_release.py`は、WSLの`/bin/bash`不在で4件、Windowsのsymlink作成権限で1件失敗する。
- 未変更の`test_novel_db_extractor.py`は、停止済みprocessへの`os.kill(pid, 0)`が
  Windowsでは`ProcessLookupError`でなく`WinError 87`となるため1件失敗する。
- 未変更の`test_qwen_dots_worker.py`はvenv Pythonのsymlink作成権限で1件失敗する。
- 未変更の`select_qwen35_dots_predictions.py`はdirectoryを`os.open(..., os.O_RDONLY)`で
  開くfsync手順がWindowsでPermissionErrorとなり、原子的JSONL出力テスト1件が失敗する。
- 未変更の`test_sol_job_package.py`はUTF-8のJSONLをencoding指定なしで読み、Windows既定の
  cp932でUnicodeDecodeErrorとなるため1件失敗する。
- Mac実機でのMLX実推論は未実施。共通計算・モデル検証・read-only I/O・引数契約の回帰で照合した。
- 実行環境はWindows、Node 24.13.0。CI指定Node 22.23.2との実行環境差は残る。

これらを全件成功やMac実推論の受入と同一視しない。環境確認の管理先は
[リファクタリング計画書](../../log/計画/リファクタリング計画書.md)とする。
