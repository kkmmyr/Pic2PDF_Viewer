# Kindle実撮影 B09P18GHW4（2026-09-12）

> status: archived | recorded-at: 2026-09-12 | baseline-revision: e4e94cd

TM-11配備後のWindows新規撮影の初回結果を保存する。登録経路の成功と画像品質の受入を区別する。
現行契約は[Kindle自動撮影ジョブ契約](../../design/詳細設計/機能別/Kindle自動撮影ジョブ契約.md)、
残作業は[リファクタリング計画書](../../log/計画/リファクタリング計画書.md)を参照する。

## 対象と準備

- ユーザー指定: 乙女ゲームのヒロインで最強サバイバル3【電子書籍限定書き下ろしSS付き】、ASIN `B09P18GHW4`。
- KindleライブラリのASIN・書名・著者「春の日びより」が一致。ダウンロード済み。
- 最初の検索中にKindle 1.0.23620.0が終了。Windows Application Errorは16:21:28、
  `ucrtbase.dll` / `0xc0000409`。process消失を確認して1回起動し直し、同一書籍の検索に成功した。
  この障害時点ではジョブも本番書誌も未作成だった。クラッシュの根本原因は未特定。
- 本番カタログ・設定済み旧DB・Amazon CSVに対象ASINがなかったため、確認済み書誌を
  既存の書誌登録処理で1冊追加した。購入・借用・返品・架空の取込履歴は追加していない。
- 同名の既存画像ディレクトリはなく、新規登録。事前SQLite 3 DB backupとintegrity検査に成功。
- backupと調査証跡: `/opt/pic2pdf-viewer/backups/2026-09-12_B09P18GHW4_pre-capture/`。
  `catalog-registration.json`に登録根拠、`capture-acceptance.json`に画像・DB差分を保存した。

## 撮影・登録結果

| 項目 | 結果 |
|---|---|
| job | `accf4ee5-604a-43cb-8988-dd30c4b1324a` |
| source / agent | novel / kindle-windows-1 |
| 依頼・完了（JST） | 16:24:09 → 16:34:00 |
| 撮影 | 183画面、左送り、single、期待枚数の指定なし |
| 画像 | PNG、3516×1940、001〜183の連番、全件復号成功 |
| カナリア | passed、撮影矩形 `[170,108,3686,2048]` |
| 終端 | `visual_no_change_after_retries`、不変観測2回、逆方向送り0回 |
| 登録 | succeeded、ASINメタ登録、品質監査1世代 |
| 転送 | processed packageと正式画像183件のSHA-256全件一致 |
| 配信 | 画像一覧183件、1・16・92・182・183画面のHTTP取得と保存画像のSHA-256一致 |
| 復旧 | 未完了job 0件、`.capture-recovery`内の残存ファイル0件 |

Chromeの撮影一覧でも「取込完了」「183画面」と品質候補5分類を確認した。
1・16・92・140・182・183画面を目視し、表紙、縦書き本文・ルビ、漫画、奥付・末尾注意書きを確認。
通常登録はTM-11配備済みコードで完走した。今回は補償失敗を注入していない。

## 品質警告と未完了事項

警告は5分類・60 finding。隣接類似3、白紙・低情報量4、低容量6、ページ端46、短時間右下通知1。
8・85・93・167画面の低情報量候補は、制作クレジット・短い本文・特典SS章扉であることを目視した。
**162〜164画面にはChatGPTのデスクトップ通知が実際に写り込んでいる。画像品質の最終受入は未完了。**
通知検出はこの3画面を候補として正しく保存・表示した。確認済みへの変更や警告削除は行っていない。
残る類似・ページ端候補をすべて問題なしと判定した記録ではない。

再撮影にはWindowsの通知抑止が必要。Windowsの通知設定を操作できるツールがないため、
ユーザーへ「応答不可」をオンにする操作を依頼した。部分画像だけを差し替える正式な経路はないため、
再撮影時は同一ASINの通常ジョブで全体を置換し、旧世代・旧監査を保持して再検査する。

作業前後の通常テーブル比較では7テーブルだけに本書関連の行が追加され、既存行の削除・変更は0件。
追加は著者1・書籍著者1・書籍1・撮影job1・品質監査1・警告5・Libraryメタ1。
購入・借用・返品、Novel DBは不変。OCR・本文公開・索引生成は実行していない。
