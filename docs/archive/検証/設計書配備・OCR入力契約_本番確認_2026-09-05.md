# 設計書配備・OCR入力契約の本番確認（2026-09-05）

> status: archived | recorded-at: 2026-09-05 | baseline-revision: 35d04d6

この記録は2026-09-05の配備・実機確認結果を保存する。現在の契約は
[OCR設計書](../../design/詳細設計/機能別/OCR設計書.md)、配備手順は
[運用ガイド](../../design/環境構築/運用ガイド.md)を参照する。

## 1. 取り込みと配備

- [PR #16](https://github.com/kkmmyr/Pic2PDF_Viewer/pull/16)を4検査（backend / frontend / docs / secrets）成功後にmasterへ取り込んだ。merge commitは`35d04d6c32f07675239df8496d059cf73f4c8161`。取り込み後のmasterのCIも成功した。
- PRのbackendテストは1625 passed / 1 skipped、カバレッジ72.82%。Kindleは132 passed、common/llmは76 passed。文書構造・ファイルマップ鮮度・MkDocs strict buildも成功した。
- MkDocsを生成し、Node.js 22.23.2 / npm 10.9.8で通常の配備スクリプトを実行した。既存のローカルNode設定は変更していない。
- Linuxの旧世代`backend-20260829203709-366`から`backend-20260905145154-1949`へ切り替えた。旧世代はrollback用に保持した。
- 配備前backupは`/opt/pic2pdf-viewer/backups/2026-09-05_145226_pre-deploy`。SQLite 3 DBとLanceDBの復元検証が成功し、dense chunksはSQLite / LanceDBとも6321件、1024次元で一致した。
- Alembicは0016、integrity_checkはok。LanceDBの固定pathとICU検索の切り替え前検査も成功した。
- 配備したPythonソース・マニフェスト・主要HTMLの472ファイルをローカルとSHA-256で照合し、差分は0件だった。
- 公開入口の`/`、`/site/index.html`、Kindle自動撮影ジョブ契約、archive索引、`/api/pdfs?source=doujin`、`/api/ocr/status`はHTTP 200。固有見出しと本番ブラウザの設計書トップで新しい入口を確認した。
- `pic2pdf-viewer`とCodex連携user serviceはactive。再起動以降のsystemd errorログは0件だった。MCPツール呼び出しのE2E検証は含めない。

## 2. 新規OCR runの実機確認

対象はbook ID 41、`「君を愛することはない」と言った氷の魔術師様の片思い相手が、変装した私だった (デジタル版SQEXノベル)`の連番PNG全57ページ。
画像数が最少の既存書籍を選び、画像・公開本文・既存QAを変更しない経路で確認した。
ページ範囲を偽って1ページrunにする操作や、一覧へ現れる検証用書籍の追加は行っていない。

| 項目 | 観測結果 |
|---|---|
| 対象指定 | `POST /api/ocr/run`へ対象書籍名を明示（all投入なし） |
| job / run | job 140 / run 192 |
| Windows agent | `windows-release-manifest-20260905`。同じcheckoutの`run_once`を1回実行 |
| 実行コード | `35d04d6c32f07675239df8496d059cf73f4c8161`、tracked変更なし |
| 開始 / OCR完了 | 14:54:31 / 15:02:27（サーバー記録、JST） |
| 終了状態 | job=`completed`、run=`awaiting_qa`、qa_state=`pending` |
| ページ | 57/57保存、全57ページがqa_state=`required`、承認0件 |
| 実行環境 | Windows 11、Python 3.12.10、RTX 5070 / CUDA 12.8、torch 2.11.0+cu128、yomitoku 0.12.0 |
| 来歴 | 推論workerが採取したschema_version=1のruntime manifestをLinuxへ保存。engine / model revisionの一致、候補manifest、各ページの保存を確認 |
| モデル資材 | Surya f16 SHA-256=`1f18abe17b1ed8b4e47ee9b1ad0e274c93daf5efbb6b29a04ff1712e37051e05`、mmproj SHA-256=`98c0563673b1657ff6d021d1e5f04af06cbf61bb40c63ac613e8bb71b42fb2c0` |
| 推論サーバー | llama.cpp b10066 / `86a9c79f8`。実行ファイルのversion出力とも一致 |
| 後処理 | agentは有限回で終了し、所有する推論プロセスも終了。OCR statusはidle、GPU使用メモリは0 MiB |

保存・QA移行の契約検証は成功したが、OCR精度の合格や本文公開を意味しない。
機械判定は54ページがpassed、3ページがfailedだった。

- 1ページ: `cross_engine_disagreement`。原画像とのQA確認が必要。
- 13・14ページ: `external_ocr_low_confidence`。Surya失敗後の外部候補が低信頼と判定されており、QA確認が必要。

失敗ページを承認済みに変更せず、他のページも含めて全57ページを未承認のまま保持した。
QAレビュー・修正・公開APIは呼び出していない。新たなH3 holdoutの評価やモデル採用判断には使用していない。

## 3. 既存データの不変確認

配備前の全行をID順にJSON化してSHA-256を取得し、OCR完了後に再計算した。
新規run / page結果だけを除外するため、OCRの2表は開始前の最大IDまでを比較した。

| 対象 | 比較行数 | 結果 |
|---|---:|---|
| books | 84 | 全行一致 |
| pages（公開本文） | 10320 | 全行一致 |
| chunks（検索用本文） | 6321 | 全行一致 |
| ocr_publications（公開履歴） | 85 | 全行一致 |
| 既存ocr_runs（ID 191まで） | 191 | 全行一致 |
| 既存ocr_page_results（ID 19613まで） | 19613 | 全行一致 |

終了時のnovel.db quick_checkもokだった。既存QA・公開済みrunに実行環境情報を後付けしていない。

## 4. 確認の境界

今回の未反映問題はLinux受信側とWindows Surya agent/workerで解消した。
Macの実機推論、停止中の旧Windowsサービスの再登録、QAの人手承認、Sol導入・OCR品質評価は別作業である。
上記世代・日時・件数はこの検証時点の観測であり、後続配備後の稼働状態を示さない。
