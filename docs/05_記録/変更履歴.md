# 変更履歴 (CHANGELOG)

過去の主要な改善・リファクタリング作業の記録。

---

## 2026-04-26: テスト修復・gitignore 整備・不要ファイル削除

### 概要

- `tests/test_meta.py` が削除済みの `_has_real_author` を import していて collection error。現在の API（`_is_missing` / `_is_unknown`）に書き換えて修復。
- `*.tsbuildinfo` を `.gitignore` に追加し、追跡対象から除外（TypeScript ビルドキャッシュ）。
- `backend/package-lock.json`（空の npm lock ファイル、誤生成と思われる）を削除。

### 主な変更ファイル

- `backend/tests/test_meta.py` — `_has_real_author` の参照を `_is_missing` / `_is_unknown` のテストに置き換え。テスト 12 件追加（以前は collection error で 0 件実行）。
- `.gitignore` — `*.tsbuildinfo` を追加。
- 削除: `frontend/tsconfig.app.tsbuildinfo`、`frontend/tsconfig.node.tsbuildinfo`、`backend/package-lock.json`

---

## 2026-04-26: 閲覧回数ソートの追加

### 概要

書籍ごとに閲覧回数を記録し、「よく見る順」でソートできるようにした。カード上の数字表示はせずソートのみで利用する。

### 主な変更ファイル

**バックエンド:**
- `backend/services/meta_store.py` — `MetaEntry` に `view_count` / `last_viewed_at` を追加（任意フィールド）
- `backend/routers/meta.py` — `POST /api/meta/view` を追加（カウント +1）

**フロントエンド:**
- `frontend/src/types/index.ts` — `BookMetaEntry` に `view_count?` / `last_viewed_at?` を追加、`SortOrder` に `'view_desc'` を追加
- `frontend/src/config/api.ts` — `META_VIEW` エンドポイント追加
- `frontend/src/hooks/useBookMeta.ts` — `getViewCount` / `recordView` を追加
- `frontend/src/hooks/useSortedPdfs.ts` — `view_desc` ケースを追加（同数は名前昇順）、`getViewCount` を任意引数として受け取る
- `frontend/src/components/reader/HeaderSortSelect.tsx` — 「よく見る順」オプション追加
- `frontend/src/components/viewer/LibraryPanel.tsx` — `useBookMeta` から `recordView`/`getViewCount` を取得、`onPdfClick` をラップして閲覧記録、`useSortedPdfs` に閲覧回数取得関数を渡す

**設計書:**
- `docs/03_詳細設計/詳細設計書.md` — 「閲覧回数ソート」節を追加

---

## 2026-04-26: 選択モードの視覚スタイル変更（青 → 琥珀）

### 概要

選択モードで選択中のカード枠が `border-blue-500` だったが、ダークモードで視認性が低く、UI 内の他の青系要素とも紛らわしかったため、`border-amber-400` + 薄い琥珀色背景に変更。チェックアイコンの色も合わせて変更。

### 主な変更ファイル

- `frontend/src/components/reader/PdfGrid.tsx` — 選択枠を amber に、選択時の背景を `bg-amber-50 dark:bg-amber-900/20` に変更。`CheckSquare` の色も `text-amber-500` に。
- `frontend/src/components/reader/FolderGrid.tsx` — 同様の変更。
- `docs/03_詳細設計/詳細設計書.md` — 「選択モードの視覚スタイル」節を追加。

---

## 2026-04-26: Python パッケージ管理を uv に移行

### 概要

`backend/` と `kindle-pdf/` の Python 環境を `venv + pip + requirements.txt` から `uv + pyproject.toml + uv.lock` に移行。依存解決の高速化と再現性の向上が目的。

### 主な変更ファイル

**新規作成:**
- `backend/pyproject.toml` — FastAPI バックエンド依存定義（`[dependency-groups] dev` で pytest 系を分離）
- `backend/uv.lock` — 依存ロックファイル
- `kindle-pdf/pyproject.toml` — Kindle キャプチャ依存定義（`[dependency-groups] gpu` で yomitoku + CUDA版 PyTorch を分離、`[[tool.uv.index]]` で pytorch-cu121 インデックスを定義）
- `kindle-pdf/uv.lock`
- `docs/04_環境構築/uv環境セットアップ.md` — uv のインストール・セットアップ手順

**更新:**
- `backend/start_server.bat` — `python -m venv` / `pip install` / `activate` を `uv sync` + `uv run uvicorn` に置換
- `起動方法.md` — 起動コマンドを `uv run` に統一
- `.claude/CLAUDE.md` — 起動方法セクションを uv ベースに更新

**削除:**
- `backend/requirements.txt` / `backend/requirements-dev.txt`
- `kindle-pdf/requirements.txt` / `kindle-pdf/requirements-gpu.txt`
- `backend/venv/` / `kindle-pdf/venv/`（旧仮想環境）

### 移行範囲外

- `D:\61.tool\common\ocr\` の共有 OCR 環境（yomitoku + PyTorch CUDA）は他プロジェクトとも共有しているため、本移行のスコープ外。`kindle-pdf/start_batch_ocr.bat` は引き続き共有 venv を参照。

### 効果

- 依存解決速度: pip 比で約 10〜100 倍（初回環境構築 31 秒で完了）
- 再現性: `uv.lock` で完全固定
- 起動: `activate` 不要（`uv run` が `.venv/` を自動検出）

---

## 2026-04-26: リファクタリング Phase 12（定数化・トースト通知導入）

### 概要

Phase 12-1〜12-3 を実施。マジックナンバーを定数化し、`alert()` をトースト通知に置き換え。

### 主な変更ファイル

**新規作成:**
- `frontend/src/hooks/useToast.ts` — トースト通知管理フック（4秒自動消去・手動解除）
- `frontend/src/components/reader/ToastContainer.tsx` — トースト表示 UI（右下固定・種別スタイル）

**更新:**
- `frontend/src/constants.ts` — `STORAGE_KEYS.DARK_MODE`, `STORAGE_KEYS.FAVORITES_PREFIX`, `UI_CONFIG.SEARCH_DEBOUNCE_MS`, `UI_CONFIG.PRELOAD_MARGIN` を追加
- `frontend/src/hooks/useDarkMode.ts` — `STORAGE_KEYS.DARK_MODE` 使用
- `frontend/src/hooks/useFavorites.ts` — `STORAGE_KEYS.FAVORITES_PREFIX` 使用
- `frontend/src/hooks/useAutoFillAuthors.ts` — `API_CONFIG.JOB_POLL_INTERVAL_MS` 使用、`alert()` を削除（呼び出し側で処理）
- `frontend/src/components/reader/PdfSearchBar.tsx` — `UI_CONFIG.SEARCH_DEBOUNCE_MS` 使用
- `frontend/src/components/reader/LazyThumbnail.tsx` — `UI_CONFIG.PRELOAD_MARGIN` 使用
- `frontend/src/components/viewer/LibraryPanel.tsx` — `alert()` → `showToast()`、`ToastContainer` 追加
- `frontend/src/components/viewer/ReaderPanel.tsx` — `alert()` → `showToast()`、`ToastContainer` 追加
- `backend/services/auto_fill_service.py` — `AUTOFILL_REQUEST_DELAY_SEC = 5.0` 追加
- `backend/services/ocr_service.py` — `PROCESS_TERMINATE_TIMEOUT_SEC = 5` 追加

---

## 2026-04-26: リファクタリング Phase 11（フロントエンドコンポーネント分割）

### 概要

Phase 11-1〜11-3 を実施。フィルタロジック・検索ロジックのフック化、LibraryHeader のサブコンポーネント分割。

### 主な変更ファイル

**新規作成:**
- `frontend/src/hooks/useLibraryFilter.ts` — PDF/フォルダのフィルタリングロジック（searchText / authorFilter）
- `frontend/src/hooks/usePdfSearch.ts` — PDF テキスト検索ロジック（全ページ走査・ハイライト）
- `frontend/src/components/reader/HeaderSearchBar.tsx` — タイトル検索・作者フィルタ UI
- `frontend/src/components/reader/HeaderSortSelect.tsx` — ソート選択 UI
- `frontend/src/components/reader/SourceSelector.tsx` — ソース切り替えタブ UI

**変更:**
- `frontend/src/components/viewer/LibraryPanel.tsx` — `useLibraryFilter` 使用、`useMemo` インライン削除
- `frontend/src/components/reader/LibraryHeader.tsx` — サブコンポーネントに委譲、144行→109行
- `frontend/src/components/viewer/ReaderPanel.tsx` — `usePdfSearch` 使用、検索関連 state/ロジック削除、352行→260行

---

## 2026-04-26: リファクタリング Phase 10（重複コード整理）

### 概要

Phase 10-1〜10-3 を実施。拡張子フィルタリングの関数化・meta.json 操作パターンの統一・MetaDict 型定義の追加。

### 主な変更ファイル

- `backend/utils/file_utils.py` (新規) — `is_webp_file()` / `is_zip_file()` / `is_image_file()` / `is_pdf_file()`
- `backend/services/meta_store.py` — `MetaEntry` / `MetaDict` TypedDict 追加、`update_meta_locked()` 追加、`load_meta()` / `save_meta()` の型ヒントを `MetaDict` に変更
- `backend/routers/pdfs.py` — `endswith(SUPPORTED_*)` → `is_webp_file()` / `is_zip_file()` に置き換え
- `backend/routers/library.py` — `endswith(SUPPORTED_*)` → `is_image_file()` / `is_pdf_file()` に置き換え
- `backend/services/pdf_generator.py` — 同上
- `backend/services/auto_fill_service.py` — `is_pdf_file()` 使用、`update_meta_locked()` 使用、型ヒントを `MetaDict` に更新
- `backend/routers/meta.py` — `update_meta_locked()` 使用

---

## 2026-04-26: 自動登録の進捗表示を対象件数ベースに修正

### 概要

`state.total` が全PDF件数になっていたため「16 / 279件」のように常に全件数が表示されていた問題を修正。
`state.total = len(targets)` に変更し、実際の処理対象件数のみ表示するようにした。

### 主な変更ファイル

- `backend/services/auto_fill_service.py` — `state.total` を `len(all_pdfs)` → `len(targets)` に変更

---

## 2026-04-26: サークル名自動登録に mode パラメータ追加（3択）

### 概要

`overwrite: bool` を `mode: str` に変更し、自動登録の対象範囲を3択で選べるようにした。

| mode | 動作 |
|---|---|
| `missing_only` | 作者名エントリが存在しない書籍のみ（完全未登録） |
| `unknown_only` | `"作者不明"` の書籍のみ（デフォルト） |
| `overwrite_all` | 登録済みを含む全件を上書き |

### 主な変更ファイル

- `backend/services/auto_fill_service.py` — `run_auto_fill(mode)` に変更、フィルタロジックを3分岐
- `backend/routers/meta.py` — `POST /meta/auto-fill` の `overwrite` パラメータを `mode` に変更
- `frontend/src/hooks/useAutoFillAuthors.ts` — `startAutoFill(mode)` に変更
- `frontend/src/components/viewer/LibraryPanel.tsx` — チェックボックス → ラジオボタン3択に変更

---

## 2026-04-26: 作者名バリデーション追加・不正データクリーニング

### 概要

`auto_fill_service.py` が `resolve_author()` の返り値をバリデーションなしで保存していたため、
Gemma が誤って検索スニペット・URL・JSON・DLsiteブランド名などを返した場合にそのまま
`meta.json` に保存されてしまう問題を修正。

### 変更内容

- `backend/services/author_resolver.py` — `_sanitize_author()` を追加。以下に該当する返り値を `"作者不明"` に置換
  - `DLsite` / `DMM` / `FANZA` を含む文字列（大文字小文字問わず）
  - URL（`http://` / `https://` / `site:` を含む）
  - JSON文字列（`{` を含む）
  - 80文字超え（タイトル・文章の混入）
  - `None` リテラル・空文字
- `backend/data/meta/generated/meta.json` — 上記ルールで不正な作者名を `"作者不明"` に一括リセット

### 主な変更ファイル

- `backend/services/author_resolver.py`
- `backend/data/meta/generated/meta.json`

---

## 2026-04-19: リファクタリング Phase 4–9 完了

### 概要

| Phase | 内容 |
|---|---|
| Phase 4 | エラーハンドリング統一（`logger.exception` / FastAPI exception_handler 追加） |
| Phase 5 | ViewerPage Props Drilling 解消（`LibraryContext.tsx` 導入、21props → 0props） |
| Phase 6 | `pdf_generator.py` リファクタリング（`_collect_images` 共通関数、bytes/path 二重実装を解消） |
| Phase 7 | AutoFillService 抽出（`services/auto_fill_service.py` / `services/meta_store.py` 新規作成） |
| Phase 8 | 環境変数化（`GEMMA_TOOL_DIR` を `.env` で上書き可能に） |
| Phase 9 | テスト追加（`tests/test_path_utils.py` / `test_meta.py` / `test_file_manager.py`） |

### 主な変更ファイル

**バックエンド:**
- `backend/services/meta_store.py` (新規) — meta.json CRUD・ロック管理
- `backend/services/auto_fill_service.py` (新規) — AutoFillState・ジョブ管理
- `backend/services/pdf_generator.py` — `_collect_images` 共通化、bytes 依存を除去
- `backend/routers/meta.py` — HTTP ハンドラのみに整理
- `backend/tests/test_meta.py`, `test_file_manager.py` (新規)

**フロントエンド:**
- `frontend/src/contexts/LibraryContext.tsx` (新規) — ライブラリ状態を一元管理
- `frontend/src/pages/ViewerPage.tsx` — LibraryProvider を使ったシンプルな構造に変更
- `frontend/src/components/viewer/LibraryPanel.tsx` — props 削除、Context から取得に変更

---

## 2026-04-19: サークル名自動登録のリクエスト間隔調整

### 変更内容
- `backend/routers/meta.py`: 自動登録ジョブの1件あたりの待機時間を 1秒 → **5秒** に変更。
  - **背景:** SearXNG の上流検索エンジン（Brave / DuckDuckGo 等）が連続リクエストにより
    レート制限・CAPTCHA ブロックを受け、全件「作者不明」が返る問題が発生した。
  - 詳細は [既知の問題.md](既知の問題.md) を参照。

---

## 2026-04-19: RTL見開きモードのページ重複バグ修正

### バグ修正
- `frontend/src/components/viewer/ReaderPanel.tsx`: RTL見開きモードで1ページ目を単独表示（表紙扱い）に変更。
  - **原因:** `renderSpreadPages()` がページ1&2を一緒に表示した後、`handleNext` で `pageNumber=2` に設定するため、次の見開き（2&3ページ）でページ2が重複表示されていた。
  - **修正:** RTLかつ `pageNumber === 1` の場合は1ページ単独レンダリングに切り替え。

---

## 2026-04-19: リファクタリング Phase 1・Phase 2

### 概要
プロジェクト全体の構造整理。定数の一元化・ユーティリティ整備（Phase 1）と、バックエンド責務分離（Phase 2）を実施。

### Phase 1: 定数・ユーティリティ整備

#### バックエンド
- `backend/config.py`: `THUMBNAIL_HEIGHT` / `JOB_MAX_JOBS` / `OCR_LOG_MAXLEN` / `SUPPORTED_IMAGE_FORMATS` / `SUPPORTED_WEBP_FORMAT` / `SUPPORTED_ZIP_FORMAT` を定数化
- `backend/utils/file_naming.py` 新規作成: `get_thumbnail_name()` を共通化（8箇所の重複を解消）
- `backend/services/pdf_generator.py`, `backend/services/ocr_service.py`, `backend/routers/pdfs.py`, `backend/routers/library.py`: 定数・ユーティリティ関数を参照するよう修正

#### フロントエンド
- `frontend/src/constants.ts` 新規作成: `STORAGE_KEYS` / `API_CONFIG` を定義
- `frontend/src/utils/storage.ts` 新規作成: `getStorageJson` / `setStorageJson` / `removeStorage` を共通化
- `frontend/src/pages/GeneratorPage.tsx`, `frontend/src/components/viewer/LibraryPanel.tsx`, `frontend/src/config/api_client.ts`: 定数・ユーティリティを参照するよう修正

### Phase 3: フロントエンド責務分離

#### 新規ファイル
- `frontend/src/hooks/useGenerateJob.ts`: GeneratorPage のジョブ管理ロジック（localStorage保存・ポーリング）をフックに抽出
- `frontend/src/components/reader/EditModePanel.tsx`: ReaderPanel のページ削除UI を切り出し
- `frontend/src/components/viewer/LibraryDialogs.tsx`: LibraryPanel の5種ダイアログ群を集約

#### 修正ファイル
- `frontend/src/pages/GeneratorPage.tsx`: `useGenerateJob` を使うよう簡略化（独自 interval 実装を廃止）
- `frontend/src/components/viewer/ReaderPanel.tsx`: `EditModePanel` をインポートして編集UI を委譲
- `frontend/src/components/viewer/LibraryPanel.tsx`: `LibraryDialogs` を使うよう変更

---

### Phase 2: バックエンド責務分離

#### 新規ファイル
- `backend/exceptions.py`: `FileOperationError` / `OcrProcessError` カスタム例外クラス
- `backend/services/job_manager.py`: `GenerateJob` / `JobStore` / `GenerateState` を `routers/pdfs.py` から分離
- `backend/services/file_manager.py`: PDF+サムネイル+画像の3点セット操作 (`move_with_assets`, `rename_with_assets`) を共通化

#### 修正ファイル
- `backend/routers/pdfs.py`: ジョブ管理クラスを `job_manager.py` からインポートするよう変更
- `backend/routers/library.py`: `move_items` / `rename_item` を `FileManager` 経由に変更。ロールバックロジックを `file_manager.py` に集約

---

## 2026-04-16: PDF検索機能修正・URLソース同期・コードリファクタリング

### 概要
PDF内テキスト検索が機能しなかった問題を修正。複数の根本原因を特定・解消。

### バグ修正

#### 1. novel ソースで画像モードに落ちていた問題
- `hooks/useBookImages.ts`: `source === 'novel'` の場合は画像取得をスキップし、PDF モードを強制。
  - `kindle_novel/images/` に PNG ファイルが存在するため従来は画像モードで開かれており、`<Document>` がレンダリングされず `onDocumentLoadSuccess` が呼ばれなかった。

#### 2. pdfjs Worker が正常に起動しない問題
- `components/viewer/ReaderPanel.tsx`: `workerSrc` を `new URL('pdfjs-dist/build/pdf.worker.min.mjs', import.meta.url).toString()` に変更。
  - `<Document>` コンポーネントと同一ファイル内で設定しなければならない（react-pdf の要件）。
  - Vite がビルド時にワーカーファイルを自動バンドルするため CDN 不要。
- `pages/ViewerPage.tsx`: 重複していた `workerSrc` 設定を削除。

#### 3. TextLayer スタイルが適用されない問題
- `components/viewer/ReaderPanel.tsx`: `import 'react-pdf/dist/Page/TextLayer.css'` を追加。ハイライト表示に必須。

#### 4. 検索バーを開いても表示されない問題
- `components/viewer/ReaderPanel.tsx`:
  - 検索バー表示条件を `isSearchOpen && showHeader` → `isSearchOpen` のみに変更。
  - `ReaderHeader` の `showHeader` を `showHeader || isSearchOpen` に変更（検索中はヘッダーを常に表示）。

#### 5. `pdfRef.current` が null になる タイミング問題
- `components/viewer/ReaderPanel.tsx`:
  - `searchAllPages(text, pdf)` の引数を `pdfRef.current` ではなく `PDFDocumentProxy` を直接受け取る形に変更。
  - `onDocumentLoadSuccess`: PDF ロード完了時点で `searchText` が入力済みなら即座に `searchAllPages` を呼び出す。

#### 6. ソース切り替え後に PDF URL が 404 になる問題
- `hooks/useUrlState.ts`: `currentSource` を URL `?source=` パラメータから取得・管理するよう変更。
  - 全ナビゲーション関数 (`navigateIntoFolder`, `navigateUp`, `selectPdf`, `clearPdf`) のシグネチャに `source: LibrarySource` を追加。
  - `resetAll()` を `setSource(source)` に置き換え（`?source=` のみ変更し他はリセット）。
- `pages/ViewerPage.tsx`: `currentSource` の `useState` を削除。`useUrlState()` から取得するよう変更。

### コードリファクタリング
- `backend/services/pdf_service.py`: `typing.List` → `list[int]`、`temp_path` の `finally` ブロッククリーンアップ。
- `backend/routers/library.py`: 未使用 `import fitz` / `List` 削除、`logger` 一元化。
- `frontend/src/hooks/useBookMeta.ts`: `updateAuthors` の `makeKey` 重複ロジックを共通関数に統一。

---

## 2026-04-14: 見開きモード3段階制御・書籍メタデータ（作者名）機能追加

### 概要
リーダーの見開き表示制御を拡充し（Auto/Spread/Single の3状態）、書籍単位の作者名管理機能を新設。

### 新機能

#### 1. 見開きモード3段階制御 (SpreadMode)
- `types/index.ts`: `SpreadMode = 'auto' | 'spread' | 'single'` 型を追加。
- `ReaderPanel.tsx`:
    - `spreadMode` + `autoIsSpread` の2状態で制御。実効値 `isSpread` を導出。
    - `cycleSpreadMode()`: Auto → Spread → Single → Auto の順に循環。
    - `handlePageSize(width, height)`: Auto モード時、縦長なら見開き・横長なら1ページに自動切り替え。
    - PDF切り替え時に `autoIsSpread` を `true` にリセット。
- `ReaderHeader.tsx`:
    - 従来の2状態トグルを3状態ボタンに変更（`SPREAD_MODE_CONFIG` 定数でラベル・アイコン・遷移先を管理）。
    - Auto モードは indigo 色で強調表示。
- `PageRenderer.tsx`:
    - `onPageSize?: (width, height) => void` コールバックを追加。
    - PDF: `<Page onRenderSuccess>` / 画像: `<img onLoad>` でページサイズを親に通知。
    - 右ページ/シングルページにのみ渡し、見開き時の二重発火を防止。

#### 2. 書籍メタデータ管理（作者名）
- `backend/routers/meta.py` 新規追加:
    - `GET /api/meta?source=` — meta.json 全体を返す。
    - `PATCH /api/meta` — 複数書籍への作者名一括上書き。ソース別 `threading.Lock` でスレッドセーフ。
    - データ: `backend/data/meta/{source}/meta.json`（キー: `"{path}/{filename}"` or `"{filename}"`）
- `hooks/useBookMeta.ts` 新規追加:
    - source 変更時に自動フェッチ。ローカル即時反映（再フェッチ不要）。
    - `allAuthors`: 全作者名の重複排除・日本語ソート済みリスト（フィルター用）。
- `components/viewer/BulkAuthorDialog.tsx` 新規追加:
    - タグ形式入力（Enter/カンマで確定、Backspace で削除）。
    - 複数書籍を選択して「作者を設定」から呼び出す。上書き方式。
- `LibraryPanel.tsx`: 検索ロジックを拡張。タイトルまたは作者名に部分一致する書籍を絞り込む。
- `LibraryHeader.tsx`: 作者フィルタードロップダウンを追加。全作者をリストアップし、選択中の作者で完全一致フィルター。
- `PdfGrid.tsx`: 書籍カードに作者名タグを表示。
- カード直クリックでPDFリーダーに遷移（BookDetailPanel を削除）。単冊の作者名編集は選択モード（1冊選択）から実施。

---

## 2026-04-14: 新機能追加 & MUI→Tailwind統一

### 概要
UX改善機能を4点実装し、フロントエンドのUIライブラリをMUI→TailwindCSS（Tailwind統一）に移行。

### 新機能

#### 1. PDF生成の非同期化 (Backend + Frontend)
- `backend/routers/pdfs.py`: `JobStore` / `GenerateJob` クラスを新設。UUIDジョブ管理 + スレッドセーフ設計。
- `POST /api/generate` → バックグラウンドスレッドで実行、`job_id` を即返却。
- `GET /api/generate/job/{job_id}` → ジョブ進捗ポーリングエンドポイントを追加。
- フロントエンド (`GeneratorPage.tsx`): 1500msポーリング、進捗UI（Clock/CheckCircle/XCircle アイコン、プログレスバー）。

#### 2. お気に入り / 並び替え
- `useFavorites.ts`: ソース別 `localStorage` 永続化。ソース切り替え時に自動再ロード。
- `useSortedPdfs.ts`: 5種類のソート順に対応 (`name_asc/desc`, `date_asc/desc`, `favorites_first`)。
- `LibraryPanel.tsx` / `LibraryHeader.tsx`: ソート順セレクタ追加 + localStorage永続化。
- `PdfGrid.tsx`: サムネイル左上に★ボタン追加。お気に入り状態をアンバー色で表示。
- `library.py`: `/api/pdfs` レスポンスに `created_at` (Unix timestamp) を追加。

#### 3. ダークモード
- `tailwind.config.js`: `darkMode: 'class'` 設定。
- `index.html`: React マウント前にインラインスクリプトで `<html class="dark">` 適用（フラッシュ防止）。
- `useDarkMode.ts`: `useState` 初期化時に即適用 + `localStorage` 永続化 + システムカラースキームフォールバック。
- 全コンポーネントに `dark:` クラスを適用。`Layout.tsx` にMoon/Sunトグルボタン追加。

#### 4. サムネイル遅延読み込み (Lazy Load)
- `LazyThumbnail.tsx`: `IntersectionObserver` (rootMargin 200px) でビューポート外のサムネイルを遅延読み込み。エラー時はFileTextアイコンにフォールバック。

#### 5. PDF内テキスト検索
- `PdfSearchBar.tsx`: Ctrl+F でサーチバーを開く。300msデバウンス入力、Enter/Shift+Enterで次/前移動、マッチ数表示。
- `ReaderPanel.tsx`: pdfjs で全ページテキストを走査してマッチカウント、`customTextRenderer` でハイライト表示。`renderTextLayer` は検索中のみ有効化（パフォーマンス最適化）。

### MUI→TailwindCSS統一
- `OCRPanel.tsx` / `OCRPage.tsx`: MUI `Box` / `Typography` / `Button` / `Chip` → Tailwindクラスに完全置き換え。`ThemeProvider` を削除。
- `CreateFolderDialog.tsx`: MUI `Dialog` → Tailwind固定オーバーレイ実装。バリデーション・Escクローズ・Enterサブミット・外クリックで閉じる。
- `package.json`: `@mui/material`, `@mui/icons-material`, `@emotion/react`, `@emotion/styled` を削除（約123パッケージ削減）。

---

## 2026-04-13: OCR品質改善

### 概要
縦書き小説OCR（yomitoku）の品質を改善。フリガナ除去・テキスト断片化・Searchable PDF生成の3点を修正。

### 主な変更

| ファイル | 内容 |
|---|---|
| `D:\61.tool\common\ocr\ocr_engine.py` | `_process_words` 実データ対応・aspect比判定追加 |
| `D:\61.tool\common\ocr\ocr_engine.py` | `filter_ruby_text` 自動閾値化（histogram valley検出） |
| `D:\61.tool\common\ocr\ocr_engine.py` | `_merge_text_fragments` Xビン分割方式に変更 |
| `D:\61.tool\common\ocr\ocr_engine.py` | `normalize_text` 追加（3点リーダー・特殊記号の正規化） |
| `kindle-pdf/searchable_pdf.py` | 縦書きテキストを1文字ずつ配置する方式に変更（長列の切れ問題を修正） |
| `D:\61.tool\common\ocr\debug_yomitoku.py` | yomitoku構造診断ツール（新規） |

詳細: [OCR設計書.md](../03_詳細設計/OCR設計書.md)

---

## 2026-04-12: リファクタリング フェーズ4（品質向上・将来対応）

### 概要
テスト基盤整備・ErrorBoundary・フォルダ作成Dialog・環境変数整備。

### 主な変更
- `backend/tests/` ディレクトリ作成、pytest基盤整備
- `frontend/src/components/ErrorBoundary.tsx` 追加（アプリ全体をラップ）
- フォルダ作成UIを `window.prompt()` から MUI `Dialog` に変更
- `.env.example` 作成、`python-dotenv` 導入

---

## 2026-03-08: リファクタリング フェーズ1〜3

### フェーズ1: セキュリティ・安定性の修正 (Critical)
- `backend/utils/path_utils.py` 新規追加（パスバリデーション一元化）
- `GenerateState` / `OCRService` に `threading.Lock` 追加（スレッドセーフ化）
- `execute_moves()` にバックアップ→操作→リストアパターンを実装

### フェーズ2: 高優先度リファクタリング (High)
- `ViewerPage.tsx` をコンポーネント分割（`LibraryPanel` / `ReaderPanel` / `EditPanel`）
- `usePolling()` 共通フック作成（`usePdfStatus` / `useOcrStatus` の重複排除）
- `api_client.ts` にエラー種別定義・タイムアウト設定追加
- `pdf_generator.py` の重複ロジックを `_process_images()` に統合

### フェーズ3: コード品質向上 (Medium)
- TypeScript `any` 型を具体的な型に置き換え
- Pythonバックエンドに型ヒントを追加
- `print()` を `logging` モジュールに置き換え
- `config.py` のディレクトリ生成をループで統一

---

## 2026-01-12: Novel OCR 機能追加

### 概要
Kindle小説のSearchable PDF生成フローを確立。

### 主な変更
- `kindle-pdf/batch_ocr.py` 新規作成（バッチOCRスクリプト）
- `kindle-pdf/searchable_pdf.py` 新規作成（ReportLab透明テキストPDF生成）
- `frontend/src/pages/OCRPage.tsx` 追加（Web UIからOCR実行・監視）
- `backend/routers/ocr.py` 追加（OCR API）
- `backend/services/ocr_service.py` 追加（OCRプロセス管理）
- `kindle-pdf/main_novel.py` から重複PDF生成処理を削除
